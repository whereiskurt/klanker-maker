# Build Complete

This task demonstrates **bold text**, _italic text_, and a [clickable link](https://example.com) that renders natively in Slack.

The second paragraph shows inline `code` and confirms that GFM is passed through verbatim without mrkdwn conversion.
