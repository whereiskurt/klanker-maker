---
milestone: v1.0
audit_scope: "Phases 67-76 (Slack cluster + VS Code Remote-SSH)"
audited: 2026-05-10
revised: 2026-05-10
status: gaps_found
counts:
  clean: 4
  verification_debt: 2
  unstarted: 5
revision_note: "Initial audit claimed Phase 74 Plan 74-02 was unexecuted. Retroactive verification (74-VERIFICATION.md) found all 74-02 deliverables are committed and tested — pkg/slack/blocks.go (329 lines), bridge BlockPoster wiring, and _km_stream_drain --render=blocks default are all in place. The audit was misled by the absent 74-02-SUMMARY.md file (a docs gap, not a code gap). Phase 74 reclassified to clean_phases."
verification_debt:
  - phase: 67
    objective: "Slack inbound — per-sandbox channel as bidirectional chat with km agent run dispatch. Closes the loop on Phase 63: Slack messages in #sb-{id} channels become Claude turns inside the sandbox via SQS FIFO → systemd poller → km agent run --resume."
    plans_completed: 13
    plan_range: "67-00 through 67-12"
    deferred_items:
      - "Pre-existing test failure: TestUserDataNotifyEnv_BridgeURLNeverEmittedAtCompileTime — Phase 67 cloud-init SSM-fetch block emits KM_SLACK_BRIDGE_URL into userdata template; test's substring check doesn't distinguish compile-time vs runtime-fetch context. Logged in deferred-items.md with three resolution options (recommended: narrow assertion to KM_NOTIFY_ENV_EOF block)."
      - "UAT Steps 15 (invite gate) and 16 (signing-secret mismatch) deferred to future UAT cycle — compensating unit test coverage documented."
      - "Slack signing-secret rotation negative test deferred — exercising it creates ~15min Events delivery outage during Lambda cache TTL."
      - "Silence-on-failure trade-off: agent failure produces no fallback Slack message; operator diagnoses via journalctl + km agent list. Documented in 67-UAT.md."
  - phase: 68
    objective: "Slack transcript streaming — per-turn assistant text + tool one-liners posted to Slack thread (PostToolUse hook), gzipped JSONL uploaded as Slack file at Stop. Opt-in via spec.cli.notifySlackTranscriptEnabled."
    plans_completed: 13
    plan_range: "68-00 through 68-12"
    deferred_items:
      - "BLOCKER (Phase 68.1 candidate): km agent run (claude -p non-interactive) does NOT fire PostToolUse hooks — transcript streaming silently no-ops for agent run. Only km shell (interactive) works. --bare flag removed as partial fix but root cause is Claude Code platform behavior."
      - "BLOCKER (Phase 68.1 candidate): files.completeUploadExternal returns internal_error for Slack Connect externally-shared channels (is_ext_shared: true). Per-turn chat lines and auto-thread work; only .jsonl.gz attachment fails. Recommended fix: channel-type detection + dual-path upload (option 3 in deferred-items.md)."
      - "UX issue (Phase 68.1 candidate): subagent fan-out (Task-tool, /clone) creates one Slack thread per session_id instead of one per operator turn. Operators see N top-level 'turn started' messages. Fix requires KM_OPERATOR_TURN_ID injection or detect-and-reuse logic."
      - "printTranscriptWarning wired into runCreate (--local path) but NOT into runCreateRemote (--remote default for EC2). Most operators never see the audience-containment warning."
      - "16 pre-existing test failures across pkg/compiler, cmd/km-slack, internal/app/cmd logged in deferred-items.md (bucket/state-check tests, environment-dependent). Not caused by Phase 68."
      - "Stale test TestKmSlackPost_BridgeReturns503ThenSuccess_Exit0 fails by design: test expects 5xx retry but PostToBridge fail-fasts on 5xx to prevent nonce-replay. Needs either test fix or nonce-lifecycle changes."
  # Phase 74 reclassified to clean_phases after retroactive verification — see revision_note.
unstarted:
  - phase: 69
    title: "AWS API SCP-style allow/deny via SigV4 inspection"
    note: "11 PLAN.md files + VALIDATION.md present (plan-stage complete); no SUMMARY files — never executed."
  - phase: 70
    title: "Codex parity for operator-notify, Slack-notify, and Slack inbound dispatcher"
    note: "Only SPEC.md present; no PLAN or SUMMARY files — not started."
  - phase: 71
    title: "Agent playbook orchestration — multi-step prompts with session continuity via cron and manual triggers"
    note: "11 PLAN.md files + VALIDATION.md present (plan-stage complete); no SUMMARY files — never executed."
  - phase: 72
    title: "Slack corporate workspace support with auto-detect, invite, and manifest generator"
    note: "10 PLAN.md files + VALIDATION.md present (plan-stage complete); no SUMMARY files — never executed."
  - phase: 75
    title: "Slack inbound file attachments — images, PDFs for per-sandbox channels"
    note: "Directory contains only .gitkeep — not started, no plans."
clean_phases:
  - phase: 67.1
    verification_status: passed
    verified: 2026-05-03T20:00:00Z
    score: "5/5 UAT criteria"
    notes: "Bridge Lambda adds configurable 👀 ACK reaction after successful SQS enqueue. Fire-and-forget goroutine; reactions:write scope enforced by km slack init and km doctor. UAT-1 human-approved ('it's working with eyes ball on the correct items and never on the bots.'). 10 new tests, 0 anti-patterns."
  - phase: 73
    verification_status: passed
    verified: 2026-05-07T00:00:00Z
    score: "9/9 must-haves"
    notes: "km vscode start/status + per-sandbox ed25519 keypair at create/destroy. Full live UAT by operator KPH on 2026-05-08 against sandbox lrn2-ee9499b5 (6 scenarios). 4 mid-phase fixes committed (remote path keypair, KM_VSCODE_SSH_PUBKEY, ResolveSandboxID, pre-bind port probe). 28 tests across 5 packages. Pre-existing pkg/compiler test failures (4) predate Phase 73; documented in deferred-items.md."
  - phase: 76
    verification_status: passed
    verified: 2026-05-10T02:16:14Z
    score: "14/14 must-haves"
    notes: "km vscode rekey <sandbox-id> — 7-step rotation flow: EC2 running gate, lock check (--force bypass), SSM probe, confirmation prompt (--yes bypass), scratch-path keygen, SSM install + byte-for-byte readback verify, atomic .pub-before-priv rename. 16 unit tests; cross-laptop bootstrap path covered. Live UAT by KPH on 2026-05-10 against L1 sandbox confirmed real-sandbox round-trip works."
  - phase: 74
    verification_status: passed
    verified: 2026-05-10T17:45:00Z
    score: "14/14 must-haves"
    notes: "Tokenizer-based markdown→Slack mrkdwn (Tier 1) AND Block Kit (Tier 2) BOTH implemented. pkg/slack/mrkdwn.go + pkg/slack/blocks.go (329 lines), 16 REND + 10 BLK + 3 BRDG + 1 HOOK tests all pass. _km_stream_drain default is --render=blocks. Initial audit error: SUMMARY file for 74-02 missing on disk but git history + code + tests confirm it is fully shipped. Live Slack render verification (2 items) remains outstanding."
---

# Milestone v1.0 Audit — Phases 67–76 (Slack Cluster + VS Code Remote-SSH)

**Audited:** 2026-05-10
**Scope:** 11 phase directories (67, 67.1, 68, 69, 70, 71, 72, 73, 74, 75, 76)
**Status:** gaps_found — two shipped features have known functional limitations without VERIFICATION.md

---

## Clean Phases (4)

| Phase | One-line Summary | Verification | Notes |
|-------|-----------------|-------------|-------|
| 67.1 | ACK 👀 reaction on Slack inbound enqueue | passed 5/5 | Human UAT approved; 0 anti-patterns |
| 73 | km vscode start/status — VS Code Remote-SSH over SSM | passed 9/9 | Live UAT by KPH; 28 unit tests; 4 pre-existing compiler failures logged |
| 74 | Slack mrkdwn renderer + Block Kit tier (Tier 1 + Tier 2) | passed 14/14 | All BLK/BRDG/HOOK tests pass; --render=blocks is the default. Initial audit error corrected by 74-VERIFICATION.md. |
| 76 | km vscode rekey — rotate ed25519 keypair in-place | passed 14/14 | 16 unit tests; live UAT confirmed real-sandbox round-trip on 2026-05-10 |

---

## Shipped Without Formal Verification (now resolved — 2 still need VERIFICATION.md)

Retroactive verifications were spawned 2026-05-10. Results:

| Phase | One-line Summary | Plans | VERIFICATION Status | Notes |
|-------|-----------------|-------|---------------------|-------|
| 67 | Slack inbound bidirectional chat via SQS + km agent run | 13/13 | `human_needed` 8/8 | Code clean, all 8 REQ-SLACK-IN-* implemented + tested. Only post-fix UAT operator sign-off (Gap A + Gap B re-test) is pending. REQUIREMENTS.md table marks 6/8 as "Planned" — stale, needs update to "Complete". |
| 68 | Transcript streaming — per-turn Slack chat + gzipped JSONL upload | 13/13 | `gaps_found` 8/12 | Confirmed 4 real gaps: **(1) `km agent run` (claude -p) skips PostToolUse hooks → streaming no-ops**; **(2) `files.completeUploadExternal` fails on Slack Connect channels**; **(3) subagent fan-out creates N thread parents**; **(4) `printTranscriptWarning` missing from `--remote` path**. Verifier added concrete fix paths for each — Phase 68.1 candidates. |

---

## Unstarted — Descoped or Deferred to v2.0 (5)

| Phase | Title | State |
|-------|-------|-------|
| 69 | AWS API SCP-style allow/deny via SigV4 inspection | Plan-stage: 11 PLANs written, VALIDATION.md present; no execution |
| 70 | Codex parity for operator-notify, Slack-notify, inbound dispatcher | Not started: SPEC.md only |
| 71 | Agent playbook orchestration — multi-step prompts via cron | Plan-stage: 11 PLANs written, VALIDATION.md present; no execution |
| 72 | Slack corporate workspace support with auto-detect + manifest generator | Plan-stage: 10 PLANs written, VALIDATION.md present; no execution |
| 75 | Slack inbound file attachments — images, PDFs | Not started: .gitkeep only |

---

## Cross-Phase Tech Debt

The following items span multiple phases and should be addressed in a single cleanup plan:

| Item | Affected Phases | Severity |
|------|----------------|----------|
| `TestUserDataNotifyEnv_BridgeURLNeverEmittedAtCompileTime` failing (pre-existing, per Phase 67 cloud-init pattern change) | 67, 68, 73 | Low — test logic stale, not a code bug |
| `TestUserDataNotifyEnv_NoChannelOverride_NoChannelID` failing (same root area) | 67, 68, 73 | Low |
| `TestKmSlackPost_BridgeReturns503ThenSuccess_Exit0` failing (test expects 5xx retry; PostToBridge intentionally fail-fasts) | 68, 74 | Low — fix test expectation |
| 16 additional pre-existing failures across `pkg/compiler`, `cmd/km-slack`, `internal/app/cmd` (state-bucket/environment gates) | 68 | Low — environment-dependent |

---

## Milestone-Level Findings (revised 2026-05-10 after retroactive verifications)

1. **Phase 67 is verification-clean.** Retroactive `67-VERIFICATION.md` shows 8/8 must-haves implemented, all REQ-SLACK-IN-* IDs wired and tested. Status `human_needed` — only the live-sandbox post-fix UAT re-test is outstanding (Gap A + Gap B fixes verified by tests but not operator-signed). REQUIREMENTS.md status table is stale: 6/8 REQ-SLACK-IN-* still marked "Planned" — needs update to "Complete".

2. **Phase 68 has 4 confirmed gaps requiring a Phase 68.1.** Retroactive `68-VERIFICATION.md` (status: gaps_found, 8/12) confirms each gap and adds concrete fix paths:
   - **`km agent run` streaming no-op** — fix path: post-process transcript from the wrapper after `claude -p` exits (no hook dependency)
   - **Slack Connect upload rejection** — fix path: `conversations.info` probe at `km create` + channel-type metadata + bridge dual-path upload
   - **Subagent fan-out N-thread issue** — fix path: inject `KM_OPERATOR_TURN_ID` into Claude env; key thread cache by it
   - **`printTranscriptWarning` `--remote` gap** — fix path: wire warning into `runCreateRemote` before EventBridge dispatch, or add to SES completion email

3. **Phase 74 was incorrectly flagged.** Initial audit said Plan 74-02 (Block Kit) was unexecuted because no `74-02-SUMMARY.md` existed on disk. Retroactive `74-VERIFICATION.md` (status: passed, 14/14) shows all 74-02 deliverables ARE committed: `pkg/slack/blocks.go` (329 lines), `BlockPoster` interface, `SlackPosterAdapter.PostMessageBlocks`, `_km_stream_drain --render "${KM_SLACK_RENDER:-blocks}"`. All 30+ Phase 74 tests pass. The phase is fully shipped; only the SUMMARY file is missing (docs gap, not code gap).

4. **Pre-existing test suite debt (cross-phase):** At least 19 failing tests across `pkg/compiler`, `cmd/km-slack`, `internal/app/cmd` were logged as pre-existing during Phases 67–68 execution. None attributed to Phases 67–76. Should be triaged in a dedicated cleanup plan.

## Recommended Next Actions

1. **Phase 68.1 cleanup plan** — addresses the 4 real functional gaps (highest-value follow-up)
2. **Live UAT for Phase 67** — re-run Gap A + Gap B post-fix tests, sign off `67-UAT.md`
3. **REQUIREMENTS.md status sync** — flip 6 REQ-SLACK-IN-* IDs from "Planned" to "Complete"
4. **Backfill 74-02-SUMMARY.md** — minor; the code is shipped, only the doc is missing
5. **Decide on unstarted phases (69, 70, 71, 72, 75)** — confirm v2.0 backlog vs reactivate

---

_Audited: 2026-05-10_
_Auditor: Claude (gsd-verifier, ad-hoc scope)_
