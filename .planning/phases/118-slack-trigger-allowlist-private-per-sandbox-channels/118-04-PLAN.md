---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 04
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - internal/app/config/config.go
  - internal/app/cmd/init.go
  - infra/modules/lambda-slack-bridge/v1.0.0/main.tf
  - infra/live/use1/lambda-slack-bridge/terragrunt.hcl
  - cmd/km-slack-bridge/main.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "slack.allow in km-config.yaml is loaded into SlackConfig.Allow (NOT silently dropped — merge-list entry present)"
    - "km init exports KM_SLACK_ALLOW (comma-joined) for the slack bridge Lambda env block"
    - "lambda-slack-bridge TF module declares slack_allow var + KM_SLACK_ALLOW env entry"
    - "Bridge main.go reads KM_SLACK_ALLOW into EventsHandler.Allow at cold-start"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "SlackConfig.Allow field + merge-list entry + loader"
      contains: "slack.allow"
    - path: "infra/modules/lambda-slack-bridge/v1.0.0/main.tf"
      provides: "slack_allow variable + KM_SLACK_ALLOW env"
      contains: "KM_SLACK_ALLOW"
    - path: "cmd/km-slack-bridge/main.go"
      provides: "KM_SLACK_ALLOW → EventsHandler.Allow wiring"
      contains: "KM_SLACK_ALLOW"
  key_links:
    - from: "internal/app/config/config.go merge-list (~line 832)"
      to: "cfg.Slack.Allow"
      via: "\"slack.allow\" in v2→v merge-list + v.GetStringSlice"
      pattern: "\"slack.allow\""
    - from: "cmd/km-slack-bridge/main.go"
      to: "EventsHandler.Allow"
      via: "os.Getenv(KM_SLACK_ALLOW) split on comma"
      pattern: "KM_SLACK_ALLOW"
---

<objective>
Feature B — install-level wiring. Carry `slack.allow` from `km-config.yaml` through the config loader (including the v2→v merge-list footgun), the `KM_SLACK_ALLOW` env export in `km init`, the `lambda-slack-bridge` Terraform module/terragrunt input, and into `EventsHandler.Allow` at bridge cold-start.

Purpose: Provide the install-level allowlist that Plan 05's enforcement gate consumes via `h.Allow`. This is the full deploy surface for the install-level path (`km init --dry-run=false`, NOT `--sidecars`).
Output: A non-empty `slack.allow` reaches the bridge as `EventsHandler.Allow`. No new AWS resources, no TF module version bump.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-RESEARCH.md

<interfaces>
<!-- EventsHandler.Allow []string field already added in Plan 01. -->

Config merge-list (internal/app/config/config.go ~lines 824-835) — the project_config_key_merge_list
footgun. Existing entries: "slack.mention_only", "slack.peer_bridges", "slack.default_router".
ADD "slack.allow" to that slice or the YAML value is silently ignored.

SlackConfig struct (config.go:24-67) — add:
```go
Allow []string `mapstructure:"allow" yaml:"allow,omitempty"`
```
Loader (config.go ~line 952, after slack.peer_bridges / slack.default_router blocks):
```go
if v.IsSet("slack.allow") { cfg.Slack.Allow = v.GetStringSlice("slack.allow") }
```

km init env export precedent — KM_SLACK_PEER_BRIDGES (init.go:1619-1626): []string → comma-joined,
env-wins drift WARN. Replicate verbatim for KM_SLACK_ALLOW (init.go ~line 1642, after default_router).

TF module (infra/modules/lambda-slack-bridge/v1.0.0/main.tf) — siblings: slack_mention_only,
slack_react_always, slack_peer_bridges, slack_default_router. Add `variable "slack_allow"` (string,
default "") + `KM_SLACK_ALLOW = var.slack_allow` in the env block (~line 375). Edit IN PLACE at
v1.0.0 (additive default="" var — NO version bump).

terragrunt.hcl (infra/live/use1/lambda-slack-bridge/terragrunt.hcl ~line 121):
`slack_allow = get_env("KM_SLACK_ALLOW", "")`.

Bridge main.go (cmd/km-slack-bridge/main.go ~line 385-408, WireMentionOnly) — add:
```go
if raw := os.Getenv("KM_SLACK_ALLOW"); raw != "" {
    h.Allow = strings.Split(raw, ",")
    slog.Info("km-slack-bridge: install-level allowlist configured", "count", len(h.Allow))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SlackConfig.Allow — struct + merge-list + loader</name>
  <files>internal/app/config/config.go</files>
  <behavior>
    Config test: a km-config.yaml with `slack: {allow: [U0OPERATOR, U0XUSER]}` loads into cfg.Slack.Allow == ["U0OPERATOR","U0XUSER"]. CRITICAL regression guard: absent the merge-list entry the value is dropped — assert it is NOT dropped.
  </behavior>
  <action>
    1. Add `Allow []string` (mapstructure:"allow" yaml:"allow,omitempty") to SlackConfig (config.go:24-67), doc-comment per RESEARCH.md Layer 6.
    2. Add `"slack.allow"` to the v2→v merge-list slice (~line 832-835, alongside "slack.peer_bridges"/"slack.default_router"). This is the footgun — without it the field is silently ignored.
    3. Add the loader block after the slack.default_router block (~line 962): `if v.IsSet("slack.allow") { cfg.Slack.Allow = v.GetStringSlice("slack.allow") }`.
    Add/extend a config_test.go case (mirror an existing slack.peer_bridges load test) asserting the slice loads correctly from a v2-merged config — this is the test that catches the merge-list omission.
  </action>
  <verify>
    <automated>go test ./internal/app/config/... -run 'Slack|Allow|Config' -count=1 -timeout 120s 2>&1 | tail -25</automated>
  </verify>
  <done>slack.allow loads into cfg.Slack.Allow via the merge-list + loader. A test guards against the silent-drop footgun.</done>
</task>

<task type="auto">
  <name>Task 2: km init exports KM_SLACK_ALLOW (env-wins drift WARN)</name>
  <files>internal/app/cmd/init.go</files>
  <action>
    After the slack.default_router export block (init.go ~line 1642), add the KM_SLACK_ALLOW export, copying the KM_SLACK_PEER_BRIDGES pattern verbatim (init.go:1619-1626):
    - Only when `len(cfg.Slack.Allow) > 0`: join comma; if env var already set to a DIFFERENT value → fmt.Fprintf stderr drift WARN; else if env empty → os.Setenv("KM_SLACK_ALLOW", joined).
    Confirm `strings`/`os`/`fmt` already imported in init.go (they are).
    If init.go has an env-export unit test table, add a KM_SLACK_ALLOW row (mirror KM_SLACK_PEER_BRIDGES). If not unit-tested there, the verify below is a build + the broader cmd suite.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... 2>&1 | tail -10 && go test ./internal/app/cmd/... -run 'Init|SlackAllow|EnvExport|PeerBridges' -count=1 -timeout 300s 2>&1 | tail -25 ; true</automated>
  </verify>
  <done>km init exports KM_SLACK_ALLOW comma-joined when slack.allow is non-empty, with the env-wins drift WARN. Empty list → no export.</done>
</task>

<task type="auto">
  <name>Task 3: TF module + terragrunt input + bridge main.go wiring</name>
  <files>infra/modules/lambda-slack-bridge/v1.0.0/main.tf, infra/live/use1/lambda-slack-bridge/terragrunt.hcl, cmd/km-slack-bridge/main.go</files>
  <action>
    1. infra/modules/lambda-slack-bridge/v1.0.0/main.tf — add `variable "slack_allow" { type = string; default = "" ; description = "Phase 118: install-level Slack trigger allowlist (comma-joined Uxxxx). Empty = everyone allowed." }`. In the Lambda environment block (~line 375, alongside KM_SLACK_DEFAULT_ROUTER etc.) add `KM_SLACK_ALLOW = var.slack_allow`. Edit in place at v1.0.0 (additive default — no version bump).
    2. infra/live/use1/lambda-slack-bridge/terragrunt.hcl — add input (~line 121, alongside slack_default_router) `slack_allow = get_env("KM_SLACK_ALLOW", "")`.
    3. cmd/km-slack-bridge/main.go — in WireMentionOnly (~line 385-408), add the KM_SLACK_ALLOW read: when non-empty, `h.Allow = strings.Split(raw, ",")` + an slog.Info with count. Confirm `strings`/`os`/`slog` imported.
    Add/extend a cmd/km-slack-bridge test (mirror the WireMentionOnly env test): KM_SLACK_ALLOW="U0OPERATOR,U0XUSER" → h.Allow == 2 elements; unset → h.Allow nil/empty.
    Run `terraform fmt -check` on the module + `terragrunt hclfmt --check` if available; at minimum `terraform validate` the module is out-of-scope (needs init); rely on `terraform fmt`.
  </action>
  <verify>
    <automated>go build ./cmd/km-slack-bridge/... 2>&1 | tail -10 && go test ./cmd/km-slack-bridge/... -run 'Wire|Allow|MentionOnly' -count=1 -timeout 120s 2>&1 | tail -25 && terraform fmt -check infra/modules/lambda-slack-bridge/v1.0.0/ 2>&1 | tail -10 ; true</automated>
  </verify>
  <done>TF module declares slack_allow + KM_SLACK_ALLOW env; terragrunt.hcl forwards get_env. Bridge main.go wires KM_SLACK_ALLOW → EventsHandler.Allow. Test confirms the split.</done>
</task>

</tasks>

<verification>
- AC2/AC4 install-level source: a non-empty slack.allow reaches EventsHandler.Allow (config → KM_SLACK_ALLOW → main.go → h.Allow). The enforcement that USES h.Allow is Plan 05.
- Merge-list regression guard test present (the silent-drop footgun).
- `go build ./...` clean; `terraform fmt -check` clean on the edited module.
- No TF module version bump; no new AWS resource.
</verification>

<success_criteria>
The install-level allowlist deploy surface is complete: `slack.allow` → `KM_SLACK_ALLOW` (via `km init --dry-run=false`) → `EventsHandler.Allow`. Plan 05 consumes `h.Allow` for enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-04-SUMMARY.md`. Note the deploy surface (km init --dry-run=false, NOT --sidecars).
</output>
