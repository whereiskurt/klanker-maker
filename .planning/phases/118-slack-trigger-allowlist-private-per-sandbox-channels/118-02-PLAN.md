---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - pkg/slack/client.go
  - internal/app/cmd/create_slack.go
  - internal/app/cmd/slack.go
  - pkg/profile/schemas/sandbox_profile.schema.json
  - pkg/profile/validate.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "km create with perSandbox:true + private:true calls CreateChannel with is_private:true"
    - "km slack init / rotate (slack.go path) still creates public channels (passes private=false)"
    - "km validate WARNS (not errors) when private:true or inbound.allow set with perSandbox not true"
    - "private:false (default) is byte-identical to pre-118 (public channel)"
  artifacts:
    - path: "pkg/slack/client.go"
      provides: "CreateChannel(ctx, name, private) — is_private now conditional"
      contains: "is_private"
    - path: "pkg/profile/validate.go"
      provides: "Rule S-private + S-allow warnings"
      contains: "perSandbox"
    - path: "pkg/profile/schemas/sandbox_profile.schema.json"
      provides: "private + inbound.allow schema entries"
      contains: "private"
  key_links:
    - from: "internal/app/cmd/create_slack.go:464"
      to: "pkg/slack.Client.CreateChannel"
      via: "passes sl.Private"
      pattern: "CreateChannel\\(ctx, channelName, "
---

<objective>
Feature A — private per-sandbox channel. Thread a `private bool` through `CreateChannel` so that `notification.slack.private:true` + `perSandbox:true` creates the channel as `is_private:true`. Add the JSON-schema entries for `private` and `inbound.allow`, and the `km validate` warn rules (covering AC6 for BOTH Feature A's `private` and Feature B's per-sandbox `allow`).

Purpose: Make AC1 and AC6 GREEN. This plan also makes internal/app/cmd compile again (Plan 01 left it red by updating mocks ahead of the real signature change).
Output: AC1 wired end-to-end (mock-level), AC6 warn rules live, schema updated.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-RESEARCH.md

<interfaces>
<!-- Stub fields already added in Plan 01: -->
<!--   NotificationSlackSpec.Private bool ; NotificationSlackInboundSpec.Allow []string -->

CreateChannel current (pkg/slack/client.go:603-612):
```go
func (c *Client) CreateChannel(ctx context.Context, name string) (string, error) {
    resp, err := c.callJSON(ctx, "conversations.create", map[string]any{
        "name":       name,
        "is_private": false,   // line 606 — make conditional on `private`
    })
}
```
Local interfaces that re-declare CreateChannel (must update signature):
  - internal/app/cmd/create_slack.go:87   SlackAPI       (create path — pass sl.Private)
  - internal/app/cmd/slack.go:54          SlackInitAPI   (init/rotate path — pass false)
Call sites:
  - internal/app/cmd/create_slack.go:464  chID, _ := api.CreateChannel(ctx, channelName)   → add sl.Private
  - internal/app/cmd/slack.go:289         newID, _ := api.CreateChannel(ctx, chName)        → add false

`sl` at create_slack.go:464 is *profile.NotificationSlackSpec (result of notificationSlack(p)); sl.Private is the new field.

Existing validate warn pattern (pkg/profile/validate.go:334-341, Rule S3):
```go
if slack.ArchiveOnDestroy != nil && !perSandbox {
    errs = append(errs, ValidationError{
        Path: "spec.notification.slack.archiveOnDestroy",
        Message: "...only meaningful when notification.slack.perSandbox: true",
        IsWarning: true,
    })
}
```
`perSandbox`, `slack`, and the inbound block are already in scope in the validate Slack block (validate.go:306+). Get inbound via `slack.Inbound`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread private bool through CreateChannel (client + both interfaces + both call sites)</name>
  <files>pkg/slack/client.go, internal/app/cmd/create_slack.go, internal/app/cmd/slack.go</files>
  <behavior>
    - pkg/slack/client_test.go TestClient_CreateChannel_ReturnsID still passes after the signature change (update its call to pass a bool).
    - create_slack_test.go (Plan 01 added createPrivate capture): a per-sandbox create with Private:true sets fakeSlackAPI.createPrivate==true; default Private:false sets it false.
  </behavior>
  <action>
    1. pkg/slack/client.go:603 — change signature to `CreateChannel(ctx context.Context, name string, private bool)` and set `"is_private": private` at line 606. Update the doc-comment.
    2. internal/app/cmd/create_slack.go:87 — update SlackAPI interface signature to `CreateChannel(ctx context.Context, name string, private bool) (string, error)`.
    3. internal/app/cmd/create_slack.go:464 — change call to `api.CreateChannel(ctx, channelName, sl.Private)`. (`sl` is the *profile.NotificationSlackSpec already in scope.)
    4. internal/app/cmd/slack.go:54 — update SlackInitAPI interface signature to match.
    5. internal/app/cmd/slack.go:289 — change call to `api.CreateChannel(ctx, chName, false)` (init/rotate always public — comment WHY: km slack init creates the shared workspace channel, not a per-sandbox one).
    6. pkg/slack/client_test.go:205 — update the test's CreateChannel call to pass `false`.
    Confirm no other concrete impls of these interfaces exist (grep `CreateChannel(` across the repo; the 6 mocks were already updated in Plan 01).
  </action>
  <verify>
    <automated>go build ./... 2>&1 | tail -20 ; go test ./pkg/slack/... ./internal/app/cmd/... -run 'CreateChannel|TestResolveSlackChannel' -count=1 -timeout 180s 2>&1 | tail -40</automated>
  </verify>
  <done>Whole repo compiles (Plan 01's mocks now match). is_private is conditional on the new param. Init/rotate pass false; create path passes sl.Private. AC1 mock-level test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validate warn rules (private + allow) and JSON-schema entries</name>
  <files>pkg/profile/validate.go, pkg/profile/schemas/sandbox_profile.schema.json</files>
  <behavior>
    TestValidate_Slack (Plan 01 added these RED cases) goes GREEN:
    - private:true + !perSandbox → one warning at spec.notification.slack.private.
    - inbound.allow non-empty + !perSandbox → one warning at spec.notification.slack.inbound.allow.
    - either + perSandbox:true → no warning.
    Schema accepts private (bool) and inbound.allow ([]string); rejects wrong types.
  </behavior>
  <action>
    1. pkg/profile/validate.go — in the Slack validate block (after Rule S3, ~line 341), add:
       - Rule (warning): `if slack.Private && !perSandbox` → ValidationError{Path:"spec.notification.slack.private", Message:"notification.slack.private: true has no effect when notification.slack.perSandbox is not true (no per-sandbox channel is created)", IsWarning:true}.
       - Rule (warning): `if slack.Inbound != nil && len(slack.Inbound.Allow) > 0 && !perSandbox` → ValidationError{Path:"spec.notification.slack.inbound.allow", Message:"notification.slack.inbound.allow has no effect when notification.slack.perSandbox is not true (per-sandbox allow requires an inbound queue)", IsWarning:true}.
       Match the existing IsWarning warn-message style.
    2. pkg/profile/schemas/sandbox_profile.schema.json — in the notification.slack object (~line 721) add `"private": {"type":"boolean"}`. In the notification.slack.inbound object (~line 744) add `"allow": {"type":"array","items":{"type":"string"}}`. Follow the existing mentionOnly/reactAlways entries. Do NOT mark either required.
    Run scripts/validate-all-profiles.sh to confirm no existing profile regresses (the 20-file inventory).
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run TestValidate_Slack -count=1 -timeout 120s 2>&1 | tail -20 && bash scripts/validate-all-profiles.sh 2>&1 | tail -15</automated>
  </verify>
  <done>Both warn rules fire only when perSandbox is not true; both are warnings, not errors. Schema validates private + inbound.allow. All 20 inventory profiles still pass. AC6 GREEN.</done>
</task>

</tasks>

<verification>
- `go build ./...` clean (repo compiles end to end again).
- AC1: km create passes sl.Private to CreateChannel (mock captures it); init/rotate pass false.
- AC6: `go test ./pkg/profile/... -run TestValidate_Slack` GREEN.
- AC8 (partial): private:false default → is_private:false (byte-identical public).
- scripts/validate-all-profiles.sh green.
</verification>

<success_criteria>
Feature A is functionally complete at the mock level (AC1), the validate warns (AC6) are live for both Feature A and Feature B fields, and the JSON schema accepts the new fields. No apiVersion bump.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-02-SUMMARY.md`.
</output>
