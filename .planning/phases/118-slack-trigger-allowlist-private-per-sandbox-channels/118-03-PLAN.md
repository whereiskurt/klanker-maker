---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 03
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - pkg/aws/sandbox_dynamo.go
  - pkg/slack/bridge/aws_adapters.go
  - pkg/slack/bridge/aws_adapters_test.go
  - internal/app/cmd/create_slack_inbound.go
  - internal/app/cmd/create_slack_inbound_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Per-sandbox notification.slack.inbound.allow is written to the km-sandboxes row as comma-joined S attribute slack_allow at km create (only when inbound.Enabled)"
    - "slack_allow round-trips through SandboxMetadata marshal/unmarshal (survives pause/resume/extend full-row PutItem)"
    - "Bridge FetchByChannel reads slack_allow and populates SandboxRoutingInfo.Allow"
    - "Empty/absent per-sandbox allow leaves the row attribute absent (fall-back signal)"
  artifacts:
    - path: "pkg/aws/sandbox_dynamo.go"
      provides: "slack_allow marshal (S, comma-joined) + unmarshal (split)"
      contains: "slack_allow"
    - path: "pkg/slack/bridge/aws_adapters.go"
      provides: "FetchByChannel reads slack_allow → SandboxRoutingInfo.Allow"
      contains: "slack_allow"
    - path: "internal/app/cmd/create_slack_inbound.go"
      provides: "UpdateSandboxAttr slack_allow write at km create"
      contains: "slack_allow"
  key_links:
    - from: "internal/app/cmd/create_slack_inbound.go"
      to: "km-sandboxes DDB row (slack_allow)"
      via: "deps.UpdateSandboxAttr(ctx, SandboxID, \"slack_allow\", comma-joined)"
      pattern: "UpdateSandboxAttr.*slack_allow"
    - from: "pkg/slack/bridge/aws_adapters.go FetchByChannel"
      to: "SandboxRoutingInfo.Allow"
      via: "item[\"slack_allow\"] S split on comma"
      pattern: "item\\[\"slack_allow\"\\]"
---

<objective>
Feature B — per-sandbox plumbing. Carry `notification.slack.inbound.allow` from the profile to the `km-sandboxes` DDB row at `km create`, round-trip it through `SandboxMetadata` (so pause/resume/extend don't strip it), and read it in the bridge's `FetchByChannel` into `SandboxRoutingInfo.Allow`.

Purpose: Make AC7 (round-trip survival) GREEN and provide `SandboxRoutingInfo.Allow` that Plan 05's enforcement gate consumes. This is the dependency for the enforcement plan.
Output: slack_allow written, round-tripped, and read — verified by the Plan-01 RED round-trip test going GREEN.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-RESEARCH.md

<interfaces>
<!-- Stub fields already added in Plan 01: -->
<!--   SandboxMetadata.SlackAllow []string (pkg/aws/metadata.go) -->
<!--   SandboxRoutingInfo.Allow []string (pkg/slack/bridge/events_interfaces.go) -->
<!--   NotificationSlackInboundSpec.Allow []string (pkg/profile/types.go) -->

DDB attribute name: "slack_allow" (S, comma-joined — NOT SS). Verify identical string
across write (create_slack_inbound.go), marshal/unmarshal (sandbox_dynamo.go), and read
(aws_adapters.go) — Phase-114 status-vs-state attr bug is the cautionary precedent.

Precedent — slack_react_always end-to-end (copy this exactly, adapting *bool → []string):
  WRITE  create_slack_inbound.go:142-149  deps.UpdateSandboxAttr(ctx, SandboxID, "slack_react_always", v)
  MARSHAL sandbox_dynamo.go:414            item["slack_react_always"] = &AttributeValueMemberBOOL{...}
  UNMARSHAL sandbox_dynamo.go:266-267      meta.SlackReactAlways = readTriStateBool(item, "slack_react_always")
  READ   aws_adapters.go:1072-1086         item["slack_react_always"].(*AttributeValueMemberBOOL) → info.ReactAlways

For slack_allow the type is comma-joined S:
  MARSHAL:   if len(meta.SlackAllow) > 0 { item["slack_allow"] = &dynamodbtypes.AttributeValueMemberS{Value: strings.Join(meta.SlackAllow, ",")} }
  UNMARSHAL: if v, ok := item["slack_allow"].(*dynamodbtypes.AttributeValueMemberS); ok && v.Value != "" { meta.SlackAllow = strings.Split(v.Value, ",") }
  READ:      if v, ok := item["slack_allow"].(*dynamodbtypes.AttributeValueMemberS); ok && v.Value != "" { info.Allow = strings.Split(v.Value, ",") }
  WRITE:     if len(inbound.Allow) > 0 { deps.UpdateSandboxAttr(ctx, deps.SandboxID, "slack_allow", strings.Join(inbound.Allow, ",")) }  // non-fatal warn on err

create_slack_inbound.go returns early when inbound.Enabled is nil/false (~line 102) —
so slack_allow is only written for inbound-enabled per-sandbox channels. Correct by design.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: slack_allow marshal + unmarshal (comma-joined S round-trip)</name>
  <files>pkg/aws/sandbox_dynamo.go</files>
  <behavior>
    TestSandboxMetadata_SlackAllow_RoundTrip (Plan 01 RED) goes GREEN:
    - marshalSandboxItem with SlackAllow=["U0OPERATOR","U0XUSER"] → item["slack_allow"] is S "U0OPERATOR,U0XUSER".
    - unmarshal → SlackAllow restored to the same 2-element slice.
    - nil/empty SlackAllow → no "slack_allow" key in the marshalled item.
  </behavior>
  <action>
    1. In marshalSandboxItem (after the slack_react_always block, ~line 414): add the comma-joined S write guarded by `len(meta.SlackAllow) > 0`.
    2. In unmarshalSlackFields (after the slack_react_always/slack_mention_only reads, ~line 267): add the S split read into meta.SlackAllow.
    Ensure `strings` is imported (it is used elsewhere in the file — confirm). Keep the attribute name string EXACTLY "slack_allow" in both places.
  </action>
  <verify>
    <automated>go test ./pkg/aws/... -run TestSandboxMetadata_SlackAllow_RoundTrip -count=1 -timeout 120s 2>&1 | tail -20</automated>
  </verify>
  <done>AC7 round-trip GREEN. Empty slice omits the attribute. Attribute name "slack_allow" identical in marshal + unmarshal.</done>
</task>

<task type="auto">
  <name>Task 2: FetchByChannel reads slack_allow → SandboxRoutingInfo.Allow</name>
  <files>pkg/slack/bridge/aws_adapters.go</files>
  <action>
    In DDBSandboxByChannel.FetchByChannel (after the slack_react_always/slack_mention_only read blocks, ~line 1086-1103): add a read of item["slack_allow"] as *dynamodbtypes.AttributeValueMemberS; when present and non-empty, set `info.Allow = strings.Split(v.Value, ",")`. Use the EXACT attribute name "slack_allow" (matches Task 1 marshal). Confirm `strings` is imported in this file.
    Add/extend a unit test in pkg/slack/bridge/aws_adapters_test.go (mirror the existing slack_react_always FetchByChannel test there): a mock GetItem returning slack_allow S "U0OPERATOR,U0XUSER" → FetchByChannel result has Allow == ["U0OPERATOR","U0XUSER"]; absent attribute → Allow nil/empty.
  </action>
  <verify>
    <automated>go test ./pkg/slack/bridge/... -run 'FetchByChannel' -count=1 -timeout 120s 2>&1 | tail -25</automated>
  </verify>
  <done>FetchByChannel populates SandboxRoutingInfo.Allow from the slack_allow S attribute; absent → empty. Attribute name matches the marshal site.</done>
</task>

<task type="auto">
  <name>Task 3: Write slack_allow to the km-sandboxes row at km create</name>
  <files>internal/app/cmd/create_slack_inbound.go</files>
  <action>
    In provisionSlackInboundQueue, after the slack_mention_only write block (~line 166-168), add the per-sandbox slack_allow write:
    - `if len(inbound.Allow) > 0 { v := strings.Join(inbound.Allow, ","); if updateErr := deps.UpdateSandboxAttr(ctx, deps.SandboxID, "slack_allow", v); updateErr != nil { log.Warn().Err(updateErr).Str("sandbox_id", deps.SandboxID).Msg("persist slack_allow per-sandbox override failed; sandbox will use install-level default") } }`
    - This runs only on the inbound-enabled path (the function already early-returns when inbound is disabled ~line 102) — correct: per-sandbox allow requires the inbound queue.
    Confirm `strings` is imported. Use the EXACT attribute name "slack_allow".
    Add/extend a unit test in internal/app/cmd/create_slack_inbound_test.go (mirror the slack_react_always write test there): with inbound.Allow=["U0OPERATOR"], assert UpdateSandboxAttr was called with key "slack_allow" value "U0OPERATOR"; with empty Allow, assert it was NOT called for slack_allow.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'SlackInbound|provisionSlackInbound|SlackAllow' -count=1 -timeout 180s 2>&1 | tail -30</automated>
  </verify>
  <done>km create writes slack_allow (comma-joined) when inbound.Allow is non-empty; non-fatal on error; not written when empty or inbound disabled. Attribute name matches marshal + FetchByChannel.</done>
</task>

</tasks>

<verification>
- AC7: `go test ./pkg/aws/... -run TestSandboxMetadata_SlackAllow_RoundTrip` GREEN — slack_allow survives a full-row marshal/unmarshal.
- The "slack_allow" string literal is byte-identical across create_slack_inbound.go (write), sandbox_dynamo.go (marshal + unmarshal), aws_adapters.go (read) — grep to confirm 4 identical sites.
- `go test ./pkg/slack/bridge/... ./pkg/aws/... ./internal/app/cmd/... -count=1 -timeout 300s` green for the touched test names.
</verification>

<success_criteria>
SandboxRoutingInfo.Allow is populated from a per-sandbox slack_allow attribute that survives pause/resume/extend. This unblocks Plan 05 enforcement (which consumes info.Allow). DDB type is comma-joined S, NOT SS.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-03-SUMMARY.md`. Confirm the 4-site attribute-name parity.
</output>
