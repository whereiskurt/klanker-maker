---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 06
type: execute
wave: 4
depends_on: [118-02, 118-03, 118-04, 118-05]
files_modified:
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "docs/slack-notifications.md has a § Phase 118 section documenting Feature A (private) + Feature B (allowlist) with both deploy paths"
    - "CLAUDE.md has a Phase 118 summary entry consistent with prior phase-summary style"
    - "The 'Where to look' table in CLAUDE.md routes private-channel + allowlist topics to the right docs"
  artifacts:
    - path: "docs/slack-notifications.md"
      provides: "Phase 118 operator runbook section"
      contains: "Phase 118"
    - path: "CLAUDE.md"
      provides: "Phase 118 summary block + where-to-look rows"
      contains: "Phase 118"
  key_links:
    - from: "CLAUDE.md Where-to-look table"
      to: "docs/slack-notifications.md § Phase 118"
      via: "table row pointers"
      pattern: "Phase 118"
---

<objective>
Document Phase 118: the operator runbook in `docs/slack-notifications.md` § Phase 118 and the phase-summary block + where-to-look rows in `CLAUDE.md`. Pure docs — no code.

Purpose: Phase summaries and the where-to-look table are the project's institutional memory; every prior Slack phase has one. This closes the documentation must-have for both features.
Output: Two docs updated, consistent with existing phase-summary conventions.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@docs/superpowers/specs/2026-06-24-slack-trigger-allowlist-private-channels-design.md

<interfaces>
<!-- Content to document (final, as implemented across Plans 02-05): -->

Feature A — private channel:
  - notification.slack.private (bool, default false). perSandbox:true + private:true -> is_private:true at create.
  - Invites unchanged; no new OAuth scopes (groups:read/history/write already requested).
  - private:true takes effect at channel creation only; reused (Phase 104) channels NOT converted. Flip via km destroy && km create.
  - private:true with perSandbox:false -> km validate WARNS.

Feature B — trigger allowlist (allow):
  - Install-level: km-config.yaml slack.allow []string (Uxxxx) -> KM_SLACK_ALLOW env on the bridge Lambda.
  - Per-sandbox: profile notification.slack.inbound.allow []string -> km-sandboxes row attr slack_allow (comma-joined S) -> bridge FetchByChannel.
  - Resolution: non-empty per-sandbox replaces install-level; else install-level; else empty = everyone allowed (backward-compatible). Empty/absent and explicitly-empty treated identically.
  - Enforced in events_handler.go on event.User; reject = silent ignore (no reaction/reply/dispatch). Always enforced, before mention-only AND before the Phase 91.3 thread-bypass. INVERTED vs GitHub deny-by-default.

Deploy surface:
  - Install-level allow -> KM_SLACK_ALLOW -> km init --dry-run=false (NOT --sidecars; km init --slack scoped apply also covers env+IAM).
  - Per-sandbox allow + private -> profile schema + create-handler + bridge -> make build-lambdas + km init --dry-run=false. Existing sandboxes need km destroy && km create for per-sandbox attrs; install-level allowlist takes effect on the next webhook.
  - No apiVersion bump.

Reference existing § Phase 96 / § Phase 104 / § Phase 111 sections in docs/slack-notifications.md
for the established section format (behavior, edges, deploy, troubleshooting). Match the
CLAUDE.md phase-summary block style of the most recent entries (Phase 112/113/114/115).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/slack-notifications.md § Phase 118</name>
  <files>docs/slack-notifications.md</files>
  <action>
    Add a new "## Phase 118 — Slack trigger allowlist + private per-sandbox channels" section (place after the latest Phase 1xx Slack section). Cover, matching the existing section style:
    - Feature A (private channel): the notification.slack.private field, perSandbox+private behavior, invites/scopes unchanged, the create-only / no-conversion edge, the validate warn.
    - Feature B (allowlist): install-level slack.allow + per-sandbox notification.slack.inbound.allow, the resolution order (per-sandbox > install > everyone), the silent-drop semantics, the always-enforced-before-mention/thread-bypass guarantee, and the INVERTED empty=everyone note.
    - A worked km-config.yaml + profile YAML example for each.
    - Deploy surface (both paths, NOT --sidecars for install-level; make build-lambdas + km init for per-sandbox).
    - Troubleshooting: the config merge-list footgun (slack.allow silently ignored if absent — already fixed), DDB attr-name parity, "everyone allowed when both empty".
    Use generic placeholder Uxxxx IDs (U0OPERATOR, U0XUSER) and example.com — never real tenant data.
  </action>
  <verify>
    <automated>grep -q "Phase 118" docs/slack-notifications.md && grep -q "notification.slack.private" docs/slack-notifications.md && grep -q "slack.allow" docs/slack-notifications.md && grep -q "KM_SLACK_ALLOW" docs/slack-notifications.md && echo OK</automated>
  </verify>
  <done>§ Phase 118 exists covering both features, both deploy paths, examples, and troubleshooting. Generic placeholders only.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md Phase 118 summary + where-to-look rows</name>
  <files>CLAUDE.md</files>
  <action>
    1. Add a "**Phase 118 (2026-06-24) — Slack trigger allowlist + private per-sandbox channels (complete):**" summary block (place alongside the other recent phase blocks, near Phase 115/114). Match the existing bullet style: what shipped, the two features, the resolution order, the INVERTED empty=everyone semantics, the four plumbing gotchas addressed (config merge-list, SandboxMetadata round-trip, FetchByChannel attr-name parity, validate warns), and the deploy surface (install-level: km init --dry-run=false NOT --sidecars; per-sandbox: make build-lambdas + km init; no apiVersion bump). Point to docs/slack-notifications.md § Phase 118.
    2. Add rows to the "Where to look" table:
       - Private per-sandbox Slack channel (notification.slack.private) -> docs/slack-notifications.md § Phase 118
       - Slack trigger allowlist (slack.allow / notification.slack.inbound.allow, KM_SLACK_ALLOW, slack_allow attr, resolution order) -> docs/slack-notifications.md § Phase 118
    Keep wording consistent with the surrounding entries; generic placeholders only.
  </action>
  <verify>
    <automated>grep -q "Phase 118" CLAUDE.md && grep -q "notification.slack.private" CLAUDE.md && grep -q "slack.allow\|KM_SLACK_ALLOW" CLAUDE.md && echo OK</automated>
  </verify>
  <done>CLAUDE.md has the Phase 118 summary block + two where-to-look rows, consistent with existing conventions.</done>
</task>

</tasks>

<verification>
- docs/slack-notifications.md § Phase 118 documents both features, both deploy paths, examples, troubleshooting.
- CLAUDE.md has the Phase 118 summary block + where-to-look rows.
- No real tenant/customer names; generic placeholders only.
</verification>

<success_criteria>
Phase 118 is documented to the same standard as prior Slack phases; operators can configure private channels and the trigger allowlist from the docs alone, and CLAUDE.md routes the topics correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-06-SUMMARY.md`.
</output>
