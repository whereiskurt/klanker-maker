---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/slack/bridge/events_handler_allowlist_test.go
  - pkg/slack/bridge/events_handler.go
  - pkg/slack/bridge/events_interfaces.go
  - pkg/aws/sandbox_dynamo_allow_test.go
  - pkg/aws/metadata.go
  - pkg/profile/types.go
  - internal/app/cmd/create_slack_test.go
  - internal/app/cmd/create_slack_transcript_test.go
  - internal/app/cmd/create_slack_invite_test.go
  - internal/app/cmd/slack_invite_test.go
  - internal/app/cmd/slack_test.go
  - pkg/profile/validate_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "RED tests exist (compile + fail/skip pending impl) for allowlist enforcement, resolution order, thread-bypass-non-exempt, byte-identical invariant"
    - "RED test exists for slack_allow comma-joined-S round-trip through SandboxMetadata marshal/unmarshal"
    - "All 6 CreateChannel mocks compile against the new (ctx, name, private) signature"
    - "validate_test.go has a stub asserting the new private/allow + perSandbox:false warn rules"
  artifacts:
    - path: "pkg/slack/bridge/events_handler_allowlist_test.go"
      provides: "Wave-0 RED tests for AC2/AC3/AC4/AC5/AC8 (EventsHandler allowlist)"
      contains: "TestEventsHandler_Allowlist"
    - path: "pkg/aws/sandbox_dynamo_allow_test.go"
      provides: "Wave-0 RED test for AC7 (slack_allow round-trip)"
      contains: "TestSandboxMetadata_SlackAllow_RoundTrip"
  key_links:
    - from: "internal/app/cmd/create_slack_test.go"
      to: "pkg/slack.Client.CreateChannel"
      via: "fakeSlackAPI mock signature"
      pattern: "CreateChannel.*private"
---

<objective>
Create the Wave-0 RED test scaffold (TDD) that defines the contract for Phase 118 BEFORE any implementation. This plan writes failing/skipped tests and updates all 6 `CreateChannel` mock signatures so the codebase compiles against the (about-to-change) `CreateChannel(ctx, name, private)` signature.

Purpose: Lock the acceptance-criteria contract in code before Feature A/B implementation, so executors build against tests, not exploration. These tests go RED (fail or skip) now and GREEN when Plans 02–05 land.
Output: 4 Wave-0 test additions covering AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-RESEARCH.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-VALIDATION.md

<interfaces>
<!-- Contracts the tests must assert against. From RESEARCH.md. Use directly. -->

Feature A — CreateChannel signature (BECOMES, implemented in Plan 02):
```go
// pkg/slack.Client and both local interfaces (create_slack.go:87 SlackAPI,
// slack.go:54 SlackInitAPI) gain a `private bool` parameter.
CreateChannel(ctx context.Context, name string, private bool) (string, error)
```
The 6 existing mocks (signature `CreateChannel(_ context.Context, _ string)`):
  - internal/app/cmd/create_slack_test.go:43        (fakeSlackAPI — capture private into a field)
  - internal/app/cmd/create_slack_transcript_test.go:24 (fakeSlackAPIWithMembers)
  - internal/app/cmd/create_slack_invite_test.go:56  (fakeSlackAPIForInvite)
  - internal/app/cmd/slack_invite_test.go:79         (fakeSlackAPIForInvite)
  - internal/app/cmd/slack_test.go:61                (fakeSlackInitAPI)
  - internal/app/cmd/slack_test.go:798               (fakeRotateAPI)

Feature B — EventsHandler + SandboxRoutingInfo (implemented Plans 03/05):
```go
// pkg/slack/bridge/events_handler.go — EventsHandler gains:
Allow []string          // install-level (KM_SLACK_ALLOW). empty = everyone allowed.
// pkg/slack/bridge/events_interfaces.go — SandboxRoutingInfo gains:
Allow []string          // per-sandbox override. non-empty replaces install-level.
// Resolution: len(info.Allow)>0 ? info.Allow : h.Allow ; empty => everyone allowed
// (INVERTED from GitHub bridge deny-by-default).
```

Feature B — SandboxMetadata (implemented Plan 03):
```go
// pkg/aws/metadata.go — SandboxMetadata gains:
SlackAllow []string `json:"slack_allow,omitempty"`
// DDB attribute "slack_allow" is a comma-joined S (AttributeValueMemberS),
// NOT SS. marshalSandboxItem joins; unmarshalSlackFields splits on comma.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write EventsHandler allowlist RED tests + add Allow field stubs</name>
  <files>pkg/slack/bridge/events_handler_allowlist_test.go, pkg/slack/bridge/events_handler.go, pkg/slack/bridge/events_interfaces.go</files>
  <behavior>
    Table-driven tests in pkg/slack/bridge/events_handler_allowlist_test.go covering:
    - TestEventsHandler_Allowlist (AC2): EventsHandler.Allow=[U_OP]; event.User=U_OP → dispatched; event.User=U_OTHER → silent drop (no reaction, no dispatch, returns 200/ok).
    - TestEventsHandler_PerSandboxAllowOverrides (AC3): EventsHandler.Allow=[U_OP]; SandboxRoutingInfo.Allow=[U_X]; event.User=U_X → dispatched; event.User=U_OP → silent drop (per-sandbox non-empty REPLACES install-level).
    - TestEventsHandler_AllowlistEmpty_EveryoneAllowed (AC4): both Allow lists empty/nil; any event.User → dispatched.
    - TestEventsHandler_Allowlist_ThreadBypassDoesNotExempt (AC5): MentionOnly=true + an active thread (Phase 91.3 bypass would skip mention) + event.User NOT in allow → STILL silent-dropped (allowlist is the outer gate, runs before mention/thread logic).
    - TestEventsHandler_NoAllowlistSet_ByteIdentical (AC8): no allowlist anywhere → existing dispatch path unchanged.
    Mirror the existing EventsHandler test setup in this package (look at events_handler*_test.go for the established harness: how msg/event, FetchByChannel result, mocks, and assertions are wired). Reuse that harness — do NOT invent a new one.
  </behavior>
  <action>
    First add the minimal struct field stubs so the test file compiles (the enforcement LOGIC lands in Plan 05; the FIELD must exist now for the test to reference it):
    - In pkg/slack/bridge/events_handler.go add `Allow []string` to the EventsHandler struct (place after the `ReactAlways bool` field). Doc-comment: install-level trigger allowlist (Uxxxx), populated from KM_SLACK_ALLOW; empty = everyone allowed (backward-compatible); per-sandbox SandboxRoutingInfo.Allow (non-empty) replaces it.
    - In pkg/slack/bridge/events_interfaces.go add `Allow []string` to SandboxRoutingInfo (place after the ReactAlways/MentionOnly fields). Doc-comment: per-sandbox trigger allowlist override; non-empty replaces EventsHandler.Allow; nil/empty = fall back to install-level (or everyone).

    Then write events_handler_allowlist_test.go with the 5 table-driven tests above. Because enforcement is NOT yet implemented, the rejection-path assertions WILL fail (every user is currently dispatched). That is the intended RED state. Mark the enforcement-dependent sub-cases so they fail loudly (do NOT t.Skip the rejection cases — they must be RED so Plan 05 turning them GREEN is observable). The AC4/AC8 allow-everyone cases should pass immediately (no enforcement yet = everyone allowed), which is correct.

    Use sentinel IDs: U_OP="U0OPERATOR", U_X="U0XUSER", U_OTHER="U0OTHER".
  </action>
  <verify>
    <automated>go vet ./pkg/slack/bridge/... && go test ./pkg/slack/bridge/... -run 'TestEventsHandler_Allowlist|TestEventsHandler_PerSandboxAllowOverrides|TestEventsHandler_AllowlistEmpty_EveryoneAllowed|TestEventsHandler_Allowlist_ThreadBypassDoesNotExempt|TestEventsHandler_NoAllowlistSet_ByteIdentical' -count=1 -timeout 120s 2>&1 | tail -40 ; true</automated>
  </verify>
  <done>Package compiles (go vet clean). The 5 tests exist and run. Allow-everyone cases (AC4/AC8) GREEN; rejection cases (AC2/AC3/AC5) RED pending Plan 05. EventsHandler.Allow and SandboxRoutingInfo.Allow fields exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write SandboxMetadata slack_allow round-trip RED test + add field stub</name>
  <files>pkg/aws/sandbox_dynamo_allow_test.go, pkg/aws/metadata.go</files>
  <behavior>
    TestSandboxMetadata_SlackAllow_RoundTrip (AC7) in pkg/aws/sandbox_dynamo_allow_test.go:
    - Build a SandboxMetadata with SlackAllow=["U0OPERATOR","U0XUSER"].
    - marshalSandboxItem(meta) → assert item["slack_allow"] is an *dynamodbtypes.AttributeValueMemberS with Value "U0OPERATOR,U0XUSER" (comma-joined S, NOT SS).
    - unmarshal the same item (via the package's read path — mirror how existing sandbox_dynamo_test.go round-trips slack_react_always/slack_mention_only) → assert SlackAllow == ["U0OPERATOR","U0XUSER"].
    - Empty/nil SlackAllow → assert item has NO "slack_allow" key (absence = fall-back signal).
    Mirror the existing slack_react_always round-trip test in sandbox_dynamo_test.go for harness shape and helper usage.
  </behavior>
  <action>
    Add the field stub so the test compiles (the marshal/unmarshal LOGIC lands in Plan 03):
    - In pkg/aws/metadata.go add `SlackAllow []string` to SandboxMetadata after `SlackReactAlways *bool` (line ~71). Doc-comment per RESEARCH.md: comma-joined "slack_allow" S attribute; nil/empty = fall back to install-level; must round-trip via marshal/unmarshal (SandboxMetadata lossy round-trip footgun).

    Then write the test. Because marshalSandboxItem/unmarshalSlackFields do NOT yet handle slack_allow (Plan 03), the round-trip assertion WILL fail (item["slack_allow"] absent after marshal). Intended RED. Do NOT add the marshal/unmarshal logic here — that is Plan 03's task.
  </action>
  <verify>
    <automated>go vet ./pkg/aws/... && go test ./pkg/aws/... -run TestSandboxMetadata_SlackAllow_RoundTrip -count=1 -timeout 120s 2>&1 | tail -30 ; true</automated>
  </verify>
  <done>pkg/aws compiles. TestSandboxMetadata_SlackAllow_RoundTrip exists and runs RED (marshal does not yet emit slack_allow). SlackAllow field exists on SandboxMetadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update all 6 CreateChannel mocks for (ctx, name, private) + extend validate warn-rule test</name>
  <files>internal/app/cmd/create_slack_test.go, internal/app/cmd/create_slack_transcript_test.go, internal/app/cmd/create_slack_invite_test.go, internal/app/cmd/slack_invite_test.go, internal/app/cmd/slack_test.go, pkg/profile/types.go, pkg/profile/validate_test.go</files>
  <behavior>
    AC1 (mock contract): fakeSlackAPI (create_slack_test.go) records the `private` argument so Plan 02's TestResolveSlack_PerSandbox_PrivateChannel can assert km create passes it through. The other 5 mocks just satisfy the new signature.
    AC6 (validate): TestValidate_Slack (extend existing, or add a sub-test) asserts:
    - private:true + perSandbox not true → exactly one WARNING at path spec.notification.slack.private (NOT an error).
    - inbound.allow non-empty + perSandbox not true → exactly one WARNING at path spec.notification.slack.inbound.allow.
    - private:true + perSandbox:true → no warning. inbound.allow + perSandbox:true → no warning.
  </behavior>
  <action>
    Mock signature updates — change each to `CreateChannel(_ context.Context, name string, _ bool) (string, error)` (keep the existing body; only the create_slack_test.go fakeSlackAPI should additionally store the bool):
    - create_slack_test.go:43 (fakeSlackAPI) — add `createPrivate bool` field to the struct; set it from the new param in CreateChannel. (Plan 02 Task 1 step 7 asserts this field.)
    - create_slack_transcript_test.go:24 (fakeSlackAPIWithMembers)
    - create_slack_invite_test.go:56 (fakeSlackAPIForInvite)
    - slack_invite_test.go:79 (fakeSlackAPIForInvite)
    - slack_test.go:61 (fakeSlackInitAPI)
    - slack_test.go:798 (fakeRotateAPI)

    Profile stub fields (so validate_test.go and all Wave-2 plans compile): in pkg/profile/types.go add `Private bool` to NotificationSlackSpec and `Allow []string` to NotificationSlackInboundSpec (doc-comments per RESEARCH.md Layer 1). These are inert data fields until Plan 02/03 wire them.

    Then write the AC6 validate_test.go cases. They go RED because the warn rules are not yet in validate.go (Plan 02 adds them).

    Compile note: the 6 mocks compile RED until Plan 02 changes the real CreateChannel signature — internal/app/cmd will NOT build until Plan 02 lands. The real signature change is OUT OF SCOPE here. Gate the verify with `|| true`/`; true` and document in the SUMMARY that internal/app/cmd compiles only after Plan 02. pkg/profile DOES compile and its validate_test.go runs RED standalone.
  </action>
  <verify>
    <automated>go vet ./pkg/profile/... 2>&1 | tail -20 ; go test ./pkg/profile/... -run TestValidate_Slack -count=1 -timeout 120s 2>&1 | tail -30 ; true</automated>
  </verify>
  <done>pkg/profile compiles with Private/Allow stub fields. validate_test.go AC6 cases exist (RED pending Plan 02 warn rules). All 6 CreateChannel mocks carry the (ctx, name, bool) signature; fakeSlackAPI captures createPrivate (internal/app/cmd builds green only after Plan 02's real signature change).</done>
</task>

</tasks>

<verification>
- `go vet ./pkg/slack/bridge/... ./pkg/aws/... ./pkg/profile/...` is clean.
- Wave-0 RED tests exist and run for AC2, AC3, AC4, AC5, AC7, AC8 (allowlist + round-trip) and AC6 (validate warns).
- Profile stub fields (Private, Allow) + handler/metadata stub fields (EventsHandler.Allow, SandboxRoutingInfo.Allow, SandboxMetadata.SlackAllow) exist so Wave-2 plans build on a stable contract.
- internal/app/cmd intentionally does NOT compile until Plan 02 lands the real CreateChannel signature (documented in SUMMARY).
</verification>

<success_criteria>
All 8 ACs have a named Go test (or validate sub-test) on disk. Rejection-path tests are RED; allow-everyone/byte-identical tests are GREEN. Stub data fields exist on all 5 structs so downstream plans build against a frozen contract.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-01-SUMMARY.md`. Note explicitly that internal/app/cmd compiles only after Plan 02's CreateChannel signature change.
</output>
