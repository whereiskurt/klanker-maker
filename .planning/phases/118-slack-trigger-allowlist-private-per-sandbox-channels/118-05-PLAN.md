---
phase: 118-slack-trigger-allowlist-private-per-sandbox-channels
plan: 05
type: execute
wave: 3
depends_on: [118-03, 118-04]
files_modified:
  - pkg/slack/bridge/events_handler.go
  - pkg/slack/bridge/events_handler_allowlist_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Allowlist gate runs in Handle() AFTER channel-ownership (FetchByChannel) and BEFORE the mention-only/thread-bypass filter"
    - "Resolution: non-empty info.Allow (per-sandbox) replaces h.Allow (install-level); else h.Allow; else empty = everyone allowed"
    - "Empty allowlist => everyone allowed (INVERTED from GitHub bridge deny-by-default)"
    - "Rejected user = silent drop: no reaction, no reply, no dispatch, returns 200"
    - "Enforced regardless of mention-only mode AND the Phase 91.3 thread-bypass (non-listed user cannot hijack an active thread)"
  artifacts:
    - path: "pkg/slack/bridge/events_handler.go"
      provides: "allowlist gate + isInSlackAllowlist helper"
      contains: "isInSlackAllowlist"
  key_links:
    - from: "pkg/slack/bridge/events_handler.go Handle()"
      to: "isInSlackAllowlist(user, effectiveAllow)"
      via: "gate inserted after info fetch, before effectiveMentionOnly"
      pattern: "isInSlackAllowlist"
---

<objective>
Feature B — enforcement. Insert the allowlist gate into `EventsHandler.Handle()` between the channel-ownership lookup (`FetchByChannel`, step 5) and the mention-only/thread filter (step 5b). Resolve the effective allowlist (per-sandbox `info.Allow` non-empty replaces install-level `h.Allow`; empty = everyone allowed) and silent-drop a non-listed `event.User`.

Purpose: Turn the Plan-01 RED rejection tests (AC2, AC3, AC5) GREEN and keep AC4/AC8 GREEN. This is the last code plan; it depends on Plan 03 (`info.Allow`) and Plan 04 (`h.Allow` wiring).
Output: AC2, AC3, AC4, AC5, AC8 all GREEN.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-CONTEXT.md
@.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-RESEARCH.md

<interfaces>
<!-- From Plan 01: EventsHandler.Allow []string ; from Plan 03: SandboxRoutingInfo.Allow []string -->

Handle() chain (events_handler.go:179-585). Insertion point: AFTER the channel-ownership
miss block (info.SandboxID == "" -> return), at the "present+yes: process locally" point
(~line 317), BEFORE the effectiveMentionOnly block (step 5b ~line 319). This ordering is LOCKED:
channel-ownership -> allowlist (new) -> mention/thread -> dedup -> dispatch.

Gate to insert (use the file's REAL local names for the message/user struct and the
response type — RESEARCH.md uses msg.User / EventsResponse; verify against the file):

    effectiveAllow := h.Allow
    if len(info.Allow) > 0 {
        effectiveAllow = info.Allow
    }
    if len(effectiveAllow) > 0 && !isInSlackAllowlist(msg.User, effectiveAllow) {
        h.log().Debug("events: allowlist: silent drop", "user", msg.User, "channel", msg.Channel, "ts", msg.TS)
        return EventsResponse{StatusCode: 200, Body: "ok"}
    }

Helper (place near isBotLoop) — note the INVERTED empty semantics vs GitHub bridge:

    // isInSlackAllowlist reports whether userID is in allow. Callers MUST gate on
    // len(allow) > 0 first: an EMPTY allow means "everyone allowed" (backward-compatible),
    // which INVERTS the GitHub bridge's isInAllowlist (empty -> deny-by-default).
    func isInSlackAllowlist(userID string, allow []string) bool {
        for _, u := range allow {
            if strings.EqualFold(u, userID) {
                return true
            }
        }
        return false
    }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert allowlist gate + isInSlackAllowlist helper</name>
  <files>pkg/slack/bridge/events_handler.go</files>
  <behavior>
    All Plan-01 RED tests go GREEN:
    - AC2 TestEventsHandler_Allowlist: h.Allow=[U_OP]; U_OP dispatched, U_OTHER silent-dropped.
    - AC3 TestEventsHandler_PerSandboxAllowOverrides: h.Allow=[U_OP], info.Allow=[U_X]; U_X dispatched, U_OP dropped.
    - AC4 TestEventsHandler_AllowlistEmpty_EveryoneAllowed: both empty; any user dispatched.
    - AC5 TestEventsHandler_Allowlist_ThreadBypassDoesNotExempt: MentionOnly=true + active thread + non-listed user -> dropped (gate runs before the thread bypass).
    - AC8 TestEventsHandler_NoAllowlistSet_ByteIdentical: no allowlist -> unchanged dispatch.
    Silent drop = returns 200/ok, NO reaction call, NO SQS dispatch call (assert the mock reaction/dispatch were not invoked).
  </behavior>
  <action>
    1. Read Handle() (events_handler.go:179-585) to confirm the exact insertion point: immediately after the channel-ownership FetchByChannel result is known to be a HIT (info.SandboxID != "") and BEFORE the effectiveMentionOnly computation (step 5b ~line 319). Verify the real local variable names for the inbound message/user (msg.User vs event.User) and the response constructor.
    2. Insert the gate block (above) using the file's real names. The per-sandbox-replaces-install resolution and the len>0 guard (empty = everyone) are mandatory and must match the locked spec.
    3. Add isInSlackAllowlist near isBotLoop, with the docstring making the INVERTED (allow-by-default-on-empty) semantics explicit vs the GitHub bridge.
    4. Confirm strings is imported.
    Do NOT place the gate after the mention check or after the thread-bypass (AC5 fails). Do NOT post a reaction or reply before returning on reject (silent).
  </action>
  <verify>
    <automated>go test ./pkg/slack/bridge/... -run 'TestEventsHandler_Allowlist|TestEventsHandler_PerSandboxAllowOverrides|TestEventsHandler_AllowlistEmpty_EveryoneAllowed|TestEventsHandler_Allowlist_ThreadBypassDoesNotExempt|TestEventsHandler_NoAllowlistSet_ByteIdentical' -count=1 -timeout 120s 2>&1 | tail -40</automated>
  </verify>
  <done>All 5 EventsHandler allowlist tests GREEN. Gate sits before mention/thread logic. Silent drop performs no reaction/dispatch and returns 200. Empty allowlist = everyone allowed.</done>
</task>

<task type="auto">
  <name>Task 2: Full bridge-package regression + whole-suite green</name>
  <files>pkg/slack/bridge/events_handler_allowlist_test.go</files>
  <action>
    Finalize the allowlist test file: remove any leftover RED scaffolding markers from Plan 01, ensure each AC test asserts the exact silent-drop invariants (mock reaction NOT called, mock dispatch NOT called, status 200). Then run the full bridge package and the whole suite to confirm no existing EventsHandler behavior regressed (AC8 byte-identical at the package level, not just the single test).
  </action>
  <verify>
    <automated>go test ./pkg/slack/bridge/... -count=1 -timeout 180s 2>&1 | tail -20 ; echo "---FULL---" ; go test ./... -count=1 -timeout 600s 2>&1 | tail -25</automated>
  </verify>
  <done>pkg/slack/bridge fully green; whole-repo go test ./... green (or only pre-existing/environmental reds documented in memory). No EventsHandler regression.</done>
</task>

</tasks>

<verification>
- AC2, AC3, AC4, AC5, AC8: all GREEN via the named tests.
- Gate ordering: channel-ownership -> allowlist -> mention/thread -> dedup -> dispatch (read the diff to confirm placement).
- Reject path is silent (no reaction/reply/dispatch; 200).
- `go test ./... -count=1 -timeout 600s` green (capture the command's own exit, not a piped tail — see memory feedback_check_go_test_exit_not_pipe).
</verification>

<success_criteria>
The allowlist is enforced exactly per the locked handler order, with per-sandbox > install-level > everyone resolution and silent-drop on reject, robust against mention-only and the Phase 91.3 thread-bypass. All automated ACs (AC2–AC8) pass; AC1/AC2 live-Slack UAT is the phase gate.
</success_criteria>

<output>
After completion, create `.planning/phases/118-slack-trigger-allowlist-private-per-sandbox-channels/118-05-SUMMARY.md`.
</output>
