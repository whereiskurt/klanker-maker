---
phase: 63.1-slack-notify-hook-gap-closure-step-11d-runtime-injection-visibility-km-destroy-archive-auto-trigger-and-bridge-token-rotation-hardening
verified: 2026-04-30T18:00:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 63.1: Slack Notify Hook Gap Closure Verification Report

**Phase Goal:** Close SLCK-11 (step 11d Slack env injection visibility + silent-failure root-cause), SLCK-12 (km destroy Slack archive auto-trigger reliability + visibility), and SLCK-13 (bot-token revoke-and-rotate hardening).
**Verified:** 2026-04-30
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | `km create --remote` stderr shows exactly one of 5 outcomes (SLCK-11 visibility) | VERIFIED | `runStep11dInject` in `create_slack.go:249-303` emits all 5 locked strings via `fmt.Fprintf(os.Stderr, ...)`, bypassing zerolog's `io.Discard` mask |
| 2 | Bounded retry loop (6 x 5s) wraps `injectSlackEnvIntoSandbox` (SLCK-11 root-cause fix) | VERIFIED | `create_slack.go:288-294`: `for attempt := 1; attempt <= retryMax; attempt++` with `time.Sleep(retryDelay)` between attempts; production caller uses `step11dSSMRetryMax=6` / `step11dSSMRetryDelay=5*time.Second` (`create.go:804-807`) |
| 3 | All 8 step-11d tests pass: 5 stderr-branch + 3 retry-loop (SLCK-11 tests) | VERIFIED | `TestStep11d_VisibleOutput_{NoBridgeURL,OutputsErr,NoInstanceID,SSMFail,Success}` + `TestInjectSlackEnv_{RetryUntilAgentReady,MaxRetryThenFail,RetryRespectsBound}` — all green |
| 4 | `captureStderr` helper lives in canonical `testhelpers_test.go` (shared by Plans 01 + 02) | VERIFIED | `testhelpers_test.go:13`: `func captureStderr(t *testing.T, fn func()) string` — single definition, no duplicate |
| 5 | `runSlackTeardown` shared helper is wired into BOTH remote and local destroy paths (SLCK-12) | VERIFIED | `destroy.go:108`: `runSlackTeardown(ctx, remoteAWSCfg, meta)` fires BEFORE `publisher.PublishSandboxCommand` at line 118; local path at `destroy.go:472` also calls `runSlackTeardown` |
| 6 | All 6 destroySlackChannel cases (A/B/E/F/G/H) emit visible stderr (SLCK-12 visibility) | VERIFIED | `destroy_slack.go:62,68,78,88,116,119,125,149,153` — all 6 cases + success-path split (posted + archived) + Case I |
| 7 | 12 new destroy tests pass (7 visibility + 2 runSlackTeardown regression + 3 wiring) | VERIFIED | `TestDestroySlackChannel_VisibleOutput_Case{A,B,E,F,G,H}` + `TestDestroySlackChannel_VisibleOutput_Success` + `TestRunSlackTeardown_{NonFatalOnBridgeError,SuccessPathFiresBothEmissions}` + `TestDestroyDestroy_{SlackTeardownWiredIntoRemotePath,LocalPathUsesSharedHelper}` + `TestDestroyCmd_RemoteSlackTeardownNonFatalWhenAWSUnavailable` — all green |
| 8 | `forceSlackBridgeColdStart` targets `km-slack-bridge` with `TOKEN_ROTATION_TS` env var (SLCK-13) | VERIFIED | `init.go:1666-1676`: `ForceSlackBridgeColdStartWith` calls `UpdateFunctionConfiguration` with `FunctionName="km-slack-bridge"` and env var key `TOKEN_ROTATION_TS`; 2 tests pass (`TestSlackBridgeColdStart_TargetsCorrectFunction`, `TestSlackBridgeColdStart_PropagatesError`) |
| 9 | `km slack rotate-token` subcommand: validate → SSM persist → cold-start → smoke test (SLCK-13) | VERIFIED | `slack.go:412-499`: `RunSlackRotateToken` with 5-step flow registered as `newSlackRotateTokenCmd` in `newSlackCmdInternal` (line 112); 3 tests pass (`TestSlackRotateToken_HappyPath`, `TestSlackRotateToken_AuthTestFails`, `TestSlackRotateToken_ColdStartFailsButSmokePasses`) |
| 10 | Bridge handler uses `log/slog` structured logging; `PostToBridge` does NOT retry 5xx (SLCK-13) | VERIFIED | `handler.go:8,20-23`: `log/slog` imported, `SetLogger()` exported; `client.go:171-225`: 5xx treated as fail-fast with explicit comment; `TestPostToBridge_5xxNotRetried` and `TestPostToBridge_503NotRetried` assert fail-fast; 10 handler logging tests pass |
| 11 | Operator runbook in `docs/slack-notifications.md` + `CLAUDE.md` CLI reference (SLCK-13) | VERIFIED | `docs/slack-notifications.md:272-343`: `## Bot Token Rotation` section with Quick path, Full revoke-and-rotate cycle, Legacy path, Cache TTL caveat; `CLAUDE.md:32`: `km slack rotate-token` in CLI list; Live UAT outcome documented in `63.1-03-SUMMARY.md` with `ts=1777638955.854989` |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/app/cmd/create_slack.go` | `runStep11dInject` with all 5 stderr branches + retry loop | VERIFIED | Lines 249-303: function exists, substantive, contains retry loop `for attempt := 1; attempt <= retryMax` and all 5 `fmt.Fprintf` emissions |
| `internal/app/cmd/create.go` | Calls `runStep11dInject` with `step11dSSMRetryMax=6` / `step11dSSMRetryDelay=5*time.Second` | VERIFIED | Lines 804-807: constants defined and passed to `runStep11dInject` |
| `internal/app/cmd/create_slack_test.go` | 8 tests: 5 stderr-branch + 3 retry-loop | VERIFIED | All 8 test functions present at lines 460-616; reference `captureStderr` from `testhelpers_test.go` |
| `internal/app/cmd/testhelpers_test.go` | `func captureStderr(t, fn) string` — single canonical home | VERIFIED | Line 13: single definition, not duplicated in other test files |
| `internal/app/cmd/destroy_slack.go` | All 6 cases + success path emit `fmt.Fprintf(os.Stderr, ...)`; `runSlackTeardown` helper | VERIFIED | Lines 62,68,78,88,116,119,125,149,153: all emissions present; lines 157-168: `runSlackTeardown` helper defined |
| `internal/app/cmd/destroy.go` | `runSlackTeardown` called BEFORE `PublishSandboxCommand` in remote path | VERIFIED | Line 108: `runSlackTeardown` fires; line 118: `publisher.PublishSandboxCommand` fires after; docker substrate sets `remote=false` at line 104 bypassing both |
| `internal/app/cmd/destroy_slack_test.go` | 7 visibility tests + 2 regression tests | VERIFIED | All 9 test functions present (lines 263-524) |
| `internal/app/cmd/destroy_test.go` | 3 wiring/structural tests | VERIFIED | Lines 220-339: `TestDestroyDestroy_SlackTeardownWiredIntoRemotePath`, `TestDestroyDestroy_LocalPathUsesSharedHelper`, `TestDestroyCmd_RemoteSlackTeardownNonFatalWhenAWSUnavailable` |
| `internal/app/cmd/init.go` | `forceSlackBridgeColdStart` + `ForceSlackBridgeColdStartWith` (exported for tests) | VERIFIED | Lines 1656-1687: both functions present; `ForceSlackBridgeColdStartWith` exported; targets `km-slack-bridge` with `TOKEN_ROTATION_TS` |
| `internal/app/cmd/init_test.go` | `TestSlackBridgeColdStart_TargetsCorrectFunction` + `TestSlackBridgeColdStart_PropagatesError` | VERIFIED | Both tests at lines 449-479; use `cmd.ForceSlackBridgeColdStartWith` via `fakeLambdaUpdater` |
| `internal/app/cmd/slack.go` | `RunSlackRotateToken` + `newSlackRotateTokenCmd` + `SlackCmdDeps.BridgeColdStart` | VERIFIED | Lines 73-76: `BridgeColdStart` field; lines 112: registered in `newSlackCmdInternal`; lines 412-499: full implementation |
| `internal/app/cmd/slack_test.go` | 3 rotate-token tests | VERIFIED | Lines 492-597: all 3 tests present |
| `pkg/slack/bridge/handler.go` | `log/slog` structured logging on every request + error path; `SetLogger` exported | VERIFIED | Lines 8,19-23: `slog` imported, `logger` var initialized, `SetLogger` function present |
| `pkg/slack/bridge/handler_logging_test.go` | 10 logging tests using `bytes.Buffer` slog handler | VERIFIED | Lines 53-300: all 10 `TestHandlerLog_*` functions present |
| `pkg/slack/client.go` | `PostToBridge` does NOT retry 5xx; fail-fast policy documented | VERIFIED | Lines 171-185: explicit comment; 5xx branch collapses into 4xx (fail-fast) |
| `pkg/slack/client_test.go` | `TestPostToBridge_5xxNotRetried` + `TestPostToBridge_503NotRetried` | VERIFIED | Lines 358-410: both tests assert fail-fast behavior; old retry tests removed |
| `docs/slack-notifications.md` | `## Bot Token Rotation` with full runbook + `channels:join` note | VERIFIED | Lines 272-412: complete runbook with Quick path, Full cycle, Legacy path, Cache TTL caveat, `channels:join` scope troubleshooting |
| `CLAUDE.md` | `km slack rotate-token` in CLI list | VERIFIED | Line 32: entry present |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `create.go` step 11d region | `runStep11dInject` in `create_slack.go` | delegation call at `create.go:807` with `step11dSSMRetryMax=6` | WIRED | Replaced 35-line inline block; constants defined at lines 804-805 |
| `runStep11dInject` retry loop | `injectSlackEnvIntoSandbox` | `for attempt := 1; attempt <= retryMax` at `create_slack.go:288` | WIRED | Loop breaks on nil error; sleeps `retryDelay` between attempts |
| `destroy.go` remote path | `runSlackTeardown` BEFORE `PublishSandboxCommand` | merged `if remote {}` block at lines 94-119 | WIRED | `runSlackTeardown` at line 108; `PublishSandboxCommand` at line 118; ordering is correct |
| `destroy.go` local path | `runSlackTeardown` (shared helper) | line 472 | WIRED | Replaced inline SSM/key setup block; shared helper prevents drift |
| `slack.go RunSlackRotateToken` | `forceSlackBridgeColdStart` via `d.BridgeColdStart` | `SlackCmdDeps.BridgeColdStart` field; `buildSlackCmdDeps` wires production binding at line 615-616 | WIRED | Dependency-injected for testability; production uses real Lambda updater |
| `newSlackCmdInternal` | `newSlackRotateTokenCmd` | `slackCmd.AddCommand(newSlackRotateTokenCmd(cfg, deps))` at line 112 | WIRED | Subcommand registered and accessible as `km slack rotate-token` |
| `handler.go` every request path | `slog` INFO/WARN/ERROR log calls | `logger.Info(...)` / `logger.Warn(...)` / `logger.Error(...)` throughout handler | WIRED | `SetLogger` allows test capture via `bytes.Buffer` |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| SLCK-11 | 63.1-01 | Step 11d runtime injection visibility + SSM retry root-cause fix | SATISFIED | `runStep11dInject` with 5 stderr branches + 6x5s retry loop; 8 tests green; build passes |
| SLCK-12 | 63.1-02 | km destroy Slack archive auto-trigger reliability | SATISFIED | `runSlackTeardown` shared helper wired before `PublishSandboxCommand` in remote path; all 6 case stderr emissions; 12 new tests green |
| SLCK-13 | 63.1-03 | Bot-token revoke-and-rotate hardening | SATISFIED | `km slack rotate-token` subcommand; `forceSlackBridgeColdStart` helper; bridge slog logging; fail-fast 5xx; operator runbook; live UAT completed (`ts=1777638955.854989`; `km doctor` ✓) |

All 3 requirements marked `Complete` in `.planning/REQUIREMENTS.md` (lines 340-342).

---

### Anti-Patterns Found

None blocking. Specific notes:

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `destroy.go:108` | `runSlackTeardown` in remote path is non-fatal by design (no return check) | Info | Intentional: Slack teardown must not block sandbox destroy; per CONTEXT.md SLCK-12 design |
| `slack.go:480` | Cold-start failure is swallowed (best-effort) | Info | Intentional: `RunSlackRotateToken` continues to smoke test; token persistence is the primary success criterion |

No TODO/FIXME/placeholder comments found in phase 63.1 modified files. No empty implementations (`return null`, `return {}`) in key paths.

---

### Build and Test Results

**`make build`:** PASSES — built `km v0.2.450 (23f5744)`

**`go test -count=1 ./pkg/slack/... ./internal/app/cmd/... -run "TestStep11d|TestInjectSlackEnv|TestDestroySlackChannel|TestRunSlackTeardown|TestDestroyDestroy_Slack|TestDestroyCmd_Remote|TestSlackRotateToken|TestSlackBridgeColdStart|TestHandlerLog|TestPostToBridge_5xx|TestPostToBridge_503"`:** ALL PASS

**Full `go test -count=1 ./internal/app/cmd/...`:** One failure — `TestUnlockCmd_RequiresStateBucket` — confirmed pre-existing (last modified in commit `22366b1` which predates all 63.1 commits; the test file `unlock_test.go` touches no phase 63.1 code). This failure is explicitly excluded per verification scope.

**`go test -count=1 ./pkg/slack/...`:** PASSES — `pkg/slack` ok, `pkg/slack/bridge` ok

---

### Human Verification Required

**1. Live UAT: step 11d injection success after `km init --sidecars`**

- **Test:** Provision a remote Slack-enabled sandbox; observe stderr for `✓ Slack: channel C... wired into sandbox env`; `env | grep KM_SLACK` inside sandbox shows both vars
- **Expected:** Both `KM_SLACK_CHANNEL_ID` and `KM_SLACK_BRIDGE_URL` present without manual export
- **Why human:** Requires Lambda toolchain refresh (`km init --sidecars`) and a real EC2 sandbox; SSM agent timing is environment-dependent
- **Status:** Partial — UAT documented root cause (SSM agent timing) and the retry loop addresses it; live re-run of UAT Scen 3 deferred pending next create run

**2. Live UAT: km destroy per-sandbox sandbox auto-archives channel**

- **Test:** `km destroy <per-sandbox-id> --remote --yes`; observe both `✓ Slack: posted teardown message` and `✓ Slack: archived channel` on stderr; verify `conversations.info?channel=<id>` shows `is_archived: true`
- **Expected:** Both stderr lines emitted; channel archived in Slack workspace
- **Why human:** Requires provisioning a per-sandbox sandbox and live destroy against real Slack API
- **Status:** Not yet re-run — SLCK-12 root cause (function never reached on `--remote` path) is architecturally fixed; live re-run deferred per Plan 02 SUMMARY "Next Phase Readiness"

These are post-deploy UAT items, not blocking for goal achievement. The architectural fixes are code-verified.

---

### Gaps Summary

No gaps. All must-haves are verified. The two human-verification items above are live UAT smoke tests that require a real sandbox provisioning cycle — they do not represent code gaps.

**Phase 63.1 gap-closure confirmed:**
- SLCK-11: `runStep11dInject` with all 5 stderr branches + 6x5s retry loop; step 11d is no longer a silent failure
- SLCK-12: `runSlackTeardown` wired before `PublishSandboxCommand` in remote path; the root architectural miss (function never called on `--remote`) is fixed
- SLCK-13: `km slack rotate-token` command; bridge slog logging; fail-fast 5xx (masked errors eliminated); live UAT completed with `ts=1777638955.854989`

---

_Verified: 2026-04-30_
_Verifier: Claude (gsd-verifier)_
