# Plan 63.1-02 Checkpoint — Task 2.5: Operator km destroy Diagnostic

**Status:** AWAITING operator observation

**Purpose:** Task 2 shipped the visibility commit (feat(63.1-02): `3746138`). Each early-return branch in
`destroySlackChannel` now emits a single stderr line. The next live `km destroy <id> --remote --yes` of a
per-sandbox slack-enabled sandbox will reveal which Case (A, B, E, F, G, H, or success) actually fires
in production.

## Operator Steps

```bash
cd /Users/khundeck/working/klankrmkr

# Pre-flight: deploy the visibility commit to the management Lambda
make build
./km init --sidecars

# Provision a per-sandbox slack-enabled sandbox
./km create profiles/slack-test-per-sandbox.yaml --alias 63-1-destroy-test
# Wait for the sandbox to be healthy (km list shows running)

# Verify the Slack channel exists in the workspace before destroy.
# Then destroy with stderr captured:
./km destroy <sandbox-id> --remote --yes 2>&1 | tee /tmp/63.1-02-destroy.log

# Inspect the captured stderr:
grep -E "Slack:" /tmp/63.1-02-destroy.log
```

## Expected Output Matrix

| Case | Stderr line |
|------|-------------|
| A    | `  Slack: no channel configured — teardown notification skipped` |
| B    | `  Slack: shared/override mode — teardown notification skipped` |
| E    | `  Slack: archive disabled (slackArchiveOnDestroy=false) — channel C... kept` |
| F    | `  ⚠ Slack: /km/slack/bridge-url not configured — teardown notification skipped` |
| G    | `  ⚠ Slack: failed to load operator signing key — teardown notification skipped: ...` |
| H    | `  ⚠ Slack: final-post bridge call failed — archive skipped: ...` |
| Success | BOTH `  ✓ Slack: posted teardown message to C...` AND `  ✓ Slack: archived channel C...` |

## Diagnosis (to be filled in by operator)

**Case letter that fired:** _____

**Verbatim stderr output:**
```
(paste grep -E "Slack:" /tmp/63.1-02-destroy.log output here)
```

**Was Slack channel archived in workspace?** (check conversations.info?channel=<id> → is_archived)
_____

## Resume Signal

Reply to the agent with the Case letter and verbatim stderr. Examples:
- `Case H: bridge HTTP 403: bad_signature` — Task 3 takes the H-signature branch
- `Case H: connection reset by peer` — Task 3 takes the H-network branch (retry loop)
- `Case G: AccessDeniedException on ssm:GetParameter` — Task 3 takes the G-remediation branch
- `Success — but channel is NOT archived in Slack` — escalate (new failure mode)
- `Case F` / `Case A/B/E` — no fix needed; plan closes after documenting
