---
phase: 63.1
slug: slack-notify-hook-gap-closure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-30
---

# Phase 63.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (standard library + testify-style mocks) |
| **Config file** | none (Go default) |
| **Quick run command** | `go test -count=1 ./internal/app/cmd/... ./pkg/slack/...` |
| **Full suite command** | `go test -count=1 ./...` |
| **Estimated runtime** | ~25s quick, ~2m full |

---

## Sampling Rate

- **After every task commit:** Run `go test -count=1 ./internal/app/cmd/ -run "Slack|Step11d|Destroy"` (~5s)
- **After every plan wave:** Run quick suite above (~25s)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 25 seconds for the affected packages

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 63.1-01-01 | 01 | 1 | SLCK-11 | unit (stderr capture via os.Pipe) | `go test -count=1 -run "TestStep11d_VisibleOutput" ./internal/app/cmd/` | ❌ W0 — new test file | ⬜ pending |
| 63.1-01-02 | 01 | 1 | SLCK-11 | unit (retry loop with mocked SSM) | `go test -count=1 -run "TestInjectSlackEnv_RetryUntilAgentReady" ./internal/app/cmd/` | ❌ W0 — new test | ⬜ pending |
| 63.1-01-03 | 01 | 1 | SLCK-11 | manual smoke (`km create --remote` slack-test-shared.yaml) | `./km create profiles/slack-test-shared.yaml --alias 63-1-test && ./km shell <id>` then `env \| grep KM_SLACK` | n/a (live) | ⬜ pending |
| 63.1-02-01 | 02 | 1 | SLCK-12 | unit (stderr capture for Cases A,B,F,G,H,E) | `go test -count=1 -run "TestDestroySlack_VisibleOutput" ./internal/app/cmd/` | ❌ W0 — extend `destroy_slack_test.go` | ⬜ pending |
| 63.1-02-02 | 02 | 1 | SLCK-12 | unit (final-post error surfaces) | `go test -count=1 -run "TestDestroySlack_CaseH_BridgeError" ./internal/app/cmd/` | ❌ W0 — new test | ⬜ pending |
| 63.1-02-03 | 02 | 1 | SLCK-12 | manual smoke (km destroy per-sandbox) | `./km destroy <id>` then verify channel archived in Slack via API | n/a (live) | ⬜ pending |
| 63.1-03-01 | 03 | 2 | SLCK-13 | unit (force-cold-start path) | `go test -count=1 -run "TestSlackBridgeColdStart" ./internal/app/cmd/` | ❌ W0 — new test | ⬜ pending |
| 63.1-03-02 | 03 | 2 | SLCK-13 | unit (`km slack rotate-token` if Claude opts in) | `go test -count=1 -run "TestSlackRotateToken" ./internal/app/cmd/` | ❌ W0 — conditional | ⬜ pending |
| 63.1-03-03 | 03 | 2 | SLCK-13 | manual UAT (full revoke→reissue cycle) | runbook in `docs/slack-notifications.md` | n/a (live) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Extend `internal/app/cmd/create_slack_test.go` with `os.Pipe()`-based stderr capture helper for step 11d branches
- [ ] Extend `internal/app/cmd/destroy_slack_test.go` with `os.Pipe()`-based stderr capture for Cases A/B/F/G/H/E (six new test cases)
- [ ] Add table-driven test for `injectSlackEnvIntoSandbox` retry loop with mockable SSMRunner that fails N times then succeeds
- [ ] If `km slack rotate-token` is in scope (planner discretion per CONTEXT.md SLCK-13 Claude's Discretion): add subcommand test scaffolding in `internal/app/cmd/slack_test.go`

*Existing infrastructure: 9 destroy_slack_test.go tests + 21 create_slack_test.go tests + 10 slack_test.go tests = 40 tests already covering logic paths. Wave 0 adds stderr-output assertions on top.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `km create --remote` of Slack-enabled profile auto-populates `KM_SLACK_CHANNEL_ID` and `KM_SLACK_BRIDGE_URL` in sandbox | SLCK-11 | Lambda subprocess + SSM SendCommand timing requires live AWS | `./km create profiles/slack-test-shared.yaml --alias 63-1-test`, wait for boot, `./km shell <id>` then `env \| grep KM_SLACK` — both vars must be set without manual export |
| `km destroy` of per-sandbox sandbox auto-archives the channel | SLCK-12 | Slack Connect channel state requires live Slack workspace | After destroy, query `https://slack.com/api/conversations.info?channel=<id>` with bot token — `is_archived: true` |
| Full bot-token rotation cycle | SLCK-13 | Requires Slack App admin UI to revoke + reissue + 15-min cache wait OR forceLambdaColdStart | Step-by-step runbook in `docs/slack-notifications.md` (added in this phase) |

---

## Validation Bands (Nyquist)

| Dimension | Coverage |
|-----------|----------|
| 1. Files exist | covered by `go test ./...` build step + unit tests |
| 2. Imports resolve | covered by `go build ./...` |
| 3. Public surface | covered by exported function tests |
| 4. Internal flow | covered by 9 destroy_slack + 21 create_slack table-driven tests + new stderr capture tests |
| 5. Integration | manual UAT scenarios in 63-10-UAT.md retest list |
| 6. Failure modes | EVERY case (A,B,F,G,H,E) gets a stderr-output test assertion (Wave 0) |
| 7. Resource limits | retry loop bounded at 6 × 5s = 30s; CacheTTL respected; no unbounded loops |
| 8. Observability | THIS IS THE PRIMARY DIMENSION OF THIS PHASE — every failure path emits visible stderr; success paths confirmed |
