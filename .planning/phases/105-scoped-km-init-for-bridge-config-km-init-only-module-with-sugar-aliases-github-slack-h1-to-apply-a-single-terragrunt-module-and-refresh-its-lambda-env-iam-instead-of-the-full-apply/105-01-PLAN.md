---
phase: 105-scoped-km-init-for-bridge-config
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/app/cmd/init_scoped_test.go
autonomous: true
requirements: [INIT-SCOPED-TESTS]
must_haves:
  truths:
    - "A test file internal/app/cmd/init_scoped_test.go exists and compiles"
    - "Running `go test ./internal/app/cmd/ -run TestScoped` exercises the stub tests (they fail/skip pending implementation, but the package compiles)"
  artifacts:
    - path: "internal/app/cmd/init_scoped_test.go"
      provides: "TDD scaffold for INIT-SCOPED-FLAG / ALIASES / GUARD / IMPL tests"
      contains: "func TestScoped"
  key_links:
    - from: "internal/app/cmd/init_scoped_test.go"
      to: "mockRunner / mockPlanRunner in init_test.go / init_plan_test.go"
      via: "shared package cmd test helpers"
      pattern: "mockRunner|mockPlanRunner"
---

<objective>
Create the Wave 0 TDD scaffold: a new test file `internal/app/cmd/init_scoped_test.go`
containing stub tests for every Phase 105 behavior (flag resolution, aliases,
mutual-exclusion guard, dry-run/apply, env-export, tier-2 gate, ses preflight).

Per project TDD norms, these tests exist BEFORE the implementation so later waves
have a failing target to satisfy. The file MUST compile against the existing
`mockRunner` / `mockPlanRunner` test helpers, but the production symbols it calls
(`resolveScopedModule`, `RunInitScopedWithRunner`, `scopedCheapAllowlist`,
`scopedGatedAllowlist`) do not exist yet — so the stubs are written to either
(a) reference symbols guarded behind `t.Skip("implemented in Wave 1")` so the
package still compiles, OR (b) be added incrementally as Wave 1/2 land them.

To keep the package compiling at Wave 0 (so the rest of the suite is runnable),
write the stub tests as compiling-but-skipping placeholders that name the exact
test functions the VALIDATION.md map expects. Wave 1/2 will replace the skip
bodies with real assertions in the same file.

Purpose: Establish the Nyquist test surface up front so every downstream task has
an automated verification target.
Output: `internal/app/cmd/init_scoped_test.go`
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-RESEARCH.md
@.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-VALIDATION.md

<interfaces>
<!-- Existing test helpers in package cmd the stubs will reuse (do NOT redefine). -->
<!-- mockRunner: init_test.go:22 — satisfies full InitRunner; .applied []string records Apply; .failOn string; .applyBlocks bool -->
<!-- mockPlanRunner: init_plan_test.go:42 — embeds mockRunner; adds planJSON map[string][]byte for per-module plan output -->
<!-- Exported test seams (existing): cmd.RegionalModules(dir), cmd.RunInitWithRunner(runner, repoRoot, region), cmd.RunInitPlanFunc (var), cmd.BuildLambdaZipsFunc (var) -->
<!-- Wave 1/2 will ADD: resolveScopedModule(...), RunInitScopedWithRunner(...), scopedCheapAllowlist, scopedGatedAllowlist -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold init_scoped_test.go with named stub tests</name>
  <files>internal/app/cmd/init_scoped_test.go</files>
  <action>
    Create `internal/app/cmd/init_scoped_test.go` in `package cmd` (the same internal
    test package as init_test.go — confirm whether init_test.go uses `package cmd` or
    `package cmd_test` and MATCH it; research says mockRunner lives in init_test.go and
    is used directly, so it is the in-package `cmd` test package).

    Define exactly these stub test functions (names MUST match VALIDATION.md so the
    `-run TestScoped` filter catches them all):
      - TestScopedModuleResolution      (INIT-SCOPED-FLAG)
      - TestScopedModuleRejection       (INIT-SCOPED-FLAG)
      - TestScopedAliases               (INIT-SCOPED-ALIASES)
      - TestScopedMutualExclusion       (INIT-SCOPED-GUARD)
      - TestScopedDryRun                (INIT-SCOPED-IMPL)
      - TestScopedApply                 (INIT-SCOPED-IMPL)
      - TestScopedEnvVarsExported       (INIT-SCOPED-IMPL)
      - TestScopedTier2Gate             (INIT-SCOPED-GUARD)
      - TestScopedTier2GateBlocked      (INIT-SCOPED-GUARD)
      - TestScopedSesPreflight          (INIT-SCOPED-IMPL)

    Each stub body: `t.Skip("Phase 105 Wave 1/2: pending implementation")`.

    CRITICAL — the file MUST compile cleanly at Wave 0. Do NOT reference any
    not-yet-existing production symbol (resolveScopedModule, RunInitScopedWithRunner,
    scopedCheapAllowlist, scopedGatedAllowlist) in the stub bodies — that would break
    `go build`/`go vet` for the whole package. Keep bodies to the single t.Skip line.
    Reference the symbol names only in a leading comment block on each test documenting
    what Wave 1/2 must assert (so the executor of later waves knows the contract).

    Add a top-of-file doc comment that lists the VALIDATION.md Req-ID → test mapping.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run TestScoped -v -timeout 30s</automated>
  </verify>
  <done>
    `go test ./internal/app/cmd/ -run TestScoped -v` runs and reports all 10 named
    tests as SKIP (not compile errors, not FAIL). Package cmd still builds:
    `go build ./internal/app/cmd/` exits 0.
  </done>
</task>

</tasks>

<verification>
- `go build ./internal/app/cmd/` exits 0 (package compiles with the new file).
- `go test ./internal/app/cmd/ -run TestScoped -v` lists all 10 TestScoped* functions as SKIP.
- The full suite `go test ./internal/app/cmd/ -timeout 120s` is not broken by the new file.
</verification>

<success_criteria>
- init_scoped_test.go exists in package cmd with 10 named, compiling stub tests.
- No references to unimplemented production symbols (compiles standalone).
- Test names exactly match the VALIDATION.md per-task map.
</success_criteria>

<output>
After completion, create `.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-01-SUMMARY.md`
</output>
