---
phase: 105-scoped-km-init-for-bridge-config
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - CLAUDE.md
  - OPERATOR-GUIDE.md
  - docs/operational-gotchas.md
  - docs/github-bridge.md
  - docs/slack-notifications.md
  - docs/h1-bridge.md
  - skills/init/SKILL.md
autonomous: false
requirements: [INIT-SCOPED-DOCS, INIT-SCOPED-IMPL]
must_haves:
  truths:
    - "CLAUDE.md CLI section documents km init --only/--github/--slack/--h1/--email"
    - "skills/init/SKILL.md Fast-path variants section documents the new scoped flags + boundary (env+IAM only)"
    - "Each bridge doc's deploy section mentions the scoped shortcut as a faster alternative for config-key edits"
    - "On a live install, km init --github --dry-run=false updates the bridge env block; a subsequent km init --plan is a no-op (no drift)"
    - "On a live install, km init --only ses (dry-run) shows no spurious aws_s3_bucket_policy destroy/replace"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "Fast-path variants documentation for scoped init"
      contains: "--github"
    - path: "CLAUDE.md"
      provides: "CLI list entry for scoped km init"
      contains: "km init --only"
  key_links:
    - from: "docs deploy sections (github/slack/h1)"
      to: "km init --<bridge> --dry-run=false"
      via: "scoped shortcut callout"
      pattern: "km init --(github|slack|h1)"
---

<objective>
Two parts:
1. Documentation (autonomous): update every doc surface the research enumerated so operators
   discover the new scoped path and understand its boundary (refreshes env + IAM for ONE module;
   NOT a stale code zip — still `make build-lambdas` + `km init --lambdas`; NOT new resources —
   still full `km init --dry-run=false`).
2. Live UAT (checkpoint): prove the no-drift invariant and the ses no-op on a real install. These
   require real AWS + terragrunt and cannot be unit-tested.

Purpose: Close the docs requirement and verify the headline acceptance criterion (zero drift).
Output: Updated docs across 7 surfaces + a verified live UAT.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-CONTEXT.md
@.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-VALIDATION.md
@skills/init/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document scoped init across all 7 surfaces</name>
  <files>CLAUDE.md, OPERATOR-GUIDE.md, docs/operational-gotchas.md, docs/github-bridge.md, docs/slack-notifications.md, docs/h1-bridge.md, skills/init/SKILL.md</files>
  <action>
    Add a Phase 105 note + the scoped-init flags to each surface. Keep wording consistent:
    "Scoped init refreshes the env block + IAM for ONE terragrunt module. It does NOT rebuild a
    stale code zip (use make build-lambdas + km init --lambdas) and does NOT provision new
    resources/tables/queues (use km init --dry-run=false)."

    1. CLAUDE.md — CLI section (the `km init` line): add --only <module> plus sugar
       --github / --slack / --h1 / --email (scoped single-module apply: env+IAM only).
       Note the tier-2 gated --only ses (destroy-class gate; no cheap alias). Optionally add a
       brief "Phase 105 (2026-06-11) — Scoped km init for bridge config (complete)" header block
       in the phase-notes style used by prior phases, with the boundary + deploy = make build.

    2. skills/init/SKILL.md — extend the "Fast-path variants" section (around lines 59-67) with
       km init --github / --slack / --h1 / --email (apply ONLY that bridge module: env block + IAM)
       and km init --only ses (tier-2 gated: SES/DNS, destroy-class gate, no cheap alias).
       Clarify the boundary vs --lambdas (code zip) vs --sidecars (binary) vs full km init (new
       resources). Note that scoped --github/--h1/--slack also republish SSM commands/bot-id, and
       that dry-run defaults true (use --dry-run=false to apply). If skill content changed
       materially, flag in the SUMMARY that plugin.json + marketplace.json versions must be bumped
       at release time (per project_plugin_version_gates_cache).

    3. OPERATOR-GUIDE.md — Init section: add scoped-apply guidance + the boundary note.

    4. docs/github-bridge.md — in the Deploy sections that say "Deploy = make build-lambdas +
       km init --dry-run=false (NOT --sidecars)", add: for a config-key-only edit (e.g.
       github.default_router, github.repos, github.commands) you can instead run
       km init --github --dry-run=false to apply just the bridge env block + IAM (Phase 105);
       code-zip changes still need make build-lambdas + km init --lambdas.

    5. docs/slack-notifications.md — same pattern with km init --slack --dry-run=false.

    6. docs/h1-bridge.md — same pattern with km init --h1 --dry-run=false.

    7. docs/operational-gotchas.md — add a note that --github/--slack/--h1 do NOT rebuild the
       Lambda zip (code changes still need make build-lambdas + full/--lambdas deploy).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q "km init --only" CLAUDE.md && grep -q "km init --github" skills/init/SKILL.md && grep -q "km init --only ses" skills/init/SKILL.md && grep -q "km init --github" docs/github-bridge.md && grep -q "km init --slack" docs/slack-notifications.md && grep -q "km init --h1" docs/h1-bridge.md && echo DOCS_OK</automated>
  </verify>
  <done>
    All 7 doc surfaces mention the scoped flags; grep gate prints DOCS_OK. Boundary note (env+IAM
    only; not code zip; not new resources) present in CLAUDE.md / SKILL.md / operational-gotchas.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — no-drift invariant + ses no-op</name>
  <action>Run the manual verification steps in <how-to-verify> on a live install; do not auto-run — pause for the operator. This is a checkpoint gate, not an automated task.</action>
  <what-built>
    Scoped km init (--only / --github / --slack / --h1 / --email) — env+IAM single-module apply
    with SSM side-effect republish (github/h1 commands, slack bot-id) and tier-2 gated --only ses.
    All unit tests green; km binary rebuilt with make build.
  </what-built>
  <how-to-verify>
    On a LIVE install (AWS_PROFILE=klanker-application), verify the no-drift invariant and ses no-op:

    1. No-drift invariant (the headline acceptance criterion):
       a. Edit a github.* config key in km-config.yaml — e.g. flip github.default_router false to true
          (revert after if you don't want it on).
       b. Run: km init --github --dry-run=false
          Expect: applies ONLY lambda-github-bridge in seconds (not the full ~27-module fleet);
          completes with the "Scoped init complete" footer.
       c. Confirm the bridge env block updated:
          aws lambda get-function-configuration --function-name km-github-bridge \
            --query 'Environment.Variables.KM_GITHUB_DEFAULT_ROUTER'
          Expect: "true".
       d. Run: km init --plan
          Expect: lambda-github-bridge plans as a NO-OP (no env change, no destroy-class trips) —
          proving the scoped apply derived from the identical yaml->KM_* pipeline (zero drift).

    2. Tier-2 ses no-op:
       a. Run: km init --only ses   (dry-run default true)
          Expect: the destroy-class gate reports NO protected destroy/replace and NO spurious
          aws_s3_bucket_policy change on a stable install; prints what would apply; does not apply.

    3. Spot-check a second alias (optional): km init --slack (dry-run) names only lambda-slack-bridge.

    4. Mutual-exclusion sanity: km init --github --plan  -> errors with mutual-exclusion message.
  </how-to-verify>
  <resume-signal>Type "approved" if no-drift + ses no-op both hold, or describe the drift/issue observed.</resume-signal>
</task>

</tasks>

<verification>
- grep gate: all 7 doc surfaces reference the scoped flags (DOCS_OK).
- Live UAT: km init --github --dry-run=false updates env block; km init --plan is a no-op afterward.
- Live UAT: km init --only ses (dry-run) shows no spurious bucket-policy destroy/replace.
- km init --github --plan errors (mutual exclusion).
</verification>

<success_criteria>
- INIT-SCOPED-DOCS: scoped flags + boundary documented on CLAUDE.md, SKILL.md, OPERATOR-GUIDE.md,
  operational-gotchas.md, github-bridge.md, slack-notifications.md, h1-bridge.md.
- INIT-SCOPED-IMPL (no-drift): live UAT confirms scoped apply causes zero drift on a subsequent full plan.
</success_criteria>

<output>
After completion, create `.planning/phases/105-scoped-km-init-for-bridge-config-km-init-only-module-with-sugar-aliases-github-slack-h1-to-apply-a-single-terragrunt-module-and-refresh-its-lambda-env-iam-instead-of-the-full-apply/105-05-SUMMARY.md`
</output>
