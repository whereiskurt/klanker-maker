---
phase: 01-schema-compiler-aws-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-03
files_modified:
  - cmd/km/main.go
  - internal/app/cmd/root.go
  - internal/app/cmd/validate.go
  - internal/app/cmd/validate_test.go
  - internal/app/config/config.go
autonomous: false
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05
  - INFR-07
user_setup:
  - service: aws
    why: "Multi-account structure required for sandbox provisioning"
    env_vars:
      - name: AWS_SSO_PROFILE
        source: "AWS Identity Center — create SSO permission sets for management, terraform, and application accounts"
    dashboard_config:
      - task: "Create three AWS accounts (management, terraform, application) in AWS Organizations"
        location: "AWS Console -> Organizations -> Create Account"
      - task: "Configure AWS SSO with permission sets for each account"
        location: "AWS Console -> IAM Identity Center -> Permission sets"
      - task: "Register domain in Route53 (management account)"
        location: "AWS Console -> Route53 -> Registered domains"
      - task: "Create hosted zone in application account and delegate NS records from management"
        location: "AWS Console -> Route53 -> Hosted zones"
      - task: "Create KMS key for SOPS encryption"
        location: "AWS Console -> KMS -> Customer managed keys (or via Terraform)"
      - task: "Create S3 artifact bucket with lifecycle policies"
        location: "AWS Console -> S3 (or via Terraform)"

must_haves:
  truths:
    - "Operator can run km validate <profile.yaml> and see validation pass for a valid profile"
    - "Operator can run km validate <profile.yaml> against an invalid profile and see clear error messages"
    - "km validate resolves extends chains before validating"
    - "AWS multi-account structure exists (management, terraform, application accounts)"
    - "Route53 hosted zone is delegated from management to application account"
    - "KMS key exists for SOPS encryption"
    - "S3 artifact bucket exists with lifecycle policies"
  artifacts:
    - path: "internal/app/cmd/validate.go"
      provides: "km validate Cobra command"
      exports: ["NewValidateCmd"]
      min_lines: 40
    - path: "internal/app/cmd/root.go"
      provides: "Root Cobra command with global flags"
      exports: ["NewRootCmd", "Execute"]
      min_lines: 20
    - path: "internal/app/config/config.go"
      provides: "Central config struct with Viper integration"
      exports: ["Config", "Load"]
      min_lines: 20
  key_links:
    - from: "cmd/km/main.go"
      to: "internal/app/cmd/root.go"
      via: "main calls Execute()"
      pattern: "cmd\\.Execute"
    - from: "internal/app/cmd/validate.go"
      to: "pkg/profile/validate.go"
      via: "calls profile.Validate()"
      pattern: "profile\\.Validate"
    - from: "internal/app/cmd/validate.go"
      to: "pkg/profile/inherit.go"
      via: "resolves extends before validation"
      pattern: "profile\\.Resolve"
---

<objective>
Wire the km validate CLI command using Cobra/Viper and verify AWS foundation infrastructure is in place for Phase 2 provisioning.

Purpose: `km validate` is the first user-facing command — operators need to validate profiles before Phase 2 adds `km create`. AWS infrastructure (accounts, Route53, KMS, S3) must exist before provisioning can begin.
Output: Working `km validate <profile.yaml>` command, verified AWS account structure.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-compiler-aws-foundation/01-CONTEXT.md
@.planning/phases/01-schema-compiler-aws-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 + Plan 03 outputs -->

From pkg/profile/validate.go (Plan 01):
- Validate(raw []byte) []ValidationError
- ValidationError{Path string, Message string}

From pkg/profile/inherit.go (Plan 03):
- Resolve(name string, searchPaths []string) (*SandboxProfile, error)

From pkg/profile/builtins.go (Plan 03):
- LoadBuiltin(name string) (*SandboxProfile, error)
- ListBuiltins() []string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: km validate CLI command with Cobra/Viper</name>
  <files>cmd/km/main.go, internal/app/cmd/root.go, internal/app/cmd/validate.go, internal/app/cmd/validate_test.go, internal/app/config/config.go</files>
  <action>
    1. Create `internal/app/config/config.go`:
       - Config struct with ProfileSearchPaths []string, LogLevel string, Version string
       - Load() function using Viper: reads from ~/.km/config.yaml, env vars (KM_ prefix), CLI flags
       - Default ProfileSearchPaths: ["./profiles", "~/.km/profiles"]

    2. Create `internal/app/cmd/root.go`:
       - NewRootCmd(cfg *config.Config) *cobra.Command
       - Root command "km" with version flag, log-level flag
       - Execute() function that creates Config, creates root cmd, adds subcommands, runs
       - Use zerolog for structured logging

    3. Create `internal/app/cmd/validate.go`:
       - NewValidateCmd(cfg *config.Config) *cobra.Command
       - Command: `km validate <profile.yaml> [profile2.yaml ...]`
       - For each file argument:
         a. Read file contents
         b. Parse to check for extends field
         c. If extends present: resolve inheritance chain using profile.Resolve() with cfg.ProfileSearchPaths
         d. Run profile.Validate() on the (resolved) profile bytes
         e. If errors: print each as "ERROR: {path}: {message}" to stderr, exit 1
         f. If valid: print "{filename}: valid" to stdout, exit 0
       - Support validating multiple files in one invocation
       - Exit code 1 if ANY file is invalid

    4. Update `cmd/km/main.go`:
       - Import internal/app/cmd
       - Call cmd.Execute()

    5. Create `internal/app/cmd/validate_test.go`:
       - Test valid profile file produces exit 0 and "valid" output
       - Test invalid profile file produces exit 1 and error messages
       - Test multiple files with one invalid produces exit 1
       - Use testdata/ fixtures from Plan 01

    6. Run `go mod tidy` and `go build ./cmd/km/`
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build -o /tmp/km ./cmd/km/ && /tmp/km validate profiles/open-dev.yaml && echo "PASS: valid profile" && /tmp/km validate testdata/profiles/invalid-unknown-field.yaml 2>&1; test $? -eq 1 && echo "PASS: invalid profile exits 1"</automated>
  </verify>
  <done>km validate accepts valid profiles (exit 0), rejects invalid profiles with clear error messages (exit 1), and resolves extends chains before validation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AWS foundation infrastructure readiness</name>
  <files>none</files>
  <action>
    Verify the following AWS resources exist as prerequisites for Phase 2 provisioning. These are human-managed infrastructure items that Claude cannot provision:

    1. AWS Accounts (INFR-01): Three accounts in AWS Organizations (management, terraform, application)
    2. AWS SSO (INFR-02): Identity Center configured with permission sets
    3. Route53 (INFR-03, INFR-07): Domain registered, hosted zone delegated
    4. KMS (INFR-04): Key provisioned for SOPS encryption
    5. S3 (INFR-05): Artifact bucket with lifecycle policies
  </action>
  <verify>
    <automated>echo "Manual verification required — see how-to-verify steps below"</automated>
  </verify>
  <done>AWS infrastructure readiness confirmed or gaps documented for setup before Phase 2</done>
  <how-to-verify>
    Confirm the following AWS resources exist (these are prerequisites for Phase 2 provisioning):

    1. **AWS Accounts (INFR-01):** Three accounts exist in AWS Organizations:
       - Management account (Route53 domain, Organizations root)
       - Terraform account (state bucket, DynamoDB lock table)
       - Application account (sandbox infrastructure target)

    2. **AWS SSO (INFR-02):** AWS Identity Center configured with permission sets for all three accounts. Run: `aws sso login --profile your-profile` succeeds.

    3. **Route53 (INFR-03, INFR-07):**
       - Domain registered in management account
       - Hosted zone in application account
       - NS delegation from management to application verified: `dig NS yourdomain +short` returns application account NS records

    4. **KMS (INFR-04):** KMS key exists for SOPS encryption. Run: `aws kms describe-key --key-id key-id --profile app-account-profile` returns key metadata.

    5. **S3 (INFR-05):** Artifact bucket exists with lifecycle policies. Run: `aws s3api get-bucket-lifecycle-configuration --bucket bucket-name --profile app-account-profile` returns lifecycle rules.

    If any of these are missing, they need to be set up before Phase 2.
  </how-to-verify>
  <resume-signal>Type "confirmed" with which items are ready, or list which items need setup</resume-signal>
</task>

</tasks>

<verification>
- `go build ./cmd/km/` succeeds
- `km validate profiles/open-dev.yaml` exits 0
- `km validate testdata/profiles/invalid-unknown-field.yaml` exits 1 with error messages
- `go test ./internal/app/cmd/... -v` passes
- AWS infrastructure readiness documented
</verification>

<success_criteria>
- `km validate` is a working CLI command that validates profiles with clear output
- Extends chains are resolved before validation
- AWS foundation status is verified and documented for Phase 2 readiness
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-compiler-aws-foundation/01-04-SUMMARY.md`
</output>
