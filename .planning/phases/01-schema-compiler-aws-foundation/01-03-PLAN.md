---
phase: 01-schema-compiler-aws-foundation
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - pkg/profile/inherit.go
  - pkg/profile/inherit_test.go
  - pkg/profile/builtins.go
  - pkg/profile/builtins_test.go
  - profiles/open-dev.yaml
  - profiles/restricted-dev.yaml
  - profiles/hardened.yaml
  - profiles/sealed.yaml
  - testdata/profiles/child-extends-open-dev.yaml
  - testdata/profiles/circular-a.yaml
  - testdata/profiles/circular-b.yaml
  - testdata/profiles/deep-inheritance.yaml
autonomous: true
requirements:
  - SCHM-04
  - SCHM-05

must_haves:
  truths:
    - "A child profile extending open-dev inherits parent values for omitted sections"
    - "A child profile overriding a parent's allowedDNSSuffixes REPLACES the list, not extends it"
    - "Circular inheritance (A extends B extends A) returns an error, not infinite recursion"
    - "Inheritance deeper than 3 levels returns a clear depth-exceeded error"
    - "All four built-in profiles (open-dev, restricted-dev, hardened, sealed) pass km validate"
    - "Built-in profiles have correct TTLs: open-dev 24h, restricted-dev 8h, hardened 4h, sealed 1h"
    - "All four profiles have all four sidecars enabled (DNS proxy, HTTP proxy, audit log, tracing)"
  artifacts:
    - path: "pkg/profile/inherit.go"
      provides: "Profile inheritance resolver with cycle detection"
      exports: ["Resolve"]
      min_lines: 40
    - path: "pkg/profile/builtins.go"
      provides: "Embedded built-in profiles via go:embed"
      exports: ["LoadBuiltin", "ListBuiltins"]
      min_lines: 20
    - path: "profiles/open-dev.yaml"
      provides: "Permissive built-in profile (24h TTL, wide allowlists)"
      contains: "open-dev"
    - path: "profiles/restricted-dev.yaml"
      provides: "Curated built-in profile (8h TTL, curated allowlists)"
      contains: "restricted-dev"
    - path: "profiles/hardened.yaml"
      provides: "Minimal-egress profile (4h TTL, AWS APIs + specific hosts)"
      contains: "hardened"
    - path: "profiles/sealed.yaml"
      provides: "Zero-egress profile (1h TTL, no network access)"
      contains: "sealed"
  key_links:
    - from: "pkg/profile/inherit.go"
      to: "pkg/profile/builtins.go"
      via: "Resolve calls LoadBuiltin for name-based extends lookup"
      pattern: "LoadBuiltin"
    - from: "pkg/profile/builtins.go"
      to: "profiles/*.yaml"
      via: "go:embed profiles/"
      pattern: "go:embed"
    - from: "pkg/profile/inherit.go"
      to: "pkg/profile/validate.go"
      via: "Resolved profile is validated as a whole"
      pattern: "Validate"
---

<objective>
Implement profile inheritance (extends resolution with cycle detection and depth limit) and create the four built-in profiles with graduated security policies.

Purpose: Operators can build custom profiles that extend built-in ones, overriding only what they need. Built-in profiles provide ready-to-use security presets from permissive to fully sealed.
Output: Working inheritance resolver tested against cycle, depth, and merge scenarios; four built-in profiles that pass validation.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-compiler-aws-foundation/01-CONTEXT.md
@.planning/phases/01-schema-compiler-aws-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these types -->

From pkg/profile/types.go (created in Plan 01):
- SandboxProfile struct with Extends string field
- Spec struct with all 10 section sub-structs
- Parse([]byte) (*SandboxProfile, error)

From pkg/profile/validate.go (created in Plan 01):
- Validate(raw []byte) []ValidationError
- ValidateSchema(raw []byte) []ValidationError
- ValidateSemantic(p *SandboxProfile) []ValidationError
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile inheritance resolver with cycle detection</name>
  <files>pkg/profile/inherit.go, pkg/profile/inherit_test.go, testdata/profiles/child-extends-open-dev.yaml, testdata/profiles/circular-a.yaml, testdata/profiles/circular-b.yaml, testdata/profiles/deep-inheritance.yaml</files>
  <behavior>
    - TestResolveInheritsParentValues: Child profile extending a parent inherits parent's lifecycle.ttl when child omits it
    - TestResolveChildOverridesParent: Child's allowedDNSSuffixes REPLACES parent's list (not union)
    - TestResolveChildWinsScalar: Child's runtime.substrate overrides parent's value
    - TestResolveCircularDetection: Profile A extends B, B extends A — returns error containing "circular"
    - TestResolveDepthExceeded: Chain of 4 profiles deep returns error containing "depth exceeded"
    - TestResolveMaxDepth3: Chain of exactly 3 profiles deep succeeds
    - TestResolveNonExistentParent: extends referencing unknown profile name returns clear error
    - TestResolveMetadataLabelsAdditive: metadata.labels from parent and child are merged (additive — the ONE exception to child-wins-replaces)
  </behavior>
  <action>
    1. Create test fixtures:
       - child-extends-open-dev.yaml: extends open-dev, overrides lifecycle.ttl to "2h" and network.egress.allowedDNSSuffixes to [".example.com"]
       - circular-a.yaml: extends circular-b
       - circular-b.yaml: extends circular-a
       - deep-inheritance.yaml: extends a chain 4 levels deep (exceeds max)
    2. Create `pkg/profile/inherit.go`:
       - Resolve(name string, searchPaths []string) (*SandboxProfile, error): public entry point
       - resolve() internal recursive function with visited map and depth counter
       - Max depth: 3 (locked decision)
       - Cycle detection via visited map — check before recursing
       - load(name string, searchPaths []string) (*SandboxProfile, error): searches built-in profiles first, then searchPaths directories
       - merge(parent, child *SandboxProfile) *SandboxProfile: child fields override parent for all values; allowlist arrays REPLACE (not extend); metadata.labels are additive (merged)
       - The merge function should iterate over each Spec section: if child section is zero-value, use parent's; otherwise use child's. For allowlist arrays specifically (allowedDNSSuffixes, allowedHosts, etc.), if child specifies them at all, child's array replaces parent's entirely.
    3. Write tests FIRST (RED), then implement to GREEN
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/profile/... -run "TestResolve" -v</automated>
  </verify>
  <done>Inheritance resolver correctly merges parent/child profiles with child-wins semantics, detects cycles, enforces 3-level depth limit, and looks up profiles by name (not path)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four built-in profiles with graduated security</name>
  <files>profiles/open-dev.yaml, profiles/restricted-dev.yaml, profiles/hardened.yaml, profiles/sealed.yaml, pkg/profile/builtins.go, pkg/profile/builtins_test.go</files>
  <behavior>
    - TestBuiltinProfilesExist: ListBuiltins() returns exactly ["open-dev", "restricted-dev", "hardened", "sealed"]
    - TestBuiltinProfilesValidate: Each built-in profile passes Validate() with zero errors
    - TestBuiltinOpenDevTTL: open-dev has lifecycle.ttl = "24h"
    - TestBuiltinRestrictedDevTTL: restricted-dev has lifecycle.ttl = "8h"
    - TestBuiltinHardenedTTL: hardened has lifecycle.ttl = "4h"
    - TestBuiltinSealedTTL: sealed has lifecycle.ttl = "1h"
    - TestBuiltinAllSidecarsEnabled: All four profiles have sidecars.dnsProxy.enabled, sidecars.httpProxy.enabled, sidecars.auditLog.enabled, sidecars.tracing.enabled all set to true
    - TestBuiltinNetworkGraduation: open-dev has broad allowedDNSSuffixes, sealed has empty/none
    - TestBuiltinSealedZeroEgress: sealed profile has no allowedDNSSuffixes and no allowedHosts
  </behavior>
  <action>
    1. Create four profile YAML files in profiles/:
       **open-dev.yaml** (permissive):
       - apiVersion: klankermaker.ai/v1alpha1, kind: SandboxProfile, metadata.name: open-dev
       - lifecycle: ttl 24h, idleTimeout 4h, teardownPolicy destroy
       - runtime: substrate ec2, spot true
       - network.egress: broad allowedDNSSuffixes ([".*"] or wide list), broad allowedHosts
       - All four sidecars enabled
       - All 10 sections populated (required by schema)

       **restricted-dev.yaml** (curated):
       - lifecycle: ttl 8h, idleTimeout 2h, teardownPolicy destroy
       - network.egress: curated allowedDNSSuffixes ([".github.com", ".githubusercontent.com", ".npmjs.org", ".pypi.org", ".golang.org", ".aws.amazon.com"]), curated allowedHosts
       - All four sidecars enabled

       **hardened.yaml** (minimal):
       - lifecycle: ttl 4h, idleTimeout 1h, teardownPolicy destroy
       - network.egress: minimal ([".aws.amazon.com"]), specific hosts only
       - All four sidecars enabled

       **sealed.yaml** (zero egress):
       - lifecycle: ttl 1h, idleTimeout 30m, teardownPolicy destroy
       - network.egress: empty allowedDNSSuffixes [], empty allowedHosts []
       - All four sidecars enabled

    2. Create `pkg/profile/builtins.go`:
       - Use `//go:embed ../../profiles/*.yaml` to embed all built-in profiles (adjust path based on Go embed rules — may need to embed from profiles/ relative to the package, which requires the embed directive to reference the correct relative path; if Go embed cannot reach ../../profiles, use a profiles/ symlink or move the embed directive to a package at the repo root)
       - ListBuiltins() []string: returns available built-in profile names
       - LoadBuiltin(name string) (*SandboxProfile, error): loads and parses a built-in profile by name
       - IsBuiltin(name string) bool: checks if a name is a built-in profile

    3. Write tests FIRST (RED), then create profiles and builtins.go to make them GREEN
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/profile/... -run "TestBuiltin" -v</automated>
  </verify>
  <done>Four built-in profiles exist, all pass validation, have correct TTLs, all four sidecars enabled on each, and network policy graduates from permissive (open-dev) to zero (sealed)</done>
</task>

</tasks>

<verification>
- `go test ./pkg/profile/... -v` passes all tests including inheritance and built-in profile tests
- Four YAML files exist in profiles/ and each passes `Validate()`
- Inheritance resolver correctly handles the merge contract (child-wins, allowlists replace)
</verification>

<success_criteria>
- Profile extending open-dev correctly inherits parent values and overrides specified fields
- Circular and depth-exceeded inheritance chains produce clear errors
- All four built-in profiles pass schema + semantic validation
- Network policy graduates: open-dev (permissive) -> restricted-dev (curated) -> hardened (minimal) -> sealed (zero)
- All profiles have all four sidecars enabled
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-compiler-aws-foundation/01-03-SUMMARY.md`
</output>
