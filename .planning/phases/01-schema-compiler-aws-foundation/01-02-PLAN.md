---
phase: 01-schema-compiler-aws-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/modules/network/v1.0.0/main.tf
  - infra/modules/network/v1.0.0/variables.tf
  - infra/modules/network/v1.0.0/outputs.tf
  - infra/modules/ec2spot/v1.0.0/main.tf
  - infra/modules/ec2spot/v1.0.0/variables.tf
  - infra/modules/ec2spot/v1.0.0/outputs.tf
  - infra/modules/ecs-cluster/v1.0.0/main.tf
  - infra/modules/ecs-cluster/v1.0.0/variables.tf
  - infra/modules/ecs-cluster/v1.0.0/outputs.tf
  - infra/modules/ecs-task/v1.0.0/main.tf
  - infra/modules/ecs-task/v1.0.0/variables.tf
  - infra/modules/ecs-task/v1.0.0/outputs.tf
  - infra/modules/ecs-service/v1.0.0/main.tf
  - infra/modules/ecs-service/v1.0.0/variables.tf
  - infra/modules/ecs-service/v1.0.0/outputs.tf
  - infra/modules/secrets/v1.0.0/main.tf
  - infra/modules/secrets/v1.0.0/variables.tf
  - infra/modules/secrets/v1.0.0/outputs.tf
  - infra/live/site.hcl
  - infra/live/terragrunt.hcl
  - infra/live/sandboxes/_template/terragrunt.hcl
autonomous: true
requirements:
  - INFR-06
  - INFR-08

must_haves:
  truths:
    - "All six Terraform modules exist under infra/modules/ with v1.0.0/ subdirectories"
    - "No file in infra/ contains references to defcon.run.34 naming (dc34, site.label dc34, AGENTS.md)"
    - "Network module does NOT contain ALB resources"
    - "EC2spot module does NOT contain SSH key pair or tls_private_key resources"
    - "EC2spot module includes IMDSv2 enforcement (http_tokens = required)"
    - "Terragrunt site.hcl uses km label and tf-km state prefix"
    - "Sandbox template terragrunt.hcl demonstrates per-sandbox directory isolation"
  artifacts:
    - path: "infra/modules/network/v1.0.0/main.tf"
      provides: "VPC, subnets, security groups, optional NAT gateway"
      min_lines: 30
    - path: "infra/modules/ec2spot/v1.0.0/main.tf"
      provides: "EC2 spot instance with IMDSv2, km:sandbox-id tag"
      min_lines: 30
    - path: "infra/modules/ecs-cluster/v1.0.0/main.tf"
      provides: "ECS cluster with Fargate capacity providers"
      min_lines: 15
    - path: "infra/modules/ecs-task/v1.0.0/main.tf"
      provides: "ECS task definition with sidecar container slots"
      min_lines: 20
    - path: "infra/modules/ecs-service/v1.0.0/main.tf"
      provides: "ECS Fargate service with FARGATE_SPOT capacity"
      min_lines: 15
    - path: "infra/modules/secrets/v1.0.0/main.tf"
      provides: "KMS key + SSM Parameter Store resources"
      min_lines: 15
    - path: "infra/live/site.hcl"
      provides: "Root Terragrunt config with km naming"
      contains: "label.*=.*km"
  key_links:
    - from: "infra/live/site.hcl"
      to: "infra/modules/"
      via: "module source paths"
      pattern: "label.*=.*\"km\""
    - from: "infra/live/sandboxes/_template/terragrunt.hcl"
      to: "infra/live/site.hcl"
      via: "find_in_parent_folders"
      pattern: "find_in_parent_folders"
---

<objective>
Copy and adapt all Terraform modules and Terragrunt patterns from defcon.run.34 into the Klanker Maker repo, establishing the infrastructure-as-code foundation with no cross-repo dependency.

Purpose: Phase 2 provisioning depends on these modules existing locally. Copying now eliminates the cross-repo dependency constraint.
Output: Six adapted Terraform modules in infra/modules/, Terragrunt hierarchy in infra/live/, all defcon.run.34 references replaced.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-compiler-aws-foundation/01-CONTEXT.md
@.planning/phases/01-schema-compiler-aws-foundation/01-RESEARCH.md

Source modules to copy from:
- ~/working/defcon.run.34/infra/terraform/modules/network/
- ~/working/defcon.run.34/infra/terraform/modules/ec2spot/
- ~/working/defcon.run.34/infra/terraform/modules/ecs-cluster/
- ~/working/defcon.run.34/infra/terraform/modules/ecs-task/
- ~/working/defcon.run.34/infra/terraform/modules/ecs-service/
- ~/working/defcon.run.34/infra/terraform/modules/secrets/
- ~/working/defcon.run.34/infra/terraform/live/site/site.hcl
- ~/working/defcon.run.34/infra/terraform/live/site/terragrunt.hcl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy and adapt network, ec2spot, and secrets modules</name>
  <files>infra/modules/network/v1.0.0/main.tf, infra/modules/network/v1.0.0/variables.tf, infra/modules/network/v1.0.0/outputs.tf, infra/modules/ec2spot/v1.0.0/main.tf, infra/modules/ec2spot/v1.0.0/variables.tf, infra/modules/ec2spot/v1.0.0/outputs.tf, infra/modules/secrets/v1.0.0/main.tf, infra/modules/secrets/v1.0.0/variables.tf, infra/modules/secrets/v1.0.0/outputs.tf</files>
  <action>
    Read each source module from ~/working/defcon.run.34/infra/terraform/modules/{network,ec2spot,secrets}/ (check for v1.0.0/ subdirectory pattern).

    **network module adaptations:**
    - REMOVE all ALB resources (aws_lb, aws_lb_listener, aws_lb_target_group, aws_security_group for ALB)
    - REMOVE ALB-related variables and outputs
    - KEEP: VPC, subnets (public + private), route tables, internet gateway, optional NAT gateway, security group skeleton
    - RENAME: Replace any `site.*` or `dc34` variable references with `km_*` equivalents (e.g. site_label -> km_label)
    - ADD: variable "sandbox_id" {} for tagging; add km:sandbox-id tag to all resources
    - Security group egress: Do NOT hard-code 0.0.0.0/0 — leave egress rules empty or with explicit proxy-port-only rules (Phase 2 will configure per-profile egress)

    **ec2spot module adaptations:**
    - REMOVE: tls_private_key, aws_key_pair, local_file resources (no SSH — SSM-only access)
    - REMOVE: DNS record creation resources (aws_route53_record)
    - REMOVE: SSH-related variables and outputs
    - KEEP: aws_spot_instance_request (or aws_instance with spot config), IAM instance profile, user_data
    - ADD: metadata_options block with http_tokens = "required" (IMDSv2 enforcement)
    - ADD: variable "sandbox_id" {} and km:sandbox-id tag on all resources
    - RENAME: site.* -> km_* variable pattern

    **secrets module adaptations:**
    - KEEP: KMS key resources, SSM Parameter Store resources
    - REMOVE: Secrets Manager resources if present (defer to Phase 2 evaluation)
    - ADD: variable "sandbox_id" {} for resource naming/tagging
    - RENAME: site.* -> km_* variable pattern
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -r "dc34\|defcon\|AGENTS\.md\|site\.label" infra/modules/network infra/modules/ec2spot infra/modules/secrets 2>/dev/null | wc -l | xargs test 0 -eq && echo "PASS: no defcon references" && test -f infra/modules/network/v1.0.0/main.tf && test -f infra/modules/ec2spot/v1.0.0/main.tf && test -f infra/modules/secrets/v1.0.0/main.tf && echo "PASS: all modules exist" && ! grep -q "aws_lb\b" infra/modules/network/v1.0.0/main.tf && echo "PASS: no ALB in network" && ! grep -q "tls_private_key\|aws_key_pair" infra/modules/ec2spot/v1.0.0/main.tf && echo "PASS: no SSH keys in ec2spot" && grep -q "http_tokens.*required" infra/modules/ec2spot/v1.0.0/main.tf && echo "PASS: IMDSv2 enforced"</automated>
  </verify>
  <done>Network module exists without ALB, ec2spot module exists without SSH keys and with IMDSv2 enforced, secrets module exists with KMS+SSM — all references to defcon.run.34 naming are replaced with km naming</done>
</task>

<task type="auto">
  <name>Task 2: Copy ECS modules + Terragrunt hierarchy + sandbox template</name>
  <files>infra/modules/ecs-cluster/v1.0.0/main.tf, infra/modules/ecs-cluster/v1.0.0/variables.tf, infra/modules/ecs-cluster/v1.0.0/outputs.tf, infra/modules/ecs-task/v1.0.0/main.tf, infra/modules/ecs-task/v1.0.0/variables.tf, infra/modules/ecs-task/v1.0.0/outputs.tf, infra/modules/ecs-service/v1.0.0/main.tf, infra/modules/ecs-service/v1.0.0/variables.tf, infra/modules/ecs-service/v1.0.0/outputs.tf, infra/live/site.hcl, infra/live/terragrunt.hcl, infra/live/sandboxes/_template/terragrunt.hcl</files>
  <action>
    Read each source module from ~/working/defcon.run.34/infra/terraform/modules/{ecs-cluster,ecs-task,ecs-service}/.

    **ecs-cluster module adaptations:**
    - KEEP: aws_ecs_cluster, capacity provider configuration (FARGATE + FARGATE_SPOT)
    - RENAME: site.* -> km_* variables
    - ADD: variable "sandbox_id" {} and km:sandbox-id tag

    **ecs-task module adaptations:**
    - KEEP: aws_ecs_task_definition with container definitions, IAM task role, task execution role
    - The task definition structure must support sidecar containers (DNS proxy, HTTP proxy, audit log, tracing) alongside the main container — ensure container_definitions is parameterized, not hard-coded
    - RENAME: site.* -> km_* variables
    - ADD: variable "sandbox_id" {} and km:sandbox-id tag

    **ecs-service module adaptations:**
    - KEEP: aws_ecs_service with capacity_provider_strategy for FARGATE_SPOT
    - REMOVE: Load balancer configuration if present (sandboxes don't need LB)
    - RENAME: site.* -> km_* variables
    - ADD: variable "sandbox_id" {} and km:sandbox-id tag

    **Terragrunt hierarchy:**
    - Copy ~/working/defcon.run.34/infra/terraform/live/site/site.hcl to infra/live/site.hcl
    - Replace find_in_parent_folders("AGENTS.md") with find_in_parent_folders("CLAUDE.md") — or another km-specific anchor file
    - Replace label = "dc34" with label = "km"
    - Replace tf_state_prefix = "tf-dc34" with tf_state_prefix = "tf-km"
    - Replace KMGUID env var references if needed
    - Keep SOPS integration pattern (jsondecode + run_cmd sops decrypt)
    - Create infra/live/terragrunt.hcl with standard backend config (S3 + DynamoDB)
    - Create infra/live/sandboxes/_template/terragrunt.hcl demonstrating per-sandbox directory isolation:
      - include root site.hcl
      - terraform source pointing to modules
      - inputs block showing expected variables (sandbox_id, km_label, etc.)
      - State key includes sandbox_id for isolation (INFR-06)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -r "dc34\|defcon\|AGENTS\.md" infra/modules/ecs-cluster infra/modules/ecs-task infra/modules/ecs-service infra/live/ 2>/dev/null | wc -l | xargs test 0 -eq && echo "PASS: no defcon references" && test -f infra/modules/ecs-cluster/v1.0.0/main.tf && test -f infra/modules/ecs-task/v1.0.0/main.tf && test -f infra/modules/ecs-service/v1.0.0/main.tf && echo "PASS: ECS modules exist" && test -f infra/live/site.hcl && test -f infra/live/sandboxes/_template/terragrunt.hcl && echo "PASS: Terragrunt hierarchy exists" && grep -q "km" infra/live/site.hcl && echo "PASS: km label in site.hcl"</automated>
  </verify>
  <done>ECS modules (cluster, task, service) exist with km naming, Terragrunt hierarchy is rooted with km config, sandbox template demonstrates per-sandbox directory isolation with state-key-based isolation</done>
</task>

</tasks>

<verification>
- All six module directories exist: infra/modules/{network,ec2spot,ecs-cluster,ecs-task,ecs-service,secrets}/v1.0.0/
- `grep -r "dc34\|defcon\|AGENTS\.md" infra/` returns zero matches
- infra/live/site.hcl contains label = "km" and tf_state_prefix = "tf-km"
- Network module has no ALB resources
- EC2spot module has no SSH key resources and has IMDSv2 enforcement
- Sandbox template terragrunt.hcl includes sandbox_id in state key path
</verification>

<success_criteria>
- Six Terraform modules copied, renamed, and trimmed — no cross-repo dependency on defcon.run.34
- Terragrunt hierarchy established with km-specific configuration
- Per-sandbox directory isolation template ready for Phase 2 compiler to generate
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-compiler-aws-foundation/01-02-SUMMARY.md`
</output>
