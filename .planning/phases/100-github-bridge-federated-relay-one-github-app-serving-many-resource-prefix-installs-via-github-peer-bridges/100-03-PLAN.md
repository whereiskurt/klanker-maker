---
phase: 100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges
plan: 03
type: tdd
wave: 2
depends_on: ["100-02"]
files_modified:
  - pkg/github/bridge/webhook_handler.go
  - pkg/github/bridge/webhook_handler_phase100_test.go
  - pkg/github/bridge/webhook_handler_phase98_test.go
  - cmd/km-github-bridge/main.go
autonomous: true
requirements: [GH-FED-REORDER, GH-FED-LOOPGUARD, GH-FED-SCALE]
must_haves:
  truths:
    - "Resolve(owner/repo) runs immediately after the PR-only filter (~line 198), BEFORE the thread-lookup (4b, line 211) and the @-mention filter (5, line 224); its (alias, profile, allow, matched) return values still flow unchanged into the downstream allowlist/dedupe/command-pass/dispatch on the matched path"
    - "On !matched + NOT relayed: if Relayer != nil, Broadcast the verbatim body + GitHub headers to peers and return 200; if Relayer == nil, return 200 (today's silent no-config drop) — byte-identical to Phase 97/98 when federation off"
    - "On !matched + relayed (X-KM-Relayed present): DROP, log github_relay_no_owner, return 200 — NEVER re-relay (single-hop loop guard)"
    - "On matched: the full matched path (thread-lookup 4b, mention 5, allowlist, dedupe, command-pass, dispatch, 👀) runs in the SAME relative order as today — dispatch outcomes are byte-identical, including the Phase 98 thread-bypass"
    - "SCALE FIX (federation OFF, Threads configured): a created PR comment on a repo NOT in github.repos performs ZERO Threads.LookupSandbox DDB read (resolve-miss short-circuits before 4b); a comment on an OWNED repo still performs the lookup"
    - "cmd/km-github-bridge/main.go reads KM_GITHUB_PEER_BRIDGES, splits on comma / trims / drops empties, builds an HTTPPeerRelayer, and sets WebhookHandler.Relayer (nil when the list is empty)"
  artifacts:
    - path: "pkg/github/bridge/webhook_handler.go"
      provides: "Relayer field on WebhookHandler + reordered Handle() with !matched relay/drop branch"
      contains: "Relayer PeerRelayer"
    - path: "pkg/github/bridge/webhook_handler_phase100_test.go"
      provides: "{relayed?, matched?} decision table, reorder correctness, loop guard, no-wasted-read"
      contains: "DecisionTable"
    - path: "cmd/km-github-bridge/main.go"
      provides: "KM_GITHUB_PEER_BRIDGES parse + HTTPPeerRelayer build + Relayer injection"
      contains: "KM_GITHUB_PEER_BRIDGES"
  key_links:
    - from: "Handle() !matched branch"
      to: "h.Relayer.Broadcast(ctx, req.RawBody, req.Headers)"
      via: "only when Relayer != nil AND request not already relayed (x-km-relayed absent)"
      pattern: "Broadcast"
    - from: "cmd/km-github-bridge/main.go env parse"
      to: "WebhookHandler.Relayer"
      via: "strings.Split(KM_GITHUB_PEER_BRIDGES, \",\") -> HTTPPeerRelayer -> webhookHandler.Relayer"
      pattern: "Relayer"
---

<objective>
Make `Resolve()` ownership unconditional-first in `WebhookHandler.Handle`, add the `!matched`
relay/drop branch (the loop-guard decision table), add the `Relayer PeerRelayer` field, and wire
the relayer in `cmd/km-github-bridge/main.go` from `KM_GITHUB_PEER_BRIDGES`. This is the
HIGHEST-RISK plan: the reorder must produce BYTE-IDENTICAL dispatch outcomes (including the Phase 98
thread-bypass), and it doubles as the 700-repo scale fix (no wasted `LookupSandbox` DDB read on
unowned repos).

The reorder moves `Resolve()` (currently step 6, line 232) to immediately after the PR-only filter
(~line 198), BEFORE the thread-lookup (4b, line 211) and the @-mention filter (5, line 224). On
`!matched` it relays (front door) or drops (relayed) or returns 200 (no-config). On `matched`, the
ENTIRE matched path — thread-lookup, mention, allowlist, dedupe, command-pass, dispatch, 👀 — runs
in the same relative order as today. Depends on Plan 02 (the `PeerRelayer` interface + `HTTPPeerRelayer`).

Purpose: This is the behavioral heart of the phase. The byte-identity invariant (CLAUDE.md dormancy
rule) and the Phase 98 thread-bypass regression are the things most likely to break.
Output: reordered + relay-branched `Handle()`, the `Relayer` field, the cmd wiring, and the Phase 100
decision-table/reorder/loop-guard/no-wasted-read tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-RESEARCH.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-VALIDATION.md
@pkg/github/bridge/webhook_handler.go
@pkg/github/bridge/webhook_handler_phase98_test.go
@pkg/github/bridge/handle_test.go
@cmd/km-github-bridge/main.go

<interfaces>
<!-- All anchors verified against the live tree 2026-06-08. -->

PeerRelayer (from Plan 02, pkg/github/bridge/interfaces.go):
  Broadcast(ctx context.Context, rawBody []byte, ghHeaders map[string]string) error

Current Handle() order (webhook_handler.go, verified line numbers):
  step1 sig verify        line 156-157  VerifyGitHubSignature — UNCHANGED, stays FIRST
        event-type filter line 163      (issue_comment only)
  step2 parse payload     line 171      json.Unmarshal -> payload
        action!="created" line 175
  step3 loop guard (Bot)  line 186-187
  step4 PR-only filter    line 194      (Issue.PullRequest != nil)
  step4b thread Lookup     line 211      h.Threads.LookupSandbox(...)  ← the wasted DDB read
  step5 @-mention filter  line 224-226
  step6 Resolve(...)      line 232      alias, profile, allow, matched := Resolve(repo, h.Entries, h.DefaultProfile)
        !matched -> 200   line 233      (today's "no repo config" return)
        allowlist         line 238
  step7 dedupe GUID       line 246      req.Headers["x-github-delivery"]
        command pass      line 271
        dispatch (3-way)  line ~338
  step10 👀 reaction      line ~446

Target order (100-RESEARCH.md "The Resolve() reorder", lines 91-117):
  ...after PR-only filter (~198): run Resolve(); on !matched apply the decision branch
  (code in 100-RESEARCH.md lines 226-252); on matched, fall through to thread-lookup(4b),
  mention(5), allowlist, dedupe, command-pass, dispatch, 👀 — UNCHANGED.

Decision table (GH-FED-LOOPGUARD; 100-RESEARCH.md lines 119-130):
  relayed=req.Headers["x-km-relayed"] != ""   (Function URL headers are lowercased)
  | x-km-relayed | matched | action |
  | absent  | yes | process locally (full matched path) |
  | absent  | no  | Relayer.Broadcast if Relayer!=nil else 200 no-op; return 200 |
  | present | yes | process locally |
  | present | no  | drop, log github_relay_no_owner, return 200 — NEVER re-relay |

cmd wiring template (cmd/km-github-bridge/main.go):
  - KM_GITHUB_REPOS parse: line 105-124 (env read pattern)
  - WebhookHandler construct: line 212  webhookHandler = &bridge.WebhookHandler{...}
  - lowercaseHeaders: line 310-313 (Function URL headers normalized to lowercase before Handle)
  - Slack template for the parse+build: cmd/km-slack-bridge/main.go:264-282

mockGitHubThreadStore (webhook_handler_phase98_test.go:21) — LookupSandbox at line 33 currently has
NO call counter; the no-wasted-read test must add a `lookupCalls int` counter incremented in
LookupSandbox so the SCALE test can assert it stays 0 on the unowned path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Phase 100 decision-table / reorder / loop-guard / no-wasted-read tests</name>
  <files>pkg/github/bridge/webhook_handler_phase100_test.go, pkg/github/bridge/webhook_handler_phase98_test.go</files>
  <behavior>
    - DecisionTable (GH-FED-REORDER/LOOPGUARD): 4 rows {relayed?, matched?}. Use a fake PeerRelayer
      mock that records Broadcast calls.
        absent+matched  -> processes locally (dispatch path invoked; Broadcast NOT called)
        absent+!matched -> Broadcast called once with all peers (Relayer set); returns 200
        present+matched -> processes locally; Broadcast NOT called
        present+!matched-> DROP: Broadcast NOT called; returns 200; (assert log/no dispatch)
    - Reorder (GH-FED-REORDER): a no-@-mention KNOWN-THREAD follow-up on a PEER-OWNED repo
      (!matched at this install) is RELAYED (front door) — NOT dropped at the mention filter; and on
      an OWNED repo a no-mention known-thread follow-up still dispatches (Phase 98 thread-bypass
      preserved). Reuse/extend the Phase 98 thread-bypass fixtures.
    - LoopGuard (GH-FED-LOOPGUARD): a request with X-KM-Relayed:1 + !matched never invokes the
      relayer (assert mock Broadcast count == 0) and never re-relays.
    - NoWastedRead (GH-FED-SCALE): federation OFF (Relayer nil) + Threads configured. A created PR
      comment on a repo NOT in Entries performs ZERO LookupSandbox calls (assert
      threadMock.lookupCalls == 0) and returns 200; a comment on an OWNED repo performs exactly the
      lookup it does today (lookupCalls >= 1). To enable the assertion, ADD a `lookupCalls int`
      counter to mockGitHubThreadStore (webhook_handler_phase98_test.go:21) incremented in
      LookupSandbox (line 33).
    Name tests so they match `-run 'DecisionTable|Reorder'`, `-run LoopGuard`, `-run NoWastedRead`.
  </behavior>
  <action>
    Create pkg/github/bridge/webhook_handler_phase100_test.go. Add a `mockPeerRelayer` with a
    `broadcastCalls int` (and captured args) satisfying bridge.PeerRelayer. Build WebhookRequest
    fixtures with valid signatures (reuse the signing helper already used by the Phase 98 tests —
    check handle_test.go / webhook_handler_phase98_test.go for the existing sign helper) so requests
    pass step-1 sig verify. Use synthetic owner/repo names. Extend mockGitHubThreadStore in
    webhook_handler_phase98_test.go with a `lookupCalls int` field incremented in LookupSandbox (do
    NOT change its return semantics — keep all existing Phase 98 tests green). These tests reference
    the Relayer field + reordered branch that do not exist yet — RED.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'DecisionTable|Reorder|LoopGuard|NoWastedRead' -count=1 2>&amp;1 | tail -10</automated>
  </verify>
  <done>webhook_handler_phase100_test.go exists and FAILS (RED); mockGitHubThreadStore has a lookupCalls counter; all PRE-EXISTING Phase 98 tests still compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — reorder Handle(), add Relayer field + !matched decision branch</name>
  <files>pkg/github/bridge/webhook_handler.go</files>
  <behavior>
    - All Task 1 tests pass AND every existing pkg/github/bridge test stays green (no regression).
    - Resolve() now runs after the PR-only filter, before thread-lookup + mention. The !matched
      branch implements the 4-row decision table exactly. The matched path is byte-identical in
      order/behavior to today (thread-lookup, mention, allowlist, dedupe, command-pass, dispatch, 👀).
  </behavior>
  <action>
    1. Add `Relayer PeerRelayer` field to the WebhookHandler struct (line 61 area); document
       nil ⇒ federation off ⇒ resolve-miss returns 200 as today.
    2. Reorder Handle(): MOVE the `Resolve(...)` call (line 232) and capture of
       (alias, profile, allow, matched) up to immediately AFTER the PR-only filter (~line 198),
       BEFORE the thread-lookup block (4b, line 202-222) and the mention filter (5, line 224-230).
    3. Replace the old `!matched -> 200` (line 233) with the decision branch from 100-RESEARCH.md
       lines 226-252:
         relayed := req.Headers["x-km-relayed"] != ""
         if relayed { log github_relay_no_owner (Warn); return 200 }   // terminal drop
         if h.Relayer != nil { if err := h.Relayer.Broadcast(ctx, req.RawBody, req.Headers); err != nil { Warn } }
         else { log "no repo config, silent drop" (Info) }
         return 200
    4. On matched: keep the thread-lookup(4b), mention(5), allowlist, dedupe, command-pass, dispatch,
       👀 EXACTLY as today — only their POSITION relative to Resolve() changed. The
       alias/profile/allow values from the earlier Resolve() must flow into the SAME downstream
       allowlist (line 238) + command-pass (line 271) code (100-RESEARCH.md "Subtlety to preserve",
       lines 117).
    CRITICAL invariants (CLAUDE.md dormancy + RESEARCH Pitfall 3): the matched-path behavior and the
    Phase 98 thread-bypass must be byte-identical; only the !matched early-exit and the position of
    Resolve() change. Add a comment block citing GH-FED-REORDER + the byte-identity argument (a thread
    row only ever exists for an owned repo).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -count=1 2>&amp;1 | tail -8</automated>
  </verify>
  <done>Full pkg/github/bridge suite GREEN (Phase 100 tests + all Phase 97/98/99 tests); Resolve() runs before thread-lookup/mention; the !matched branch implements the 4-row table; Relayer field present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the relayer in cmd/km-github-bridge/main.go from KM_GITHUB_PEER_BRIDGES</name>
  <files>cmd/km-github-bridge/main.go</files>
  <action>
    Before the WebhookHandler is constructed (line 212), read KM_GITHUB_PEER_BRIDGES, split on ",",
    strings.TrimSpace each, drop empties (model on cmd/km-slack-bridge/main.go:264-282 and the
    existing KM_GITHUB_REPOS read at line 105-124). If the resulting slice is non-empty, build
    `&bridge.HTTPPeerRelayer{PeerURLs: peers, HTTPClient: <the existing/cold-start http.Client>}` and
    set `webhookHandler.Relayer = relayer` (set on the struct literal at line 212 or immediately
    after). If empty, leave Relayer nil (federation off). slog.Info the configured peer count (or a
    dormant line when empty), mirroring the KM_GITHUB_REPOS dormant log at line 124. Reuse an existing
    http.Client if main.go already builds one for GitHub API calls; otherwise use a package client
    with a sane timeout consistent with relayBroadcastTimeout.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go build ./cmd/km-github-bridge/... &amp;&amp; grep -q "KM_GITHUB_PEER_BRIDGES" cmd/km-github-bridge/main.go &amp;&amp; grep -q "HTTPPeerRelayer" cmd/km-github-bridge/main.go &amp;&amp; echo WIRED</automated>
  </verify>
  <done>cmd/km-github-bridge builds; reads KM_GITHUB_PEER_BRIDGES, builds HTTPPeerRelayer when non-empty, sets WebhookHandler.Relayer (nil when empty).</done>
</task>

</tasks>

<verification>
- `go test ./pkg/github/bridge/... -count=1` GREEN (full suite — no Phase 97/98/99 regression)
- `go test ./pkg/github/bridge/... -run 'DecisionTable|Reorder|LoopGuard|NoWastedRead' -count=1` GREEN
- `go build ./cmd/km-github-bridge/...` succeeds
- Byte-identity: with Relayer nil, a resolve-miss returns 200 with NO LookupSandbox read (NoWastedRead test) and matched-path dispatch is unchanged from Phase 98
</verification>

<success_criteria>
GH-FED-REORDER + GH-FED-LOOPGUARD + GH-FED-SCALE satisfied: Resolve() runs unconditionally ahead of
the thread-lookup/mention filter; the 4-row decision table (relay/process/drop) is correct;
X-KM-Relayed:1 is a terminal single-hop loop guard; the matched path + Phase 98 thread-bypass are
byte-identical; an unowned-repo PR comment performs zero LookupSandbox DDB read; the relayer is wired
from KM_GITHUB_PEER_BRIDGES (nil ⇒ dormant).
</success_criteria>

<output>
After completion, create `.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-03-SUMMARY.md`
</output>
