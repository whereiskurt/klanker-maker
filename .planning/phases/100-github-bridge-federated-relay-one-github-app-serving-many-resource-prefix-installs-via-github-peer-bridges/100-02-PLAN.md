---
phase: 100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/github/bridge/interfaces.go
  - pkg/github/bridge/relayer.go
  - pkg/github/bridge/relayer_test.go
autonomous: true
requirements: [GH-FED-RELAY, GH-FED-VERIFY]
must_haves:
  truths:
    - "A PeerRelayer interface exists with a SINGLE method Broadcast(ctx, rawBody []byte, ghHeaders map[string]string) error — fire-and-forget, NO []PeerClaimResult return (Phase-95-era shape, NOT the Phase-96 claim-aware scatter-gather)"
    - "HTTPPeerRelayer.Broadcast POSTs rawBody verbatim to every PeerURL in parallel and forwards X-Hub-Signature-256, X-GitHub-Event, X-GitHub-Delivery verbatim plus adds X-KM-Relayed: 1"
    - "Broadcast is SYNCHRONOUS: it calls wg.Wait() before returning (Lambda freezes on return — RESEARCH Pitfall 1)"
    - "Broadcast runs under a bounded context (relayBroadcastTimeout, ~5s) and a failing/slow peer is logged and non-fatal (returns nil)"
    - "Empty PeerURLs ⇒ Broadcast is a no-op returning nil (dormancy)"
    - "A relayed request carrying the forwarded X-Hub-Signature-256 over the verbatim body passes VerifyGitHubSignature with the same shared secret (peer re-verify)"
  artifacts:
    - path: "pkg/github/bridge/interfaces.go"
      provides: "PeerRelayer interface declaration"
      contains: "PeerRelayer interface"
    - path: "pkg/github/bridge/relayer.go"
      provides: "HTTPPeerRelayer impl + relayBroadcastTimeout const + Broadcast"
      contains: "func (r *HTTPPeerRelayer) Broadcast"
    - path: "pkg/github/bridge/relayer_test.go"
      provides: "RED-first relayer unit tests (headers, parallel, bounded ctx, failing peer, empty no-op) + RelayedVerify HMAC re-verify"
      contains: "TestHTTPPeerRelayer"
  key_links:
    - from: "HTTPPeerRelayer.Broadcast"
      to: "each peer bridge Function URL (POST)"
      via: "http.Client.Do with X-KM-Relayed:1 + forwarded GitHub headers, parallel under bounded ctx, wg.Wait()"
      pattern: "X-KM-Relayed"
    - from: "forwarded X-Hub-Signature-256 + verbatim body"
      to: "VerifyGitHubSignature on the peer"
      via: "verbatim body forward ⇒ same HMAC verifies with shared secret"
      pattern: "VerifyGitHubSignature"
---

<objective>
Build the GitHub peer relayer: a `PeerRelayer` interface in `pkg/github/bridge/interfaces.go`
and an `HTTPPeerRelayer` impl in a new `pkg/github/bridge/relayer.go`. This is a FIRE-AND-FORGET
broadcast (Phase-95-era shape): `Broadcast(ctx, rawBody []byte, ghHeaders map[string]string) error`,
POSTing the verbatim webhook to every peer in parallel under a bounded context, forwarding the
GitHub auth/routing headers + adding `X-KM-Relayed: 1`, synchronously (`wg.Wait()`).

CRITICAL: copy the STRUCTURE of `pkg/slack/bridge/relayer.go` (synchronous wg.Wait, bounded
context, parallel POSTs, X-KM-Relayed loop guard) but DROP all Phase-96 claim machinery —
`peerRelayResponse`, `PeerClaimResult`, response-body parsing. The current live Slack relayer
returns `([]PeerClaimResult, error)`; the GitHub one returns plain `error`. The orphan-reply
(claim-aware) is deferred to Phase 101 (see 100-RESEARCH.md Anti-Patterns + ROADMAP Phase 101).

Purpose: Plan 03 wires this relayer into the `webhook_handler.go` `!matched` branch and cmd/main.go
builds it from the parsed peer list. This plan delivers the engine + interface in isolation, fully
unit-tested with an httptest server (no AWS).
Output: PeerRelayer interface + HTTPPeerRelayer impl + relayer_test.go.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-RESEARCH.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-VALIDATION.md
@pkg/github/bridge/interfaces.go
@pkg/slack/bridge/relayer.go
@pkg/slack/bridge/events_interfaces.go
@pkg/github/bridge/webhook_handler.go

<interfaces>
<!-- All anchors verified against the live tree 2026-06-08. -->

Slack template (COPY STRUCTURE, DROP CLAIM MACHINERY):
  pkg/slack/bridge/relayer.go:
    - peerRelayResponse (line 15-19), PeerClaimResult (Phase 96, line 31), Broadcast returns
      ([]PeerClaimResult, error) (line 65) — ALL of this is OUT OF SCOPE; do NOT port it.
    - DO port: HTTPPeerRelayer struct (line 41: PeerURLs []string, HTTPClient *http.Client),
      the sync.WaitGroup + context.WithTimeout pattern, the X-KM-Relayed header, wg.Wait()
      before return (the documented Lambda-freeze PITFALL).
  pkg/slack/bridge/events_interfaces.go:44: `PeerRelayer interface` (Slack's has the claim return;
    the GitHub one is simpler — plain error).

GitHub target shape (from 100-RESEARCH.md "Relayer impl pattern", lines 132-169):
  type PeerRelayer interface {
      Broadcast(ctx context.Context, rawBody []byte, ghHeaders map[string]string) error
  }
  type HTTPPeerRelayer struct { PeerURLs []string; HTTPClient *http.Client }
  const relayBroadcastTimeout = 5 * time.Second
  // forwards ghHeaders["x-hub-signature-256"], ["x-github-event"], ["x-github-delivery"]
  // (Lambda Function URL headers are lowercased — lowercaseHeaders in cmd/km-github-bridge/main.go:313)
  // adds X-KM-Relayed: 1 ; Content-Type: application/json

HMAC verify (for the RelayedVerify test): VerifyGitHubSignature(secret, sigHeader, rawBody)
  at pkg/github/bridge/webhook_handler.go:464.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — relayer unit tests (headers, parallel, bounded, failing peer, empty no-op)</name>
  <files>pkg/github/bridge/relayer_test.go</files>
  <behavior>
    - TestHTTPPeerRelayer_Broadcast_ForwardsHeaders: stand up 2 httptest.Servers; Broadcast a known
      rawBody with ghHeaders {x-hub-signature-256, x-github-event, x-github-delivery}; assert EACH
      server received the verbatim body, the three forwarded headers verbatim, AND X-KM-Relayed: 1,
      AND Content-Type application/json.
    - TestHTTPPeerRelayer_Broadcast_AllPeers / parallel: with N peers, all N receive the POST
      (count via atomic counter or per-server hit flags).
    - TestHTTPPeerRelayer_Broadcast_FailingPeerNonFatal: one peer returns 500 / one is an
      unreachable URL; Broadcast still returns nil and the healthy peer still received the POST.
    - TestHTTPPeerRelayer_Broadcast_BoundedContext: a peer that sleeps longer than
      relayBroadcastTimeout does not hang Broadcast (Broadcast returns within ~timeout; assert via
      elapsed bound). Keep the timeout test fast (use a short HTTPClient or rely on the bounded ctx).
    - TestHTTPPeerRelayer_Broadcast_EmptyPeers_NoOp: PeerURLs empty ⇒ returns nil, no panic.
    - TestHTTPPeerRelayer_RelayedVerify (GH-FED-VERIFY): compute X-Hub-Signature-256 over a body with
      a known secret; have the httptest peer recompute via VerifyGitHubSignature(secret, fwdSig,
      receivedBody) and assert it passes — proving the verbatim forward preserves the HMAC.
    Name the HMAC test so it matches `-run RelayedVerify`; name the others so they match `-run Relayer`.
  </behavior>
  <action>
    Create pkg/github/bridge/relayer_test.go (package bridge_test or bridge — match the existing
    test package convention in pkg/github/bridge; check relayer-adjacent tests). Use net/http/httptest
    to capture POSTs. These tests reference HTTPPeerRelayer / PeerRelayer / relayBroadcastTimeout which
    do not exist yet — RED. Use synthetic on.aws-style URLs only for any literal strings. For the HMAC
    test, reuse VerifyGitHubSignature (webhook_handler.go:464) to assert the forwarded signature still
    validates over the verbatim body.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'Relayer|RelayedVerify' -count=1 2>&amp;1 | tail -8</automated>
  </verify>
  <done>relayer_test.go exists and FAILS to compile / fails (RED) — HTTPPeerRelayer and PeerRelayer are not yet defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — PeerRelayer interface + HTTPPeerRelayer fire-and-forget impl</name>
  <files>pkg/github/bridge/interfaces.go, pkg/github/bridge/relayer.go</files>
  <behavior>
    - All Task 1 tests pass.
    - Broadcast forwards the three GitHub headers verbatim + X-KM-Relayed: 1, POSTs to all peers in
      parallel, waits (wg.Wait) before returning, bounds with relayBroadcastTimeout, tolerates a
      failing peer (logs, non-fatal), no-ops on empty PeerURLs.
    - NO PeerClaimResult / peerRelayResponse / response-body parsing anywhere (return type is error).
  </behavior>
  <action>
    1. interfaces.go: add
         `// PeerRelayer broadcasts an unowned webhook to sibling github-bridge installs.`
         `// Fire-and-forget (Phase 100); claim-aware scatter-gather is deferred to Phase 101.`
         `type PeerRelayer interface { Broadcast(ctx context.Context, rawBody []byte, ghHeaders map[string]string) error }`
       (add the context import if not present — check the file's existing imports).
    2. New pkg/github/bridge/relayer.go: implement exactly the pattern in 100-RESEARCH.md lines
       132-169. `const relayBroadcastTimeout = 5 * time.Second`. HTTPPeerRelayer{PeerURLs []string;
       HTTPClient *http.Client}. Broadcast: early-return nil on empty PeerURLs; context.WithTimeout
       (defer cancel); sync.WaitGroup; per-peer goroutine builds an http.NewRequestWithContext POST
       with Content-Type application/json, sets X-Hub-Signature-256 / X-GitHub-Event /
       X-GitHub-Delivery from ghHeaders (lowercase keys: ghHeaders["x-hub-signature-256"] etc.) and
       X-KM-Relayed "1"; on error slog.Warn with event="github_relay_peer_error" and return (non-fatal);
       resp.Body.Close() on success; wg.Wait() before return nil. Use r.HTTPClient (fall back to
       http.DefaultClient if nil — match the Slack relayer's handling). Add a comment block citing
       RESEARCH Pitfall 1 (Lambda freeze ⇒ must wg.Wait) and the "DROP Phase-96 claim machinery" note.
    3. Add a compile-time assertion `var _ PeerRelayer = (*HTTPPeerRelayer)(nil)`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'Relayer|RelayedVerify' -count=1 2>&amp;1 | tail -6 &amp;&amp; ! grep -q "PeerClaimResult" pkg/github/bridge/relayer.go &amp;&amp; echo NO_CLAIM_MACHINERY</automated>
  </verify>
  <done>All relayer + RelayedVerify tests pass; relayer.go contains NO PeerClaimResult / peerRelayResponse; Broadcast returns plain error and is synchronous (wg.Wait).</done>
</task>

</tasks>

<verification>
- `go test ./pkg/github/bridge/... -run 'Relayer|RelayedVerify' -count=1` GREEN
- `grep -L "PeerClaimResult" pkg/github/bridge/relayer.go` confirms NO claim machinery
- `grep -q "wg.Wait" pkg/github/bridge/relayer.go` — synchronous broadcast (Lambda-freeze safe)
- `grep -q "X-KM-Relayed" pkg/github/bridge/relayer.go` — loop-guard header set on every relayed POST
</verification>

<success_criteria>
GH-FED-RELAY + GH-FED-VERIFY satisfied: HTTPPeerRelayer broadcasts the verbatim webhook to all peers
in parallel under a bounded context, forwards GitHub auth/routing headers + X-KM-Relayed: 1
synchronously, tolerates failing peers, no-ops when empty; a relayed request's forwarded signature
re-verifies against the shared secret over the verbatim body. Phase-95-era fire-and-forget shape (no
claim machinery).
</success_criteria>

<output>
After completion, create `.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-02-SUMMARY.md`
</output>
