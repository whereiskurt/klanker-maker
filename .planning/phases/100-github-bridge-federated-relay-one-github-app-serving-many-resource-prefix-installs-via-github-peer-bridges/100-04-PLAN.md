---
phase: 100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges
plan: 04
type: tdd
wave: 3
depends_on: ["100-01", "100-03"]
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/doctor_github_test.go
  - docs/github-bridge.md
  - OPERATOR-GUIDE.md
  - CLAUDE.md
  - .planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-UAT.md
autonomous: true
requirements: [GH-FED-DOCTOR, GH-FED-E2E]
must_haves:
  truths:
    - "checkGitHubPeerBridges(peerBridges, ownBridgeURL) returns WARN on a malformed peer URL, WARN on a self-loop (peer URL == this install's own github bridge-url), and OK/SKIP on an empty list — mirroring checkSlackPeerBridges (doctor_slack.go:586)"
    - "The check is wired into km doctor's GitHub bridge check group (gated on github being configured) using cfg.Github.PeerBridges and the own bridge-url read from SSM {prefix}config/github/bridge-url (the path checkGitHubBridgeURL already reads at doctor.go:1059)"
    - "docs/github-bridge.md gains a Multi-install federated-relay operator runbook: config (github.peer_bridges), the front-door + symmetry model, the make build-lambdas + km init --dry-run=false (NOT --sidecars) deploy sequence, and the dormancy/byte-identity note"
    - "A 100-UAT.md two-install/one-App live runbook documents the GH-FED-E2E manual verification: exactly one 👀, correct dispatch by the owning install, no double-processing"
  artifacts:
    - path: "internal/app/cmd/doctor.go"
      provides: "checkGitHubPeerBridges + wiring into the GitHub bridge check group"
      contains: "checkGitHubPeerBridges"
    - path: "internal/app/cmd/doctor_github_test.go"
      provides: "RED-first unit tests for checkGitHubPeerBridges (malformed URL / self-loop / empty)"
      contains: "checkGitHubPeerBridges"
    - path: "docs/github-bridge.md"
      provides: "federated-relay operator runbook + deploy sequence"
      contains: "peer_bridges"
    - path: ".planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-UAT.md"
      provides: "two-install one-App E2E runbook (GH-FED-E2E)"
      contains: "peer_bridges"
  key_links:
    - from: "km doctor GitHub bridge check group"
      to: "checkGitHubPeerBridges(cfg.Github.PeerBridges, ownBridgeURL)"
      via: "gated on githubConfigured; ownBridgeURL from SSM {prefix}config/github/bridge-url"
      pattern: "checkGitHubPeerBridges"
---

<objective>
Add `km doctor` peer-bridge health checks for GitHub federation (malformed URL / self-loop / empty),
mirroring the Slack `checkSlackPeerBridges`, and write the operator-facing documentation: the
`docs/github-bridge.md` federated-relay runbook, the `OPERATOR-GUIDE.md` + `CLAUDE.md` Phase 100
notes, and the two-install/one-App E2E UAT runbook that satisfies the manual-only GH-FED-E2E.

This plan depends on Plan 01 (the `cfg.Github.PeerBridges` config field the check reads) and Plan 03
(the shipped relay behavior the docs/UAT describe). It is documentation-heavy + one self-contained
doctor check — deliberately separated from the high-risk reorder.

Purpose: Operators need a doctor that catches mis-configured peer URLs and a runbook for the
one-App-many-installs deploy + the live two-install verification. GH-FED-E2E is manual-only (requires
two live installs sharing one GitHub App webhook + real GitHub delivery) and ships as a documented
runbook, not an automated test (per 100-VALIDATION.md Manual-Only Verifications).
Output: checkGitHubPeerBridges + tests; docs/github-bridge.md runbook; OPERATOR-GUIDE.md + CLAUDE.md
notes; 100-UAT.md.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-RESEARCH.md
@.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-VALIDATION.md
@internal/app/cmd/doctor_slack.go
@internal/app/cmd/doctor.go
@docs/github-bridge.md

<interfaces>
<!-- All anchors verified against the live tree 2026-06-08. -->

Slack doctor template:
  internal/app/cmd/doctor_slack.go:575-586  checkSlackPeerBridges(peerBridges []string, ownBridgeURL string) CheckResult
    — url.Parse + scheme/host validation + self-loop (peer == ownBridgeURL) WARN semantics.
  Wiring template: doctor.go:3936  `r := checkSlackPeerBridges(peerBridges, ownBridgeURL)`

GitHub doctor anchors:
  doctor.go:1052-1075  checkGitHubBridgeURL — reads SSM param `{prefix}config/github/bridge-url`
    (line 1059) → this is the source of ownBridgeURL.
  doctor.go:3429+      GitHub bridge check group, gated on github being configured (non-empty repos).
  appConfigAdapter accessors: GetSlackPeerBridges() at doctor.go:308; GetGithubRepos at 321.
    Add a GetGithubPeerBridges() accessor returning a.cfg.Github.PeerBridges if the doctor reads
    config via the adapter; otherwise read cfg.Github.PeerBridges directly at the wiring site —
    match how the GitHub group already reads cfg.Github.Repos.

CheckResult shape: see other check funcs in doctor.go / doctor_slack.go (Status, Message,
Remediation fields).

Open question (100-RESEARCH.md OQ2): the "front-door-with-empty-peers" WARN is best-effort / may be
SKIPPED if the App's configured webhook isn't locally knowable. Implement the DETERMINISTIC checks
(malformed URL, self-loop, empty→OK/SKIP); do not block on the undeterminable front-door WARN.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN — checkGitHubPeerBridges + wiring into km doctor</name>
  <files>internal/app/cmd/doctor_github_test.go, internal/app/cmd/doctor.go</files>
  <behavior>
    - GithubPeerBridges doctor test (name matches `-run GithubPeerBridges`):
        empty list           -> OK or SKIP (no WARN)
        valid distinct URLs  -> OK
        malformed URL (e.g. "not a url", missing scheme/host) -> WARN naming the bad entry
        self-loop (peer URL == ownBridgeURL) -> WARN naming the self-loop
      Mirror the checkSlackPeerBridges test cases if one exists in the cmd package; otherwise model
      assertions on CheckResult.Status.
    - Implementation mirrors checkSlackPeerBridges (doctor_slack.go:586): url.Parse, require
      scheme+host, compare against ownBridgeURL for self-loop.
    - Wiring: invoked in the GitHub bridge check group (doctor.go:3429+), gated on github configured,
      passing cfg.Github.PeerBridges and ownBridgeURL from SSM {prefix}config/github/bridge-url.
  </behavior>
  <action>
    First write the RED test in internal/app/cmd/doctor_github_test.go (table-driven over
    checkGitHubPeerBridges). Then implement checkGitHubPeerBridges in doctor.go (or a doctor_github.go
    if that's the package convention — check where the other GitHub checks live) mirroring
    checkSlackPeerBridges (doctor_slack.go:586). Wire it into the GitHub bridge check group
    (doctor.go:3429+) gated on githubConfigured, reading ownBridgeURL via the same SSM param
    checkGitHubBridgeURL uses (doctor.go:1059). Add a GetGithubPeerBridges() adapter accessor if the
    group reads config through appConfigAdapter (next to GetGithubRepos at doctor.go:321). Note:
    `make test` excludes internal/app/cmd — run the explicit package path. Do NOT implement the
    undeterminable front-door-empty-peers WARN (RESEARCH OQ2) — deterministic checks only.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./internal/app/cmd/... -run GithubPeerBridges -count=1 2>&amp;1 | tail -6</automated>
  </verify>
  <done>checkGitHubPeerBridges tests pass; the check is wired into km doctor's GitHub group; malformed URL + self-loop → WARN, empty → OK/SKIP.</done>
</task>

<task type="auto">
  <name>Task 2: Operator docs — github-bridge.md runbook + OPERATOR-GUIDE.md + CLAUDE.md note</name>
  <files>docs/github-bridge.md, OPERATOR-GUIDE.md, CLAUDE.md</files>
  <action>
    1. docs/github-bridge.md — add a "§ Phase 100 — Federated relay (one App, many installs)" section
       (mirror the structure of docs/slack-notifications.md § Phase 95): the github.peer_bridges
       config surface; the front-door + full-symmetry model (every install lists every other);
       verbatim forward of body + X-Hub-Signature-256 + X-GitHub-Event + X-GitHub-Delivery +
       X-KM-Relayed:1; the single-hop loop guard; the per-install X-GitHub-Delivery dedupe; the
       owner-posts-the-single-👀 rule; the correctness invariant (each repo owned by exactly one
       install — documented, not enforced); km doctor peer checks; and the deploy sequence:
       `make build-lambdas` (clean) + `km init --dry-run=false` on each affected install — explicitly
       NOT `km init --sidecars` (env-block change requires full apply; cite
       project_km_init_lambdas_doesnt_deploy + project_km_init_skips_existing_lambda_zips). State the
       dormancy/byte-identity invariant and that NO SandboxProfile schema change ⇒ no sandbox
       recreate. If the doc has a "Multi-install Pattern B" placeholder (design spec line 285), mark
       it implemented. Use only synthetic on.aws Lambda URLs and generic install names (kph/sec).
    2. OPERATOR-GUIDE.md — add a short federated-relay subsection (or cross-link to
       docs/github-bridge.md § Phase 100) consistent with the existing Slack federation entry.
    3. CLAUDE.md — add a "Phase 100 (...) — GitHub bridge federated relay (complete)" bullet in the
       phase-notes block, mirroring the Phase 95/96/97 entries: dormant by default, github.peer_bridges
       activation, the Resolve() reorder + 700-repo scale fix, the deploy = make build-lambdas +
       km init --dry-run=false (NOT --sidecars) note, no schema change / no recreate, and a pointer to
       docs/github-bridge.md § Phase 100. Add the "Phase 100 federated relay" row to the "Where to
       look" table.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; grep -q "peer_bridges" docs/github-bridge.md &amp;&amp; grep -q "km init --dry-run=false" docs/github-bridge.md &amp;&amp; grep -qi "Phase 100" CLAUDE.md &amp;&amp; echo DOCS_OK</automated>
  </verify>
  <done>docs/github-bridge.md has the Phase 100 federated-relay runbook with the correct deploy sequence; OPERATOR-GUIDE.md cross-links it; CLAUDE.md has the Phase 100 note + Where-to-look row.</done>
</task>

<task type="auto">
  <name>Task 3: E2E UAT runbook (GH-FED-E2E manual verification)</name>
  <files>.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-UAT.md</files>
  <action>
    Write 100-UAT.md documenting the manual two-install/one-App live verification (per
    100-VALIDATION.md Manual-Only Verifications + design spec Operator flow lines 215-229). Steps:
    (1) one GitHub App, App creds + webhook secret pasted into BOTH installs via km github init;
    (2) each install lists only its own repos in github.repos (unique ownership); (3) front-door
    install (e.g. kph) sets github.peer_bridges to the other install's (sec) bridge Function URL
    (km github status → bridge-url); (4) point the App webhook at the front-door bridge;
    (5) make build-lambdas + km init --dry-run=false on both installs; (6) comment @-mentioning the
    bot on a sec-OWNED repo. Expected: the comment is delivered to kph (front door), relayed to sec,
    sec re-verifies HMAC + dispatches, EXACTLY ONE 👀 (from sec, the owner — front door posts none),
    no double-processing. Add a negative check: a comment on a repo NO install owns is dropped
    (github_relay_no_owner in the front-door logs) with no reaction (the helpful reply is the deferred
    Phase 101). Include the loop-guard check (self in peer_bridges → wasted hop, no loop) and a
    rollback note. Use synthetic install names + on.aws URLs only. Mark status: pending (operator
    runs it during /gsd:verify-work or post-deploy).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; test -f .planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-UAT.md &amp;&amp; grep -q "peer_bridges" .planning/phases/100-*/100-UAT.md &amp;&amp; grep -qi "one 👀\|single 👀\|exactly one" .planning/phases/100-*/100-UAT.md &amp;&amp; echo UAT_OK</automated>
  </verify>
  <done>100-UAT.md documents the two-install/one-App E2E runbook with the single-👀 + correct-dispatch + no-double-processing assertions and the orphan-drop negative case.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run GithubPeerBridges -count=1` GREEN
- `docs/github-bridge.md` references peer_bridges + the `km init --dry-run=false` (NOT --sidecars) deploy sequence
- `CLAUDE.md` has the Phase 100 note + Where-to-look row
- `100-UAT.md` exists with the two-install/one-App runbook + single-👀 assertion
- Full phase suite green: `go test ./pkg/github/bridge/... ./internal/app/config/... ./internal/app/cmd/... -count=1`
</verification>

<success_criteria>
GH-FED-DOCTOR satisfied: km doctor reports peer-bridge health (malformed URL / self-loop → WARN,
empty → OK/SKIP), wired into the GitHub group reading cfg.Github.PeerBridges + the SSM bridge-url.
GH-FED-E2E satisfied: a documented two-install/one-App live runbook verifies exactly one 👀, correct
owner dispatch, and no double-processing. Operator docs (github-bridge.md, OPERATOR-GUIDE.md,
CLAUDE.md) describe the federation + the correct deploy sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/100-github-bridge-federated-relay-one-github-app-serving-many-resource-prefix-installs-via-github-peer-bridges/100-04-SUMMARY.md`
</output>
