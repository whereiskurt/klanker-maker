---
phase: 03-sidecar-enforcement-lifecycle-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecars/dns-proxy/main.go
  - sidecars/http-proxy/main.go
  - sidecars/dns-proxy/dns_proxy_test.go
  - sidecars/http-proxy/http_proxy_test.go
autonomous: true
requirements:
  - NETW-02
  - NETW-03
  - OBSV-10

must_haves:
  truths:
    - "DNS proxy resolves names in the allowlist and returns NXDOMAIN for names outside it"
    - "HTTP proxy forwards CONNECT tunnels for allowlisted hosts and rejects blocked hosts with connection refused"
    - "HTTP proxy injects W3C traceparent header on outbound CONNECT requests"
    - "Both sidecars compile as standalone Go binaries in sidecars/dns-proxy/ and sidecars/http-proxy/"
  artifacts:
    - path: "sidecars/dns-proxy/main.go"
      provides: "DNS filtering proxy using miekg/dns, listens :5353 (dev) / :53 (prod)"
      exports: ["main"]
    - path: "sidecars/http-proxy/main.go"
      provides: "HTTPS CONNECT proxy using elazarl/goproxy with SNI allowlist and OTel header injection"
      exports: ["main"]
    - path: "sidecars/dns-proxy/dns_proxy_test.go"
      provides: "Unit tests for DNS allowlist logic"
    - path: "sidecars/http-proxy/http_proxy_test.go"
      provides: "Unit tests for CONNECT allowlist and header injection"
  key_links:
    - from: "sidecars/dns-proxy/main.go"
      to: "169.254.169.253:53"
      via: "miekg/dns Client.Exchange"
      pattern: "client\\.Exchange"
    - from: "sidecars/http-proxy/main.go"
      to: "W3C traceparent header"
      via: "otel.GetTextMapPropagator().Inject"
      pattern: "GetTextMapPropagator"
---

<objective>
Build the DNS proxy and HTTP/HTTPS proxy sidecar binaries — the two enforcement sidecars that filter outbound traffic inside a sandbox.

Purpose: Implements NETW-02 (DNS allowlist) and NETW-03 (HTTP/S allowlist) with trace context propagation (OBSV-10). These are standalone Go programs that run as systemd services on EC2 or containers in an ECS task definition.

Output: Two compilable `main.go` programs plus their unit test files.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-sidecar-enforcement-lifecycle-management/03-CONTEXT.md
@.planning/phases/03-sidecar-enforcement-lifecycle-management/03-VALIDATION.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from existing codebase. -->

From pkg/aws/discover_test.go — mock interface pattern used throughout pkg/aws:
```go
// package aws_test
// Define a narrow interface for the SDK client, implement with a struct, test with mock
type mockTagAPI struct {
    output *...Output
    err    error
}
func (m *mockTagAPI) GetResources(ctx context.Context, ...) (*...Output, error) {
    return m.output, m.err
}
```

DNS proxy decisions from 03-CONTEXT.md (LOCKED):
- Port: :53 UDP+TCP (configurable via DNS_PORT env var, default 53; use 5353 in tests to avoid root)
- Upstream: 169.254.169.253 (VPC resolver) configured via UPSTREAM_DNS env var
- Denied queries: return NXDOMAIN (dns.RcodeNameError)
- No caching
- Config: ALLOWED_SUFFIXES env var (comma-separated), SANDBOX_ID env var

HTTP proxy decisions from 03-CONTEXT.md (LOCKED):
- Port: :3128 (configurable via PROXY_PORT env var)
- HTTPS: SNI-only — intercept CONNECT, read hostname from CONNECT target, no decryption
- Blocked: return 403 (proxy rejects CONNECT; workload sees connection error)
- OTel: inject W3C traceparent on outbound CONNECT requests
- Config: ALLOWED_HOSTS env var (comma-separated), SANDBOX_ID env var

From 03-RESEARCH.md — library APIs:
```go
// miekg/dns v1.1.72
mux := dns.NewServeMux()
mux.HandleFunc(".", handlerFunc)
server := &dns.Server{Addr: ":53", Net: "udp", Handler: mux}
// Deny:
m.SetRcode(r, dns.RcodeNameError)
// Forward:
client := &dns.Client{}
resp, _, err := client.Exchange(r, upstream+":53")

// elazarl/goproxy v1.8.2
proxy := goproxy.NewProxyHttpServer()
proxy.OnRequest().HandleConnectFunc(func(host string, ctx *goproxy.ProxyCtx) (*goproxy.ConnectAction, string) {
    if !isAllowed(host, allowedHosts) {
        return goproxy.RejectConnect, host
    }
    return goproxy.OkConnect, host
})
// OTel inject (go.opentelemetry.io/otel/propagation):
otel.GetTextMapPropagator().Inject(ctx, propagation.HeaderCarrier(req.Header))
```

go.mod module path: github.com/whereiskurt/klankrmkr
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DNS proxy sidecar (NETW-02)</name>
  <files>sidecars/dns-proxy/main.go, sidecars/dns-proxy/dns_proxy_test.go</files>
  <behavior>
    - TestDNSProxy_AllowedSuffix: query for "allowed.example.com." with ALLOWED_SUFFIXES="example.com" → forwards to upstream, returns A record
    - TestDNSProxy_DeniedSuffix: query for "evil.com." with ALLOWED_SUFFIXES="example.com" → returns NXDOMAIN (RcodeNameError)
    - TestDNSProxy_AllowedExact: query for "example.com." → allowed (suffix match includes exact)
    - TestDNSProxy_EmptyAllowlist: query for any domain with ALLOWED_SUFFIXES="" → all denied (NXDOMAIN)
    - TestIsAllowed_SuffixMatch: unit test for the allowlist predicate function directly
  </behavior>
  <action>
    Add dependencies: `go get github.com/miekg/dns@v1.1.72`

    Create `sidecars/dns-proxy/main.go` as a standalone `package main`:

    1. Define `isAllowed(name string, suffixes []string) bool` — trims trailing dot from DNS name, checks if name has any suffix in the list (case-insensitive). Empty suffixes list = deny all.

    2. Define `handleDNS(allowed []string, upstream string) dns.HandlerFunc` — if isAllowed: forward to upstream via `dns.Client.Exchange`; if denied: write NXDOMAIN response via `m.SetRcode(r, dns.RcodeNameError)`. Log every query as JSON using zerolog: fields sandbox_id, event_type="dns_query", domain, allowed (bool).

    3. `main()`: read config from env vars:
       - `ALLOWED_SUFFIXES` (comma-separated, default empty = deny all)
       - `UPSTREAM_DNS` (default "169.254.169.253")
       - `DNS_PORT` (default "53")
       - `SANDBOX_ID` (default "unknown")
       Set up ServeMux with HandleFunc(".", ...), start UDP server and TCP server both on same port. Block on server.ListenAndServe().

    Create `sidecars/dns-proxy/dns_proxy_test.go` (package main_test or package dns_proxy — use exported functions from proxy.go if you split logic into proxy.go / main.go pattern to enable testing):

    Split `sidecars/dns-proxy/` into two files:
    - `proxy.go` (package dnsproxy): exports `IsAllowed()` and `NewHandler()`
    - `main.go` (package main): imports dnsproxy, wires env vars, starts server

    Tests use in-process DNS server on random port (127.0.0.1:0) with a mock upstream.

    Zerolog is already in go.mod (v1.33.0). Use `log.Logger` with `zerolog.ConsoleWriter` disabled — JSON output to stdout only.
  </action>
  <verify>
    <automated>go test ./sidecars/dns-proxy/... -run TestDNSProxy -v -timeout 30s</automated>
  </verify>
  <done>All TestDNSProxy_* tests pass. `go build ./sidecars/dns-proxy/` compiles without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTTP/HTTPS proxy sidecar with OTel injection (NETW-03, OBSV-10)</name>
  <files>sidecars/http-proxy/main.go, sidecars/http-proxy/http_proxy_test.go</files>
  <behavior>
    - TestHTTPProxy_AllowedHost: CONNECT to "allowed.example.com:443" with ALLOWED_HOSTS="allowed.example.com" → returns 200 CONNECT established
    - TestHTTPProxy_BlockedHost: CONNECT to "evil.com:443" with ALLOWED_HOSTS="allowed.example.com" → returns 403 (RejectConnect)
    - TestHTTPProxy_AllowedWithPort: host matching strips port from "host:port" before allowlist check
    - TestHTTPProxy_TraceparentInjected: CONNECT to allowed host → outbound request carries "traceparent" header
    - TestIsHostAllowed: unit test for the host allowlist predicate
  </behavior>
  <action>
    Add dependencies: `go get github.com/elazarl/goproxy@v1.8.2 && go get go.opentelemetry.io/otel@latest && go get go.opentelemetry.io/otel/propagation@latest`

    Create `sidecars/http-proxy/` split into:
    - `proxy.go` (package httpproxy): exports `IsHostAllowed(host string, allowed []string) bool`, `NewProxy(allowed []string, sandboxID string) *goproxy.ProxyHttpServer`
    - `main.go` (package main): reads ALLOWED_HOSTS, PROXY_PORT (default "3128"), SANDBOX_ID; starts `http.ListenAndServe`

    In `proxy.go`:
    1. `IsHostAllowed`: strips port from "host:port", checks exact match against allowed list (case-insensitive). Empty list = deny all.
    2. `NewProxy`: creates goproxy proxy. Register `OnRequest().HandleConnectFunc`:
       - If allowed: inject W3C traceparent via `otel.GetTextMapPropagator().Inject(req.Context(), propagation.HeaderCarrier(req.Header))`, return `goproxy.OkConnect, host`
       - If denied: log JSON event (event_type="http_blocked", host, sandbox_id) via zerolog; return `goproxy.RejectConnect, host`
    3. Register `OnRequest().DoFunc` for plain HTTP: same allowlist check; if denied return 403 response.
    4. Set `proxy.Logger` to a zerolog-backed logger for goproxy internal logs.

    Tests start an in-process proxy on a random port using `httptest.NewServer`. Use `net/http` with `Proxy: http.ProxyURL(proxyURL)` transport to send requests through the in-process proxy. Test blocked CONNECT by checking response status code 403.

    Note on OTel init: use `otel.SetTextMapPropagator(propagation.TraceContext{})` in proxy init; no OTLP exporter needed here — just the propagation API for header injection.
  </action>
  <verify>
    <automated>go test ./sidecars/http-proxy/... -run TestHTTPProxy -v -timeout 30s</automated>
  </verify>
  <done>All TestHTTPProxy_* tests pass. `go build ./sidecars/http-proxy/` compiles without error.</done>
</task>

</tasks>

<verification>
Run full sidecar tests: `go test ./sidecars/... -timeout 30s`
Confirm both binaries build: `go build ./sidecars/dns-proxy/ && go build ./sidecars/http-proxy/`
`go vet ./sidecars/...` passes with no errors.
</verification>

<success_criteria>
- DNS proxy allows configured suffixes, returns NXDOMAIN for all others
- HTTP proxy rejects blocked CONNECT with 403, injects traceparent on allowed requests
- Both binaries compile as standalone programs
- All unit tests pass in < 30 seconds
- No iptables or root privileges needed for tests (bind to high ports)
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidecar-enforcement-lifecycle-management/03-01-SUMMARY.md`
</output>
