---
phase: 117-composable-multi-parent-profile-inheritance-deep-merge-list-union-extends
plan: 03
type: execute
wave: 3
depends_on: ["117-02"]
files_modified:
  - internal/app/cmd/validate.go
  - internal/app/cmd/create.go
  - scripts/validate-all-profiles.sh
  - internal/app/cmd/validate_test.go
autonomous: true
requirements: []   # NOTE: new architectural phase — no ROADMAP requirement IDs. must_haves derived from phase GOAL.

must_haves:
  truths:
    - "km validate resolves multi-parent extends before validating the merged leaf"
    - "km create resolves multi-parent extends before validating/compiling"
    - "A standalone abstract fragment (metadata.abstract:true) skips standalone validation with a clear message, not a crash"
    - "scripts/validate-all-profiles.sh skips profiles/base/ fragments and still validates every refactored leaf"
  artifacts:
    - path: "internal/app/cmd/validate.go"
      provides: "Resolve-before-Validate for multi-parent; abstract-fragment skip message"
      contains: "IsAbstractFragment"
    - path: "scripts/validate-all-profiles.sh"
      provides: "base/ fragment skip"
      contains: "base"
  key_links:
    - from: "km validate / km create"
      to: "profile.Resolve(<all extends>)"
      via: "pass the leaf and resolve its full extends DAG, not just the first parent"
      pattern: "profile.Resolve"
    - from: "km validate"
      to: "IsAbstractFragment"
      via: "skip standalone validation when the leaf is an abstract fragment"
      pattern: "IsAbstractFragment"
---

<objective>
Wire the new DAG resolve into the `km validate` and `km create` command paths so
multi-parent `extends:` is fully resolved BEFORE the merged leaf is validated /
compiled, make standalone validation of an abstract fragment skip cleanly with a
helpful message (not a required-field crash), and teach
`scripts/validate-all-profiles.sh` (the Phase 92 SSOT gate) to skip
`profiles/base/` fragments while still validating every leaf.

Plan 01 made the call sites compile with single-parent behavior; this plan makes
them actually resolve the FULL extends list. Plan 02 delivered the engine; here
we connect it to the user-facing commands and the validation gate.

Purpose: a multi-parent profile resolves + validates end-to-end via the CLI, and
fragments don't break the validate-all gate. Output: wired commands, fragment
skip, updated SSOT gate script.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-composable-multi-parent-profile-inheritance-deep-merge-list-union-extends/117-RESEARCH.md
@.planning/phases/117-composable-multi-parent-profile-inheritance-deep-merge-list-union-extends/117-VALIDATION.md

@internal/app/cmd/validate.go
@internal/app/cmd/create.go
@scripts/validate-all-profiles.sh

<interfaces>
<!-- Resolve contract (delivered by Plan 02). -->
func profile.Resolve(name string, searchPaths []string) (*SandboxProfile, error)
func profile.IsAbstractFragment(raw []byte) bool

Resolve now follows the FULL extends DAG from the named leaf. The call sites
already pass searchPaths that include filepath.Dir(filePath) as the first entry
(RESEARCH Pitfall 6), so `profiles/dc34.yaml` extending `base/safenetwork`
resolves to `profiles/base/safenetwork.yaml`.

Call sites (from Plan 01, currently single-parent-shaped):
- internal/app/cmd/validate.go:76-88 — gated on parsed.Extends.IsSet()
- internal/app/cmd/create.go:342, 2094 — gated on parsed.Extends.IsSet()

The key change: ensure these pass the LEAF's name to Resolve (so the full DAG is
walked), not just the first extends entry. Read each site and confirm Resolve is
invoked with the leaf identity and the validated/compiled profile is the RESOLVED
one, not `parsed`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve full extends DAG before validate/compile + abstract-fragment skip</name>
  <files>internal/app/cmd/validate.go, internal/app/cmd/create.go, internal/app/cmd/validate_test.go</files>
  <action>
    1. internal/app/cmd/validate.go:
       - When `parsed.Extends.IsSet()`, call `profile.Resolve(<leaf name>, searchPaths)`
         and validate the RESOLVED profile (the merged leaf), not `parsed`. Read
         lines 70-100 to confirm the post-resolve profile flows into Validate.
       - Add an abstract-fragment guard at the top of the validate path: read the
         raw bytes, `if profile.IsAbstractFragment(raw)` → print a clear,
         non-error message to stdout (e.g. "SKIP: <path> is an abstract base
         fragment (metadata.abstract: true); validated only when merged into a
         leaf profile via extends:") and return success (exit 0). This satisfies
         the manual-UX check in 117-VALIDATION.md (no crash, helpful message).
    2. internal/app/cmd/create.go (lines ~342 and ~2094): confirm the resolved
       profile (full DAG) is what gets validated + compiled, mirroring validate.go.
       If create resolves only the first parent (Plan 01 stopgap), fix it to use
       the full Resolve. A `km create` on an abstract fragment should fail with a
       clear "cannot create from an abstract fragment" message (fragments aren't
       launchable) — add that guard if not already present.
    3. Add an internal/app/cmd test (validate_test.go) or extend an existing one:
       validating an abstract fragment returns exit 0 with the skip message;
       validating a multi-parent leaf resolves (no required-field failure that
       would occur if extends weren't resolved). Use a small testdata fixture or
       reuse profiles/base once Plan 04 exists — but since Plan 04 is downstream,
       prefer a self-contained inline fixture written in the test's temp dir.
    Run gofmt; make build.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/app/cmd/... -run 'TestValidate' -count=1 -timeout 600s</automated>
  </verify>
  <done>km validate/create resolve the full extends DAG and validate the merged leaf; an abstract fragment skips standalone validation with a clear message; km create refuses an abstract fragment.</done>
</task>

<task type="auto">
  <name>Task 2: validate-all-profiles.sh skips base/ fragments</name>
  <files>scripts/validate-all-profiles.sh</files>
  <action>
    Update the Phase 92 SSOT gate (scripts/validate-all-profiles.sh) so it does
    NOT run `km validate` standalone against `profiles/base/*.yaml` fragments
    (they would fail required-field checks). Per RESEARCH Code Examples:
    - Add a loop that prints a "skip <path> (base fragment)" line for each
      profiles/base/*.yaml (glob-guard for the dir not yet existing — Plan 04
      creates it; use `[ -d profiles/base ]` / nullglob so the script is green
      both before and after Plan 04).
    - Do NOT add base fragments to the PROFILES inventory array.
    - Keep the existing leaf inventory intact (dc34, learn.v2.*, etc. — Plan 04
      refactors these onto bases but they remain leaves that MUST validate).
    - Update the header comment's "21-file"/"22-file" count note to reflect that
      base/ is intentionally excluded.
    Keep `set -euo pipefail`; ensure the script still exits non-zero on any leaf
    failure. Build km first if needed (the script requires ./km).
  </action>
  <verify>
    <automated>make build && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>validate-all-profiles.sh runs green, prints skip lines for any profiles/base/*.yaml, and still validates every leaf in the inventory; exits non-zero if any leaf fails.</done>
</task>

</tasks>

<verification>
- `go build ./...` green; `go test ./internal/app/cmd/... -run TestValidate` green.
- `bash scripts/validate-all-profiles.sh` exits 0 (green) with base/ skipped.
- `./km validate <an abstract fragment>` prints a clear skip message, exit 0
  (manual UX check from 117-VALIDATION.md).
- A multi-parent leaf validated via `./km validate` resolves its full DAG (will be
  exercised end-to-end in Plan 04 against the real bases).
</verification>

<success_criteria>
km validate / km create resolve the full multi-parent extends DAG and validate
the merged leaf; abstract fragments skip standalone validation gracefully; the
SSOT validate-all gate skips base/ fragments and stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/117-composable-multi-parent-profile-inheritance-deep-merge-list-union-extends/117-03-SUMMARY.md`
</output>
