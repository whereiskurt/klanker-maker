---
phase: 111-rich-slack-rendering-markdown-and-table-blocks-opt-in
plan: 04
subsystem: slack
tags: [slack, block-kit, markdown-block, table-block, rich-renderer, golden-corpus, docs, plugin-version]

# Dependency graph
requires:
  - phase: 111-01
    provides: RenderRich + segmentInput + emitProseBlocks + chunkMarkdown + blockMarkdown
  - phase: 111-02
    provides: buildTableBlock + blockTable/columnSetting/tableCell structs
  - phase: 111-03
    provides: blocks-rich wired in km-slack runPost/runReply/runWith + KM_SLACK_AI_FOOTER
provides:
  - "testdata/rich-mixed.md: combined H1+prose+link+3-col table (left/center/right)+tool-line fixture"
  - "testdata/rich-mixed.expected-blocks.json: golden corpus (header+markdown+table+context blocks)"
  - "TestRichCorpus: now covers rich-mixed case (RICH-17) via auto-discovered glob walk"
  - "TestRichDefaultPathFrozen (RICH-18): explicit regression guard — Tier-2 blocks corpus byte-identical after Phase 111"
  - "docs/slack-notifications.md § Phase 111: render modes, markdown/table block adoption, KM_SLACK_AI_FOOTER, surface caveats, deploy"
  - "skills/slack/SKILL.md: blocks-rich row in render-mode table + KM_SLACK_AI_FOOTER mention"
  - ".claude-plugin/plugin.json: version 0.4.8 → 0.4.9"
  - ".claude-plugin/marketplace.json: version 0.4.8 → 0.4.9"
affects:
  - "112 (flip default): operator docs now in place; Phase 112 flips default to blocks-rich after soak"
  - "klanker plugin clients: version bump 0.4.9 clears cached plugin and delivers new blocks-rich SKILL.md row"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Golden corpus auto-discovery: TestRichCorpus uses filepath.Glob('rich-*.md') — new fixtures automatically picked up"
    - "TestRichDefaultPathFrozen handles both single-key (plain array) and two-key ({blocks,text}) fixture formats"
    - "Plugin version lockstep: plugin.json + marketplace.json bumped together per project_plugin_version_gates_cache"
    - "RICH-20 protected: KM_SLACK_AI_FOOTER is NOT compiler-emitted; compiler golden tests unaffected"

key-files:
  created:
    - pkg/slack/testdata/rich-mixed.md
    - pkg/slack/testdata/rich-mixed.expected-blocks.json
  modified:
    - pkg/slack/blocks_rich_test.go
    - docs/slack-notifications.md
    - skills/slack/SKILL.md
    - .claude-plugin/plugin.json
    - .claude-plugin/marketplace.json

key-decisions:
  - "rich-mixed fixture generated by running RenderRich then capturing output as the authoritative golden — same approach as rich-table-basic"
  - "TestRichDefaultPathFrozen added as an explicit RICH-18 guard in blocks_rich_test.go (not just TestBlocksCorpus in blocks_test.go) so the requirement is traceable by name"
  - "docs/slack-notifications.md § Phase 111 documents the fallback chain, 12K cumulative cap, surface caveats, and KM_SLACK_AI_FOOTER as operator-controlled (not compiler-emitted)"
  - "Plugin version bump is lockstep: both plugin.json and marketplace.json bumped to 0.4.9 in the same commit"

requirements-completed: [RICH-17, RICH-18, RICH-20]

# Metrics
duration: 8min
completed: 2026-06-14
---

# Phase 111 Plan 04: Golden Corpus + Docs + Plugin Version Bump Summary

**rich-mixed golden corpus, TestRichDefaultPathFrozen regression guard, docs/slack-notifications.md § Phase 111, blocks-rich SKILL.md row, plugin version 0.4.9 lockstep bump — Phase 111 complete**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-06-14T15:46:00Z
- **Completed:** 2026-06-14T15:52:00Z
- **Tasks:** 2 (Task 1: corpus + regression guard; Task 2: docs + skill + plugin version)
- **Files created/modified:** 7

## Accomplishments

- `pkg/slack/testdata/rich-mixed.md`: combined golden input fixture — H1, prose with `[label](url)` link, 3-column GFM table with left/center/right alignment and numeric values, and a `🔧 Tool:` line.
- `pkg/slack/testdata/rich-mixed.expected-blocks.json`: expected output verified by running `RenderRich` — `header` block, `markdown` block, `table` block (bold `rich_text` header, `raw_number`/`raw_text` body, left/center/right column settings), `context` block.
- `pkg/slack/blocks_rich_test.go`: `TestRichCorpus` now auto-discovers and validates `rich-mixed` (RICH-17). Added `TestRichDefaultPathFrozen` (RICH-18) — explicit regression guard that walks `blocks-*.md`, handles both single-key and two-key fixture formats, and asserts `RenderBlocks` (Tier-2) output is byte-identical after Phase 111.
- `docs/slack-notifications.md` § Phase 111: complete operator reference — render mode tier table, what `blocks-rich` does (prose/H1/table/tool-line/AI-footer), 12K cumulative cap, 50-block cap, fallback chain, surface caveats (heading flatten, table cells not markdown, email text fallback, over-limit tables), `KM_SLACK_AI_FOOTER` usage, activation instructions, deploy mechanics.
- `skills/slack/SKILL.md` Step 5 render-mode table: added `blocks-rich` row describing Tier-3 opt-in with native GFM `markdown` blocks, `table` blocks for pipe tables, `KM_SLACK_AI_FOOTER` mention, and reference to docs § Phase 111.
- `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json`: lockstep version bump `0.4.8` → `0.4.9`.

## Task Commits

1. **Task 1: rich-mixed golden corpus + TestRichDefaultPathFrozen regression guard** - `33edfc94` (feat)
2. **Task 2: docs Phase 111 section + blocks-rich skill row + plugin 0.4.9 bump** - `aedde95d` (feat)

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/pkg/slack/testdata/rich-mixed.md` — combined H1+prose+table+tool-line golden input
- `/Users/khundeck/working/klankrmkr/pkg/slack/testdata/rich-mixed.expected-blocks.json` — expected blocks-rich JSON (header+markdown+table+context)
- `/Users/khundeck/working/klankrmkr/pkg/slack/blocks_rich_test.go` — TestRichDefaultPathFrozen added + TestRichCorpus comment updated
- `/Users/khundeck/working/klankrmkr/docs/slack-notifications.md` — § Phase 111 render mode documentation
- `/Users/khundeck/working/klankrmkr/skills/slack/SKILL.md` — blocks-rich render mode row
- `/Users/khundeck/working/klankrmkr/.claude-plugin/plugin.json` — version 0.4.9
- `/Users/khundeck/working/klankrmkr/.claude-plugin/marketplace.json` — version 0.4.9

## Decisions Made

- `rich-mixed.expected-blocks.json` generated by running `RenderRich` and capturing the output, then writing it as the authoritative golden — same methodology as `rich-table-basic.expected-blocks.json` in Plan 02. The fixture was manually reviewed before writing.
- `TestRichDefaultPathFrozen` is in `blocks_rich_test.go` (not `blocks_test.go`) so RICH-18 traceability is explicit — `go test -run TestRichDefaultPathFrozen` immediately identifies the regression guard.
- The `TestRichDefaultPathFrozen` implementation handles both fixture formats (single-key plain array + two-key `{blocks, text}` object) by attempting the two-key unmarshal first, mirroring `TestBlocksCorpus` logic from `blocks_test.go`.
- `KM_SLACK_AI_FOOTER` documented as operator-controlled in `/etc/km/notify.env` — the compiler does NOT emit it, protecting compiler golden tests (RICH-20 confirmed green).
- Plugin version 0.4.9 bumped in both files in the same commit — single atomic lockstep, per `project_plugin_version_gates_cache` constraint.

## Deviations from Plan

**[Rule 1 - Bug] TestRichDefaultPathFrozen fixture format mismatch — auto-fixed**
- **Found during:** Task 1 implementation
- **Issue:** Initial implementation assumed blocks-*.expected-blocks.json used the two-key `{"blocks":[...],"text":"..."}` format, but most files are single-key plain JSON arrays. First run failed with `json: cannot unmarshal array into Go value of type struct`.
- **Fix:** Updated `TestRichDefaultPathFrozen` to handle both formats, mirroring `TestBlocksCorpus`'s proven approach: try two-key unmarshal first; fall back to single-key plain array on failure.
- **Files modified:** `pkg/slack/blocks_rich_test.go`
- **Commit:** `33edfc94` (fix included in same task commit)

## Verification

- `go test ./pkg/slack/... -run 'TestRichCorpus|TestBlocksCorpus|TestRichDefaultPathFrozen' -count=1 -timeout 60s`: PASS (rich-mixed/3, blocks/7, frozen/7)
- `go test ./pkg/slack/... ./cmd/km-slack/... -count=1 -timeout 120s`: PASS (full phase suite)
- `go test ./pkg/compiler/... -run 'TestUserdataAdditionalVolumeOnly_Golden|TestH1ByteIdentity' -count=1 -timeout 120s`: PASS (RICH-20 protected)
- `go test ./... -count=1 -timeout 600s`: PASS (full repo — all 35 packages green)
- `grep -q "Phase 111" docs/slack-notifications.md`: PASS
- `grep -q "blocks-rich" skills/slack/SKILL.md`: PASS
- `grep -q '"0.4.9"' .claude-plugin/plugin.json && .claude-plugin/marketplace.json`: PASS (lockstep)

## Self-Check: PASSED

- `pkg/slack/testdata/rich-mixed.md`: FOUND
- `pkg/slack/testdata/rich-mixed.expected-blocks.json`: FOUND
- `pkg/slack/blocks_rich_test.go` contains "TestRichDefaultPathFrozen": FOUND
- `docs/slack-notifications.md` contains "Phase 111": FOUND
- `skills/slack/SKILL.md` contains "blocks-rich": FOUND
- `.claude-plugin/plugin.json` contains "0.4.9": FOUND
- `.claude-plugin/marketplace.json` contains "0.4.9": FOUND
- Commit `33edfc94`: FOUND
- Commit `aedde95d`: FOUND
- `go test ./pkg/slack/...`: PASS
- `go test ./pkg/compiler/... -run TestUserdataAdditionalVolumeOnly_Golden`: PASS (RICH-20)
- `go test ./...`: PASS (full repo green)

## Phase 111 Completion

Phase 111 is complete across all 4 plans:
- **Plan 01:** `RenderRich` Tier-3 prose skeleton — segmentInput + emitProseBlocks + chunkMarkdown + blockMarkdown
- **Plan 02:** GFM table → Slack `table` block transformer — buildTableBlock + column/cell/alignment logic + guards
- **Plan 03:** `km-slack` wiring — `blocks-rich` in runPost/runReply/runWith + `KM_SLACK_AI_FOOTER` + fallback chain
- **Plan 04:** Mixed corpus + regression guards + operator docs + skill update + plugin version bump

The `blocks-rich` mode is now end-to-end available and documented. Phase 112 (flip default to `blocks-rich` after real-workspace soak/UAT) is the logical next step.

---
*Phase: 111-rich-slack-rendering-markdown-and-table-blocks-opt-in*
*Completed: 2026-06-14*
