---
phase: 111-rich-slack-rendering-markdown-and-table-blocks-opt-in
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - pkg/slack/rich.go
  - pkg/slack/table.go
  - pkg/slack/blocks_rich_test.go
  - pkg/slack/testdata/rich-table-basic.md
  - pkg/slack/testdata/rich-table-basic.expected-blocks.json
  - pkg/slack/testdata/rich-table-guards.md
autonomous: true
requirements: [RICH-04, RICH-05, RICH-06, RICH-07, RICH-08, RICH-09]
must_haves:
  truths:
    - "A GFM pipe table becomes a Slack table block with column_settings alignment derived from the delimiter row"
    - "The header row renders as bold rich_text cells"
    - "Pure-numeric body cells render as raw_number; other body cells as raw_text"
    - "Ragged rows are padded to the column count"
    - "A table wider than 20 columns falls back to the monospace fencePipeTables reflow"
    - "A table longer than 100 data rows falls back to the monospace fencePipeTables reflow"
  artifacts:
    - path: "pkg/slack/table.go"
      provides: "buildTableBlock + alignFromSep + classifyCell + makeBoldCell + blockTable/columnSetting/tableCell/richTextElement structs"
      contains: "func buildTableBlock"
      min_lines: 90
    - path: "pkg/slack/rich.go"
      provides: "table segment emission wired to buildTableBlock with fencePipeTables fallback (Plan 01 stub replaced)"
      contains: "buildTableBlock"
    - path: "pkg/slack/testdata/rich-table-basic.md"
      provides: "3-col table with mixed alignment + numeric cells"
    - path: "pkg/slack/testdata/rich-table-basic.expected-blocks.json"
      provides: "expected table block output"
    - path: "pkg/slack/testdata/rich-table-guards.md"
      provides: ">20-col table asserting monospace fallback (no expected-blocks.json)"
  key_links:
    - from: "pkg/slack/table.go buildTableBlock"
      to: "pkg/slack/mrkdwn.go splitTableRow/isSeparatorRow"
      via: "row parsing + separator detection reuse mrkdwn primitives"
      pattern: "splitTableRow|isSeparatorRow"
    - from: "pkg/slack/rich.go table segment branch"
      to: "pkg/slack/mrkdwn.go fencePipeTables"
      via: "guard-hit (>20 col / >100 row) falls back to monospace reflow"
      pattern: "fencePipeTables"
---

<objective>
Build the GFM-pipe-table → Slack `table`-block transformer and wire it into the renderRich
table-segment branch (replacing the Plan 01 stub). Alignment comes from the delimiter row;
the header row is bold `rich_text`; body cells are `raw_number`/`raw_text`; ragged rows pad;
and tables exceeding ≤20 columns / ≤100 rows fall back to the existing `fencePipeTables`
monospace reflow.

Purpose: Deliver real Slack tables (the operator's primary ask) reusing the exact detection
primitives so the fallback path is consistent.
Output: `pkg/slack/table.go`, the wired rich.go branch, table golden + guard fixtures, and tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-CONTEXT.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-RESEARCH.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-VALIDATION.md

@pkg/slack/rich.go
@pkg/slack/mrkdwn.go

<interfaces>
<!-- REUSE verbatim from mrkdwn.go (do not modify). Source: pkg/slack grep. -->
```go
func splitTableRow(line string) []string  // trims outer pipes, handles \| escaping, trims cell ws
func isSeparatorRow(cells []string) bool   // detects :?-+:? per cell
func fencePipeTables(s string) string      // monospace-reflow fallback for over-limit tables
```

NEW table structs (RESEARCH Pattern 4). v1 SIMPLIFICATION (per RESEARCH Open Question 1):
ONLY header cells use rich_text/bold; body cells are raw_number or raw_text. Do NOT implement a
full rich_text body-cell encoder.
```go
type blockTable struct {
    Type           string          `json:"type"` // "table"
    ColumnSettings []columnSetting `json:"column_settings"`
    Rows           [][]tableCell   `json:"rows"`
}
type columnSetting struct { Align string `json:"align"`; IsWrapped bool `json:"is_wrapped"` }
type tableCell struct {
    Type     string            `json:"type"` // "raw_text"|"raw_number"|"rich_text"
    Text     string            `json:"text,omitempty"`
    Elements []richTextElement `json:"elements,omitempty"`
}
type richTextElement struct { Type string `json:"type"`; Text string `json:"text"`; Style *rtStyle `json:"style,omitempty"` }
type rtStyle struct { Bold bool `json:"bold,omitempty"` }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildTableBlock transformer + guards</name>
  <files>pkg/slack/table.go, pkg/slack/blocks_rich_test.go, pkg/slack/testdata/rich-table-guards.md</files>
  <behavior>
    - TestRichTable_Alignment: delimiter `|:--|:-:|--:|` → column_settings align left/center/right (alignFromSep: leading+trailing ':' = center, trailing ':' = right, else left).
    - TestRichTable_HeaderBold: header row cells are `{"type":"rich_text","elements":[{"type":"text","text":...,"style":{"bold":true}}]}`.
    - TestRichTable_RawNumber: a pure-numeric body cell (e.g. `42`, `3.14`, `1,000`) → `{"type":"raw_number","text":...}`; a non-numeric cell → `{"type":"raw_text",...}`.
    - TestRichTable_RaggedPad: a body row with fewer cells than the header → padded to numCols with empty raw_text cells.
    - TestRichTable_ColsGuard: a table with >20 columns → buildTableBlock returns ok=false (rich.go then emits fencePipeTables monospace; asserted in Task 2).
    - TestRichTable_RowsGuard: a table with >100 data rows → buildTableBlock returns ok=false.
  </behavior>
  <action>
Create `pkg/slack/table.go` (`package slack`) implementing
`buildTableBlock(lines []string) (blockTable, bool)` per RESEARCH Pattern 4:

- Parse every line with `splitTableRow`; mark separator rows with `isSeparatorRow`.
- Find the separator index; require sepIdx >= 1 (header row 0 + separator row 1 minimum), else ok=false.
- numCols = len(header cells). Guard: numCols > 20 → ok=false. dataRows = total - sepIdx - 1;
  guard: dataRows > 100 → ok=false.
- `alignFromSep(cell)`: trim; leading ':' && trailing ':' → "center"; trailing ':' → "right"; else "left".
  Build column_settings (IsWrapped:false for v1).
- Header row → `makeBoldCell` (rich_text + Style.Bold). Body rows (after separator) → per cell
  `classifyCell`: `reNumeric` match (`^\s*[-+]?[\d,]*\.?\d+\s*$`) → raw_number (trimmed); else raw_text
  (trimmed). v1: cells with inline markup ALSO degrade to raw_text (no rich_text body encoder).
- Pad ragged rows: when a row has fewer cells than numCols, fill with empty raw_text cells.

Add `pkg/slack/testdata/rich-table-guards.md` — a >20-column pipe table (header + delimiter +
one body row) used by the Task 2 fallback assertion. Add the Task-1 unit tests to
`pkg/slack/blocks_rich_test.go`. Do NOT modify renderBlocks or any mrkdwn/blocks- golden.
  </action>
  <verify>
    <automated>go test ./pkg/slack/... -run 'TestRichTable_Alignment|TestRichTable_HeaderBold|TestRichTable_RawNumber|TestRichTable_RaggedPad|TestRichTable_ColsGuard|TestRichTable_RowsGuard' -count=1 -timeout 60s</automated>
  </verify>
  <done>RICH-04/05/06/07/08/09 transformer tests pass; buildTableBlock returns ok=false on the two guards.</done>
</task>

<task type="auto">
  <name>Task 2: Wire table emission into renderRich + table corpus fixture</name>
  <files>pkg/slack/rich.go, pkg/slack/blocks_rich_test.go, pkg/slack/testdata/rich-table-basic.md, pkg/slack/testdata/rich-table-basic.expected-blocks.json</files>
  <action>
Replace the Plan 01 table-segment STUB in `renderRich`'s segment loop: for a `segRichTable`
segment call `buildTableBlock(seg.lines)`. On ok=true append the table block. On ok=false (guard
hit or malformed), fall back to `fencePipeTables(strings.Join(seg.lines,"\n"))` emitted as a
`blockMarkdown` (the fenced monospace is valid GFM). Append the segment's stripped lines to the
plain-text fallback either way (tables don't render in the text fallback — same as today).

Create `pkg/slack/testdata/rich-table-basic.md` — a 3-column table with mixed alignment
(`:--`/`:-:`/`--:`), a bold header, and at least one pure-numeric body column. Create the
companion `rich-table-basic.expected-blocks.json` (`{"blocks":[...],"text":...}`) by running the
implementation and pasting verified output. Extend `TestRichCorpus` to cover rich-table-basic.
Add a `TestRichTable_GuardFallback` assertion: rich-table-guards.md renders through renderRich as
a monospace fenced markdown block (no `"type":"table"` in the output), proving the >20-col guard
path routes to fencePipeTables.
  </action>
  <verify>
    <automated>go test ./pkg/slack/... -run 'TestRichCorpus|TestRichTable_GuardFallback' -count=1 -timeout 60s</automated>
  </verify>
  <done>renderRich emits real table blocks for valid tables and monospace fallback for over-limit tables; table corpus fixture matches.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/slack/... -run TestRich -count=1 -timeout 60s` green (Plan 01 + Plan 02 tests).
- `go test ./pkg/slack/... -run TestBlocksCorpus -count=1 -timeout 60s` STILL green (default path frozen).
- `go vet ./pkg/slack/...` clean.
</verification>

<success_criteria>
- Valid GFM tables become Slack table blocks with correct alignment, bold header, raw_number/raw_text body cells, ragged-row padding (RICH-04..RICH-07).
- Over-limit tables (>20 col / >100 row) fall back to fencePipeTables monospace (RICH-08, RICH-09).
- Table transformer reuses splitTableRow/isSeparatorRow/fencePipeTables; default path untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-02-SUMMARY.md`
</output>
