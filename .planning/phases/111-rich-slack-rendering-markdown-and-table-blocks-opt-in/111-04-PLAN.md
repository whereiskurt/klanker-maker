---
phase: 111-rich-slack-rendering-markdown-and-table-blocks-opt-in
plan: 04
type: execute
wave: 4
depends_on: ["111-01", "111-02", "111-03"]
files_modified:
  - pkg/slack/testdata/rich-mixed.md
  - pkg/slack/testdata/rich-mixed.expected-blocks.json
  - pkg/slack/blocks_rich_test.go
  - docs/slack-notifications.md
  - skills/slack/SKILL.md
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements: [RICH-17, RICH-18, RICH-20]
must_haves:
  truths:
    - "A mixed H1 + prose + table + tool-line message renders to the expected blocks-rich JSON corpus"
    - "The default blocks (Tier-2) golden corpus is unchanged and still green"
    - "The compiler userdata byte-identity goldens are unaffected (no new NotifyEnv keys)"
    - "docs/slack-notifications.md documents the Phase 111 render mode + KM_SLACK_AI_FOOTER + caveats"
    - "skills/slack/SKILL.md lists the blocks-rich render mode and the plugin version is bumped"
  artifacts:
    - path: "pkg/slack/testdata/rich-mixed.md"
      provides: "H1 + prose + table + 🔧 tool-line combined fixture"
    - path: "pkg/slack/testdata/rich-mixed.expected-blocks.json"
      provides: "expected combined blocks-rich output"
    - path: "docs/slack-notifications.md"
      provides: "§ Phase 111 render-mode + table/markdown adoption + KM_SLACK_AI_FOOTER + surface caveats"
      contains: "Phase 111"
    - path: "skills/slack/SKILL.md"
      provides: "blocks-rich render-mode row + KM_SLACK_AI_FOOTER mention"
      contains: "blocks-rich"
    - path: ".claude-plugin/plugin.json"
      provides: "bumped plugin version"
      contains: "version"
    - path: ".claude-plugin/marketplace.json"
      provides: "matching bumped version"
      contains: "version"
  key_links:
    - from: "pkg/slack/blocks_rich_test.go TestRichCorpus"
      to: "pkg/slack/testdata/rich-mixed.{md,expected-blocks.json}"
      via: "corpus walk asserts rendered == expected"
      pattern: "rich-mixed"
    - from: ".claude-plugin/plugin.json version"
      to: ".claude-plugin/marketplace.json version"
      via: "lockstep bump per plugin-version cache gate"
      pattern: "version"
---

<objective>
Close out Phase 111: add the combined H1+prose+table+tool-line golden corpus fixture, assert the
default `blocks` (Tier-2) corpus and the compiler userdata goldens are untouched (regression
guards), document the new `blocks-rich` mode in `docs/slack-notifications.md` § Phase 111 and the
`klanker:slack` skill, and bump the plugin version in lockstep (`.claude-plugin/plugin.json` +
`marketplace.json`) per the plugin-version cache gate.

Purpose: Ship the golden corpus + operator docs + skill update so the opt-in tier is discoverable
and the default path is provably frozen.
Output: mixed corpus fixture, regression assertions, docs, skill update, plugin version bump.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-CONTEXT.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-RESEARCH.md
@.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-VALIDATION.md

@skills/slack/SKILL.md
@docs/slack-notifications.md

<interfaces>
<!-- Plugin version files (lockstep bump). Current: 0.4.8 in both. -->
.claude-plugin/plugin.json      → "version": "0.4.8"
.claude-plugin/marketplace.json → "version": "0.4.8"
<!-- SKILL.md render-mode table lives ~lines 97-114; add a blocks-rich row. -->
<!-- TestRichCorpus (Plans 01/02) walks rich-*.md + .expected-blocks.json — extend with rich-mixed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rich-mixed golden corpus + default-path regression assertions</name>
  <files>pkg/slack/testdata/rich-mixed.md, pkg/slack/testdata/rich-mixed.expected-blocks.json, pkg/slack/blocks_rich_test.go</files>
  <action>
Create `pkg/slack/testdata/rich-mixed.md` — a leading `# Title` H1, a prose paragraph with a
`[label](https://example.com)` link, a 3-column GFM table with mixed alignment + a numeric column,
and a `🔧 Tool: ...` line. Create the companion `rich-mixed.expected-blocks.json`
(`{"blocks":[...],"text":...}`) by running RenderRich and pasting verified output — expect a
`header` block, a `markdown` block, a `table` block, and a `context` block in order.

Confirm `TestRichCorpus` (Plans 01/02) auto-discovers rich-mixed via the testdata walk; if it
filters by explicit name, add rich-mixed to the set (RICH-17).

Add an explicit `TestRichDefaultPathFrozen` regression assertion (RICH-18) that re-runs
`TestBlocksCorpus`-style assertions OR simply documents that the default `blocks` corpus is
covered — the canonical guard is the verify command below running both TestRichCorpus and
TestBlocksCorpus green simultaneously.
  </action>
  <verify>
    <automated>go test ./pkg/slack/... -run 'TestRichCorpus|TestBlocksCorpus' -count=1 -timeout 60s</automated>
  </verify>
  <done>rich-mixed corpus passes (RICH-17); the default blocks corpus still passes (RICH-18).</done>
</task>

<task type="auto">
  <name>Task 2: docs + skill + plugin version bump</name>
  <files>docs/slack-notifications.md, skills/slack/SKILL.md, .claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
1. `docs/slack-notifications.md`: add a `## Phase 111` section documenting the `blocks-rich`
   render mode — `markdown` block (GA Feb 2025) for prose + native `[label](url)` anchors,
   dedicated `table` block (GA Aug 2025) for GFM tables, H1→header promotion, 🔧→context,
   `KM_SLACK_AI_FOOTER` opt-in (default off, settable in `/etc/km/notify.env`), the fallback chain
   (blocks-rich → blocks → mrkdwn), and the surface caveats from CONTEXT.md (heading hierarchy
   flattens, table cells aren't markdown, no inline images, email/search see only the text
   fallback, over-limit tables fall back to monospace). State default stays `blocks`; flip is
   Phase 112. State deploy = `make build` then `km init --sidecars`; existing sandboxes pick it up
   on `km destroy && km create`.
2. `skills/slack/SKILL.md`: add a `blocks-rich` row to the render-mode table (~lines 97-114)
   describing it as the opt-in Tier-3 markdown/table-block renderer, with a one-line
   `KM_SLACK_AI_FOOTER` mention referencing the Phase 111 doc section.
3. Bump the plugin version in BOTH `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`
   from `0.4.8` to `0.4.9` (lockstep — clients cache the old version otherwise; per project memory
   project_plugin_version_gates_cache).
  </action>
  <verify>
    <automated>grep -q "Phase 111" docs/slack-notifications.md && grep -q "blocks-rich" skills/slack/SKILL.md && grep -q '"0.4.9"' .claude-plugin/plugin.json && grep -q '"0.4.9"' .claude-plugin/marketplace.json</automated>
  </verify>
  <done>Phase 111 doc section, blocks-rich skill row, and lockstep 0.4.9 version bump all present.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/slack/... ./cmd/km-slack/... -count=1 -timeout 120s` green (full phase suite).
- `go test ./pkg/compiler/... -run 'TestUserdataAdditionalVolumeOnly_Golden|TestH1ByteIdentity' -count=1 -timeout 120s` green (RICH-20: userdata goldens untouched).
- `go test ./... -count=1 -timeout 600s` full repo green — read go test's OWN exit code, not a pipe (feedback_check_go_test_exit_not_pipe) — before /gsd:verify-work.
- Plugin versions match (0.4.9) in both files.
</verification>

<success_criteria>
- Mixed H1+prose+table+tool corpus renders to expected JSON (RICH-17).
- Default blocks corpus + compiler userdata goldens unchanged (RICH-18, RICH-20).
- Phase 111 documented in docs + skill; plugin version bumped in lockstep.
</success_criteria>

<output>
After completion, create `.planning/phases/111-rich-slack-rendering-markdown-and-table-blocks-opt-in/111-04-SUMMARY.md`
</output>
