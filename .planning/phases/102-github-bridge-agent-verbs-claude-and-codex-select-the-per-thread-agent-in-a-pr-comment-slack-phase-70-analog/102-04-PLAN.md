---
phase: 102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog
plan: 04
type: tdd
wave: 2
depends_on: ["102-01", "102-02"]
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/doctor_github_commands_test.go
  - pkg/github/bridge/commands.go
  - pkg/github/bridge/webhook_handler.go
  - pkg/github/bridge/commands_test.go
  - docs/github-bridge.md
  - CLAUDE.md
autonomous: true
requirements: [GH-AGENT-VERB, GH-AGENT-PROFILE]
must_haves:
  truths:
    - "km doctor WARNs when a github.commands entry shadows the reserved name claude, codex, or help"
    - "The /help built-in reply lists /claude and /codex and notes the thread's current agent"
    - "docs/github-bridge.md has a Phase 102 operator section covering the verbs, precedence, Codex-profile precondition, and deploy steps"
    - "CLAUDE.md has a Phase 102 bullet summarizing the verbs + the make build-lambdas + km init deploy surface"
  artifacts:
    - path: "internal/app/cmd/doctor.go"
      provides: "Reserved-shadow check extended to claude/codex/help"
      contains: "claude"
    - path: "pkg/github/bridge/commands.go"
      provides: "buildHelpReply lists agents + current thread agent"
      contains: "/claude"
    - path: "docs/github-bridge.md"
      provides: "Phase 102 operator runbook section"
      contains: "Phase 102"
    - path: "CLAUDE.md"
      provides: "Phase 102 summary bullet"
      contains: "Phase 102"
  key_links:
    - from: "internal/app/cmd/doctor.go reserved-shadow check"
      to: "github.commands map"
      via: "loop over [help, claude, codex]"
      pattern: "help.*claude.*codex|reserved"
    - from: "pkg/github/bridge/commands.go buildHelpReply"
      to: "/help reply body"
      via: "agent listing + current thread agent line"
      pattern: "Available agents"
---

<objective>
Round out the bridge-layer UX and operator surface for Phase 102: extend the `km doctor`
reserved-name shadow check to cover `claude`/`codex` (not just `help`), extend the `/help`
built-in reply to advertise the agent verbs + the thread's current agent, and write the
operator documentation (docs/github-bridge.md § Phase 102 + a CLAUDE.md bullet).

Purpose: Closes the self-documenting-UX and operator-guardrail decisions from CONTEXT.md (D6
precondition, reserved tokens, /help extension). All Go changes are unit-testable; the docs
are the operator-facing record this phase requires.

Output: doctor reserved-shadow loop covers all three reserved verbs; `buildHelpReply` lists
agents + current thread agent; docs/github-bridge.md gains a Phase 102 section; CLAUDE.md gains
a Phase 102 bullet.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-CONTEXT.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-RESEARCH.md

@internal/app/cmd/doctor.go
@internal/app/cmd/doctor_github_commands_test.go
@pkg/github/bridge/commands.go
@docs/github-bridge.md

<interfaces>
<!-- Verified against live code 2026-06-08. -->

doctor.go reserved-shadow check (lines 1502-1504) — today only "help":
```go
if _, hasHelp := commands["help"]; hasHelp {
    warnings = append(warnings, `command "help" shadows the reserved built-in /help reply — rename to avoid unexpected behavior`)
}
```
Replace with a loop over `[]string{"help", "claude", "codex"}` emitting a WARN per shadowed name.
Also update the remediation text at line 1567 (currently mentions only 'help') to reference the
agent verbs.

buildHelpReply (commands.go:374) — today `func buildHelpReply(commands map[string]CommandEntry,
effectiveDefaultCmd string) string`. Extend to advertise agents + current thread agent. Add a
`currentAgentType string` parameter (RESEARCH pattern). Prepend an "Available agents" block:
```
**Available agents:**
- `/claude` — dispatch this thread to Claude
- `/codex` — dispatch this thread to Codex
[**Current thread agent:** `<currentAgentType>`   — only when currentAgentType != ""]
```
The signature change cascades to RunCommandPass / the /help path in Handle(). When the thread
has no row yet (fresh), pass "". When a row exists, pass the agentType returned from the Plan-02
LookupSandbox (this is WHY this plan depends on 102-02). Wire the LookupSandbox agentType through
to the help reply at the /help build site in Handle()/RunCommandPass.

doctor_github_commands_test.go — extend TestDoctorGitHubCommandsHelpShadow (or add a sibling
table case) to assert a WARN fires for a `github.commands` entry named "claude" and one named
"codex", in addition to the existing "help" case.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN — doctor reserved-shadow for claude/codex</name>
  <files>internal/app/cmd/doctor_github_commands_test.go, internal/app/cmd/doctor.go</files>
  <behavior>
    - A github.commands map containing key "claude" → exactly one WARN naming claude as reserved.
    - Containing "codex" → one WARN.
    - Containing "help" → one WARN (existing, must still pass).
    - A clean commands map (no reserved names) → no shadow WARN.
  </behavior>
  <action>
    RED: extend the existing help-shadow test in doctor_github_commands_test.go with claude/codex
    cases (assert FAIL first against current single-help check).
    GREEN: replace the doctor.go lines 1502-1504 single-help check with a loop over
    `[]string{"help","claude","codex"}` emitting one WARN each; update the remediation string at
    line 1567 to mention the agent verbs.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run TestDoctorGitHubCommands -count=1</automated>
  </verify>
  <done>doctor WARNs on help/claude/codex shadows; clean map produces none; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /help reply lists agents + current thread agent</name>
  <files>pkg/github/bridge/commands.go, pkg/github/bridge/webhook_handler.go, pkg/github/bridge/commands_test.go</files>
  <behavior>
    - buildHelpReply output contains "/claude" and "/codex" agent lines.
    - When currentAgentType=="codex", output contains "Current thread agent" and "codex".
    - When currentAgentType=="", no current-agent line (fresh thread).
    - /help with an agent verb present still returns the help reply (Pitfall 7 — HelpRequested
      short-circuit wins) — assert via the Plan-01 handler test path or a commands_test case.
  </behavior>
  <action>
    Add `currentAgentType string` to buildHelpReply (line 374) and prepend the agents block per
    <interfaces>. Cascade the signature through RunCommandPass and the /help build site in
    Handle(): pass the agentType from the Plan-02 LookupSandbox (already called in Handle at
    webhook_handler.go:303 — thread it to the help reply; pass "" when no row). Extend
    commands_test.go to assert the agent listing and the current-thread-agent line.
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... -run 'TestCommandParse|TestHandle_AgentVerb|TestBuildHelp|Help' -count=1</automated>
  </verify>
  <done>/help reply advertises /claude + /codex and the current thread agent; /help still wins over a co-present agent verb.</done>
</task>

<task type="auto">
  <name>Task 3: Operator docs — docs/github-bridge.md § Phase 102 + CLAUDE.md bullet</name>
  <files>docs/github-bridge.md, CLAUDE.md</files>
  <action>
    docs/github-bridge.md: add a "## Phase 102 — agent verbs (/claude, /codex)" section (mirror the
    structure of the existing Phase 100/101 sections). Cover: syntax (slash verbs, anywhere, code-
    stripped, ≤1 agent verb, two-verb error reply); composition with Phase 99 commands
    (`/codex /patch …`); per-thread persistence + precedence (verb > thread agent_type > profile
    default); cross-agent switch resets the session; the Codex-capable-profile PRECONDITION (lean
    github-review is Claude-only) + the runtime helpful-error comment; reserved tokens + km doctor
    WARN; /help extension; back-compat (no verb = byte-identical); and the DEPLOY surface:
    `make build-lambdas` (clean) + `km init --dry-run=false` (NOT --sidecars — no SandboxProfile
    schema change), bridge + poller redeploy together, agent_type is schema-on-write (no TF/migration),
    existing sandboxes need `km destroy && km create` to gain the new poller.
    CLAUDE.md: add a Phase 102 bullet in the phase-history block (mirror the Phase 100/101 bullets),
    plus a row in the "Where to look" table pointing /claude//codex agent verbs to docs/github-bridge.md § Phase 102.
    Use generic placeholders only (no real tenants/customers).
  </action>
  <verify>
    <automated>grep -q "Phase 102" docs/github-bridge.md && grep -q "Phase 102" CLAUDE.md && grep -q "make build-lambdas" docs/github-bridge.md && echo OK</automated>
  </verify>
  <done>docs/github-bridge.md has a complete Phase 102 operator section (incl. deploy + Codex precondition); CLAUDE.md has the Phase 102 bullet + where-to-look row.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run TestDoctorGitHubCommands -count=1` green
- `go test ./pkg/github/bridge/... -count=1` green
- docs/github-bridge.md § Phase 102 + CLAUDE.md bullet present with deploy surface documented
</verification>

<success_criteria>
ROADMAP success criterion #6 (reserved tokens + km doctor WARN) met; /help self-documents the
agent verbs; operators have a complete Phase 102 runbook.
</success_criteria>

<output>
After completion, create `.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-04-SUMMARY.md`
</output>
