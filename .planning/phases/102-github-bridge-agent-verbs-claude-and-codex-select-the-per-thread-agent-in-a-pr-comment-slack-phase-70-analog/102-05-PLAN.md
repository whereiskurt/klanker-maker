---
phase: 102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog
plan: 05
type: execute
wave: 3
depends_on: ["102-01", "102-02", "102-03", "102-04"]
files_modified:
  - .planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-UAT.md
autonomous: false
requirements: [GH-AGENT-E2E]
must_haves:
  truths:
    - "A deploy-surface audit confirms the bridge + poller changes are actually reachable via make build-lambdas + km init --dry-run=false (NOT --sidecars)"
    - "A live GH-AGENT-E2E sequence proves /codex /patch dispatches Codex, persists agent_type=codex, a no-verb follow-up continues Codex, and /claude switches back with a fresh session"
    - "/codex on a Claude-only github-review profile posts the helpful-error comment (no stranded turn)"
    - "Two distinct agent verbs in one comment produce the single error reply with no dispatch"
  artifacts:
    - path: ".planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-UAT.md"
      provides: "GH-AGENT-E2E runbook + deploy-surface checklist + recorded results"
      contains: "GH-AGENT-E2E"
  key_links:
    - from: "userdata.go poller change"
      to: "deployed sandbox"
      via: "make build-lambdas + km init --dry-run=false + km destroy && km create"
      pattern: "km init --dry-run=false"
    - from: "bridge verb-parse + envelope"
      to: "deployed bridge Lambda"
      via: "same make build-lambdas + km init redeploy"
      pattern: "build-lambdas"
---

<objective>
Verify Phase 102 is DEPLOYABLE (not merely green-compiling) and prove the end-to-end agent-verb
behavior on a live GitHub PR. This is the phase gate: a deploy-surface audit plus a recorded
GH-AGENT-E2E run.

Purpose: Honors the "verify deploy surface, not just code" lesson (feedback_verify_deploy_surface_
not_just_code) — Phase 97 shipped 7 deploy-surface gaps that code-green did not catch. Phase 102
has no new Lambda/queue/IAM/TF module (lower risk), but the bridge + poller still must be confirmed
reachable through the documented deploy path, and the runtime poller behaviors (no unit harness)
must be proven live.

Output: `102-UAT.md` with a completed deploy-surface checklist and the recorded results of the
two-comment GH-AGENT-E2E sequence.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-CONTEXT.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-RESEARCH.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-VALIDATION.md
@docs/github-bridge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy-surface audit + full test suite + UAT runbook authoring</name>
  <files>.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-UAT.md</files>
  <action>
    Run the full phase test gate and author the UAT runbook. Specifically:
    1. Full suite: `go test ./pkg/github/bridge/... ./internal/app/cmd/... ./pkg/compiler/... -count=1`
       and `make build` — record both as the code-green baseline.
    2. Deploy-surface AUDIT (the lesson — code-green != deployable). Confirm and record:
       - NO new Lambda / SQS queue / DDB table / TF module was added (Phase 102 is additive to
         existing surfaces) — so NO init.go module-list entry, NO new live terragrunt unit, NO
         lambdaBuilds() entry is required. Verify by grepping that no new files were added under
         infra/modules or infra/live and init.go's curated lists are untouched.
       - agent_type is schema-on-write on km-github-threads → confirm NO change under the Phase 98
         dynamodb module (no migration, no TF diff).
       - NO SandboxProfile schema change → `km init --sidecars` is NOT the deploy verb.
       - The bridge Lambda zip is produced by `make build-lambdas` and the env/code is applied by
         `km init --dry-run=false` (full terragrunt apply), per the existing github-bridge module.
       - The poller (userdata.go) is compiled by the create-handler Lambda → same
         `make build-lambdas` + `km init --dry-run=false`; existing sandboxes need
         `km destroy && km create`.
       Record the exact deploy command sequence in 102-UAT.md as a checklist.
    3. Author the GH-AGENT-E2E runbook in 102-UAT.md (operator-executed in Task 2):
       - Deploy: `make build-lambdas` + `km init --dry-run=false` (front-door / target install).
       - `km create` a Codex-capable profile sandbox with `notification.github.inbound.enabled: true`
         on a test repo (and separately note the Claude-only `profiles/github-review.yaml` for the
         GH-AGENT-PROFILE check).
       - Comment sequence on a real PR/issue:
         (a) `@<bot> /codex /patch <task>` → expect Codex dispatch; DDB row agent_type=codex;
             agent_session_id captured (Codex thread_id).
         (b) `@<bot> <follow-up, no verb>` → expect Codex continues (resumes captured session).
         (c) `@<bot> /claude <task>` → expect cross-agent switch: fresh Claude session (NO --resume
             of the codex session in journald), DDB row agent_type=claude, new agent_session_id.
         (d) `@<bot> /claude /codex …` → expect single "🤖 Specify one agent" reply, NO dispatch.
         (e) On the Claude-only github-review sandbox: `@<bot> /codex …` → expect the
             "This sandbox's profile has no Codex" comment, no stranded turn.
       - For each: the observable signal (journald `km-github-inbound-poller` lines, `aws dynamodb
         get-item` on km-github-threads, the posted GitHub comment).
       Mark the runbook status: pending operator execution.
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... ./internal/app/cmd/... ./pkg/compiler/... -count=1 && make build</automated>
  </verify>
  <done>Full suite + make build green; 102-UAT.md contains the deploy-surface checklist (confirming no new Lambda/queue/TF/sidecars, correct deploy verbs) and the complete GH-AGENT-E2E runbook (a-e) ready for operator execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live GH-AGENT-E2E verification (operator-executed)</name>
  <action>Operator executes the GH-AGENT-E2E runbook authored in 102-UAT.md against a live test repo (deploy via make build-lambdas + km init --dry-run=false, km create the sandbox(es), run the comment sequence a-e, record PASS/FAIL + evidence in 102-UAT.md). See how-to-verify for the exact steps.</action>
  <what-built>
    Phase 102 agent verbs: bridge verb-parse + envelope.Agent (Plan 01), agent_type DDB
    persistence (Plan 02), poller precedence/switch/codex-guard/write-back (Plan 03), doctor
    reserved-shadow + /help extension + docs (Plan 04). Deployed via `make build-lambdas` +
    `km init --dry-run=false`; the E2E sandbox(es) created via `km create` (existing sandboxes
    need `km destroy && km create`).
  </what-built>
  <how-to-verify>
    Execute the GH-AGENT-E2E runbook authored in 102-UAT.md against a live test repo:
    1. Deploy: `make build-lambdas` then `km init --dry-run=false` (target install). Confirm
       NO use of `km init --sidecars`.
    2. `km create` a Codex-capable profile sandbox (github inbound enabled) + note the Claude-only
       github-review sandbox for step 5.
    3. Comment `@<bot> /codex /patch <task>` on a PR → confirm Codex ran (journald) and
       `aws dynamodb get-item` on km-github-threads shows `agent_type=codex` + a captured session.
    4. Comment a no-verb follow-up → confirm Codex continues; then `@<bot> /claude <task>` →
       confirm a FRESH Claude session (no `--resume` of the codex session in journald) and
       `agent_type=claude` in DDB.
    5. Comment `@<bot> /claude /codex …` → confirm the single "🤖 Specify one agent — found
       /claude and /codex." reply and NO dispatch. On the github-review (Claude-only) sandbox,
       comment `@<bot> /codex …` → confirm the "no Codex here" helpful comment, no stranded turn.
    6. Optionally add a `github.commands` entry named `claude`/`codex`/`help` and run `km doctor`
       → confirm the reserved-shadow WARN.
    Record PASS/FAIL + observed evidence in 102-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" once the GH-AGENT-E2E sequence passes (or describe failures to feed a --gaps pass).</resume-signal>
</task>

</tasks>

<verification>
- Full suite `go test ./pkg/github/bridge/... ./internal/app/cmd/... ./pkg/compiler/... -count=1` + `make build` green
- 102-UAT.md deploy-surface checklist confirms additive-only surface + correct deploy verbs (make build-lambdas + km init --dry-run=false, NOT --sidecars)
- Live GH-AGENT-E2E sequence (a-e) recorded PASS in 102-UAT.md
</verification>

<success_criteria>
ROADMAP success criteria #1-#7 are confirmed live end-to-end; the phase is proven DEPLOYABLE
(not merely compiling), closing GH-AGENT-E2E.
</success_criteria>

<output>
After completion, create `.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-05-SUMMARY.md`
</output>
