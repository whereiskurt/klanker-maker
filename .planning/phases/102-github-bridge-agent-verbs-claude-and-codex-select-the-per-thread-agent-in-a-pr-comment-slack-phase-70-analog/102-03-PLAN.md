---
phase: 102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog
plan: 03
type: execute
wave: 2
depends_on: ["102-01", "102-02"]
files_modified:
  - pkg/compiler/userdata.go
autonomous: true
requirements: [GH-AGENT-POLLER, GH-AGENT-SWITCH, GH-AGENT-PROFILE]
must_haves:
  truths:
    - "The GitHub poller parses AGENT_OVERRIDE from the envelope .agent field"
    - "EFFECTIVE_AGENT is computed by precedence: verb override > thread agent_type > profile default"
    - "A cross-agent switch (override differs from stored agent_type with a session present) clears RESUME_ARG and the stored session so the new agent starts fresh"
    - "On a successful turn the poller writes agent_type back to km-github-threads alongside agent_session_id"
    - "When EFFECTIVE_AGENT=codex but the codex binary is absent, the poller posts a helpful km-github comment and acks the message instead of stranding the turn"
    - "No verb and no stored agent_type dispatches the profile default — byte-identical to today"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "GitHub poller precedence block, cross-agent reset, agent_type write-back, codex-missing guard"
      contains: "AGENT_OVERRIDE"
  key_links:
    - from: "envelope .agent field (Plan 01)"
      to: "poller AGENT_OVERRIDE"
      via: "jq -r '.agent // empty'"
      pattern: "AGENT_OVERRIDE=.*\\.agent"
    - from: "poller EFFECTIVE_AGENT"
      to: "km-github-threads agent_type write-back"
      via: "update-item SET agent_session_id, agent_type"
      pattern: "agent_type"
    - from: "poller EFFECTIVE_AGENT=codex"
      to: "km-github comment helpful error"
      via: "command -v codex guard"
      pattern: "command -v codex"
---

<objective>
Replace the `EFFECTIVE_AGENT="$AGENT"` hardwire in the GitHub inbound poller with the D4
precedence block (verb > thread agent_type > profile default), the D5 cross-agent session reset,
the agent_type write-back, and the D6 codex-missing helpful-error guard — mirroring the proven
Slack poller (userdata.go:1670-1871).

Purpose: This is the runtime half of Phase 102 — the piece that actually varies which agent runs.
The dispatch fork + session capture already exist (userdata.go:2253-2317); this plan only adds the
agent selection + persistence around them. The change lives in a bash heredoc inside a Go string;
there is NO unit harness for poller bash (same as Phases 97/98/99) — behaviors are verified manually
in Plan 05's E2E UAT. The Go gate here is that userdata still COMPILES and renders.

Output: the GitHub poller heredoc gains AGENT_OVERRIDE parsing, THREAD_AGENT_TYPE read, the
precedence + cross-agent-switch block, the codex-missing guard, and the agent_type write-back.

DEPENDS ON: Plan 01 (envelope `.agent` field exists) and Plan 02 (agent_type read/write semantics
established — the poller does its DDB I/O via raw AWS CLI, but the projection/update shape must
match what Plan 02 standardized).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-CONTEXT.md
@.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-RESEARCH.md

# The GitHub poller heredoc to edit (lines ~2079-2342) AND the Slack poller analog to mirror
# (lines ~1670-1871). Read BOTH before editing — the Slack block is the proven template.
@pkg/compiler/userdata.go

<interfaces>
<!-- Verified against live code 2026-06-08. Line numbers drift as edits accumulate; locate by anchor. -->

GitHub poller anchors (userdata.go):
- ~line 2096: `AGENT="${KM_AGENT:-claude}"` — profile default, read once at boot. LEAVE AS-IS.
- ~lines 2147-2153: envelope field parsing (REPO, NUMBER, COMMENT_BODY, HTML_URL via jq).
  ADD HERE: `AGENT_OVERRIDE=$(echo "$BODY" | jq -r '.agent // empty' 2>/dev/null || true)`
- ~lines 2181-2188: DDB get-item + session lookup. Projection currently `agent_session_id`.
  EXTEND projection to include `agent_type`; ADD a THREAD_AGENT_TYPE extraction:
  `THREAD_AGENT_TYPE=$(echo "$DDB_THREAD" | jq -r '.Item.agent_type.S // empty' 2>/dev/null || true)`
- ~line 2248: `EFFECTIVE_AGENT="$AGENT"` — REPLACE with the precedence block below.
- ~lines 2253-2317: existing Claude/Codex dispatch fork + session capture. LEAVE AS-IS
  (it already keys on EFFECTIVE_AGENT).
- ~lines 2319-2326: DDB update-item write-back (success path). EXTEND to also write agent_type.

Precedence + cross-agent-switch block (replaces line 2248) — mirrors Slack poller lines 1670-1871:
```bash
[ -z "$THREAD_AGENT_TYPE" ] && THREAD_AGENT_TYPE="$AGENT"   # Pitfall 2: default to profile

if [ -n "$AGENT_OVERRIDE" ]; then
  if [ "$AGENT_OVERRIDE" != "$THREAD_AGENT_TYPE" ] && [ -n "$GITHUB_SESSION" ]; then
    echo "[km-github-inbound-poller] cross-agent switch ${THREAD_AGENT_TYPE}->${AGENT_OVERRIDE}; resetting session"
    GITHUB_SESSION=""    # Pitfall 3: clear both together
    RESUME_ARG=""
  fi
  EFFECTIVE_AGENT="$AGENT_OVERRIDE"
else
  EFFECTIVE_AGENT="$THREAD_AGENT_TYPE"
fi
```
IMPORTANT ordering: this block must run AFTER GITHUB_SESSION and RESUME_ARG are computed from the
thread lookup, and BEFORE the dispatch fork (so RESUME_ARG reset takes effect). Confirm against the
actual RESUME_ARG computation line in the GitHub poller and place accordingly (mirror Slack ordering).

Codex-missing guard (D6) — insert immediately before the `if [ "$EFFECTIVE_AGENT" = "codex" ]`
dispatch fork:
```bash
if [ "$EFFECTIVE_AGENT" = "codex" ] && ! command -v codex >/dev/null 2>&1; then
  /opt/km/bin/km-github comment --repo "$REPO" --number "$NUMBER" \
    --body "This sandbox's profile has no Codex; \`/codex\` is unavailable here."
  aws sqs delete-message --queue-url "$QUEUE_URL" --receipt-handle "$RECEIPT" --region "$REGION" 2>/dev/null || true
  continue
fi
```
Use the EXACT km-github comment + sqs delete-message + REGION/QUEUE_URL/RECEIPT variable names
already present in the surrounding poller scope (confirm by reading; do not invent var names).

Write-back extension (~line 2319-2326): change the update-expression from
`SET agent_session_id = :sid` to `SET agent_session_id = :sid, agent_type = :at` and add the
`:at` attribute value `{"S":"<EFFECTIVE_AGENT>"}`. Per Pitfall 5 this write is correct even when
EFFECTIVE_AGENT is the profile default (it pins the thread — intended D3 behavior).

GOTCHA: this is a Go raw-string heredoc. Escapes (backticks, $, quoting) must survive Go string
literal + bash. Mirror the exact escaping style the surrounding GitHub/Slack poller code already uses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENT_OVERRIDE parse + extended DDB read (THREAD_AGENT_TYPE)</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    In the GitHub poller heredoc: (a) add `AGENT_OVERRIDE=$(echo "$BODY" | jq -r '.agent // empty'
    ...)` alongside the existing envelope-field jq parses (~2147-2153); (b) extend the
    km-github-threads get-item projection (~2181-2188) to include `agent_type` and add the
    THREAD_AGENT_TYPE extraction with the `[ -z ... ] && THREAD_AGENT_TYPE="$AGENT"` default
    (Pitfall 2). Do NOT yet touch the EFFECTIVE_AGENT hardwire or write-back.
    After editing, `make build` (NOT bare go build — needs version ldflags) to confirm the Go
    string + heredoc still compile and the binary builds.
  </action>
  <verify>
    <automated>make build && go vet ./pkg/compiler/... </automated>
  </verify>
  <done>userdata.go compiles via `make build`; poller parses .agent into AGENT_OVERRIDE and reads agent_type into THREAD_AGENT_TYPE (defaulting to $AGENT).</done>
</task>

<task type="auto">
  <name>Task 2: Precedence block + cross-agent switch + codex guard + agent_type write-back</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    1. Replace `EFFECTIVE_AGENT="$AGENT"` (~2248) with the D4 precedence + D5 cross-agent-switch
       block from <interfaces>, placed after GITHUB_SESSION/RESUME_ARG are computed and before the
       dispatch fork. Mirror the Slack poller (lines 1670-1871) for ordering and the reset (clear
       GITHUB_SESSION + RESUME_ARG together — Pitfall 3).
    2. Insert the D6 codex-missing guard immediately before the `if [ "$EFFECTIVE_AGENT" = "codex" ]`
       dispatch fork, using the real surrounding var names (QUEUE_URL/RECEIPT/REGION).
    3. Extend the success-path write-back (~2319-2326): update-expression
       `SET agent_session_id = :sid, agent_type = :at` with `:at` = EFFECTIVE_AGENT.
    Then `make build` to confirm the heredoc + Go literal still compile.
  </action>
  <verify>
    <automated>make build && go vet ./pkg/compiler/...</automated>
  </verify>
  <done>The hardwire is gone; EFFECTIVE_AGENT follows D4 precedence; cross-agent switch clears session+RESUME_ARG; codex-missing posts helpful comment + acks; write-back persists agent_type; `make build` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Render-and-grep guard against escaping regressions</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    The poller is bash-in-Go-string with no unit harness, so guard against escaping/compile drift:
    run the compiler package tests (`go test ./pkg/compiler/... -count=1`) to confirm any
    userdata-render test still passes. If a userdata render/golden test exists, confirm the rendered
    output contains the new tokens (AGENT_OVERRIDE, THREAD_AGENT_TYPE, "command -v codex",
    "agent_type = :at"). If no render test exists, write a minimal Go test that compiles the GitHub
    poller for a github-inbound-enabled profile and greps the rendered string for those four tokens
    (this is the only automated signal available for the bash block).
    Document in the SUMMARY that all runtime behaviors (precedence, switch, codex-guard, write-back)
    are verified manually in Plan 05's E2E UAT — NOT by automated poller tests.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... -count=1</automated>
  </verify>
  <done>Compiler tests green; rendered GitHub poller contains the four Phase 102 tokens; SUMMARY notes the manual-E2E verification path for runtime behavior.</done>
</task>

</tasks>

<verification>
- `make build` clean (Go string + bash heredoc compiles with version ldflags)
- `go test ./pkg/compiler/... -count=1` green
- Rendered poller contains AGENT_OVERRIDE, THREAD_AGENT_TYPE, `command -v codex`, `agent_type = :at`
- Runtime behavior (precedence, switch, codex-guard, persistence) deferred to Plan 05 E2E UAT (poller bash has no unit harness — same as Phases 97/98/99)
</verification>

<success_criteria>
ROADMAP success criteria #1, #2, #4, #5 are satisfied at the runtime layer: per-thread agent
selection + persistence, profile-default back-compat, cross-agent fresh session, and the
Codex-missing helpful error — all dispatched through the existing fork.
</success_criteria>

<output>
After completion, create `.planning/phases/102-github-bridge-agent-verbs-claude-and-codex-select-the-per-thread-agent-in-a-pr-comment-slack-phase-70-analog/102-03-SUMMARY.md`
</output>
