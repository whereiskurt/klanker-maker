---
phase: 116-km-check-serverless-check-runner
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/dispatch.go
  - pkg/dispatch/interfaces.go
  - pkg/dispatch/dispatch_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "pkg/dispatch.ResumeOrCreate resolves an alias and chooses agent-run (exists) vs cold-create/skip (absent)"
    - "The warm/resume terminal action is an SSM agent-run dispatch sink, NOT an SQS enqueue"
    - "Cooldown via the nonces table (key check-trigger:{name}) suppresses a second fire in-window"
    - "Bridges are NOT modified by this plan (pkg/dispatch is a standalone library + tests)"
  artifacts:
    - path: "pkg/dispatch/dispatch.go"
      provides: "ResumeOrCreate decision function"
      min_lines: 40
    - path: "pkg/dispatch/interfaces.go"
      provides: "small interfaces: AliasResolver, AgentRunSink, ColdCreateSink, NonceStore"
    - path: "pkg/dispatch/dispatch_test.go"
      provides: "5 unit tests with mock sinks"
  key_links:
    - from: "pkg/dispatch/dispatch.go"
      to: "AliasResolver.ResolveByAliasWithStatus"
      via: "alias resolution returning (sandboxID, status, err)"
      pattern: "ResolveByAliasWithStatus"
    - from: "pkg/dispatch/dispatch.go"
      to: "AgentRunSink (warm) / ColdCreateSink (absent)"
      via: "terminal action selection on resolve result"
      pattern: "AgentRun|ColdCreate"
---

<objective>
Factor the alias-resolution + resume/cold DECISION into a standalone `pkg/dispatch`
package with clean interfaces, built test-first. This is the shared decision core
that Plan 116-06 (ttl-handler) wires into a `check-dispatch` case. Per the LOCKED
design (CONTEXT + spec Stage B): the warm/resume terminal is an **SSM agent-run
dispatch**, NOT an SQS FIFO enqueue — so the sinks are an `AgentRunSink` (warm) and
a `ColdCreateSink` (absent), plus a `NonceStore` for cooldown.

IMPORTANT — supersedes RESEARCH §2: RESEARCH proposed an SQS-based signature with
`SQSSender`. That is OBSOLETE; the resolved warm path is agent-run SSM dispatch
(CONTEXT "RESOLVED", spec lines 216-235). Do NOT add an SQS sink. Do NOT modify the
GitHub/H1/Slack bridges in this plan (parity preserved; spec §Stage B consequence b).

Purpose: Keep the decision logic independently testable (5 mock-based unit tests)
so Plan 116-06 wiring is mechanical.

Output: `pkg/dispatch/` (interfaces + ResumeOrCreate + tests).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md

# Logic to mirror (read for the 3-way decision shape + sentinel):
@pkg/github/bridge/aws_adapters.go
@pkg/github/bridge/interfaces.go

<interfaces>
<!-- Existing anchors (pkg/github/bridge) the new interfaces MIRROR but do not import: -->
<!-- ResolveByAliasWithStatus(ctx, alias) (sandboxID, status string, err error)  (aws_adapters.go:273) -->
<!-- ErrNoResumableInstance sentinel (aws_adapters.go:92) — terminal: instance gone -->
<!-- DynamoGitHubNonceStore.CheckAndStore(ctx, key, ttlSeconds) (alreadySeen bool, err error) (aws_adapters.go:200) -->

<!-- NEW pkg/dispatch interfaces (small, mockable): -->
type AliasResolver interface {
    ResolveByAliasWithStatus(ctx context.Context, alias string) (sandboxID, status string, err error)
}
type AgentRunSink interface {
    // Warm path: dispatch the prompt to the existing (auto-resumed) sandbox via SSM.
    DispatchAgentRun(ctx context.Context, sandboxID, prompt string) error
}
type ColdCreateSink interface {
    // Absent path: cold-create a new sandbox carrying the prompt envelope.
    ColdCreate(ctx context.Context, alias, profile, prompt string) error
}
type NonceStore interface {
    CheckAndStore(ctx context.Context, key string, ttlSeconds int) (alreadySeen bool, err error)
}

<!-- ResumeOrCreate decision (the locked Stage-B logic): -->
func ResumeOrCreate(ctx, check, alias, prompt, profile, onAbsent string,
    cooldownSeconds int,
    resolver AliasResolver, agentRun AgentRunSink, cold ColdCreateSink,
    nonces NonceStore, log *slog.Logger) error
//   cooldown FIRST: if cooldownSeconds>0 && nonces.CheckAndStore("check-trigger:"+check, cooldownSeconds) alreadySeen -> log+drop.
//   resolve alias:
//     err == nil && status != "" (any non-terminated state) -> agentRun.DispatchAgentRun(sandboxID, prompt)
//     resolve err (alias absent) OR ErrNoResumableInstance-equiv -> branch on onAbsent:
//        "cold-create" -> cold.ColdCreate(alias, profile, prompt)
//        "skip"        -> log + drop, return nil
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write pkg/dispatch interfaces + failing ResumeOrCreate tests (RED)</name>
  <files>pkg/dispatch/interfaces.go, pkg/dispatch/dispatch_test.go</files>
  <behavior>
    - TestResumeOrCreate_RunningAgentRun: resolver returns (id,"running",nil) -> AgentRunSink.DispatchAgentRun called with (id, prompt); ColdCreate NOT called.
    - TestResumeOrCreate_StoppedAgentRun: resolver returns (id,"stopped",nil) -> AgentRunSink.DispatchAgentRun called (auto-resume is the sink's responsibility; dispatch decision is "exists -> agent-run").
    - TestResumeOrCreate_AbsentColdCreate: resolver returns ("","",errNotFound), onAbsent="cold-create" -> ColdCreateSink.ColdCreate called with (alias, profile, prompt); AgentRun NOT called.
    - TestResumeOrCreate_AbsentSkip: resolver returns absent, onAbsent="skip" -> neither sink called; returns nil.
    - TestResumeOrCreate_Cooldown: first call fires; second call (nonces returns alreadySeen=true) -> neither sink called, returns nil.
  </behavior>
  <action>
Create `pkg/dispatch/interfaces.go` with the four interfaces from the interfaces
block. Create `pkg/dispatch/dispatch_test.go` with mock implementations
(record call args) and the 5 table-or-named tests above. Tests reference
`dispatch.ResumeOrCreate` which does not yet exist -> compile/RED.
Use `errors.New`/a package sentinel for the "absent" resolver error in tests.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/dispatch/ -count=1 -timeout 60s > /tmp/116-02-red.out 2>&1; echo "EXIT=$? (expect non-zero: ResumeOrCreate undefined)"; tail -15 /tmp/116-02-red.out</automated>
  </verify>
  <done>interfaces.go compiles; dispatch_test.go references ResumeOrCreate; test run fails because ResumeOrCreate is undefined (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ResumeOrCreate (GREEN)</name>
  <files>pkg/dispatch/dispatch.go</files>
  <action>
Implement `ResumeOrCreate` exactly per the interfaces block decision logic:
  1. Cooldown first (if cooldownSeconds>0): `nonces.CheckAndStore("check-trigger:"+check, cooldownSeconds)`;
     if alreadySeen -> log debug "check_cooldown_suppressed" + return nil. If the
     nonce store ERRORS, fail-open (proceed to dispatch — never strand a real fire
     on a read error; mirror bridge fail-open semantics) and log a warning.
  2. resolver.ResolveByAliasWithStatus(alias):
     - err == nil && status != "" -> agentRun.DispatchAgentRun(ctx, sandboxID, prompt); return its err.
       (The "exists -> agent-run" decision is independent of stopped/running; the
       AgentRunSink owns auto-resume per the spec — Stage B handleAgentRun AutoStart=true.)
     - otherwise (resolve err / absent) -> switch onAbsent:
         "cold-create" (default if empty) -> cold.ColdCreate(ctx, alias, profile, prompt)
         "skip" -> log "check_dispatch_skipped_absent" + return nil
Use `*slog.Logger`. Keep the function pure decision + delegation — no AWS SDK calls
here (sinks are the SDK boundary). Mirror the ErrNoResumableInstance-style terminal
handling conceptually but the AgentRunSink/ColdCreateSink abstraction means
pkg/dispatch itself does not import EC2 types.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/dispatch/ -count=1 -timeout 60s > /tmp/116-02-green.out 2>&1; echo "EXIT=$?"; tail -15 /tmp/116-02-green.out</automated>
  </verify>
  <done>All 5 pkg/dispatch tests pass (GREEN); ResumeOrCreate implements cooldown-first + 3-way decision; no SQS sink; bridges untouched.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/dispatch/ -count=1 -timeout 60s` green (5 tests).
- `pkg/dispatch` imports NO bridge package and NO SQS types.
- No file under pkg/github/bridge or pkg/h1/bridge modified (git status clean there).
</verification>

<success_criteria>
Covers Phase 116 Success Criterion 4 (shared pkg/dispatch helper performs
alias-resolution + resume/cold decision; warm = agent-run SSM dispatch, NOT new
queue; cold = cold-create; bridges not modified). Decision core is unit-tested and
ready for mechanical wiring in Plan 116-06.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-02-SUMMARY.md`
</output>
