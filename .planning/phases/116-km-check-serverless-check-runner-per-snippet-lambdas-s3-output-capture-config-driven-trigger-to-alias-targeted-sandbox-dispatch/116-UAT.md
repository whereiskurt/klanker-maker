# Phase 116 — Live UAT (Wave 5 / 116-08)

**Date:** 2026-06-18 (UTC)
**Install:** default `km` (AWS_PROFILE=klanker-application, account 052251888500, us-east-1)
**Binary:** km v0.4.995

## Deploy surface (Task 1) — ✅ applied live

- `make build` (v0.4.992+) + `make build-lambdas` (8 zips incl. ttl-handler) — clean.
- Full Go suite green before deploy (`go test ./... -timeout 600s` EXIT 0; the
  initial 300s run timed out cumulatively — VALIDATION specifies 600s — and a real
  red surfaced + fixed: `uninit_test` module count 22→24, commit 864bd377).
- `km init --dry-run=false` — applied cleanly, **no destroy-class gate trip**. New
  modules applied: `dynamodb-checks... done`, `check-runner-role... done`, `ses` last.
- Verified live in AWS:
  - `km-checks` DDB table: ACTIVE, hash key `name`, PAY_PER_REQUEST.
  - `km-check-runner` IAM role: `arn:aws:iam::052251888500:role/km-check-runner`.
  - `km-check-dispatch` EventBridge rule: ENABLED, pattern
    `{detail-type:[CheckDispatch], source:[km.sandbox]}`, target `km-ttl-handler`.
  - `km doctor` → `✓ Checks Table (km-checks) exists`.

## QOTD (Task 2) — ✅ Success Criteria 2 + open-egress + packaging

- `km check deploy profiles/checks/qotd/snippet.py --name qotd --requirements ... --schedule "rate(1 hour)"`
  — pip installed `requests` + deps as arm64/manylinux2014_aarch64 wheels; created
  `km-check-qotd`.
- `km check run qotd` → wrote `s3://km-artifacts-12345/check-runs/qotd/<ts>/output.json`.
- **Real quote fetched live** (proves open internet egress + requests packaging):
  `{"quote":"I have been given the authority over you... set me right.","author":"Abu Bakr (R.A)","category":"general"}`
- Note: the original example API `api.quotable.io` has an **expired TLS cert** (abandoned
  service) — egress still reached it (TLS handshake error proved connectivity); repointed
  the example to `dummyjson.com` (maintained). The snippet's error handler emitted valid
  JSON on the failure (graceful).

## Wiz Threat Intel trigger (Task 2) — ⚠️ partial (dispatch proven; cold-create prompt is a gap)

Trigger wired in `km-config.yaml` (gitignored): `check: wiz-intel`, `when_py: max_affected>100`,
`alias: wiz-triage-box`, `profile: github-review`, `on_absent: cold-create`, `cooldown_seconds: 3600`.

- **Deploy** baked `KM_CHECK_TRIGGER` correctly (when_py/prompt/profile/on_absent inlined).
- **Below threshold** (`km check run wiz-intel --env WIZ_SIM_CAP=50`): `max_affected=50` →
  predicate False → `triggered:false`, output still written to S3. ✅ (negative path)
- **Above threshold** (`km check run wiz-intel`): `triggered:true`,
  `reason:"153 systems affected by top advisory"`. Bootstrap emitted CheckDispatch. ✅
- **Stage B (ttl-handler) logs** confirm the chain:
  `check-dispatch event received` → `check_dispatch_alias_absent` (wiz-triage-box absent) →
  `check_dispatch_cold_create` with **`profile:"github-review"`** (after Bug A fix).
- **create-handler** received the SandboxCreate (`sandbox_id=chk-8e47e4ff`), downloaded
  terraform, and began provisioning **from the pre-staged profile** (after Bug C fix) —
  then stopped on the `github-review` SOPS bundle 404 (expected: that profile requires a
  SOPS secrets bundle; not a bug — use a non-SOPS/Bedrock profile for cold-create).

## Findings (bugs surfaced by live UAT)

| # | Severity | Finding | Status |
|---|----------|---------|--------|
| A | blocker | `config.CheckTrigger` had no `Profile` field + `BakeTrigger` didn't map it → cold-create got `profile=""` | ✅ FIXED (commit 8377a6f7) |
| B | high | `km check deploy` raced `UpdateFunctionConfiguration` after `UpdateFunctionCode` → 409 ResourceConflictException on redeploy/sync | ✅ FIXED (waitFunctionUpdated) |
| C | blocker | cold-create profile never staged to `check-profiles/{slug}/.km-profile.yaml` → create-handler 404 | ✅ FIXED (km check deploy pre-stages it) |
| D | n/a | `github-review` requires a SOPS bundle (by design) → 404 for cold-create | not a bug; use a SOPS-free/Bedrock profile |
| E | high | **cold-create does not convey the prompt** to the new box (`SandboxCreateDetail` has no prompt field; `ttlColdCreateSink` drops the `prompt` arg). Warm/resume delivers it via `handleAgentRun`; cold does not. | ⏳ FOLLOW-UP (needs a generic prompt-delivery path in create-handler) |
| F | low | `km doctor` sub-checks *Check Trigger Drift / Orphan Check Lambdas / Orphan Check Schedules* skip with `- DynamoDB client not available` (the table-existence check works) | ⏳ FOLLOW-UP (doctor deps wiring) |

## Success Criteria status

1. SDK per-check Lambda + DDB row, no terragrunt — ✅ live (`km-check-qotd`, `km-check-wiz-intel`, `km-checks` rows).
2. Run + S3 output capture — ✅ live (QOTD real quote).
3. `when_py` → CheckDispatch → resume-or-cold-create (cooldown) — ⚠️ emission + ttl-handler consumption + cold-create branch + correct profile + create-handler provisioning all proven live; **cold-create prompt delivery (Bug E) is a follow-up**; warm/resume prompt delivery is code-complete (handleAgentRun) but not live-demoed (no existing alias box).
4. Shared `pkg/dispatch` decision; warm=agent-run, cold=PutSandboxCreate; bridges unmodified — ✅ (unit-tested + cold/below live; bridges confirmed unmodified).
5. Two scaffolding modules + km init + CheckDispatch rule + widened IAM — ✅ live.
6. Packaging zip+arm64 wheels default; `--image` opt-in — ✅ zip live (QOTD); `--image` not live-tested.
7. `km check ls|get|logs|schedule|rm|sync` + `km doctor` — ✅ ls/get/rm/deploy live; doctor table-check live; doctor drift/orphan sub-checks = Bug F.
8. Two examples deployed live + demonstrated — ✅ QOTD fully; ⚠️ Wiz dispatch chain fully proven through cold-create branch + provisioning, prompt-on-cold = Bug E.

## Recommended follow-up (gap closure)

- **Bug E** (cold-create prompt delivery): add a prompt field to `SandboxCreateDetail`,
  set it in `ttlColdCreateSink`, and have create-handler deliver it on boot (reuse the
  Phase-86 prompt-queue or write to the box's prompt prefix). Until then, document
  cold-create as "provisions a box; prompt delivery is warm/resume-only".
- **Bug F** (doctor wiring): pass a DynamoDB client into the check doctor sub-checks.
- Optional: a SOPS-free `profiles/check-triage.yaml` (Bedrock) as the documented default
  cold-create target so the example trigger works out of the box.

## Teardown

- `km-check-qotd` left deployed (harmless hourly example) / or remove with `km check rm qotd`.
- `km-check-wiz-intel` — remove with `km check rm wiz-intel` (its cold-create trigger hits Bug E).
- `chk-8e47e4ff` cold-create stopped at the SOPS 404 before EC2 launch; `km list` shows 0
  sandboxes (no running infra). `km doctor` debris sweep covers any stray DDB row.
