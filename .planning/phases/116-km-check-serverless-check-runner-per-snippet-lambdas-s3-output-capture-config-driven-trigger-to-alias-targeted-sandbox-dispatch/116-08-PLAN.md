---
phase: 116-km-check-serverless-check-runner
plan: 08
type: execute
wave: 5
depends_on: [116-01, 116-04, 116-05, 116-06, 116-07]
files_modified:
  - .planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The scaffolding (table + role + CheckDispatch rule + widened ttl-handler IAM) is deployed live via make build + km init --dry-run=false"
    - "QOTD deploys via km check deploy, runs, and writes a quote to s3://{artifacts}/check-runs/qotd/<ts>/output.json"
    - "The Wiz check above threshold emits CheckDispatch and ttl-handler resumes/cold-creates the alias box with the expanded prompt; below threshold emits NO dispatch"
    - "km doctor reports the check group correctly against the live fleet"
  artifacts:
    - path: ".planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-UAT.md"
      provides: "the executed live E2E runbook with observed results"
  key_links:
    - from: "km check run wiz-intel (above threshold)"
      to: "alias box resumed + prompted"
      via: "CheckDispatch -> ttl-handler -> handleAgentRun"
      pattern: "CheckDispatch"
---

<objective>
Deploy the check runner live and demonstrate it end to end against the default
`km` install (AWS_PROFILE=klanker-application). This is the proof that the layered
feature works in AWS, not just in unit tests.

Purpose: Phase 116 Success Criterion 8 requires the two example checks deployed live
and demonstrated. The executor RUNS the deploy + the happy/negative paths live (the
user has AWS auth; expense is acceptable per planning guidance) and records observed
results in 116-UAT.md.

Output: a deploy + E2E run, with observed evidence captured in 116-UAT.md.

NOTE: This plan has a checkpoint because some steps need human judgment / live
observation (resume of a real EC2 box, prompt delivery via SSM/otel). The executor
should AUTOMATE everything it can via CLI first (project feedback:
attempt_before_asking) and only pause for genuine human verification.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-VALIDATION.md
@docs/check-runner.md

<interfaces>
<!-- Deploy surface (CONTEXT + spec): -->
<!--   make build (binary carries new regionalModules entries — stale binary skips them -> AccessDenied) -->
<!--   make build-lambdas (ttl-handler code change) -->
<!--   AWS_PROFILE=klanker-application km init --dry-run=false (table + role + CheckDispatch rule + widened IAM) -->
<!--   km check deploy / sync (per-check, pure SDK) -->
<!-- 116-VALIDATION.md "Manual-Only Verifications" table is the canonical runbook. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live deploy of the scaffolding + ttl-handler</name>
  <files>(no source changes — deploy + capture)</files>
  <action>
With AWS_PROFILE=klanker-application:
  1. `make build` (binary carries the new regionalModules entries — REQUIRED before km init).
  2. `make build-lambdas` (ttl-handler code change).
  3. `go test ./... -count=1 -timeout 600s > /tmp/116-full.out 2>&1; echo "EXIT=$?"` —
     full suite MUST be green before deploying (check exit code, not pipe).
  4. `km init --dry-run=false` — provisions {prefix}-checks table, {prefix}-check-runner
     role, the CheckDispatch EventBridge rule, and the widened ttl-handler IAM/env.
     (If a destroy-class gate trips unexpectedly, STOP and report — do not --i-accept-destroys.)
  5. Verify with `km doctor` that the checks table is reported present.
Capture the init output + doctor lines into 116-UAT.md.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./... -count=1 -timeout 600s > /tmp/116-full.out 2>&1; echo "EXIT=$?"; tail -5 /tmp/116-full.out</automated>
  </verify>
  <done>Full suite green; km init applied the 2 new modules + CheckDispatch rule + widened IAM; km doctor sees the checks table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live E2E — QOTD S3 output + Wiz trigger dispatch + doctor</name>
  <action>Executor automates the km commands below; human confirms the live AWS effects. See how-to-verify.</action>
  <what-built>QOTD check deployed live and run; S3 output captured. Wiz check trigger fired CheckDispatch -> ttl-handler resumed/cold-created the alias box.</what-built>
  <how-to-verify>
Run (executor automates the km commands; human confirms the live AWS effects):

QOTD (Success Criterion 2 + 8):
  1. `km check deploy profiles/checks/qotd/qotd.py --name qotd --requirements profiles/checks/qotd/requirements.txt --schedule "rate(1 hour)"`
  2. `km check run qotd` -> prints a quote JSON.
  3. Confirm `s3://{artifacts}/check-runs/qotd/<ts>/output.json` exists and contains a quote
     (aws s3 ls / cp). CONFIRM the quote is real (open-egress fetch worked).

Wiz trigger -> dispatch (Success Criterion 3 + 4 + 8):
  4. Pick or create a target alias box (e.g. an existing stopped/paused sandbox alias).
  5. Paste profiles/checks/wiz-intel/checks.triggers.example.yaml into km-config.yaml
     with that alias; `km check deploy profiles/checks/wiz-intel/wiz_threat_intel.py --name wiz-intel`
     then `km check sync wiz-intel` (bakes KM_CHECK_TRIGGER).
  6. ABOVE threshold: force the sim data over the threshold; `km check run wiz-intel`.
     CONFIRM: a CheckDispatch event in EventBridge + ttl-handler CloudWatch logs show
     the check-dispatch case ran; the alias box is resumed (km list) and received the
     expanded prompt (km otel / km logs / km agent results show the {{out.max_affected}}
     value substituted).
  7. BELOW threshold (negative path): force sub-threshold sim data; `km check run wiz-intel`.
     CONFIRM: NO CheckDispatch, output still written to S3.

km doctor (Success Criterion 7):
  8. `km doctor` reports the table present; induce an orphan (delete a DDB row but leave
     the Lambda, or vice versa) and confirm the WARN; restore.

Record every observed result (S3 keys, event IDs, log excerpts, km list/otel output)
in 116-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" when QOTD S3 output + Wiz above/below-threshold dispatch behavior + doctor are confirmed, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the UAT runbook + results</name>
  <files>.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-UAT.md</files>
  <action>
Write 116-UAT.md capturing: the deploy commands run, the QOTD S3 output evidence,
the Wiz above-threshold CheckDispatch + resume evidence, the below-threshold
no-dispatch evidence, and the doctor output. Note any gaps for follow-up. Mark each
Success Criterion (1-8) as demonstrated or note which are unit-only vs live-demoed.
Tear down any throwaway test sandbox with `km destroy --remote --yes`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f .planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-UAT.md && grep -qi 'qotd\|wiz\|CheckDispatch' .planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-UAT.md && echo OK</automated>
  </verify>
  <done>116-UAT.md records the live deploy + E2E evidence and maps it to Success Criteria 1-8.</done>
</task>

</tasks>

<verification>
- Full Go suite green before deploy (exit code checked).
- QOTD output.json present in S3 with a real quote.
- Wiz above-threshold: CheckDispatch + ttl-handler resume observed; below-threshold: no dispatch.
- km doctor check group behaves on the live fleet.
- 116-UAT.md captures the evidence.
</verification>

<success_criteria>
Covers Phase 116 Success Criterion 8 (both examples deployed live + demonstrated) and
provides the live evidence for Criteria 1, 2, 3, 4, 5, 7. This is the phase's
end-to-end proof.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-08-SUMMARY.md`
</output>
