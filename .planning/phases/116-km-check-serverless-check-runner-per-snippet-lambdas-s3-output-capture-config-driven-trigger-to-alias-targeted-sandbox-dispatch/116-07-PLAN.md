---
phase: 116-km-check-serverless-check-runner
plan: 07
type: execute
wave: 4
depends_on: [116-05, 116-06]
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/doctor_check_test.go
  - pkg/github/bridge/event_router.go
  - pkg/github/bridge/webhook_handler.go
  - pkg/github/bridge/interfaces.go
  - internal/app/config/config.go
  - infra/modules/lambda-github-bridge/v1.1.0/main.tf
  - profiles/checks/qotd/qotd.py
  - profiles/checks/qotd/requirements.txt
  - profiles/checks/wiz-intel/wiz_threat_intel.py
  - profiles/checks/wiz-intel/checks.triggers.example.yaml
  - docs/check-runner.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "km doctor reports the {prefix}-checks table, orphan {prefix}-check-* Lambdas, stale schedules, and KM_CHECK_TRIGGER drift"
    - "A github.events rule may carry a check: field; the bridge synchronously invokes the check Lambda first and only dispatches if it triggers"
    - "Two working example checks exist: QOTD (internet fetch + requirements) and Wiz Threat Intel (advisories + a checks.triggers example)"
    - "Operator docs (docs/check-runner.md) + CLAUDE.md document the open-egress posture, deploy surface, and CLI"
  artifacts:
    - path: "profiles/checks/qotd/qotd.py"
      provides: "QOTD example check (fetches a quote, prints JSON)"
    - path: "profiles/checks/wiz-intel/wiz_threat_intel.py"
      provides: "simulated Wiz advisories + affected-system counts (JSON)"
    - path: "profiles/checks/wiz-intel/checks.triggers.example.yaml"
      provides: "checks.triggers when_py threshold example wiring the Wiz check to an alias"
    - path: "docs/check-runner.md"
      provides: "operator guide: posture, deploy surface, CLI, trigger model"
  key_links:
    - from: "pkg/github/bridge/webhook_handler.go (handleEventRoute)"
      to: "check Lambda Invoke"
      via: "rule.Check != \"\" -> synchronous invoke gate before dispatch"
      pattern: "Check"
---

<objective>
Close out the operator-facing surface: `km doctor` check group, the `github.events`
`check:` pre-filter (GitHub only — Slack/email deferred), the two live example
checks in `profiles/checks/`, and the operator documentation.

Purpose: doctor gives health/drift visibility; the `check:` pre-filter makes checks
usable as a gate for the existing GitHub event router; the examples + docs make the
feature demonstrable and adoptable.

Output: doctor checks + bridge `check:` field + example checks + docs.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md
@profiles/checks/_bootstrap/KM_CHECK_TRIGGER.schema.md

# Patterns to mirror:
@internal/app/cmd/doctor.go
@pkg/github/bridge/event_router.go
@pkg/github/bridge/webhook_handler.go

<interfaces>
<!-- github.events check: pre-filter (RESEARCH section 6): -->
<!--   Add `Check string` to EventRule (event_router.go:14) + GithubEventRule (config.go ~152). -->
<!--   In handleEventRoute (webhook_handler.go:724) after match, if rule.Check != "": -->
<!--     synchronously lambda.Invoke {prefix}-check-{name}; only dispatch if bootstrap returns triggered=true. -->
<!--   Bridge IAM: add lambda:InvokeFunction on {prefix}-check-* to lambda-github-bridge/v1.1.0 -->
<!--     (additive; edit in place, no version bump per the v1.1.0 in-place convention). -->
<!--   Add a LambdaInvoker interface seam to interfaces.go so it is unit-testable. -->

<!-- doctor checks (spec section "km doctor additions"): table exists; orphan check Lambdas not in -->
<!--   DDB (WARN); schedules referencing a missing check Lambda (WARN); KM_CHECK_TRIGGER baked value -->
<!--   vs current config-resolved BakeTrigger (nudge `km check sync`). Reuse pkg/check helpers. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: km doctor check group</name>
  <files>internal/app/cmd/doctor.go, internal/app/cmd/doctor_check_test.go</files>
  <action>
Add a `checkChecksGroup` (mirror an existing doctor group like the Slack/GitHub
groups) that reports:
  - `{prefix}-checks` table exists (use config table name) — OK/FAIL.
  - Orphan `{prefix}-check-*` Lambdas (ListFunctions filtered by prefix) NOT present
    in the DDB checks table -> WARN listing them.
  - EventBridge Scheduler entries targeting a `{prefix}-check-*` Lambda that no longer
    exists -> WARN.
  - Per-check `KM_CHECK_TRIGGER` drift: for each DDB check row with a config trigger,
    re-run pkg/check.BakeTrigger from current km-config.yaml and compare sourceHash vs
    the deployed Lambda's env hash -> WARN nudging `km check sync`.
Gate the group to skip silently when the checks table is absent (dormant install),
mirroring how the github group skips when unconfigured.

Add `doctor_check_test.go` with a unit test for the drift-compare + orphan-detection
logic using mock listers (mirror existing doctor unit tests in this package).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run 'TestDoctor.*Check|TestCheckDoctor' -count=1 -timeout 120s > /tmp/116-07-1.out 2>&1; echo "EXIT=$?"; tail -20 /tmp/116-07-1.out</automated>
  </verify>
  <done>doctor reports table presence, orphan Lambdas, stale schedules, trigger drift; skips silently when unconfigured; unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: github.events check: pre-filter</name>
  <files>pkg/github/bridge/event_router.go, pkg/github/bridge/webhook_handler.go, pkg/github/bridge/interfaces.go, internal/app/config/config.go, infra/modules/lambda-github-bridge/v1.1.0/main.tf</files>
  <action>
Add `Check string` field (mapstructure/yaml/json "check") to `EventRule`
(event_router.go) and `GithubEventRule` (config.go). Add a `LambdaInvoker` interface
to interfaces.go (`InvokeCheck(ctx, name, payload) (triggered bool, err error)`).

In `handleEventRoute` (webhook_handler.go), after `MatchEventRule` selects a rule
and before dispatching the sandbox: if `rule.Check != ""`, call
`LambdaInvoker.InvokeCheck` synchronously on `{prefix}-check-{rule.Check}`; only
proceed to the existing dispatch if it returns triggered=true; otherwise log
`github_check_prefilter_skipped` and drop. Fail-open vs fail-closed: a check INVOKE
ERROR should fail-CLOSED (do NOT dispatch on an errored pre-filter) and log —
document this choice in a comment (a check that errors should not silently fire a sandbox).

Bridge IAM: add `lambda:InvokeFunction` on `arn:aws:lambda:*:*:function:{prefix}-check-*`
to `infra/modules/lambda-github-bridge/v1.1.0/main.tf` (additive, edit in place — the
v1.1.0 in-place convention). `terraform fmt`.

Add a unit test for the pre-filter gate (mock LambdaInvoker: triggered=true dispatches,
triggered=false drops, error -> fail-closed drop).

NOTE: this is the ONLY bridge modification in Phase 116, and it is ADDITIVE (a new
optional field + a pre-dispatch gate). It does NOT change the existing
resume-or-cold-create dispatch path, so no parity risk with the unmodified H1/Slack bridges.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/github/bridge/ -run 'Check|Prefilter|EventRoute' -count=1 -timeout 120s > /tmp/116-07-2.out 2>&1; echo "EXIT=$?"; grep -q 'InvokeFunction' infra/modules/lambda-github-bridge/v1.1.0/main.tf && echo "IAM OK"; tail -15 /tmp/116-07-2.out</automated>
  </verify>
  <done>EventRule/GithubEventRule have Check; handleEventRoute gates on a synchronous check invoke (fail-closed on error); bridge IAM grants check invoke; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Example checks (QOTD + Wiz) + operator docs</name>
  <files>profiles/checks/qotd/qotd.py, profiles/checks/qotd/requirements.txt, profiles/checks/wiz-intel/wiz_threat_intel.py, profiles/checks/wiz-intel/checks.triggers.example.yaml, docs/check-runner.md, CLAUDE.md</files>
  <action>
`profiles/checks/qotd/qotd.py`: plain Python (no handler) — fetch a quote-of-the-day
from a public internet API (e.g. a free quotes endpoint) using `requests`, print
a JSON object `{"quote": ..., "author": ...}` to stdout. Proves open egress + wheel
packaging.
`profiles/checks/qotd/requirements.txt`: `requests` (pinned).

`profiles/checks/wiz-intel/wiz_threat_intel.py`: plain Python (stdlib only) — emit a
simulated JSON list of Wiz advisories with affected-system counts, e.g.
`{"advisories": [{"id": ..., "severity": ..., "affected": <int>}], "max_affected": <int>}`.
`profiles/checks/wiz-intel/checks.triggers.example.yaml`: a `checks.triggers:` block
with a `when_py` threshold predicate (e.g. `return out["max_affected"] > 100`),
`alias: <example-box>`, a `prompt` template using `{{reason}}` + `{{out.max_affected}}`,
`onAbsent: cold-create`, `cooldownSeconds: 3600`. Document it is an EXAMPLE to paste
into km-config.yaml.

`docs/check-runner.md`: operator guide — what a check is (workload, not infra),
the OPEN-EGRESS posture (prominent: checks are NOT under sandbox eBPF/proxy
enforcement), the snippet contract (plain Python + bootstrap), the KM_CHECK_TRIGGER
trigger model (when_py + prompt, inline/@file, sync re-bake), the two-stage firing
path (Stage A Python eval -> CheckDispatch -> Stage B ttl-handler resume-or-cold-create),
the CLI table, the github.events `check:` pre-filter, km doctor checks, and the
DEPLOY SURFACE (make build + km init --dry-run=false for scaffolding+rule+IAM;
make build-lambdas + km init for ttl-handler; km check deploy/sync per-check pure SDK;
--image needs Docker). Reference the two examples.

`CLAUDE.md`: add a Phase 116 section (mirror the existing phase blocks) + a
"Where to look" row + `km check ...` to the CLI list, noting the open-egress posture
and the deploy surface.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && python3 -c "import ast; ast.parse(open('profiles/checks/qotd/qotd.py').read()); ast.parse(open('profiles/checks/wiz-intel/wiz_threat_intel.py').read()); print('parse OK')" && test -f profiles/checks/qotd/requirements.txt && test -f profiles/checks/wiz-intel/checks.triggers.example.yaml && test -f docs/check-runner.md && grep -q 'km check' CLAUDE.md && echo OK</automated>
  </verify>
  <done>QOTD + Wiz snippets parse; requirements.txt + checks.triggers example present; docs/check-runner.md covers posture+deploy+CLI; CLAUDE.md updated.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ ./pkg/github/bridge/ -count=1 -timeout 120s` green.
- Bridge IAM grants `lambda:InvokeFunction` on `{prefix}-check-*`; `terraform fmt -check` clean.
- Both example snippets parse; checks.triggers example present.
- `go test ./... -count=1 -timeout 600s` green (wave-merge gate — check exit code).
</verification>

<success_criteria>
Covers Phase 116 Success Criterion 7 (doctor reports orphans/schedules/drift) and the
github.events `check:` pre-filter (Invocation mode 3). Provides the two example
checks that Criterion 8 demonstrates live (Plan 116-08). Documents the open-egress
posture and deploy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-07-SUMMARY.md`
</output>
