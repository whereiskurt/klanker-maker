---
phase: 116-km-check-serverless-check-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/modules/dynamodb-checks/v1.0.0/main.tf
  - infra/modules/dynamodb-checks/v1.0.0/variables.tf
  - infra/modules/dynamodb-checks/v1.0.0/outputs.tf
  - infra/modules/check-runner-role/v1.0.0/main.tf
  - infra/modules/check-runner-role/v1.0.0/variables.tf
  - infra/modules/check-runner-role/v1.0.0/outputs.tf
  - infra/live/use1/dynamodb-checks/terragrunt.hcl
  - infra/live/use1/check-runner-role/terragrunt.hcl
  - internal/app/cmd/init.go
  - internal/app/cmd/init_plan_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "km init provisions the {prefix}-checks DynamoDB table and the {prefix}-check-runner IAM role"
    - "regionalModules() includes dynamodb-checks and check-runner-role (count 22->24)"
    - "TestRunInitPlan_ModuleOrder passes with count 24; ses remains last"
  artifacts:
    - path: "infra/modules/dynamodb-checks/v1.0.0/main.tf"
      provides: "{prefix}-checks table (hash key name, PAY_PER_REQUEST, SSE)"
      contains: "aws_dynamodb_table"
    - path: "infra/modules/check-runner-role/v1.0.0/main.tf"
      provides: "shared {prefix}-check-runner Lambda execution role + inline policies"
      contains: "aws_iam_role"
    - path: "infra/live/use1/dynamodb-checks/terragrunt.hcl"
      provides: "live unit for the checks table"
    - path: "infra/live/use1/check-runner-role/terragrunt.hcl"
      provides: "live unit for the check-runner role"
  key_links:
    - from: "internal/app/cmd/init.go"
      to: "regionalModules()"
      via: "two new regionalModule entries before ses"
      pattern: "dynamodb-checks|check-runner-role"
---

<objective>
Scaffold the two control-plane terragrunt modules for the check runner — the
`{prefix}-checks` DynamoDB table and the shared `{prefix}-check-runner` Lambda
execution role — register both in `regionalModules()`, and bump the module-order
test count.

Purpose: These are the ONLY two always-on terragrunt resources for Phase 116
(per-check Lambdas are SDK-managed, not fleet). They must be reachable by
`km init` and pass the module-order invariant before any SDK or dispatch work can run.

Output: Two TF module dirs + two live terragrunt units + two `regionalModules()`
entries + the bumped `TestRunInitPlan_ModuleOrder` count.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md

# Template modules to copy patterns from:
@infra/modules/dynamodb-github-threads/v1.0.0/main.tf
@infra/live/use1/dynamodb-github-threads/terragrunt.hcl

<interfaces>
<!-- regionalModules() entry shape — extract from init.go:511 before editing. -->
<!-- RESEARCH.md section 7 gives the exact entries to add (before `ses` which stays last): -->
{ name: "dynamodb-checks",   dir: filepath.Join(regionDir, "dynamodb-checks"),   envReqs: nil },
{ name: "check-runner-role", dir: filepath.Join(regionDir, "check-runner-role"), envReqs: []string{"KM_ARTIFACTS_BUCKET"} }

<!-- TestRunInitPlan_ModuleOrder current assertion (init_plan_test.go:439): -->
if len(mods) != 22 { ... }   // bump BOTH the literal and the "want 22" message to 24

<!-- CRITICAL terragrunt rules (project memory): -->
<!-- - Modules MUST NOT declare required_providers (root.hcl owns provider via generate stanza). -->
<!-- - dynamodb-checks + check-runner-role have NO dependency blocks -> no mock_outputs needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dynamodb-checks + check-runner-role TF modules</name>
  <files>infra/modules/dynamodb-checks/v1.0.0/{main,variables,outputs}.tf, infra/modules/check-runner-role/v1.0.0/{main,variables,outputs}.tf</files>
  <action>
Create `infra/modules/dynamodb-checks/v1.0.0/` mirroring
`infra/modules/dynamodb-github-threads/v1.0.0/` (read it first):
  - main.tf: `aws_dynamodb_table "checks"` — `name = var.table_name`,
    `billing_mode = "PAY_PER_REQUEST"`, `hash_key = "name"`, single string
    attribute `name`, `server_side_encryption { enabled = true }`,
    `tags = merge(var.tags, { Name = var.table_name, Component = "km-check" })`.
  - variables.tf: `table_name` (string, default "km-checks"), `tags` (map(string), default {}).
  - outputs.tf: `table_name`, `table_arn`.

Create `infra/modules/check-runner-role/v1.0.0/`:
  - main.tf: `aws_iam_role "check_runner"` with Lambda trust policy
    (`lambda.amazonaws.com`), name `var.role_name`. Attach inline policies
    (per CONTEXT "Shared baseline execution role" + RESEARCH section 8):
      * CloudWatch Logs: logs:CreateLogGroup/CreateLogStream/PutLogEvents (resource "*").
      * S3 read: s3:GetObject on arn:aws:s3:::${var.artifacts_bucket}/checks/*.
      * S3 write: s3:PutObject on arn:aws:s3:::${var.artifacts_bucket}/check-runs/*.
      * EventBridge: events:PutEvents on the bus that pkg/aws/eventbridge.go emits to
        (verify which bus — likely the default bus; match the resource ARN to it).
      * SSM: ssm:GetParameter on arn:aws:ssm:*:*:parameter/${var.resource_prefix}/checks/*.
      * DynamoDB: dynamodb:GetItem,Query on arn:aws:dynamodb:*:*:table/${var.table_name}.
    NO EC2/SQS/resume perms — those live in ttl-handler (Plan 116-06).
  - variables.tf: `role_name`, `artifacts_bucket`, `resource_prefix`, `table_name`, `tags`.
  - outputs.tf: `role_arn`, `role_name`.

CRITICAL: Neither module declares `required_providers` (root.hcl owns it —
project memory terragrunt_providers_in_root). Run `terraform fmt` on both dirs.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && terraform -chdir=infra/modules/dynamodb-checks/v1.0.0 fmt -check && terraform -chdir=infra/modules/check-runner-role/v1.0.0 fmt -check && ! grep -rq required_providers infra/modules/dynamodb-checks/v1.0.0 infra/modules/check-runner-role/v1.0.0 && echo OK</automated>
  </verify>
  <done>Both module dirs exist with main/variables/outputs.tf; no required_providers blocks; terraform fmt clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two live terragrunt units</name>
  <files>infra/live/use1/dynamodb-checks/terragrunt.hcl, infra/live/use1/check-runner-role/terragrunt.hcl</files>
  <action>
Read `infra/live/use1/dynamodb-github-threads/terragrunt.hcl` first as the exact template.

Create `infra/live/use1/dynamodb-checks/terragrunt.hcl`:
  - same `locals` (repo_root, site_vars, region_config, region_label), `include "root"`,
    `remote_state` with key `.../dynamodb-checks/terraform.tfstate`,
    `terraform { source = "${local.repo_root}/infra/modules/dynamodb-checks/v1.0.0" }`,
  - inputs: `table_name = "${local.site_vars.locals.site.label}-checks"`,
    `tags = { "km:component" = "km-check", "km:managed" = "true" }`.

Create `infra/live/use1/check-runner-role/terragrunt.hcl`:
  - same scaffolding; source `check-runner-role/v1.0.0`,
  - inputs: `role_name = "${local.site_vars.locals.site.label}-check-runner"`,
    `resource_prefix = local.site_vars.locals.site.label`,
    `artifacts_bucket = <derive same way other live units derive the artifacts bucket>`,
    `table_name = "${local.site_vars.locals.site.label}-checks"`,
    `tags = { ... }`.
  - NO `dependency` blocks (so no mock_outputs needed). The role references only
    bucket/prefix/table-name strings, not other modules' outputs.

Verify the artifacts_bucket derivation against how another live unit obtains it
(grep `artifacts_bucket` under infra/live/use1/) and mirror that exactly.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f infra/live/use1/dynamodb-checks/terragrunt.hcl && test -f infra/live/use1/check-runner-role/terragrunt.hcl && grep -q 'dynamodb-checks/v1.0.0' infra/live/use1/dynamodb-checks/terragrunt.hcl && grep -q 'check-runner-role/v1.0.0' infra/live/use1/check-runner-role/terragrunt.hcl && echo OK</automated>
  </verify>
  <done>Both live units exist, source the correct module versions, set table/role names from site.label, no dependency blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Register both modules in regionalModules() + bump module-order test</name>
  <files>internal/app/cmd/init.go, internal/app/cmd/init_plan_test.go</files>
  <action>
In `internal/app/cmd/init.go` `regionalModules()` (line ~511), add the two new
entries (from the interfaces block) positioned BEFORE the `ses` entry (ses MUST
remain last per the module-order invariant). Place them near the other dynamodb /
role modules following existing ordering conventions:
  { name: "dynamodb-checks",   dir: filepath.Join(regionDir, "dynamodb-checks"),   envReqs: nil },
  { name: "check-runner-role", dir: filepath.Join(regionDir, "check-runner-role"), envReqs: []string{"KM_ARTIFACTS_BUCKET"} },

In `internal/app/cmd/init_plan_test.go` (TestRunInitPlan_ModuleOrder, line 439):
change `if len(mods) != 22` -> `if len(mods) != 24` and the `want 22` message -> `want 24`.

Leave `TestRunInitPlan_PlanFailureStopsLoop`'s "module 9" comment alone — the new
modules are added near the end (before ses), so create-handler's index is unchanged.

After editing, `make build` (NOT bare go build — ldflags) so the binary carries
the new entries (project memory make_build_precedes_km_init).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run 'TestRunInitPlan_ModuleOrder|TestRegionalModules' -count=1 -timeout 120s > /tmp/116-01.out 2>&1; echo "EXIT=$?"; tail -20 /tmp/116-01.out</automated>
  </verify>
  <done>Module-order test passes with count 24; both new modules present in regionalModules(); ses still last.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run TestRunInitPlan_ModuleOrder -count=1` green (count 24).
- `terraform fmt -check` clean on both new module dirs; no `required_providers` blocks.
- Both live units source the correct `v1.0.0` module paths.
- `make build` succeeds (binary carries new regionalModules entries).
</verification>

<success_criteria>
Covers Phase 116 Success Criterion 5 (two scaffolding modules only; km init deploys
them). Both modules registered, module-order invariant green, no provider drift.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-01-SUMMARY.md`
</output>
