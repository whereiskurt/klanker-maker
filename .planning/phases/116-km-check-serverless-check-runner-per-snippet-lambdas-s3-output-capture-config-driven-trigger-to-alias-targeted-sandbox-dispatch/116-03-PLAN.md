---
phase: 116-km-check-serverless-check-runner
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_check_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "checks.triggers in km-config.yaml populates Config.Checks.Triggers (not silently dropped)"
    - "Each CheckTrigger carries check, when_py, alias, prompt, onAbsent, cooldownSeconds"
    - "YAMLDefaults snapshot accounts for the checks block for drift WARN"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "ChecksConfig + CheckTrigger structs, merge-list entry, UnmarshalKey decode"
      contains: "ChecksConfig"
    - path: "internal/app/config/config_check_test.go"
      provides: "TestChecksConfigMerge"
  key_links:
    - from: "internal/app/config/config.go (Load v2->v merge-list)"
      to: "cfg.Checks"
      via: "checks key in merge-list + v.UnmarshalKey(checks, &cfg.Checks)"
      pattern: "\"checks\""
---

<objective>
Plumb the `checks.triggers:` config block through `config.Load()` following the
established new-config-key ritual: struct + tags, the v2->v merge-list entry
(omitting it silently drops the file value — the project_config_key_merge_list
footgun), and a `YAMLDefaults` snapshot touch for drift WARN.

Purpose: Without the merge-list entry, `checks.triggers:` in `km-config.yaml` is
silently ignored. Every downstream plan (CLI bake, doctor drift) reads
`cfg.Checks.Triggers`.

Output: `ChecksConfig`/`CheckTrigger` types + wired Load() + a merge unit test.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md

# Exact precedent — the github block in the same file:
@internal/app/config/config.go

<interfaces>
<!-- RESEARCH section 7 anchors in internal/app/config/config.go: -->
<!-- - Config struct ~line 364 -->
<!-- - GithubEventRule struct ~line 152-186 (the mapstructure+yaml+json tag discipline to mirror) -->
<!-- - v2->v merge-list for-range block ~line 707-768 ("github" entry at ~760 is the exact precedent) -->
<!-- - UnmarshalKey decode in the cfg := block ~line 794+ -->
<!-- - YAMLDefaults snapshot ~line 782-791 -->

<!-- NEW types (RESEARCH section 7): -->
type ChecksConfig struct {
    Triggers []CheckTrigger `mapstructure:"triggers" yaml:"triggers,omitempty"`
}
type CheckTrigger struct {
    Check           string `mapstructure:"check"            yaml:"check"`
    WhenPy          string `mapstructure:"when_py"          yaml:"when_py,omitempty"`
    Alias           string `mapstructure:"alias"            yaml:"alias"`
    Prompt          string `mapstructure:"prompt"           yaml:"prompt,omitempty"`
    OnAbsent        string `mapstructure:"on_absent"        yaml:"onAbsent,omitempty"`
    CooldownSeconds int    `mapstructure:"cooldown_seconds" yaml:"cooldownSeconds,omitempty"`
}
<!-- Config struct gains: Checks ChecksConfig `mapstructure:"checks" yaml:"checks,omitempty"` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ChecksConfig/CheckTrigger structs + Config field</name>
  <files>internal/app/config/config.go</files>
  <action>
Add `ChecksConfig` and `CheckTrigger` structs (from the interfaces block) near the
`GithubEventRule`/`GithubConfig` definitions. Add `Checks ChecksConfig` field to the
`Config` struct (mapstructure+yaml tags). Mirror the github block's tag discipline
exactly (read GithubEventRule first).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./internal/app/config/ && grep -q 'ChecksConfig' internal/app/config/config.go && grep -q 'CheckTrigger' internal/app/config/config.go && echo OK</automated>
  </verify>
  <done>Structs compile; Config has a Checks field.</done>
</task>

<task type="auto">
  <name>Task 2: Wire checks into the v2->v merge-list + UnmarshalKey + YAMLDefaults</name>
  <files>internal/app/config/config.go</files>
  <action>
In `config.Load()`:
  1. Add `"checks"` to the v2->v merge-list for-range `[]string{...}` block (~line 707-768),
     right after the `"github"` entry. WITHOUT this the checks block is silently dropped.
  2. In the `cfg :=` decode section (~line 794+), add (mirroring github's UnmarshalKey):
       if err := v.UnmarshalKey("checks", &cfg.Checks); err != nil { /* non-fatal: absent -> zero value -> dormant */ }
  3. YAMLDefaults snapshot (~line 782-791): `checks` is a list-of-objects with no
     scalar top-level key. Either snapshot a `checks.triggers` count for drift, OR
     document in a comment why no scalar snapshot is needed (follow what github did —
     github.events is also list-of-objects; mirror its drift treatment exactly).
Keep the change minimal and consistent with the github precedent.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./internal/app/config/ && grep -q '"checks"' internal/app/config/config.go && grep -q 'UnmarshalKey("checks"' internal/app/config/config.go && echo OK</automated>
  </verify>
  <done>checks in merge-list; UnmarshalKey decodes into cfg.Checks; YAMLDefaults consistent with github.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestChecksConfigMerge</name>
  <files>internal/app/config/config_check_test.go</files>
  <behavior>
    - Given a km-config.yaml (temp file or in-memory viper) with a checks.triggers list
      of one rule (check/when_py/alias/prompt/onAbsent/cooldown_seconds), config.Load
      populates cfg.Checks.Triggers with len==1 and the exact field values.
    - Given NO checks block, cfg.Checks.Triggers is empty (dormant) — no error.
  </behavior>
  <action>
Write `internal/app/config/config_check_test.go` with `TestChecksConfigMerge`.
Follow the existing config-load test pattern in this package (find a test that
loads a temp km-config.yaml and asserts a decoded block — mirror its setup). Assert
both the populated and absent cases. This proves the merge-list entry works.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/config/ -run TestChecksConfigMerge -count=1 -timeout 60s > /tmp/116-03.out 2>&1; echo "EXIT=$?"; tail -15 /tmp/116-03.out</automated>
  </verify>
  <done>TestChecksConfigMerge passes; populated + absent cases both green.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/config/ -run TestChecksConfigMerge -count=1` green.
- `"checks"` present in the merge-list AND a `v.UnmarshalKey("checks", ...)` call exists.
- `go build ./internal/app/config/` clean.
</verification>

<success_criteria>
Covers part of Phase 116 Success Criterion 3 (the checks.triggers config block is
parsed and available). The merge-list footgun is avoided; downstream plans (116-05
bake, 116-07 doctor drift) can read cfg.Checks.Triggers.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-03-SUMMARY.md`
</output>
