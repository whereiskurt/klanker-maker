---
phase: 116-km-check-serverless-check-runner
plan: 05
type: execute
wave: 2
depends_on: [116-03, 116-04]
files_modified:
  - pkg/check/lambda.go
  - pkg/check/package.go
  - pkg/check/ecr.go
  - pkg/check/bootstrap.go
  - pkg/check/trigger.go
  - pkg/check/ddb.go
  - internal/app/cmd/check.go
  - internal/app/cmd/check_test.go
  - internal/app/cmd/root.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "km check deploy packages a snippet (zip+requirements arm64 wheels by default, --image opt-in) and SDK-provisions a per-check Lambda with the shared {prefix}-check-runner role"
    - "The resolved trigger (when_py + prompt, inline or @file) is baked into that Lambda's KM_CHECK_TRIGGER; sourceHash covers the resolved predicate+prompt"
    - "km check ls|get|logs|schedule|rm|sync|run operate the check fleet via DDB row + SDK"
    - "--image lazily SDK-creates the shared {prefix}-checks ECR repo with a lambda.amazonaws.com pull policy"
  artifacts:
    - path: "pkg/check/lambda.go"
      provides: "Lambda CRUD: CreateFunction/UpdateFunctionCode/UpdateFunctionConfiguration/Invoke/Delete"
    - path: "pkg/check/package.go"
      provides: "zip + pip arm64-wheel packaging; >50MB S3 upload"
    - path: "pkg/check/trigger.go"
      provides: "KM_CHECK_TRIGGER bake (inline + @file resolve) + sourceHash"
    - path: "internal/app/cmd/check.go"
      provides: "km check deploy|run|ls|get|logs|schedule|sync|rm"
    - path: "internal/app/cmd/check_test.go"
      provides: "TestCheckTriggerBakeInline, TestCheckTriggerBakeAtFile, TestCheckSourceHash"
  key_links:
    - from: "internal/app/cmd/check.go (deploy)"
      to: "lambda CreateFunction with KM_CHECK_TRIGGER env"
      via: "pkg/check.BakeTrigger + lambda.CreateFunction"
      pattern: "KM_CHECK_TRIGGER"
    - from: "internal/app/cmd/root.go"
      to: "NewCheckCmd"
      via: "root.AddCommand(NewCheckCmd(cfg))"
      pattern: "NewCheckCmd"
---

<objective>
Build the `km check` CLI family and its supporting `pkg/check/` package: SDK Lambda
CRUD, Python packaging (zip + arm64 wheels default, `--image` container opt-in with
lazy ECR repo), the `{prefix}-checks` DDB row CRUD, EventBridge Scheduler entry, and
the per-check `KM_CHECK_TRIGGER` env-bake (inline + `@file`) with `sourceHash`.

Purpose: This is the operator surface — deploy/run/ls/get/logs/schedule/sync/rm.
Per-check Lambdas are SDK-managed (no terragrunt). The trigger bake reuses the
canonical KM_CHECK_TRIGGER schema from Plan 116-04.

Output: `pkg/check/` + `internal/app/cmd/check.go` + unit tests for bake + sourceHash.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md
@profiles/checks/_bootstrap/KM_CHECK_TRIGGER.schema.md

# Patterns to mirror:
@internal/app/cmd/github.go
@pkg/aws/client.go

<interfaces>
<!-- AWS config: pkg/aws/client.go:24 LoadAWSConfig(ctx, profile); :40 LoadAWSConfigInRegion. -->
<!-- AWS_PROFILE=klanker-application on operator workstation. -->

<!-- Lambda CRUD (RESEARCH section 1) — NO existing wrapper, net-new in pkg/check/lambda.go: -->
<!--   lambdaClient := lambdapkg.NewFromConfig(awsCfg) -->
<!--   CreateFunction{FunctionName:"{prefix}-check-{name}", Role: check-runner ARN, -->
<!--     Runtime:"python3.13", Architectures:[arm64], Handler:"_km_check_bootstrap.handler", -->
<!--     Code:{ZipFile | S3Bucket/S3Key for >50MB}, Environment:{KM_CHECK_NAME, KM_ARTIFACTS_BUCKET, -->
<!--       KM_CHECK_TRIGGER, KM_CHECK_SECRET_PATHS, ...static --env}, MemorySize:256, Timeout:30, -->
<!--       Tags:{km:component=check, km:resource-prefix=prefix}} -->
<!--   Existing fn -> UpdateFunctionCode + UpdateFunctionConfiguration (two calls). -->

<!-- CLI parent (RESEARCH section 9) mirrors NewGithubCmd; root.go registers NewCheckCmd(cfg). -->
<!-- DDB row shape (RESEARCH per-check DDB row): name(hash), arn, runtime, packageType, -->
<!--   imageUri, memory, timeout, schedule, env(M non-secret), secretPaths(SS), sourceHash, -->
<!--   triggerSummary, createdAt, updatedAt. Use UpdateItem (never PutItem) on existing rows -->
<!--   (project_sandboxmetadata_lossy_roundtrip). -->

<!-- Trigger bake: resolve @file operator-side -> KM_CHECK_TRIGGER JSON (schema from 116-04) -->
<!--   -> sourceHash = SHA256 of the resolved KM_CHECK_TRIGGER JSON (drift detection). -->
<!-- Schedule: EventBridge Scheduler entry targeting the check Lambda (operator-side -->
<!--   scheduler.CreateSchedule; the handleScheduleCreate shape is the conceptual reference). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pkg/check trigger bake + sourceHash (test-first)</name>
  <files>pkg/check/trigger.go, pkg/check/bootstrap.go, internal/app/cmd/check_test.go</files>
  <behavior>
    - TestCheckTriggerBakeInline: a config.CheckTrigger with inline when_py + inline prompt
      bakes to a KM_CHECK_TRIGGER JSON matching the 116-04 schema (when_py, alias, prompt,
      on_absent, cooldown_seconds[, profile]).
    - TestCheckTriggerBakeAtFile: when_py="@/tmp/pred.py" and prompt="@/tmp/prompt.txt" are
      resolved by READING those files at bake time; the baked JSON contains file CONTENTS, not the @ref.
    - TestCheckSourceHash: sourceHash is the SHA256 of the resolved KM_CHECK_TRIGGER JSON;
      identical resolved content -> identical hash; changing the predicate -> different hash.
  </behavior>
  <action>
Create `pkg/check/bootstrap.go` embedding the bootstrap via go:embed. Simplest:
keep a copy of `_km_check_bootstrap.py` in `pkg/check/` and add a unit assertion
that it is byte-identical to `profiles/checks/_bootstrap/_km_check_bootstrap.py`
(so the canonical source stays single). Expose `BootstrapBytes() []byte`.

Create `pkg/check/trigger.go`:
  - `BakeTrigger(t config.CheckTrigger) (jsonBytes []byte, sourceHash string, err error)`:
    resolve `@file` for WhenPy and Prompt (when the value starts with "@", read the
    referenced file), marshal to the KM_CHECK_TRIGGER schema (116-04), compute
    sourceHash = SHA256 hex of the resolved JSON. on_absent defaults to "cold-create" if empty.
  - `TriggerSummary(t)` -> human string ("alias=<a> cooldown=<n>s").

Write the 3 tests in `internal/app/cmd/check_test.go` (VALIDATION command path is
`./internal/app/cmd/ -run TestCheckTriggerBake*`), calling `pkg/check.BakeTrigger`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run 'TestCheckTriggerBakeInline|TestCheckTriggerBakeAtFile|TestCheckSourceHash' -count=1 -timeout 120s > /tmp/116-05-1.out 2>&1; echo "EXIT=$?"; tail -20 /tmp/116-05-1.out</automated>
  </verify>
  <done>BakeTrigger resolves @file + inline, computes stable sourceHash; 3 tests green; bootstrap embedded + byte-identity asserted.</done>
</task>

<task type="auto">
  <name>Task 2: pkg/check Lambda CRUD + packaging + ECR + DDB</name>
  <files>pkg/check/lambda.go, pkg/check/package.go, pkg/check/ecr.go, pkg/check/ddb.go</files>
  <action>
`pkg/check/package.go` — zip packaging:
  - `BuildZip(snippetPath, requirementsPath string, bootstrapBytes []byte) (zipPath string, err error)`:
    temp dir; if requirementsPath present, run `pip install --platform manylinux2014_aarch64
    --only-binary=:all: --target <dir> -r requirements.txt` via os/exec (detect pip3/pip;
    error clearly if pip missing). Copy snippet -> snippet.py and write bootstrap ->
    _km_check_bootstrap.py into the dir. Zip via archive/zip (stdlib). Return zip path.
  - `MaybeUploadLargeZip(...)`: if zip >50MB, PutObject to checks/{name}/package.zip and
    return (s3Bucket, s3Key); else return zipBytes for direct upload.

`pkg/check/ecr.go` — `EnsureECRRepo(ctx, ecrClient, repoName)`: lazy CreateRepository
({prefix}-checks) if absent + SetRepositoryPolicy granting lambda.amazonaws.com pull.
Idempotent (RepositoryAlreadyExistsException -> ok).

`pkg/check/lambda.go` — net-new CRUD wrappers over lambdapkg.NewFromConfig:
  - `DeployFunction(ctx, in DeployInput)`: CreateFunction if absent else
    UpdateFunctionCode + UpdateFunctionConfiguration (two calls). Sets Runtime
    python3.13, arm64, Handler _km_check_bootstrap.handler, Environment (incl
    KM_CHECK_TRIGGER, KM_CHECK_NAME, KM_ARTIFACTS_BUCKET, KM_CHECK_SECRET_PATHS,
    static env), MemorySize/Timeout, Role=check-runner ARN, Tags. For --image:
    PackageType=Image, Code.ImageUri, no Handler/Runtime.
  - `InvokeFunction(ctx, name, payload)` (synchronous, returns response bytes).
  - `DeleteFunction(ctx, name)`.
  - `UpdateTriggerEnv(ctx, name, triggerJSON)`: GetFunctionConfiguration, merge
    KM_CHECK_TRIGGER into existing env, UpdateFunctionConfiguration.

`pkg/check/ddb.go` — DDB row CRUD on {prefix}-checks:
  - `PutCheckRow` (initial create), `UpdateCheckRow` (UpdateItem — never PutItem on
    existing, per project_sandboxmetadata_lossy_roundtrip), `GetCheckRow`,
    `ListCheckRows`, `DeleteCheckRow`. Row shape per interfaces block. secretPaths
    stores ONLY paths (never values).

Resolve the check-runner role ARN + checks table name + artifacts bucket from
config / SSM the way github.go resolves its analogues (mirror it).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./pkg/check/ && go vet ./pkg/check/ && echo OK</automated>
  </verify>
  <done>pkg/check builds: Lambda CRUD (create/update two-call/invoke/delete/trigger-env), zip+pip arm64 packaging, lazy ECR, DDB row CRUD with UpdateItem on existing rows.</done>
</task>

<task type="auto">
  <name>Task 3: internal/app/cmd/check.go CLI + root registration</name>
  <files>internal/app/cmd/check.go, internal/app/cmd/root.go</files>
  <action>
Create `NewCheckCmd(cfg)` mirroring `NewGithubCmd` with subcommands:
  - `deploy <file.py> [--name --env K=V(repeatable) --secret <ssm-path>(repeatable)
    --memory --timeout --schedule <expr> --requirements <path> --image]`:
    package (zip default; --image -> EnsureECRRepo + docker build/push linux/arm64),
    resolve the matching cfg.Checks.Triggers entry for this check name -> BakeTrigger
    -> DeployFunction with KM_CHECK_TRIGGER + sourceHash, write/Update DDB row, if
    --schedule create the EventBridge Scheduler entry targeting the check Lambda.
    Print the function ARN + S3 output prefix + trigger summary.
  - `run <name> [--env K=V --wait]`: synchronous InvokeFunction with per-run env in
    the payload; print captured output + triggered? + dispatch outcome + S3 URI
    (parse the bootstrap return dict).
  - `ls [--json]`: ListCheckRows -> table (name, schedule, sourceHash drift flag vs
    current config bake, updatedAt).
  - `get <name>`: detail (arn, env keys, secret paths, schedule, triggerSummary, sourceHash).
  - `logs <name> [--follow]`: tail the check Lambda CloudWatch log group
    (/aws/lambda/{prefix}-check-{name}) — reuse the existing km logs CW helper if present.
  - `schedule <name> "<expr>" | --off`: create/update/delete the Scheduler entry; update DDB row.
  - `sync [<name>]`: re-resolve @file predicates/prompts from current km-config.yaml,
    re-BakeTrigger, UpdateTriggerEnv on the Lambda, update sourceHash in DDB.
  - `rm <name>`: DeleteFunction + delete schedule + DeleteCheckRow.
Register in `root.go`: `root.AddCommand(NewCheckCmd(cfg))` (near other "c" commands).
`make build` after (ldflags — not bare go build).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && make build > /tmp/116-05-3.out 2>&1; echo "EXIT=$?"; ./km check --help > /tmp/116-05-help.out 2>&1; grep -Eq 'deploy|run|ls|get|logs|schedule|sync|rm' /tmp/116-05-help.out && echo "subcommands present"; tail -5 /tmp/116-05-3.out</automated>
  </verify>
  <done>km binary builds; `km check --help` lists deploy/run/ls/get/logs/schedule/sync/rm.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'TestCheckTriggerBake|TestCheckSourceHash' -count=1` green.
- `go build ./pkg/check/` + `go vet ./pkg/check/` clean.
- `make build` succeeds; `km check --help` shows all 8 subcommands.
- NOTE: live deploy/run/invoke against AWS is verified in Plan 116-08 UAT.
</verification>

<success_criteria>
Covers Phase 116 Success Criteria 1 (deploy provisions a per-check Lambda via SDK,
records DDB row, no terragrunt per check), 6 (zip+wheels default, --image with lazy
ECR), and 7 (ls/get/logs/schedule/rm/sync operate the fleet). The deploy/sync bake
is the env half of Criterion 3.
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-05-SUMMARY.md`
</output>
