---
phase: 116-km-check-serverless-check-runner
plan: 06
type: execute
wave: 2
depends_on: [116-02]
files_modified:
  - cmd/ttl-handler/main.go
  - cmd/ttl-handler/check_dispatch.go
  - cmd/ttl-handler/main_test.go
  - infra/modules/ttl-handler/v1.0.0/main.tf
  - internal/app/cmd/at.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "A CheckDispatch EventBridge rule routes km.sandbox/CheckDispatch events to ttl-handler"
    - "ttl-handler has a new check-dispatch case calling pkg/dispatch.ResumeOrCreate with adapters that delegate warm to handleAgentRun (canonical builder) and cold to PutSandboxCreate"
    - "km at '...' check run <name> schedules a one-shot check-run op (new ttl-handler case)"
    - "ttl-handler IAM is widened with events:PutEvents (PutSandboxCreate) + any missing SSM/EC2 perms"
  artifacts:
    - path: "cmd/ttl-handler/check_dispatch.go"
      provides: "handleCheckDispatch + handleCheckRun + dispatch adapters"
      min_lines: 50
    - path: "infra/modules/ttl-handler/v1.0.0/main.tf"
      provides: "CheckDispatch rule + target + lambda permission + widened IAM"
      contains: "CheckDispatch"
  key_links:
    - from: "cmd/ttl-handler/main.go switch"
      to: "handleCheckDispatch / handleCheckRun"
      via: "new cases check-dispatch / check-run"
      pattern: "check-dispatch|check-run"
    - from: "handleCheckDispatch"
      to: "pkg/dispatch.ResumeOrCreate"
      via: "AgentRunSink delegates to handleAgentRun, ColdCreateSink to PutSandboxCreate"
      pattern: "ResumeOrCreate"
---

<objective>
Wire `pkg/dispatch` (Plan 116-02) into `ttl-handler` as the Stage B consumer: a new
`CheckDispatch` EventBridge rule, a `check-dispatch` case that runs
`ResumeOrCreate` with adapters delegating the warm path to the existing
`handleAgentRun` SSM-dispatch (canonical command builder) and the cold path to
`PutSandboxCreate`; plus a `check-run` case for `km at '...' check run`. Widen the
ttl-handler IAM accordingly.

Purpose: This makes a truthy `when_py` actually resume-or-cold-create the target
alias box. CRITICAL: the warm terminal is `handleAgentRun`'s SSM dispatch (NOT an
SQS FIFO) — so NO new per-sandbox queue/poller, and existing sandboxes need no
recreate. Bridges are NOT touched.

Output: ttl-handler cases + adapters + CheckDispatch TF rule + widened IAM + at.go entry.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-CONTEXT.md
@.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-RESEARCH.md
@docs/superpowers/specs/2026-06-17-km-check-serverless-runner-design.md

# Wiring anchors:
@cmd/ttl-handler/main.go
@pkg/aws/eventbridge.go

<interfaces>
<!-- ttl-handler switch (main.go:215-243): add cases -->
case "check-dispatch": return h.handleCheckDispatch(ctx, event)
case "check-run":      return h.handleCheckRun(ctx, event)

<!-- TTLEvent already has Alias + ProfileName + AutoStart. NEW fields needed: -->
CheckName string `json:"check,omitempty"`
OnAbsent  string `json:"on_absent,omitempty"`
Reason    string `json:"reason,omitempty"`
<!-- Prompt already present on TTLEvent. -->

<!-- handleAgentRun anchor: cmd/ttl-handler/main.go:555 — resolves instance by tag:km:sandbox-id, -->
<!--   AutoStart resume (StartInstances + wait-for-running + budget-resume-close + DDB status + -->
<!--   wait-for-SSM), then SSM SendCommand the prompt. Builds shell via BuildAgentShellCommands -->
<!--   (internal/app/cmd/agent.go:1272 = CANONICAL). project_ttl_agent_run_stale_fork: ensure the -->
<!--   check path routes through the canonical builder, NOT a divergent fork. -->

<!-- pkg/dispatch.ResumeOrCreate signature (from Plan 116-02 interfaces): -->
ResumeOrCreate(ctx, check, alias, prompt, profile, onAbsent string, cooldownSeconds int,
   resolver AliasResolver, agentRun AgentRunSink, cold ColdCreateSink, nonces NonceStore, log)

<!-- Adapters (ttl-handler-side, satisfy pkg/dispatch interfaces): -->
<!--   AliasResolver  -> reuse pkg/github/bridge DynamoAliasResolver.ResolveByAliasWithStatus -->
<!--                     (instantiated against the same DDB client) OR a thin ttl-handler resolver. -->
<!--   AgentRunSink.DispatchAgentRun(ctx, sandboxID, prompt) -> build a TTLEvent{EventType:"agent-run", -->
<!--                     SandboxID, Prompt, AutoStart:true} and call h.handleAgentRun (reuse, canonical). -->
<!--   ColdCreateSink.ColdCreate(ctx, alias, profile, prompt) -> PutSandboxCreate (pkg/aws/eventbridge.go -->
<!--                     PutSandboxCreateEvent; created_by:"check", check- sandbox-id prefix, prompt envelope). -->
<!--   NonceStore -> reuse the nonces table CheckAndStore (key check-trigger:{name}). -->

<!-- CheckDispatch EventBridge rule (RESEARCH "EventBridge rule pattern", in ttl-handler module): -->
<!--   aws_cloudwatch_event_rule.check_dispatch: source ["km.sandbox"], detail-type ["CheckDispatch"]; -->
<!--   target ttl-handler ARN with input_path "$.detail"; aws_lambda_permission principal events.amazonaws.com. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handleCheckDispatch + handleCheckRun + dispatch adapters</name>
  <files>cmd/ttl-handler/check_dispatch.go, cmd/ttl-handler/main.go, cmd/ttl-handler/main_test.go</files>
  <behavior>
    - TestHandleCheckDispatch_RunningAlias: a CheckDispatch TTLEvent for an alias that
      resolves to a running sandbox -> the AgentRunSink path is invoked (mock h.handleAgentRun
      seam) with the event prompt; ColdCreate not invoked.
    - TestHandleCheckDispatch_AbsentColdCreate: alias absent + on_absent=cold-create ->
      PutSandboxCreate seam invoked; agent-run not.
    - TestHandleCheckRun_InvokesCheckLambda: a check-run TTLEvent triggers a synchronous
      check-Lambda invoke (mock seam) — one-shot relay.
  </behavior>
  <action>
Add the new TTLEvent fields (CheckName, OnAbsent, Reason) per the interfaces block.
Add the two switch cases in `main.go` HandleTTLEvent.

Create `cmd/ttl-handler/check_dispatch.go`:
  - `handleCheckDispatch(ctx, event)`: construct the pkg/dispatch adapters wired to
    ttl-handler's existing AWS clients:
      * resolver: reuse pkg/github/bridge.DynamoAliasResolver (or a thin equivalent)
        against the sandboxes table.
      * agentRun adapter: DispatchAgentRun builds a TTLEvent{EventType:"agent-run",
        SandboxID, Prompt:event.Prompt, AutoStart:true} and calls h.handleAgentRun —
        REUSING the canonical builder path (project_ttl_agent_run_stale_fork: do NOT
        fork; if handleAgentRun itself uses a stale builder, route through the
        canonical BuildAgentShellCommands). Resolve sandboxID from the alias inside
        the adapter (resolver already returned it — pass it through).
      * cold adapter: ColdCreate -> PutSandboxCreateEvent with created_by="check".
      * nonces: existing nonce store, key "check-trigger:"+event.CheckName.
    Call pkg/dispatch.ResumeOrCreate(ctx, event.CheckName, event.Alias, event.Prompt,
      event.ProfileName, event.OnAbsent, <cooldown from event or 0>, resolver, agentRun,
      cold, nonces, log). NOTE cooldown is enforced HERE (Stage B), key check-trigger:{name}.
  - `handleCheckRun(ctx, event)`: synchronous lambda:Invoke of {prefix}-check-{event.CheckName}
    (one-shot relay for `km at '...' check run`). Mirror handleScheduleCreate's structure.

Make the agentRun/cold/invoke calls go through small interface seams so main_test.go
can mock them. Write the 3 tests in main_test.go.

`make build-lambdas` after (ttl-handler is a code Lambda).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./cmd/ttl-handler/ -run 'TestHandleCheckDispatch|TestHandleCheckRun' -count=1 -timeout 120s > /tmp/116-06-1.out 2>&1; echo "EXIT=$?"; tail -20 /tmp/116-06-1.out</automated>
  </verify>
  <done>check-dispatch + check-run cases exist; ResumeOrCreate wired with handleAgentRun (canonical) + PutSandboxCreate adapters; cooldown via nonces; 3 tests green; no SQS queue introduced.</done>
</task>

<task type="auto">
  <name>Task 2: CheckDispatch EventBridge rule + widened ttl-handler IAM</name>
  <files>infra/modules/ttl-handler/v1.0.0/main.tf</files>
  <action>
In `infra/modules/ttl-handler/v1.0.0/main.tf` (the module that owns all rules
targeting ttl-handler), add (mirror the existing sandbox_command rule + RESEARCH
"EventBridge rule pattern"):
  - `aws_cloudwatch_event_rule.check_dispatch`: event_pattern
    `{ source = ["km.sandbox"], detail-type = ["CheckDispatch"] }`.
  - `aws_cloudwatch_event_target.check_dispatch_to_ttl`: rule -> ttl-handler Lambda ARN,
    `input_path = "$.detail"` (so the inner dict maps to TTLEvent fields).
  - `aws_lambda_permission.eventbridge_check_dispatch`: principal events.amazonaws.com,
    source_arn = the rule ARN.

Widen the ttl-handler IAM policy (in this module's role/policy doc):
  - `events:PutEvents` on the km.sandbox bus (for the cold-create PutSandboxCreate).
  - `lambda:InvokeFunction` on `arn:aws:lambda:*:*:function:{prefix}-check-*`
    (for handleCheckRun's one-shot invoke).
  - Confirm SSM SendCommand + EC2 Start/Describe perms already present for
    handleAgentRun/handleResume (RESEARCH: delta is small — add only what is missing).
Run `terraform fmt`. No `required_providers` change (module already conforms).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q 'CheckDispatch' infra/modules/ttl-handler/v1.0.0/main.tf && grep -q 'events:PutEvents' infra/modules/ttl-handler/v1.0.0/main.tf && grep -Eq 'check-\*|check-' infra/modules/ttl-handler/v1.0.0/main.tf && terraform -chdir=infra/modules/ttl-handler/v1.0.0 fmt -check && echo OK</automated>
  </verify>
  <done>CheckDispatch rule + target + permission present; IAM widened with events:PutEvents + lambda:InvokeFunction on check-*; terraform fmt clean.</done>
</task>

<task type="auto">
  <name>Task 3: km at check-run schedulable command</name>
  <files>internal/app/cmd/at.go</files>
  <action>
Add `"check-run": {targetARNField: "ttl", eventType: "check-run"}` to the
`schedulableCommands` map in `internal/app/cmd/at.go` (line ~40-49). Ensure the
`km at '...' check run <name>` invocation parses `check run <name>` into the
check-run event with CheckName=<name> on the TTLEvent (follow how agent-run threads
its extra args via the targetARNField=="ttl" extraArgs path at at.go:196). `make build`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q '"check-run"' internal/app/cmd/at.go && go test ./internal/app/cmd/ -run 'TestAt|TestSchedulable' -count=1 -timeout 120s > /tmp/116-06-3.out 2>&1; echo "EXIT=$?"; tail -15 /tmp/116-06-3.out</automated>
  </verify>
  <done>check-run is a schedulable command routed to ttl with eventType check-run; at tests green.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/ttl-handler/ -count=1 -timeout 120s` green (new cases + adapters).
- `go test ./internal/app/cmd/ -run TestAt -count=1` green.
- CheckDispatch rule + widened IAM present in the ttl-handler TF module; `terraform fmt -check` clean.
- No new per-sandbox SQS queue / on-box poller introduced; no pkg/github/bridge or pkg/h1/bridge files modified.
- `go test ./... -count=1 -timeout 600s` green (wave-merge gate) — check exit code, not pipe.
</verification>

<success_criteria>
Covers the Stage B half of Phase 116 Success Criterion 3 (ttl-handler performs
alias-targeted resume-or-cold-create, cooldown-guarded) and Criterion 4 (warm =
handleAgentRun SSM dispatch, NO new queue -> no recreate; cold = PutSandboxCreate;
bridges unmodified) and part of 5 (CheckDispatch rule + widened ttl-handler IAM via km init).
</success_criteria>

<output>
After completion, create `.planning/phases/116-km-check-serverless-check-runner-per-snippet-lambdas-s3-output-capture-config-driven-trigger-to-alias-targeted-sandbox-dispatch/116-06-SUMMARY.md`
</output>
