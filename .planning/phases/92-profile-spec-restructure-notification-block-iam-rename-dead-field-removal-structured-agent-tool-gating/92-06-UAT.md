---
phase: 92
plan: 92-06
type: operator-uat
status: in-progress
updated: 2026-05-31
executed_by: claude (automatable scenarios) + operator (interactive scenarios pending)
deploy: km init --dry-run=false applied to klanker-application (052251888500) us-east-1 @ v0.3.787 (ce3b46db)
---

# Phase 92 — Operator UAT Results

Automatable scenarios executed against the live klanker install (`AWS_PROFILE=klanker-application`).
Interactive scenarios (live Claude behavior + live Slack) left for the operator — Wave 6 is `autonomous: false` by design.

| # | Scenario | VC | Result | Evidence |
|---|----------|----|--------|----------|
| 0 | Full-inventory validation (HARD GATE) | VC-11 | ✅ PASS | `scripts/validate-all-profiles.sh` → all 20 profiles valid |
| 1 | Spot-check individual profiles | VC-11 | ✅ PASS | covered by Scenario 0 |
| 2 | Stale-key rejection | VC-2 | ✅ PASS | `km validate` on `95280201:profiles/learn.v2.yaml` (v1alpha1) → rejects apiVersion pattern, missing `iam`, dead `spec.agent` (allowedTools/maxConcurrentTasks/taskTimeout), `cli.notify*`, `spec.identity` |
| 3 | Sidecar/Lambda deploy | VC-9 | ✅ PASS | `km init --dry-run=false` → "Init complete"; plan was 0 add / 5 change / 0 destroy (5 mgmt Lambda code updates), no destroy-class trips |
| 4 | Real create + on-box config | VC-10 | ✅ PASS | `km create profiles/learn.v2.yaml` → `learn-8ec3372a` reached `run`; remote-compiled `.km-profile.yaml` is `v1alpha2` w/ iam+agent+notification+runtime; the box's userdata-written `/home/sandbox/.claude/settings.json` = canonical `permissions.allow:[Bash,Read,Write,Edit,Glob,Grep,WebFetch,WebSearch,NotebookEdit]` + trustedDirectories — **semantic MATCH vs VC-5 golden; tool set identical to pre-migration inlined autoApprove** |
| 5 | Denied-tool refusal | VC-10 | ⚠️ SYNTHESIS PROVEN / runtime pending operator | `agent_claude.go` maps `tools.deny → permissions.deny` (canonical; deny>ask>allow); `deny:[WebFetch]` profile passes `km validate`. Runtime "Claude refuses WebFetch" needs an interactive agent run + observation |
| 6 | Slack notify-hook on idle | VC-10 | ⏳ OPERATOR | needs live Slack workspace + idle event on a `notification.slack.perSandbox:true` profile |
| 7 | Slack inbound mention-only | VC-10 | ⏳ OPERATOR | needs live Slack workspace (post w/o mention → no dispatch; with mention → dispatch) |
| 8 | Per-message `codex:` prefix routing | VC-10 | ⏳ OPERATOR | needs live Slack inbound → SSM dispatch on a `agent.default:claude` profile |
| 9 | km doctor | VC-8 | ✅ PASS | `km doctor` → 38 passed, 8 warnings, 0 errors (warnings are stale-resource nudges for a pre-existing orphan `learnpolite-a7dd3236`, unrelated to Phase 92) |

## Deploy details
- Plan: `Total across 16 modules: 0 to add, 5 to change, 0 to destroy` — the 5 changes were management-Lambda code updates (create-handler, ttl-handler ×2, email-handler, lambda-slack-bridge) carrying the v1alpha2 toolchain + synthesizer. Destroy-class safety gate: clean.
- create-handler toolchain re-uploaded with v1alpha2 `km`; remote `km create` confirmed compiling v1alpha2 profiles.
- Test sandbox `learn-8ec3372a` provisioned, inspected, and torn down (`km destroy --remote --yes` — also exercised the v1alpha2 destroy path).

## Remaining for operator (interactive — Wave 6)
Run these against the live Slack workspace + a real agent session:
- Scenario 5: `km create` a profile with `agent.claude.tools.deny:[WebFetch]`, `km agent run <id> --prompt "use WebFetch on https://example.com"`, confirm refusal.
- Scenario 6: `km create profiles/learn.v2.yaml` (perSandbox slack), trigger idle, observe Slack post.
- Scenario 7: post in a shared channel w/o @-mention (no dispatch) then with mention (dispatch).
- Scenario 8: post `codex: ls` in a `#sb-<id>` channel on a claude-default profile; observe Codex answer.

On all-PASS, mark `status: resolved` and run `node ~/.claude/get-shit-done/bin/gsd-tools.cjs phase complete 92`.
