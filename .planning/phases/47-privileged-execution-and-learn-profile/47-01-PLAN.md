---
phase: 47-privileged-execution-and-learn-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/profile/types.go
  - pkg/compiler/userdata.go
  - pkg/compiler/userdata_test.go
  - profiles/learn.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "ExecutionSpec has a Privileged bool field with yaml tag privileged,omitempty"
    - "userDataParams has a Privileged bool field"
    - "generateUserData wires p.Spec.Execution.Privileged into params.Privileged"
    - "When Privileged is true, userdata adds sandbox user to wheel group via -G wheel"
    - "When Privileged is true, userdata writes /etc/sudoers.d/sandbox with NOPASSWD:ALL"
    - "When Privileged is false or unset, sandbox user has no wheel group and no sudoers entry"
    - "profiles/learn.yaml exists, passes km validate, has privileged: true, enforcement: both, and broad DNS suffixes"
  artifacts:
    - path: "pkg/profile/types.go"
      provides: "Privileged bool field on ExecutionSpec"
      contains: "Privileged"
    - path: "pkg/compiler/userdata.go"
      provides: "Privileged field on userDataParams + conditional wheel/sudoers in template"
      contains: "Privileged"
    - path: "pkg/compiler/userdata_test.go"
      provides: "Tests verifying privileged userdata output"
      contains: "Privileged"
    - path: "profiles/learn.yaml"
      provides: "Wide-open learn profile for traffic observation"
      contains: "privileged: true"
  key_links:
    - from: "pkg/compiler/userdata.go"
      to: "pkg/profile/types.go"
      via: "generateUserData reads p.Spec.Execution.Privileged"
      pattern: "Execution\\.Privileged"
    - from: "profiles/learn.yaml"
      to: "pkg/profile/types.go"
      via: "Profile YAML parsed into SandboxProfile with Privileged field"
      pattern: "privileged"
---

# Plan 47-01: Privileged execution field + learn profile

## Goal

Add `spec.execution.privileged` as a general-purpose boolean field that grants the sandbox user wheel/sudo access, and create `profiles/learn.yaml` — a permissive profile designed for traffic observation with `km shell --learn`.

## Tasks

<task id="1" title="Add Privileged field to ExecutionSpec">
**File:** `pkg/profile/types.go`

Add to `ExecutionSpec` struct, after the `RsyncFileList` field:

```go
// Privileged grants the sandbox user wheel group membership and
// passwordless sudo access. When false (default), the sandbox user
// has no sudo capability. Operators who want to remove sudo entirely
// from the instance can use a custom AMI without sudo installed.
Privileged bool `yaml:"privileged,omitempty"`
```

No validation changes needed — boolean defaults to false, omitempty means existing profiles are unaffected.
</task>

<task id="2" title="Add Privileged to userDataParams and wire it">
**File:** `pkg/compiler/userdata.go`

1. Add `Privileged bool` to `userDataParams` struct (after the `L7ProxyHosts` field):

```go
// Privileged grants sandbox user wheel group + passwordless sudo.
// Derived from profile spec.execution.privileged.
Privileged bool
```

2. In `generateUserData()`, after the EFS block (~line 1793), add:

```go
// Privileged execution mode (Phase 47)
params.Privileged = p.Spec.Execution.Privileged
```
</task>

<task id="3" title="Update userdata template for conditional sudo">
**File:** `pkg/compiler/userdata.go`

Replace the sandbox user creation block at ~line 341-347:

**Before:**
```bash
# Create restricted sandbox user for agent workloads (no sudo, no root).
# This is the default user for km shell sessions.
useradd -m -s /bin/bash -d /home/sandbox sandbox 2>/dev/null || true
mkdir -p /workspace
chown sandbox:sandbox /workspace
```

**After:**
```bash
# Create sandbox user for agent workloads.
# When privileged=true, the user gets wheel group + passwordless sudo.
{{- if .Privileged }}
useradd -m -s /bin/bash -d /home/sandbox -G wheel sandbox 2>/dev/null || true
echo "sandbox ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/sandbox
chmod 0440 /etc/sudoers.d/sandbox
{{- else }}
# Restricted: no sudo, no root.
useradd -m -s /bin/bash -d /home/sandbox sandbox 2>/dev/null || true
{{- end }}
mkdir -p /workspace
chown sandbox:sandbox /workspace
```
</task>

<task id="4" title="Add tests for privileged userdata">
**File:** `pkg/compiler/userdata_test.go`

Add two test functions:

1. `TestUserdataPrivilegedEnabled` — create a profile with `Privileged: true`, generate userdata, assert output contains:
   - `-G wheel sandbox`
   - `NOPASSWD:ALL`
   - `/etc/sudoers.d/sandbox`

2. `TestUserdataPrivilegedDisabled` — create a profile with `Privileged: false` (or unset), generate userdata, assert output does NOT contain:
   - `-G wheel`
   - `NOPASSWD`
   - `sudoers.d`

Follow existing test patterns in the file (use `generateUserData` with a minimal profile + assert on string contents).
</task>

<task id="5" title="Create profiles/learn.yaml">
**File:** `profiles/learn.yaml`

Create a wide-open profile designed for traffic observation:

```yaml
apiVersion: klankermaker.ai/v1alpha1
kind: SandboxProfile
metadata:
  name: learn
  labels:
    tier: development
    tool: traffic-observation
    builtin: "true"
  prefix: learn

spec:
  lifecycle:
    ttl: "2h"
    idleTimeout: "30m"
    teardownPolicy: destroy

  runtime:
    substrate: ec2
    spot: true
    instanceType: t3.medium
    region: us-east-1

  execution:
    shell: /bin/bash
    workingDir: /workspace
    privileged: true
    initCommands:
      - "yum install -y git jq tar gzip unzip curl wget"

  sourceAccess:
    mode: allowlist
    github:
      allowedRepos: []
      allowedRefs:
        - "*"

  network:
    enforcement: both
    egress:
      allowedDNSSuffixes:
        # Wide-open for traffic observation — covers common TLDs.
        # eBPF runs in observe mode, logging all traffic for km shell --learn.
        - ".com"
        - ".org"
        - ".net"
        - ".io"
        - ".dev"
        - ".ai"
        - ".co"
        - ".app"
        - ".cloud"
        - ".sh"
        - ".me"
        - ".info"
        - ".edu"
        - ".gov"
        - ".amazonaws.com"
      allowedHosts: []

  identity:
    roleSessionDuration: "1h"
    allowedRegions:
      - us-east-1
    sessionPolicy: minimal

  sidecars:
    dnsProxy:
      enabled: true
      image: km-dns-proxy:latest
    httpProxy:
      enabled: true
      image: km-http-proxy:latest
    auditLog:
      enabled: true
      image: km-audit-log:latest
    tracing:
      enabled: true
      image: km-tracing:latest

  observability:
    commandLog:
      destination: cloudwatch
      logGroup: /klankrmkr/sandboxes
    networkLog:
      destination: cloudwatch
      logGroup: /klankrmkr/network
    tlsCapture:
      enabled: true

  budget:
    compute:
      maxSpendUSD: 2.00
    ai:
      maxSpendUSD: 0.00
    warningThreshold: 0.80

  artifacts:
    paths:
      - /workspace
    maxSizeMB: 500
```

Key design decisions:
- **Short TTL (2h)** — safety net since network is wide open
- **teardownPolicy: destroy** — don't keep permissive sandboxes around
- **enforcement: both** — eBPF observe captures DNS/TLS traffic, proxy captures L7
- **tlsCapture: enabled** — so eBPF TLS uprobes record host/repo observations
- **Broad TLD suffixes** — `.com`, `.org`, `.io`, etc. rather than trying to enumerate every domain; the learn mode will capture the specific domains actually accessed
- **privileged: true** — sudo access for installing packages, configuring services
- **No AI budget** — this is for human-driven observation, not agent workloads
- **Empty allowedRepos** — git clone will work via the broad DNS suffixes; specific repos get captured by learn mode
</task>

<task id="6" title="Verify profiles/learn.yaml passes validation">
Run `make build && ./bin/km validate profiles/learn.yaml` to confirm the profile is valid.
If validation fails on any field, fix the profile.
</task>

## Verification

After all tasks complete:

1. `go test ./pkg/compiler/... -run TestUserdataPrivileged` — both tests pass
2. `go test ./pkg/profile/... -run TestParse` — existing parse tests still pass (no regression)
3. `make build && ./bin/km validate profiles/learn.yaml` — profile validates cleanly
4. Manual inspection: generated userdata for a privileged profile contains wheel + sudoers; non-privileged profile does not
