---
phase: 80-km-cluster-cross-account-irsa-for-k8s-integrations
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - infra/modules/km-operator-policy/v1.0.0/main.tf
  - infra/modules/km-operator-policy/v1.0.0/variables.tf
  - infra/modules/km-operator-policy/v1.0.0/outputs.tf
  - infra/modules/create-handler/v1.0.0/main.tf
autonomous: false
requirements:
  - operator-feature-80
must_haves:
  truths:
    - "Shared km-operator-policy/v1.0.0/ module exists with 14 aws_iam_role_policy resources (cloudwatch_logs excluded — stays inline in create-handler)"
    - "create-handler/v1.0.0/main.tf consumes the shared module instead of inline 14 policy resources"
    - "create-handler/v1.0.0/main.tf has 14 moved {} blocks mapping old addresses → new module addresses"
    - "`terragrunt plan -detailed-exitcode` in infra/live/use1/create-handler exits 0 (no changes) OR shows only address reorganization with byte-identical JSON — NO destroy+create of any aws_iam_role_policy and NO modification of aws_iam_role.create_handler"
    - "All 14 module variables (role_id + 7 substantive inputs) flow correctly from create-handler caller to shared module"
  artifacts:
    - path: "infra/modules/km-operator-policy/v1.0.0/main.tf"
      provides: "14 aws_iam_role_policy resources keyed identically to the originals in create-handler"
      contains: "resource \"aws_iam_role_policy\" \"s3_artifacts\""
    - path: "infra/modules/km-operator-policy/v1.0.0/variables.tf"
      provides: "8 inputs — role_id, resource_prefix, artifact_bucket_arn, state_bucket, dynamodb_table_name, dynamodb_budget_table_arn, sandbox_table_name, identities_table_name"
      contains: "variable \"role_id\""
    - path: "infra/modules/create-handler/v1.0.0/main.tf"
      provides: "module.km_operator_policy call + 14 moved blocks + cloudwatch_logs still inline"
      contains: "module \"km_operator_policy\""
  key_links:
    - from: "infra/modules/create-handler/v1.0.0/main.tf"
      to: "infra/modules/km-operator-policy/v1.0.0/"
      via: "module \"km_operator_policy\" { source = \"../../km-operator-policy/v1.0.0\" ... }"
      pattern: "source\\s*=\\s*\"\\.\\./\\.\\./km-operator-policy/v1\\.0\\.0\""
    - from: "infra/modules/create-handler/v1.0.0/main.tf moved blocks"
      to: "module.km_operator_policy.aws_iam_role_policy.*"
      via: "Terraform 1.1+ moved {} declaration per policy"
      pattern: "moved\\s*\\{[^}]*module\\.km_operator_policy\\.aws_iam_role_policy"
---

<objective>
Extract the 14 inline `aws_iam_role_policy` resources from `infra/modules/create-handler/
v1.0.0/main.tf:28-528` into a new shared module `infra/modules/km-operator-policy/v1.0.0/`
and refactor create-handler to consume it. **Critical gate: `terragrunt plan -detailed-
exitcode` must show zero net change** — any policy replacement or role modification
violates the locked decision from 80-CONTEXT.md and risks an IAM permission gap on the
production Lambda role.

Purpose: Single source of truth for km-operator permissions. Both the Lambda role
(create-handler) and the future IRSA role (cluster-irsa, Plan 80-03) consume the same
module, eliminating drift risk identified in CONTEXT.md ("Avoids documented drift risk
between Lambda role and IRSA role").

Output: New module directory with three files (main.tf, variables.tf, outputs.tf) +
refactored create-handler main.tf with 14 `moved {}` blocks + verified zero-diff plan
output. **Plan 80-03 (cluster-irsa) cannot start until this plan's plan-detailed-exitcode
gate passes.**
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-CONTEXT.md
@.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-RESEARCH.md
@infra/modules/create-handler/v1.0.0/main.tf
@infra/live/use1/create-handler/terragrunt.hcl
@CLAUDE.md

<interfaces>
<!-- Authoritative policy list (from 80-RESEARCH.md "Code Examples — Full Policy List"). -->
<!-- 14 policies to EXTRACT (cloudwatch_logs stays inline per RESEARCH.md "Open Questions" §1): -->

| # | Resource Name | Policy Name (AWS) |
|---|---|---|
| 1 | s3_artifacts | {prefix}-create-handler-s3 |
| 2 | dynamodb | {prefix}-create-handler-dynamodb |
| 3 | dynamodb_sandboxes | {prefix}-create-handler-dynamodb-sandboxes |
| 4 | terraform_state | {prefix}-create-handler-tf-state |
| 5 | ec2_provisioning | {prefix}-create-handler-ec2 |
| 6 | iam_sandbox | {prefix}-create-handler-iam |
| 7 | ecs_provisioning | {prefix}-create-handler-ecs |
| 8 | scheduler | {prefix}-create-handler-scheduler |
| 9 | ssm | {prefix}-create-handler-ssm |
| 10 | ssm_send_command | {prefix}-create-handler-ssm-send-command |
| 11 | ses_send | {prefix}-create-handler-ses |
| 12 | lambda_budget | {prefix}-create-handler-lambda |
| 13 | kms | {prefix}-create-handler-kms |
| 14 | sqs_slack_inbound | {prefix}-create-handler-sqs-slack-inbound |

<!-- Module variables required (from RESEARCH.md "Pattern 1 Implementation"): -->
```hcl
variable "role_id"                   { type = string }
variable "resource_prefix"           { type = string }
variable "artifact_bucket_arn"       { type = string }
variable "state_bucket"              { type = string }
variable "dynamodb_table_name"       { type = string }
variable "dynamodb_budget_table_arn" { type = string }
variable "sandbox_table_name"        { type = string }
variable "identities_table_name"     { type = string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create km-operator-policy/v1.0.0/ module with 14 policies extracted from create-handler</name>
  <files>
    infra/modules/km-operator-policy/v1.0.0/main.tf,
    infra/modules/km-operator-policy/v1.0.0/variables.tf,
    infra/modules/km-operator-policy/v1.0.0/outputs.tf
  </files>
  <action>
    1. Read infra/modules/create-handler/v1.0.0/main.tf in full. Identify the 14 `aws_iam_role_policy` resources listed in <interfaces> above (skipping `cloudwatch_logs`).

    2. Create `infra/modules/km-operator-policy/v1.0.0/variables.tf` declaring the 8 inputs in the <interfaces> block, each with `type = string` and a one-line `description` matching its create-handler usage.

    3. Create `infra/modules/km-operator-policy/v1.0.0/main.tf`:
       - Copy each of the 14 `aws_iam_role_policy` resource blocks VERBATIM from create-handler/v1.0.0/main.tf.
       - In each copied block, change `role = aws_iam_role.create_handler.id` to `role = var.role_id`.
       - Replace any `var.resource_prefix` and other substantive variable references — they should already match because the source module exports the same variable names. Confirm by grep, do not rename anything else.
       - If a policy body references `data.aws_caller_identity.current.account_id`, declare `data "aws_caller_identity" "current" {}` at the top of the new module's main.tf (verify against create-handler usage).
       - **Do NOT include cloudwatch_logs** — it stays in create-handler. Document this with a comment at the top of main.tf: `# cloudwatch_logs intentionally NOT extracted — Lambda-specific (see RESEARCH.md Open Question 1)`

    4. Create `infra/modules/km-operator-policy/v1.0.0/outputs.tf` with content `# No outputs — policies are attached to var.role_id directly.` (an empty outputs.tf or a single comment line).

    5. Validate the new module compiles with Terraform:
       ```
       cd /Users/khundeck/working/klankrmkr/infra/modules/km-operator-policy/v1.0.0
       terraform init -backend=false
       terraform validate
       ```
       Both commands must exit 0.

    AVOID: Reordering policy statements within a policy body (e.g. shuffling actions in a `Statement` array). Even a syntactically-equivalent JSON reordering counts as a diff in some Terraform versions. Copy verbatim, byte-for-byte where possible.
    AVOID: Renaming any resource label (`s3_artifacts`, `dynamodb`, etc.). The `moved` blocks in Task 2 reference these exact labels.
    AVOID: Hand-rolling a policy body — always paste from the existing create-handler source.

    REFERENCE FILES:
    - infra/modules/create-handler/v1.0.0/main.tf — authoritative source. Lines 28-528 contain the policies per CONTEXT.md.
    - infra/modules/budget-enforcer-lambda/v1.0.0/ — example "module with policies attached to a passed-in role_id" if you need a reference layout (verify it exists; this is a guess based on common module conventions).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/infra/modules/km-operator-policy/v1.0.0 && terraform init -backend=false 2>&1 | tail -5 && terraform validate 2>&1 | tail -5 && grep -c '^resource "aws_iam_role_policy"' main.tf</automated>
  </verify>
  <done>
    Module directory exists with three files. `terraform validate` exits 0. `grep -c '^resource "aws_iam_role_policy"' main.tf` returns exactly 14. `grep -c "var.role_id" main.tf` returns ≥ 14 (one role binding per policy).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor create-handler/v1.0.0/main.tf to consume shared module + add 14 moved blocks</name>
  <files>infra/modules/create-handler/v1.0.0/main.tf</files>
  <action>
    1. In `infra/modules/create-handler/v1.0.0/main.tf`, REMOVE the 14 `aws_iam_role_policy` resource blocks listed in <interfaces> (NOT cloudwatch_logs — leave it).

    2. ADD a `module "km_operator_policy"` block (anywhere after `aws_iam_role.create_handler` is defined) wiring all 8 inputs from the existing variables in scope:
       ```hcl
       module "km_operator_policy" {
         source = "../../km-operator-policy/v1.0.0"

         role_id                   = aws_iam_role.create_handler.id
         resource_prefix           = var.resource_prefix
         artifact_bucket_arn       = var.artifact_bucket_arn
         state_bucket              = var.state_bucket
         dynamodb_table_name       = var.dynamodb_table_name
         dynamodb_budget_table_arn = var.dynamodb_budget_table_arn
         sandbox_table_name        = var.sandbox_table_name
         identities_table_name     = var.identities_table_name
       }
       ```
       Confirm each `var.*` reference matches an existing variable in create-handler's `variables.tf` — if any name differs (e.g. the variable is `artifact_bucket` not `artifact_bucket_arn`), use the existing name on the right side.

    3. ADD 14 `moved {}` blocks at the bottom of create-handler/v1.0.0/main.tf (or in a new `moved.tf` file in the same directory, whichever you choose — be consistent):
       ```hcl
       moved {
         from = aws_iam_role_policy.s3_artifacts
         to   = module.km_operator_policy.aws_iam_role_policy.s3_artifacts
       }
       moved {
         from = aws_iam_role_policy.dynamodb
         to   = module.km_operator_policy.aws_iam_role_policy.dynamodb
       }
       # ... 12 more for: dynamodb_sandboxes, terraform_state, ec2_provisioning,
       #     iam_sandbox, ecs_provisioning, scheduler, ssm, ssm_send_command,
       #     ses_send, lambda_budget, kms, sqs_slack_inbound
       ```
       One moved block per extracted policy. cloudwatch_logs does NOT get a moved block — it stays inline.

    4. Run `terraform fmt` in `infra/modules/create-handler/v1.0.0/` to normalize whitespace.

    5. Validate locally:
       ```
       cd /Users/khundeck/working/klankrmkr/infra/modules/create-handler/v1.0.0
       terraform init -backend=false
       terraform validate
       ```
       Both must exit 0.

    AVOID: Removing `aws_iam_role_policy.cloudwatch_logs` — it must stay inline per RESEARCH.md Open Question 1.
    AVOID: Leaving a `moved` block dangling for a policy you didn't actually remove (would cause "no resource at source address" warning).
    AVOID: Adding moved blocks before adding the module call — terraform validate will fail with "to address does not exist".
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/infra/modules/create-handler/v1.0.0 && terraform init -backend=false 2>&1 | tail -3 && terraform validate 2>&1 | tail -3 && grep -c '^moved {' main.tf moved.tf 2>/dev/null | awk -F: 'BEGIN{s=0} {s+=$2} END{print "moved_blocks:"s}'</automated>
  </verify>
  <done>
    `terraform validate` exits 0 in create-handler/v1.0.0/. Total of exactly 14 `moved {` blocks across the directory. `cloudwatch_logs` resource block is still present (`grep -c 'aws_iam_role_policy" "cloudwatch_logs"' main.tf` returns 1).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HARD GATE — Zero-net-diff terragrunt plan verification</name>
  <files>none</files>
  <what-built>
    The km-operator-policy shared module + create-handler refactor with `moved {}` blocks.
    Before any downstream consumer (cluster-irsa in Plan 80-03) is built, this gate
    confirms the refactor produces NO change to the running Lambda's IAM role.
  </what-built>
  <action>
    Operator runs `terragrunt plan -detailed-exitcode` against `infra/live/use1/create-handler/`
    and confirms the output shows either (Pattern A) zero changes or (Pattern B) only address-only
    `moved` rewrites — NO destroy+create of any aws_iam_role_policy and NO modification of the
    aws_iam_role.create_handler resource. See <how-to-verify> for the exact pass/fail criteria.

    On approval: Plan 80-03 (cluster-irsa module) is unblocked.
    On failure: executor returns to Task 2 to add missing `moved` blocks (one per offending policy).
  </action>
  <how-to-verify>
    1. Ensure AWS creds are loaded for the klanker-application profile:
       ```bash
       export AWS_PROFILE=klanker-application
       aws sts get-caller-identity   # should print the application account
       ```

    2. Run the locked-decision verification command from CONTEXT.md:
       ```bash
       cd /Users/khundeck/working/klankrmkr/infra/live/use1/create-handler
       terragrunt plan -detailed-exitcode 2>&1 | tee /tmp/80-02-plan.txt
       echo "exit:$?"
       ```

    3. Inspect `/tmp/80-02-plan.txt` for the EXPECTED OUTCOME (one of these two patterns):

       **Pattern A (preferred):** exit code 0 — "No changes. Your infrastructure matches the configuration."

       **Pattern B (acceptable):** exit code 2 (changes present) BUT all changes are address-only moves:
       - Lines beginning with `# module.km_operator_policy.aws_iam_role_policy.X has moved from aws_iam_role_policy.X`
       - NO line containing `~ aws_iam_role.create_handler` (would mean the role itself is being modified)
       - NO line containing `- aws_iam_role_policy.` followed by `+ aws_iam_role_policy.` (would mean destroy+create)
       - NO line containing `# aws_iam_role_policy.{policy_name} will be destroyed`
       - NO line containing `# aws_iam_role_policy.{policy_name} will be created` (unless paired with corresponding move)

    4. If pattern A or B is observed → reply `approved`.
       If `terragrunt plan` shows ANY destroy+create of an aws_iam_role_policy, or any `~ aws_iam_role.create_handler` modification → reply with the diff snippet and the executor must return to Task 2 to add the missing `moved` blocks.

    5. As an additional sanity check, run:
       ```bash
       grep -c "moved from aws_iam_role_policy" /tmp/80-02-plan.txt
       ```
       This should return 14 (one per extracted policy) when Pattern B applies.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification required — see <how-to-verify> steps above"</automated>
  </verify>
  <done>
    Operator confirms `terragrunt plan -detailed-exitcode` shows Pattern A (no changes) or Pattern B (address-only moves) and replies "approved". No `~ aws_iam_role.create_handler` modifications. No `aws_iam_role_policy` destroy+create pairs.
  </done>
  <resume-signal>Type "approved" if the plan is zero-diff or address-only. Paste the offending diff snippet if any policy is destroyed/created/modified.</resume-signal>
</task>

</tasks>

<verification>
- `terraform validate` exits 0 for both `infra/modules/km-operator-policy/v1.0.0/` and `infra/modules/create-handler/v1.0.0/`
- Exactly 14 `aws_iam_role_policy` resources in km-operator-policy/v1.0.0/main.tf
- Exactly 14 `moved {}` blocks across create-handler/v1.0.0/
- `cloudwatch_logs` policy still inline in create-handler (not extracted)
- `terragrunt plan -detailed-exitcode` in infra/live/use1/create-handler shows zero net change (exit 0) or address-only moves with byte-identical JSON
- Human-verify checkpoint approved before Plan 80-03 begins
</verification>

<success_criteria>
- Shared module at `infra/modules/km-operator-policy/v1.0.0/` exists and validates.
- create-handler consumes it via `module "km_operator_policy"` block.
- 14 `moved` blocks present, mapped 1:1 to the extracted policies.
- Operator confirms zero-diff `terragrunt plan` result.
- The phase gate is now OPEN for Plan 80-03 (cluster-irsa) to consume the shared module.
</success_criteria>

<output>
After completion, create `.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-02-SUMMARY.md` documenting:
- Module file layout
- Verbatim list of variable names exposed by km-operator-policy/v1.0.0/variables.tf (Plan 80-03 will consume these exact names)
- Pasted output of `terragrunt plan -detailed-exitcode` (the zero-diff or address-only-moves diff) for the audit trail
- Confirmation of cloudwatch_logs intentional non-extraction
</output>
