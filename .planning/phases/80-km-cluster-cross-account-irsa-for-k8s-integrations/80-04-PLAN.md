---
phase: 80-km-cluster-cross-account-irsa-for-k8s-integrations
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_clusters_test.go
autonomous: true
requirements:
  - operator-feature-80
must_haves:
  truths:
    - "ClusterConfig struct exists in internal/app/config/config.go with the 5 fields (Name, OIDCProviderARN, Namespace, ServiceAccount, RoleARN) and yaml/mapstructure tags as locked in CONTEXT.md"
    - "Config struct has Clusters []ClusterConfig field with mapstructure:\"clusters\" + yaml:\"clusters\" tags"
    - "config.Load() merges 'clusters' viper key — `v.SetDefault(\"clusters\", []interface{}{})` AND \"clusters\" appended to the merge key list"
    - "config.Load() populates cfg.Clusters via v.UnmarshalKey(\"clusters\", &cfg.Clusters)"
    - "TestClustersField in config_clusters_test.go is no longer t.Skip — it loads a temp km-config.yaml and asserts cfg.Clusters[0] matches the seeded entry"
    - "Backwards compat: absent `clusters:` key in km-config.yaml produces an empty (nil-equivalent) slice, NOT an error"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "ClusterConfig struct + Config.Clusters field + viper merge"
      contains: "type ClusterConfig struct"
    - path: "internal/app/config/config_clusters_test.go"
      provides: "TestClustersField that exercises real Load() against temp km-config.yaml"
      contains: "func TestClustersField"
  key_links:
    - from: "internal/app/config/config.go Config struct"
      to: "km-config.yaml clusters: list"
      via: "viper UnmarshalKey + mapstructure tags"
      pattern: "Clusters\\s+\\[\\]ClusterConfig"
    - from: "Plan 80-05's km cluster add/list/rm subcommands"
      to: "cfg.Clusters slice"
      via: "reads cfg.Clusters at command entry; writes via persistClustersConfig (defined in cluster.go, Plan 80-05)"
      pattern: "cfg\\.Clusters"
---

<objective>
Add the `ClusterConfig` Go struct and `Config.Clusters []ClusterConfig` field that the
CLI in Plan 80-05 will read and persist. Implement the viper merge wiring so existing
operator km-config.yaml files with new `clusters:` entries actually populate the field
at runtime (RESEARCH.md Pitfall 6: without explicit merge key inclusion, `cfg.Clusters`
is always empty).

Purpose: Locked from CONTEXT.md — the config schema must exist before the CLI can
reference it. Decoupled from CLI implementation (Plan 80-05) so the config layer can be
validated against config_clusters_test.go (already scaffolded in Plan 80-01) before any
Cobra command logic is built.

Output: One modified file (config.go) + one test file unskipped (config_clusters_test.go).
TestClustersField transitions from `t.Skip` to a real round-trip assertion that exercises
config.Load() against a temp km-config.yaml.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-CONTEXT.md
@.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-RESEARCH.md
@.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-01-SUMMARY.md
@internal/app/config/config.go
@internal/app/config/config_clusters_test.go

<interfaces>
<!-- LOCKED config schema (CONTEXT.md decisions section) — must match these tags exactly. -->

```go
type ClusterConfig struct {
    Name             string `mapstructure:"name"               yaml:"name"`
    OIDCProviderARN  string `mapstructure:"oidc_provider_arn"  yaml:"oidc_provider_arn"`
    Namespace        string `mapstructure:"namespace"          yaml:"namespace"`
    ServiceAccount   string `mapstructure:"service_account"    yaml:"service_account"`
    RoleARN          string `mapstructure:"role_arn"           yaml:"role_arn"`
}

// Config struct (existing) gains one new field:
//   Clusters []ClusterConfig `mapstructure:"clusters" yaml:"clusters"`
```

<!-- Viper wiring locations (RESEARCH.md Pattern 5 + Pitfall 6, citing internal/app/config/config.go:Load() — verify exact line numbers when reading the file). -->

Three edits required inside config.Load():

1. Before the merge loop: `v.SetDefault("clusters", []interface{}{})`
2. Inside the merge key list (the slice of strings around the line that already has "domain", "accounts", etc.): append `"clusters"`
3. In the `cfg := &Config{...}` block (or immediately after): `if err := v.UnmarshalKey("clusters", &cfg.Clusters); err != nil { return nil, fmt.Errorf("unmarshal clusters: %w", err) }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ClusterConfig struct + Config.Clusters field + viper wiring in config.go</name>
  <files>internal/app/config/config.go</files>
  <behavior>
    Tests in internal/app/config/config_clusters_test.go (Task 2 will un-skip them) must pass against the new code path:
    - With km-config.yaml containing `clusters: []`: cfg.Clusters is non-nil, len 0
    - With km-config.yaml omitting `clusters:` entirely: cfg.Clusters is empty slice (nil or len-0 acceptable), no error returned from Load()
    - With km-config.yaml containing one cluster entry: cfg.Clusters[0].Name, OIDCProviderARN, Namespace, ServiceAccount, RoleARN all match the seeded values
    - With km-config.yaml containing two entries: len(cfg.Clusters) == 2, both round-trip cleanly
  </behavior>
  <action>
    1. Read internal/app/config/config.go fully to locate:
       a. Where existing struct definitions live (likely near the top after package + imports)
       b. The Config struct body
       c. The Load() function — specifically the v.SetDefault() block, the merge-keys slice, and the `cfg := &Config{...}` block
       d. The exact pattern for `v.UnmarshalKey` (or equivalent) usage on other slice/struct fields if any exist

    2. ADD the ClusterConfig struct above the Config struct (or wherever other helper structs live). Use the verbatim definition from <interfaces>.

    3. ADD `Clusters []ClusterConfig \`mapstructure:"clusters" yaml:"clusters"\`` field to the Config struct. Place it next to other list-typed fields for readability (or at the end of the struct — be consistent with existing style).

    4. INSIDE config.Load():
       a. Add `v.SetDefault("clusters", []interface{}{})` next to the other SetDefault calls (RESEARCH.md says near line 196 — verify by reading the file).
       b. Add `"clusters"` to the merge key list (RESEARCH.md says around line 259-293 — verify by reading the file).
       c. After the existing `cfg := &Config{...}` block, add:
          ```go
          if err := v.UnmarshalKey("clusters", &cfg.Clusters); err != nil {
              return nil, fmt.Errorf("unmarshal clusters: %w", err)
          }
          ```
          (Adjust the existing return signature/error-wrap pattern to match. If config.Load() returns `(*Config, error)`, the above matches. If it returns just `*Config` and logs, follow the existing pattern.)

    5. Run `go build ./internal/app/config/...` to confirm compilation passes.

    AVOID: Reordering existing fields in the Config struct — minimize diff to avoid breaking unrelated YAML key ordering. Append `Clusters` at the end.
    AVOID: Changing existing field tag conventions — match the surrounding mapstructure/yaml double-tag pattern verbatim.
    AVOID: Adding a separate `LoadClusters()` helper — RESEARCH.md is explicit: merge into the existing Load() flow.
    AVOID: Using yaml.Marshal/Unmarshal directly — config.go uses viper. Stick with v.UnmarshalKey.

    REFERENCES:
    - RESEARCH.md "Pattern 5: Config Persistence — Cluster List (NEW requirement)"
    - RESEARCH.md "Pitfall 6: persistClustersConfig — clusters: Not in Viper Merge Keys"
    - CONTEXT.md "Config schema" section (LOCKED struct definition)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./internal/app/config/... && go vet ./internal/app/config/... && grep -c "type ClusterConfig struct" internal/app/config/config.go && grep -c "Clusters\s*\[\]ClusterConfig" internal/app/config/config.go && grep -c '"clusters"' internal/app/config/config.go</automated>
  </verify>
  <done>
    `go build ./internal/app/config/...` exits 0. `grep -c "type ClusterConfig struct" internal/app/config/config.go` returns 1. `grep -c "Clusters\\s*\\[\\]ClusterConfig" internal/app/config/config.go` returns 1. `grep -c '"clusters"' internal/app/config/config.go` returns ≥ 2 (SetDefault + merge-list — UnmarshalKey reference uses string literal too, so ≥ 3 is fine). `go vet` is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Un-skip TestClustersField — exercise real Load() against temp km-config.yaml</name>
  <files>internal/app/config/config_clusters_test.go</files>
  <behavior>
    - TestClustersField no longer calls t.Skip()
    - Test creates a temp directory via t.TempDir(), writes a km-config.yaml containing the existing required keys (domain, accounts, region as already required by config.Load — read config.go to determine the minimum), plus a `clusters:` list with one entry
    - Test invokes config.Load (with appropriate path override — env var, working-directory chdir, or whatever existing convention exists) and asserts:
      - len(cfg.Clusters) == 1
      - cfg.Clusters[0].Name == "dev-use1-0"
      - cfg.Clusters[0].OIDCProviderARN == "arn:aws:iam::123456789012:oidc-provider/oidc.eks.us-east-1.amazonaws.com/id/EXAMPLE"
      - cfg.Clusters[0].Namespace == "*"
      - cfg.Clusters[0].ServiceAccount == "km"
      - cfg.Clusters[0].RoleARN == "arn:aws:iam::987654321098:role/km-cluster-dev-use1-0"
    - Optionally: a second sub-test (`t.Run("empty list")`) writes km-config.yaml WITHOUT the clusters key and asserts cfg.Clusters is len 0 (nil-or-empty), Load returns nil error
  </behavior>
  <action>
    1. Read internal/app/config/config_clusters_test.go (Plan 80-01 scaffold).
    2. Remove the `t.Skip(...)` line.
    3. Implement the test body per <behavior>. The exact mechanism for pointing config.Load() at the temp file depends on what config.go supports:
       - If config.go has a `LoadFromPath(path string)` helper or honors `KM_CONFIG_FILE` env var → use that.
       - If config.Load() always reads from a fixed `findRepoRoot()` location → use `t.Chdir(tempDir)` (Go 1.24+) or `os.Chdir(tempDir); t.Cleanup(...)` to redirect.
       - Look at any existing config_test.go (or init_test.go for the pattern that other test files use).

    4. Write the temp km-config.yaml. Minimum content (verify required keys by reading config.go's validation in Load()):
       ```yaml
       # Minimum required keys for Load() to succeed — copy from a real km-config.yaml or
       # from config.go validation. Below is a placeholder structure:
       domain: example.com
       email_subdomain: sandboxes
       region: us-east-1
       resource_prefix: km
       accounts:
         management: "111111111111"
         terraform:  "222222222222"
         application: "333333333333"
       operator_email: ops@example.com
       sso:
         start_url: https://example.awsapps.com/start
         region: us-east-1
       clusters:
         - name: dev-use1-0
           oidc_provider_arn: arn:aws:iam::123456789012:oidc-provider/oidc.eks.us-east-1.amazonaws.com/id/EXAMPLE
           namespace: "*"
           service_account: km
           role_arn: arn:aws:iam::987654321098:role/km-cluster-dev-use1-0
       ```
       (If the real config.go demands a different minimum set, follow that. The integration is that the `clusters` key under the existing schema parses correctly.)

    5. Run `go test ./internal/app/config/ -run TestClustersField -v` — assert PASS (no SKIP).

    AVOID: Mocking viper or using internal package access — exercise the public config.Load() so the test reflects production behaviour.
    AVOID: Hardcoding the temp file path outside t.TempDir() — would leak across test runs.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/config/ -run TestClustersField -v 2>&1 | tee /tmp/80-04-test.log | grep -E "(PASS|FAIL|SKIP)" && ! grep -q "SKIP" /tmp/80-04-test.log && grep -q "PASS" /tmp/80-04-test.log</automated>
  </verify>
  <done>
    `go test ./internal/app/config/ -run TestClustersField -v` exits 0, prints `--- PASS` for TestClustersField, contains zero SKIP lines for the cluster tests. `go test ./...` is no worse than before this plan started (no regression in other packages).
  </done>
</task>

</tasks>

<verification>
- `go build ./...` exits 0 from repo root
- `go test ./internal/app/config/ -run TestClustersField -v` PASSES (not SKIP)
- `go vet ./...` is clean
- ClusterConfig struct + Config.Clusters field present in config.go with locked tags
- Viper merge wiring complete (SetDefault, merge list, UnmarshalKey)
</verification>

<success_criteria>
- Plan 80-05 (CLI) can reference cfg.Clusters and config.ClusterConfig immediately.
- Operators with an existing km-config.yaml see no change in behaviour (absent `clusters:` key → empty slice).
- TestClustersField is now a real test, not a placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/80-km-cluster-cross-account-irsa-for-k8s-integrations/80-04-SUMMARY.md` documenting:
- Final placement of ClusterConfig in config.go (line range)
- Exact merge-key list change (which existing key it was appended after)
- Whether config.Load() needed signature changes (it should not have)
- How TestClustersField redirects config.Load() to the temp file (chdir vs env var vs path argument)
</output>
