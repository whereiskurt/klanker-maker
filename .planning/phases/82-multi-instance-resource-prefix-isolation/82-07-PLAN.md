---
phase: 82-multi-instance-resource-prefix-isolation
plan: 07
type: execute
wave: 3
depends_on: ["82-01", "82-02", "82-03", "82-04", "82-05"]
files_modified:
  - infra/modules/email-handler/v1.0.0/main.tf
  - infra/modules/email-handler/v1.0.0/variables.tf
  - infra/live/use1/email-handler/terragrunt.hcl
autonomous: true
requirements: []

must_haves:
  truths:
    - "Email-handler IAM policy reads `s3:GetObject` from `arn:aws:s3:::${var.state_prefix}/sandboxes/*/metadata.json` instead of the literal `arn:aws:s3:::tf-km/sandboxes/*/metadata.json`"
    - "For the existing `km` install (state_prefix='tf-km'), the evaluated ARN is identical to today — zero Terraform diff"
    - "For a second install (state_prefix='tf-rg'), the policy scopes to that install's state bucket only"
  artifacts:
    - path: "infra/modules/email-handler/v1.0.0/variables.tf"
      provides: "New `state_prefix` variable with default 'tf-km'"
      contains: "variable \"state_prefix\""
    - path: "infra/modules/email-handler/v1.0.0/main.tf"
      provides: "S3 IAM policy ARN uses var.state_prefix interpolation"
      contains: "${var.state_prefix}"
    - path: "infra/live/use1/email-handler/terragrunt.hcl"
      provides: "Passes state_prefix derived from KM_RESOURCE_PREFIX"
      contains: "state_prefix"
  key_links:
    - from: "infra/live/use1/email-handler/terragrunt.hcl"
      to: "infra/modules/email-handler/v1.0.0/main.tf"
      via: "inputs block passing state_prefix = \"tf-${get_env(\"KM_RESOURCE_PREFIX\", \"km\")}\""
      pattern: "state_prefix\\s*=.*tf-"
---

<objective>
Fix infrastructure blocker B2 from CONTEXT.md `<decisions>` — `infra/modules/email-handler/v1.0.0/main.tf:75` hardcodes the S3 state-bucket prefix `tf-km/` in an IAM policy resource ARN. A second install's email-handler can't read sandbox metadata because the policy points at the wrong bucket.

Purpose: Add a `state_prefix` Terraform variable (default `"tf-km"` for backward compat) and replace the literal. The live terragrunt computes `state_prefix = "tf-${KM_RESOURCE_PREFIX}"` so each install's handler scopes IAM to its own state bucket.

Output: One new variable, one literal-to-interpolation substitution, one terragrunt `inputs` addition. RESEARCH.md confirms `email-handler` already has `variable "resource_prefix"` declared in `variables.tf` — but the simpler/cleaner approach is the new `state_prefix` variable per CONTEXT.md's locked decision.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-multi-instance-resource-prefix-isolation/82-CONTEXT.md
@.planning/phases/82-multi-instance-resource-prefix-isolation/82-RESEARCH.md

@infra/modules/email-handler/v1.0.0/main.tf
@infra/modules/email-handler/v1.0.0/variables.tf
@infra/live/use1/email-handler/terragrunt.hcl

<interfaces>
<!-- Existing variables in the module (RESEARCH.md). -->

`infra/modules/email-handler/v1.0.0/variables.tf` already declares `variable "resource_prefix"`. This plan ADDS a new `variable "state_prefix"` rather than overloading the existing one — CONTEXT.md's locked decision is "Add `variable "state_prefix"`". Keeps the policy-arn concern (state-bucket name) separate from the resource-naming concern (DDB tables / Lambda names).

<!-- Site-level convention for state bucket. -->

infra/live/site.hcl:5: `tf_state_prefix = "tf-${get_env("KM_RESOURCE_PREFIX", "km")}"`. The new module variable's caller-supplied value should follow this convention exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add state_prefix variable to email-handler + replace tf-km literal + wire terragrunt</name>
  <files>
    infra/modules/email-handler/v1.0.0/variables.tf
    infra/modules/email-handler/v1.0.0/main.tf
    infra/live/use1/email-handler/terragrunt.hcl
  </files>
  <action>
    1. **`infra/modules/email-handler/v1.0.0/variables.tf`** — add:
       ```hcl
       variable "state_prefix" {
         type        = string
         description = "Terragrunt state-bucket prefix (e.g. 'tf-km', 'tf-rg'). Used in IAM policies that read sandbox metadata from S3."
         default     = "tf-km"
       }
       ```
       Sort alphabetically with existing variables if file convention requires.
    2. **`infra/modules/email-handler/v1.0.0/main.tf`** — replace literal at line 75 (and any other occurrences). RESEARCH.md identifies line 75 specifically:
       Before:
       ```hcl
       Resource = "arn:aws:s3:::tf-km/sandboxes/*/metadata.json"
       ```
       (or whatever the exact form is — confirm by reading the file)
       After:
       ```hcl
       Resource = "arn:aws:s3:::${var.state_prefix}/sandboxes/*/metadata.json"
       ```
       Run `grep -n 'tf-km' infra/modules/email-handler/v1.0.0/main.tf` — every match must be replaced. If a bucket-name suffix pattern exists (e.g. `tf-km-state-${region}`), match the substitution to that pattern's shape (use `${var.state_prefix}-state-${var.region_label}` if the literal includes the state suffix).
    3. **`infra/live/use1/email-handler/terragrunt.hcl`** — add to `inputs`:
       ```hcl
       state_prefix = "tf-${get_env("KM_RESOURCE_PREFIX", "km")}"
       ```
    4. **Validate the HCL:** `cd infra/live/use1/email-handler && terragrunt validate` (or `hclvalidate`).
    5. **Predicted plan output:** for the existing `km` install, the evaluated ARN is `arn:aws:s3:::tf-km/sandboxes/*/metadata.json` — IDENTICAL to today. Terraform should show zero changes for the email-handler IAM policy.
  </action>
  <verify>
    <automated>grep -q 'var.state_prefix' /Users/khundeck/working/klankrmkr/infra/modules/email-handler/v1.0.0/main.tf && grep -q 'variable "state_prefix"' /Users/khundeck/working/klankrmkr/infra/modules/email-handler/v1.0.0/variables.tf && grep -q 'state_prefix.*=.*"tf-' /Users/khundeck/working/klankrmkr/infra/live/use1/email-handler/terragrunt.hcl && ! grep -q '"arn:aws:s3:::tf-km/' /Users/khundeck/working/klankrmkr/infra/modules/email-handler/v1.0.0/main.tf && echo OK</automated>
    <manual>
      Verified in Plan 10's Wave 3 dry-run gate per VALIDATION.md row 1.
    </manual>
  </verify>
  <done>
    Grep-based verify passes: `state_prefix` variable declared, `var.state_prefix` interpolation present in main.tf, NO `tf-km` literal remains in the ARN, terragrunt.hcl passes `state_prefix = "tf-..."`.
  </done>
</task>

</tasks>

<verification>
**Per-task automated:** Grep-based literal-substitution check passes (fails closed if any substitution didn't happen).

**Plan-level (deferred to Plan 10):** `km init --dry-run=true` shows zero changes for `module.email_handler.aws_iam_policy.*` (the policy doc string evaluates to the same value).
</verification>

<success_criteria>
- [ ] `state_prefix` variable declared with `default = "tf-km"`
- [ ] No `tf-km` literal remains in the IAM-policy ARN in `main.tf`
- [ ] Live terragrunt passes `state_prefix = "tf-${get_env(\"KM_RESOURCE_PREFIX\", \"km\")}"`
- [ ] Grep-based literal-substitution check passes (no swallowed errors)
</success_criteria>

<output>
After completion, create `.planning/phases/82-multi-instance-resource-prefix-isolation/82-07-SUMMARY.md`
</output>
