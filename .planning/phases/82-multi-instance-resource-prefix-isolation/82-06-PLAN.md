---
phase: 82-multi-instance-resource-prefix-isolation
plan: 06
type: execute
wave: 3
depends_on: ["82-01", "82-02", "82-03", "82-04", "82-05"]
files_modified:
  - infra/modules/ses/v1.0.0/main.tf
  - infra/modules/ses/v1.0.0/variables.tf
  - infra/live/use1/ses/terragrunt.hcl
autonomous: true
requirements: []

must_haves:
  truths:
    - "SES receipt rule set name evaluates to `${var.resource_prefix}-sandbox-email` instead of the literal `km-sandbox-email`"
    - "For the existing `km` install (resource_prefix='km'), the evaluated name is `km-sandbox-email` — IDENTICAL to today (zero Terraform diff, no rule-set recreation)"
    - "For a hypothetical second install (resource_prefix='rg'), the evaluated name is `rg-sandbox-email` — different rule set, no collision"
  artifacts:
    - path: "infra/modules/ses/v1.0.0/variables.tf"
      provides: "New `resource_prefix` variable with default 'km'"
      contains: "variable \"resource_prefix\""
    - path: "infra/modules/ses/v1.0.0/main.tf"
      provides: "rule_set_name uses var.resource_prefix interpolation"
      contains: "${var.resource_prefix}-sandbox-email"
    - path: "infra/live/use1/ses/terragrunt.hcl"
      provides: "Passes resource_prefix from KM_RESOURCE_PREFIX env / site-level config"
      contains: "resource_prefix"
  key_links:
    - from: "infra/live/use1/ses/terragrunt.hcl"
      to: "infra/modules/ses/v1.0.0/main.tf"
      via: "inputs block passing resource_prefix"
      pattern: "resource_prefix\\s*="
---

<objective>
Fix infrastructure blocker B1 from CONTEXT.md `<decisions>` — the SES module currently hardcodes `rule_set_name = "km-sandbox-email"` at `infra/modules/ses/v1.0.0/main.tf:62`. SES allows only one active receipt rule set per account/region, so a second install's `km init` collides or silently fights the first install over rule-set activation.

Purpose: Make the SES rule-set name install-aware via a `resource_prefix` Terraform variable. RESEARCH.md § Q1 confirms `moved {}` blocks do NOT help here (they update Terraform state addresses, not API identities), but for the existing `km` install the evaluated name is identical (`"km-sandbox-email"` → `"km-sandbox-email"`) — Terraform sees no diff. Zero downtime for the existing install.

Output: One new variable in the SES module, one literal-to-interpolation substitution in `main.tf`, one `inputs` block addition in the live terragrunt file.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-multi-instance-resource-prefix-isolation/82-CONTEXT.md
@.planning/phases/82-multi-instance-resource-prefix-isolation/82-RESEARCH.md

@infra/modules/ses/v1.0.0/main.tf
@infra/modules/ses/v1.0.0/variables.tf
@infra/live/use1/ses/terragrunt.hcl
@infra/live/site.hcl

<interfaces>
<!-- Existing terragrunt env var threading. -->

From infra/live/site.hcl (per RESEARCH.md):
- Line 5: `tf_state_prefix = "tf-${get_env("KM_RESOURCE_PREFIX", "km")}"` — confirms `KM_RESOURCE_PREFIX` env var is the canonical source for the install prefix at terragrunt time.

The new variable in the module should accept `resource_prefix` and the live terragrunt should pass `resource_prefix = get_env("KM_RESOURCE_PREFIX", "km")` (matching the site-level pattern).
</interfaces>

<no_moved_block>
**RESEARCH.md § Q1:** Do NOT add a `moved {}` block. Terraform `moved {}` only updates state addresses; the AWS resource ID for `aws_ses_receipt_rule_set` is the rule_set_name itself. For the existing install, the new variable's evaluated value matches the old literal exactly, so no destroy+create occurs. For a hypothetical second install, the new rule set is created from scratch. No state move is needed.
</no_moved_block>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resource_prefix variable to SES module + replace literal + wire terragrunt</name>
  <files>
    infra/modules/ses/v1.0.0/variables.tf
    infra/modules/ses/v1.0.0/main.tf
    infra/live/use1/ses/terragrunt.hcl
  </files>
  <action>
    1. **`infra/modules/ses/v1.0.0/variables.tf`** — add the new variable (appended below existing variables, sorted if the file uses alphabetical order):
       ```hcl
       variable "resource_prefix" {
         type        = string
         description = "Install-level resource prefix (e.g. 'km', 'rg'). Ensures SES receipt rule sets are namespaced per install."
         default     = "km"
       }
       ```
       The `default = "km"` preserves backward compatibility for callers that don't yet pass the variable.
    2. **`infra/modules/ses/v1.0.0/main.tf:62`** — replace the literal:
       ```hcl
       resource "aws_ses_receipt_rule_set" "km_sandbox" {
         rule_set_name = "${var.resource_prefix}-sandbox-email"
       }
       ```
       Search the rest of `main.tf` for any other references to the literal string `"km-sandbox-email"` (e.g., in `aws_ses_active_receipt_rule_set` or related resources) and replace each with `"${var.resource_prefix}-sandbox-email"` consistently. Run: `grep -n '"km-sandbox-email"' infra/modules/ses/v1.0.0/main.tf` — every match must be replaced.
    3. **`infra/live/use1/ses/terragrunt.hcl`** — add to the `inputs = { ... }` block:
       ```hcl
       resource_prefix = get_env("KM_RESOURCE_PREFIX", "km")
       ```
       If `inputs` already exists, append the new line. If not, create the block.
    4. **Sanity-check the module compiles:** `cd infra/live/use1/ses && terragrunt validate` (does NOT call AWS; pure HCL validation). Fix any syntax errors.
    5. **Predicted plan output (for verification in Plan 10):** for the current `km` install, `terragrunt plan` should show ZERO resource changes for `aws_ses_receipt_rule_set.km_sandbox` (because `"${var.resource_prefix}-sandbox-email"` with `resource_prefix="km"` evaluates to the same `"km-sandbox-email"` it was before).
  </action>
  <verify>
    <automated>grep -q 'var.resource_prefix.*sandbox-email\|"\${var.resource_prefix}-sandbox-email"' /Users/khundeck/working/klankrmkr/infra/modules/ses/v1.0.0/main.tf && grep -q 'variable "resource_prefix"' /Users/khundeck/working/klankrmkr/infra/modules/ses/v1.0.0/variables.tf && ! grep -q '"km-sandbox-email"' /Users/khundeck/working/klankrmkr/infra/modules/ses/v1.0.0/main.tf && grep -q 'resource_prefix' /Users/khundeck/working/klankrmkr/infra/live/use1/ses/terragrunt.hcl && echo OK</automated>
    <manual>
      Per VALIDATION.md row 1 (Wave 2 plan shows zero recreations):
      `cd /Users/khundeck/working/klankrmkr && km init --dry-run=true 2>&1 | grep -A2 "aws_ses_receipt_rule_set"` — expect NO `# must be replaced` line; only an in-place attribute change (or no change at all if evaluation matches).
    </manual>
  </verify>
  <done>
    Grep-based verify passes: `resource_prefix` variable declared in variables.tf, `${var.resource_prefix}-sandbox-email` interpolation present in main.tf, NO literal `"km-sandbox-email"` remains in main.tf, terragrunt.hcl passes `resource_prefix`.
  </done>
</task>

</tasks>

<verification>
**Per-task automated:** Grep-based literal-substitution check passes (fails closed if any substitution didn't happen).

**Plan-level (deferred to Plan 10's Wave 3 apply checkpoint):** `km init --dry-run=true` against the existing `km` install shows the SES rule set as unchanged (no recreate, no in-place tag change unless this plan's edits also add tags — they don't for this plan).
</verification>

<success_criteria>
- [ ] `infra/modules/ses/v1.0.0/variables.tf` defines `variable "resource_prefix"` with `default = "km"`
- [ ] `infra/modules/ses/v1.0.0/main.tf` has NO `"km-sandbox-email"` literal — every site uses `"${var.resource_prefix}-sandbox-email"`
- [ ] `infra/live/use1/ses/terragrunt.hcl` passes `resource_prefix = get_env("KM_RESOURCE_PREFIX", "km")` via `inputs`
- [ ] Grep-based literal-substitution check passes (no swallowed errors)
- [ ] No `moved {}` block introduced (RESEARCH.md § Q1)
</success_criteria>

<output>
After completion, create `.planning/phases/82-multi-instance-resource-prefix-isolation/82-06-SUMMARY.md`
</output>
