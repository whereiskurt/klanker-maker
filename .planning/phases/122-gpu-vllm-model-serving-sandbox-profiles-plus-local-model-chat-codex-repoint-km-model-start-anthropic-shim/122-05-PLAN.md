---
phase: 122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim
plan: 05
type: execute
wave: 3
depends_on: ["122-02", "122-03", "122-04"]
files_modified:
  - .planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-UAT.md
  - CLAUDE.md
  - docs/gpu-model-serving.md
autonomous: false
requirements: [REQ-122-UAT, REQ-122-PROFILES, REQ-122-CODEX, REQ-122-MODELSTART, REQ-122-SHIM]
user_setup:
  - service: huggingface
    why: "Llama-3.3-70B is a gated model; the 2 Llama leaves need an HF token to pull weights"
    env_vars:
      - name: HF_TOKEN
        source: "HuggingFace → Settings → Access Tokens (after accepting the Meta Llama-3.3 license)"
    dashboard_config:
      - task: "Accept the Meta Llama-3.3-70B license on the HF model page"
        location: "huggingface.co/meta-llama/Llama-3.3-70B-Instruct"
must_haves:
  truths:
    - "A real g6e.12xlarge GPU sandbox boots from the DLAMI, nvidia-smi sees all 4 GPUs, weights pull to /data, and vllm.service serves the 'local' model"
    - "On-box codex round-trips through LiteLLM :8001 → vLLM :8000 and returns a completion; Slack codex chat-with-resume works; /claude still hits the cloud"
    - "km model start passthrough yields a local codex/curl completion; km model start --anthropic drives local Claude Code chat"
    - "All 7 DoD gates are recorded (PASS/FAIL/skip-with-reason) in 122-UAT.md"
  artifacts:
    - path: ".planning/phases/.../122-UAT.md"
      provides: "operator runbook + per-gate (G1-G7) results"
      contains: "Gate 3"
    - path: "docs/gpu-model-serving.md"
      provides: "operator guide for the GPU serving profiles + km model start + shim"
      contains: "km model start"
  key_links:
    - from: "km create profiles/gpu-qwen-12x.yaml"
      to: "running g6e.12xlarge serving 'local'"
      via: "DLAMI boot → vllm.service → curl localhost:8000/v1/models"
      pattern: "vllm.service"
---

<objective>
Run the FULL LIVE UAT (the phase Definition of Done) across the 7 gates on real
GPU hardware, with an explicit operator runbook covering deploy, secrets, cost
control, and the synthetic-HMAC Slack drive. Record every gate's outcome in
122-UAT.md and write the operator-facing doc + CLAUDE.md phase block.

Purpose: Code-green is not deployable-green (project memory: verify deploy surface,
not just code). GPU boot + vLLM serving + the codex/LiteLLM/vLLM round-trip + the
Slack poller bash + the GUI clients are all invisible to Go goldens and MUST be
live-verified.
Output: 122-UAT.md with 7 gate results; docs/gpu-model-serving.md; CLAUDE.md block.

NOTE: autonomous:false — this plan spends real GPU money (~$10/hr for a g6e.12x)
and has operator-GUI gates (G4, G7). The agent drives the automatable gates and
PAUSES at the human-verify checkpoints.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-RESEARCH.md
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-VALIDATION.md
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy surface — build, lambdas, full apply, SOPS the HF token</name>
  <files>.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-UAT.md</files>
  <action>
    Create 122-UAT.md as the runbook + results log. Execute the deploy sequence and
    record command output for each step:

    1. `make build` — operator binary (carries km model start + the extends-flatten
       path for remote create). Confirm the binary is current.
    2. `make build-lambdas` — rebuild the create-handler zip (the new
       agent.codex.localBaseURL/localModel schema field must be known to the
       Lambda's km binary). CRITICAL (project memory: toolchain_km_must_be_linux):
       the embedded toolchain/km must be linux/arm64 — a macOS binary breaks ALL
       cold-create with "exec format error". Verify the toolchain arch.
    3. `km init --dry-run=false` — full apply to refresh the create-handler
       toolchain (NOT `--sidecars` — the schema field needs the create-handler zip
       updated via a full apply; project memory: schema_change_requires_km_init).
    4. Re-resolve the DLAMI AMI ID (RESEARCH O1 describe-images command) — if it
       drifted from ami-0a9d213b92dabc044, update base/gpu/serve.yaml and re-run
       validate-all.
    5. SOPS the HF token (only needed for the 2 Llama leaves): create
       `secrets/llama-hf.enc.yaml` per RESEARCH O3 using AWS profile
       `klanker-application` + the shared secrets KMS key. Do NOT commit a real token.

    Use AWS profile klanker-application (account 052251888500) for AWS ops.
  </action>
  <verify>
    <automated>file ./km | grep -qi 'Mach-O\|ELF' && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>km binary built; create-handler refreshed via full apply; DLAMI AMI re-resolved; HF token SOPS-encrypted (Llama leaves only); validate-all green. Steps + output logged in 122-UAT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Gates G3, G5, G6 — GPU boot + serving + Slack codex + km model start passthrough</name>
  <files>.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-UAT.md</files>
  <action>
    Bring up ONE g6e.12x first (cheapest GPU leaf) and drive the automatable gates.
    Record each result (PASS/FAIL + evidence) in 122-UAT.md. PROMPT the operator to
    `km destroy` as soon as the gates pass (cost control — ~$10/hr).

    - **G3 (GPU boot + serving):** `km create profiles/gpu-qwen-12x.yaml --alias gpu1`
      (verify BOTH local and remote create flatten extends — project memory:
      remote_create_flattens_extends). Then via SSM:
        `nvidia-smi` (expect 4 L40S),
        `systemctl status vllm` (active),
        `curl -s localhost:8000/v1/models` (lists `local`),
        `systemctl status litellm` (active),
        `curl -s localhost:8001/health` (LiteLLM up).
      Confirm the Slack inbound poller IS emitted (project memory:
      notify_setup_gated_on_spec_cli — base/platform satisfies the gate; verify
      `systemctl is-active km-slack-inbound-poller` + `/etc/km/notify.env` exists).

    - **G5 (Slack codex round-trip + resume):** drive a synthetic-HMAC
      `event_callback` POST to the bridge `/events` (project memory:
      slack_bridge_inbound_e2e_and_status_attr — reuse the helper technique).
      Verify: a reply is posted (codex → LiteLLM → vLLM → Slack), a follow-up
      message RESUMES the thread (codex exec resume), and a `/claude`-prefixed
      message still hits the CLOUD (the A/B). Record the on-box LiteLLM
      `/v1/responses` translation evidence.

    - **G6 (km model start passthrough):** `km model start gpu1` (forwards :8001),
      then from the laptop: `curl http://localhost:8001/v1/responses -d '{"model":"local","input":"ping","stream":false}'`
      → expect a completion. Optionally drive raw `codex` against the forwarded port.

    If a gate fails, capture the diagnostic (vLLM container log, LiteLLM log,
    nvidia-smi, poller journal) into 122-UAT.md before iterating. Per-model
    --max-model-len/--gpu-memory-utilization may need live tuning (O2/O8 are MEDIUM
    confidence) — adjust the leaf's VLLM_EXTRA and re-create if OOM on bring-up.
  </action>
  <verify>
    <automated>echo "live GPU UAT — evidence recorded in 122-UAT.md; gates G3/G5/G6 marked PASS/FAIL with curl + SSM output"</automated>
  </verify>
  <done>G3, G5, G6 recorded PASS (or FAIL with diagnostics + fix) in 122-UAT.md. Sandbox destroyed after gates to stop GPU spend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gates G4 + G7 — operator GUI verification</name>
  <what-built>
    The g6e.12x sandbox (gpu1) is serving the local model on :8000 (vLLM) with the
    LiteLLM dual-gateway on :8001. VS Code Remote-SSH + Continue, and laptop Claude
    Code via the Anthropic shim, both reach the model through the SSM forward.
  </what-built>
  <how-to-verify>
    G4 (VS Code + Continue chat — GUI):
      1. `km vscode start gpu1` → VS Code F1 → Remote-SSH: Connect to Host → km-gpu1.
      2. Install the Continue extension; it reads ~/.continue/config.yaml
         (apiBase http://localhost:8000/v1, model `local`).
      3. Open Continue chat, send a prompt → expect a streamed completion from the
         local 70B.

    G7 (local Claude Code via the Anthropic shim — GUI):
      1. `km model start gpu1 --anthropic` (forwards :8001).
      2. On the laptop: `export ANTHROPIC_BASE_URL=http://localhost:8001` and
         `export ANTHROPIC_AUTH_TOKEN=sk-dummy`, then launch Claude Code.
      3. Chat + a light edit against the local model (scope: chat + light edits
         per R7 — Claude-Code-on-70B is not full-fidelity).

    Record PASS/FAIL for G4 and G7 in 122-UAT.md, then `km destroy gpu1 --remote --yes`
    to stop GPU spend.
  </how-to-verify>
  <resume-signal>Type "approved" (with G4/G7 outcomes) or describe issues to iterate on.</resume-signal>
  <files>.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-UAT.md</files>
  <action>
    Operator GUI gates (cannot be automated — require a human at a VS Code / Claude
    Code client). The agent PAUSES here. The operator follows the how-to-verify
    steps (G4: VS Code Remote-SSH + Continue chat; G7: laptop Claude Code via the
    Anthropic shim), records PASS/FAIL for G4 and G7 in 122-UAT.md, then destroys
    the GPU sandbox to stop spend.
  </action>
  <verify>Operator reports G4 + G7 outcomes; results recorded in 122-UAT.md.</verify>
  <done>G4 and G7 recorded PASS (or FAIL with notes) in 122-UAT.md; gpu1 destroyed.</done>
</task>

<task type="auto">
  <name>Task 4: Docs — gpu-model-serving.md + CLAUDE.md phase block</name>
  <files>docs/gpu-model-serving.md, CLAUDE.md</files>
  <action>
    Write `docs/gpu-model-serving.md`: the 7-profile matrix, the LiteLLM dual-gateway
    architecture (O7 — codex Responses API, Claude Code Anthropic Messages), the
    base/gpu/serve fragment, `km model start [--anthropic]`, the HF_TOKEN SOPS step
    for Llama leaves, the deploy surface (make build + build-lambdas + km init
    --dry-run=false), cost/lifecycle notes (stop teardown, 8h ttl, 1h idle), and a
    troubleshooting table (vLLM container GPU loss on daemon-reload, OOM tuning,
    AMI re-resolution).

    Add a `**Phase 122 (2026-06-27) — ...**` block to CLAUDE.md mirroring the house
    style of recent phase blocks (what shipped, dormancy/back-compat, deploy =
    surface, A/B note, pointers to docs/gpu-model-serving.md), and a row in the
    "Where to look" table.
  </action>
  <verify>
    <automated>test -f docs/gpu-model-serving.md && grep -q "km model start" docs/gpu-model-serving.md && grep -q "Phase 122" CLAUDE.md</automated>
  </verify>
  <done>docs/gpu-model-serving.md exists and covers all 3 deliverables; CLAUDE.md has the Phase 122 block + a Where-to-look row.</done>
</task>

</tasks>

<verification>
- 122-UAT.md records all 7 gates (G1/G2 already automated upstream; G3-G7 live here)
  with PASS/FAIL + evidence; any FAIL has a diagnostic + resolution.
- The g6e.12x sandbox is destroyed after gates (no lingering GPU spend).
- Remote create flattened extends cleanly (no "profile base/gpu/serve not found").
- docs + CLAUDE.md updated.
</verification>

<success_criteria>
- All 7 DoD gates pass on real hardware (or documented, justified deferrals).
- Operator runbook + troubleshooting captured.
- Phase shippable: deploy surface verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-05-SUMMARY.md`
</output>
