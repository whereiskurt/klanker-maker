---
phase: 122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim
plan: 03
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - pkg/compiler/agent_codex.go
  - pkg/compiler/agent_codex_golden_test.go
  - pkg/compiler/testdata
autonomous: true
requirements: [REQ-122-CODEX]
must_haves:
  truths:
    - "synthesizeCodexConfig emits a [model_providers.local] block (wire_api=responses, base_url from LocalBaseURL, env_key=OPENAI_API_KEY) when AgentCodexSpec.LocalBaseURL is set"
    - "A nil/empty Codex spec still returns the inert base hook block unchanged (dormancy — no model_providers block)"
    - "The representative GPU-leaf full-output golden is regenerated via the sanctioned CAPTURE flag; the frozen pre-92 baseline is NOT re-captured"
  artifacts:
    - path: "pkg/compiler/agent_codex.go"
      provides: "[model_providers.local] emission in synthesizeCodexConfig"
      contains: "model_providers.local"
  key_links:
    - from: "pkg/compiler/agent_codex.go"
      to: "AgentCodexSpec.LocalBaseURL"
      via: "guarded emission when c.LocalBaseURL != \"\""
      pattern: "LocalBaseURL"
---

<objective>
Implement the `synthesizeCodexConfig` change that turns Plan 01's RED test green:
emit a `[model_providers.local]` block (Responses API, base_url :8001 = the
Bifrost gateway) when the codex local-provider knob is set, keeping the dormant
path byte-identical. Update the codex golden + the representative GPU-leaf
full-output golden.

Gateway note (CONTEXT § "Gateway = consolidation layer"): :8001 is **Bifrost**
(the multi-provider router from Plan 02), which serves Responses→vLLM. The emitted
base_url + wire_api are UNCHANGED from the original plan (:8001, wire_api="responses").
Document in the code comment that current vLLM also serves `/v1/responses` natively,
so `:8000` is a valid documented fallback if Bifrost is unavailable — but the
profile routes codex through Bifrost :8001 for uniform multi-provider access.

Purpose: Deliverable 2 — repoint the on-box codex agent at the local model so
Slack inbound / km shell / km agent run --codex all hit the local 70B (box-global
repoint = 4 free interfaces); claude stays cloud for the A/B.
Output: green codex local-provider test + updated goldens; full suite green.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-RESEARCH.md
@pkg/compiler/agent_codex.go
@pkg/compiler/agent_codex_golden_test.go

<interfaces>
<!-- Plan 01 added these to AgentCodexSpec; emit from them. -->
```go
c := agent.Codex // *AgentCodexSpec
c.LocalBaseURL // string, e.g. "http://localhost:8001/v1"
c.LocalModel   // string, default "local"
```
<!-- Expected TOML appended after the base hook block (RESEARCH § O6 / Code Examples):
model_provider = "local"
model = "local"

[model_providers.local]
name = "Local vLLM (via Bifrost)"
base_url = "http://localhost:8001/v1"
wire_api = "responses"
env_key = "OPENAI_API_KEY"
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit [model_providers.local] in synthesizeCodexConfig</name>
  <files>pkg/compiler/agent_codex.go</files>
  <behavior>
    - LocalBaseURL set → output contains `model_provider = "local"`, `model = "local"`
      (or LocalModel when given), `[model_providers.local]`, `base_url = "<LocalBaseURL>"`,
      `wire_api = "responses"`, `env_key = "OPENAI_API_KEY"`.
    - LocalModel empty → defaults to "local".
    - LocalBaseURL empty (incl. nil agent / nil Codex) → no model_providers block;
      output is byte-identical to today's base block (dormancy).
    - The emission appends AFTER the existing args/tools comment blocks so ordering is stable.
  </behavior>
  <action>
    In `synthesizeCodexConfig` (pkg/compiler/agent_codex.go:67), after the existing
    `c.Tools` NOTE block and before `return`, add a guarded emission:
      `if c.LocalBaseURL != "" { ... }`
    Emit the TOML from the interfaces block above using `fmt.Fprintf(&b, ...)`,
    defaulting `model := c.LocalModel; if model == "" { model = "local" }`.
    Add a doc-comment paragraph to the function explaining O7 (Codex requires the
    Responses API since Feb 2026; base_url points at the Bifrost gateway :8001, NOT
    vLLM :8000 directly). Note that vLLM now serves /v1/responses natively too, so
    :8000 is a documented fallback — but the profile routes through Bifrost :8001 for
    uniform multi-provider access (LiteLLM is only a fallback gateway). Do NOT hardcode
    the URL in the synthesizer — emit c.LocalBaseURL verbatim (the profile sets :8001).
    Do NOT change the `codexConfigBaseBlock` constant (the byte-identity contract
    for dormant profiles depends on it).
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run 'TestSynthesizeCodexConfig' -count=1 -timeout 120s</automated>
  </verify>
  <done>Plan 01's `TestSynthesizeCodexConfig_LocalProvider` is GREEN; the existing `TestSynthesizeCodexConfigGolden` (dormant path) still passes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update codex golden + representative GPU-leaf full-output golden</name>
  <files>pkg/compiler/agent_codex_golden_test.go, pkg/compiler/testdata</files>
  <action>
    1. If `agent_codex_golden_test.go` pins the dormant config and a new
       local-provider variant is wanted as a golden, add a second golden fixture
       (e.g. testdata/codex_config_local.golden.toml) and a golden case for the
       LocalBaseURL profile. The DORMANT golden must stay byte-identical — only ADD.
    2. Regenerate the full-output userdata golden for ONE representative GPU leaf
       (gpu-qwen-12x) using the SANCTIONED CAPTURE flag mechanism this package
       already uses (grep for `CAPTURE_` env-gated golden capture in
       pkg/compiler/*_test.go to find the exact flag name + invocation). Run it
       once to write the fixture, then run without the flag to confirm green.
    CRITICAL (project memory: frozen_byte_identity_golden_capture_trap):
       Do NOT touch `userdata_learn_v2_pre92_baseline.golden.sh`. That frozen
       baseline is hand-patched only; re-capturing folds the SubagentStop script
       into it and corrupts the byte-identity test. The GPU-leaf golden is a
       NEW full-output fixture, captured via the sanctioned flag — never the frozen one.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run 'TestUserdata|TestSynthesizeCodexConfig|ByteIdentity' -count=1 -timeout 300s</automated>
  </verify>
  <done>New/updated goldens pass; the frozen pre-92 byte-identity test is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression sweep</name>
  <files>pkg/compiler/agent_codex.go</files>
  <action>
    Run the full Go suite to confirm the codex change + goldens introduced no
    regression in the userdata pipeline (the configFiles block embeds the codex
    TOML). Capture the command's OWN exit code (project memory:
    feedback_check_go_test_exit_not_pipe — `go test | tail` masks a FAIL). If any
    pre-existing-unrelated red surfaces, note it in the SUMMARY but do not expand scope.
  </action>
  <verify>
    <automated>go test ./... -count=1 -timeout 600s</automated>
  </verify>
  <done>Full suite green (or only documented pre-existing reds unrelated to Phase 122).</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/ -count=1 -timeout 300s` green.
- Dormant codex path byte-identical (codexConfigBaseBlock untouched; existing golden unchanged).
- Frozen pre-92 baseline NOT re-captured.
</verification>

<success_criteria>
- synthesizeCodexConfig emits the local-provider block only when the knob is set.
- Plan 01's RED codex test is green; goldens updated via sanctioned flag.
- Full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-03-SUMMARY.md`
</output>
