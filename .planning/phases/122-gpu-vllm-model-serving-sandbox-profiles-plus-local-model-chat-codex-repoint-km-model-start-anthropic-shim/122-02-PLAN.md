---
phase: 122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - profiles/base/gpu/serve.yaml
  - profiles/gpu-qwen-12x.yaml
  - profiles/gpu-llama-12x.yaml
  - profiles/gpu-qwen-48x.yaml
  - profiles/gpu-llama-48x.yaml
  - profiles/gpu-glmair-12x.yaml
  - profiles/gpu-kimidev-12x.yaml
  - profiles/gpu-glm46-48x.yaml
  - scripts/validate-all-profiles.sh
autonomous: true
requirements: [REQ-122-PROFILES]
must_haves:
  truths:
    - "An abstract base/gpu/serve fragment holds the ~90% shared GPU-serving config (AMI, additionalVolume, vLLM + LiteLLM systemd units, Continue config, codex local-provider knob, raised budget, lifecycle)"
    - "7 GPU leaf profiles each set only instanceType + /etc/km/vllm.env + (48x) budget bump + (llama) HF_TOKEN, and km validate is green on all 7 merged"
    - "scripts/validate-all-profiles.sh validates all 7 GPU leaves and exits 0"
  artifacts:
    - path: "profiles/base/gpu/serve.yaml"
      provides: "abstract GPU serving fragment (metadata.abstract:true)"
      contains: "model_providers"
    - path: "profiles/gpu-qwen-12x.yaml"
      provides: "ungated AWQ Qwen 12x leaf"
      contains: "g6e.12xlarge"
    - path: "profiles/gpu-llama-12x.yaml"
      provides: "HF-gated Llama 12x leaf with SOPS HF_TOKEN injection"
      contains: "sopsFile"
    - path: "scripts/validate-all-profiles.sh"
      provides: "inventory gate covering the 7 GPU leaves"
      contains: "gpu-qwen-12x"
  key_links:
    - from: "profiles/gpu-qwen-12x.yaml"
      to: "profiles/base/gpu/serve.yaml"
      via: "extends list"
      pattern: "base/gpu/serve"
    - from: "profiles/base/gpu/serve.yaml"
      to: "/etc/km/litellm-config.yaml"
      via: "configFiles writes the LiteLLM dual-gateway config"
      pattern: "litellm-config"
---

<objective>
Author the abstract `profiles/base/gpu/serve.yaml` fragment and the 7 GPU leaf
profiles, then extend `scripts/validate-all-profiles.sh` so all 7 leaves are
`km validate`-green on their MERGED bytes.

Purpose: Deliverable 1 of the phase — GPU EC2 sandboxes that serve 70B-class
local models via vLLM, fronted by a core on-box LiteLLM (:8001) gateway, composed
via Phase 117 inheritance.
Output: 1 fragment + 7 leaves + updated validate-all gate, all green.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-RESEARCH.md
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-CONTEXT.md
@profiles/learner.yaml
@profiles/base/platform.yaml
@profiles/base/os/debian.yaml
@profiles/base/slack-persandbox.yaml
@scripts/validate-all-profiles.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the abstract base/gpu/serve.yaml fragment</name>
  <files>profiles/base/gpu/serve.yaml</files>
  <action>
    Create `profiles/base/gpu/serve.yaml` with `metadata.abstract: true` and
    `metadata.name: base-gpu-serve`. Use the RESEARCH § "base/gpu/serve.yaml
    pattern" (lines ~512-615) VERBATIM as the spec content, with these MUST-honor
    rules:

    - **Bool zero-value trap (Phase 117 / Pitfall 2):** DO NOT set `spot`,
      `hibernation`, or `mountEFS` in this fragment. RuntimeSpec uses plain bools;
      setting `spot: false` here would force-false every leaf. Those go in leaves.
      So `spec.runtime` here carries ONLY: `ami: ami-0a9d213b92dabc044`,
      `substrate: ec2`, and `additionalVolume: {size: 300, mountPoint: /data}`.
    - `spec.lifecycle`: ttl 8h, idleTimeout 1h, teardownPolicy stop.
    - `spec.budget.compute.maxSpendUSD: 120` (12x floor; 48x leaves override to 300).
      `spec.budget.ai.maxSpendUSD: 2.00`.
    - `spec.execution.env`: `HF_HOME: /data/hf`, `OPENAI_API_KEY: ""` (dummy).
    - `spec.execution.initCommandsAppend` (these UNION onto the leaf's, per Phase 117):
        `pip3 install 'litellm[proxy]' --quiet` (pin a version if O-Q1 risk —
        prefer pinning to a known-good LiteLLM, e.g. `'litellm[proxy]==1.x.y'`;
        document the exact pin you chose in the SUMMARY),
        `systemctl daemon-reload`,
        `systemctl enable --now vllm.service`,
        `systemctl enable --now litellm.service`.
    - `spec.execution.configFiles` (map merges cleanly with leaf keys):
        `/etc/systemd/system/vllm.service` (the docker-run unit, `--ipc=host`,
          `ExecStartPre=/bin/sleep 5`, `EnvironmentFile=-/etc/km/vllm.env`,
          `-p 127.0.0.1:8000:8000`, `-v /data/hf:/root/.cache/huggingface`,
          `--served-model-name local`, expands `${VLLM_MODEL}/${VLLM_TP}/${VLLM_EXTRA}`),
        `/etc/systemd/system/litellm.service` (`ExecStartPre=/bin/sleep 10`,
          `After=vllm.service`, runs litellm on `--port 8001 --host 127.0.0.1`),
        `/etc/km/litellm-config.yaml` (the dual-gateway model_list: `claude-*` and
          `local` both → `openai/local` api_base `http://localhost:8000/v1`,
          `use_chat_completions_api: true`; `general_settings.disable_key_check: true`),
        `/home/sandbox/.continue/config.yaml` (provider openai, model local,
          apiBase `http://localhost:8000/v1`).
      NOTE: per Pitfall 5, do NOT write `/etc/km/vllm.env` here — the leaves write
      it in initCommandsAppend (ordering: initCommands runs before configFiles).
    - `spec.agent`: `default: codex`, and `codex: {localBaseURL: "http://localhost:8001/v1", localModel: "local"}`
      (the Plan 01 knob — points at LiteLLM :8001, NOT vLLM :8000, per O7).

    Validate the fragment is SKIPPED standalone (abstract) but contributes when merged.
  </action>
  <verify>
    <automated>make build && ./km validate profiles/base/gpu/serve.yaml 2>&1 | grep -qi 'skip\|abstract'</automated>
  </verify>
  <done>Fragment exists with metadata.abstract:true; `km validate` prints a SKIP/abstract line (not validated standalone); no `spot`/`hibernation`/`mountEFS` bool fields present.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 7 GPU leaf profiles</name>
  <files>profiles/gpu-qwen-12x.yaml, profiles/gpu-llama-12x.yaml, profiles/gpu-qwen-48x.yaml, profiles/gpu-llama-48x.yaml, profiles/gpu-glmair-12x.yaml, profiles/gpu-kimidev-12x.yaml, profiles/gpu-glm46-48x.yaml</files>
  <action>
    Create all 7 leaves. Every leaf:
      `extends: [base/os/debian, base/network/safenetwork, base/userinit, base/platform, base/slack-persandbox, base/gpu/serve]`
      `spec.runtime.instanceType: <g6e.12xlarge | g6e.48xlarge>`, `region: us-east-1`,
        and (per Pitfall 2) set the mixed-bool runtime block in the LEAF:
        `spot: false`, `hibernation: false` (GPU spot unreliable).
      `spec.execution.initCommandsAppend` writes `/etc/km/vllm.env` via a single
        `printf ... > /etc/km/vllm.env` line (mkdir -p /etc/km first), carrying
        `VLLM_MODEL=`, `VLLM_TP=`, and `VLLM_EXTRA=` per the O2/O7/O8 table.

    Per-leaf VLLM_EXTRA (from RESEARCH O2 + O7 tool-call flags + O8):
      - gpu-qwen-12x: g6e.12xlarge, Qwen/Qwen2.5-72B-Instruct-AWQ, TP=4,
        `--quantization awq --max-model-len 32768 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`
      - gpu-llama-12x: g6e.12xlarge, meta-llama/Llama-3.3-70B-Instruct, TP=4,
        `--quantization fp8 --max-model-len 16384 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser llama3_json`  [GATED — see Task 3]
      - gpu-qwen-48x: g6e.48xlarge, Qwen/Qwen2.5-72B-Instruct, TP=8,
        `--max-model-len 65536 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`; budget bump to 300.
      - gpu-llama-48x: g6e.48xlarge, meta-llama/Llama-3.3-70B-Instruct, TP=8,
        `--max-model-len 65536 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser llama3_json`; budget bump to 300. [GATED — Task 3]
      - gpu-glmair-12x: g6e.12xlarge, cyankiwi/GLM-4.5-Air-AWQ-4bit, TP=4,
        `--quantization awq --max-model-len 32768 --gpu-memory-utilization 0.90 --trust-remote-code --enable-auto-tool-choice --tool-call-parser glm45 --reasoning-parser glm45`
      - gpu-kimidev-12x: g6e.12xlarge, btbtyler09/Kimi-Dev-72B-GPTQ-8bit, TP=4,
        `--quantization gptq --max-model-len 16384 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`
      - gpu-glm46-48x: g6e.48xlarge, QuantTrio/GLM-4.6-AWQ, TP=8,
        `--quantization awq --max-model-len 65536 --gpu-memory-utilization 0.90 --trust-remote-code --enable-auto-tool-choice --tool-call-parser glm45 --reasoning-parser glm45`; budget bump to 300.

    For 48x leaves, override `spec.budget.compute.maxSpendUSD: 300`.
    Keep each leaf ~15-25 lines. metadata.name = the profile basename.
  </action>
  <verify>
    <automated>for p in qwen-12x qwen-48x glmair-12x kimidev-12x glm46-48x; do ./km validate profiles/gpu-$p.yaml || exit 1; done</automated>
  </verify>
  <done>All 5 ungated leaves `km validate` green on merged bytes. Llama leaves authored too (validated in Task 3 once secrets stanza is present).</done>
</task>

<task type="auto">
  <name>Task 3: Wire HF_TOKEN SOPS injection into the 2 Llama leaves + update validate-all gate</name>
  <files>profiles/gpu-llama-12x.yaml, profiles/gpu-llama-48x.yaml, scripts/validate-all-profiles.sh</files>
  <action>
    For BOTH Llama leaves, add (per RESEARCH O3):
      `spec.secrets.sopsFile: ./secrets/llama-hf.enc.yaml`
      `spec.iam.allowedSecretPaths: [/sandbox/*/secrets]`
      and a SECOND initCommandsAppend line AFTER the vllm.env write:
        `grep '^HF_TOKEN=' /etc/sandbox-secrets.env >> /etc/km/vllm.env 2>/dev/null || true`
    NOTE: the actual SOPS-encrypted `secrets/llama-hf.enc.yaml` is created by the
    operator at UAT time (Plan 05) with the klanker-application profile — do NOT
    commit a real token. `km validate` runs OFFLINE (no KMS) per Phase 89, so it
    validates the sopsFile reference shape without decrypting. If `km validate`
    requires the file to exist, create a placeholder encrypted stub OR confirm the
    validator only checks the `.enc.yaml` suffix + metadata (validate.go) — document
    which in the SUMMARY.

    Then update `scripts/validate-all-profiles.sh`: add all 7 GPU leaves to the
    `PROFILES=(...)` inventory array. Bump the header comment's inventory count.
    The base/gpu/ subdir fragment is auto-skipped by the existing
    `find profiles/base -name '*.yaml'` loop — confirm it prints a skip line.
  </action>
  <verify>
    <automated>make build && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>validate-all-profiles.sh exits 0 with all 7 gpu-*.yaml leaves showing `ok` and profiles/base/gpu/serve.yaml showing a `skip` line. Both Llama leaves declare sopsFile + allowedSecretPaths + the HF_TOKEN copy line.</done>
</task>

</tasks>

<verification>
- `bash scripts/validate-all-profiles.sh` exits 0 (all 13 pre-existing + 7 GPU leaves green).
- `km validate` on the fragment prints SKIP (abstract), never validated standalone.
- Merged-bytes validation only — never assert on leaf-alone bytes (Phase 117 rule).
- Confirm no `spot`/`hibernation` bool fields leaked into base/gpu/serve.yaml.
</verification>

<success_criteria>
- 1 abstract fragment + 7 concrete leaves authored.
- All 7 leaves validate green (merged); ungated leaves need no secrets; the 2 Llama leaves carry the SOPS + HF_TOKEN plumbing.
- validate-all gate updated and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-02-SUMMARY.md`
</output>
