---
phase: 122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - profiles/base/gpu/serve.yaml
  - profiles/gpu-qwen-12x.yaml
  - profiles/gpu-llama-12x.yaml
  - profiles/gpu-qwen-48x.yaml
  - profiles/gpu-llama-48x.yaml
  - profiles/gpu-glmair-12x.yaml
  - profiles/gpu-kimidev-12x.yaml
  - profiles/gpu-glm46-48x.yaml
  - scripts/validate-all-profiles.sh
autonomous: true
requirements: [REQ-122-PROFILES, REQ-122-SHIM]
must_haves:
  truths:
    - "An abstract base/gpu/serve fragment holds the ~90% shared GPU-serving config (AMI, additionalVolume, vLLM systemd unit, Bifrost gateway install+unit+route-config, Continue config, codex local-provider knob, OTEL export, raised budget, lifecycle)"
    - "The Bifrost gateway (:8001) registers 5 named routes: local (vLLM), claude-bedrock (role), claude-anthropic (key), gpt-oss-bedrock (role), gpt-frontier (dormant)"
    - "7 GPU leaf profiles each set only instanceType + /etc/km/vllm.env + (48x) budget bump + (llama) HF_TOKEN, and km validate is green on all 7 merged"
    - "scripts/validate-all-profiles.sh validates all 7 GPU leaves and exits 0"
  artifacts:
    - path: "profiles/base/gpu/serve.yaml"
      provides: "abstract GPU serving fragment (metadata.abstract:true) with Bifrost router"
      contains: "bifrost"
    - path: "profiles/gpu-qwen-12x.yaml"
      provides: "ungated AWQ Qwen 12x leaf"
      contains: "g6e.12xlarge"
    - path: "profiles/gpu-llama-12x.yaml"
      provides: "HF-gated Llama 12x leaf with SOPS HF_TOKEN injection"
      contains: "sopsFile"
    - path: "scripts/validate-all-profiles.sh"
      provides: "inventory gate covering the 7 GPU leaves"
      contains: "gpu-qwen-12x"
  key_links:
    - from: "profiles/gpu-qwen-12x.yaml"
      to: "profiles/base/gpu/serve.yaml"
      via: "extends list"
      pattern: "base/gpu/serve"
    - from: "profiles/base/gpu/serve.yaml"
      to: "/etc/km/bifrost-config.json"
      via: "configFiles writes the Bifrost multi-provider route map"
      pattern: "bifrost-config"
    - from: "profiles/base/gpu/serve.yaml"
      to: "http://localhost:4318"
      via: "Bifrost OTLP export → km tracing sidecar"
      pattern: "4318"
---

<objective>
Author the abstract `profiles/base/gpu/serve.yaml` fragment and the 7 GPU leaf
profiles, then extend `scripts/validate-all-profiles.sh` so all 7 leaves are
`km validate`-green on their MERGED bytes.

The on-box gateway is **Bifrost** (`:8001`) — a Go single-binary multi-provider
model router (CONTEXT § "Gateway = consolidation layer", locked 2026-06-27). It
is NOT a single-model LiteLLM shim. Clients pick local-vs-cloud BY MODEL NAME
through one endpoint; almost every route is keyless via the sandbox IAM role.

Purpose: Deliverable 1 — GPU EC2 sandboxes that serve a local 70B via vLLM,
fronted by the Bifrost router that also exposes Bedrock Claude / Bedrock gpt-oss /
Anthropic (keyed) under named routes, with per-model telemetry flowing into km's
OTEL pipeline.
Output: 1 fragment + 7 leaves + updated validate-all gate, all green.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-RESEARCH.md
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-CONTEXT.md
@profiles/learner.yaml
@profiles/base/platform.yaml
@profiles/base/os/debian.yaml
@profiles/base/slack-persandbox.yaml
@profiles/base/userinit.yaml
@scripts/validate-all-profiles.sh

<gateway_routes>
<!-- CONTEXT § Gateway = consolidation layer. Bifrost :8001 route map. -->
| Route name        | Backend                                  | Auth                          | Status              |
|-------------------|------------------------------------------|-------------------------------|---------------------|
| local             | vLLM localhost:8000 (chat-completions)   | none                          | active (always)     |
| claude-bedrock    | AWS Bedrock (Claude Sonnet/Opus)         | sandbox IAM role / SigV4 — NO key | active          |
| claude-anthropic  | api.anthropic.com                        | ANTHROPIC_API_KEY (SOPS @ UAT) | active/wired       |
| gpt-oss-bedrock   | Bedrock OpenAI gpt-oss (openai.gpt-oss-120b-1:0 / -20b) | sandbox IAM role — NO key | active   |
| gpt-frontier (opt)| api.openai.com (GPT-5/o-series)          | OPENAI_API_KEY (SOPS)         | dormant unless key present |
<!-- Cloud routes egress through km's MITM proxy → already metered into BUDGET#ai
     rows. NO new metering code. Claude Code reaches the gateway via
     ANTHROPIC_BASE_URL → Bifrost /anthropic path. -->
</gateway_routes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author base/gpu/serve.yaml — vLLM unit + Bifrost router install/unit/config + OTEL</name>
  <files>profiles/base/gpu/serve.yaml</files>
  <action>
    Create `profiles/base/gpu/serve.yaml` with `metadata.abstract: true` and
    `metadata.name: base-gpu-serve`. Build on RESEARCH § "base/gpu/serve.yaml
    pattern" for the vLLM unit, but REPLACE the LiteLLM gateway with **Bifrost**
    per CONTEXT § "Gateway = consolidation layer". MUST-honor rules:

    BOOL ZERO-VALUE TRAP (Phase 117 / Pitfall 2): DO NOT set `spot`/`hibernation`/
    `mountEFS` in this fragment. `spec.runtime` here carries ONLY:
      `ami: ami-0a9d213b92dabc044`, `substrate: ec2`,
      `additionalVolume: {size: 300, mountPoint: /data}`.

    `spec.lifecycle`: ttl 8h, idleTimeout 1h, teardownPolicy stop.
    `spec.budget.compute.maxSpendUSD: 120` (12x floor; 48x leaves override 300),
      `ai.maxSpendUSD: 2.00`.
    `spec.execution.env`: `HF_HOME: /data/hf`, `OPENAI_API_KEY: ""` (dummy).
      (base/userinit already exports OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
      — confirm by reading base/userinit.yaml; do NOT duplicate if present.)

    GATEWAY = BIFROST (Go single-binary, ~40MB — install via initCommandsAppend
    like a km sidecar, NOT a Python venv; LiteLLM is the documented FALLBACK only —
    note that in a comment):
    `spec.execution.initCommandsAppend` (these UNION onto leaf's, per Phase 117):
      - download a PINNED Bifrost release binary from its GitHub releases (linux
        amd64) to `/usr/local/bin/bifrost`, chmod +x. Pin an exact version tag and
        record it in the SUMMARY (avoids the "latest moved" drift class). If the
        release asset name/URL is uncertain at execution time, resolve it from the
        Bifrost GitHub releases page and document the exact URL used.
      - `systemctl daemon-reload`
      - `systemctl enable --now vllm.service`
      - `systemctl enable --now bifrost.service`

    `spec.execution.configFiles` (map — leaf keys merge cleanly):
      "/etc/systemd/system/vllm.service": the docker-run unit (`--ipc=host`,
        `ExecStartPre=/bin/sleep 5`, `EnvironmentFile=-/etc/km/vllm.env`,
        `-p 127.0.0.1:8000:8000`, `-v /data/hf:/root/.cache/huggingface`,
        `--served-model-name local`, expands `${VLLM_MODEL}/${VLLM_TP}/${VLLM_EXTRA}`).
        (Per Pitfall 5, do NOT write /etc/km/vllm.env here — leaves write it.)
      "/etc/systemd/system/bifrost.service": runs
        `/usr/local/bin/bifrost ... --config /etc/km/bifrost-config.json` bound to
        `127.0.0.1:8001`, `After=vllm.service network.target`, `Restart=on-failure`,
        `ExecStartPre=/bin/sleep 10`. Use the actual Bifrost CLI flags (resolve from
        Bifrost docs at execution time; the systemd unit just needs config-path +
        host/port + the OTLP export env). Set `Environment=` lines so Bifrost's
        telemetry/OTLP export targets `http://localhost:4318` (km tracing sidecar).
      "/etc/km/bifrost-config.json": the multi-provider ROUTE MAP registering the 5
        named routes from <gateway_routes> above:
          - `local` → OpenAI-compatible provider, base_url http://localhost:8000/v1,
            keyless (dummy key), chat-completions.
          - `claude-bedrock` → Bedrock provider, region us-east-1, Claude model IDs
            (e.g. anthropic.claude-3-5-sonnet / opus on Bedrock), **NO api key — uses
            the instance role SigV4** (Bifrost must be configured to use the ambient
            AWS credentials chain / instance role, not a static key).
          - `claude-anthropic` → Anthropic provider, api.anthropic.com, api key from
            env `ANTHROPIC_API_KEY` (active/wired — operator SOPS-provisions the key
            at UAT; wire it ASSUMING the key will be present, do not gate dormant).
          - `gpt-oss-bedrock` → Bedrock provider, OpenAI gpt-oss model IDs
            (`openai.gpt-oss-120b-1:0`, `openai.gpt-oss-20b-1:0`), **NO key — instance
            role**.
          - `gpt-frontier` → OpenAI provider, api.openai.com, key from env
            `OPENAI_API_KEY` — DORMANT (route present but only functional when the key
            is provisioned; do NOT fail config-load when absent).
        Also configure Bifrost's `/anthropic` ingress (Anthropic Messages API
        translation surface) so Claude Code's `ANTHROPIC_BASE_URL` → `…/anthropic`
        works (Plan 04 + UAT). Resolve the exact Bifrost config JSON schema from
        Bifrost docs at execution time and map the route table onto it; document the
        chosen schema shape in the SUMMARY.
      "/home/sandbox/.continue/config.yaml": provider openai, model local,
        apiBase http://localhost:8000/v1, apiKey dummy.

    `spec.iam.allowedSecretPaths`: add `/sandbox/*/secrets` here in the fragment so
      the claude-anthropic route's ANTHROPIC_API_KEY (and llama HF_TOKEN, llama
      leaves) can be SOPS-injected. (If putting it in the fragment conflicts with the
      Phase 117 list-union semantics for leaves, keep it here AND in llama leaves —
      union dedupes. Document the choice.)

    `spec.agent`: `default: codex`, and `codex: {localBaseURL: "http://localhost:8001/v1", localModel: "local"}`
      (Plan 01 knob — points at Bifrost :8001, not vLLM :8000; Bifrost serves
      Responses→vLLM. See Plan 03 note: vLLM also serves /v1/responses natively as a
      documented :8000 fallback).

    IAM NOTE (flag in SUMMARY, verify in Plan 05 UAT): the sandbox instance role must
      grant `bedrock:InvokeModel` (+ `InvokeModelWithResponseStream`) for the Claude
      model IDs AND the `openai.gpt-oss-*` model IDs for the keyless Bedrock routes to
      work. If the existing ec2spot/sandbox role allowlist does not already cover
      gpt-oss model IDs, record this as a UAT/IAM gap (do NOT silently assume).
  </action>
  <verify>
    <automated>make build && ./km validate profiles/base/gpu/serve.yaml 2>&1 | grep -qi 'skip\|abstract'</automated>
  </verify>
  <done>Fragment exists, metadata.abstract:true (km validate SKIPs standalone); no spot/hibernation/mountEFS bool fields; Bifrost install + unit + 5-route config + OTLP→:4318 export present; codex knob points at :8001.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 7 GPU leaf profiles</name>
  <files>profiles/gpu-qwen-12x.yaml, profiles/gpu-llama-12x.yaml, profiles/gpu-qwen-48x.yaml, profiles/gpu-llama-48x.yaml, profiles/gpu-glmair-12x.yaml, profiles/gpu-kimidev-12x.yaml, profiles/gpu-glm46-48x.yaml</files>
  <action>
    Create all 7 leaves. Every leaf:
      `extends: [base/os/debian, base/network/safenetwork, base/userinit, base/platform, base/slack-persandbox, base/gpu/serve]`
      `spec.runtime.instanceType: <g6e.12xlarge | g6e.48xlarge>`, `region: us-east-1`,
        and (per Pitfall 2) set the mixed-bool runtime block in the LEAF:
        `spot: false`, `hibernation: false`.
      `spec.execution.initCommandsAppend` writes `/etc/km/vllm.env` via one
        `mkdir -p /etc/km && printf 'VLLM_MODEL=...\nVLLM_TP=...\nVLLM_EXTRA=...\n' > /etc/km/vllm.env`.

    Per-leaf VLLM_EXTRA (RESEARCH O2 + O7 tool-call flags + O8) — unchanged from the
    original plan; the gateway change does not affect the vLLM serving flags:
      - gpu-qwen-12x: g6e.12xlarge, Qwen/Qwen2.5-72B-Instruct-AWQ, TP=4,
        `--quantization awq --max-model-len 32768 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`
      - gpu-llama-12x: g6e.12xlarge, meta-llama/Llama-3.3-70B-Instruct, TP=4,
        `--quantization fp8 --max-model-len 16384 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser llama3_json`  [GATED — Task 3]
      - gpu-qwen-48x: g6e.48xlarge, Qwen/Qwen2.5-72B-Instruct, TP=8,
        `--max-model-len 65536 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`; budget bump 300.
      - gpu-llama-48x: g6e.48xlarge, meta-llama/Llama-3.3-70B-Instruct, TP=8,
        `--max-model-len 65536 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser llama3_json`; budget bump 300. [GATED — Task 3]
      - gpu-glmair-12x: g6e.12xlarge, cyankiwi/GLM-4.5-Air-AWQ-4bit, TP=4,
        `--quantization awq --max-model-len 32768 --gpu-memory-utilization 0.90 --trust-remote-code --enable-auto-tool-choice --tool-call-parser glm45 --reasoning-parser glm45`
      - gpu-kimidev-12x: g6e.12xlarge, btbtyler09/Kimi-Dev-72B-GPTQ-8bit, TP=4,
        `--quantization gptq --max-model-len 16384 --gpu-memory-utilization 0.90 --enable-auto-tool-choice --tool-call-parser hermes`
      - gpu-glm46-48x: g6e.48xlarge, QuantTrio/GLM-4.6-AWQ, TP=8,
        `--quantization awq --max-model-len 65536 --gpu-memory-utilization 0.90 --trust-remote-code --enable-auto-tool-choice --tool-call-parser glm45 --reasoning-parser glm45`; budget bump 300.

    48x leaves override `spec.budget.compute.maxSpendUSD: 300`. ~15-25 lines each;
    metadata.name = profile basename.
  </action>
  <verify>
    <automated>for p in qwen-12x qwen-48x glmair-12x kimidev-12x glm46-48x; do ./km validate profiles/gpu-$p.yaml || exit 1; done</automated>
  </verify>
  <done>All 5 ungated leaves km validate green on merged bytes; both Llama leaves authored (validated in Task 3 once secrets stanza is present).</done>
</task>

<task type="auto">
  <name>Task 3: HF_TOKEN SOPS injection (Llama leaves) + validate-all gate</name>
  <files>profiles/gpu-llama-12x.yaml, profiles/gpu-llama-48x.yaml, scripts/validate-all-profiles.sh</files>
  <action>
    For BOTH Llama leaves (RESEARCH O3):
      `spec.secrets.sopsFile: ./secrets/llama-hf.enc.yaml`
      `spec.iam.allowedSecretPaths: [/sandbox/*/secrets]` (unions with the
        fragment's — dedupes),
      and a SECOND initCommandsAppend line AFTER the vllm.env write:
        `grep '^HF_TOKEN=' /etc/sandbox-secrets.env >> /etc/km/vllm.env 2>/dev/null || true`
    NOTE: the real `secrets/llama-hf.enc.yaml` is created by the operator at UAT
    (Plan 05) with the klanker-application profile. km validate runs OFFLINE — it
    validates the sopsFile reference shape without decrypting. Confirm whether the
    validator needs the file to exist (validate.go) — if so, create a placeholder
    encrypted stub or document the suffix/metadata-only check in the SUMMARY.

    Then update `scripts/validate-all-profiles.sh`: add all 7 GPU leaves to
    `PROFILES=(...)`; bump the inventory-count header comment. The base/gpu/ subdir
    fragment is auto-skipped by the existing `find profiles/base -name '*.yaml'` loop.
  </action>
  <verify>
    <automated>make build && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>validate-all-profiles.sh exits 0 with all 7 gpu-*.yaml leaves `ok` and profiles/base/gpu/serve.yaml `skip`; both Llama leaves declare sopsFile + allowedSecretPaths + HF_TOKEN copy.</done>
</task>

</tasks>

<verification>
- `bash scripts/validate-all-profiles.sh` exits 0 (all pre-existing + 7 GPU leaves green).
- km validate on the fragment prints SKIP (abstract); merged-bytes validation only.
- No spot/hibernation bool leaked into base/gpu/serve.yaml.
- Bifrost (not LiteLLM) is the gateway; the 5 routes + OTLP→:4318 are in the config.
</verification>

<success_criteria>
- 1 abstract fragment (Bifrost router + 5 routes + OTEL) + 7 concrete leaves authored.
- All 7 leaves validate green (merged); the 2 Llama leaves carry SOPS + HF_TOKEN plumbing.
- validate-all gate updated and passing.
- Bedrock-route IAM dependency (Claude + gpt-oss InvokeModel) flagged for Plan 05 UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-02-SUMMARY.md`
</output>
