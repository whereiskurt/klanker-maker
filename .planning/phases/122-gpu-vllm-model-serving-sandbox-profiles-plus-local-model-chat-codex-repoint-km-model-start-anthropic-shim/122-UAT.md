---
phase: 122
plan: 05
status: blocked-on-quota
created: 2026-06-27
---

# Phase 122 — Live UAT log

## Gate results

| Gate | What | Status |
|------|------|--------|
| G1 | `km validate` green on all 7 GPU profiles (merged bytes) | ✅ PASS (20/20 via validate-all-profiles.sh) |
| G2 | `synthesizeCodexConfig` → `[model_providers.local]` (:8001/responses) | ✅ PASS (unit + golden) |
| G2b–G2d | codex schema round-trip, DLAMI raw-AMI passthrough, `km model start` wiring | ✅ PASS (Wave 0/2 tests; full `go test ./...` = 41 ok / 0 FAIL) |
| G3 | GPU boot + `nvidia-smi` + vLLM serves `local` | ⛔ BLOCKED — G-instance quota = 0 |
| G4 | VS Code Remote-SSH + Continue (GUI) | ⛔ pending G3 |
| G5 | Slack codex round-trip + resume (synthetic-HMAC) | ⛔ pending G3 |
| G6 | `km model start` passthrough | ⛔ pending G3 |
| G7 | laptop Claude Code via Bifrost `/anthropic` (GUI) | ⛔ pending G3 |
| G8 | Bifrost routing: local / claude-bedrock / gpt-oss-bedrock / claude-anthropic | ⛔ pending G3 |
| G9 | Bifrost OTEL → :4318 → `km otel` | ⛔ pending G3 |

## Blocker — AWS G-instance quota

`km create --local profiles/gpu-qwen-12x.yaml --alias gpu1` (2026-06-27 19:23Z) failed at
`RunInstances` with **`VcpuLimitExceeded: current vCPU limit of 0`** for the g6e bucket.

- **Root cause:** account 052251888500 (klanker-application) / us-east-1 has the AWS-default
  G+VT on-demand vCPU quota of **0**. (The earlier "768" was from the org *management* account —
  quotas are per-account, so it did not apply.)
- **No GPU spend incurred** — rejected pre-launch; failed record `sb-03ec9591` destroyed.
- **Quota increase requested:** L-DB2E81BA → 768 vCPU, request ID
  `d7fe8a96593a401b8ffa36adc42f254fpCvEm6pd`, status PENDING (filed 2026-06-27).
- **Check status:** `AWS_PROFILE=klanker-application aws service-quotas
  get-requested-service-quota-change --request-id d7fe8a96593a401b8ffa36adc42f254fpCvEm6pd --region us-east-1`

## GPU-free Bifrost rehearsal (2026-06-27, while quota pending)

Ran `maximhq/bifrost:v1.6.0` locally (Mac/Docker) against `klanker-application`
Bedrock — **no EC2/GPU** — and found the authored gateway config would have failed
every route on the GPU box (fake install URL, invented config schema, wrong model-ID
formats, wrong codex base_url). All corrected + **proven live** (gpt-oss-120b/20b +
Claude via `/openai` and `/anthropic`, OpenAI Responses, codex 0.23.0 on both
wire_apis). Fix committed `08dac83a`; full write-up `122-BIFROST-VALIDATION.md`.

This pre-validates the gateway portion of G8 — so on the GPU box, G8 reduces to the
**box-only** residue: the live `vllm-local/local` route (needs vLLM serving :8000),
the instance-role SigV4 + Bedrock IAM allowlist covering the gpt-oss model IDs, the
real-key `anthropic`/`openai` routes, and OTLP spans reaching `:4318` (G9).

## Deploy-surface notes (verified pre-quota)

- `make build` → km v0.5.36; `make build-lambdas` → all 8 zips built.
- S3 `toolchain/km` is linux/arm64 (healthy) — but from a prior `km init`, so it lacks the new
  `agent.codex.localBaseURL` field. **Remote create** of the GPU profiles needs
  `km init --dry-run=false` to refresh the create-handler toolchain. **Local create** (used here)
  works with the v0.5.36 operator binary directly.
- DLAMI AMI re-resolved: `ami-0a9d213b92dabc044` is still current (no drift).

## Resume procedure (once quota lands)

1. Confirm quota: G+VT applied value ≥ 48 (one g6e.12x) in 052251888500/us-east-1.
2. `km create profiles/gpu-qwen-12x.yaml --alias gpu1 --local` (Qwen AWQ — ungated, no HF token).
3. Drive G3 → G5/G6/G8/G9 via SSM (see 122-05-PLAN.md Task 2). For G8 `claude-anthropic`:
   wire `ANTHROPIC_API_KEY` (in `secrets/gpu.enc.yaml` / SSM `/km/secrets/anthropic-api-key`)
   into Bifrost's env on-box and confirm Bifrost expands it; verify keyless Bedrock routes via
   the instance role (check `bedrock:InvokeModel` covers the Claude + `openai.gpt-oss-*` IDs).
4. Operator GUI gates G4 (Continue) + G7 (Claude Code), then `km destroy gpu1 --remote --yes`.
