---
phase: 122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim
plan: 04
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - internal/app/cmd/model.go
  - internal/app/cmd/model_test.go
  - internal/app/cmd/shell.go
  - internal/app/cmd/root.go
autonomous: true
requirements: [REQ-122-MODELSTART, REQ-122-SHIM]
must_haves:
  truths:
    - "km model start <sandbox> [--local-port] [--anthropic] opens an auto-reconnecting SSM port-forward to the on-box Bifrost gateway (:8001), mirroring km vscode start / km desktop start"
    - "km model status <sandbox> reports whether the gateway answers an HTTP probe"
    - "The command is registered on the root command tree and the wiring test (mock execFn/fetcher) is green"
  artifacts:
    - path: "internal/app/cmd/model.go"
      provides: "km model start + km model status cobra commands"
      contains: "runModelStart"
    - path: "internal/app/cmd/shell.go"
      provides: "httpTunnelProbe (plain-HTTP variant for Bifrost :8001)"
      contains: "httpTunnelProbe"
  key_links:
    - from: "internal/app/cmd/model.go"
      to: "runReconnectingPortForward"
      via: "buildPortForwardCmd targeting remote port 8001"
      pattern: "runReconnectingPortForward"
    - from: "internal/app/cmd/root.go"
      to: "NewModelCmd"
      via: "root.AddCommand"
      pattern: "NewModelCmd"
---

<objective>
Implement the `km model start` / `km model status` operator commands: a laptop-side
SSM port-forward to the on-box Bifrost gateway (:8001), reusing the shared
`runReconnectingPortForward` helper plus a new plain-HTTP tunnel probe. The
forwarded :8001 (Bifrost) serves BOTH Codex (Responses API) and Claude Code
(Anthropic Messages API via Bifrost's `/anthropic` ingress) — so `--anthropic` is a
semantic alias, not a different port. The printed Claude-Code guidance must set
`ANTHROPIC_BASE_URL` to Bifrost's `/anthropic` path (NOT `/v1/messages`).

Purpose: Deliverable 3 — laptop dev against the remote local model (Claude Code
via the Anthropic shim, raw codex/curl via the OpenAI/Responses surface).
Output: new model.go command + httpTunnelProbe + root registration + green wiring test.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-RESEARCH.md
@internal/app/cmd/vscode.go
@internal/app/cmd/shell.go

<interfaces>
<!-- Shared SSM forward helpers (internal/app/cmd/shell.go) -->
```go
func buildPortForwardCmd(ctx context.Context, instanceID, region, localPort, remotePort string) *exec.Cmd
func runReconnectingPortForward(ctx context.Context, execFn ShellExecFunc, buildCmd func(context.Context) *exec.Cmd, probe tunnelProbe, reconnect bool, out io.Writer) error
type tunnelProbe func() bool
func httpsTunnelProbe(localPort int) tunnelProbe // existing HTTPS variant to mirror
```
<!-- vscode.go is the canonical pattern: resolveVSCodeDeps, FetchSandbox,
     extractResourceID(rec.Resources, ":instance/"), rec.Region, ResolveSandboxID. -->
<!-- Root registration (internal/app/cmd/root.go ~91): root.AddCommand(NewVSCodeCmd(cfg)) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add httpTunnelProbe (plain-HTTP) to shell.go</name>
  <files>internal/app/cmd/shell.go</files>
  <action>
    Next to `httpsTunnelProbe` (shell.go:733), add `httpTunnelProbe(localPort int) tunnelProbe`
    that probes `http://127.0.0.1:{localPort}/` (Bifrost gateway — any non-error HTTP
    response, incl. 4xx, means the tunnel carries data end-to-end; resolve Bifrost's
    actual health/ready path from its docs at execution time, else probe `/`) with
    a 6s-timeout plain-HTTP client (no TLS transport). Any non-error response (incl.
    4xx) means the tunnel carries data end-to-end → return true; transport error →
    false. Copy the doc-comment style of httpsTunnelProbe (cite Bifrost :8001).
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/ && go vet ./internal/app/cmd/</automated>
  </verify>
  <done>httpTunnelProbe exists, compiles, vet-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement internal/app/cmd/model.go (start + status)</name>
  <files>internal/app/cmd/model.go</files>
  <action>
    Create `internal/app/cmd/model.go` mirroring vscode.go's structure:
      - `NewModelCmd(cfg)` parent + `newModelStartCmd` + `newModelStatusCmd`,
        with the DI-injectable `newModelCmdInternal(cfg, fetcher, execFn, ssmClient)`
        pattern so tests inject mocks (reuse `resolveVSCodeDeps`-style helper or a
        local `resolveModelDeps`).
      - `km model start <sb>` flags: `--local-port` (default 8001),
        `--anthropic` (bool; semantic alias — forwards :8001 (Bifrost) same as
        default since Bifrost serves both the OpenAI/Responses and Anthropic-Messages
        surfaces; when set, print Claude-Code-oriented hints).
      - `runModelStart`: probe local port free (mirror vscode's net.Listen guard) →
        FetchSandbox → extractResourceID(rec.Resources, ":instance/") → print the
        connection hint block (Claude Code: `export ANTHROPIC_BASE_URL=http://localhost:<port>/anthropic`
        — Bifrost's Anthropic ingress path, officially documented by Maxim AI, NOT
        `/v1/messages`;
        Codex: `base_url="http://localhost:<port>/v1"`, `wire_api="responses"`) →
        `buildPF := func(c){ return buildPortForwardCmd(c, instanceID, region, strconv.Itoa(localPort), "8001") }` →
        `runReconnectingPortForward(ctx, execFn, buildPF, httpTunnelProbe(localPort), true, os.Stdout)`.
      - `runModelStatus`: FetchSandbox + extract instance; do a SHORT one-shot SSM
        send (mirror vsCodeStatusScript) querying `systemctl is-active vllm` +
        `systemctl is-active bifrost` + `curl -s -o /dev/null -w '%{http_code}' localhost:8001/`,
        parse, print a one-line summary, non-zero exit if not healthy.
    Reuse the existing region/instance extraction helpers — do not reinvent.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/ && go vet ./internal/app/cmd/</automated>
  </verify>
  <done>model.go compiles; NewModelCmd / runModelStart / runModelStatus exist following the vscode pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Register km model + make the Plan 01 wiring test green</name>
  <files>internal/app/cmd/root.go, internal/app/cmd/model_test.go</files>
  <action>
    Add `root.AddCommand(NewModelCmd(cfg))` to root.go next to the vscode/desktop
    registrations (~line 91).

    Fill in / un-skip `internal/app/cmd/model_test.go` (Plan 01 scaffold) so
    `TestModelStart_PortForwardWiring` is green: inject a mock fetcher (record with
    `:instance/i-deadbeef` + Region us-east-1) and a mock execFn that captures the
    *exec.Cmd and returns nil (one-shot, no reconnect loop because reconnect path
    only triggers on non-nil err). Assert the built command forwards remote port
    **8001** and the requested local port. If Plan 01 used a `t.Skip`, remove it
    and write the real assertions now.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run TestModel -count=1 -timeout 600s</automated>
  </verify>
  <done>`km model start`/`status` registered on the root tree; TestModel* green; cmd suite still green.</done>
</task>

</tasks>

<verification>
- `go build ./... && go vet ./internal/app/cmd/` clean.
- `go test ./internal/app/cmd/ -count=1 -timeout 600s` green (use the command's own
  exit code — project memory: cmd suite needs -timeout 600s and real exit).
- `./km model --help` lists start + status after `make build`.
</verification>

<success_criteria>
- km model start forwards :8001 with auto-reconnect + plain-HTTP liveness probe.
- km model status reports vllm/bifrost health.
- Command registered; wiring test green.
</success_criteria>

<output>
After completion, create `.planning/phases/122-gpu-vllm-model-serving-sandbox-profiles-plus-local-model-chat-codex-repoint-km-model-start-anthropic-shim/122-04-SUMMARY.md`
</output>
