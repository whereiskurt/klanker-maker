---
phase: 75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels
plan: 06
type: execute
wave: 4
depends_on: [02, 03, 04, 05]
files_modified:
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: false
requirements:
  - REQ-FILES-DEPLOY

must_haves:
  truths:
    - "docs/slack-notifications.md has a Phase 75 subsection covering the inbound file flow, the new files:read scope, and the operator deployment runbook"
    - "CLAUDE.md Slack-inbound subsection mentions file-attachment support, points at the spec, and does NOT duplicate spec content"
    - "Operator UAT confirms end-to-end behavior: drag image → Claude describes; drag PDF → Claude reads; drag 26 files → warning posted + first 25 dispatched"
    - "Manual UAT verifies the IAM + memory + scope changes are live in deployed AWS state (terragrunt plan diff post-apply, aws iam get-role-policy, aws s3api get-bucket-lifecycle-configuration)"
  artifacts:
    - path: docs/slack-notifications.md
      provides: "Phase 75 subsection with operator setup, profile field (none new — uses existing notifySlackInboundEnabled), and troubleshooting"
      contains: "Phase 75"
    - path: CLAUDE.md
      provides: "Inline Slack-inbound subsection update mentioning file attachments"
      contains: "Phase 75"
  key_links:
    - from: docs/slack-notifications.md
      to: docs/superpowers/specs/2026-05-15-slack-inbound-file-attachments-design.md
      via: "explicit link in the Phase 75 subsection; no content duplication"
      pattern: "2026-05-15-slack-inbound-file-attachments-design"
    - from: CLAUDE.md
      to: docs/slack-notifications.md (Phase 75 subsection)
      via: "short paragraph pointing at the operator guide"
      pattern: "docs/slack-notifications.md"
---

<objective>
Close out Phase 75 with docs + manual UAT. Two doc files updated, then an operator-driven UAT gate that exercises the full deployed pipeline: re-install Slack app with `files:read` → rotate bot token → `make build && km init` (NOT `km init --lambdas`!) → drag image / PDF / 26 files into a live `#sb-{sandbox-id}` channel → verify each behavior.

Purpose: Plans 01-05 ship implementation + tests but no human has seen the feature work end-to-end against real Slack + real AWS. UAT is the gate that catches issues unit tests can't (e.g., the IAM policy's principal not actually allowing the deployed Lambda role, the Slack app re-install needing manual workspace-admin approval, the bot token rotation hitting an unexpected error). It's also the gate that catches deployment-procedure mistakes (e.g., using `km init --lambdas` instead of full `km init`, per the memory `project_km_init_lambdas_doesnt_deploy`).

Output:
1. `docs/slack-notifications.md` — new subsection under the existing "Slack inbound (Phase 67)" section, covering Phase 75 specifics
2. `CLAUDE.md` — short paragraph added to the existing "Slack inbound (Phase 67)" subsection mentioning file attachments + scope addition + deployment note, pointing at the doc + spec for detail
3. Manual UAT performed by the operator with a documented set of pass/fail criteria

This plan is NON-AUTONOMOUS — the UAT step requires a live Slack workspace, a live AWS deployment, and human judgment on whether Claude's reply correctly describes the image/PDF content.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-CONTEXT.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-RESEARCH.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-VALIDATION.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-01-SUMMARY.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-02-SUMMARY.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-03-SUMMARY.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-04-SUMMARY.md
@.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-05-SUMMARY.md

@docs/slack-notifications.md
@CLAUDE.md
@docs/superpowers/specs/2026-05-15-slack-inbound-file-attachments-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 06-01: Document Phase 75 in docs/slack-notifications.md and CLAUDE.md</name>
  <files>docs/slack-notifications.md, CLAUDE.md</files>
  <action>
    1. Open `docs/slack-notifications.md`. Find the section titled "Slack inbound (Phase 67)" (or the closest existing inbound section — Phase 67 may have been documented under different headings depending on history). Append a NEW subsection after the existing Phase 67/67.1/68 inbound content:

       ```markdown
       ### Slack inbound file attachments (Phase 75)

       Users can drag-and-drop files (images, PDFs, etc.) into a per-sandbox
       `#sb-{sandbox-id}` channel. The bridge Lambda downloads each file from
       Slack using the bot token, stages it to S3, and the sandbox-side poller
       mirrors each file to `/workspace/.km-slack/attachments/<thread_ts>/`.
       A natural-language master-prompt wrapper is prepended to the Claude
       turn enumerating absolute paths and MIME types — Claude reads each file
       with its Read tool when relevant to the question.

       **Profile field:** No new field. Gated on the existing
       `spec.cli.notifySlackInboundEnabled: true` (Phase 67).

       **Caps:**

       - 25 files per message — over-cap files dropped with thread-reply warning
       - 100 MB per file — oversize files dropped with thread-reply warning

       **One-time operator setup (after Phase 75 deploys):**

       1. Add `files:read` to the Slack App's bot scopes (App config → OAuth & Permissions)
       2. Re-install the app to your workspace (admin approval may be required)
       3. Rotate the bot token to pick up the new scope:
          ```bash
          km slack rotate-token --bot-token <new-token-from-Slack-App-admin>
          ```
       4. Rebuild + redeploy the bridge:
          ```bash
          make build && km init
          ```
          **Important:** use full `km init`, NOT `km init --lambdas`. The lambdas-only
          path builds the zip but does NOT deploy it (see `project_km_init_lambdas_doesnt_deploy`
          in operator memory).
       5. Verify scopes via `km doctor` — `slack_app_events_subscription` should
          report `(channels:history, groups:history, reactions:write, files:read)`.

       **Sandbox provisioning:** Existing sandboxes do NOT get the Phase 75
       userdata changes retroactively (the poller bash is baked into userdata
       at create time). Run `km destroy && km create` on any sandbox that
       needs file-attachment support.

       **S3 staging layout:**

       - Key format: `slack-inbound/<sandbox-id>/<thread_ts>/<file_id>-<sanitized_name>`
       - `<file_id>` is the Slack `F012345` identifier — guarantees uniqueness
         even when two files in the same thread share a name
       - `<sanitized_name>` strips path-unsafe characters and truncates to 255 bytes
       - **30-day lifecycle expiration** on the `slack-inbound/` prefix (matches
         the `km-slack-threads` DDB TTL)

       **Sandbox-side layout:**

       - Directory: `/workspace/.km-slack/attachments/<thread_ts>/`
       - Files persist for the sandbox lifetime (cleaned by `km destroy`
         taking the EBS volume); subsequent turns in the same thread don't
         re-download

       **Troubleshooting:**

       | Symptom | Likely cause | Fix |
       |---|---|---|
       | 👀 appears but Claude doesn't read the file | `files:read` scope missing | Re-install app + `km slack rotate-token`; verify with `km doctor` |
       | 👀 appears, agent replies as text-only | Sandbox was provisioned before Phase 75 deploy | `km destroy && km create` |
       | Bridge logs "files:read scope may be missing" | Same as above | Same as above |
       | Bridge logs "request body offset reset failed" | Lambda memory_size < 1024 (pre-bump) | Verify `terragrunt plan` and re-apply `infra/live/use1/lambda-slack-bridge/` |
       | First 25 of N files attached; rest skipped | 25-file cap by design | Split the upload across multiple messages |
       | Skipped foo.png (>100 MB cap) | 100MB cap by design | Trim or split the file |

       **Authoritative design:** `docs/superpowers/specs/2026-05-15-slack-inbound-file-attachments-design.md` —
       full PRD with failure-handling matrix, Pitfall catalog, and rollback procedure.
       ```

    2. Open `CLAUDE.md`. Find the existing "Slack inbound (Phase 67)" subsection in the "Slack Notifications" section. Append a SHORT paragraph (do NOT duplicate the operator guide):

       ```markdown
       ### Slack inbound file attachments (Phase 75)

       Per-sandbox channels now accept file_share uploads (images, PDFs, etc.).
       Bridge Lambda downloads with bot token, stages to S3 under
       `slack-inbound/<sandbox-id>/<thread_ts>/`, sandbox poller mirrors to
       `/workspace/.km-slack/attachments/<thread_ts>/`, a natural-language
       wrapper prepended to the prompt lists absolute paths + MIME types.
       Caps: 25 files/msg, 100 MB/file. Over-cap → thread-reply warning.

       New bot scope: `files:read`. Operator path: re-install the Slack app
       with the new scope, run `km slack rotate-token --bot-token <new>`,
       then `make build && km init` (NOT `km init --lambdas` — that path
       doesn't deploy bridge zips).

       Lambda memory_size bumped 256 → 1024 to fit 100MB in-memory file
       buffering for SDK PutObject retry-rewindability.

       See `docs/slack-notifications.md` § Slack inbound file attachments
       (Phase 75) for the full operator runbook. Authoritative design:
       `docs/superpowers/specs/2026-05-15-slack-inbound-file-attachments-design.md`.
       ```

    3. DO NOT modify any other section in either doc. DO NOT touch the existing Phase 67/67.1/67.2/68 subsections — they remain accurate.

    Why: Operators reading `docs/slack-notifications.md` need the runbook; developers reading `CLAUDE.md` (which Claude itself uses as project context) need a pointer + the critical-not-to-miss bits (the `km init --lambdas` pitfall, the new scope, the memory bump). The spec carries the design detail; the docs are entry-points.
  </action>
  <verify>
    <automated>grep -q "Slack inbound file attachments (Phase 75)" docs/slack-notifications.md && grep -q "Slack inbound file attachments (Phase 75)" CLAUDE.md && grep -q "files:read" docs/slack-notifications.md && grep -q "km init --lambdas" docs/slack-notifications.md</automated>
  </verify>
  <done>
    - `docs/slack-notifications.md` has a new Phase 75 subsection with profile-field info, caps, operator setup steps, S3 layout, sandbox-side layout, troubleshooting table
    - `CLAUDE.md` has a short Phase 75 paragraph in the Slack-inbound subsection — points at docs + spec, does not duplicate
    - Both docs warn about the `km init --lambdas` pitfall (per project memory)
    - Both docs mention the `files:read` scope addition and the bot-token rotation requirement
    - Spec is referenced by absolute path in both docs (link, do not duplicate)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06-02: Operator UAT — deploy, drag files, verify end-to-end</name>
  <files>NONE — operator runs commands against live AWS + live Slack; no repo files modified by this task</files>
  <what-built>
    Phase 75 implementation: Slack file_share events admitted by bridge → 25-file/100MB caps enforced → S3 staging with 30-day lifecycle → sandbox poller mirrors files + prepends master-prompt wrapper → Claude reads files via its Read tool. Plans 01-05 shipped:

    - Plan 01: SlackFile/Attachment types, isBotLoop allow-list, ROADMAP fix
    - Plan 02: S3FileDownloader + bridge Handle fork + Pitfall 1 mitigation
    - Plan 03: Sandbox poller bash extension + master-prompt wrapper + Pitfall 4 fix
    - Plan 04: Bridge IAM (s3:PutObject on slack-inbound/*) + memory_size 1024 + S3 lifecycle module + files:read scope checks
    - Plan 05: Bridge cold-start wiring (S3FileDownloader instantiated in main.go)
    - Plan 06 Task 01: Docs updated (docs/slack-notifications.md + CLAUDE.md)
  </what-built>
  <action>
    This task is a human-verify checkpoint. The "action" is the operator-driven UAT runbook below. The executor's job in autonomous mode is to PAUSE here and surface the runbook + pass/fail criteria. Do not proceed to any next task or summary until the operator types "approved".

    Run this UAT runbook step-by-step. Each numbered step has a pass criterion — stop and diagnose at the first failure.

    **Step 0 — Prerequisites:**
    - Slack workspace admin access (for app re-install)
    - AWS SSO logged in to the application account
    - One existing or freshly-created per-sandbox channel `#sb-{sandbox-id}` (i.e., a sandbox provisioned with `notifySlackInboundEnabled: true` and `notifySlackPerSandbox: true`)

    **Step 1 — Add `files:read` scope:**
    - Visit your Slack App admin page → OAuth & Permissions → Bot Token Scopes
    - Add `files:read` to the existing scope list (channels:history, groups:history, reactions:write should already be present)
    - Click "Reinstall to Workspace" — approve as workspace admin
    - Copy the NEW bot token (it changes on re-install)

    **PASS:** Reinstall succeeded; new `xoxb-...` token captured

    **Step 2 — Rotate the bot token:**
    ```bash
    km slack rotate-token --bot-token <new-xoxb-token>
    ```

    **PASS:** Command exits 0; SSM `/km/slack/bot-token` updated; bridge Lambda cold-start triggered; smoke test (`km slack test`) succeeds.

    **Step 3 — Build + deploy:**
    ```bash
    make build              # rebuild km CLI + bridge zip (Phase 75 wiring + memory bump)
    km init                 # full deploy — IAM update + memory bump + lifecycle module
    ```
    **CRITICAL:** Do NOT use `km init --lambdas` — that path only builds zips, doesn't deploy them.

    **PASS:** `make build` succeeds; `km init` apply completes; terragrunt output shows:
    - `aws_iam_role_policy.slack_bridge_files_s3_write` created (or no-changes if already applied)
    - `aws_lambda_function.slack_bridge` memory_size updated to 1024
    - `aws_s3_bucket_lifecycle_configuration.artifacts` created with `slack-inbound-30day` rule

    **Step 4 — Verify deployed state via AWS CLI:**
    ```bash
    aws iam get-role-policy --role-name $(km info | grep slack-bridge-role | awk '{print $2}') --policy-name <function-name>-files-s3-write
    aws s3api get-bucket-lifecycle-configuration --bucket $(km info | grep KM_ARTIFACTS_BUCKET | awk '{print $2}')
    aws lambda get-function-configuration --function-name <function-name> --query 'MemorySize'
    ```

    **PASS:** First command shows IAM statement with `s3:PutObject` on `slack-inbound/*`; second shows lifecycle rule with `Prefix: slack-inbound/` and `Days: 30`; third returns `1024`.

    **Step 5 — Verify km doctor reports new scope:**
    ```bash
    km doctor
    ```

    **PASS:** `slack_app_events_subscription` check reports success message including `files:read` in the parenthesized scope list.

    **Step 6 — Create a fresh sandbox for end-to-end UAT** (existing sandboxes don't have the Phase 75 userdata bash):
    ```bash
    km create profiles/<your-inbound-enabled-profile>.yaml --alias phase75-uat
    SB=$(km list | awk '/phase75-uat/ {print $1}')
    echo "Sandbox: $SB"
    ```

    **PASS:** `km create` succeeds; sandbox is `running`; `#sb-$SB` channel created and bot is a member.

    **Step 7 — Drag IMAGE test:**
    1. In the `#sb-$SB` channel, drag a small (< 5MB) PNG screenshot from your desktop
    2. Add a comment: "What's in this image? Describe in one sentence."
    3. Send the message

    **PASS:**
    - Within ~1 second: 👀 reaction appears on the message
    - Within ~10-30 seconds: bot reply describes the image content accurately in one sentence (e.g., "A screenshot of a code editor showing Go source code with the function downloadOneFile.")
    - The reply is threaded under the original message

    **FAIL (and what to check):**
    - No 👀 → bridge didn't receive the event; check Slack App Event Subscriptions URL + signing secret
    - 👀 but no reply → bridge dropped the event after admit; check CloudWatch logs for the bridge Lambda
    - Reply is text-only and ignores the image → either the FileDownloader is nil at runtime (check Plan 05 wiring) or `files:read` scope is missing (check Step 5 again)
    - Reply mentions "I see an attached file but cannot access it" → the file mirror failed; check `journalctl -t km-slack-inbound -f` on the sandbox

    **Step 8 — Drag PDF test:**
    1. Drag a small PDF (< 5MB) into the same channel
    2. Comment: "What is the title of this document?"
    3. Send

    **PASS:** Bot reply correctly identifies the document title from PDF text content. (Claude's Read tool handles PDFs natively — no OCR needed.)

    **Step 9 — Drag 26 FILES test (cap enforcement):**
    1. Select 26 small files (any type, < 1MB each)
    2. Drag all 26 into the channel as a single message
    3. Comment: "How many files did you receive?"

    **PASS:**
    - Thread-reply warning appears BEFORE the agent reply: `⚠️ Only first 25 of 26 files attached; rest skipped`
    - Agent reply mentions receiving 25 files (or describes the first 25 in some way)

    **Step 10 — Drag oversize file test:**
    1. Find or create a file > 100MB (e.g., `dd if=/dev/zero of=big.bin bs=1m count=101`)
    2. Drag it into the channel
    3. Comment: "What's this?"

    **PASS:**
    - Thread-reply warning: `⚠️ Skipped big.bin (101 MB > 100 MB cap)` (exact wording per failure-handling matrix)
    - Agent reply is text-only or notes no attached file was received

    **Step 11 — File-only upload test (Pitfall 4 regression):**
    1. Drag a small image into the channel
    2. Send with NO comment (empty text)

    **PASS:**
    - 👀 reaction appears
    - Agent replies — does NOT silently drop the message
    - Agent's reply uses the wrapper's `[no text — file-only]` fallback text internally (you can confirm by checking the prompt that was generated — `ls /workspace/.km-slack/attachments/<thread_ts>/` on the sandbox should show the file)

    **Step 12 — Cleanup:**
    ```bash
    km destroy $SB --remote --yes
    ```

    **PASS:** Sandbox + channel + queue all cleaned up.
  </action>
  <verify>
    Manual gate — no automated command. The operator confirms via the runbook PASS criteria above. Resume signal: type "approved" if all 12 steps passed. If any step failed, describe which step + the failure mode (e.g., "Step 7: no 👀 reaction — bridge Lambda logs show signature verification failure"). Failures may indicate a regression in one of Plans 01-05 — diagnose, file a follow-up gap-closure plan if needed, do not approve until live behavior matches the runbook.
  </verify>
  <done>
    - All 12 runbook steps completed by the operator with documented PASS criteria met
    - End-to-end image drag → Claude describes content
    - End-to-end PDF drag → Claude reads document
    - 26-file cap exercise → thread-reply warning + first-25 dispatch
    - Oversize file exercise → thread-reply warning + skip
    - File-only upload (empty text) → admitted (Pitfall 4 fix proven live)
    - Sandbox cleanup successful (channel archived per Phase 63 slackArchiveOnDestroy default)
    - No regressions in existing Phase 67/67.1/67.2/68 inbound + transcript flows
    - Operator types "approved" to resume; if not, the failure mode is documented for gap-closure planning
  </done>
</task>

</tasks>

<verification>
1. Both docs updated with Phase 75 subsections
2. UAT runbook executed by operator — all 12 steps pass
3. Live deployment verified via AWS CLI commands in Step 4
4. End-to-end image + PDF + cap + file-only flows verified in Steps 7-11

This is the phase-completion gate. After UAT approval:
- Phase 75 is functionally complete
- Update `.planning/STATE.md` to mark Phase 75 done in `/gsd:verify-work` follow-up
- Update `.planning/REQUIREMENTS.md` to add REQ-FILES-* entries to the traceability table (Phase: 75, Status: Complete)
- Optionally write `.planning/phases/75-.../75-RETROSPECTIVE.md` capturing what went smoothly vs friction
</verification>

<success_criteria>
- docs/slack-notifications.md has the Phase 75 subsection with operator runbook + troubleshooting
- CLAUDE.md has a short Phase 75 paragraph in the Slack-inbound subsection
- Operator UAT pass — all 12 runbook steps complete with documented PASS criteria met
- No regressions observed in existing Phase 67/67.1/67.2/68 inbound + transcript behavior
</success_criteria>

<output>
After completion, create `.planning/phases/75-slack-inbound-file-attachments-images-pdfs-for-per-sandbox-channels/75-06-SUMMARY.md` per template, listing doc additions and UAT results (one row per runbook step, PASS/FAIL, any diagnosis notes).
</output>
