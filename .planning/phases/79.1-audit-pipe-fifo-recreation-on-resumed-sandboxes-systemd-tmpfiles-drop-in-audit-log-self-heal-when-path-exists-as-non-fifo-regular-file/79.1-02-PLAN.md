---
phase: 79.1
plan: 02
type: execute
wave: 1
depends_on:
  - "79.1-01"
files_modified:
  - pkg/compiler/userdata.go
autonomous: true
requirements:
  - L1-TMPFILES
  - L1-ORDER
  - L1-MODE

must_haves:
  truths:
    - "Rendered EC2 userdata writes /usr/lib/tmpfiles.d/km.conf containing a `p+` line for /run/km/audit-pipe with km-sidecar:km-sidecar 0666 ownership"
    - "The tmpfiles.d heredoc appears BEFORE the km-audit-log.service unit-file heredoc in the rendered output"
    - "On every EC2 boot (fresh AND stop+resume), systemd-tmpfiles-setup.service recreates /run/km/audit-pipe as a correctly-owned FIFO before any sidecar starts"
    - "The three RED tests from Plan 01 turn GREEN; no other userdata test regresses"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "Userdata template now writes /usr/lib/tmpfiles.d/km.conf in the EC2 boot script"
      contains: "cat > /usr/lib/tmpfiles.d/km.conf"
  key_links:
    - from: "pkg/compiler/userdata.go inserted heredoc"
      to: "systemd-tmpfiles-setup.service (kernel-managed early-boot unit, AL2023)"
      via: "tmpfiles.d configuration file loaded at boot Before=sysinit.target"
      pattern: "p\\+ /run/km/audit-pipe"
    - from: "userdata template ordering"
      to: "km-audit-log.service unit (line ~982) consumes the FIFO via Environment=KM_AUDIT_PIPE"
      via: "heredoc written to /usr/lib/tmpfiles.d/ BEFORE unit heredoc"
      pattern: "/usr/lib/tmpfiles.d/km.conf"
---

<objective>
Wave 1 — Layer 1 implementation: add a `/usr/lib/tmpfiles.d/km.conf` heredoc to the EC2 userdata template that recreates the audit-pipe FIFO with correct ownership at every boot (the root cause fix for the stop+resume integration gap).

Purpose: Eliminate the race where km-presence (root) writes `/run/km/audit-pipe` as a regular file before km-audit-log (km-sidecar) can mkfifo it. With this fix, systemd-tmpfiles-setup.service creates the FIFO during early-boot (`Before=sysinit.target`), guaranteeing both sidecars see a correctly-owned FIFO.

Output: One template edit in `pkg/compiler/userdata.go` (~10 lines added, zero lines removed). The three RED tests from Plan 01 turn GREEN.

Runs in parallel with Plan 03 (different file, no dependency).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-RESEARCH.md
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-01-PLAN.md
@pkg/compiler/userdata.go

<interfaces>
<!-- The exact insertion site in pkg/compiler/userdata.go. Extracted verbatim
     from lines 969-982 of the current file. The new heredoc must be inserted
     between the existing mkfifo block (ends at line 980) and the
     km-audit-log.service heredoc (begins at line 982). -->

```text
# CURRENT FILE STATE (pkg/compiler/userdata.go lines 969-982):
969: # Create named pipe for audit-log sidecar input.
970: # Shell commands are written here by PROMPT_COMMAND; sidecar reads from it.
971: mkdir -p /run/km
972: chown km-sidecar:km-sidecar /run/km
973: mkfifo /run/km/audit-pipe
974: chown km-sidecar:km-sidecar /run/km/audit-pipe
975: chmod 666 /run/km/audit-pipe
976: {{- if .LearnMode }}
977: # Learn mode: create command log file writable by all users (root and sandbox user).
978: touch /run/km/learn-commands.log
979: chmod 666 /run/km/learn-commands.log
980: {{- end }}
981: <empty line — INSERT NEW HEREDOC BLOCK HERE>
982: cat > /etc/systemd/system/km-audit-log.service << 'UNIT'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert /usr/lib/tmpfiles.d/km.conf heredoc between mkfifo block and km-audit-log.service heredoc</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    Edit `pkg/compiler/userdata.go`. Locate the existing `mkfifo /run/km/audit-pipe` block (currently lines 969-980, ending with `{{- end }}` for the LearnMode conditional). Immediately after line 980 (the `{{- end }}` line) and BEFORE the blank line + `cat > /etc/systemd/system/km-audit-log.service << 'UNIT'` heredoc, insert the following block VERBATIM (preserving the leading newline so it renders cleanly into the bash script):

    ```text

    # Layer 1 (Phase 79.1): systemd-tmpfiles drop-in for /run/km/audit-pipe.
    # Ensures the FIFO is recreated with correct ownership on every boot
    # (including EC2 stop+resume, when cloud-init does NOT re-run and the
    # /run tmpfs is wiped). systemd-tmpfiles-setup.service runs before
    # sysinit.target, so the FIFO exists before km-audit-log.service or
    # km-presence.service start. The `p+` type replaces a regular file
    # (e.g., one left by km-presence's `tee` racing the sidecar on a prior
    # boot) — `p` alone would be a silent no-op in that case.
    cat > /usr/lib/tmpfiles.d/km.conf << 'TMPFILES'
    # Klankrmkr sandbox: volatile runtime directory and audit-pipe FIFO.
    # systemd-tmpfiles-setup.service reads this before sysinit.target on every boot.
    d  /run/km                  0755 km-sidecar km-sidecar - -
    p+ /run/km/audit-pipe       0666 km-sidecar km-sidecar - -
    TMPFILES
    ```

    Critical placement constraints:
    - MUST appear AFTER the `{{- end }}` of the LearnMode conditional (line 980 in current file).
    - MUST appear BEFORE `cat > /etc/systemd/system/km-audit-log.service << 'UNIT'` (line 982 in current file).
    - The heredoc terminator `TMPFILES` MUST be at column 0 (start of line), no leading whitespace — heredoc terminators in bash are positional.
    - Column alignment inside the heredoc is whitespace-tolerant; the Plan 01 regex test allows any `\s+` between columns. The literal mode/owner/group strings (`0666`, `0755`, `km-sidecar`, `km-sidecar`) MUST be exact.
    - DO NOT modify the existing `mkfifo /run/km/audit-pipe` block (lines 971-975). Both layers are belt-and-suspenders — the bash mkfifo handles first-boot before systemd-tmpfiles-setup runs, the tmpfiles.d entry handles every subsequent boot.
    - DO NOT touch the km-bootstrap.service comment at lines 1092-1094. RESEARCH.md § Q4 explicitly says leave it alone.
    - DO NOT add `After=systemd-tmpfiles-setup.service` to any unit. The ordering chain through `network.target` already guarantees correctness (RESEARCH.md § Q12 Pitfall 3).

    NO other files change in this plan. NO new template fields, NO Go struct edits, NO new test files.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/compiler/... -run 'TestUserdata_TmpfilesD' -count=1 -timeout 30s -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three Phase 79.1 Layer 1 tests turn GREEN:
    - `TestUserdata_TmpfilesDDropInPresent` PASS
    - `TestUserdata_TmpfilesDDropInBeforeKmAuditLogUnit` PASS
    - `TestUserdata_TmpfilesDDropInMode` PASS

    No other test in `pkg/compiler/...` regresses (full package test still green). The diff in `pkg/compiler/userdata.go` is a pure insertion of ~14 lines (comment + heredoc) between the existing mkfifo block and the km-audit-log.service heredoc — zero deletions.
  </done>
</task>

</tasks>

<verification>
1. Layer 1 tests green:
   ```bash
   go test ./pkg/compiler/... -run 'TestUserdata_TmpfilesD' -count=1 -timeout 30s -v
   ```
   Expected: 3/3 PASS.

2. No regression in the full compiler package:
   ```bash
   go test ./pkg/compiler/... -count=1 -timeout 60s
   ```
   Expected: ok with no failures (all Phase 79 tests + all pre-existing userdata tests still pass).

3. Diff sanity:
   ```bash
   git diff pkg/compiler/userdata.go | head -50
   ```
   Expected: pure insertion block, no deletions, ~14 added lines.
</verification>

<success_criteria>
- [ ] All three Phase 79.1 Layer 1 tests PASS.
- [ ] All pre-existing tests in `pkg/compiler/...` continue to PASS.
- [ ] The diff in `pkg/compiler/userdata.go` is additive only (no deletions).
- [ ] The new heredoc renders into the EC2 bash script between the existing `mkfifo /run/km/audit-pipe` block and the `km-audit-log.service` unit-file heredoc.
- [ ] Heredoc terminator `TMPFILES` is at column 0.
- [ ] km-bootstrap comment at lines 1092-1094 is UNCHANGED (per RESEARCH.md § Q4).
</success_criteria>

<output>
After completion, create `.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-02-SUMMARY.md` documenting:

- The exact line range of the inserted block in `pkg/compiler/userdata.go`.
- The full diff (annotated).
- Confirmation that the three Plan 01 RED tests are now GREEN.
- Confirmation that no other test regressed.
- Reminder for Plan 04: rollout = `make build && km init --sidecars` (Layer 1 is template-only, no sidecar binary rebuild required for THIS plan — but Plan 03 will also need `make sidecars` so the combined Phase 79.1 rollout is the full three-command sequence).
</output>
