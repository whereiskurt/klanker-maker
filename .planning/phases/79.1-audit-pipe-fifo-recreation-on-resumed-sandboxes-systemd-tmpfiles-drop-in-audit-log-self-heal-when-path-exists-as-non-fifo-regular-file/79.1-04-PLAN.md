---
phase: 79.1
plan: 04
type: execute
wave: 2
depends_on:
  - "79.1-02"
  - "79.1-03"
files_modified:
  - CLAUDE.md
autonomous: false
requirements:
  - UAT-RESUME

must_haves:
  truths:
    - "CLAUDE.md 'Presence daemon (Phase 79)' section gains a Phase 79.1 follow-up subsection covering the bug, both fix layers, rollout, retroactivity, and the manual recovery snippet for pre-79.1 sandboxes"
    - "A live AWS UAT (km create + km pause + km resume + journalctl + km doctor + ls -la) confirms the audit pipeline survives a stop+resume cycle on a fresh post-Phase-79.1 sandbox"
    - "After UAT passes, a phase SUMMARY.md is written documenting the full Phase 79.1 outcome"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Operator-facing documentation of the Phase 79.1 fix, rollout steps, and manual recovery snippet for legacy sandboxes"
      contains: "Phase 79.1"
    - path: ".planning/phases/79.1-.../79.1-SUMMARY.md"
      provides: "Phase closeout: full UAT log, files modified across all plans, lessons learned, traceability for future similar bugs"
      contains: "UAT-RESUME"
  key_links:
    - from: "CLAUDE.md Phase 79.1 subsection"
      to: "the manual recovery snippet from STATE.md (rm + mkfifo + chown + chmod + systemctl restart km-audit-log)"
      via: "documentation of operator workaround for pre-Phase-79.1 sandboxes"
      pattern: "rm /run/km/audit-pipe"
    - from: "Live AWS UAT (km pause + km resume)"
      to: "journalctl -u km-audit-log + km doctor presence_daemon_healthy"
      via: "operator runs the VALIDATION.md UAT script end-to-end"
      pattern: "reading from audit pipe"
---

<objective>
Wave 2 — Phase closeout: document the Phase 79.1 fix in CLAUDE.md for operators, run the BLOCKING live AWS UAT that proves the fix works on a real stop+resume cycle, and write the phase SUMMARY.

Purpose: Phase 79 was closed without ever testing the stop+resume cycle — the integration gap was only discovered post-ship on sandbox `learn-14f484c7`. Phase 79.1 cannot repeat that mistake. The phase does NOT close until a real `km create` + `km pause` + `km resume` against AWS confirms: (a) audit-log opens the FIFO cleanly (no `permission denied` in journalctl), (b) `/run/km/audit-pipe` is a FIFO (not a regular file), (c) `km doctor presence_daemon_healthy` returns OK within ~90 seconds of the liveness signal.

Output: Updated CLAUDE.md section. Completed UAT with captured journalctl + ls -la + km doctor output. Final phase SUMMARY.md documenting all four plans' outcomes.

Depends on BOTH Plan 02 (Layer 1) AND Plan 03 (Layer 2) — without both, the rollout instruction in the docs would be wrong and the UAT would not exercise the full fix.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-RESEARCH.md
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-VALIDATION.md
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-01-SUMMARY.md
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-02-SUMMARY.md
@.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Insertion point in CLAUDE.md.
     The existing "## Presence daemon (Phase 79)" section spans lines 494-567.
     The "### Roll back" subsection (lines 560-565) and the trailing PRD pointer
     (line 567) are the last elements before "## Cross-account k8s integrations (Phase 80)".
     The new Phase 79.1 subsection MUST be inserted as a fresh "### Phase 79.1 follow-up:
     audit-pipe FIFO recreation on resumed sandboxes" between line 565 (end of Roll back)
     and line 567 (PRD pointer), OR appended after the PRD pointer but BEFORE
     "## Cross-account k8s integrations (Phase 80)" — pick whichever preserves
     section flow best. -->

Existing CLAUDE.md structure around insertion point (lines 560-570):
```text
### Roll back
[paragraphs about reverting]

See `docs/superpowers/specs/2026-05-10-km-presence-daemon-design.md` for the full PRD.

## Cross-account k8s integrations (Phase 80)
```

Phase 79 sub-section conventions (matching style of existing CLAUDE.md sub-sections like Phase 75 hotfix lessons):
- `### Phase 79.1 follow-up: audit-pipe FIFO recreation on resumed sandboxes`
- Short prose explaining the bug observed on `learn-14f484c7`
- Bulleted two-layer fix summary
- Code block with the three-command rollout
- Bullet about retroactivity + the manual recovery snippet for legacy sandboxes
- Closing pointer to `.planning/phases/79.1-.../79.1-RESEARCH.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "Phase 79.1 follow-up" subsection to CLAUDE.md under the Presence daemon section</name>
  <files>CLAUDE.md</files>
  <action>
    Edit `CLAUDE.md`. Locate the existing `## Presence daemon (Phase 79)` section (line 494 in current file) and its `### Roll back` subsection plus closing PRD pointer (around lines 560-567). Insert a new subsection `### Phase 79.1 follow-up: audit-pipe FIFO recreation on resumed sandboxes` AFTER the `See \`docs/superpowers/...\` for the full PRD.` line and BEFORE the next top-level `## Cross-account k8s integrations (Phase 80)` heading.

    Use this content VERBATIM (adjust only if the existing line wrapping suggests minor adjustments):

    ````markdown
    ### Phase 79.1 follow-up: audit-pipe FIFO recreation on resumed sandboxes

    Phase 79 shipped the km-presence daemon but its initial UAT was performed
    only on a fresh `km create` — never on a `km pause` + `km resume` cycle.
    On resumed sandboxes (`/run` is tmpfs and is wiped on every boot; cloud-init
    does NOT re-run on second boot because the instance-id semaphore matches),
    `/run/km/audit-pipe` vanishes. km-presence (root) starts before km-audit-log
    (km-sidecar) finishes its `openAuditPipeWithRetry` and writes via
    `printf ... | timeout 0.1 tee /run/km/audit-pipe` — which CREATES the path
    as a regular `root:root 0644` file. km-audit-log then tries `os.OpenFile`
    with `O_RDWR` and gets `EACCES`, gives up after 10 retries, the audit
    pipeline is dead, and `km doctor presence_daemon_healthy` falsely WARNs.
    Discovered on sandbox `learn-14f484c7` (2026-05-16).

    Two-layer fix:

    | Layer | What | Where |
    |---|---|---|
    | 1 (root cause) | `/usr/lib/tmpfiles.d/km.conf` with `p+` recreates the FIFO at every boot, before `sysinit.target`, with `km-sidecar:km-sidecar 0666` ownership | `pkg/compiler/userdata.go` (heredoc inserted between the existing `mkfifo /run/km/audit-pipe` block and the `km-audit-log.service` unit heredoc) |
    | 2 (defense in depth) | `openAuditPipeWithRetry` detects a non-FIFO path (`info.Mode()&os.ModeNamedPipe == 0`), unlinks it via `os.Remove`, and recreates the FIFO via `syscall.Mkfifo` before retrying the open | `sidecars/audit-log/cmd/main.go::openAuditPipeWithRetry` |

    `p+` (not `p`) is critical — `p` is a silent no-op when a regular file
    occupies the path; `p+` clobbers it. Linux VFS semantics allow km-sidecar
    to `unlink` the root-owned regular file because km-sidecar owns the
    containing `/run/km` directory (no sticky bit set).

    **Rollout (matches Phase 79 pattern):**

    ```bash
    make build               # rebuild km binary (Layer 1 template change)
    make sidecars            # rebuild + upload audit-log sidecar (Layer 2)
    km init --sidecars       # refresh management Lambda's userdata template
    ```

    `make build` alone is insufficient — Layer 2 requires `make sidecars` so
    the new audit-log binary lands in S3 for new sandboxes to fetch. `km init
    --lambdas` is NOT needed (no Lambda code changes); `terragrunt apply` is
    NOT needed (no Terraform resource changes).

    **Not retroactive.** Existing sandboxes provisioned before Phase 79.1 do
    NOT have `/usr/lib/tmpfiles.d/km.conf` and have the old audit-log binary.
    `km destroy && km create` to roll the fix forward, OR apply the manual
    recovery snippet on the resumed sandbox:

    ```bash
    # Operator workaround for pre-Phase-79.1 sandboxes that have already
    # tripped the bug (audit pipe is a regular file, not a FIFO).
    km shell <sandbox-id>
    sudo rm /run/km/audit-pipe && \
      sudo mkfifo /run/km/audit-pipe && \
      sudo chown km-sidecar:km-sidecar /run/km/audit-pipe && \
      sudo chmod 666 /run/km/audit-pipe && \
      sudo systemctl restart km-audit-log
    ```

    Validation: after recovery, `journalctl -u km-audit-log` shows `reading
    from audit pipe pipe=/run/km/audit-pipe` (not `permission denied`), and
    `km doctor` reports `✓ Presence daemon healthy` within ~5 minutes of a
    real liveness signal (login shell, tmux attach, or agent process).

    See `.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-RESEARCH.md` for full design (incl. systemd-tmpfiles boot ordering, `p` vs `p+` semantics, Linux unlink permission model).
    ````

    Critical constraints:
    - DO NOT modify the existing Phase 79 prose, table, Migration block, Doctor check, Observability block, or Roll back block. All remain authoritative for the Phase 79 daemon itself.
    - The new subsection is purely additive (a fifth `###` under the Phase 79 `##` heading).
    - DO NOT use any emojis except the `✓` already present in the rest of the file and the existing `🤖` convention. Inline `✓` is acceptable; do not introduce others.
    - Verify by reading back the affected line range to confirm placement.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -c "Phase 79.1 follow-up" CLAUDE.md | grep -q "^1$" && grep -c "rm /run/km/audit-pipe" CLAUDE.md | grep -q "^1$" && grep -c "make sidecars" CLAUDE.md | xargs test 1 -le && echo "DOCS-INSERTED-OK"</automated>
  </verify>
  <done>
    `CLAUDE.md` contains exactly one `### Phase 79.1 follow-up: audit-pipe FIFO recreation on resumed sandboxes` subsection placed under `## Presence daemon (Phase 79)`. The subsection includes: bug summary, two-layer fix table, rollout code block (`make build && make sidecars && km init --sidecars`), retroactivity note, the manual recovery snippet from STATE.md, and a pointer to 79.1-RESEARCH.md. The grep automated verify returns `DOCS-INSERTED-OK`. The existing Phase 79 content is unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live UAT on AWS — km create + km pause + km resume cycle</name>
  <files>(none — live AWS interaction)</files>
  <what-built>
    Phase 79.1's complete two-layer fix:
    - Layer 1: `/usr/lib/tmpfiles.d/km.conf` heredoc in EC2 userdata (Plan 02)
    - Layer 2: `openAuditPipeWithRetry` self-heal in audit-log sidecar (Plan 03)
    - Operator documentation in CLAUDE.md (Plan 04 Task 1)
  </what-built>
  <action>
    PAUSE for operator. This checkpoint is a BLOCKING human-verification gate — Claude does NOT proceed to Task 3 until the operator pastes the captured UAT outputs (steps 5, 6, 7 from the script in `<how-to-verify>` below) OR describes specific failures for diagnosis. See `<how-to-verify>` for the exact pre-flight commands and the UAT script to run, and `<resume-signal>` for the format Claude expects when the operator returns.
  </action>
  <how-to-verify>
    Operator runs the UAT script from `.planning/phases/79.1-.../79.1-VALIDATION.md` against a live AWS sandbox. Captures all output. The phase does NOT close until step 5 shows `reading from audit pipe` with NO `permission denied`, step 6 shows `✓ Presence daemon healthy`, and step 7 shows `prw-rw-rw-` (FIFO mode).

    Required pre-flight (must be done by operator BEFORE running the UAT script):
    ```bash
    # From klankrmkr repo root, with klanker-application SSO active.
    make build               # rebuilds km with Layer 1 template change
    make sidecars            # rebuilds + uploads audit-log sidecar with Layer 2 code
    km init --sidecars       # refreshes management Lambda's userdata template + km binary

    # Sanity: confirm the new template is in the rendered userdata for a NEW sandbox.
    # (Will be verified end-to-end by the UAT script below.)
    ```

    Then run the UAT script verbatim:

    ```bash
    # 1. Provision a fresh post-Phase-79.1 sandbox
    km create profiles/learn.yaml --alias p79-1-test

    # 2. Confirm presence daemon healthy on fresh boot
    SB=$(km list | awk '/p79-1-test/ {print $1}')
    km shell $SB -- journalctl -u km-audit-log | grep "reading from audit pipe"
    # Expected: "reading from audit pipe pipe=/run/km/audit-pipe"

    km doctor 2>&1 | grep -A1 "Presence daemon healthy"
    # Expected: OK

    # 3. Pause + resume
    km pause $SB
    km resume $SB

    # 4. Wait for presence daemon to emit (60s tick + CloudWatch ingestion delay)
    sleep 90

    # 5. Assert audit-log opened the FIFO correctly (not permission denied)
    km shell $SB -- journalctl -u km-audit-log | grep -E "audit pipe|permission denied"
    # Expected: "reading from audit pipe pipe=/run/km/audit-pipe" (no "permission denied")

    # 6. Assert km doctor goes green
    km doctor 2>&1 | grep -A1 "Presence daemon healthy"
    # Expected: ✓ Presence daemon healthy (OK, not WARN)

    # 7. Verify /run/km/audit-pipe is a FIFO (not a regular file)
    km shell $SB -- ls -la /run/km/audit-pipe
    # Expected: prw-rw-rw- (p = FIFO, not -rw- = regular file)

    # 8. Cleanup
    km destroy $SB --remote --yes
    ```

    Capture the full output of steps 5, 6, and 7. These three outputs are the BLOCKING evidence that the fix works on a real resumed sandbox.

    If step 5 shows `permission denied` OR step 7 shows a regular file (`-rw-`), the fix did NOT take effect. Possible causes to debug:
    - `make sidecars` was skipped → old audit-log binary still in S3 (Layer 2 not applied)
    - `km init --sidecars` was skipped → management Lambda still rendering old template (Layer 1 not applied)
    - `km init --sidecars` ran but the Lambda hadn't picked up the new km binary yet (give it 30s and retry step 1)

    If step 6 still says WARN after step 5 confirms the FIFO is healthy, wait another 90s for CloudWatch ingestion and re-run step 6. CloudWatch ingestion can lag 1-2 minutes after the first km-presence event lands.
  </how-to-verify>
  <verify>Operator-only: blocking checkpoint, no Claude-automated command. Resume only when the operator pastes step 5/6/7 captures or describes failures (see `<resume-signal>`).</verify>
  <done>Operator has pasted UAT outputs from steps 5, 6, 7 showing: (5) `reading from audit pipe` with no `permission denied`; (6) `✓ Presence daemon healthy`; (7) `prw-rw-rw-` mode on `/run/km/audit-pipe`. OR operator has confirmed a diagnostic path was taken and the fix is now live.</done>
  <resume-signal>Type "approved" with the captured UAT output (steps 5, 6, 7) pasted into the chat OR describe specific failures with the journalctl/km doctor stderr so the assistant can diagnose.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write phase SUMMARY.md documenting all four plans + the UAT outcome</name>
  <files>.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-SUMMARY.md</files>
  <action>
    Create the phase summary file documenting the complete Phase 79.1 outcome. Use the project's existing phase-SUMMARY conventions (see other `.planning/phases/*/[NN]-SUMMARY.md` files for reference patterns). Include:

    1. **Phase metadata**: phase id (79.1), goal (one-sentence), start date, close date (today), depends-on (Phase 79).
    2. **Bug summary**: the exact race observed on `learn-14f484c7` (2026-05-16); the root cause (km-presence beats km-audit-log on resumed sandboxes; cloud-init does not re-run; /run is tmpfs).
    3. **Two-layer fix table**: Layer 1 (file + line + what), Layer 2 (file + line + what).
    4. **Plan-by-plan outcome table**: 79.1-01 (4 RED tests), 79.1-02 (Layer 1 GREEN), 79.1-03 (Layer 2 GREEN), 79.1-04 (docs + UAT).
    5. **Files modified** (combined across all plans, with line counts):
       - `pkg/compiler/userdata_phase79_1_test.go` (NEW, ~80 lines)
       - `sidecars/audit-log/cmd/main_test.go` (MODIFIED, +~25 lines)
       - `pkg/compiler/userdata.go` (MODIFIED, +~14 lines)
       - `sidecars/audit-log/cmd/main.go` (MODIFIED, ~+12 / ~-5 lines)
       - `CLAUDE.md` (MODIFIED, +~70 lines new subsection)
       - `.planning/ROADMAP.md` (MODIFIED — phase goal + plans populated by the plan-phase workflow)
    6. **Tests added**: list the four new tests by full name.
    7. **UAT outcome**: paste the captured outputs from steps 5, 6, and 7 of the Task 2 UAT. Mark them as evidence that UAT-RESUME criterion is satisfied. Include the date/time of the UAT and the sandbox alias used.
    8. **Retroactivity**: explicit statement that pre-Phase-79.1 sandboxes need `km destroy && km create` (or the manual recovery snippet).
    9. **Rollout commands** (operator-runnable, ready-to-paste): `make build && make sidecars && km init --sidecars`.
    10. **Lessons learned**:
        - Phase 79 closed without testing `km pause` + `km resume` — VERIFICATION should always include a lifecycle cycle test for any new boot-time daemon.
        - tmpfs + cloud-init-skip-on-resume is a recurring class of bug; future sandbox state in `/run` should always be paired with a tmpfiles.d drop-in OR a per-boot systemd oneshot.
        - `p+` vs `p` matters; document this pattern for future tmpfiles.d work.
    11. **Pointer to RESEARCH.md** for the design rationale.
    12. **Pointer to VALIDATION.md** for the test contract.

    No frontmatter required (matches existing phase SUMMARY conventions). Markdown only. Keep it concise but complete — this is the artifact future bug investigations will consult.
  </action>
  <verify>
    <automated>test -f "/Users/khundeck/working/klankrmkr/.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-SUMMARY.md" && grep -q "UAT-RESUME" "/Users/khundeck/working/klankrmkr/.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-SUMMARY.md" && grep -q "make build && make sidecars && km init --sidecars" "/Users/khundeck/working/klankrmkr/.planning/phases/79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file/79.1-SUMMARY.md" && echo "SUMMARY-OK"</automated>
  </verify>
  <done>
    `79.1-SUMMARY.md` exists in the phase directory with the structure above. It references all four plans, includes the rollout command, includes the UAT evidence captured in Task 2, includes the manual recovery snippet, and is ready as the artifact future bug investigations will consult.
  </done>
</task>

</tasks>

<verification>
1. CLAUDE.md docs subsection lands cleanly:
   ```bash
   grep -A3 "Phase 79.1 follow-up" CLAUDE.md | head -10
   ```
   Expected: subsection header + first prose paragraph visible.

2. Full repo test suite still green (regression guard across all Phase 79.1 changes):
   ```bash
   go test ./... -count=1 -timeout 120s 2>&1 | tail -20
   ```
   Expected: every package `ok`, zero `FAIL`.

3. UAT outputs captured (steps 5, 6, 7 from Task 2) prove the production behavior:
   - Step 5: journal contains `reading from audit pipe pipe=/run/km/audit-pipe`, no `permission denied` lines.
   - Step 6: `km doctor` shows `✓ Presence daemon healthy` for the test sandbox.
   - Step 7: `ls -la /run/km/audit-pipe` shows `prw-rw-rw-` (leading `p` = named pipe).

4. SUMMARY.md exists, references UAT outputs, and links the rollout command.
</verification>

<success_criteria>
- [ ] `CLAUDE.md` has the new `### Phase 79.1 follow-up` subsection inserted under `## Presence daemon (Phase 79)`.
- [ ] Existing Phase 79 prose, tables, and subsections are unchanged.
- [ ] Live AWS UAT on a fresh post-Phase-79.1 sandbox passes steps 5, 6, AND 7.
- [ ] `79.1-SUMMARY.md` is written and includes the UAT evidence + rollout command + manual recovery snippet + lessons learned.
- [ ] Full `go test ./...` suite remains green.
- [ ] Operator UAT artifacts (journalctl excerpts, km doctor output, ls -la output) are captured in the SUMMARY.
</success_criteria>

<output>
After completion:

1. Phase 79.1 is closed. ROADMAP.md status for Phase 79.1 should be updated to `Plans: 4/4 plans complete` (handled by the plan-phase or execute-phase workflow — not this plan's responsibility).

2. `79.1-SUMMARY.md` is the artifact future operators consult when they see the same symptom on a different boot-time daemon.

3. Recommended follow-on (for the operator, not Phase 79.1):
   - Audit other `/run/km/*` paths in the codebase for the same tmpfs-vanishes-on-resume pattern (`grep -rn "/run/km" pkg/compiler/userdata.go`). Anything in `/run/km` that is referenced by a systemd unit needs either a tmpfiles.d drop-in OR a km-bootstrap recreation step.
   - Add a Phase 79.X follow-up if any other paths are found vulnerable.
</output>
