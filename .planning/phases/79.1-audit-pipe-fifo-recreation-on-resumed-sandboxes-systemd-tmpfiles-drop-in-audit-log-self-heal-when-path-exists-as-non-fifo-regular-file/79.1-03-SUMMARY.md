---
phase: 79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file
plan: 03
subsystem: sidecar
tags: [audit-log, fifo, named-pipe, self-heal, systemd, go]

requires:
  - phase: 79.1-01
    provides: RED test sub-test "path exists as regular file - self-heal replaces with FIFO"
  - phase: 79.1-02
    provides: Layer 1 tmpfiles.d drop-in (parallel — same boot fix)

provides:
  - openAuditPipeWithRetry with three-branch self-heal (absent / correct-FIFO / wrong-type)
  - Detects and repairs regular-file-at-FIFO-path before open attempt
  - Logs "audit-log: path exists as non-FIFO, replacing" as operator-visible diagnostic

affects:
  - 79.1-04 (deployment — make sidecars required to ship this to running sandboxes)

tech-stack:
  added: []
  patterns:
    - "stat-before-mkfifo: three-branch pattern (absent / correct-type / wrong-type) for robust FIFO lifecycle management"

key-files:
  created: []
  modified:
    - sidecars/audit-log/cmd/main.go

key-decisions:
  - "Self-heal on wrong-type path uses os.Remove + syscall.Mkfifo (same tools already imported) — no new imports required"
  - "On os.Remove failure, record lastErr and let os.OpenFile fail naturally; the outer retry loop handles recovery — consistent with existing error-handling discipline"
  - "os.ModeNamedPipe check (not ModeType mask) directly from the os package constant — no io/fs import, matches Go codebase pattern"

patterns-established:
  - "three-branch stat check: IsNotExist → create, statErr==nil && not FIFO → remove+recreate, statErr==nil && is FIFO → fall through"

requirements-completed:
  - L2-SELFHEAL
  - L2-EXISTING-FIFO

duration: 3min
completed: 2026-05-16
---

# Phase 79.1 Plan 03: Layer 2 Audit-Log Sidecar Self-Heal Summary

**openAuditPipeWithRetry now unlinks and recreates wrong-type paths (regular file at FIFO path) before opening, turning the previously-doomed EACCES into a successful open**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-16T05:54:45Z
- **Completed:** 2026-05-16T05:55:32Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Replaced the 5-line single-branch stat guard with a three-branch self-heal in `openAuditPipeWithRetry`
- Branch (a): path absent — create fresh FIFO via `syscall.Mkfifo` (existing behavior preserved)
- Branch (b): path exists as correct FIFO — no-op, fall through to `os.OpenFile` (existing behavior preserved; L2-EXISTING-FIFO regression guard)
- Branch (c): path exists as wrong type — `os.Remove` + `syscall.Mkfifo` recovery, then fall through to `os.OpenFile` (new L2-SELFHEAL behavior)
- Fourth RED sub-test `path exists as regular file - self-heal replaces with FIFO` from Plan 01 now GREEN
- All three pre-existing sub-tests remain GREEN (`fresh tmpdir missing parent`, `pre-existing FIFO opens on attempt 1`, `persistent failure exhausts retries`)

## Modified Line Range

Lines 236-242 of `sidecars/audit-log/cmd/main.go` (old) replaced by lines 236-263 (new).
The edit spans 20 insertions, 2 deletions — only the stat+mkfifo guard block inside the retry loop's `else` branch.

## Diff (annotated)

```diff
-			// Create FIFO if missing (ignore EEXIST).
-			if _, statErr := os.Stat(pipePath); os.IsNotExist(statErr) {
+			// Phase 79.1 Layer 2 self-heal. Three cases:
+			//   (a) path absent: create fresh FIFO.
+			//   (b) path exists as FIFO: do nothing, fall through to OpenFile.
+			//   (c) path exists as wrong type (e.g., regular file left by
+			//       km-presence's `tee` racing this sidecar on resumed
+			//       sandboxes before Phase 79.1 Layer 1 lands): unlink and
+			//       recreate as FIFO. Layer 1 (systemd-tmpfiles.d) handles
+			//       this on every boot for new sandboxes; this self-heal is
+			//       defense in depth + recovery path for any future race.
+			if info, statErr := os.Stat(pipePath); os.IsNotExist(statErr) {
 				if mkfErr := syscall.Mkfifo(pipePath, 0666); mkfErr != nil && !os.IsExist(mkfErr) {
 					lastErr = mkfErr
 					log.Warn().Err(mkfErr).Int("attempt", attempt).Msg("audit-log: mkfifo failed")
 				}
+			} else if statErr == nil && info.Mode()&os.ModeNamedPipe == 0 {
+				log.Warn().Str("pipe", pipePath).Str("mode", info.Mode().String()).
+					Int("attempt", attempt).Msg("audit-log: path exists as non-FIFO, replacing")
+				if rmErr := os.Remove(pipePath); rmErr != nil {
+					lastErr = rmErr
+					log.Warn().Err(rmErr).Int("attempt", attempt).Msg("audit-log: remove non-FIFO failed")
+				} else if mkfErr := syscall.Mkfifo(pipePath, 0666); mkfErr != nil && !os.IsExist(mkfErr) {
+					lastErr = mkfErr
+					log.Warn().Err(mkfErr).Int("attempt", attempt).Msg("audit-log: mkfifo after remove failed")
+				}
 			}
```

## Test Results

| Sub-test | Status | Notes |
|---|---|---|
| `fresh tmpdir missing parent` | PASS | Regression guard — absent path branch |
| `pre-existing FIFO opens on attempt 1` | PASS | Regression guard — L2-EXISTING-FIFO: no re-creation |
| `persistent failure exhausts retries` | PASS | Regression guard — mkdir-blocked failure path |
| `path exists as regular file - self-heal replaces with FIFO` | PASS | NEW — L2-SELFHEAL criterion |

Full package test: `go test ./sidecars/audit-log/... -count=1 -timeout 60s` — ok (both packages)
`go vet ./sidecars/audit-log/...` — clean

## Task Commits

1. **Task 1: Replace stat+mkfifo guard with three-branch self-heal** - `dd39901` (feat)

**Plan metadata:** (final commit below)

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/sidecars/audit-log/cmd/main.go` — `openAuditPipeWithRetry` body: three-branch stat check replacing single-branch guard

## Decisions Made

- No new imports required: `os.ModeNamedPipe` is part of the already-imported `os` package
- `os.Remove` failure records `lastErr` but does NOT break the loop — the subsequent `os.OpenFile` fails naturally and the outer retry handles it (consistent with existing error-handling discipline)
- Log messages are operator-visible: `journalctl -u km-audit-log` will show self-heal events if they fire on a resumed sandbox

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Layer 2 (sidecar self-heal) is now complete alongside Layer 1 (tmpfiles.d drop-in from Plan 02)
- **Plan 04 reminder:** This change requires `make sidecars` (rebuild + S3 upload) to reach running sandboxes. Layer 1 alone (Plan 02, template-only) does not ship this binary fix. Both layers are needed for full defense-in-depth coverage.

## Self-Check: PASSED

- `sidecars/audit-log/cmd/main.go` — FOUND
- commit `dd39901` — FOUND in git log

---
*Phase: 79.1-audit-pipe-fifo-recreation-on-resumed-sandboxes-systemd-tmpfiles-drop-in-audit-log-self-heal-when-path-exists-as-non-fifo-regular-file*
*Completed: 2026-05-16*
