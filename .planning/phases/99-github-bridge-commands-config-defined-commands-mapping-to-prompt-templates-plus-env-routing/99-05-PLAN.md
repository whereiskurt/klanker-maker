---
phase: 99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing
plan: 05
type: execute
wave: 3
depends_on: ["99-01", "99-03"]
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/github.go
  - internal/app/cmd/doctor_github_commands_test.go
  - docs/github-bridge.md
autonomous: true
requirements: [GH-CMD-CONFIG, GH-CMD-HELP, GH-CMD-E2E]

must_haves:
  truths:
    - "km doctor reports a WARN/ERROR for each command @file that is missing/unreadable"
    - "km doctor reports unresolvable command profiles, a user-defined 'help' shadow, and command-alias ↔ repo-alias overlap"
    - "km doctor errors when any default_command (top-level or per-repo) names an undefined command"
    - "km doctor reports whether the SSM commands param is present when github.commands is configured"
    - "km github status lists configured commands (name + description + target) and the effective default command per repo"
    - "docs/github-bridge.md gains a Phase 99 command section with the deploy sequence (make build-lambdas + km init --dry-run=false; NO --sidecars, NO sandbox recreate)"
  artifacts:
    - path: "internal/app/cmd/doctor.go"
      provides: "checkGitHubCommandsValid + checkGitHubCommandsSSMParam appended to the existing GitHub doctor group; extends DetectGitHubAliasIssues for command↔repo alias overlap"
      contains: "checkGitHubCommands"
    - path: "internal/app/cmd/github.go"
      provides: "km github status command listing + per-repo effective default"
      contains: "commands"
    - path: "internal/app/cmd/doctor_github_commands_test.go"
      provides: "WARN tests for @file-missing/profile-unresolvable/help-shadow/alias-overlap/default-undefined/ssm-present + status listing"
      contains: "func TestDoctorGitHubCommands"
    - path: "docs/github-bridge.md"
      provides: "Phase 99 operator section + deploy-surface sequence"
      contains: "Phase 99"
  key_links:
    - from: "internal/app/cmd/doctor.go checkGitHubCommandsValid"
      to: "cfg.Github.Commands + DefaultCommand + repos"
      via: "config-only validation (no live AWS for the config checks)"
      pattern: "checkGitHubCommandsValid"
    - from: "internal/app/cmd/github.go RunGitHubStatus"
      to: "SSM {prefix}/config/github/commands"
      via: "read + display command listing"
      pattern: "config/github/commands"
---

<objective>
Add the operator-facing validation + visibility surface for GitHub bridge commands:
`km doctor` checks (every `@file` exists, command profiles resolvable, `help` not
shadowed, command-alias ↔ repo-alias overlap WARN, `default_command` names a defined
command, SSM commands param present when configured) and `km github status` command
listing + per-repo effective default. Plus the `docs/github-bridge.md` Phase 99
section and the explicit deploy-surface documentation/verification (no `--sidecars`,
no sandbox recreate; `make build-lambdas` + `km init --dry-run=false`).

Purpose: Operators can validate and inspect their command config; the deploy surface
is captured so this phase isn't bitten by the Phase 97 deploy-gap class (memory
`feedback_verify_deploy_surface_not_just_code`).
Output: doctor checks + status listing + docs + deploy-surface task, RED→GREEN.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-CONTEXT.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-RESEARCH.md
@docs/github-bridge.md

<interfaces>
<!-- VERIFIED live source. -->

internal/app/config/config.go — cfg.Github.Commands / DefaultCommand / repos[].DefaultCommand (Plan 01).
internal/app/cmd/doctor.go:1086 — checkGitHubReposResolvable(repos, defaultProfile) — profile-resolvable pattern to mirror.
internal/app/cmd/doctor.go:1163 — DetectGitHubAliasIssues(entries) — EXTEND for command-alias ↔ repo-alias overlap (98-03).
internal/app/cmd/doctor.go:1310 — checkGitHubAliasCollision(repos) — sibling check; append new checks AFTER it (~3285–3297 registration).
internal/app/cmd/doctor.go:250 — GetSsmPrefix() on the config adapter; ssmClient available in the doctor context (for the SSM-present check).
internal/app/cmd/github.go:170 — github SSM prefix = GetSsmPrefix()+"config/github/".
internal/app/cmd/github.go — RunGitHubStatus current output (webhook-secret/bot-login/bridge-url/app-client-id/installation-id) — EXTEND after it.

Doctor check registration pattern (RESEARCH doctor.go example):
```go
checks = append(checks, func(_ context.Context) CheckResult {
    return checkGitHubCommandsValid(cfg.Github.Commands, cfg.Github.DefaultCommand, githubRepos, githubDefaultProfile, configDir)
})
checks = append(checks, func(ctx context.Context) CheckResult {
    return checkGitHubCommandsSSMParam(ctx, ssmClient, cfg.GetSsmPrefix(), cfg.Github.Commands)
})
```
@file existence in doctor uses the config-dir base (same as Plan 03) — thread configDir in.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing doctor + status tests (Wave 0 RED)</name>
  <files>internal/app/cmd/doctor_github_commands_test.go</files>
  <action>
Create the test file FIRST. Pure config-check tests (no live AWS for the config checks;
use the existing fake SSM for the SSM-present check). Sub-tests under TestDoctorGitHubCommands*:
- TestDoctorGitHubCommandsAtFileMissing — a command with prompt "@nope.txt" → WARN/ERROR.
- TestDoctorGitHubCommandsProfileUnresolvable — command.profile pointing at a non-existent
  profile path → WARN (mirror checkGitHubReposResolvable expectations).
- TestDoctorGitHubCommandsHelpShadow — a user-defined `help` command → WARN.
- TestDoctorGitHubCommandsAliasOverlap — a command.alias equal to a repo.alias → WARN
  (assert via the extended DetectGitHubAliasIssues).
- TestDoctorGitHubCommandsDefaultUndefined — github.default_command (and a per-repo
  default_command) naming an undefined command → ERROR.
- TestDoctorGitHubCommandsSSMParam — fake SSM: param present → green; absent while
  commands configured → WARN.
- A clean config → all checks pass (no false positives).

TestGitHubStatusCommands — RunGitHubStatus (capture stdout, fake SSM holding a commands
doc) lists each command name + description + target, and prints the effective default per
repo (per-repo override beats install-wide). Reuse the existing github status test harness;
grep github_test.go for it.

RED: references the new check functions + extended status output → fails to compile.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestDoctorGitHubCommands|TestGitHubStatusCommands' -count=1 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists, references new doctor/status functions, fails to compile (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement doctor checks + km github status listing (GREEN)</name>
  <files>internal/app/cmd/doctor.go, internal/app/cmd/github.go</files>
  <behavior>
    - @file-missing, profile-unresolvable, help-shadow, alias-overlap, default-undefined,
      ssm-present checks all behave per the RED tests; clean config = no warnings.
    - km github status lists commands + per-repo effective default.
  </behavior>
  <action>
1. doctor.go — add `checkGitHubCommandsValid(commands, defaultCommand, repos, defaultProfile, configDir)`
   covering: every command `@file` exists/readable (config-dir base, mirror Plan 03);
   every command.profile resolvable (mirror checkGitHubReposResolvable); user-defined `help`
   → WARN (reserved built-in); every `default_command` (top-level + each repo) names a
   defined command → ERROR otherwise. Add `checkGitHubCommandsSSMParam(ctx, ssmClient,
   prefix, commands)` → WARN when commands configured but the SSM param absent. EXTEND
   DetectGitHubAliasIssues (doctor.go:1163) to also flag a command.alias colliding with a
   repo.alias (keep the 98-03 behavior intact; intentional shared explicit aliases stay
   un-warned per the 98 decision). Register both new checks in the GitHub doctor group
   after checkGitHubAliasCollision (~3285–3297).

2. github.go — extend RunGitHubStatus: after the existing output, read the SSM commands doc,
   parse, and print `commands (N):` with `  /name — description [→ alias/profile target]`
   per command, then per-repo effective default
   (`repo.default_command || github.default_command`, labeled install-wide vs per-repo).
   When no commands configured, print nothing extra (dormant — keep status output stable).
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestDoctorGitHubCommands|TestGitHubStatusCommands' -count=1</automated>
  </verify>
  <done>All doctor + status tests GREEN; `go build ./...` clean; existing 98-03 alias-collision tests still GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 99 commands + deploy surface in docs/github-bridge.md</name>
  <files>docs/github-bridge.md</files>
  <action>
Add a "Phase 99 — config-defined /commands" section to docs/github-bridge.md covering:
- the `github.commands:` config surface (copy the worked example from the design spec),
  `default_command` (install-wide + per-repo), `@file` prompts, `{{args}}`, the
  command-overrides-repo routing model, and the auth intersection.
- `/help` + the multi-command + not-authorized replies; lenient-unknown behavior.
- `km doctor` command-health checks and `km github status` command listing.
- DEPLOY SURFACE (explicit, per memory feedback_verify_deploy_surface_not_just_code):
  the ONLY deploy steps are `make build-lambdas` (rebuild the km-github-bridge zip) then
  `km init --dry-run=false` (uploads the bridge zip + writes the SSM commands param +
  drift WARN). State plainly: NO `km init --sidecars` (no SandboxProfile schema change),
  NO `make build`-gated new module (no new regionalModules() entry / DDB table / Lambda —
  km-github-bridge already exists from Phase 97 and is already in lambdaBuilds()), and
  NO `km destroy && km create` (no sandbox-side change). Note that removing the
  github.commands block on a later `km init` leaves a stale SSM param (mention the
  doctor SSM-present check + manual cleanup if desired).

This task IS the deploy-surface verification artifact: cross-check each claim against
Plan 03 (SSM write path + lambdaBuilds untouched) and Plan 04 (cold-start read) and
record any discrepancy in the SUMMARY rather than silently papering over it.
  </action>
  <verify>
    <automated>grep -q "Phase 99" docs/github-bridge.md && grep -q "make build-lambdas" docs/github-bridge.md && grep -qi "no .*sidecars\|NOT.*sidecars\|without --sidecars" docs/github-bridge.md && echo OK</automated>
  </verify>
  <done>docs/github-bridge.md has the Phase 99 command section + explicit deploy sequence stating no --sidecars / no sandbox recreate; deploy-surface claims cross-checked against Plans 03/04.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run 'TestDoctorGitHubCommands|TestGitHubStatusCommands' -count=1` GREEN.
- `go test ./internal/app/cmd/... -count=1` GREEN (no regression to 98-03 alias checks).
- `go build ./...` clean.
- docs/github-bridge.md documents the Phase 99 commands + the exact deploy surface.
</verification>

<success_criteria>
- km doctor: @file-missing, profile-unresolvable, help-shadow, alias-overlap, default-undefined, ssm-present checks live.
- km github status lists commands + per-repo effective default.
- docs/github-bridge.md Phase 99 section + deploy-surface sequence (make build-lambdas + km init; no --sidecars/recreate).
- GH-CMD-CONFIG (doctor), GH-CMD-HELP (status), GH-CMD-E2E (deploy/docs) covered.
</success_criteria>

<output>
After completion, create `.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-05-SUMMARY.md`
</output>
