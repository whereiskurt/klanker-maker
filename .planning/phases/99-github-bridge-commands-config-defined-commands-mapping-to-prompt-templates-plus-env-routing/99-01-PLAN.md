---
phase: 99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_github_commands_test.go
autonomous: true
requirements: [GH-CMD-CONFIG]

must_haves:
  truths:
    - "A github.commands: block in km-config.yaml round-trips through config.Load() into GithubConfig.Commands"
    - "github.default_command (install-wide) and repos[].default_command (per-repo) round-trip"
    - "Each GithubCommandEntry exposes Description, Alias, Profile, Allow, Prompt"
    - "Absent github.commands leaves GithubConfig.Commands nil/empty (dormancy preserved)"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "GithubCommandEntry struct + GithubConfig.Commands/DefaultCommand + GithubRepoEntry.DefaultCommand + getters"
      contains: "type GithubCommandEntry struct"
    - path: "internal/app/config/config_github_commands_test.go"
      provides: "UnmarshalKey round-trip + dormancy tests"
      contains: "func TestGithubConfigCommands"
  key_links:
    - from: "internal/app/config/config.go GithubCommandEntry"
      to: "viper UnmarshalKey(\"github\", &cfg.Github)"
      via: "mapstructure struct tags"
      pattern: "mapstructure:\"prompt\""
---

<objective>
Add the config-layer plumbing for GitHub bridge commands: a `GithubCommandEntry`
struct, `GithubConfig.Commands map[string]GithubCommandEntry`,
`GithubConfig.DefaultCommand`, and `GithubRepoEntry.DefaultCommand`, with getters
and a round-trip regression test. This is the shared config dependency that
`km init` (Plan 03), `km doctor`/`km github status` (Plan 05) consume.

Purpose: Operators define commands declaratively in `km-config.yaml`; this is the
struct surface that load + the three-edit config rule must satisfy.
Output: Extended `GithubConfig` with command fields + getters + RED→GREEN test.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-CONTEXT.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-RESEARCH.md

<interfaces>
<!-- Existing structs the executor extends (VERIFIED from live source). -->

internal/app/config/config.go:83 — existing GithubRepoEntry struct (gains DefaultCommand)
internal/app/config/config.go:107 — existing GithubConfig struct (gains Commands + DefaultCommand)
internal/app/config/config.go:484 — the SINGLE "github" entry in the v2→v merge-list loop
internal/app/config/config.go:607 — `v.UnmarshalKey("github", &cfg.Github)` decodes the WHOLE github: block

Recommended GithubCommandEntry shape (from RESEARCH.md Pattern 2):
```go
type GithubCommandEntry struct {
    Description string   `mapstructure:"description" yaml:"description,omitempty" json:"description,omitempty"`
    Alias       string   `mapstructure:"alias"       yaml:"alias,omitempty"       json:"alias,omitempty"`
    Profile     string   `mapstructure:"profile"     yaml:"profile,omitempty"     json:"profile,omitempty"`
    Allow       []string `mapstructure:"allow"       yaml:"allow,omitempty"       json:"allow,omitempty"`
    Prompt      string   `mapstructure:"prompt"      yaml:"prompt"               json:"prompt"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing config round-trip test (Wave 0 RED)</name>
  <files>internal/app/config/config_github_commands_test.go</files>
  <action>
Create the test file FIRST (TDD red-first). Mirror the existing config test style
in this package (table-driven where natural). Write a Go YAML fixture string
containing a full `github:` block with `default_profile`, `default_command`,
`repos:` (one entry with a per-repo `default_command`, one without), and a
`commands:` map of two commands (one inline prompt, one with `alias`+`profile`+`allow`).
Load it through the package's config.Load() path (use the same harness the existing
github config tests use — write the YAML to a temp file and load via the same
entry point used by `TestGithubConfig*`/repos tests; grep the package for the
existing github load-test helper and reuse it).

Assert:
- `func TestGithubConfigCommands` — cfg.Github.Commands has len 2; each command's
  Description/Alias/Profile/Allow/Prompt round-trip correctly; the inline-prompt
  command has empty Alias/Profile; the routing-override command has populated fields.
- `cfg.Github.DefaultCommand` == the install-wide value.
- the per-repo entry's `DefaultCommand` round-trips; the other repo's is empty.
- A dormancy sub-test: a `github:` block with NO `commands:` and NO
  `default_command` yields `len(cfg.Github.Commands) == 0` and empty DefaultCommand.

These will NOT compile yet (struct fields don't exist) — that is the RED state.
Do NOT add a second merge-list entry anywhere; the single existing "github" entry
(config.go:484) already covers the whole block via UnmarshalKey — this test proves it.
  </action>
  <verify>
    <automated>go test ./internal/app/config/... -run TestGithubConfigCommands -count=1 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists, references the new struct fields, and fails to compile (RED) because GithubCommandEntry / Commands / DefaultCommand do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GithubCommandEntry + command fields + getters (GREEN)</name>
  <files>internal/app/config/config.go</files>
  <behavior>
    - TestGithubConfigCommands passes: two commands round-trip with all fields.
    - DefaultCommand (install-wide) and per-repo DefaultCommand round-trip.
    - Dormancy sub-test passes: absent commands → empty map, no panic.
  </behavior>
  <action>
Implement the three-edit config rule MINUS the redundant merge-list edit:

1. STRUCT — Add `type GithubCommandEntry struct` (after GithubRepoEntry ~line 99)
   with the field/tag shape in the <interfaces> block above. Add to GithubConfig
   (config.go:107): `Commands map[string]GithubCommandEntry mapstructure:"commands" yaml:"commands,omitempty" json:"commands,omitempty"` and
   `DefaultCommand string mapstructure:"default_command" yaml:"default_command,omitempty" json:"default_command,omitempty"`.
   Add to GithubRepoEntry (config.go:83):
   `DefaultCommand string mapstructure:"default_command" yaml:"default_command,omitempty" json:"default_command,omitempty"`.

2. CONSTRUCTION — none needed beyond the existing `UnmarshalKey("github", &cfg.Github)`
   at config.go:607, which already populates the whole struct. No new cfg-construction line.

3. MERGE-LIST — **DO NOT ADD A NEW ENTRY.** Per project memory
   `project_config_key_merge_list`, the rule is "new km-config.yaml key needs a merge-list
   entry" — but RESEARCH.md finding #6 + config.go:484 confirms the single `"github"`
   merge-list entry already covers the ENTIRE github: block (Repos already round-trips
   this way). Adding `"github.commands"` would be a no-op or break parsing. Leave the
   merge-list untouched. Add a one-line code comment near the new fields stating this
   so a future reader doesn't "fix" it.

4. GETTERS — Add idiomatic getters consistent with existing GithubConfig accessors
   (grep for the existing `GetGithub*` / repo getters and match their style):
   e.g. a getter returning the Commands map and one returning DefaultCommand. If the
   existing code accesses cfg.Github fields directly (no getters), match that and skip
   getters — follow the prevailing pattern in this file rather than inventing one.

mapstructure tags are MANDATORY on every field (RESEARCH Pitfall 1 — viper's
UnmarshalKey ignores untagged fields silently).
  </action>
  <verify>
    <automated>go test ./internal/app/config/... -run TestGithubConfigCommands -count=1</automated>
  </verify>
  <done>TestGithubConfigCommands GREEN; `go build ./...` clean; no new merge-list entry added.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/config/... -count=1` passes (no regression to existing config tests).
- `go build ./...` clean.
- Round-trip proven for Commands, install-wide DefaultCommand, per-repo DefaultCommand.
- Dormancy: absent commands → empty map (no SSM write trigger downstream).
</verification>

<success_criteria>
- GithubCommandEntry struct exists with Description/Alias/Profile/Allow/Prompt + mapstructure tags.
- GithubConfig.Commands, GithubConfig.DefaultCommand, GithubRepoEntry.DefaultCommand exist + round-trip.
- No redundant merge-list entry (documented why in a code comment).
- TestGithubConfigCommands GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-01-SUMMARY.md`
</output>
