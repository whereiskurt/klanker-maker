---
phase: 99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing
plan: 03
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - internal/app/cmd/init.go
  - internal/app/cmd/init_github_commands_test.go
autonomous: true
requirements: [GH-CMD-FILEREF, GH-CMD-SSM]

must_haves:
  truths:
    - "A command prompt of the form @<path> is read at km init time, relative to the km-config.yaml directory, and inlined into the published JSON"
    - "A missing/unreadable @file is a HARD km init error (not a silent empty prompt)"
    - "@@ escape yields a literal @-prefixed inline prompt (no file read)"
    - "km init assembles the command map (with @file inlined) and writes it to SSM {prefix}/config/github/commands as a plain String"
    - "The SSM write is gated on len(cfg.Github.Commands) > 0 — absent commands ⇒ NO SSM param written (dormancy)"
    - "An env-wins drift WARN fires when the existing SSM value differs from the assembled JSON"
  artifacts:
    - path: "internal/app/cmd/init.go"
      provides: "command @file resolution + SSM commands publication + drift WARN, gated on len(commands)>0"
      contains: "config/github/commands"
    - path: "internal/app/cmd/init_github_commands_test.go"
      provides: "@file resolution (present/missing/@@) + SSM write/gate/drift tests with fake SSM"
      contains: "func TestResolveCommandPrompts"
  key_links:
    - from: "internal/app/cmd/init.go command @file resolver"
      to: "filepath.Dir(km-config.yaml path)"
      via: "config-dir-relative path resolution (NOT os.Getwd)"
      pattern: "filepath.(Dir|Join)"
    - from: "internal/app/cmd/init.go"
      to: "SSM {prefix}/config/github/commands"
      via: "putSSMParam String"
      pattern: "putSSMParam"
---

<objective>
Make `km init` resolve command `@file` prompts (operator-side) and publish the
assembled command set to SSM `{prefix}/config/github/commands` — the operator-side
half that feeds the bridge (Plan 04 reads this param at cold start). Mirror the
existing `KM_GITHUB_REPOS` gate + drift WARN and the `resolvePrompts()` `@file`
convention; gate the SSM write on `len(cfg.Github.Commands) > 0` for dormancy.

Purpose: The bridge Lambda never reads a filesystem; `@file` templates must be
inlined at init and shipped via SSM (D4/D8). Missing files fail loudly.
Output: init.go command-publication path + RED→GREEN tests with a fake SSM client.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-CONTEXT.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-RESEARCH.md

<interfaces>
<!-- VERIFIED live source the executor builds on. -->

internal/app/config/config.go — GithubConfig.Commands (added in Plan 01; map[string]GithubCommandEntry)
internal/app/cmd/create_prompt.go:82 — resolvePrompts() @file/@@ pattern to MIRROR:
  @@ → literal (strip one @); @ → os.ReadFile(path); missing → hard error; else literal.
  Phase 99 difference: path is relative to filepath.Dir(config-file), NOT os.Getwd().
internal/app/cmd/init.go:1066–1092 — KM_GITHUB_REPOS gate (len>0) + env-wins drift WARN to MIRROR.
internal/app/cmd/init.go:1877 — km-github-bridge already in lambdaBuilds() (no list edit needed).
internal/app/cmd/configure_github.go:357 — putSSMParam(ctx, client, name, value, paramType, kmsKeyID, overwrite).
  Usage: putSSMParam(ctx, ssmClient, cfg.GetSsmPrefix()+"config/github/commands",
         string(commandsJSON), ssmtypes.ParameterTypeString, "", true)  // plain String, NOT SecureString
internal/app/cmd/github.go:170 — confirms SSM github prefix is GetSsmPrefix()+"config/github/".

RESEARCH open-Q-1 (RESOLVE during this plan): confirm how to obtain the km-config.yaml
directory for @file base resolution. The viper instance exposes ConfigFileUsed(); locate
the init command's access to the loaded config path (grep init.go + config.Load for a
stored config-file path or a viper handle). If ConfigFileUsed() is not reachable from the
init context, thread the config path through from where Load() is called. The base dir is
filepath.Dir(<that path>). Document the resolved answer in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing @file-resolution + SSM-write tests (Wave 0 RED)</name>
  <files>internal/app/cmd/init_github_commands_test.go</files>
  <action>
Create the test file FIRST. Two groups:

TestResolveCommandPrompts (pure @file resolution helper):
- inline prompt (no @) → unchanged
- `@@literal` → "@literal" (escape, no file read)
- `@relative/path.txt` resolved against a tmp config-dir base → file contents inlined
- missing `@nope.txt` → hard error (assert err != nil, message references the path)
- assert the base dir is the CONFIG dir, not CWD: write the @file under a temp config dir,
  run the resolver with CWD set elsewhere (use a temp dir + the config-dir base param),
  and confirm it resolves from the config dir.

TestInitGitHubCommands (SSM publication with a fake SSM client — reuse the SSMWriteAPI
fake style already in this package; grep for existing fake SSM put tests):
- given cfg with 2 commands (one @file, one inline) → assembled JSON written to
  {prefix}config/github/commands as ParameterTypeString, Overwrite=true; the @file is
  inlined in the written value; assert the JSON shape matches what bridge.CommandEntry
  unmarshals (Description/Alias/Profile/Allow/Prompt keys).
- dormancy: cfg with NO commands → NO PutParameter call for the commands param.
- drift WARN: pre-seed the fake SSM with a DIFFERENT commands value → assert a WARN is
  emitted (capture stderr) and the new value is still written (env/SSM-wins semantics
  mirror KM_GITHUB_REPOS — write the yaml-derived value, WARN on prior divergence).

Reference the new helper/function names you will implement in Task 2. RED: won't compile.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestResolveCommandPrompts|TestInitGitHubCommands' -count=1 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists, references the new init command-publication functions, fails to compile (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement @file resolution + SSM commands publication (GREEN)</name>
  <files>internal/app/cmd/init.go</files>
  <behavior>
    - TestResolveCommandPrompts: inline/@@/@file/missing-error/config-dir-base all pass.
    - TestInitGitHubCommands: assembled JSON written to SSM as String; dormancy = no write;
      drift WARN fires on divergent prior value.
  </behavior>
  <action>
1. @FILE RESOLVER — Add a helper that takes a command map + the config-dir base and returns
   a resolved copy (or error). Mirror create_prompt.go:82 resolvePrompts semantics: `@@`→
   literal, `@path`→ ReadFile(filepath.Join(configDir, path)), missing → hard error, else
   inline. Resolve open-Q-1: derive configDir from the loaded config-file path (see
   <interfaces>); if not reachable, thread it from the Load() call site. NEVER os.Getwd().

2. SSM PUBLICATION — In the github init section of init.go (near the KM_GITHUB_REPOS block,
   ~line 1066), after resolving @file prompts: marshal the resolved command map to JSON
   (shape = map[string]{description,alias,profile,allow,prompt} matching bridge.CommandEntry
   json tags from Plan 02). Gate on `len(cfg.Github.Commands) > 0` (RESEARCH Pitfall 2 —
   absent commands ⇒ no SSM param). Read the current SSM value first; if present and != the
   assembled JSON, print an env-wins-style drift WARN to stderr (mirror init.go:1086). Then
   putSSMParam(ctx, ssmClient, cfg.GetSsmPrefix()+"config/github/commands", json,
   ssmtypes.ParameterTypeString, "", true). Plain String (config, not a secret).

3. Wire the @file resolution to FAIL the whole `km init` on a missing file (return the error
   up the init call chain) — do not swallow it.

Do NOT touch lambdaBuilds() — km-github-bridge is already listed (init.go:1877).
Do NOT add a regionalModules() entry — no new TF module / DDB table this phase.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestResolveCommandPrompts|TestInitGitHubCommands' -count=1</automated>
  </verify>
  <done>Both test groups GREEN; `go build ./...` clean; SSM write gated + drift WARN present; @file base = config dir.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run 'TestResolveCommandPrompts|TestInitGitHubCommands' -count=1` GREEN.
- `go build ./...` clean.
- Missing @file fails km init (hard error).
- Dormancy: absent commands ⇒ no SSM commands param written.
- Drift WARN fires on divergent prior SSM value.
</verification>

<success_criteria>
- @file prompts resolved at init relative to the km-config.yaml dir; missing = hard error; @@ escape works.
- Assembled command JSON published to SSM {prefix}/config/github/commands as plain String.
- SSM write gated on len(commands)>0; env-wins drift WARN present.
- GH-CMD-FILEREF + GH-CMD-SSM covered.
</success_criteria>

<output>
After completion, create `.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-03-SUMMARY.md`
</output>
