---
phase: 99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing
plan: 04
type: execute
wave: 2
depends_on: ["99-02"]
files_modified:
  - pkg/github/bridge/webhook_handler.go
  - pkg/github/bridge/interfaces.go
  - pkg/github/bridge/aws_adapters.go
  - pkg/github/bridge/webhook_handler_phase99_test.go
  - cmd/km-github-bridge/main.go
autonomous: true
requirements: [GH-CMD-AUTH, GH-CMD-HELP, GH-CMD-E2E]

must_haves:
  truths:
    - "CRITICAL DORMANCY: with h.Commands empty AND h.DefaultCommand empty, Handle() is byte-identical to Phase 98 — no command pass runs, no extra GitHub API calls, free-form dispatch only"
    - "A comment with exactly one known /command expands its template and dispatches with command-overrides-repo alias/profile; the {alias,profile,prompt} tuple feeds Phase 98 dispatch UNCHANGED"
    - "A comment with no command runs the effective default command when configured, else free-form passthrough"
    - "Two distinct known commands → one-at-a-time error reply, NO dispatch"
    - "A sender in repo.allow but failing command.allow → 'not authorized for /cmd' reply, NO dispatch"
    - "/help posts the command listing (name + description) plus the effective default for that repo; it is recognized BEFORE the defined-command lookup"
    - "An unknown/typo'd /token dispatches free-form with no reply"
    - "The bridge reads the command map from SSM {prefix}/config/github/commands at cold start (empty map when the param is absent)"
  artifacts:
    - path: "pkg/github/bridge/webhook_handler.go"
      provides: "command pass slotted after the dedupe gate (line ~241) and before envelope construction (line ~244); WebhookHandler gains Commands/DefaultCommand/Commenter fields"
      contains: "RunCommandPass"
    - path: "pkg/github/bridge/interfaces.go"
      provides: "CommentPoster (Commenter) interface + CommandsFetcher interface"
      contains: "PostComment"
    - path: "pkg/github/bridge/aws_adapters.go"
      provides: "SSMCommandsFetcher (15-min cache) + InstallationCommenter (token-mint POST comment)"
      contains: "SSMCommandsFetcher"
    - path: "cmd/km-github-bridge/main.go"
      provides: "cold-start wiring: eager Fetch of commands + Commenter + DefaultCommand env into WebhookHandler"
      contains: "SSMCommandsFetcher"
    - path: "pkg/github/bridge/webhook_handler_phase99_test.go"
      provides: "handler reply-path + dormancy tests with mockCommenter"
      contains: "func TestHandle_CommandsDormant"
  key_links:
    - from: "pkg/github/bridge/webhook_handler.go command pass"
      to: "Phase 98 dispatch (lines 265–388)"
      via: "modifies alias/profile/promptBody before envelope; dispatch unchanged"
      pattern: "promptBody = |alias = |profile = "
    - from: "cmd/km-github-bridge/main.go"
      to: "SSM {prefix}/config/github/commands"
      via: "SSMCommandsFetcher.Fetch at cold start → WebhookHandler.Commands"
      pattern: "SSMCommandsFetcher"
    - from: "pkg/github/bridge/aws_adapters.go InstallationCommenter"
      to: "GitHub issues/{n}/comments API"
      via: "mint App JWT → installation token (mirror InstallationReactor.AddReaction)"
      pattern: "ExchangeForInstallationToken"
---

<objective>
Wire the command pass into the bridge handler and the Lambda cold start. Add the
`SSMCommandsFetcher` (reads `{prefix}/config/github/commands`, 15-min cache, empty
map when absent), a `CommentPoster`/`Commenter` interface + `InstallationCommenter`
(POST a reply comment via the installation token, mirroring `InstallationReactor`),
and slot the pure `RunCommandPass` (Plan 02) into `Handle()` between the dedupe gate
(line ~241) and envelope construction (line ~244). The command pass only changes
`{alias, profile, promptBody}` — Phase 98's warm/resume/cold-create dispatch (lines
265–388) consumes the tuple UNCHANGED.

THE phase invariant lives here: absent `github.commands` ⇒ byte-identical Phase 98.

Purpose: Turn the pure command logic into live bridge behavior + the three reply paths.
Output: handler wiring + SSM reader + commenter + cold-start wiring + RED→GREEN handler tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-CONTEXT.md
@.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-RESEARCH.md
@docs/superpowers/specs/2026-06-07-github-bridge-commands-design.md

<interfaces>
<!-- VERIFIED live source — exact insertion point and patterns. -->

pkg/github/bridge/commands.go — RunCommandPass + result {Action: Dispatch|Reply|Deny|Passthrough, Alias, Profile, Prompt, ReplyText} (Plan 02). PURE — handler does the IO.
pkg/github/bridge/commands.go — bridge.CommandEntry (Plan 02; SSM JSON unmarshal target).
pkg/github/bridge/resolve.go:15 — RepoEntry.DefaultCommand (Plan 02) — per-repo default available via the matched entry.

pkg/github/bridge/webhook_handler.go:60 — WebhookHandler struct (add Commands map[string]CommandEntry,
  DefaultCommand string, Commenter CommentPoster, optionally CommandsFetcher CommandsFetcher).
pkg/github/bridge/webhook_handler.go:215 — Resolve(...) → alias, profile, allow, matched.
  NOTE: Resolve returns alias/profile/allow but NOT the per-repo default_command; to get
  repoDefaultCommand, also locate the matched RepoEntry (add a small lookup or extend Resolve's
  use — simplest: scan h.Entries for the matched fullName and read .DefaultCommand). Keep it minimal.
pkg/github/bridge/webhook_handler.go:221 — isInAllowlist(sender, allow) repo-allow gate (UNCHANGED — outer gate).
pkg/github/bridge/webhook_handler.go:228–241 — Step 7 dedupe.
pkg/github/bridge/webhook_handler.go:244 — promptBody := ExtractMentionBody(...) ← INSERT command pass BEFORE this.
pkg/github/bridge/webhook_handler.go:246–257 — GitHubEnvelope{...Body: promptBody...} (consumes the tuple).
pkg/github/bridge/webhook_handler.go:265–388 — warm/resume/cold dispatch (UNCHANGED).

pkg/github/bridge/aws_adapters.go:94 — SSMSecretFetcher (mirror for SSMCommandsFetcher; 15-min cachedValue).
pkg/github/bridge/aws_adapters.go (InstallationReactor.AddReaction ~line 823) — mint App JWT
  (pkggithub.GenerateGitHubAppJWT) → exchange (pkggithub.ExchangeForInstallationToken) → POST.
  InstallationCommenter.PostComment mirrors this: POST /repos/{owner}/{repo}/issues/{number}/comments.

cmd/km-github-bridge/main.go:128 — secretFetcher wiring; :133 botLoginFetcher; :142 readAppCredentials;
  :177 webhookHandler = &bridge.WebhookHandler{...}. Add commandsFetcher + eager Fetch + commenter +
  DefaultCommand (from a new KM_GITHUB_DEFAULT_COMMAND env, populated by Plan 03/05's init export OR
  read alongside — confirm with Plan 03; if install-wide default is shipped via SSM-doc metadata
  rather than env, read it from there. Decide + document; the SSM commands doc may carry the
  default_command as a sibling field to keep env untouched).

Insertion sketch (RESEARCH Pattern 3) — dormant-guarded:
```go
if len(h.Commands) > 0 || h.DefaultCommand != "" {
    repoDefaultCommand := lookupRepoDefault(h.Entries, payload.Repository.FullName)
    res := RunCommandPass(payload.Comment.Body, botLogin, h.Commands, h.DefaultCommand,
                          repoDefaultCommand, payload.Comment.User.Login,
                          alias, profile, h.DefaultProfile, allow)
    switch res.Action {
    case CommandActionReply, CommandActionDeny:
        _ = h.Commenter.PostComment(ctx, installID, owner, repo, number, res.ReplyText)
        return WebhookResponse{StatusCode: 200, Body: "ok"}
    case CommandActionDispatch:
        alias, profile, promptBody = res.Alias, res.Profile, res.Prompt
        // fall through to envelope construction WITHOUT the ExtractMentionBody line
    case CommandActionPassthrough:
        promptBody = ExtractMentionBody(payload.Comment.Body, botLogin)
    }
} else {
    promptBody = ExtractMentionBody(payload.Comment.Body, botLogin)
}
```
Ensure the dormant `else` path leaves Handle() byte-identical to today (do NOT call
the commenter, do NOT construct command state). When CommandActionDispatch sets promptBody,
make sure the existing line 244 `ExtractMentionBody` is not ALSO run (restructure so it's
exclusive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing handler reply-path + dormancy tests (Wave 0 RED)</name>
  <files>pkg/github/bridge/webhook_handler_phase99_test.go</files>
  <action>
Create the test file FIRST. Reuse existing helpers (handle_test.go: mockSecretFetcher,
mockBotLoginFetcher, mockNonceStore, mockResolver, mockPublisher, mockSQS, mockReactor;
webhook_handler_phase98_test.go: mockGitHubThreadStore, buildPayloadJSON, buildRequest,
defaultOpts). Add a NEW `mockCommenter` (records PostComment calls; mirror mockReactor).

Tests (handler with fakes):
- TestHandle_CommandsDormant — h.Commands nil/empty AND DefaultCommand "" → a mentioned
  comment dispatches free-form EXACTLY as Phase 98 (assert no PostComment call, envelope
  Body == ExtractMentionBody result, dispatch fires). This is the byte-identity guard.
- TestHandle_DefaultCommand — DefaultCommand set, command-less comment → effective default's
  template expanded ({{args}} = comment − mention), dispatched to the resolved alias/profile.
- TestHandle_MultiCommand — `@bot /patch /review` → PostComment called with a one-at-a-time
  error mentioning both; NO dispatch (assert SQS/Publisher NOT called).
- TestHandle_CommandNotAuthorized — sender in repo.allow but command.allow excludes them →
  PostComment "not authorized for /deploy"; NO dispatch.
- TestHandle_Help — `@bot /help` → PostComment listing commands (name + description) + the
  effective default for the repo; NO dispatch.
- TestHandle_UnknownToken — `@bot can you check the /api endpoint` (no known command) →
  free-form dispatch, NO PostComment.
- One known-command happy path — `@bot /patch fix login` → expanded template dispatched to
  command.alias||repo.alias / command.profile||repo.profile, {{args}}="fix login".

RED: references WebhookHandler.Commands/DefaultCommand/Commenter + RunCommandPass wiring
that don't exist yet → fails to compile.
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... -run 'TestHandle_(MultiCommand|CommandNotAuthorized|Help|UnknownToken|DefaultCommand|CommandsDormant)' -count=1 2>&1 | head -30</automated>
  </verify>
  <done>Test file + mockCommenter exist; reference the new handler fields; fail to compile (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Add CommentPoster + SSMCommandsFetcher + InstallationCommenter</name>
  <files>pkg/github/bridge/interfaces.go, pkg/github/bridge/aws_adapters.go</files>
  <action>
1. interfaces.go — add `CommentPoster` interface:
   `PostComment(ctx, installationID, owner, repo string, issueNumber int, body string) error`
   (call it Commenter on the handler). Add `CommandsFetcher` interface
   `Fetch(ctx) (map[string]CommandEntry, error)` for testability/cache refresh. Add the
   compile-time `var _ CommentPoster = (*InstallationCommenter)(nil)` check in the same
   style as existing interface assertions.

2. aws_adapters.go — add `SSMCommandsFetcher` mirroring SSMSecretFetcher (RESEARCH Pattern 1):
   struct {Client, Path, CacheTTL, mu, cache{commands map[string]CommandEntry, expiry}}.
   Fetch: lock, check cache, GetParameter WithDecryption=false (plain String); on
   ParameterNotFound return an EMPTY map (not nil, not error → dormant, RESEARCH Pitfall 3);
   json.Unmarshal into map[string]CommandEntry; cache + return.

3. aws_adapters.go — add `InstallationCommenter` mirroring InstallationReactor.AddReaction
   (~line 823): mint App JWT (pkggithub.GenerateGitHubAppJWT) → exchange for installation
   token (pkggithub.ExchangeForInstallationToken) → POST /repos/{owner}/{repo}/issues/{number}/comments
   with {"body": ...}. Parse owner/repo from the repo full name as the reactor does.
  </action>
  <verify>
    <automated>go build ./pkg/github/bridge/... ./cmd/km-github-bridge/...</automated>
  </verify>
  <done>CommentPoster + CommandsFetcher interfaces exist; SSMCommandsFetcher returns empty map on absent param; InstallationCommenter compiles with the compile-time assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Slot command pass into Handle() + cold-start wiring (GREEN)</name>
  <files>pkg/github/bridge/webhook_handler.go, cmd/km-github-bridge/main.go</files>
  <action>
1. webhook_handler.go — add fields to WebhookHandler: `Commands map[string]CommandEntry`,
   `DefaultCommand string`, `Commenter CommentPoster` (and optionally `CommandsFetcher`).
   Insert the dormant-guarded command pass between the dedupe gate (line ~241) and the
   envelope construction (line ~244), exactly per the <interfaces> sketch. Add the small
   `lookupRepoDefault(entries, fullName)` helper to read the matched RepoEntry.DefaultCommand.
   Restructure so ExtractMentionBody runs ONLY on the dormant/passthrough paths (not double-run
   on Dispatch). The Reply/Deny paths PostComment + return 200 with NO dispatch. Dispatch sets
   alias/profile/promptBody then falls through to the UNCHANGED envelope + dispatch (265–388).

2. main.go — after botLoginFetcher wiring (~line 137), add:
   `commandsFetcher := &bridge.SSMCommandsFetcher{Client: ssmClient, Path: "/"+prefix+"/config/github/commands", CacheTTL: 15*time.Minute}`
   then eager `commands, _ := commandsFetcher.Fetch(ctx)` (empty map if absent → dormant).
   Build an `InstallationCommenter` from the same appClientID/privateKeyPEM the reactor uses.
   Resolve the install-wide DefaultCommand source (env KM_GITHUB_DEFAULT_COMMAND OR a sibling
   field in the SSM commands doc — match what Plan 03 ships; coordinate + document). Set the
   new WebhookHandler fields (Commands, DefaultCommand, Commenter).

CRITICAL: when commands map is empty AND DefaultCommand "", the handler must take the dormant
path — TestHandle_CommandsDormant must prove byte-identity (no PostComment, free-form dispatch).
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... -run 'TestHandle_(MultiCommand|CommandNotAuthorized|Help|UnknownToken|DefaultCommand|CommandsDormant)' -count=1 && go build ./...</automated>
  </verify>
  <done>All Phase-99 handler tests GREEN; dormancy proven byte-identical; full build clean; existing Phase 97/98 handler tests still GREEN.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/github/bridge/... -count=1` GREEN (Phase 97/98 tests + new Phase 99 tests).
- `go build ./...` clean (bridge + main.go).
- Dormancy: TestHandle_CommandsDormant proves byte-identical Phase 98 behavior with no command config.
- The {alias,profile,prompt} tuple feeds the unchanged dispatch (no new dispatch path added).
</verification>

<success_criteria>
- WebhookHandler has Commands/DefaultCommand/Commenter; command pass slotted at line ~244.
- SSMCommandsFetcher reads the SSM doc at cold start (empty map when absent).
- InstallationCommenter posts the 3 replies (multi-command, not-authorized, /help) via installation token.
- /help recognized before defined-command lookup; unknown tokens lenient (free-form, no reply).
- DORMANCY invariant holds (byte-identical Phase 98 when no commands).
- GH-CMD-AUTH (handler), GH-CMD-HELP, GH-CMD-E2E (dormancy unit) covered.
</success_criteria>

<output>
After completion, create `.planning/phases/99-github-bridge-commands-config-defined-commands-mapping-to-prompt-templates-plus-env-routing/99-04-SUMMARY.md`
</output>
