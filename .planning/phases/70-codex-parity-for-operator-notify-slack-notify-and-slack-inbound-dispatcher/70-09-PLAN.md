---
phase: 70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher
plan: 09
type: execute
wave: 6
depends_on: [00, 01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-VERIFY.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8, SC-9, SC-10]

must_haves:
  truths:
    - "70-VERIFY.md is a signed UAT document with PASS/FAIL rows for each of the 10 Success Criteria from ROADMAP.md / SPEC.md"
    - "Operator runs the 9-step demo storyboard (SPEC.md) on real EC2 sandboxes — at least one `agent: claude` and one `agent: codex` sandbox, both with `notifySlackInboundEnabled: true`"
    - "The operator's existing `learn` sandbox (Codex pre-authenticated via AMI) is the Codex-side fixture; a fresh sandbox is created for the Claude side and for the cross-agent switch flow (so the switch can spawn a new top-level message in the same channel)"
    - "Every step's result is captured: PASS / FAIL / DEFERRED, with a one-line evidence note (screenshot path, command output, journald excerpt, or Slack message link)"
    - "Cross-agent switch verification (SC-10) explicitly tests: (a) NEW top-level appears in the same channel, (b) handoff post in OLD thread has the NEW permalink, (c) Codex's reply seeded with prior Claude assistant message lands in the NEW thread, (d) two DDB rows exist after the switch, (e) old Claude thread is still resumable (operator posts a no-prefix reply in the old thread; Claude resumes)"
    - "make build && make sidecars && km init --sidecars are executed before the UAT (required for the schema addition to propagate to management Lambdas + new km-slack binary to be uploaded to S3)"
    - "Any failed criterion either gets fixed in-session OR is documented in 70-VERIFY.md with a follow-up plan reference (e.g., 'failed — gap closure plan 70-10 needed')"
  artifacts:
    - path: ".planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-VERIFY.md"
      provides: "UAT verification record with 10 SC rows + 9 demo-step rows + operator sign-off"
      min_lines: 100
  key_links:
    - from: "70-VERIFY.md SC-1..SC-10 rows"
      to: "All Plan 70-01..70-08 implementations (the actual end-to-end behavior on a real sandbox)"
      via: "Manual operator verification of each success criterion's observable end-state"
      pattern: "PASS|FAIL|DEFERRED"
---

<objective>
Execute the 9-step end-to-end demo storyboard from SPEC.md on real EC2 sandboxes (using the operator's existing `learn` sandbox as the Codex fixture per CONTEXT.md) and capture the results in `70-VERIFY.md`. This is the verification surface for ALL 10 Success Criteria (SC-1..SC-10).

The 9 storyboard steps from SPEC.md map to success criteria as follows:

| Step | Storyboard scenario | Criteria covered |
|------|---------------------|------------------|
| 1 | Provisioning: `km create` creates both `~/.claude/settings.json` and `~/.codex/config.toml` | SC-1 |
| 2 | Operator-side Codex run: `km agent run --codex --prompt "What model are you?"` → operator email + Slack post via last_assistant_message fast-path | SC-2 |
| 3 | Permission notification: Codex triggers PermissionRequest, operator gets ping, run completes (auto-approve via --dangerously-bypass-approvals-and-sandbox) | SC-3 |
| 4 | Slack inbound first turn (codex sandbox): operator types in `#sb-x`, poller dispatches `codex exec --json`, DDB row written with agent_type=codex | SC-4 |
| 5 | Slack inbound multi-turn resume: follow-up message, `codex exec resume <id>` continues the session | SC-5 |
| 6 | `km doctor` runs `codex_hook_config_present` + `agent_type_consistency` green | SC-7 (+ SC-6 if KM_SLACK_THREAD_TS gating verified inline) |
| 7 | Top-level prefix on claude-default sandbox: `codex: list workspace files` spawns codex thread + new DDB row | SC-8 |
| 8 | Same-agent prefix is invisible: `codex: another question` in a codex thread → no handoff | SC-9 |
| 9 | Cross-agent mid-thread switch: `codex: check this` in a running claude thread → new top-level + handoff + seeded codex reply + 2 DDB rows + old session still resumable | SC-10 |

This plan is **autonomous: false** — the operator manually executes each step and confirms PASS/FAIL. The plan blocks on the operator's resume signal before declaring the phase complete.

CONTEXT.md re-emphasis:
- The operator's `learn` sandbox is the Codex-side fixture (Codex auth in AMI; survives `km destroy && km create`)
- The cross-agent switch flow needs a sandbox where both a Claude thread and a Codex thread can coexist — pick whichever profile the operator prefers; the spec accepts either direction (claude→codex switch or codex→claude switch)
- All Slack interactions require the bridge Lambda to be deployed (via `km init` from Phase 63) and the Slack workspace's Events API URL to point at the bridge's `/events` path (Phase 67 prerequisite)

Output:
- A populated `70-VERIFY.md` with 10 SC rows + 9 demo-step rows + operator sign-off line at the bottom
- If any criterion fails, a clear note pointing at the gap (e.g., "FAILED — Plan 70-10 needed to address X")
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-CONTEXT.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/SPEC.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-RESEARCH.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-VALIDATION.md

@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-00-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-01-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-02-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-03-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-04-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-05-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-06-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-07-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-08-SUMMARY.md

@docs/codex-parity.md

<interfaces>
<!-- Operator inputs the UAT needs ready:
- The `learn` sandbox alias must resolve (km list | grep learn). If not, the operator
  refreshes it (`km destroy learn --remote --yes && km create profiles/learn.yaml`).
- A Codex-agent profile (e.g., profiles/codex-uat.yaml) and a Claude-agent profile
  (existing profile in profiles/, just confirm it does NOT set spec.cli.agent or
  sets it to claude).
- Both profiles must have:
    notifyEmailEnabled: true
    notifySlackEnabled: true
    notifySlackPerSandbox: true
    notifySlackInboundEnabled: true
    notifyOnPermission: true
    notifyOnIdle: true
  ... so the full demo storyboard can run.
- Slack workspace must already have the bridge deployed (km slack status returns OK)
  AND the Events API URL must point at the bridge's /events path (Phase 67 setup).
-->

<!-- The UAT operator is the user. The agent's role is to:
1. Pre-flight: confirm `make build && make sidecars && km init --sidecars` have been run
2. Provide the UAT runbook (the 9 steps from SPEC.md, with concrete command lines)
3. Block on operator confirmation (PASS/FAIL/DEFERRED) for each step
4. Write the populated 70-VERIFY.md with operator's responses
-->

<!-- Failure-handling rule: if SC-2..SC-10 fails, the operator decides:
- Fix in-session (revert + retry; or small bash edit on the running sandbox; rebuild)
- File a gap closure plan (note in 70-VERIFY.md as DEFERRED with reason; planner runs /gsd:plan-phase 70 --gaps later)
- Sign off with caveat (PASS with note)

The agent does NOT auto-fix — the operator decides scope.
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator-driven UAT — execute 9-step demo storyboard on real EC2 sandboxes; populate 70-VERIFY.md</name>
  <files>.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-VERIFY.md</files>
  <action>
The operator manually executes the 9-step demo storyboard from SPEC.md (steps 1-9 mirror SC-1..SC-10 coverage per the table in <objective>). The agent provides the runbook (see <how-to-verify>), captures PASS/FAIL/DEFERRED responses with evidence, and writes the populated 70-VERIFY.md.

Pre-flight (confirm BEFORE starting step 1):
1. `make build && make sidecars && km init --sidecars` have been run AFTER all prior Plan 70 commits land
2. The operator's `learn` sandbox is alive OR will be refreshed from AMI
3. The Slack bridge is deployed (`km slack status` returns OK)
4. The operator has read docs/codex-parity.md

The agent does NOT auto-fix failures. For any FAILED criterion, the operator decides: fix in-session (small bash edit + retry), DEFER (note in 70-VERIFY.md + plan gap closure), or sign off with caveat. The agent surfaces any deviation from expected behavior immediately so the operator can decide scope.

When all 9 steps are complete (or the operator stops early), commit 70-VERIFY.md:
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(70): UAT verification record" --files .planning/phases/70-*/70-VERIFY.md
  </action>
  <verify>
    <automated>MISSING — Plan 70-09 is end-to-end manual UAT by design. The verify gate is: `test -f .planning/phases/70-*/70-VERIFY.md && grep -cE "(PASS|FAIL|DEFERRED)" .planning/phases/70-*/70-VERIFY.md | awk '$1 >= 19 {exit 0} {exit 1}'` — at least 19 status markers (10 SC rows + 9 demo step rows) must be populated.</automated>
  </verify>
  <done>70-VERIFY.md exists with PASS/FAIL/DEFERRED set on all 10 SCs and all 9 demo steps; operator sign-off line filled in; file is committed. If any criterion FAILED, the failure mode is described in the resume signal so /gsd:plan-phase 70 --gaps can pick it up next.</done>

  <what-built>
This is a manual operator-driven UAT. The "build" produced by all prior Plan 70-XX is now end-to-end on a live EC2 sandbox. The agent's role is to provide the runbook and capture PASS/FAIL for each step.

**Pre-flight (the agent confirms with the operator before the UAT begins):**
1. `make build && make sidecars && km init --sidecars` have been run AFTER all prior Plan 70 commits land — required for the Lambda + sidecar S3 uploads to reflect Phase 70 changes
2. The operator's `learn` sandbox is alive (alias `learn` resolves in `km list`) OR the operator confirms they will refresh it from the AMI before step 2
3. A Slack workspace with the bridge deployed is available (`km slack status` returns OK)
4. The operator has read `docs/codex-parity.md` (Plan 70-08) so they understand the cross-agent switch flow before testing it
  </what-built>

  <how-to-verify>
**Execute the 9-step UAT in sequence. After each step, the operator records PASS / FAIL / DEFERRED in `70-VERIFY.md` with a one-line evidence note.**

The agent populates `70-VERIFY.md` skeleton at the start of the session:

```markdown
# Phase 70 — Verification Record

**Operator:** {your name}
**Date executed:** {YYYY-MM-DD}
**Pre-flight:**
- [ ] `make build && make sidecars && km init --sidecars` confirmed run
- [ ] `learn` sandbox alive (Codex auth'd via AMI)
- [ ] Slack bridge deployed (`km slack status` returns OK)
- [ ] `docs/codex-parity.md` reviewed by operator

## Success Criteria (from ROADMAP.md / SPEC.md)

| SC | Behavior | Status | Evidence |
|----|----------|--------|----------|
| SC-1 | profile.cli.agent: codex → both ~/.claude/settings.json + ~/.codex/config.toml present | ⬜ | |
| SC-2 | km agent run --codex → Stop event last_assistant_message → email + Slack | ⬜ | |
| SC-3 | Codex PermissionRequest fires hook + auto-approves | ⬜ | |
| SC-4 | Slack inbound first turn → codex exec --json + DDB agent_type=codex | ⬜ | |
| SC-5 | Slack inbound resume → codex exec resume <id> continues session | ⬜ | |
| SC-6 | KM_SLACK_THREAD_TS gating works for Codex (poller silent, operator path posts) | ⬜ | |
| SC-7 | km doctor codex_hook_config_present + agent_type_consistency green | ⬜ | |
| SC-8 | Top-level codex: prefix on claude-default sandbox dispatches codex | ⬜ | |
| SC-9 | Same-agent prefix is invisible (no handoff, no new thread) | ⬜ | |
| SC-10 | Cross-agent mid-thread switch: new top-level + handoff + 2 DDB rows + old session resumable | ⬜ | |

## Demo Storyboard Steps

### Step 1: Provisioning (SC-1)
**Command:** `km create profiles/codex-uat.yaml --alias codex-uat`
**Verify on sandbox:**
```
km shell codex-uat -- cat ~/.claude/settings.json
km shell codex-uat -- cat ~/.codex/config.toml
km shell codex-uat -- env | grep KM_AGENT
```
**Expected:** Both files exist. `KM_AGENT=codex` in env. `~/.codex/config.toml` contains `codex_hooks = true` + PermissionRequest + Stop hook entries.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {fill in}

### Step 2: Operator-side Codex run (SC-2)
**Command:** `km agent run --codex --prompt "What model are you?" codex-uat --wait`
**Verify:** Operator email arrives with subject `[codex-uat-NNN] idle` and body containing the assistant reply (read from `last_assistant_message`, NOT from a transcript file). Same body appears in the per-sandbox Slack channel.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {email link / Slack message link / journald excerpt confirming the last_assistant_message fast-path fired}

### Step 3: Permission notification (SC-3)
**Setup:** Run a Codex turn that triggers a permission request — e.g., agent attempts to write outside cwd. One option: `km agent run --codex --prompt "Write a small test file to /etc/codex-uat-test.txt with content hello" codex-uat --wait`.
**Verify:** Operator email arrives with `[codex-uat-NNN] needs permission` subject + tool name in body. `--dangerously-bypass-approvals-and-sandbox` causes the run to complete despite the permission event.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {email + journald excerpt showing PermissionRequest fired and exit was 0 with empty stdout}

### Step 4: Slack inbound first turn (SC-4)
**Action:** Operator types in `#sb-codex-uat-NNN`: `list workspace files`
**Verify:** Within ~30 seconds, Codex's reply appears in the same Slack thread. Check DDB:
```
aws dynamodb get-item --table-name km-slack-threads \
  --key '{"channel_id":{"S":"<CHANNEL_ID>"},"thread_ts":{"S":"<TS>"}}' \
  --query 'Item.{agent:agent_type,session:claude_session_id,msg:last_assistant_msg}'
```
**Expected:** `agent_type=codex`, `claude_session_id` is a non-empty Codex session ID, `last_assistant_msg` is the truncated reply.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {DDB get-item output + Slack thread link}

### Step 5: Slack inbound multi-turn resume (SC-5)
**Action:** Operator types in the SAME Slack thread: `now show me README.md`
**Verify:** Reply lands in same thread. Codex's context carries forward (e.g., it references the earlier `list workspace files` result). Inspect DDB — `claude_session_id` unchanged (same session); `last_assistant_msg` updated; `last_turn_ts` updated.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {Slack thread link showing continuity}

### Step 6: km doctor + KM_SLACK_THREAD_TS gating (SC-6, SC-7)
**Command:** `km doctor 2>&1 | grep -E '(codex_hook|agent_type)'`
**Expected:** Both checks return green (OK) for the codex-uat sandbox.

Then run the same on a claude-default sandbox: `km doctor 2>&1 | grep -E '(codex_hook|agent_type)'` — the codex_hook check should return Skipped (no codex sandboxes match) OR OK (the codex config.toml is still present and well-formed on the claude sandbox — Plan 70-02 emits it unconditionally). No false-positive WARN.

**Also verify SC-6** by tailing journald on codex-uat during step 4's first turn: `journalctl -u km-slack-inbound-poller -f`. The Stop hook's Slack branch must STAY SILENT (KM_SLACK_THREAD_TS is set, so the hook script's existing gate suppresses the post; the poller does the post instead).

**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {km doctor output + journald excerpt}

### Step 7: Top-level prefix routing (SC-8)
**Setup:** A second sandbox, `claude-uat`, with `spec.cli.agent: claude` (or no agent field) and inbound enabled.
**Action:** Operator posts a new top-level message in `#sb-claude-uat-NNN`: `codex: list workspace files`
**Verify:** Codex dispatched (NOT claude — verify via journald). Reply lands in the same (new) thread. DDB row keyed on the new thread_ts has `agent_type=codex`. Profile's `KM_AGENT` on disk is unchanged: `km shell claude-uat -- grep KM_AGENT /etc/km/notify.env` still shows `KM_AGENT=claude`.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {journald + DDB get-item + grep output}

### Step 8: Same-agent prefix no-op (SC-9)
**Action:** In the SAME Codex thread from step 4 (`#sb-codex-uat-NNN`), operator posts: `codex: another question`
**Verify:** Reply lands in the same thread. No handoff message. No new top-level. Only ONE DDB row exists for this channel+thread. `claude_session_id` unchanged.
**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {DDB query showing one row only}

### Step 9: Cross-agent mid-thread switch (SC-10)
**Setup:** A running claude thread in `#sb-claude-uat-NNN` from a non-prefixed message (or from step 7 if you started a fresh Claude turn after the codex sub-thread).

Quick reset path if needed: post a no-prefix message first ("hello") to start a Claude thread, wait for the reply, then proceed.

**Action:** In the running Claude thread, operator posts: `codex: check the code change`

**Verify ALL of:**
1. **NEW top-level message** appears in `#sb-claude-uat-NNN` (NOT a thread reply). Body contains `Continuing from {permalink-to-old-thread}` and `Previous assistant (claude) said:` + truncated excerpt.
2. **Handoff post** in the OLD claude thread says `Switching to codex → continuing in this thread.` followed by the permalink to the NEW top-level.
3. **Codex's reply** to the seeded prompt (the user's `check the code change` + the prior assistant context) appears as the first reply in the NEW thread.
4. **Two DDB rows** exist now for `#sb-claude-uat-NNN`: the original claude row (unchanged) and a new codex row keyed on the NEW thread_ts. Verify:
   ```
   aws dynamodb query --table-name km-slack-threads \
     --key-condition-expression "channel_id = :c" \
     --expression-attribute-values '{":c":{"S":"<CHANNEL_ID>"}}'
   ```
5. **OLD Claude session still resumable.** Operator posts a reply in the OLD thread (NO prefix): `whats the next step?`. Claude responds, referencing the original context. DDB row for the OLD thread updated (new last_turn_ts, last_assistant_msg); claude_session_id UNCHANGED.

**Result:** ⬜ PASS / FAIL / DEFERRED
**Evidence:** {Slack thread links + DDB query output + permalink verification}

## Operator Sign-off

**All 10 SCs:** ⬜ PASS / FAIL / DEFERRED with notes

**Notes / follow-ups:**
{anything that needs a gap closure plan, e.g., "SC-10 step 5 failed — old session was killed; needs Plan 70-10"}

**Signed:** {operator name + date}
```

**As the operator works through the 9 steps, the agent:**
- Provides exact command lines (adapt sandbox aliases to whatever the operator uses)
- Captures pass/fail responses + evidence into `70-VERIFY.md`
- Surfaces any deviation from expected behavior immediately (does NOT auto-fix; the operator decides whether to attempt in-session repair or DEFER)
- For DEFERRED items, notes the gap precisely so a follow-up `/gsd:plan-phase 70 --gaps` run can build Plan 70-10

When all 9 steps are complete (or the operator stops early), commit `70-VERIFY.md`:
```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(70): UAT verification record" --files .planning/phases/70-*/70-VERIFY.md
```

Resume with `approved` when the operator has signed off (PASS or signed-with-caveats); or describe any FAILED criteria so the orchestrator can plan gap closure.
  </how-to-verify>

  <resume-signal>Type "approved" once 70-VERIFY.md is populated with PASS/FAIL/DEFERRED for all 10 SCs and the 9 demo steps, and is committed. If any criterion FAILED, describe the failure mode so /gsd:plan-phase 70 --gaps can pick it up next.</resume-signal>
</task>

</tasks>

<verification>
- `70-VERIFY.md` exists at `.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-VERIFY.md`
- All 10 SC rows have a Status (PASS / FAIL / DEFERRED) — none left ⬜
- All 9 demo step rows have a Status — none left ⬜
- Operator sign-off line is filled in (name + date)
- File is committed to git
- If any SC FAILED, the failure mode is described in the resume signal so gap closure can be planned
</verification>

<success_criteria>
- Phase 70's 10 Success Criteria are EITHER all PASS OR the failing criteria are documented as DEFERRED with clear follow-up scope
- The two test fixtures (codex-uat sandbox + claude-uat sandbox) demonstrate the dispatch fork, prefix routing, and cross-agent switch end-to-end
- Phase 70's deliverables match the demo storyboard from SPEC.md
- Plan 70-10 (gap closure), if needed, has a clear specification from the failure notes in 70-VERIFY.md
</success_criteria>

<output>
After completion, create `.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-09-SUMMARY.md` summarizing: (1) which SCs PASSED outright, (2) which FAILED + the gap closure scope, (3) which DEFERRED + the reason, (4) any deviations from the spec that surfaced during UAT (e.g., Codex field name differed; ordering required adjustment). The summary is the bridge between Phase 70 and any potential Phase 70 gap-closure or follow-up phase.
</output>
</output>
