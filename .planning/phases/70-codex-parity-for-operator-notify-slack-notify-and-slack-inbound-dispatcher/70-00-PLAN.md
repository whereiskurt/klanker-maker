---
phase: 70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-RESEARCH.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]

must_haves:
  truths:
    - "On the operator's existing learn sandbox (Codex already auth'd), a minimal ~/.codex/config.toml with a Stop hook + a PermissionRequest hook successfully fires the hook script via `codex exec --json --dangerously-bypass-approvals-and-sandbox`"
    - "The Stop hook's stdin payload includes a non-empty `session_id` field and a non-empty `last_assistant_message` field (or its actual field name)"
    - "The PermissionRequest hook fires on a permission-triggering prompt even though --dangerously-bypass-approvals-and-sandbox is set, and the hook's stdin payload carries the tool name (verify exact field name: `tool_name` vs `tool` vs other)"
    - "An exported KM_CODEX_RUN_ID env var visible in the poller-equivalent shell IS visible inside the hook subprocess (write it to a known file from the hook + read after)"
    - "The Codex resume syntax confirmed via `codex --help` on the AMI is `codex exec resume <session-id> <prompt>` (subcommand form), not `--resume` flag"
    - "Findings appended to 70-RESEARCH.md as a new `## Spike Findings (Plan 70-00)` section"
  artifacts:
    - path: ".planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-RESEARCH.md"
      provides: "Spike-confirmed Codex hook contract: exact payload field names, session-ID extraction path, resume syntax, KM_CODEX_RUN_ID visibility"
      contains: "Spike Findings"
      min_lines: 1
  key_links:
    - from: "70-RESEARCH.md § Spike Findings (Plan 70-00)"
      to: "Plans 70-02, 70-03, 70-05, 70-06"
      via: "downstream plans cite spike-confirmed field names and syntax in their actions"
      pattern: "Spike Findings"
---

<objective>
Eliminate Phase 70's only remaining technical-risk surface BEFORE any production code is written. The Plan 70-00 spike confirms — on the operator's existing `learn` sandbox (which already has Codex CLI authenticated via the baked AMI) — that:

1. `codex exec --json` fires the same hook system as the interactive TUI
2. The `Stop` hook's stdin payload contains the assistant reply directly (exact field name: `last_assistant_message` per SPEC.md prior, but spike confirms)
3. `session_id` is present in the `Stop` payload (so the hook can write it to a per-run file)
4. `PermissionRequest` fires even with `--dangerously-bypass-approvals-and-sandbox` (so notify-only flow works)
5. `KM_CODEX_RUN_ID` env var exported from the poller-equivalent shell is visible inside the hook subprocess
6. `codex exec resume <id> <prompt>` is the canonical 2026 syntax (not `--resume` flag)

This plan is throwaway code. It produces NO committed Go source, NO test stubs, NO sidecar binaries. Its only durable artifact is a `## Spike Findings (Plan 70-00)` section appended to `70-RESEARCH.md` listing the confirmed field names and syntax. Plans 70-02, 70-03, 70-05, 70-06 then cite those findings as their contract.

Purpose: SPEC.md and 70-RESEARCH.md repeatedly flag this as "strong-prior-probability-yes, but the spike eliminates the entire phase's risk surface before any code is written." This plan executes that risk elimination.

Output:
- A `## Spike Findings (Plan 70-00)` section appended to `70-RESEARCH.md` with:
  - Exact Codex hook payload field names (Stop: `last_assistant_message` confirmed/corrected; `session_id` confirmed/corrected; PermissionRequest: `tool_name` confirmed/corrected)
  - Confirmation of whether `--dangerously-bypass-approvals-and-sandbox` still fires `PermissionRequest`
  - Confirmation of `KM_CODEX_RUN_ID` env visibility inside the hook subprocess
  - Exact `codex exec resume` syntax from `codex --help`
  - Notes on any surprises (e.g., hook runs as which UID, file permissions on `/tmp/km-codex-session.*`)
- NO committed Go code from this plan
- NO Wave 0 test stubs (spike is pre-code; stubs land in Plans 70-01..70-07)
- The `learn` sandbox is left in whatever state the operator chooses (the test files in `/tmp/` and the toy `~/.codex/config.toml` are discarded)
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-CONTEXT.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-RESEARCH.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/SPEC.md

<interfaces>
<!-- The spike is run on the operator's existing `learn` sandbox, alias `learn`. -->
<!-- Reach it with `km shell learn` (interactive SSM shell as the sandbox user). -->
<!-- Codex is pre-authenticated in the AMI; `codex --version` should print a version. -->

<!-- Spike runbook (from SPEC.md § Risks & open research items + 70-RESEARCH.md § Plan 70-00 Spike Specification): -->

<!-- Step 1 — verify Codex installed + auth:
       codex --version
       codex --help | head -40                          # confirm `exec resume` subcommand syntax
-->

<!-- Step 2 — write minimal ~/.codex/config.toml on the sandbox:
[features]
codex_hooks = true

[[hooks.PermissionRequest]]
matcher = ".*"

[[hooks.PermissionRequest.hooks]]
type = "command"
command = "/bin/bash -c 'tee /tmp/codex-spike-permissionrequest.json > /dev/null; echo \"FIRED at $(date -u)\" >> /tmp/codex-spike.log'"
timeout = 30

[[hooks.Stop]]

[[hooks.Stop.hooks]]
type = "command"
command = "/bin/bash -c 'tee /tmp/codex-spike-stop.json > /dev/null; echo \"STOP_FIRED at $(date -u) RUN_ID=${KM_CODEX_RUN_ID:-unset}\" >> /tmp/codex-spike.log'"
timeout = 30
-->

<!-- Step 3 — run a basic prompt and a permission-triggering prompt:
export KM_CODEX_RUN_ID=spike01
codex exec --json --dangerously-bypass-approvals-and-sandbox "What is 2+2?"
codex exec --json --dangerously-bypass-approvals-and-sandbox "Write a file to /tmp/codex-spike-permission-test.txt with content hello"
-->

<!-- Step 4 — inspect captured payloads:
cat /tmp/codex-spike-stop.json | jq 'keys'
cat /tmp/codex-spike-stop.json | jq '.session_id, .last_assistant_message, .hook_event_name'
cat /tmp/codex-spike-permissionrequest.json | jq 'keys'
cat /tmp/codex-spike-permissionrequest.json | jq '.tool_name, .tool, .message'
cat /tmp/codex-spike.log
-->

<!-- Step 5 — confirm resume syntax + record session ID:
SID=$(jq -r '.session_id' /tmp/codex-spike-stop.json)
codex exec resume "$SID" "follow-up" --json --dangerously-bypass-approvals-and-sandbox
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual operator spike on the `learn` sandbox; append findings to 70-RESEARCH.md</name>
  <files>(operator drives — only .planning/phases/70-*/70-RESEARCH.md modified)</files>
  <action>
This is a manual operator-driven spike. No Go code, no test stubs. The operator:
1. Opens an interactive shell into the `learn` sandbox (`km shell learn`).
2. Writes the toy `~/.codex/config.toml` from the `<interfaces>` block above.
3. Sets `KM_CODEX_RUN_ID=spike01` and runs the two `codex exec --json --dangerously-bypass-approvals-and-sandbox ...` commands.
4. Inspects `/tmp/codex-spike-stop.json`, `/tmp/codex-spike-permissionrequest.json`, and `/tmp/codex-spike.log`.
5. Runs `codex --help` and `codex exec --help` to confirm the resume subcommand syntax.
6. Records findings in a NEW `## Spike Findings (Plan 70-00)` section appended to `70-RESEARCH.md`.

The sandbox is discarded after the spike (operator may `rm /tmp/codex-spike-*` and `rm ~/.codex/config.toml` or destroy and recreate from AMI — either is fine; the AMI carries the Codex auth).

The agent's role during this checkpoint is to (a) present the runbook (the `<how-to-verify>` block below), (b) wait on the operator's results, (c) help append findings into `70-RESEARCH.md`, (d) detect any HARD STOP signal (PermissionRequest doesn't fire with bypass; resume syntax differs materially from spec) and report it back so downstream plans can be revised before execution.
  </action>
  <verify>
    <automated>MISSING — Plan 70-00 is a pre-code spike by design. The verification surface is operator inspection of payload field names and resume-syntax output from the `learn` sandbox. The durable verify is: `grep -q "## Spike Findings (Plan 70-00)" .planning/phases/70-*/70-RESEARCH.md` (run AFTER operator updates the file).</automated>
  </verify>
  <done>Operator types "approved" once 70-RESEARCH.md has a Spike Findings section with the 8 table rows filled in. Any HARD STOP findings are described in the resume signal so the planner can revise downstream plans before they're executed.</done>

  <what-built>
1. The operator opens an interactive shell into the `learn` sandbox (`km shell learn`).
2. Writes the toy `~/.codex/config.toml` from the `<interfaces>` block above.
3. Sets `KM_CODEX_RUN_ID=spike01` and runs the two `codex exec --json --dangerously-bypass-approvals-and-sandbox ...` commands.
4. Inspects `/tmp/codex-spike-stop.json`, `/tmp/codex-spike-permissionrequest.json`, and `/tmp/codex-spike.log`.
5. Runs `codex --help` and `codex exec --help` to confirm the resume subcommand syntax.
6. Records findings in a NEW `## Spike Findings (Plan 70-00)` section appended to `70-RESEARCH.md`.

No Go code is written. No test stubs are created. The sandbox is discarded after the spike (operator may `rm /tmp/codex-spike-*` and `rm ~/.codex/config.toml` or destroy and recreate from AMI — either is fine; the AMI carries the Codex auth).
  </what-built>

  <how-to-verify>
The operator executes the spike runbook and updates `70-RESEARCH.md`. The expected end-state:

1. **Resume syntax confirmed.** `codex --help | grep -A 3 resume` shows the subcommand form. Record the exact invocation (e.g., `codex exec resume <SESSION_ID> "<PROMPT>"`). If it differs from SPEC.md's assumption, NOTE LOUDLY — downstream plans 70-05 and 70-06 must use the confirmed form.

2. **`Stop` hook payload field names.** `cat /tmp/codex-spike-stop.json | jq 'keys'` lists the field names. Confirm or correct:
   - `session_id` (the field the hook writes to `/tmp/km-codex-session.$KM_CODEX_RUN_ID`)
   - `last_assistant_message` (the field km-notify-hook reads for the Stop body; SPEC.md and 70-RESEARCH.md both assume this name)
   - `transcript_path` (the Claude-equivalent fallback — may be present or absent on Codex)
   - `hook_event_name` (should be `"Stop"`)

3. **`PermissionRequest` hook fires with bypass flag.** `cat /tmp/codex-spike.log` contains `FIRED` lines from the permission-triggering test. If it does NOT fire, that's a HARD STOP for Phase 70 — the notify-only flow assumes the hook fires; the operator must escalate (re-research SPEC.md § Risks bullet about `--dangerously-bypass-approvals-and-sandbox`).

4. **`PermissionRequest` payload field name for tool.** `cat /tmp/codex-spike-permissionrequest.json | jq 'keys, .tool_name, .tool'`. Confirm or correct: SPEC.md assumes `tool_name`; record whatever it actually is.

5. **`KM_CODEX_RUN_ID` env visibility.** `cat /tmp/codex-spike.log` lines should show `RUN_ID=spike01` (not `RUN_ID=unset`). If unset, the hook subprocess does NOT inherit exported env vars — downstream Plan 70-05's poller MUST use the inline `VAR=val sudo -u sandbox bash -lc "..."` pattern (per Pitfall 3 in 70-RESEARCH.md) rather than a separate `export`.

6. **Filename + UID for the session-ID file.** Note which UID the hook runs as (record `id -u` inside the hook to a separate file, or check ownership of `/tmp/codex-spike-stop.json`). Phase 67's poller runs as root and `sudo -u sandbox` for the Codex process — the hook needs to write to a path the sandbox user can write (or the file must be in `/tmp/` with world-writable bits).

**Append findings to `70-RESEARCH.md`** as a new section `## Spike Findings (Plan 70-00)` at the bottom of the file (BEFORE the `## Sources` section, AFTER the `## Plan 70-00 Spike Specification` section), formatted as a Markdown table:

```markdown
## Spike Findings (Plan 70-00)

**Date:** YYYY-MM-DD
**Sandbox:** learn (Codex version: <output of codex --version>)

| Question | Answer | Impact on downstream plans |
|----------|--------|----------------------------|
| Does `codex exec --json` fire Stop hook? | yes/no | If no — Phase 70 blocked; escalate. |
| Stop payload field for assistant reply | `last_assistant_message` / `<other>` | Plan 70-03 hook script uses this exact field name |
| Stop payload field for session ID | `session_id` / `<other>` | Plan 70-03 hook + Plan 70-05 poller read this exact field |
| PermissionRequest fires with --dangerously-bypass-approvals-and-sandbox? | yes/no | If no — Phase 70 blocked; permission notify path requires it |
| PermissionRequest payload field for tool name | `tool_name` / `<other>` | Plan 70-03 hook script reads this exact field |
| KM_CODEX_RUN_ID visible inside hook? | yes/no | Plan 70-05 uses `export` (yes) or inline `VAR=val sudo -u sandbox bash -lc` (no) |
| Resume syntax (from `codex --help`) | `codex exec resume <id> <prompt>` / `<other>` | Plan 70-05 poller and Plan 70-06 dispatch use this exact form |
| Hook UID | <uid> / sandbox / root | Plan 70-05 path for `/tmp/km-codex-session.$RUN_ID` writable by hook |

**Confirmed contract for Plan 70-05:** [one-line summary of the session-ID extraction mechanism]
**Surprises:** [anything unexpected — Codex CLI version quirks, payload field name differences, hook timing edge cases]
**Spike artifacts discarded:** /tmp/codex-spike-*.json, /tmp/codex-spike.log, ~/.codex/config.toml on the learn sandbox (no commit)
```

Confirm 70-RESEARCH.md is updated and changes are committed. Resume with `approved` to proceed; describe any issues found.
  </how-to-verify>

  <resume-signal>Type "approved" once `70-RESEARCH.md` has a Spike Findings section with the 8 table rows filled in. Describe any HARD STOP findings (PermissionRequest doesn't fire with bypass flag, or resume syntax differs materially from spec) so the planner can revise downstream plans before they're executed.</resume-signal>
</task>

</tasks>

<verification>
- A `## Spike Findings (Plan 70-00)` section exists in `70-RESEARCH.md` with confirmed field names and syntax
- The Spike Findings table has all 8 rows filled in
- No committed Go code, no test stubs
- The operator can articulate (in the resume signal) whether any downstream plan needs revision based on findings
</verification>

<success_criteria>
- Plans 70-02, 70-03, 70-05, 70-06 have an authoritative reference for: Codex Stop payload field names, PermissionRequest payload field name for tool, KM_CODEX_RUN_ID env visibility pattern, exact `codex exec resume` syntax
- Phase 70 risk-surface is eliminated before Wave 1 starts
- No throwaway code is committed to the repo
</success_criteria>

<output>
After completion, create `.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-00-SUMMARY.md` summarizing the spike findings (1-2 paragraphs) and explicitly listing which downstream plan actions cite the findings.
</output>
