---
phase: 70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - docs/codex-parity.md
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8, SC-9, SC-10]

must_haves:
  truths:
    - "New file docs/codex-parity.md is the operator guide for spec.cli.agent + Slack prefix routing + cross-agent switching"
    - "docs/codex-parity.md covers: profile field reference (agent enum), runtime behavior (KM_AGENT env, hook config.toml location, dispatch fork), Slack prefix grammar with worked examples, cross-agent switch demo storyboard, troubleshooting (km doctor checks, common drift symptoms)"
    - "docs/slack-notifications.md gains a new `## Prefix routing & agent switching` section with the same prefix grammar table + cross-agent switch flow as the demo storyboard"
    - "CLAUDE.md gains: (a) `agent: codex` field documented under profile schema notes, (b) `claude:` / `codex:` prefix grammar in the Slack inbound notes, (c) note on the `claude_session_id` column-name hangover (it now stores Codex session IDs too — agent-agnostic), (d) note about `km init --sidecars` required after this phase lands (schema addition propagates to management Lambdas)"
    - "Operator can read docs/codex-parity.md and execute Plan 70-09 UAT independently"
    - "Existing CLAUDE.md sections not regressed (additive only; preserve existing operator runbook content)"
  artifacts:
    - path: "docs/codex-parity.md"
      provides: "Operator-facing guide for Phase 70 features"
      min_lines: 200
    - path: "docs/slack-notifications.md"
      provides: "Phase 67/68 existing content + new Phase 70 section on prefix routing & agent switching"
      contains: "Prefix routing"
    - path: "CLAUDE.md"
      provides: "Operator runbook reference for agent: codex + prefix syntax + claude_session_id hangover note + km init --sidecars reminder"
      contains: "agent: codex"
  key_links:
    - from: "docs/codex-parity.md"
      to: "All Plan 70 implementation files (~/.codex/config.toml, km-notify-hook, km-slack-inbound-poller, km doctor)"
      via: "feature description + troubleshooting cross-references"
      pattern: "spec.cli.agent"
    - from: "CLAUDE.md"
      to: "docs/codex-parity.md"
      via: "Where to look table link"
      pattern: "codex-parity"
---

<objective>
Document the Phase 70 surface for operators. Three documents updated:

1. **`docs/codex-parity.md`** — NEW file. The primary operator guide for `spec.cli.agent`, the Slack prefix routing grammar, and the cross-agent thread switch flow. Includes:
   - Profile field reference (`spec.cli.agent: claude | codex` — default claude; absence ≡ claude)
   - Runtime behavior summary (KM_AGENT env var, `~/.codex/config.toml` file, dispatch fork in the poller)
   - Hook contract: PermissionRequest + Stop event names; `last_assistant_message` payload shape; `KM_NOTIFY_ON_PERMISSION` / `KM_NOTIFY_ON_IDLE` gates work identically for both agents
   - Slack prefix routing: grammar (`^claude:|^codex: ?`), case-insensitivity, anchored-at-start, same-agent no-op semantics
   - Cross-agent switch demo: walk through the 9-step flow from the demo storyboard with screenshots-as-text
   - Troubleshooting matrix: symptoms → causes → fixes (drift in `~/.codex/config.toml`, `agent_type` mismatch in DDB, `chat.update` failures, etc.)
   - `km doctor` reference for the two new checks

2. **`docs/slack-notifications.md`** — EXTEND existing file. Add a new top-level section `## Prefix routing & agent switching` covering the same content as the codex-parity doc but scoped to the Slack inbound surface (cross-link to codex-parity for the broader picture).

3. **`CLAUDE.md`** — EXTEND existing file. Add:
   - The `agent: codex` profile field to the implicit schema reference area (look for existing notes on `spec.cli.*` fields)
   - The `claude:` / `codex:` prefix grammar in the Slack inbound notes section
   - A note on the `claude_session_id` column-name hangover: the column is reused as the agent-agnostic session-ID storage (future agents — Goose etc — slot in as additional `agent_type` values without further schema work)
   - A reminder that `km init --sidecars` is required after this phase ships so management Lambdas pick up the schema addition

This plan is documentation-only — no code changes, no tests. Verification is a small bash check that the three files exist and contain required substring markers.

Output:
- New file `docs/codex-parity.md` (≥200 lines, operator-focused)
- Extended `docs/slack-notifications.md` with prefix routing section
- Extended `CLAUDE.md` with the four bullet points above
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-CONTEXT.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/SPEC.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-01-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-02-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-03-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-04-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-05-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-06-SUMMARY.md
@.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-07-SUMMARY.md

@docs/slack-notifications.md
@CLAUDE.md

<interfaces>
<!-- SPEC.md § Demo storyboard has the 9-step flow that docs/codex-parity.md should walk through.
     Reuse the same flow language for consistency. -->

<!-- The 10 success criteria from ROADMAP.md / SPEC.md are the structural backbone for
     the operator-facing feature reference. -->

<!-- CLAUDE.md existing structure (from the project root CLAUDE.md visible at top of session):
- ## Project (already says "Multi-instance support: ...")
- ## Where to look (table with operator entry points; ADD a "Codex parity & agent switching" row)
- ## CLI (km command reference; NO new top-level commands in Phase 70 — but `km validate` gets the new enum check; consider noting under km validate)
- ## Architecture (file/dir reference)
- ## SES per-install rule namespacing (Phase 84)
- ## Plan-before-apply destroy-class gate (Phase 84.2)
- ## Wrapper-level bootstrap UX (Phase 84.3)
- ## Network Enforcement
- ## Learn Mode
- ## Agent Execution (Phase 50/58 — add agent: codex note here)
-->

<!-- docs/slack-notifications.md likely structured by phase (Phase 63 sections, then Phase 67, then Phase 68).
     Add the new Phase 70 section at the end with the prefix routing content. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/codex-parity.md (new file)</name>
  <files>
    docs/codex-parity.md
  </files>
  <action>
Create `docs/codex-parity.md` with the following structure. Aim for ≥200 lines but prefer substance over filler.

```markdown
# Codex parity & agent switching

Phase 70 brings the Codex CLI to feature parity with Claude Code for operator notifications, Slack notifications, and bidirectional Slack chat. It also adds per-message Slack prefix routing and cross-agent mid-thread thread switching.

This document is the operator guide. For the design narrative, see `.planning/phases/70-*/SPEC.md`.

## What changed

| Concern | Before Phase 70 | After Phase 70 |
|---------|-----------------|----------------|
| Operator-notify hook for Codex | Claude-only | Both agents fire the same `/opt/km/bin/km-notify-hook` script |
| Slack notify hook for Codex | Claude-only | Both agents post to `#sb-{id}` via the same bridge |
| Slack inbound chat for Codex | Claude-only | Poller dispatches `codex exec` when `KM_AGENT=codex` |
| Per-message agent selection | Profile-only (one agent per sandbox) | Slack message prefix `claude:` / `codex:` overrides per-turn |
| Mid-thread agent switch | Not supported | Cross-agent prefix in an existing thread spawns a new top-level + handoff post |
| DDB schema | Claude-only `claude_session_id` | Same column reused agent-agnostic; new attrs `agent_type` + `last_assistant_msg` |
| Profile field | `spec.cli` had Claude-specific args only | New `spec.cli.agent: claude | codex` (default claude) |

## Profile schema

`spec.cli.agent` selects the default agent for **this sandbox**:

```yaml
spec:
  cli:
    agent: codex  # or "claude"; absence ≡ claude
    notifyEmailEnabled: true
    notifySlackEnabled: true
    notifySlackPerSandbox: true
    notifySlackInboundEnabled: true
    notifyOnPermission: true
    notifyOnIdle: true
```

Validation: `km validate <profile.yaml>` rejects any value other than `claude` or `codex` with a path-pointing error.

Absence ≡ `claude`. There is no way to declare "no agent" — every sandbox gets a default. The reason: `km shell` and `km agent run` still need a default when no `--claude` / `--codex` flag is passed.

## Runtime behavior

After `km create`, the sandbox carries:

- `/etc/profile.d/km-notify-env.sh` exports `KM_AGENT=claude` or `KM_AGENT=codex`. Interactive shells see it.
- `/etc/km/notify.env` has the same line in systemd `EnvironmentFile=` form. The `km-slack-inbound-poller` systemd unit reads from here.
- `~/.claude/settings.json` (already present from Phase 62/63) — unchanged.
- `~/.codex/config.toml` is NEW. Always installed regardless of `spec.cli.agent`. Contains:
  ```toml
  [features]
  codex_hooks = true

  [[hooks.PermissionRequest]]
  matcher = ".*"

  [[hooks.PermissionRequest.hooks]]
  type = "command"
  command = "/opt/km/bin/km-notify-hook PermissionRequest"
  timeout = 30
  statusMessage = "km: notifying operator"

  [[hooks.Stop]]

  [[hooks.Stop.hooks]]
  type = "command"
  command = "/opt/km/bin/km-notify-hook Stop"
  timeout = 30
  ```

  Claude-default sandboxes never start Codex, so the file is inert. Codex sandboxes have a one-stop hook target (the same bash script Claude uses).

- `/opt/km/bin/km-notify-hook` (already present from Phase 62) gains two small branches:
  - `PermissionRequest` event: synonym of Claude's `Notification`. Same gate var (`KM_NOTIFY_ON_PERMISSION`), same email/Slack body shape (tool name extracted from `.tool_name`).
  - `Stop` event payload-shape detection: prefers `.last_assistant_message` (Codex payload) when present; falls back to tailing `transcript_path` JSONL (Claude payload) when absent.

The hook script is **shared** — there's no per-agent fork. The only agent-specific behavior is which field on the Stop payload it reads.

## Slack inbound dispatch

When an operator posts in `#sb-{id}` against an inbound-enabled sandbox:

1. Bridge Lambda receives the Slack Events API webhook, verifies signature, dedupes, and enqueues to the sandbox's SQS FIFO queue (Phase 67 path).
2. `km-slack-inbound-poller` (systemd unit on the sandbox) picks up the message, looks up the DDB row, applies the **dispatch fork**:
   - Boot-time read: `AGENT=${KM_AGENT:-claude}` (from `/etc/km/notify.env`)
   - DDB lookup: existing row carries `agent_type` (default `claude` when absent)
   - **Prefix parser** (NEW Phase 70): see § Slack prefix routing
   - Routing decision: if prefix matched and differs from row's agent_type AND a session exists → cross-agent switch; otherwise EFFECTIVE_AGENT may be overridden in place
3. Dispatch executes:
   - Claude: `claude -p "$PROMPT" --output-format json --dangerously-skip-permissions [--resume $SESSION]` (unchanged from Phase 67)
   - Codex first turn: `codex exec --json --dangerously-bypass-approvals-and-sandbox "$PROMPT"`
   - Codex resume: `codex exec resume $SESSION "$PROMPT" --json --dangerously-bypass-approvals-and-sandbox` (subcommand form, not `--resume` flag)
4. Session ID captured:
   - Claude: parsed from `output.json` `.session_id` field
   - Codex: the Stop hook writes the session ID to `/tmp/km-codex-session.$RUN_ID` (via the `KM_CODEX_RUN_ID` env var the poller inlines into the `sudo -u sandbox bash -lc` command string)
5. DDB write-back: every successful turn updates `claude_session_id`, `agent_type`, `last_assistant_msg` (truncated to 2000 chars; JSON-escaped via `jq -Rs .` for safety).

## Slack prefix routing

A Slack message starting with `claude:` or `codex:` selects the agent for that turn.

### Grammar

Regex: `^([Cc]laude|[Cc]odex|CLAUDE|CODEX):[[:space:]]?`

- Case-insensitive on the agent name (`claude`, `Claude`, `CLAUDE` all match)
- Exactly **zero or one** space after the colon
- **Anchored at message start** (`^`) — no leading whitespace tolerated
- No tolerance for spaces before the colon (`claude :` doesn't match)
- Mid-sentence occurrences are ignored (`what does claude: mean?` won't trigger routing)

### Semantics

**Case 1 — no prefix:** dispatch the row's `agent_type` (or profile default for fresh thread). Existing Phase 67 behavior.

**Case 2 — top-level prefix on fresh thread (SC-8):**
```
[#sb-x channel, profile has spec.cli.agent: claude]
Operator: codex: list workspace files
```
Poller strips the prefix, dispatches `codex exec --json --dangerously-bypass-approvals-and-sandbox "list workspace files"`, writes a new DDB row keyed on the new `thread_ts` with `agent_type=codex`. The **profile's compiled `KM_AGENT` on disk is unchanged** — the override is per-thread only. Follow-up replies in this thread (no prefix) resume Codex.

**Case 3 — same-agent prefix in existing thread (SC-9):**
```
[Existing claude-rooted thread]
Operator: claude: do another thing
```
Poller strips the prefix and dispatches the **same agent** (`claude -p --resume $SESSION "do another thing"`). No new thread. No new DDB row. No handoff post. The prefix is a no-op signal.

**Case 4 — cross-agent prefix in existing thread (SC-10):** see § Cross-agent thread switch.

## Cross-agent thread switch

When a prefix names the **other** agent inside an existing thread (DDB row's `agent_type` differs from `requested_agent`), the poller orchestrates a clean handoff. The locked ordering fetches the OLD permalink FIRST (the OLD thread's `ts` is already known from the inbound SQS event), then posts the NEW top-level with that permalink already embedded in the body — no `<placeholder>` string is ever sent to Slack, and `chat.update` is not used in the critical path.

### Eight-step sequence

1. **Fetch the OLD-thread permalink** via `km-slack permalink --channel C --ts $OLD_THREAD_TS`. The OLD `thread_ts` is already known from the inbound SQS event, so this call has no dependency on any later step. Falls back to `(unavailable)` on Slack API hiccup.
2. **Build the new top-level body** with `$OLD_PERMALINK` already substituted in. The body is finalised BEFORE the post — no placeholder pattern reaches Slack. Body:
   ```
   Continuing from {old_permalink}

   Previous assistant ({old_agent}) said:
   > {first 500 chars of last_assistant_msg}
   ```
   The `{old_permalink}` token here is shorthand for the actual URL captured in step 1; the poller substitutes it at body-build time (see 70-06's `<interfaces>` bash block — `Continuing from $OLD_PERMALINK`).
3. **Post the NEW top-level message** in the same `#sb-x` channel via `km-slack post --new-message`. Capture the new message's `ts` from sidecar stdout into `NEW_TOP_TS`.
4. **Abort guard** — if `NEW_TOP_TS` is empty (post failed), post an error reply in the OLD thread, delete the SQS receipt, and skip dispatch. The OLD DDB row remains untouched; the operator can retry.
5. **Fetch the NEW-thread permalink** via `km-slack permalink --channel C --ts $NEW_TOP_TS`. Falls back to `(unavailable)` on failure.
6. **Post the handoff message** in the OLD thread via `km-slack post --thread $OLD_THREAD_TS`. Body:
   ```
   Switching to {new_agent} → continuing in this thread.
   {new_permalink}
   ```
7. **Compose the seeded prompt** by concatenating the stripped user prompt, a `--- Context from prior thread ---` separator, and up to 2000 chars of the old `last_assistant_msg`.
8. **Dispatch the new agent** as a fresh first turn (no `--resume`) into the NEW thread. Plan 70-05's existing put-item path writes the new DDB row keyed on `(channel_id, NEW_TOP_TS)` with `agent_type={new_agent}` (because the poller rewrote `THREAD_TS` to `NEW_TOP_TS` and `EFFECTIVE_AGENT` to `{new_agent}` before falling through to dispatch).

The OLD DDB row is **untouched** — no `aws dynamodb update-item` and no `aws dynamodb delete-item` call references the OLD `thread_ts` inside the cross-agent block. The OLD agent's session remains resumable — if the operator replies in the old thread (with or without prefix), the original session continues.

### Failure modes

- **OLD permalink retrieval fails** → embed `(unavailable)` placeholder in the new top-level body; journald log; both posts continue.
- **NEW top-level post fails** → poller aborts the switch, posts an error reply in the OLD thread, deletes the SQS message (no infinite retry).
- **NEW permalink retrieval fails** → embed `(unavailable)` placeholder in the handoff post; journald log; switch continues.
- **NEW DDB row write fails** → next reply in the NEW thread is treated as a first turn (acceptable two-turn-loss of continuity).

### Demo storyboard

See `.planning/phases/70-*/SPEC.md § Demo storyboard` for the 9-step end-to-end flow used by Plan 70-09 UAT.

## km doctor

Two new checks (Plan 70-07):

- `codex_hook_config_present` — for each sandbox with `spec.cli.agent: codex`, SSM RunCommand verifies `~/.codex/config.toml` exists, contains `codex_hooks = true`, and registers both PermissionRequest + Stop hooks. WARN on drift. Skipped when no codex sandboxes exist.
- `agent_type_consistency` — for each `km-slack-threads` row with `agent_type` set, fetches the sandbox profile from S3 and confirms it still declares the same agent. WARN on drift (catches profiles flipped post-create).

Run with `km doctor` (or `km doctor --all-regions` for multi-region installs).

## Troubleshooting

| Symptom | Likely cause | Fix |
|---------|--------------|-----|
| `km validate` rejects `agent: goose` | Schema enum only allows claude/codex | Use `claude` or `codex` (Goose / other-agent parity is deferred) |
| Codex hook never fires on `Stop` | `~/.codex/config.toml` missing or malformed | Check `km doctor` → `codex_hook_config_present` |
| Codex session doesn't resume across turns | Session ID not captured in DDB | Look for `KM_CODEX_RUN_ID` and `/tmp/km-codex-session.*` in `journalctl -u km-slack-inbound-poller` |
| `claude:` mid-sentence triggers routing | Should not happen with `^` anchor | File a bug — the regex MUST anchor; see Plan 70-06's TestPoller_PrefixParser_AnchoredAtStart |
| Cross-agent switch shows `Continuing from (unavailable)` | Slack `chat.getPermalink` failed | Transient; retry, or check Slack API status |
| OLD thread doesn't resume after switch | OLD DDB row was modified | Should not happen — the cross-agent block never calls update-item or delete-item on the OLD row |
| `agent_type_consistency` WARN on `sb-x` | Profile flipped from claude to codex (or back) post-create | `km destroy sb-x && km create profile.yaml` to align |

## Hangover: `claude_session_id` column

The DynamoDB column `claude_session_id` now stores **either** a Claude session ID or a Codex session ID, based on the row's `agent_type`. The column name is a Phase 67 hangover — renaming requires a migration job we explicitly chose not to run (cosmetic).

Future agents (Goose, etc.) reuse this column. Adding a new agent means adding a new enum value to `agent_type` and a new dispatch branch in the poller.

## Phase 70 deferrals

These are explicitly OUT of Phase 70 scope (see CONTEXT.md `<deferred>`):

- **Transcript streaming for Codex** (Tier 3, follow-up phase). When it lands, `~/.codex/config.toml` adds a `[[hooks.PostToolUse]]` entry; everything else is the JSONL parser.
- **Slack-driven approve/deny on Codex PermissionRequest** — Slack interactivity webhook into the bridge; phase of its own.
- **Goose / other-agent parity** — `agent_type` extends cleanly; one new enum value + one new dispatch branch.
- **Auto-routing by content heuristics** — no "this looks like code → codex" classifier. Routing is explicit.
- **Thread merge / rejoin after switch** — no mechanism. Two threads run in parallel after a switch.

## See also

- `docs/slack-notifications.md` — Slack-side runbook (Phases 63/67/68)
- `OPERATOR-GUIDE.md` — full operator runbook
- `CLAUDE.md` — terse operator reference
- `.planning/phases/70-*/SPEC.md` — full design narrative + risk register
- `.planning/phases/70-*/70-VERIFY.md` — UAT results from Plan 70-09
```

Verify the file exists and has at least the required key markers:
```
test -f docs/codex-parity.md && \
  grep -q "spec.cli.agent" docs/codex-parity.md && \
  grep -q "Cross-agent thread switch" docs/codex-parity.md && \
  grep -q "claude_session_id" docs/codex-parity.md && \
  grep -q "km doctor" docs/codex-parity.md
```
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f docs/codex-parity.md && grep -q "spec.cli.agent" docs/codex-parity.md && grep -q "Cross-agent thread switch" docs/codex-parity.md && grep -q "claude_session_id" docs/codex-parity.md && grep -q "km doctor" docs/codex-parity.md && wc -l docs/codex-parity.md | awk '$1 >= 150 {exit 0} {exit 1}'</automated>
  </verify>
  <done>docs/codex-parity.md exists with ≥150 lines and contains the required substring markers (spec.cli.agent, Cross-agent thread switch, claude_session_id, km doctor).</done>
</task>

<task type="auto">
  <name>Task 2: Extend docs/slack-notifications.md with prefix routing & agent switching section; extend CLAUDE.md with Phase 70 references</name>
  <files>
    docs/slack-notifications.md,
    CLAUDE.md
  </files>
  <action>
**1. Extend `docs/slack-notifications.md`** — Append a new top-level section at the end of the file (do not modify existing sections):

```markdown
## Prefix routing & agent switching (Phase 70)

Phase 70 adds two related features to the Slack inbound flow:

1. **Per-message prefix routing** — a Slack message starting with `claude:` or `codex:` selects the agent for that turn (overriding the profile default).
2. **Cross-agent mid-thread switching** — a prefix in an existing thread that names the *other* agent spawns a new top-level message with a clean handoff post in the original thread.

For the full design, see `docs/codex-parity.md`. This section is the Slack-scoped quick reference.

### Prefix grammar

Regex: `^([Cc]laude|[Cc]odex|CLAUDE|CODEX):[[:space:]]?`

- Case-insensitive on agent name
- Anchored at message start (mid-sentence `claude:` is ignored)
- Exactly zero or one space after the colon

### Behavior matrix

| Scenario | Result |
|----------|--------|
| No prefix, existing thread | Dispatch the row's agent (Phase 67 unchanged) |
| `codex: ...` on a fresh top-level message in a claude-default sandbox | Codex dispatched; new DDB row with `agent_type=codex`; profile `KM_AGENT` unchanged |
| `claude: ...` inside an existing claude-rooted thread | No-op switch: strip prefix, same dispatch, same thread, no handoff |
| `codex: ...` inside an existing claude-rooted thread | Cross-agent switch: new top-level + handoff post + new DDB row + new Codex first turn |

### Cross-agent switch artifacts

In the **OLD** thread, after a switch:
```
Switching to codex → continuing in this thread.
https://workspace.slack.com/archives/C12345/p1716393742000300
```

In the **NEW** top-level message:
```
Continuing from https://workspace.slack.com/archives/C12345/p1716393640000200

Previous assistant (claude) said:
> {first 500 chars of last_assistant_msg from the old DDB row}
```

The new agent's prompt seed (NOT posted to Slack — passed via stdin to the agent CLI) is:
```
{stripped_prompt}

--- Context from prior thread (agent: claude) ---
{up to 2000 chars of last_assistant_msg}
```

### km-slack sidecar additions

Plan 70-04 adds three surfaces to the existing `km-slack` sidecar:

- `--new-message` flag on `km-slack post`: omits `thread_ts`, returns `ts=...` to stdout
- `km-slack permalink --channel C --ts T`: wraps `chat.getPermalink`
- `km-slack update --channel C --ts T --text "..."`: wraps `chat.update` (subject to Slack's 10-min bot edit window; not in the cross-agent critical path)

All three go through the bridge Lambda via signed envelopes; sandboxes never touch a raw Slack bot token.

### km doctor checks (Phase 70)

- `codex_hook_config_present` — verifies every codex-configured sandbox has a well-formed `~/.codex/config.toml`. WARN on drift.
- `agent_type_consistency` — verifies every `km-slack-threads` row's `agent_type` matches the corresponding profile. WARN on drift (catches post-create profile flips).
```

**2. Extend `CLAUDE.md`** — Make four additive edits to preserve existing content:

**Edit A — Add a row to the "Where to look" table** (find the section starting `## Where to look`):

Insert before the existing operator-guide row (or in a sensible position alphabetically / by phase):
```markdown
| Codex parity + Slack prefix routing & agent switching | `docs/codex-parity.md` (Phase 70) |
```

**Edit B — Add a `## Agent selection` subsection** (or extend the existing `## Agent Execution` section):

```markdown
### Agent: claude | codex (Phase 70)

`spec.cli.agent` selects the default agent for `km shell` / `km agent run` / Slack inbound dispatch:

```yaml
spec:
  cli:
    agent: codex  # or "claude"; default claude; absence ≡ claude
```

The compiler writes `~/.codex/config.toml` on every sandbox (regardless of value — Claude-default sandboxes have an inert config). The poller and CLI read `KM_AGENT` from `/etc/km/notify.env` at boot.

Per-turn override via Slack prefix: a message starting with `claude:` or `codex:` selects the agent for that turn. See `docs/codex-parity.md` for the cross-agent switch flow.

**Important:** `km init --sidecars` is required after this phase ships so the management Lambdas pick up the schema addition. Existing sandboxes don't pick up `agent: codex` retroactively — `km destroy && km create`.
```

**Edit C — Add the `claude_session_id` hangover note**:

Place this either in the Architecture section, near the DynamoDB table listing, or as a small subsection under Agent Execution:

```markdown
### DDB column hangover

The `km-slack-threads.claude_session_id` column (Phase 67) now stores agent-agnostic session IDs — either a Claude session ID or a Codex session ID, based on the row's `agent_type` (Phase 70). The column name is cosmetic; renaming would require a migration job we chose not to run.

Future agents (Goose etc.) slot in as new `agent_type` enum values without further DDB schema work.
```

**Edit D — Note in CLI section (optional, near `km validate`)**:

```markdown
- `km validate <profile.yaml>` — validate a SandboxProfile (Phase 70: `spec.cli.agent` enum validated against `claude` | `codex`)
```

Adjust the exact existing line; the change is appending the parenthetical, not duplicating the bullet.

**3. Verify**
```
cd /Users/khundeck/working/klankrmkr && \
  test -f docs/slack-notifications.md && grep -q "Prefix routing & agent switching" docs/slack-notifications.md && \
  test -f CLAUDE.md && grep -q "spec.cli.agent" CLAUDE.md && grep -q "claude_session_id" CLAUDE.md && grep -q "km init --sidecars" CLAUDE.md
```
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f docs/slack-notifications.md && grep -q "Prefix routing & agent switching" docs/slack-notifications.md && test -f CLAUDE.md && grep -q "spec.cli.agent" CLAUDE.md && grep -q "claude_session_id" CLAUDE.md && grep -q "km init --sidecars" CLAUDE.md</automated>
  </verify>
  <done>docs/slack-notifications.md has the new Phase 70 section; CLAUDE.md has the four bullet points (spec.cli.agent, prefix grammar, claude_session_id hangover, km init --sidecars reminder); no existing content removed.</done>
</task>

</tasks>

<verification>
- `docs/codex-parity.md` exists with ≥150 lines and required substring markers
- `docs/slack-notifications.md` has the new "Prefix routing & agent switching" section
- `CLAUDE.md` references `spec.cli.agent`, `claude_session_id`, and `km init --sidecars`
- No existing documentation content removed; all edits are additive
- `git diff --stat` shows exactly three files changed (modulo formatting normalization)
</verification>

<success_criteria>
- Operators can find Phase 70 documentation by searching the repo or starting from `CLAUDE.md`'s "Where to look" table
- Plan 70-09 UAT operator can read `docs/codex-parity.md` for context before executing the 9-step demo storyboard
- Future maintainers reading the code (km-notify-hook PermissionRequest branch, poller dispatch fork, km doctor checks) can find the spec rationale via cross-references
</success_criteria>

<output>
After completion, create `.planning/phases/70-codex-parity-for-operator-notify-slack-notify-and-slack-inbound-dispatcher/70-08-SUMMARY.md` listing: (1) the three documents modified, (2) the major sections added to each, (3) line counts before/after, (4) any existing operator-facing content this phase changed (should be none — additive only).
</output>
</output>
