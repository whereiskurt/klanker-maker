---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - infra/modules/dynamodb-capacity/v1.0.0/main.tf
  - infra/modules/dynamodb-capacity/v1.0.0/variables.tf
  - infra/modules/dynamodb-capacity/v1.0.0/outputs.tf
  - infra/live/use1/dynamodb-capacity/terragrunt.hcl
  - internal/app/cmd/init.go
  - internal/app/cmd/init_plan_test.go
  - infra/modules/ec2spot/v1.2.0/main.tf
  - infra/modules/ec2spot/v1.2.0/variables.tf
  - pkg/compiler/ec2spot_timeout_test.go
autonomous: true
requirements: [REQ-124-STORE, REQ-124-WAITER]
must_haves:
  truths:
    - "A {prefix}-capacity DynamoDB table is provisioned with (instanceType,az) key and TTL enabled"
    - "regionalModules() includes dynamodb-capacity and km init applies it"
    - "A capacity-dry spot AZ errors in ~3min (bounded waiter) instead of hanging forever"
    - "On-demand aws_instance has NO bounded create timeout (spot-only fix)"
  artifacts:
    - path: "infra/modules/dynamodb-capacity/v1.0.0/main.tf"
      provides: "aws_dynamodb_table.capacity with TTL"
      contains: "hash_key     = \"instanceType\""
    - path: "infra/live/use1/dynamodb-capacity/terragrunt.hcl"
      provides: "live unit deriving {prefix}-capacity table name"
      contains: "capacity"
    - path: "infra/modules/ec2spot/v1.2.0/main.tf"
      provides: "bounded timeouts{create} on aws_spot_instance_request"
      contains: "timeouts"
  key_links:
    - from: "internal/app/cmd/init.go regionalModules()"
      to: "infra/live/use1/dynamodb-capacity"
      via: "regionalModule dir entry"
      pattern: "dynamodb-capacity"
    - from: "infra/modules/ec2spot/v1.2.0/main.tf aws_spot_instance_request"
      to: "var.spot_create_timeout default 3m"
      via: "timeouts { create = var.spot_create_timeout }"
      pattern: "spot_create_timeout"
---

<objective>
Stand up the deploy-surface for Phase 124: the new `{prefix}-capacity` DynamoDB table
(TF module + live unit + `regionalModules()` entry + module-count test bump) and the
bounded spot waiter that kills the `wait_for_fulfillment` forever-loop.

Independent of the Go `pkg/capacity` work — runs in Wave 0 parallel to 124-01.

Purpose: Give the capacity store a real table and stop a capacity-dry spot AZ from hanging
Terraform forever, so the sweep loop can advance within the Lambda 900s budget.
Output: dynamodb-capacity module + live unit, regionalModules entry, bumped module-count test,
`timeouts { create = "3m" }` on the spot request only.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-RESEARCH.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-VALIDATION.md

<!-- Patterns to follow EXACTLY: -->
<!-- DDB module: infra/modules/dynamodb-action-quota/v1.0.0/ (Phase 121) and dynamodb-slack-nonces -->
<!-- Live unit: infra/live/use1/dynamodb-slack-nonces/terragrunt.hcl -->
<!-- regionalModules(): internal/app/cmd/init.go:511 (dynamodb-* entries at 528-547) -->
<!-- module-count test: internal/app/cmd/init_plan_test.go:436-442 (len(mods) != 26) -->
<!-- allModuleNames mock dir list: internal/app/cmd/init_plan_test.go:132-138 -->
<!-- spot request resource: infra/modules/ec2spot/v1.2.0/main.tf:583-637 (wait_for_fulfillment at 604) -->
@infra/modules/ec2spot/v1.2.0/main.tf
@internal/app/cmd/init.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: dynamodb-capacity TF module + live unit + regionalModules entry + module-count bump</name>
  <files>infra/modules/dynamodb-capacity/v1.0.0/main.tf, infra/modules/dynamodb-capacity/v1.0.0/variables.tf, infra/modules/dynamodb-capacity/v1.0.0/outputs.tf, infra/live/use1/dynamodb-capacity/terragrunt.hcl, internal/app/cmd/init.go, internal/app/cmd/init_plan_test.go</files>
  <action>
    Create infra/modules/dynamodb-capacity/v1.0.0/ following the dynamodb-action-quota/v1.0.0 and
    dynamodb-slack-nonces patterns (124-RESEARCH lines 449-491):
    - main.tf: `aws_dynamodb_table.capacity` — PAY_PER_REQUEST, hash_key="instanceType"(S),
      range_key="az"(S), `ttl { attribute_name = "ttl"; enabled = true }`, server_side_encryption
      enabled, km: tags. Do NOT declare required_providers (root.hcl owns the provider stanza —
      see project memory project_terragrunt_providers_in_root).
    - variables.tf: `table_name` (string), `tags` (map(string), default {}).
    - outputs.tf: `table_name`, `table_arn`.
    Create infra/live/use1/dynamodb-capacity/terragrunt.hcl: inputs.table_name =
    "${local.site_vars.locals.site.label}-capacity" + km: tags (mirror dynamodb-slack-nonces unit).
    Include "show" in mock_outputs_allowed_terraform_commands if any dependency block is used
    (project memory project_terragrunt_show_needs_mocks) — match the sibling unit's structure.

    Add a `dynamodb-capacity` entry to regionalModules() in init.go alongside the other dynamodb-*
    entries (init.go:528-547), envReqs: nil, no upstreamOutputs. Place it adjacent to dynamodb-sandboxes
    for readability.

    Update init_plan_test.go: bump the assertion at line 440 from `len(mods) != 26` / `want 26` to 27
    (project memory project_module_order_test_count_debt), update the comment block at 430-435, AND add
    "dynamodb-capacity" to the `allModuleNames` mock-dir list at lines 133-138 (else plan tests fail on a
    missing dir — 124-RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run TestRunInitPlan_ModuleOrder -timeout 60s && terraform -chdir=infra/modules/dynamodb-capacity/v1.0.0 fmt -check 2>/dev/null; go build ./...</automated>
  </verify>
  <done>Module + live unit exist; regionalModules has dynamodb-capacity; TestRunInitPlan_ModuleOrder expects 27 and is green.</done>
</task>

<task type="auto">
  <name>Task 2: bounded spot waiter on aws_spot_instance_request</name>
  <files>infra/modules/ec2spot/v1.2.0/main.tf, infra/modules/ec2spot/v1.2.0/variables.tf, pkg/compiler/ec2spot_timeout_test.go</files>
  <behavior>
    - aws_spot_instance_request gains a `timeouts { create = var.spot_create_timeout }` block
    - var.spot_create_timeout default is "3m" (hardcoded value per locked decision; not per-profile tunable)
    - aws_instance (on-demand) gains NO timeouts block — the bounded waiter is spot-only
  </behavior>
  <action>
    Add to the `aws_spot_instance_request "ec2spot"` resource (main.tf:583-637, after
    wait_for_fulfillment at line 604):
    ```hcl
    timeouts {
      create = var.spot_create_timeout
      delete = "10m"
    }
    ```
    Add to variables.tf:
    ```hcl
    variable "spot_create_timeout" {
      type        = string
      description = "Spot fulfillment timeout. Keep <=3m so a 4-AZ sweep fits the Lambda 900s budget."
      default     = "3m"
    }
    ```
    Do NOT wire spot_create_timeout through the service.hcl template (locked decision: hardcode "3m",
    no per-profile tuning, no service.hcl churn — preserves byte-identity, 124-RESEARCH Pattern 2 +
    Open Question 1). Do NOT add a timeouts block to the on-demand `aws_instance` resource
    (124-RESEARCH Pitfall 7 — on-demand errors fast on ICE already).

    Create pkg/compiler/ec2spot_timeout_test.go with two tests that READ the static module files
    (the timeout lives in the module, not compiled output, per the locked no-service.hcl-wiring decision):
    - `TestEC2ServiceHCL_SpotTimeout`: read infra/modules/ec2spot/v1.2.0/main.tf; assert the
      aws_spot_instance_request block contains `timeouts {` and `create = var.spot_create_timeout`,
      and variables.tf declares `spot_create_timeout` with default "3m".
    - `TestEC2ServiceHCL_OnDemandNoTimeout`: assert the aws_instance (on-demand) resource block in the
      same file does NOT contain a `timeouts` block.
    Resolve the repo-relative module path from the test working dir robustly (walk up to repo root, or
    use a relative path from pkg/compiler — `../../infra/modules/ec2spot/v1.2.0/main.tf`).
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... -run 'TestEC2ServiceHCL_SpotTimeout|TestEC2ServiceHCL_OnDemandNoTimeout' -timeout 30s</automated>
  </verify>
  <done>Spot request has bounded timeouts{create=var.spot_create_timeout} (default 3m); on-demand has none; both tests green.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run TestRunInitPlan_ModuleOrder -timeout 60s` green (27 modules).
- `go test ./pkg/compiler/... -run 'TestEC2ServiceHCL_SpotTimeout|TestEC2ServiceHCL_OnDemandNoTimeout' -timeout 30s` green.
- `go build ./...` clean.

DEPLOY-SURFACE NOTE (executed in 124-06, called out here because this plan adds the regionalModules
entry): because dynamodb-capacity is a new regionalModules() entry, the operator MUST run
`make build` to rebuild the km binary BEFORE `km init --dry-run=false` — a stale binary silently
skips the new module and the create-handler/ec2spot IAM for the table is never created
(project memory project_make_build_precedes_km_init; 124-RESEARCH Pitfall 1).
</verification>

<success_criteria>
- {prefix}-capacity DDB table module + live unit exist and are wired into regionalModules().
- Module-count contract test asserts 27 and passes.
- Spot requests use a bounded 3-min create timeout; on-demand unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-03-SUMMARY.md`
</output>
