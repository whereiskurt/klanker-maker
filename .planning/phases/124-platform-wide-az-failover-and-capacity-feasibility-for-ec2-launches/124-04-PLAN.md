---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
plan: 04
type: execute
wave: 2
depends_on: [124-01, 124-02]
files_modified:
  - pkg/capacity/rankaz.go
  - pkg/capacity/rankaz_test.go
  - internal/app/cmd/create.go
  - internal/app/cmd/capacity.go
  - internal/app/cmd/capacity_test.go
  - internal/app/cmd/root.go
autonomous: true
requirements: [REQ-124-RANK, REQ-124-CAPCMD, REQ-124-SWEEP]
must_haves:
  truths:
    - "rankAZs drops AZs that do not offer the instance type (DescribeInstanceTypeOfferings)"
    - "rankAZs fail-fasts with QuotaError when GPU-family quota headroom is 0 (L-DB2E81BA), before any apply"
    - "spec.runtime.azPreference AZs appear first in the ranked list when offered"
    - "last-success AZ ranks first; an AZ with a fresh cached ICE ranks last"
    - "km create pre-orders network.AvailabilityZones via rankAZs before the sweep loop"
    - "km capacity prints an honest per-AZ report (offered / quota / last-ICE / last-success / verdict) and never says 'available'"
  artifacts:
    - path: "pkg/capacity/rankaz.go"
      provides: "full RankAZs impl + DescribeAZOfferings + GetGPUVCPUQuota + isGPUFamily"
      contains: "DescribeInstanceTypeOfferings"
    - path: "internal/app/cmd/capacity.go"
      provides: "km capacity command (profile | --type)"
      contains: "func newCapacityCmd"
  key_links:
    - from: "internal/app/cmd/create.go"
      to: "pkg/capacity.RankAZs"
      via: "pre-order network.AvailabilityZones before the apply loop"
      pattern: "capacity\\.RankAZs"
    - from: "internal/app/cmd/capacity.go"
      to: "pkg/capacity (offerings + quota + store)"
      via: "per-AZ verdict computation"
      pattern: "DescribeAZOfferings|GetGPUVCPUQuota"
---

<objective>
Make AZ ordering capacity-aware and ship the honest `km capacity` feasibility report.

Implement the real `RankAZs` (stubbed in 124-01): drop non-offering AZs, gate on regional
GPU quota up front, prefer the sticky last-success AZ, deprioritize fresh-ICE AZs, and merge
`spec.runtime.azPreference` ahead of the learned ranking. Wire it into `km create` so the
sweep loop tries the capacity-likely AZ first. Add `km capacity` for a per-AZ verdict table.

Purpose: GPU launches try us-east-1c first and skip the capacity-dry 1a entirely; operators
get a truthful feasibility report (never a false "available").
Output: full RankAZs, rankAZs wired into create.go, `km capacity <profile|--type>` command.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-RESEARCH.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-VALIDATION.md

<interfaces>
<!-- From 124-01 (merged): the interface surface + stubbed RankAZs to implement now. -->
type EC2OfferingsAPI interface { DescribeInstanceTypeOfferings(...) }
type ServiceQuotasAPI interface { GetServiceQuota(...) }
type CapacityStore interface { RecordICE; RecordSuccess; Get(...) (*CapacityEntry, error) }
type QuotaError struct { QuotaCode string; Headroom float64 }
const GPUVCPUQuotaCode = "L-DB2E81BA"; GPUQuotaServiceCode = "ec2"
func RankAZs(ctx, instanceType, region string, azPreference []string, store CapacityStore, ec2c EC2OfferingsAPI, sqc ServiceQuotasAPI, allAZs []string) ([]string, error)

<!-- API shapes provided verbatim in 124-RESEARCH Code Examples (lines 377-443): -->
<!-- DescribeAZOfferings (LocationType=availability-zone, filters location+instance-type) -->
<!-- GetGPUVCPUQuota (ServiceCode "ec2", QuotaCode "L-DB2E81BA", returns *Quota.Value) -->
</interfaces>

<!-- create.go loop now has the classify-and-retry from 124-02; this plan adds the rankAZs pre-order. -->
@internal/app/cmd/create.go
<!-- Cobra command registration pattern -->
@internal/app/cmd/root.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement capacity-aware RankAZs</name>
  <files>pkg/capacity/rankaz.go, pkg/capacity/rankaz_test.go</files>
  <behavior>
    - TestRankAZs_DropsNonOffering: offerings mock returns only [1c,1b] for the type -> 1a dropped from result
    - TestRankAZs_GPUQuotaBlock: isGPUFamily type + quota mock returns 0 -> returns *QuotaError{QuotaCode:"L-DB2E81BA"}, no AZs
    - TestRankAZs_AZPreference: azPreference [1c] + offered [1a,1b,1c] -> 1c first
    - TestRankAZs_ICEStickySuccess: store says 1b last-success + 1c fresh-ICE -> 1b before 1c; 1c last
    - TestRankAZs: non-GPU type skips the quota call; offerings error falls back to allAZs (warn, no fail)
  </behavior>
  <action>
    Replace the stub RankAZs body in pkg/capacity/rankaz.go with the real implementation
    (124-RESEARCH Pattern 4, lines 280-312):
    1. `DescribeAZOfferings(ctx, ec2c, instanceType, allAZs)` -> drop non-offering AZs (on error: log/warn
       and fall back to allAZs, do not fail the create).
    2. If `isGPUFamily(instanceType)` (prefix "g" or "vt", case-insensitive — 124-RESEARCH Open Question 2):
       `GetGPUVCPUQuota(ctx, sqc)`; if headroom==0 return `&QuotaError{QuotaCode: GPUVCPUQuotaCode, Headroom: 0}`.
       Skip the quota call entirely for non-GPU types.
    3. Merge azPreference ahead: `intersect(azPreference, offered)` first, then the remaining offered AZs.
    4. Sort the remaining by rankScore: last-success AZ first (sticky), deprioritize AZs whose
       store entry has a fresh LastICEAt (within 45min), alphabetical tiebreak for stability.
    Add helpers `DescribeAZOfferings`, `GetGPUVCPUQuota`, `isGPUFamily` per the Code Examples. Use a
    short in-process cache (5min) for offerings if convenient (124-RESEARCH Claude's Discretion) — optional.

    Create pkg/capacity/rankaz_test.go with the five tests above, using fake EC2OfferingsAPI /
    ServiceQuotasAPI / CapacityStore implementations (table-driven where natural).
  </action>
  <verify>
    <automated>go test ./pkg/capacity/... -run TestRankAZs -timeout 30s</automated>
  </verify>
  <done>RankAZs drops non-offering AZs, fail-fasts on 0 GPU quota, honors azPreference + last-success + ICE deprioritize; all five tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RankAZs into km create's pre-loop AZ ordering</name>
  <files>internal/app/cmd/create.go</files>
  <action>
    Before the apply loop (around create.go:741-746, after maxAttempts is computed and before
    `for attempt := ...`), call `capacity.RankAZs(ctx, instanceType, region, resolvedProfile.Spec.Runtime.AZPreference,
    capacityStore, ec2OfferingsClient, sqClient, network.AvailabilityZones)` to pre-order
    network.AvailabilityZones (and reorder network.PublicSubnets in lockstep so subnet[i] stays paired
    with AZ[i]). On a returned `*QuotaError`, fail fast immediately with the L-DB2E81BA-named message
    (do NOT enter the loop). On any other ranking error, log a warning and keep the original AZ order
    (ranking is best-effort; never block a launch because an offerings/quota probe failed).

    Construct the clients from awsCfg/region (ec2svc.NewFromConfig with the target region as already done
    at create.go:733-735; servicequotas.NewFromConfig). Construct the DynamoCapacityStore from
    cfg.GetCapacityTableName() + a dynamodb client. Only call RankAZs for the EC2/spot path (skip when
    onDemand keeps maxAttempts=1? — still rank: ranking also benefits on-demand by skipping non-offering
    AZs; but the within-loop rotation only happens for spot). Keep N>1 spread (km clone --count) intact:
    rankAZs reorders the AZ list; the `idx % len` spread in the ec2spot module is preserved because the
    list still contains all AZs (124-RESEARCH §AZ Override Injection; design spec § km clone edge case).
    After RankAZs, recompute maxAttempts from len(network.AvailabilityZones) if AZs were dropped.

    Verify byte-identity is preserved when azPreference is absent AND ranking yields the same order
    (that contract is already covered by TestEC2ServiceHCL_AZPreferenceAbsent from 124-01; do not
    regress it).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/app/cmd/... -run 'TestAZSweepLoop|TestNewCreateCmd' -timeout 120s</automated>
  </verify>
  <done>network.AvailabilityZones is rankAZs-ordered before the loop; QuotaError fail-fasts pre-loop; N>1 spread preserved; build + existing create tests green.</done>
</task>

<task type="auto">
  <name>Task 3: km capacity feasibility command</name>
  <files>internal/app/cmd/capacity.go, internal/app/cmd/capacity_test.go, internal/app/cmd/root.go</files>
  <behavior>
    - `km capacity --type g6e.12xlarge` prints a per-AZ report with 5 columns: AZ | offered? | quota headroom | last ICE | last success | verdict
    - verdicts are exactly one of: likely | quota-blocked | not-offered | recently-dry | unknown (NEVER "available")
    - `km capacity <profile.yaml>` resolves the instance type from the profile
  </behavior>
  <action>
    Create internal/app/cmd/capacity.go with `newCapacityCmd()`: accepts either a positional
    profile path OR `--type <instanceType>` (+ optional `--region`). Resolve AZs from the region's
    network outputs / DescribeAvailabilityZones, then for each AZ compute: offered? (DescribeAZOfferings),
    quota headroom (GetGPUVCPUQuota for GPU families; "—" otherwise), last_ice_at / last_success_at
    (CapacityStore.Get), and a verdict via this precedence (124-RESEARCH § km capacity, design spec
    lines 110-120):
      not-offered (not in offerings) > quota-blocked (GPU family + headroom 0) >
      recently-dry (fresh LastICEAt) > likely (offered + has quota + recent/any success or no ICE) >
      unknown (offered but no signal). NEVER emit "available".
    Render a tab/aligned table to stdout. Register the command in root.go alongside the other
    rootCmd.AddCommand(...) calls.

    Create capacity_test.go with `TestCapacityReport` (or the smoke-equivalent): drive the
    verdict-computation function with mocked offerings/quota/store and assert each verdict value
    is produced for the matching inputs, and that "available" is never emitted. (The live
    DescribeInstanceTypeOfferings accuracy is a 124-06 UAT gate.)
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/app/cmd/... -run TestCapacity -timeout 60s</automated>
  </verify>
  <done>km capacity command registered; prints 5-column per-AZ report with honest verdicts (never "available"); verdict test green.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/capacity/... -run TestRankAZs -timeout 30s` green (all five ranking tests).
- `go test ./internal/app/cmd/... -run 'TestCapacity|TestAZSweepLoop' -timeout 120s` green.
- `go build ./...` clean; `km capacity --type g6e.12xlarge` produces a 5-column report.
</verification>

<success_criteria>
- RankAZs is capacity-aware (offerings drop, GPU quota gate, azPreference, sticky success, ICE deprioritize).
- km create pre-orders AZs via RankAZs; quota wall fails fast pre-loop; N>1 spread preserved.
- km capacity gives an honest per-AZ verdict table that never claims "available".
</success_criteria>

<output>
After completion, create `.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-04-SUMMARY.md`
</output>
