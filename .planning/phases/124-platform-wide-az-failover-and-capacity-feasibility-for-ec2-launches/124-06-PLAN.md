---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
plan: 06
type: execute
wave: 4
depends_on: [124-02, 124-03, 124-04, 124-05]
files_modified:
  - docs/operational-gotchas.md
  - CLAUDE.md
  - .planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-UAT.md
autonomous: false
requirements: [REQ-124-UAT]
must_haves:
  truths:
    - "Full Go suite green and 20/20 profiles validate before any deploy"
    - "Deploy sequence ran in the correct order: make build BEFORE km init (regionalModules gotcha) + make build-lambdas for the create-handler classifier"
    - "A GPU launch fails over 1a->1c (skips the non-offering/dry AZ) and the sandbox boots"
    - "A quota=0 account fails fast naming L-DB2E81BA, NOT a 4-AZ sweep"
    - "km capacity reports accurate offered/not-offered per AZ, and all 4 public subnets exist"
  artifacts:
    - path: ".planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-UAT.md"
      provides: "live UAT log with per-gate evidence"
      contains: "1a"
    - path: "docs/operational-gotchas.md"
      provides: "AZ-failover + km capacity operator notes"
      contains: "capacity"
  key_links:
    - from: "make build (operator binary)"
      to: "km init --dry-run=false"
      via: "regionalModules() includes dynamodb-capacity only after rebuild"
      pattern: "make build"
---

<objective>
Ship Phase 124: run the full test gates, deploy in the correct order, document the feature,
and execute the live UAT. The live GPU gates are G-vCPU-quota gated (L-DB2E81BA) exactly like
Phase 122 — the test/docs/deploy parts complete now; the live GPU launch gates resume once the
account has quota.

Purpose: Prove the AZ sweep, fail-fast classification, bounded waiter, capacity store, and
km capacity report all work end-to-end on real AWS.
Output: green suite, deployed table+classifier, docs, and a 124-UAT.md evidence log.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-RESEARCH.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-VALIDATION.md

<!-- Deploy-surface reference (124-RESEARCH Pitfall 1 + design spec § Deploy surface): -->
<!-- 1. make build (operator binary) BEFORE km init — stale binary silently skips dynamodb-capacity -->
<!-- 2. make build-lambdas — refactored create-handler classifier -->
<!-- 3. km init --dry-run=false — creates {prefix}-capacity table -->
<!-- toolchain/km in S3 must be linux/arm64 (project memory project_toolchain_km_must_be_linux) -->
<!-- UAT profile: profiles/gpu-qwen-12x-l4.yaml -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test gates + docs</name>
  <files>docs/operational-gotchas.md, CLAUDE.md</files>
  <action>
    Run the full gates and confirm GREEN (capture the command's OWN exit code, not a piped tail —
    project memory feedback_check_go_test_exit_not_pipe):
      go test ./... -timeout 600s
      scripts/validate-all-profiles.sh   (expect 20/20)
    Fix any regressions before proceeding.

    Document the feature:
    - docs/operational-gotchas.md: add an "AZ failover + capacity feasibility" section — the sweep
      (rankAZs + classify-and-retry), the fail-fast quota wall (L-DB2E81BA) vs ICE distinction, the
      bounded spot waiter, `km capacity` verdicts, `km create --wait-for-capacity`, the {prefix}-capacity
      table, and the deploy-surface order (make build BEFORE km init).
    - CLAUDE.md: add a "Phase 124" summary block (mirror the existing phase blocks) and a "Where to look"
      table row pointing at the new gotchas section + `km capacity` / `km create --wait-for-capacity`.
  </action>
  <verify>
    <automated>go test ./... -timeout 600s && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>Full suite green; 20/20 profiles validate; operational-gotchas.md + CLAUDE.md document Phase 124.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy the capacity table + refactored classifier</name>
  <files>(deploy — no source edits)</files>
  <what-built>The {prefix}-capacity DDB table (regionalModules entry), the refactored create-handler nocap classifier, and the operator-side sweep/rankAZs/km capacity.</what-built>
  <action>
    Run the deploy sequence IN THIS ORDER (the order is load-bearing — 124-RESEARCH Pitfall 1):
    1. `make build`            # operator binary FIRST — so regionalModules() includes dynamodb-capacity
    2. `make build-lambdas`    # refactored create-handler classifier zip
    3. `km init --dry-run=false`  # full apply: creates the {prefix}-capacity table + IAM
    4. `km doctor`             # Capacity Table present; GPU vCPU quota check shows current value
    A stale binary at step 1 silently skips the new module and the table/IAM is never created.
  </action>
  <how-to-verify>
    `km doctor` shows the Capacity Table check; `aws dynamodb describe-table --table-name <prefix>-capacity`
    returns the table. Confirm toolchain/km in S3 is linux/arm64 if cold-create is exercised (project memory).
  </how-to-verify>
  <verify>km doctor shows "Capacity Table" present and describe-table succeeds</verify>
  <done>make build ran before km init; {prefix}-capacity table exists; km doctor surfaces it.</done>
  <resume-signal>Type "deployed" once km doctor shows the Capacity Table and the table exists, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT gates (G-quota gated)</name>
  <files>.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-UAT.md</files>
  <what-built>End-to-end AZ failover + fail-fast + capacity report on real AWS.</what-built>
  <action>
    Create 124-UAT.md and record per-gate evidence. Gates G1-G2 run without GPU quota; G3-G5 require
    G-vCPU quota (L-DB2E81BA) and are BLOCKED until the target account/region has it — resume those
    exactly like Phase 122's live-UAT pattern once quota lands.
  </action>
  <how-to-verify>
    G1 (no quota needed): all 4 public subnets exist —
      `aws ec2 describe-subnets --filters Name=vpc-id,Values=$VPC_ID` shows 4 distinct AZs
      (verifies availability_zone_count: 4 actually provisioned subnets — design spec edge case).
    G2 (no quota needed): `km capacity --type g6e.12xlarge` shows accurate offered/not-offered per AZ
      (cross-check against the AWS console; 1a not-offered, 1c offered) and NEVER prints "available".
    G3 (quota gated): `km create profiles/gpu-qwen-12x-l4.yaml` — observe the sweep skip the
      non-offering/dry AZ (1a) and land in 1c; sandbox boots; the EBS additionalVolume is in the same AZ
      as the instance (co-location).
    G4 (quota gated): from a region/account with G-quota 0, run a GPU create — assert an IMMEDIATE
      fail-fast naming L-DB2E81BA + the increase path, NOT a 4-AZ sweep.
    G5 (quota gated): confirm the {prefix}-capacity table recorded last_success_at for (g6e.12xlarge, 1c)
      after G3, and a subsequent create prefers 1c (sticky).
  </how-to-verify>
  <verify>G1 (4 subnets) + G2 (km capacity accuracy, never "available") pass; G3-G5 pass or are documented as G-quota-deferred</verify>
  <done>124-UAT.md records G1/G2 evidence (and G3-G5 if quota available, else explicit quota-deferred note).</done>
  <resume-signal>Type "approved" with G1/G2 evidence (and G3-G5 if quota available), or "blocked: G-quota" to defer the GPU gates like Phase 122.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./... -timeout 600s` green; `scripts/validate-all-profiles.sh` 20/20.
- Deploy ran make build BEFORE km init; {prefix}-capacity table exists; km doctor surfaces it.
- G1 (4 subnets) + G2 (km capacity accuracy) pass; G3-G5 pass or are explicitly deferred on G-quota.
</verification>

<success_criteria>
- Full suite + profile gate green.
- Capacity table + classifier deployed in the correct order.
- Phase 124 documented; live UAT evidence captured (GPU gates passed or quota-deferred per Phase 122 pattern).
</success_criteria>

<output>
After completion, create `.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-06-SUMMARY.md`
and the live evidence log at `.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-UAT.md`.
</output>
