---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
type: uat
status: partial
gates_passed: [deploy, G1, G2]
gates_deferred: [G3, G4, G5]
updated: 2026-06-28
install: km (resource_prefix=km)
region: us-east-1
aws_profile: klanker-application
---

# Phase 124 — Live UAT Evidence Log

AZ failover + fail-fast capacity classification + honest capacity feasibility report,
validated against real AWS (install `km`, us-east-1).

## Deploy (Task 2) — PASSED

Operator ran the load-bearing sequence in order: `make build` → `make build-lambdas` →
`km init --dry-run=false`. The full terragrunt apply created the new `dynamodb-capacity`
module and re-applied the 27-module fleet to completion (clean exit).

**Evidence — capacity table provisioned to spec:**
```
$ aws dynamodb describe-table --table-name km-capacity --region us-east-1
Status:   ACTIVE
Keys:     instanceType (HASH), az (RANGE)
Billing:  PAY_PER_REQUEST
TTL:      ENABLED on attribute "ttl"
```

**Evidence — `km doctor` surfaces the new checks:**
```
✓ Capacity Table (km-capacity)        table "km-capacity" exists
✓ GPU vCPU quota (L-DB2E81BA)         64 vCPUs available
```
Note: the GPU vCPU quota (L-DB2E81BA) on this account/region is now **64 vCPUs** (no longer 0),
so the doctor check reports the healthy (non-WARN) path. The WARN-on-zero path is covered by the
unit test `doctor_capacity_test.go` (a live zero-quota region is the G4 gate, deferred below).

## G1 — 4 distinct AZ subnets provisioned — PASSED

Verifies the design-spec edge case `availability_zone_count: 4` actually materialised as subnets
the AZ-sweep can rotate through.

```
$ aws ec2 describe-subnets --filters Name=vpc-id,Values=vpc-027ba3e68c2e32549 --region us-east-1
VPC: vpc-027ba3e68c2e32549
  us-east-1a  km-use1-public-us-east-1a   (public)   subnet-0f3ecafc34664de8c
  us-east-1a  km-use1-private-us-east-1a  (private)  subnet-007df53b99d0d944f
  us-east-1b  km-use1-public-us-east-1b   (public)   subnet-0514a7c2217ff6a94
  us-east-1b  km-use1-private-us-east-1b  (private)  subnet-07812cd842e0ea922
  us-east-1c  km-use1-public-us-east-1c   (public)   subnet-04e6a8d5c8796ccef
  us-east-1c  km-use1-private-us-east-1c  (private)  subnet-027e3e8fcab24b6d6
  us-east-1d  km-use1-public-us-east-1d   (public)   subnet-0c6d86c52824c7821
  us-east-1d  km-use1-private-us-east-1d  (private)  subnet-067ac4a914cdb6a25

Distinct AZs: us-east-1a, us-east-1b, us-east-1c, us-east-1d  → 4 ✓
```

## G2 — `km capacity` honest verdict table, never "available" — PASSED

```
$ km capacity --type g6e.12xlarge --region us-east-1
Capacity report: g6e.12xlarge  region: us-east-1

AZ          OFFERED  QUOTA HEADROOM  LAST ICE  LAST SUCCESS  VERDICT
--          -------  --------------  --------  ------------  -------
us-east-1a  yes      64 vCPU         —         —             likely
us-east-1b  yes      64 vCPU         —         —             likely
us-east-1c  yes      64 vCPU         —         —             likely
us-east-1d  yes      64 vCPU         —         —             likely
us-east-1e  no       64 vCPU         —         —             not-offered
us-east-1f  no       64 vCPU         —         —             not-offered
```

- Per-AZ offered/not-offered is accurate (1e/1f correctly `not-offered` for g6e.12xlarge in us-east-1).
- The verdict column uses `likely` / `not-offered` — it **never prints "available"** ✓ (honest-report invariant).
- LAST ICE / LAST SUCCESS columns are empty because no GPU create has run against this table yet
  (those populate via G3/G5).

## G3 — GPU create AZ failover (1a→1c) + EBS co-location — DEFERRED

Requires an actual GPU sandbox launch (`km create profiles/gpu-qwen-12x-l4.yaml`). Deferred for this
unattended session: a live GPU create incurs real per-hour spend and carries the known us-east-1
g6e/g6.12xlarge capacity-hang risk (project memory: `project_gpu_capacity_az_and_l4_fallback`).
GPU vCPU quota (64) is now present, so an operator can run this gate in an attended session.

## G4 — fail-fast on GPU quota=0 naming L-DB2E81BA — DEFERRED

Requires a region/account where L-DB2E81BA is still 0 to assert an immediate fail-fast (not a 4-AZ
sweep). The classification + fail-fast path is unit-tested (`create_az_sweep_test.go` sweepDecision,
`doctor_capacity_test.go` quota-zero WARN); the live assertion is deferred pending a zero-quota target.

## G5 — capacity-store sticky routing (last_success records, next create prefers 1c) — DEFERRED

Depends on G3 (a successful GPU create writing `last_success_at` for `(g6e.12xlarge, 1c)`). Deferred
with G3.

## Summary

| Gate | Needs GPU quota | Status |
|------|-----------------|--------|
| Deploy | no | ✓ PASSED |
| G1 — 4 AZ subnets | no | ✓ PASSED |
| G2 — honest capacity report | no | ✓ PASSED |
| G3 — GPU AZ failover | yes | ⏸ DEFERRED (cost/capacity-hang; operator-runnable) |
| G4 — quota-0 fail-fast | yes (zero-quota target) | ⏸ DEFERRED |
| G5 — sticky routing | yes (depends on G3) | ⏸ DEFERRED |

All non-GPU gates pass against real AWS. The GPU-launch gates (G3–G5) follow the Phase 122
live-UAT pattern — code-complete and unit-verified now; live GPU evidence captured in an attended
session to control GPU spend and absorb the us-east-1 capacity-hang risk.
