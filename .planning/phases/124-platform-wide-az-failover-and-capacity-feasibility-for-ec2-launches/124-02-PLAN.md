---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
plan: 02
type: execute
wave: 1
depends_on: [124-01]
files_modified:
  - cmd/create-handler/main.go
  - cmd/create-handler/main_test.go
  - internal/app/cmd/create.go
  - internal/app/cmd/create_az_sweep_test.go
autonomous: true
requirements: [REQ-124-CLASSIFY, REQ-124-SWEEP]
must_haves:
  truths:
    - "km create's AZ-sweep loop classifies each apply failure: ICE/spot-price/spot-limit/waiter-timeout iterate to the next AZ; quota/auth/invalid fail-fast immediately"
    - "A quota wall (VcpuLimitExceeded) fails fast with a message naming L-DB2E81BA and the increase path, NOT a 4-AZ sweep"
    - "CleanupSandboxDir runs between attempts; the loop exits on first success"
    - "The cold-create Lambda's nocap classification uses the shared pkg/capacity.ClassifyError taxonomy, not bespoke substring matches"
  artifacts:
    - path: "internal/app/cmd/create.go"
      provides: "classify-and-retry AZ-sweep loop using pkg/capacity.ClassifyError"
      contains: "capacity.ClassifyError"
    - path: "cmd/create-handler/main.go"
      provides: "nocap status derived from ClassifyError.ShouldIterate()"
      contains: "capacity.ClassifyError"
  key_links:
    - from: "internal/app/cmd/create.go apply loop"
      to: "pkg/capacity.ClassifyError"
      via: "classify applyStderr + applyErr per attempt"
      pattern: "capacity\\.ClassifyError"
    - from: "cmd/create-handler/main.go failStatus"
      to: "pkg/capacity.ClassifyError"
      via: "ShouldIterate() -> nocap"
      pattern: "ShouldIterate"
---

<objective>
Replace the two ad-hoc capacity-error string matchers with the shared `pkg/capacity`
taxonomy: upgrade the existing AZ-rotation loop in `km create` (create.go:746-869) into a
classify-and-retry sweep, and refactor the cold-create Lambda's `nocap` classifier
(create-handler/main.go:341-346) onto the same `ClassifyError`.

The AZ-rotation loop ALREADY EXISTS — this plan UPGRADES it. It does not build it from scratch.

Purpose: Make "iterate vs fail-fast" correct everywhere. A regional GPU-vCPU quota wall
(L-DB2E81BA=0) must fail fast with named remediation instead of masquerading as no-capacity
and burning a full AZ sweep.
Output: create.go sweep loop driven by ClassifyError; create-handler nocap on the shared taxonomy.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-RESEARCH.md
@.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-VALIDATION.md

<interfaces>
<!-- From 124-01 (already merged). Consume these — do not re-define. -->
pkg/capacity.ClassifyError(stderr string, err error) capacity.ErrorClass
pkg/capacity.ErrorClass.ShouldIterate() bool  // true for ICE/SpotPrice/SpotLimit/WaiterTimeout
capacity.ClassQuota / ClassAuth / ClassInvalid // fail-fast classes
capacity.GPUVCPUQuotaCode = "L-DB2E81BA"
</interfaces>

<!-- The existing loop to UPGRADE (do not rewrite): create.go:704-869 -->
@internal/app/cmd/create.go
<!-- The existing nocap classifier to REFACTOR: create-handler/main.go:328-360 -->
@cmd/create-handler/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor create-handler nocap classifier onto pkg/capacity.ClassifyError</name>
  <files>cmd/create-handler/main.go, cmd/create-handler/main_test.go</files>
  <behavior>
    - Subprocess output containing InsufficientInstanceCapacity / MaxSpotInstanceCountExceeded / SpotMaxPriceTooLow / "no Spot capacity" -> failStatus "nocap" (ShouldIterate()==true)
    - Subprocess output containing VcpuLimitExceeded (quota) -> failStatus "failed" (NOT nocap — quota is not a capacity dry-spell)
    - AuthFailure / InvalidParameterValue -> failStatus "failed"
    - No capacity signal -> failStatus "failed" (unchanged)
  </behavior>
  <action>
    Replace the four-string substring block at cmd/create-handler/main.go:341-346 with a call to
    `capacity.ClassifyError(outStr, runErr)`: if the class `.ShouldIterate()` is true, failStatus =
    "nocap"; otherwise "failed". This preserves the existing "nocap" status code but correctly
    excludes quota/auth/invalid from it (124-RESEARCH §State of the Art — the nocap substring match
    is superseded). Import the pkg/capacity package. Leave the surrounding metadata-write logic
    (UpdateSandboxStatusAndReasonDynamo, extractFailureReason) unchanged.

    Add `TestNocapClassifier` to cmd/create-handler/main_test.go covering the behavior cases above
    (drive the classification helper with representative subprocess output strings).
  </action>
  <verify>
    <automated>go test ./cmd/create-handler/... -run TestNocapClassifier -timeout 30s</automated>
  </verify>
  <done>nocap derived from ClassifyError.ShouldIterate(); quota/auth/invalid map to "failed"; TestNocapClassifier green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade km create AZ-sweep loop to classify-and-retry with fail-fast</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/create_az_sweep_test.go</files>
  <behavior>
    - On apply failure, classify (applyStderr, applyErr); ShouldIterate() classes -> CleanupSandboxDir + rotate to next AZ + continue (when attempts remain)
    - Fail-fast classes (ClassQuota/ClassAuth/ClassInvalid) -> CleanupSandboxDir + return immediately, NO further AZ attempts
    - ClassQuota fail-fast message names L-DB2E81BA and the Service Quotas increase path
    - First success -> break; CleanupSandboxDir is called between attempts
    - on-demand still maxAttempts=1 (unchanged at create.go:709-711)
  </behavior>
  <action>
    Upgrade the existing loop at create.go:746-869. Replace the `isSpotCapacity` substring check
    (lines 846-847) and the retry/exit branching (854-868) with `class := capacity.ClassifyError(stderrStr, applyErr)`:
    - `class == ClassSuccess` is already handled by the `applyErr == nil` branch (lines 833-842) — leave it.
    - If `class.ShouldIterate()` and `attempt < maxAttempts-1`: keep the existing CleanupSandboxDir +
      "capacity unavailable in {AZ}" message + `continue` (the AZ rotation at 747-752 is unchanged for now;
      capacity-aware pre-ordering via rankAZs is wired in 124-04).
    - If `class` is a fail-fast class (ClassQuota/ClassAuth/ClassInvalid): CleanupSandboxDir then return a
      non-retriable error. For ClassQuota, the message MUST name `capacity.GPUVCPUQuotaCode` ("L-DB2E81BA")
      and the increase URL (https://console.aws.amazon.com/servicequotas, service ec2). Do NOT iterate.
    - Exhausted iterate-class attempts: keep the existing "unavailable in all N AZs / use --on-demand" summary.
    Keep CleanupSandboxDir between attempts (already at 850-852) — state is wiped, so NO explicit
    -replace/taint and NO new terragrunt.Runner method (124-RESEARCH §Between-Attempt State Cleanup).

    Create internal/app/cmd/create_az_sweep_test.go with `TestAZSweepLoop`: extract or test the
    classify-driven decision so a mocked apply runner that returns ICE on attempt 0 then success on
    attempt 1 drives a retry+success, while a quota stderr returns immediately without a second attempt.
    If create.go's loop is hard to unit-test in place, factor the per-attempt decision into a small
    helper (e.g. `sweepDecision(class capacity.ErrorClass, attempt, maxAttempts int) (retry bool, failFast bool)`)
    and test that helper plus a thin integration assertion that CleanupSandboxDir is invoked between attempts.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run TestAZSweepLoop -timeout 60s</automated>
  </verify>
  <done>Sweep loop classifies failures; iterate classes retry, quota/auth/invalid fail fast with L-DB2E81BA-named message; CleanupSandboxDir between attempts; TestAZSweepLoop green.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/create-handler/... -run TestNocapClassifier -timeout 30s` green.
- `go test ./internal/app/cmd/... -run TestAZSweepLoop -timeout 60s` green.
- `go test ./internal/app/cmd/... -timeout 600s` and `go test ./cmd/create-handler/... -timeout 120s` green (no regressions).
- DEPLOY: the create-handler classifier change requires `make build-lambdas` (executed in 124-06).
</verification>

<success_criteria>
- Both capacity-error matchers consume the single pkg/capacity.ClassifyError taxonomy.
- Quota walls fail fast with named remediation; ICE iterates; success breaks.
- No new terragrunt.Runner method; CleanupSandboxDir remains the between-attempt "taint".
</success_criteria>

<output>
After completion, create `.planning/phases/124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches/124-02-SUMMARY.md`
</output>
