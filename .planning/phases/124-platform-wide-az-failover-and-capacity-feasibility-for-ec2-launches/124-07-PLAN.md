---
phase: 124-platform-wide-az-failover-and-capacity-feasibility-for-ec2-launches
plan: 07
type: execute
wave: 5
gap_closure: true
depends_on: [124-02, 124-04, 124-05]
requirements: [REQ-124-STORE, REQ-124-RANK]
files_modified:
  - internal/app/cmd/create.go
  - internal/app/cmd/create_az_sweep_test.go
  - infra/modules/km-operator-policy/v1.0.0/main.tf
  - infra/modules/km-operator-policy/v1.0.0/variables.tf
  - infra/modules/create-handler/v1.0.0/main.tf
  - infra/modules/create-handler/v1.0.0/variables.tf
  - infra/live/use1/create-handler/terragrunt.hcl
autonomous: true
must_haves:
  truths:
    - "The km create sweep loop WRITES to the capacity store: RecordSuccess on a successful apply and RecordICE on an ICE-classified apply failure (both best-effort, non-fatal)"
    - "The write-back covers the cold-create Lambda path too (the Lambda shells out to the bundled km create subprocess, so the create.go change applies to both runtime paths)"
    - "The create-handler Lambda role is granted dynamodb:UpdateItem + GetItem on {prefix}-capacity so the subprocess write succeeds"
    - "RankAZs now reads a store that the sweep actually populates (feedback loop closed): last-success makes an AZ sticky; fresh-ICE deprioritizes it"
  artifacts:
    - path: "internal/app/cmd/create.go"
      provides: "RecordSuccess/RecordICE wired into the sweep loop success + ICE branches"
      contains: "RecordSuccess"
    - path: "infra/modules/km-operator-policy/v1.0.0/main.tf"
      provides: "dynamodb_capacity IAM policy on {prefix}-capacity"
      contains: "capacity_table_name"
  key_links:
    - from: "internal/app/cmd/create.go sweep loop"
      to: "pkg/capacity.DynamoCapacityStore.RecordSuccess/RecordICE"
      via: "best-effort write on success and on ICE-class failure"
      pattern: "RecordSuccess|RecordICE"
    - from: "create-handler Lambda role"
      to: "{prefix}-capacity table"
      via: "km-operator-policy dynamodb_capacity statement"
      pattern: "capacity_table_name"
---

<objective>
Close the Phase 124 verification gap: the capacity memory store has a read path (RankAZs +
km capacity both read it) but NO write path — RecordICE/RecordSuccess are defined and unit-tested
in pkg/capacity/store.go but never called from production. The result is RankAZs always reads an
empty store, so its sticky-last-success and fresh-ICE-deprioritize ranking is inert.

Wire the write-back into the km create sweep loop (covers BOTH the operator-side path AND the
cold-create Lambda path, since the Lambda runs `km create` as a subprocess), and grant the
create-handler Lambda role dynamodb write access on {prefix}-capacity so the cold-path write
succeeds. Operator + Lambda scope, per the user's decision.
</objective>

<context>
Scope already gathered by the orchestrator:

- pkg/capacity/store.go: `RecordICE(ctx, instanceType, az) error` and `RecordSuccess(ctx, instanceType, az) error`
  both use UpdateItem. `CapacityStore` is the interface; `NewDynamoCapacityStore(client, tableName)` constructs it.
- internal/app/cmd/create.go:
  - The capacity store is already constructed at ~line 771 (`capacityStore := capacity.NewDynamoCapacityStore(...)`)
    INSIDE the `if substrate != "docker" && len(network.AvailabilityZones) > 0 {` block (line ~759),
    which is BEFORE the apply/sweep loop. You will likely need to hoist `capacityStore` and
    `rankInstanceType` to a scope visible inside the apply loop (declare `var capacityStore capacity.CapacityStore`
    + `var rankInstanceType string` before the block; assign inside).
  - The sweep loop classifies each failure at ~line 981 (`class := capacity.ClassifyError(stderrStr, applyErr)`)
    and breaks on success a few lines above (~line 975). The current AZ for an attempt is
    `network.AvailabilityZones[0]` (the loop rotates AZs — confirm exactly how the current AZ is tracked
    per attempt before recording; record the AZ that was actually attempted).
  - Determine the correct ICE class constant in pkg/capacity (e.g. capacity.ClassICE / the insufficient-capacity
    class). RecordICE should fire ONLY for the genuine insufficient-capacity class, not for spot-price/
    waiter-timeout/quota/auth/invalid. RecordSuccess fires on the successful-apply branch.
  - Both writes are BEST-EFFORT: wrap in a short context, log.Warn on error, NEVER fail or block the create.
- IAM: the create-handler role's DynamoDB grants live in module `km-operator-policy` (NOT in create-handler
  directly). Mirror the existing per-table pattern (see `aws_iam_role_policy "dynamodb_sandboxes"` at
  infra/modules/km-operator-policy/v1.0.0/main.tf:71): add a `dynamodb_capacity` policy granting
  GetItem/PutItem/UpdateItem/DescribeTable on `arn:aws:dynamodb:*:${account}:table/${var.capacity_table_name}`.
  Add a `capacity_table_name` variable to km-operator-policy/variables.tf, pass it from the create-handler
  module (main.tf module "km_operator_policy" block + a new variable in create-handler/variables.tf), and
  wire it from infra/live/use1/create-handler/terragrunt.hcl (derive `{label}-capacity`, same way the
  dynamodb-capacity live unit derives its table name).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RecordSuccess/RecordICE into the km create sweep loop (TDD)</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/create_az_sweep_test.go</files>
  <what>
    RED: add/extend a test in create_az_sweep_test.go asserting the sweep loop calls
    RecordSuccess(instanceType, az) on a successful apply and RecordICE(instanceType, az) on an
    ICE-classified failure, using a fake CapacityStore that records calls. Assert best-effort:
    a store that returns an error does NOT fail the create.
    GREEN: hoist capacityStore + rankInstanceType to loop scope; on the success branch call
    capacityStore.RecordSuccess(...); on an ICE-class failure call capacityStore.RecordICE(...).
    Best-effort (log.Warn on error, never return). Guard nil store (docker substrate path).
  </what>
  <verify>go test ./internal/app/cmd/ -run 'AZSweep|Capacity' -count=1 passes; go build ./... clean</verify>
  <done>RecordSuccess + RecordICE have production call sites in create.go; grep confirms; tests green</done>
</task>

<task type="auto">
  <name>Task 2: Grant the create-handler Lambda role dynamodb write on {prefix}-capacity</name>
  <files>infra/modules/km-operator-policy/v1.0.0/main.tf, infra/modules/km-operator-policy/v1.0.0/variables.tf, infra/modules/create-handler/v1.0.0/main.tf, infra/modules/create-handler/v1.0.0/variables.tf, infra/live/use1/create-handler/terragrunt.hcl</files>
  <what>
    Add a `dynamodb_capacity` aws_iam_role_policy in km-operator-policy (mirror dynamodb_sandboxes)
    granting GetItem/PutItem/UpdateItem/DescribeTable on the {prefix}-capacity table ARN. Add the
    `capacity_table_name` variable; thread it through the create-handler module and the live
    terragrunt unit (derive `{label}-capacity`). Run `terragrunt hclfmt` / `terraform fmt` on edited files.
    Do NOT run terragrunt apply — deploy is a separate operator checkpoint.
  </what>
  <verify>terraform fmt -check passes on edited modules; the new policy references var.capacity_table_name</verify>
  <done>create-handler role has UpdateItem on {prefix}-capacity via km-operator-policy; table name wired from live unit</done>
</task>

<task type="auto">
  <name>Task 3: Note the closed feedback loop in docs + STATE</name>
  <files>CLAUDE.md, .planning/STATE.md</files>
  <what>
    Update the Phase 124 mention to state the capacity store feedback loop is now closed
    (sweep writes RecordSuccess/RecordICE; create-handler role granted UpdateItem on {prefix}-capacity;
    deploy needs `make build-lambdas` + `km init --dry-run=false` for the IAM). Keep it brief.
  </what>
  <verify>CLAUDE.md mentions the write-back + the deploy requirement</verify>
  <done>docs reflect the closed loop and the redeploy requirement</done>
</task>

</tasks>

<verification>
- go build ./... clean; go test ./internal/app/cmd/ + ./pkg/capacity/ green.
- grep for RecordSuccess|RecordICE shows production call sites in create.go (not just tests/store.go).
- km-operator-policy has a capacity-table grant wired from the live unit; terraform fmt clean.
</verification>

<success_criteria>
- Capacity store write path wired into the sweep loop (both runtime paths via the subprocess).
- create-handler Lambda role granted dynamodb write on {prefix}-capacity.
- Feedback loop closed: RankAZs now reads a store the sweep populates.
- Tests green, build clean, terraform fmt clean. NO terragrunt apply (deploy is an operator checkpoint).
</success_criteria>
