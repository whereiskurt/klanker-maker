---
phase: 89
plan: 06
type: execute
wave: 2
depends_on:
  - 89-02
  - 89-04
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/doctor_secrets_test.go
  - docs/sandbox-secrets.md
  - CLAUDE.md
  - OPERATOR-GUIDE.md
autonomous: true
requirements:
  - SOPS-08-IAM-OPERATOR
  - SOPS-18-DOCTOR-CHECK
  - SOPS-22-DOCS

must_haves:
  truths:
    - "`checkSharedSecretsKey(ctx, kmsClient, localPrefix)` lives in doctor.go and follows the orphan-WARN shape of checkSESRules (doctor.go:3487-3552) verbatim with KMS substituted for SES"
    - "Returns CheckOK when `alias/${localPrefix}-sandbox-secrets` exists and no sibling aliases are present; CheckWarn with helpful Remediation when the own alias is missing; CheckWarn with sibling-list when other `*-sandbox-secrets` aliases exist (expected when sibling install present — message phrasing makes this clear)"
    - "Check is added to the default checkset (registered alongside checkSESRules); appears in `km doctor` output as `Shared secrets KMS key`"
    - "SOPS-08-IAM-OPERATOR no-op verified: `grep 'kms:\\*' infra/modules/km-operator-policy/v1.0.0/main.tf` returns ≥1 line (the broad operator KMS grant already in place; no change required for Phase 89)"
    - "`docs/sandbox-secrets.md` operator runbook exists covering: feature overview + when to use SOPS vs spec.execution.secrets, prerequisites (km bootstrap --shared-secrets-key, age key for offline test), workflow (sops binary install, create bundle, attach to profile via spec.secrets.sopsFile, km create), troubleshooting (decrypt 403 = check kms:ResourceAliases on key + sandbox IAM, first-boot timing pitfall), security model (one shared key per install, v2 deferred per-sandbox isolation)"
    - "CLAUDE.md gains a row in the 'Where to look' table linking to docs/sandbox-secrets.md, and adds spec.secrets fields to the schema overview if a schema list already exists"
    - "OPERATOR-GUIDE.md gains a ## SOPS secret injection section linking to docs/sandbox-secrets.md"
  artifacts:
    - path: internal/app/cmd/doctor.go
      provides: "checkSharedSecretsKey function + invocation from runDoctor default checkset"
      contains: "checkSharedSecretsKey"
    - path: internal/app/cmd/doctor_secrets_test.go
      provides: "Table tests: OK, WARN-missing-own, WARN-orphans, sibling prefix not falsely WARNed"
      contains: "TestCheckSharedSecretsKey"
      min_lines: 150
    - path: docs/sandbox-secrets.md
      provides: "Full operator runbook covering setup, usage, troubleshooting, security model"
      contains: "spec.secrets.sopsFile"
      min_lines: 80
    - path: CLAUDE.md
      provides: "Where-to-look row pointing at docs/sandbox-secrets.md; brief mention in CLI/spec section"
      contains: "sandbox-secrets"
    - path: OPERATOR-GUIDE.md
      provides: "New ## SOPS secret injection section with link to docs/sandbox-secrets.md"
      contains: "SOPS"
  key_links:
    - from: internal/app/cmd/doctor.go
      to: infra/modules/sandbox-secrets-key/v1.0.0/main.tf
      via: "checkSharedSecretsKey expects `alias/${prefix}-sandbox-secrets` name produced by 89-02's module"
      pattern: "alias/.*-sandbox-secrets"
    - from: docs/sandbox-secrets.md
      to: CLAUDE.md
      via: "CLAUDE.md Where-to-look table row links to docs/sandbox-secrets.md"
      pattern: "docs/sandbox-secrets\\.md"
---

<objective>
Two complementary closures of Phase 89: the operator-facing observability (doctor check) and the operator-facing documentation (docs/sandbox-secrets.md + CLAUDE.md row + OPERATOR-GUIDE.md section). Plus a one-line no-op verify confirming SOPS-08-IAM-OPERATOR doesn't need code changes.

Purpose: After 89-04 lets operators bootstrap the KMS key and 89-05 lets sandboxes use it, 89-06 ensures operators can VERIFY the install is healthy (`km doctor`) and READ how to use the feature (docs). Without this plan, the feature works but operators have no observability or runbook.

Output: One doctor function + one test file + three documentation files (one new, two extended).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-CONTEXT.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-RESEARCH.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-VALIDATION.md

# Template — checkSESRules verbatim shape
@internal/app/cmd/doctor.go

# Existing operator IAM (verify-only — should already have kms:*)
@infra/modules/km-operator-policy/v1.0.0/main.tf

# Docs being extended
@CLAUDE.md
@OPERATOR-GUIDE.md

<interfaces>
<!-- Doctor surface — extends KMSAliasLister from 89-04 with DescribeKey for completeness; reuse the same interface if possible -->
```go
// Interface (may already exist in doctor.go for the ListAliases-based scans):
type KMSAliasLister interface {
    ListAliases(ctx context.Context, params *kms.ListAliasesInput, optFns ...func(*kms.Options)) (*kms.ListAliasesOutput, error)
}

// New check:
func checkSharedSecretsKey(ctx context.Context, client KMSAliasLister, localPrefix string) CheckResult
```

<!-- Output format pinned by mirroring checkSESRules: -->
- CheckOK message: "alias alias/{prefix}-sandbox-secrets healthy"
- CheckWarn (missing): "alias alias/{prefix}-sandbox-secrets not found" + Remediation "Run `km bootstrap --shared-secrets-key`"
- CheckWarn (orphans): "orphan secrets KMS aliases: {comma-list}" + Remediation "Other installs' aliases — expected when sibling install present"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkSharedSecretsKey doctor check + RED tests + register in default checkset</name>
  <files>internal/app/cmd/doctor.go, internal/app/cmd/doctor_secrets_test.go</files>
  <behavior>
    Four subtests in doctor_secrets_test.go (RED → GREEN):

    - TestCheckSharedSecretsKey_OK: fake KMSAliasLister returns one alias matching `alias/km-sandbox-secrets`; localPrefix="km". Expect: Status==CheckOK; Message contains "healthy"; no Remediation needed.
    - TestCheckSharedSecretsKey_MissingOwn: fake returns empty alias list; localPrefix="km". Expect: Status==CheckWarn; Message contains "not found"; Remediation contains "km bootstrap --shared-secrets-key".
    - TestCheckSharedSecretsKey_OrphansPresent: fake returns BOTH `alias/km-sandbox-secrets` and `alias/km2-sandbox-secrets`; localPrefix="km". Expect: Status==CheckWarn; Message contains "orphan" and contains "alias/km2-sandbox-secrets" (the sibling); Remediation hints at sibling install being expected.
    - TestCheckSharedSecretsKey_OrphansWithoutOwn: fake returns ONLY `alias/km2-sandbox-secrets`; localPrefix="km". Expect: Status==CheckWarn for missing-own (takes precedence over orphan-list — pick the more-actionable warning; mirror checkSESRules' precedence).

    Implementation maps verbatim to 89-RESEARCH.md § "Pattern 4: Doctor Orphan-WARN" — code is given in full in research, just port it.

    Registration: locate where checkSESRules is invoked in the doctor flow (grep for `checkSESRules` — likely in runDoctor or a default-check-set construction). Add `checkSharedSecretsKey` immediately after.
  </behavior>
  <action>
    1. **Read existing checkSESRules pattern:**
       `grep -nE 'checkSESRules|CheckResult|CheckOK|CheckWarn|CheckError' internal/app/cmd/doctor.go | head -30`
       Note: the function signature shape, the CheckResult struct fields, and where checks are registered in the runDoctor flow.

    2. **Read existing KMSAliasLister usage** if any:
       `grep -nE 'KMSAliasLister|kms\\.ListAliases' internal/app/cmd/doctor.go internal/app/cmd/bootstrap.go | head -10`
       If 89-04 already declared `KMSAliasLister` interface, REUSE it. If not, declare a new one here (doctor.go is fine).

    3. **Write the RED tests** at `internal/app/cmd/doctor_secrets_test.go` with the four subtests in <behavior>. Use a fakeKMSAliasLister struct returning canned ListAliasesOutput. For pagination, return a single page (Truncated=false) — that's adequate for the test surface.

       Run: `go test ./internal/app/cmd/ -run TestCheckSharedSecretsKey 2>&1 | tail -20`
       MUST be RED.

    4. **Implement checkSharedSecretsKey** in doctor.go — port verbatim from 89-RESEARCH.md § "Pattern 4: Doctor Orphan-WARN" (lines 305-350). Constants:
       - `const checkNameSharedSecretsKey = "Shared secrets KMS key"`
       - suffix = `"-sandbox-secrets"`
       - expectAlias = `fmt.Sprintf("alias/%s-sandbox-secrets", localPrefix)`

    5. **Register in default checkset:** locate where checkSESRules is added to the slice of checks (grep for `checkSESRules(`). Add `checkSharedSecretsKey(ctx, kmsClient, localPrefix)` immediately after. Reuse the same `kmsClient` already in scope for KMS-related doctor checks (verify it exists — look for `kms.NewFromConfig`). If no KMS client is currently constructed in runDoctor, add one near where other AWS clients are built.

    6. **Re-run tests** — four GREEN:
       `go test ./internal/app/cmd/ -run TestCheckSharedSecretsKey -v 2>&1 | tail -30`

    7. **Regression sweep:** `go test ./internal/app/cmd/ -run TestDoctor 2>&1 | tail -10`

    8. `go vet ./internal/app/cmd/...` clean.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run TestCheckSharedSecretsKey -v 2>&1 | tail -30 && echo "---REGRESSION---" && go test ./internal/app/cmd/ -run TestDoctor 2>&1 | tail -10 && echo "---VET---" && go vet ./internal/app/cmd/... 2>&1 | tail -5 && echo "---REGISTRATION---" && grep -n checkSharedSecretsKey internal/app/cmd/doctor.go</automated>
    Expected: Four Phase-89 doctor tests PASS; existing TestDoctor* tests still PASS; vet silent; grep shows func definition + at least one registration call (≥2 hits).
  </verify>
  <done>checkSharedSecretsKey function added (mirrors checkSESRules shape), registered in default checkset, four subtests GREEN, no doctor regression.</done>
</task>

<task type="auto">
  <name>Task 2: docs/sandbox-secrets.md + CLAUDE.md row + OPERATOR-GUIDE.md section + SOPS-08 verify</name>
  <files>docs/sandbox-secrets.md, CLAUDE.md, OPERATOR-GUIDE.md</files>
  <action>
    1. **Create `docs/sandbox-secrets.md`** as a new file. Cover these sections (use ## headers; aim for 80-150 lines total):

       ```markdown
       # SOPS secret injection for sandboxes (Phase 89)

       Declarative SOPS-encrypted secrets attached to a SandboxProfile. At boot, the
       sandbox decrypts the bundle using a shared per-install KMS key and exposes the
       keys as environment variables in login shells (`/etc/sandbox-secrets.env` +
       `/etc/profile.d/zz-sandbox-secrets.sh`).

       ## When to use SOPS secrets vs `spec.execution.secrets`

       - **`spec.secrets.sopsFile`** (Phase 89): declarative bundle versioned with profiles, decrypted at boot, env-var exposed. Right answer for API keys (OPENAI_API_KEY, ANTHROPIC_API_KEY, GITHUB_TOKEN) that downstream tooling reads from env vars.
       - **`spec.execution.secrets`** (legacy SSM path): list of SSM SecureString paths injected at sandbox-side runtime. Right answer for secrets owned by other AWS systems (e.g., RDS connection strings provisioned by a separate stack).

       ## Prerequisites

       1. `km bootstrap --shared-secrets-key` (or `km bootstrap --all`) to provision the per-install KMS key. Verify with `km doctor` → look for `✓ Shared secrets KMS key healthy`.
       2. Install `sops` locally (the operator workstation): `brew install sops` (Mac) or download from `https://github.com/getsops/sops/releases`. (The sandbox-side binary is auto-pushed to S3 by `km init --sidecars`.)
       3. Optional but recommended: `brew install age` for offline encryption keys used in CI/CD testing.

       ## Workflow

       (Step-by-step: create a profile, encrypt a bundle with sops + the KMS alias from step 1, attach via spec.secrets.sopsFile, km create.)

       ## Repo layout convention

       `km configure` writes two lines to `.gitignore`:
       ```
       /secrets/*
       !/secrets/*.enc.yaml
       ```
       So encrypted bundles version with profiles but plaintext workdir files never leak. Encrypted bundles live under `secrets/`, names operator-chosen (e.g., `secrets/codex.enc.yaml`, `secrets/shared.enc.yaml`). Operators decide profile-to-bundle topology based on blast radius — km does NOT enforce 1:1 or 1:N.

       ## Encryption example

       ```bash
       # Find the KMS alias from km doctor or directly:
       aws kms list-aliases --query 'Aliases[?starts_with(AliasName, `alias/km-sandbox-secrets`)]'

       # Create plaintext
       cat > /tmp/codex.yaml <<EOF
       OPENAI_API_KEY: sk-...
       EOF

       # Encrypt with the KMS alias (NOT raw key ARN — alias enables cross-prefix isolation)
       sops --encrypt --kms 'arn:aws:kms:us-east-1:<acct>:alias/km-sandbox-secrets' \
            --input-type yaml --output-type yaml /tmp/codex.yaml > secrets/codex.enc.yaml
       rm /tmp/codex.yaml

       # Reference from profile
       cat >> profiles/codex.yaml <<EOF
       spec:
         secrets:
           sopsFile: ./secrets/codex.enc.yaml
       EOF
       km validate profiles/codex.yaml
       km create profiles/codex.yaml
       ```

       ## Troubleshooting

       - **Decrypt 403 at boot**: check (a) sandbox role has `kms:Decrypt` with `kms:ResourceAliases` condition matching `alias/${resource_prefix}-sandbox-secrets` (auto-emitted by ec2spot v1.2.0+); (b) the alias is actually attached to the key (terraform handles this, but check `aws kms list-aliases` if first-boot fails).
       - **Sandbox boots but env vars missing**: check `/var/log/cloud-init-output.log` for the `[km-bootstrap] FATAL: sops decrypt failed` line. Check `cat /etc/sandbox-secrets.env` (root-readable only); if file exists but is empty, the dotenv conversion failed — check the bundle for non-ASCII or quoted values.
       - **`km validate` fails with "missing 'sops:' metadata block"**: file is plaintext, not sops-encrypted. Re-run `sops --encrypt`.

       ## Security model (v1 → v2)

       v1 (Phase 89): one shared KMS key per install (`alias/${resource_prefix}-sandbox-secrets`). Any sandbox in the install can decrypt any bundle if it gets the file (per-sandbox isolation comes from S3 IAM scoping to `sandboxes/${sandbox_id}/secrets.enc.yaml`, NOT KMS). Blast radius: a compromised sandbox can decrypt its own bundle only (S3 ARN scope), but the key itself is broadly usable within the install.

       v2 (deferred): per-profile or per-sandbox KMS key isolation via key policy + `aws:PrincipalTag`. Schema-compatible — v1 profiles will keep working when v2 ships.

       ## Operator commands cheat-sheet

       | Command | What it does |
       |---------|---|
       | `km bootstrap --shared-secrets-key` | Provision the shared KMS key (one-time per install) |
       | `km bootstrap --shared-secrets-key --plan` | Preview with Phase 84.2 destroy-class gate |
       | `km doctor` | Verifies alias exists + warns on orphan sibling aliases |
       | `km uninit` | Deletes own-prefix alias + schedule-deletes own key (7-day window); sibling installs untouched |
       | `km init --sidecars` | Pushes sops binary to S3 (one-time per phase/sops-version bump) |
       ```

    2. **Edit `CLAUDE.md`** — add ONE row to the "Where to look" table (locate the table — first table in the file, "You want to… / Look at" columns). Insert after the "Send / receive email from inside a sandbox" row:
       ```
       | Inject SOPS-encrypted secrets into a sandbox | `docs/sandbox-secrets.md` (Phase 89) |
       ```
       Also: if there's a CLI commands list, add `km bootstrap --shared-secrets-key` to it (search for the existing `km bootstrap` entry). Do NOT rewrite other CLAUDE.md sections — additive only.

    3. **Edit `OPERATOR-GUIDE.md`** — add a new top-level section near the existing platform-setup sections:
       ```markdown
       ## SOPS secret injection

       Declarative secret injection into sandboxes via `spec.secrets.sopsFile`. Full
       runbook in [docs/sandbox-secrets.md](docs/sandbox-secrets.md). One-time setup:

       ```
       km bootstrap --shared-secrets-key
       km init --sidecars       # if not already done since Phase 89 shipped
       km configure              # writes /secrets/* + !/secrets/*.enc.yaml to .gitignore
       ```

       Then encrypt a bundle with `sops --kms alias/${resource_prefix}-sandbox-secrets`
       and reference it from a profile via `spec.secrets.sopsFile`.
       ```
       Place this section near other one-time-setup runbooks (e.g., after `## Slack integration` or `## SES setup` — whichever pattern exists; grep for `^## ` to find existing top-level sections).

    4. **SOPS-08-IAM-OPERATOR verify (one-line check, no code change):**
       ```
       grep -n 'kms:\*' infra/modules/km-operator-policy/v1.0.0/main.tf
       ```
       Must return ≥1 line at or around the documented line 484. If MISSING (unexpected — would invalidate research), STOP and report; do NOT add the policy in this plan. (Adding broad kms:* to operator IAM is a security-class decision that needs explicit owner approval.)

       Add a comment in 89-06-SUMMARY.md confirming this grep returned non-empty.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f docs/sandbox-secrets.md && wc -l docs/sandbox-secrets.md && echo "---" && grep -c 'sandbox-secrets' CLAUDE.md && grep -c 'docs/sandbox-secrets\.md' CLAUDE.md && echo "---" && grep -c 'SOPS secret injection' OPERATOR-GUIDE.md && echo "---SOPS-08-VERIFY---" && grep -n 'kms:\*' infra/modules/km-operator-policy/v1.0.0/main.tf | head -3</automated>
    Expected: docs/sandbox-secrets.md exists with ≥80 lines; CLAUDE.md mentions 'sandbox-secrets' ≥1 time AND contains link to docs/sandbox-secrets.md; OPERATOR-GUIDE.md has 'SOPS secret injection' header; operator policy grep returns ≥1 'kms:*' line (proving SOPS-08 no-op).
  </verify>
  <done>docs/sandbox-secrets.md (new, 80+ lines), CLAUDE.md Where-to-look row added + link present, OPERATOR-GUIDE.md ## SOPS secret injection section added, SOPS-08 grep verified.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run 'TestCheckSharedSecretsKey|TestDoctor'` — all PASS.
- `go vet ./internal/app/cmd/...` — silent.
- `test -f docs/sandbox-secrets.md && grep -q 'spec.secrets.sopsFile' docs/sandbox-secrets.md` — present.
- `grep -c 'docs/sandbox-secrets.md' CLAUDE.md` — ≥1.
- `grep -c 'SOPS secret injection' OPERATOR-GUIDE.md` — ≥1.
- `grep -c 'kms:\*' infra/modules/km-operator-policy/v1.0.0/main.tf` — ≥1 (SOPS-08 no-op confirmed).
</verification>

<success_criteria>
- SOPS-08-IAM-OPERATOR: verified no-op — operator policy already has `kms:*` (no code change shipped, just confirmed).
- SOPS-18-DOCTOR-CHECK: checkSharedSecretsKey present + registered + four-state test coverage (OK / missing-own / orphans / missing-own-with-orphans precedence).
- SOPS-22-DOCS: docs/sandbox-secrets.md + CLAUDE.md row + OPERATOR-GUIDE.md section all in place.
</success_criteria>

<output>
After completion, create `.planning/phases/89-sops-secret-injection-for-sandboxes/89-06-SUMMARY.md` summarizing:
- doctor.go: checkSharedSecretsKey added, registered in default checkset; four tests GREEN
- New file: docs/sandbox-secrets.md (line count + section list)
- CLAUDE.md row + which existing row it's adjacent to
- OPERATOR-GUIDE.md new section position
- SOPS-08 verify: grep returned the exact line numbers in km-operator-policy/v1.0.0/main.tf (paste the matches)
- Operator follow-up: `make build` (so `km doctor` picks up the new check), then any operator who runs `km doctor` post-bootstrap will see `✓ Shared secrets KMS key healthy`.
</output>
