---
phase: 89
plan: 07
type: execute
wave: 3
depends_on:
  - 89-03
  - 89-04
  - 89-05
  - 89-06
files_modified:
  - .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md
  - profiles/codex.yaml
autonomous: false
requirements:
  - SOPS-23-UAT-ACCEPTANCE

must_haves:
  truths:
    - "make build + make sidecars + km init --sidecars produce + deploy the new km binary (with profile schema understanding spec.secrets) + the sops binary at s3://${bucket}/binaries/sops"
    - "km bootstrap --shared-secrets-key creates a real `alias/${prefix}-sandbox-secrets` KMS key in the install's AWS account; `km doctor` reports `✓ Shared secrets KMS key healthy`"
    - "A real encrypted bundle (secrets/codex.enc.yaml, sops --kms alias/...) attached to profiles/codex.yaml via spec.secrets.sopsFile drives a sandbox boot to a successful state where /etc/sandbox-secrets.env exists with OPENAI_API_KEY=sk-..."
    - "Codex inside that sandbox makes a real api.openai.com call (via `km agent run` or shell + curl through the http-proxy MITM); a `BUDGET#ai#gpt-*` row appears in km-budgets DynamoDB within ~30s of the request, proving Phase 88's OpenAI meter + Phase 89's secret injection compose end-to-end with NO operator post-create wiring"
    - "Compared to Phase 88 plan 07's UAT, no out-of-band SSM SecureString or SSM SendCommand was needed — the OPENAI_API_KEY landed via the declarative sops path alone"
  artifacts:
    - path: .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md
      provides: "Operator runbook with 5 scenarios + observed-results section + fresh-install plan-output expectation + escape-hatch (--i-accept-destroys) workaround note"
      contains: "Scenario 1"
      min_lines: 130
    - path: profiles/codex.yaml
      provides: "Updated to declare spec.secrets.sopsFile: ./secrets/codex.enc.yaml; removes legacy `OPENAI_API_KEY: ''` placeholder if present"
      contains: "spec.secrets"
  key_links:
    - from: profiles/codex.yaml
      to: secrets/codex.enc.yaml
      via: "profile-relative path resolution via filepath.Join(filepath.Dir(profilePath), spec.secrets.sopsFile)"
      pattern: "secrets/codex\\.enc\\.yaml"
    - from: .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md
      to: km-budgets DynamoDB table
      via: "UAT runs km status and `aws dynamodb get-item` to confirm BUDGET#ai#gpt-* row appeared after the OpenAI call"
      pattern: "BUDGET#ai#gpt-"
---

<objective>
Live end-to-end UAT for Phase 89 — prove the declarative secret injection path works against real AWS, real KMS, real sandbox boot, real Phase 88 OpenAI metering. NOT autonomous — Task 2 is a `checkpoint:human-verify` requiring the operator to run live commands and confirm DDB rows.

Purpose: Wave 0 + Wave 1 + Wave 2 exercise correctness via unit tests with stubs (mock S3, mock KMS, age-encrypted fixtures). Only a live sandbox can prove the full chain: real KMS decrypt → real bundle download → real sops binary execution → real env exposure → real api.openai.com call → real DynamoDB write.

Output: One UAT runbook + one updated profiles/codex.yaml. NOT autonomous — Task 2 is operator-checkpoint.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-CONTEXT.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-VALIDATION.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-01-PLAN.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-02-PLAN.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-03-PLAN.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-04-PLAN.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-05-PLAN.md
@.planning/phases/89-sops-secret-injection-for-sandboxes/89-06-PLAN.md

# Reference UAT runbook to mirror — same shape as Phase 88 plan 07
# (read this to match the formatting + scenario-numbering convention)

# Profile to update
@profiles/codex.yaml

# Documentation referenced by UAT
@docs/sandbox-secrets.md
@CLAUDE.md

<tasks>

<task type="auto">
  <name>Task 1: Build + deploy + update codex.yaml + author UAT runbook</name>
  <files>profiles/codex.yaml, .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md</files>
  <action>
    Three sequential blocks. Each must succeed before proceeding.

    **A. Build + deploy (operator workstation):**
    ```bash
    cd /Users/khundeck/working/klankrmkr
    make build              # picks up profile schema change for spec.secrets + bootstrap CLI
    make sidecars           # rebuilds http-proxy + bridge etc. (no schema change, but stay consistent)
    km init --sidecars      # pushes sops binary to S3 (consumes Phase 89-03 fetchAndUploadSops)
    km init --lambdas       # picks up the schema-aware km binary in the create-handler Lambda
    km init --plan          # preview foundation changes including s3-artifacts-lifecycle v1.1.0 bump
    # If the plan is clean / only the additive lifecycle rule is shown:
    km init                 # apply, picks up s3-artifacts-lifecycle v1.1.0 (sandbox-secrets 7-day rule)

    # --- Step A.bootstrap (Pitfall 7 fresh-install expectation) ---
    km bootstrap --shared-secrets-key --plan
    # Expected output on a FRESH install:
    #   ~3 creates (aws_kms_key, aws_kms_alias, possibly an account-id data source)
    #   ZERO destroys, ZERO replaces, ZERO trip messages from the Phase 84.2 destroy-class gate.
    # The gate fires only on destroy/replace — creates do NOT trip it.
    #
    # If the gate DOES trip unexpectedly on a fresh install (e.g., prevent_destroy lifecycle
    # being added to a fresh resource somehow surfaces as a "replace" diff, or plugin-cache state
    # misreads a create as a replace):
    #   1. Run:  km bootstrap --shared-secrets-key --i-accept-destroys --plan
    #      This surfaces the underlying diff so it can be diagnosed.
    #   2. Capture the diff in the UAT log (paste into Observed Results, Scenario A).
    #   3. Determine if the diff is truly destructive (true positive) or a false alarm.
    #      For a fresh install, only true positive would be a schema misconfiguration.
    #   4. Once reviewed, re-run WITHOUT --plan to actually apply:
    #         km bootstrap --shared-secrets-key
    #
    # --i-accept-destroys is owned by Phase 84.2 — not introduced by Phase 89.

    km bootstrap --shared-secrets-key             # provision the per-install KMS key
    km doctor 2>&1 | grep -i secret               # expect: ✓ Shared secrets KMS key healthy
    km configure                                  # idempotently appends gitignore lines (89-03)
    grep -q '^/secrets/\*$' .gitignore && grep -q '^!/secrets/\*.enc.yaml$' .gitignore && echo "gitignore OK"
    ```

    **B. Update profiles/codex.yaml:** read the existing file. If it has a placeholder `OPENAI_API_KEY: ""` field under spec.execution or similar, REMOVE that line (replaced by the sops bundle). Add a new top-level section:
    ```yaml
    spec:
      # ... existing fields ...
      secrets:
        sopsFile: ./secrets/codex.enc.yaml
    ```
    Run `km validate profiles/codex.yaml` — must succeed. Note: the bundle file doesn't exist yet (operator creates it in scenario 2 of the UAT), so initially the file-existence check will fail — that's expected during this Task A pre-flight. Run validate AFTER the bundle is created.

    Alternative: if the team prefers to leave profiles/codex.yaml unchanged and create a sibling `profiles/codex-with-sops.yaml`, document that in the UAT runbook. Either works; mirror whatever Phase 88's codex.yaml integration did.

    **C. Author the UAT runbook** at `.planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md`. Five scenarios + Observed-Results section + Outcome verdict line at the bottom. Frontmatter shape (mirror Phase 88 plan 07's UAT.md):

    ```markdown
    ---
    phase: 89
    plan: 07
    type: uat
    status: pending
    created: <today>
    ---

    # Phase 89 — Live UAT: SOPS Secret Injection End-to-End

    Pre-conditions (verify before running scenarios):
    - [ ] make build + make sidecars + km init --sidecars + km init --lambdas + km init (full) completed
    - [ ] km bootstrap --shared-secrets-key succeeded; km doctor shows ✓ Shared secrets KMS key healthy
    - [ ] km configure has been run; .gitignore contains /secrets/* + !/secrets/*.enc.yaml
    - [ ] AWS_PROFILE points at the install's application account; can `aws kms list-aliases`
    - [ ] sops v3.13.1 installed locally (operator workstation): `which sops && sops --version`

    ## Scenario A (pre-bootstrap): Fresh-install km bootstrap --shared-secrets-key --plan expectation

    Goal: Confirm the Phase 84.2 destroy-class gate does NOT trip on a fresh install (Pitfall 7 from RESEARCH.md).

    Steps:
    1. Run: `km bootstrap --shared-secrets-key --plan`
    2. Inspect output. Expected:
       - ~3 creates: `aws_kms_key.secrets[0]`, `aws_kms_alias.secrets[0]`, possibly a data-source lookup for account id.
       - ZERO destroys.
       - ZERO replaces.
       - NO "destroy-class change detected" trip messages from the gate.
    3. **If the gate trips unexpectedly** (unlikely on fresh install — would indicate a schema misconfiguration or plugin-cache anomaly):
       - Run: `km bootstrap --shared-secrets-key --i-accept-destroys --plan`
       - Paste the resulting diff into Observed Results below.
       - Determine if the diff is truly destructive (true positive). For a fresh install, only true positive would be a schema misconfiguration.
       - Once reviewed and reviewed-safe, re-run WITHOUT --plan to actually apply.
    4. Apply: `km bootstrap --shared-secrets-key` (without --plan) once Step 2 passes (or Step 3 is reviewed-safe).
    5. Verify: `km doctor 2>&1 | grep -i secret` should now print `✓ Shared secrets KMS key healthy`.

    PASS criterion: Step 2 shows ~3 creates / 0 destroys / 0 replaces; Step 5 prints the healthy message. (If Step 3 fires, document the escape-hatch usage and the diff.)

    ## Scenario 1: Encrypt a bundle with the new shared KMS alias

    Goal: verify the operator-side encryption workflow works against the per-install KMS key.

    Steps:
    1. Run: `aws kms list-aliases --query 'Aliases[?starts_with(AliasName, ` + "`alias/$(km env | grep KM_RESOURCE_PREFIX | cut -d= -f2)-sandbox-secrets`)" + `].AliasArn' --output text` — record the alias ARN.
    2. Create plaintext: `cat > /tmp/codex-plain.yaml <<EOF`
       `OPENAI_API_KEY: sk-<real-openai-api-key>`
       `EOF`
    3. Encrypt: `sops --encrypt --kms <alias-arn-from-step-1> --input-type yaml --output-type yaml /tmp/codex-plain.yaml > secrets/codex.enc.yaml`
    4. Verify: `head -5 secrets/codex.enc.yaml` shows ENC[AES256_GCM,...] lines; `tail -20 secrets/codex.enc.yaml` shows the `sops:` metadata block with `kms:` entry.
    5. `rm /tmp/codex-plain.yaml` — DO NOT commit plaintext.
    6. `km validate profiles/codex.yaml` — must succeed.

    PASS criterion: secrets/codex.enc.yaml exists, has `sops:` block, km validate passes.

    ## Scenario 2: km create the codex sandbox

    Goal: full creation flow including bundle upload + ec2spot v1.2.0 IAM attachment.

    Steps:
    1. `km create profiles/codex.yaml --wait 2>&1 | tee /tmp/sb-create.log`
    2. Capture sandbox ID: `SB=$(grep -oE 'sb-[a-f0-9]+' /tmp/sb-create.log | head -1)`
    3. Verify bundle was uploaded: `aws s3 ls s3://${KM_ARTIFACTS_BUCKET}/sandboxes/${SB}/secrets.enc.yaml`
    4. Inspect cloud-init output once the sandbox is up: `km shell ${SB} -- sudo grep -A2 'SOPS bundle' /var/log/cloud-init-output.log`

    PASS: create succeeds, bundle in S3, cloud-init log shows `Decrypting SOPS bundle...` and `SOPS bundle decrypted to /etc/sandbox-secrets.env`.

    ## Scenario 3: Verify env-var exposure inside the sandbox

    Goal: confirm /etc/sandbox-secrets.env + /etc/profile.d/zz-sandbox-secrets.sh wired correctly.

    Steps:
    1. `km shell ${SB} -- bash -lc 'sudo ls -la /etc/sandbox-secrets.env /etc/profile.d/zz-sandbox-secrets.sh'`
       - Expect: secrets.env is root:root 0400; profile.d script is root:root 0644.
    2. `km shell ${SB} -- bash -lc 'echo $OPENAI_API_KEY | cut -c1-7'`
       - Expect: prints `sk-` followed by ~4 chars (NOT empty).
       - If empty: cloud-init log will show the failure; check /var/log/cloud-init-output.log inside the sandbox.

    PASS: env var visible in login shell with the correct prefix.

    ## Scenario 4: Codex inside sandbox accrues BUDGET#ai#gpt-* via Phase 88 meter

    Goal: prove Phase 88 + Phase 89 compose end-to-end.

    Steps:
    1. `km agent run ${SB} --prompt "Say 'phase 89 end-to-end works' once, nothing else" --wait 2>&1 | tee /tmp/sb-agent.log`
       - Codex picks up OPENAI_API_KEY from env (via profile.d sourcing); makes a real api.openai.com call; http-proxy intercepts it.
    2. Wait ~30s for DDB to settle.
    3. Query DDB for the budget row: `aws dynamodb query --table-name km-budgets --key-condition-expression 'PK = :pk' --expression-attribute-values "{\":pk\":{\"S\":\"BUDGET#${SB}\"}}" --query 'Items[?starts_with(SK.S, ` + "`ai#gpt-`)" + `]'`
       - Expect: ≥1 row with `spentUSD > 0` and modelID like `gpt-5`, `gpt-4o`, etc.
    4. Cross-check: `km status ${SB}` — AI spend section shows non-zero gpt-* entry.

    PASS: BUDGET#ai#gpt-* row exists with spentUSD > 0 — proves the entire chain (sops decrypt → env exposure → Codex picks up key → real OpenAI call → http-proxy meters → DDB write) worked WITHOUT operator post-create wiring.

    ## Scenario 5: km destroy cleans up the S3 bundle

    Goal: confirm Phase 89 destroy.go cleanup fires (and is non-fatal).

    Steps:
    1. `km destroy ${SB} --remote --yes`
    2. `aws s3 ls s3://${KM_ARTIFACTS_BUCKET}/sandboxes/${SB}/secrets.enc.yaml` — expect: NoSuchKey or empty output (object deleted by Phase 89 destroy path).
    3. Optional: confirm the S3 lifecycle rule from 89-02 exists: `aws s3api get-bucket-lifecycle-configuration --bucket ${KM_ARTIFACTS_BUCKET} --query 'Rules[?ID==` + "`sandbox-secrets-7day`]'" + ` ` — expect non-empty.

    PASS: bundle no longer in S3 post-destroy; lifecycle rule is present as belt-and-suspenders.

    ## Scenario 6 (optional): km uninit symmetric cleanup (do NOT run in production)

    Goal: verify uninit removes own-prefix KMS only. SKIP unless a throwaway install is available.

    Steps:
    1. (test install only) `km uninit --yes`
    2. `aws kms list-aliases --query 'Aliases[?starts_with(AliasName, ` + "`alias/${KM_RESOURCE_PREFIX}-sandbox-secrets`)" + `]'` — expect empty.
    3. `aws kms describe-key --key-id <previously-noted-key-id>` — expect `KeyState: PendingDeletion`, `DeletionDate` ≈ now+7days.
    4. If a sibling install was set up: verify its alias still exists.

    PASS: own alias removed, own key scheduled for deletion, siblings untouched.

    ## Observed Results

    (Fill in during execution — each scenario's actual output, any deltas from expected, screenshots/logs if helpful. Be sure to capture Scenario A's plan output verbatim to confirm the fresh-install gate expectation.)

    ## Outcome

    PASS | PARTIAL PASS (note which scenarios) | FAIL (with root cause)
    ```

    Save this UAT runbook. Do NOT execute the scenarios in this Task — Task 2 is the operator-checkpoint where the operator runs them.
  </action>
  <verify>
    <automated>test -f .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md && wc -l .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md && echo "---" && grep -cE 'Scenario [A1-5]' .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md && echo "---I-ACCEPT-DESTROYS---" && grep -c 'i-accept-destroys' .planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md && echo "---PROFILES---" && grep -A2 'secrets:' profiles/codex.yaml 2>&1 | head -10 && echo "---BUILD---" && make build 2>&1 | tail -5</automated>
    Expected: UAT.md exists with ≥130 lines + ≥6 Scenario headings (A + 1–5) + ≥1 mention of `i-accept-destroys` (the escape hatch); profiles/codex.yaml contains `secrets:` block with `sopsFile`; make build succeeds.
  </verify>
  <done>UAT runbook authored with Scenario A (fresh-install plan expectation + escape-hatch workaround) plus 5 mandatory + 1 optional scenarios, profiles/codex.yaml updated to declare spec.secrets.sopsFile, make build succeeds. Task 2 (operator checkpoint) ready to run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs UAT scenarios + records observed results</name>
  <files>(no source files — operator runs live AWS commands; results captured in 89-07-UAT.md from Task 1)</files>
  <what-built>
    Phase 89's full declarative-SOPS path is now implemented end-to-end:
    - Profile schema: spec.secrets.sopsFile (89-01)
    - KMS module + S3 lifecycle + ec2spot IAM bump (89-02)
    - sops binary in S3 + gitignore (89-03)
    - km bootstrap --shared-secrets-key + uninit cleanup with orphan-recovery (89-04)
    - Compiler userdata block + create.go upload + destroy.go cleanup (89-05)
    - km doctor check + docs (89-06)
    - 89-07 Task 1: UAT runbook + profiles/codex.yaml updated

    What needs operator-verified ground truth: that ALL OF THE ABOVE compose against real AWS infrastructure to produce a Codex sandbox that boots with OPENAI_API_KEY injected via sops and meters real api.openai.com traffic into BUDGET#ai#gpt-* DDB rows WITHOUT any out-of-band operator wiring (the gap that Phase 89 was created to close).
  </what-built>
  <action>
    Hand control to the operator to execute Scenario A + the 5 mandatory UAT scenarios (+ 1 optional) from `.planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md`. Claude does NOT run these commands — they require live AWS credentials, a real OpenAI API key (operator-owned, real money), and tolerance for ~$0.001-$0.05 in OpenAI charges per Codex turn. The operator runs the scenarios; Claude waits at the resume-signal.

    Operator workflow:
    1. Open `.planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md` in another window.
    2. Verify all pre-conditions in the runbook's top section.
    3. Execute Scenario A (pre-bootstrap plan-output expectation), then Scenarios 1-5 in order. Record actual output in each scenario's section.
    4. (Optional, throwaway install only) Execute Scenario 6.
    5. Update the runbook's "Observed Results" section + "Outcome" line + frontmatter `status` field.
    6. Report "approved" (or detailed failure description) via the resume-signal.

    Total operator time estimate: ~30-45 minutes happy path; +15-30 minutes per failing scenario.
  </action>
  <how-to-verify>
    Execute Scenario A + scenarios 1-5 from `.planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md` in order. Scenario 6 is OPTIONAL and only safe on a throwaway install.

    For each scenario:
    1. Run the documented steps.
    2. Compare actual output to the PASS criterion in the runbook.
    3. Record the actual output (truncated to <50 lines per scenario) in the runbook's "Observed Results" section.
    4. If any scenario fails: stop, root-cause, and either fix in-place (if it's a fixable misconfiguration) or escalate to gap-closure (if it's a Phase 89 implementation defect — file as a follow-up `/gsd:plan-phase 89 --gaps`).

    After all scenarios complete:
    - Fill in the "Outcome" line at the bottom of the runbook (PASS / PARTIAL PASS / FAIL).
    - Update the frontmatter `status:` from `pending` to `passed` / `partial` / `failed`.
  </how-to-verify>
  <verify>Operator-reported pass/fail of Scenario A + Scenarios 1-5 (Scenario 6 optional). No Claude-automatable verify command — that's the whole point of the checkpoint. After resume, Claude reads the updated 89-07-UAT.md to confirm status is no longer `pending` and Observed Results is non-empty.</verify>
  <done>Operator types "approved" via resume-signal AFTER Scenario A + Scenarios 1-5 PASS, AND the 89-07-UAT.md file has frontmatter status: passed + populated Observed Results. On failure, branch to `/gsd:plan-phase 89 --gaps` from the failure entries captured in 89-07-UAT.md.</done>
  <resume-signal>Type "approved" (all scenarios PASS) or describe issues (per-scenario observed results that diverged from the runbook's PASS criteria).</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/89-sops-secret-injection-for-sandboxes/89-07-UAT.md` exists, ≥130 lines, includes Scenario A + Scenario 1-5 headings + Observed Results + Outcome, mentions `--i-accept-destroys` as the escape hatch.
- `profiles/codex.yaml` declares `spec.secrets.sopsFile: ./secrets/codex.enc.yaml`.
- After operator-checkpoint completion: UAT.md frontmatter `status` field updated to one of `passed | partial | failed`; Observed Results section populated.
</verification>

<success_criteria>
- SOPS-23-UAT-ACCEPTANCE: Live: a Codex sandbox with `spec.secrets.sopsFile: ./secrets/codex.enc.yaml` boots, picks up OPENAI_API_KEY from the decrypted bundle, makes a real api.openai.com call via Codex, and Phase 88's http-proxy meter writes `BUDGET#ai#gpt-*` to DynamoDB — with NO operator out-of-band wiring (no SSM SendCommand, no SecureString hand-off).
- The Phase 88 UAT regression (out-of-band SSM SecureString workaround used in plan 88-07) is now obsolete — `km destroy && km create` with the new profile yields the same Phase 88 metering behavior without operator post-create steps.
- Fresh-install Pitfall 7 expectation documented in UAT Scenario A: `--plan` on fresh install must show ~3 creates / 0 destroys; `--i-accept-destroys` is documented as the operator escape hatch if the gate trips unexpectedly.
</success_criteria>

<output>
After completion, the 89-07-UAT.md file IS the SUMMARY for this plan (mirrors Phase 88-07 pattern). Update:
- frontmatter status: pending → passed | partial | failed
- Observed Results section: populated with actual command output excerpts
- Outcome line: PASS | PARTIAL PASS (scenarios X, Y) | FAIL (root cause: ...)

If FAIL or PARTIAL PASS: kick off `/gsd:plan-phase 89 --gaps` to plan gap-closure work. If PASS: Phase 89 is shippable — proceed to `/gsd:verify-work 89`.
</output>
</output>
