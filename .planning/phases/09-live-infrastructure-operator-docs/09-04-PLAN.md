---
phase: 09-live-infrastructure-operator-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/compiler/budget_enforcer_hcl.go
  - pkg/compiler/budget_enforcer_hcl_test.go
autonomous: true
gap_closure: true
requirements:
  - PROV-05
  - BUDG-02
  - BUDG-06
  - BUDG-07
  - MAIL-01
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "Budget enforcer template references build/budget-enforcer.zip, matching Makefile output path"
    - "All existing budget enforcer tests continue to pass"
    - "Generated HCL for any sandbox references build/ not dist/"
  artifacts:
    - path: "pkg/compiler/budget_enforcer_hcl.go"
      provides: "Budget enforcer Terragrunt HCL template with correct lambda_zip_path"
      contains: "build/budget-enforcer.zip"
    - path: "pkg/compiler/budget_enforcer_hcl_test.go"
      provides: "Tests that verify the correct lambda zip path"
      contains: "build/budget-enforcer.zip"
  key_links:
    - from: "pkg/compiler/budget_enforcer_hcl.go"
      to: "Makefile"
      via: "lambda_zip_path references build/budget-enforcer.zip matching Makefile build-lambdas output"
      pattern: "build/budget-enforcer\\.zip"
---

<objective>
Fix the budget-enforcer lambda_zip_path directory mismatch identified in 09-VERIFICATION.md.

Purpose: The budget enforcer HCL template hardcodes `dist/budget-enforcer.zip` but `make build-lambdas` outputs to `build/budget-enforcer.zip`. This causes every per-sandbox budget-enforcer `terragrunt apply` to fail at Terraform validation with a missing zip error. The `dist/` directory does not exist.

Output: Corrected template and test confirming the path matches the Makefile output.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-live-infrastructure-operator-docs/09-VERIFICATION.md
@pkg/compiler/budget_enforcer_hcl.go
@pkg/compiler/budget_enforcer_hcl_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lambda_zip_path in budget enforcer template and add path assertion to tests</name>
  <files>pkg/compiler/budget_enforcer_hcl.go, pkg/compiler/budget_enforcer_hcl_test.go</files>
  <action>
1. In `pkg/compiler/budget_enforcer_hcl.go` line 68, change:
   `lambda_zip_path    = "${local.repo_root}/dist/budget-enforcer.zip"`
   to:
   `lambda_zip_path    = "${local.repo_root}/build/budget-enforcer.zip"`

   This aligns with the Makefile `build-lambdas` target which outputs to `build/budget-enforcer.zip` (Makefile line 71), and is consistent with the ttl-handler terragrunt.hcl which already correctly references `build/ttl-handler.zip`.

2. In `pkg/compiler/budget_enforcer_hcl_test.go`, add `"build/budget-enforcer.zip"` to the `checks` slice in `TestGenerateBudgetEnforcerHCL_EC2` (after the existing `"lambda_zip_path"` check). This ensures the specific path is verified, not just the presence of the field name.

   Similarly add `"build/budget-enforcer.zip"` to the `checks` slice in `TestGenerateBudgetEnforcerHCL_ECS`.

   Do NOT remove any existing checks -- only add the new path-specific assertion.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/compiler/... -run BudgetEnforcer -count=1 -v</automated>
  </verify>
  <done>
- Line 68 of budget_enforcer_hcl.go references `build/budget-enforcer.zip` (not `dist/`)
- All 4 budget enforcer tests pass: TestGenerateBudgetEnforcerHCL_EC2, TestGenerateBudgetEnforcerHCL_ECS, TestCompileBudgetEnforcerHCL_WithBudget, TestCompileBudgetEnforcerHCL_NoBudget
- `grep -r "dist/budget-enforcer" pkg/compiler/` returns no results
  </done>
</task>

</tasks>

<verification>
1. `go test ./pkg/compiler/... -count=1` -- full compiler test suite passes
2. `grep -r "dist/budget-enforcer" pkg/` -- no remaining references to the incorrect dist/ path
3. `grep "build/budget-enforcer.zip" pkg/compiler/budget_enforcer_hcl.go` -- confirms the fix
</verification>

<success_criteria>
- The budget enforcer HCL template references `build/budget-enforcer.zip`, matching the Makefile `build-lambdas` output path
- All compiler tests pass
- No references to `dist/budget-enforcer` remain in the compiler package
</success_criteria>

<output>
After completion, create `.planning/phases/09-live-infrastructure-operator-docs/09-04-SUMMARY.md`
</output>
