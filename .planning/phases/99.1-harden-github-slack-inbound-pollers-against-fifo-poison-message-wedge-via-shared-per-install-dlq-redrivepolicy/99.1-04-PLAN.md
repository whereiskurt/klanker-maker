---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 04
type: tdd
wave: 3
depends_on: ["01", "03"]
files_modified:
  - internal/app/cmd/doctor_inbound_dlq_test.go
  - internal/app/cmd/doctor_inbound_dlq.go
  - internal/app/cmd/doctor.go
  - docs/github-bridge.md
  - docs/slack-notifications.md
  - OPERATOR-GUIDE.md
autonomous: true
requirements: [GH-DLQ-DOCTOR, GH-DLQ-DEPLOY]
must_haves:
  truths:
    - "km doctor reports inbound-DLQ depth: SKIP when SQS client nil, SKIP when the DLQ doesn't exist, OK when depth==0, WARN when depth>0"
    - "The deploy sequence is documented: make build + make build-lambdas + km init --dry-run=false (NO --sidecars); existing sandboxes need km destroy && km create (no silent backfill)"
    - "The deploy-surface verification confirms module reachability (km init --plan enumerates sqs-inbound-dlq), IAM-vs-runtime coverage, that NO create-handler change was needed, and the recreate migration story"
    - "Absent inbound config / absent DLQ ⇒ doctor check dormant (SKIP) and runtime byte-identical to today"
  artifacts:
    - path: "internal/app/cmd/doctor_inbound_dlq.go"
      provides: "checkInboundDLQDepth"
      contains: "func checkInboundDLQDepth"
    - path: "docs/github-bridge.md"
      provides: "DLQ behavior + deploy sequence + migration note"
      contains: "sqs-inbound-dlq"
  key_links:
    - from: "buildChecks in doctor.go"
      to: "checkInboundDLQDepth"
      via: "checks = append(checks, func(ctx) CheckResult { return checkInboundDLQDepth(...) })"
      pattern: "checkInboundDLQDepth"
---

<objective>
Give operators visibility on poison messages and document the deploy/migration surface. Add
`checkInboundDLQDepth` (new file `doctor_inbound_dlq.go`), register it in `buildChecks`, update the
operator docs with the DLQ behavior + the exact deploy sequence, and run a deploy-surface
verification pass (reachability, IAM-vs-runtime, no-create-handler-change, recreate migration).

Purpose: GH-DLQ-DOCTOR (D6 — OK empty / WARN non-empty / SKIP when absent) and GH-DLQ-DEPLOY (D7 +
memory feedback_verify_deploy_surface_not_just_code — code-green ≠ deployable).
Output: a doctor DLQ-depth check + operator docs + a recorded deploy-surface verification.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-CONTEXT.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-RESEARCH.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-01-SUMMARY.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-03-SUMMARY.md
@internal/app/cmd/doctor.go
@internal/app/cmd/doctor_slack.go

<interfaces>
<!-- Plan 01 shipped (pkg/aws/sqs.go): -->
func GitHubInboundDLQName(resourcePrefix string) string
func SlackInboundDLQName(resourcePrefix string) string
func QueueDepth(ctx, SQSClient, queueURL string) (int64, error)   // EXISTING, sqs.go:172

<!-- Doctor patterns (RESEARCH Finding 6, lines 248-291):
     DoctorDeps struct doctor.go:329-525 — add (by extension only):
       InboundDLQSQS kmaws.SQSClient        // nil ⇒ check SKIPPED; can reuse SlackInboundSQS
       InboundResourcePrefix string         // can reuse SlackResourcePrefix
     buildChecks registration doctor.go:~3322 — append a closure after the Slack/GitHub checks.
     Mirror checkSlackInboundQueueExists in doctor_slack.go:219 (resolve URL via GetQueueUrl/
       ListQueues prefix, then QueueDepth). On QueueDoesNotExist ⇒ CheckSkipped.
     New check signature (RESEARCH lines 279-290):
       func checkInboundDLQDepth(ctx, sqsClient kmaws.SQSClient, resourcePrefix string) CheckResult
     CheckResult states: use the existing CheckSkipped / OK / WARN constants in doctor.go. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED test — checkInboundDLQDepth branches</name>
  <files>internal/app/cmd/doctor_inbound_dlq_test.go</files>
  <behavior>
    Net-new file (VALIDATION Wave-0: doctor_inbound_dlq_test.go). Using a mock SQSClient (reuse the
    in-package fakeSQS in create_slack_inbound_test.go — it supports getAttrsErr + listResult +
    GetQueueAttributes returning ApproximateNumberOfMessages), write the four branches
    (VALIDATION lines 522-525, RESEARCH lines 279-290):
    - TestCheckInboundDLQDepth_Nil: sqsClient == nil ⇒ CheckSkipped.
    - TestCheckInboundDLQDepth_NotExist: mock returns *sqstypes.QueueDoesNotExist (via getAttrsErr
      or an empty ListQueues so the URL can't resolve) ⇒ CheckSkipped.
    - TestCheckInboundDLQDepth_OK: both DLQs resolvable, QueueDepth==0 ⇒ OK status.
    - TestCheckInboundDLQDepth_Warn: a DLQ returns depth>0 ⇒ WARN status, message mentions the
      count + a remediation hint (e.g. aws sqs receive-message / purge-queue).
    Run and CONFIRM RED (checkInboundDLQDepth undefined).
  </behavior>
  <action>
    Create internal/app/cmd/doctor_inbound_dlq_test.go. If fakeSQS needs a per-queue depth knob to
    distinguish OK from WARN, add a minimal field to the existing fakeSQS (or a small local mock in
    this file). Commit as `test(99.1-04): RED checkInboundDLQDepth branches`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCheckInboundDLQDepth' -count=1 2>&1 | grep -qiE 'undefined|FAIL|build failed' && echo RED-OK</automated>
  </verify>
  <done>Test file exists; all four branches RED (checkInboundDLQDepth not yet defined).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checkInboundDLQDepth + register in buildChecks (GREEN)</name>
  <files>internal/app/cmd/doctor_inbound_dlq.go, internal/app/cmd/doctor.go</files>
  <behavior>
    - New file doctor_inbound_dlq.go: implement checkInboundDLQDepth(ctx, sqsClient, resourcePrefix)
      mirroring checkSlackInboundQueueExists (doctor_slack.go:219). For each of GitHubInboundDLQName
      + SlackInboundDLQName: resolve URL (GetQueueUrl or ListQueues prefix match), then QueueDepth.
      nil client ⇒ CheckSkipped; QueueDoesNotExist ⇒ CheckSkipped; all depths 0 ⇒ OK;
      any depth>0 ⇒ WARN with the count + remediation hint (RESEARCH line 288).
    - doctor.go: add InboundDLQSQS + InboundResourcePrefix to DoctorDeps (extension only,
      RESEARCH lines 260-267; production can reuse the Slack SQS client + resource prefix). Register
      the check in buildChecks (~line 3322) as an appended closure after the Slack/GitHub checks.
  </behavior>
  <action>
    Create doctor_inbound_dlq.go; edit doctor.go (DoctorDeps + buildChecks). Wire the production
    DoctorDeps construction site to populate InboundDLQSQS/InboundResourcePrefix from the existing
    Slack inbound SQS client + resource prefix (so the check is live, not dead). Run `make build`.
    Commit as `feat(99.1-04): km doctor inbound DLQ-depth check`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCheckInboundDLQDepth' -count=1 && make build</automated>
  </verify>
  <done>All four branches GREEN; check registered in buildChecks; `make build` succeeds (GH-DLQ-DOCTOR).</done>
</task>

<task type="auto">
  <name>Task 3: Docs + deploy-surface verification</name>
  <files>docs/github-bridge.md, docs/slack-notifications.md, OPERATOR-GUIDE.md</files>
  <behavior>
    Document the Phase 99.1 DLQ behavior + deploy/migration surface AND run the deploy-surface
    verification (memory feedback_verify_deploy_surface_not_just_code). Docs must state:
    - The shared per-install FIFO DLQs ({prefix}-github-inbound-dlq.fifo / {prefix}-slack-inbound-dlq.fifo),
      RedrivePolicy maxReceiveCount=3, and that a poison message now dead-letters instead of
      head-of-line-blocking its FIFO group.
    - km doctor now reports DLQ depth (OK empty / WARN non-empty / SKIP when absent).
    - Deploy sequence (D7): `make build` + `make build-lambdas` + `km init --dry-run=false`
      (NOT --sidecars — new TF module + IAM require a full terragrunt apply; no SandboxProfile
      schema change). Existing sandboxes do NOT gain redrive retroactively → `km destroy && km create`
      (no silent backfill).
    - Dormant by default: absent inbound config ⇒ no DLQ traffic, byte-identical to today.
    Deploy-surface VERIFICATION (record results in the SUMMARY):
    - Reachability: `km init --plan` (dry) enumerates the sqs-inbound-dlq module (or, if no live AWS
      creds, confirm via grep that regionalModules() lists it + the live unit + module exist — the
      reachability triple from Plan 03).
    - IAM-vs-runtime: confirm the operator/create-handler role can Create/Delete/GetAttributes the
      DLQ + SetQueueAttributes on the source queues (RESEARCH Finding 5 + Plan 03 IAM conclusion);
      confirm the sandbox EC2 role needs NO DLQ grant (it never reads the DLQ).
    - No create-handler change: confirm cmd/create-handler/main.go is NOT in files_modified across
      any 99.1 plan (RESEARCH Finding 3 — Lambda drains, does not create the queue).
    - Migration story: existing-sandbox recreate documented; no backfill assumed.
  </behavior>
  <action>
    Update the three docs. Keep examples generic (memory feedback_generic_doc_examples). Run the
    reachability + IAM + no-handler-change checks; record outcomes in the SUMMARY. Commit as
    `docs(99.1-04): inbound DLQ behavior + deploy sequence + deploy-surface verification`.
  </action>
  <verify>
    <automated>grep -q 'sqs-inbound-dlq' docs/github-bridge.md docs/slack-notifications.md && grep -qi 'km destroy && km create' docs/github-bridge.md docs/slack-notifications.md && grep -qi 'NOT.*--sidecars\|not.*--sidecars' docs/github-bridge.md docs/slack-notifications.md && ! grep -rln 'cmd/create-handler/main.go' .planning/phases/99.1-*/99.1-0*-PLAN.md | grep -q files_modified && echo OK</automated>
  </verify>
  <done>Docs cover DLQ behavior + deploy sequence (NOT --sidecars) + recreate migration; deploy-surface verification recorded in SUMMARY; create-handler confirmed unmodified (GH-DLQ-DEPLOY).</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'TestCheckInboundDLQDepth' -count=1` GREEN.
- Full module: `go test ./pkg/aws/... ./internal/app/cmd/... -count=1` GREEN; `make build` succeeds.
- Docs grep gate passes (sqs-inbound-dlq + recreate + NOT --sidecars).
- Deploy-surface verification recorded (reachability, IAM, no-create-handler-change, migration).
</verification>

<success_criteria>
Operators see poison-message depth via km doctor (GH-DLQ-DOCTOR); the deploy/migration surface is
documented and verified (GH-DLQ-DEPLOY). Dormancy invariant holds end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-04-SUMMARY.md`
</output>
