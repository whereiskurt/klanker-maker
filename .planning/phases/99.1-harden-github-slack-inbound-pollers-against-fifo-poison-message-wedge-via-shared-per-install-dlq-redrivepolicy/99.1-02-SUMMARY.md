---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 02
subsystem: cli
tags: [sqs, fifo, dead-letter-queue, redrive-policy, km-create, km-destroy, dependency-injection]

# Dependency graph
requires:
  - phase: 99.1-01
    provides: "CreateGitHubInboundQueue / CreateSlackInboundQueue 4-arg signature (dlqARN) + DLQArn / {GitHub,Slack}InboundDLQName helpers"
provides:
  - "DLQArn field on githubInboundDeps / slackInboundDeps, threaded into the 4-arg Create*InboundQueue helpers"
  - "create.go steps 11e/11f derive the shared-DLQ ARN from region + ApplicationAccountID + {GitHub,Slack}InboundDLQName(prefix)"
  - "Green module build — repaired the two callers broken by Plan 01's signature change"
  - "Regression-locked teardown guard: km destroy deletes only the per-sandbox source queue, never the shared DLQ"
affects:
  - "99.1-03 (km CLI / create-handler wiring of the shared DLQ create + ARN resolution)"
  - "99.1-04 (Terraform/operator surface for the shared DLQ)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Derive-don't-call: the DLQ ARN is computed deterministically (awspkg.DLQArn) at the call site, no SQS API round-trip"
    - "Dormancy gate at the call site: leave DLQArn empty when region OR account ID is unresolvable rather than fabricating a partial ARN"

key-files:
  created: []
  modified:
    - internal/app/cmd/create_github_inbound_test.go
    - internal/app/cmd/create_slack_inbound_test.go
    - internal/app/cmd/create_github_inbound.go
    - internal/app/cmd/create_slack_inbound.go
    - internal/app/cmd/create.go
    - internal/app/cmd/destroy_github_inbound.go
    - internal/app/cmd/destroy_slack_inbound.go

key-decisions:
  - "Account ID source = cfg.ApplicationAccountID (already in scope at both call sites); avoided the redundant STS GetCallerIdentity round-trip available at create.go:1555."
  - "Region source = the per-call-site sqsRegion / sqsRegionForGH already computed (resolvedProfile.Spec.Runtime.Region with awsCfg.Region fallback) — reused rather than re-derived."
  - "Dormancy is enforced at the call site (empty DLQArn when region/account empty) AND at the SQS layer (Plan 01 inboundQueueAttrs) — belt and braces."
  - "Teardown was already correct (drains delete only deps.QueueURL); Task 3 made it INTENTIONAL via comments + executable guard tests, adding no delete logic."

patterns-established:
  - "DLQArn deps field + 4th-arg passthrough = uniform DLQ wiring across the Slack and GitHub warm-create paths"

requirements-completed: [GH-DLQ-BOTHPATHS, GH-DLQ-SLACK, GH-DLQ-TEARDOWN]

# Metrics
duration: 3min
completed: 2026-06-08
---

# Phase 99.1 Plan 02: DLQ-Aware Warm Create Provisioning Summary

**Wired the Plan-01 SQS DLQ layer into the warm `km create` path — a `DLQArn` deps field threaded into the now-4-arg `Create{GitHub,Slack}InboundQueue` helpers, populated at create.go steps 11e/11f by deriving the shared-DLQ ARN from region + account ID + `{GitHub,Slack}InboundDLQName(prefix)`; repaired the two callers broken by Plan 01 (`make build` green again) and regression-locked that `km destroy` never deletes the shared DLQ.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-06-08T02:53:22Z
- **Completed:** 2026-06-08T02:56:22Z
- **Tasks:** 3 (TDD: RED → GREEN → guard-annotate)
- **Files modified:** 7

## Accomplishments
- Added a `DLQArn string` field to `githubInboundDeps` and `slackInboundDeps`, passed as the new 4th arg to `CreateGitHubInboundQueue` / `CreateSlackInboundQueue` (Plan 01 signature). Empty ⇒ no RedrivePolicy (dormancy preserved).
- Populated `DLQArn` at `create.go` steps 11e (Slack) and 11f (GitHub) by deriving the shared-DLQ ARN via `awspkg.DLQArn(region, cfg.ApplicationAccountID, awspkg.{Slack,GitHub}InboundDLQName(prefix))`. When region OR account ID is empty the ARN is left empty — provisioning stays dormant, no partial ARN fabricated (RESEARCH Finding 4 / A3).
- Restored a green `make build` (the module was deliberately left non-compiling by Plan 01's signature change).
- Annotated `drainGitHubInbound` / `drainSlackInbound` that the shared per-install DLQ is install-scoped and never deleted by `km destroy` (CONTEXT D5; `km uninit` owns its lifecycle), and locked it with executable guard tests.
- create-handler Lambda source verified untouched (RESEARCH Finding 3 — it drains into a pre-existing queue and does NOT create the source queue).

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave-0 RED tests — DLQ-ARN threading + teardown guard** - `fc9bc8a5` (test)
2. **Task 2: Thread DLQ ARN through provisioning + repair callers (GREEN) + rebuild km** - `8acd7366` (feat)
3. **Task 3: Teardown guard — destroy leaves the shared DLQ** - `0a138fe1` (docs)

_TDD: RED landed first and was confirmed fail-to-compile (DLQArn field absent / 3-arg mismatch); GREEN added the field + passthrough + call-site derivation and `make build` succeeded._

## Files Created/Modified
- `internal/app/cmd/create_github_inbound_test.go` - Added `TestCreate_GitHubInboundQueueWithDLQ` (RedrivePolicy maxReceiveCount=3 + exact deadLetterTargetArn), `TestCreate_GitHubInboundQueueNoDLQ` (dormancy), `TestDrainGitHubInbound_NoSharedDLQDelete` (source-only delete).
- `internal/app/cmd/create_slack_inbound_test.go` - Mirror trio for the Slack path; added `encoding/json` import.
- `internal/app/cmd/create_github_inbound.go` - Added `DLQArn` to `githubInboundDeps`; passed `deps.DLQArn` to `CreateGitHubInboundQueue`.
- `internal/app/cmd/create_slack_inbound.go` - Added `DLQArn` to `slackInboundDeps`; passed `deps.DLQArn` to `CreateSlackInboundQueue`.
- `internal/app/cmd/create.go` - Steps 11e/11f derive `slackDLQArn` / `githubDLQArn` and set them on the deps structs (empty when region/account unresolvable).
- `internal/app/cmd/destroy_github_inbound.go` - Comment locking the source-only delete (GH-DLQ-TEARDOWN).
- `internal/app/cmd/destroy_slack_inbound.go` - Same comment on the Slack drain.

## Decisions Made
- Reused `cfg.ApplicationAccountID` + the already-computed per-call-site region; no extra STS or SQS round-trip.
- Call-site dormancy gate (empty ARN when region/account empty) layered on top of the Plan-01 SQS-layer dormancy gate.
- Teardown left functionally unchanged — Task 3 is comments + tests only (the correct behavior already existed).

## Deviations from Plan

None - plan executed exactly as written. (No DLQ references existed in either drain, so the Task-3 "remove stray DLQ reference" contingency was a no-op.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. The shared-DLQ create + operator surface land in Plans 03/04.

## Next Phase Readiness
- Warm `km create` now threads a derived DLQ ARN into both inbound source queues; teardown leaves the shared DLQ; module builds; dormancy invariant preserved end-to-end.
- Plan 03 owns actually CREATING the shared DLQ (`CreateSharedInboundDLQ`) at the right lifecycle point and any create-handler-path / `km init` wiring; Plan 04 owns the Terraform/operator surface.

## Self-Check: PASSED

- FOUND: internal/app/cmd/create_github_inbound_test.go
- FOUND: internal/app/cmd/create_slack_inbound_test.go
- FOUND: internal/app/cmd/create_github_inbound.go (DLQArn)
- FOUND: internal/app/cmd/create_slack_inbound.go (DLQArn)
- FOUND: internal/app/cmd/create.go (DLQArn call-site derivation)
- FOUND: internal/app/cmd/destroy_github_inbound.go (teardown comment)
- FOUND: internal/app/cmd/destroy_slack_inbound.go (teardown comment)
- FOUND commit: fc9bc8a5 (Task 1, test)
- FOUND commit: 8acd7366 (Task 2, feat)
- FOUND commit: 0a138fe1 (Task 3, docs)

---
*Phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy*
*Completed: 2026-06-08*
</content>
</invoke>
