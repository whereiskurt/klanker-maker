---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 02
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - internal/app/cmd/create_github_inbound_test.go
  - internal/app/cmd/create_slack_inbound_test.go
  - internal/app/cmd/create_github_inbound.go
  - internal/app/cmd/create_slack_inbound.go
  - internal/app/cmd/create.go
  - internal/app/cmd/destroy_github_inbound.go
  - internal/app/cmd/destroy_slack_inbound.go
autonomous: true
requirements: [GH-DLQ-BOTHPATHS, GH-DLQ-SLACK, GH-DLQ-TEARDOWN]
must_haves:
  truths:
    - "provisionGitHubInboundQueue / provisionSlackInboundQueue thread a derived DLQ ARN into the create helper so new per-sandbox source queues carry a RedrivePolicy"
    - "Only the warm km create path is touched — the create-handler Lambda is NOT modified (it drains into a pre-existing queue, RESEARCH Finding 3)"
    - "When no DLQ ARN is resolvable (absent shared DLQ), provisioning stays dormant: no RedrivePolicy, byte-identical to today"
    - "km destroy (drainGitHubInbound / drainSlackInbound) deletes ONLY the per-sandbox source queue and never the shared DLQ"
  artifacts:
    - path: "internal/app/cmd/create_github_inbound.go"
      provides: "DLQArn dep field threaded into CreateGitHubInboundQueue"
      contains: "DLQArn"
    - path: "internal/app/cmd/create_slack_inbound.go"
      provides: "DLQArn dep field threaded into CreateSlackInboundQueue"
      contains: "DLQArn"
  key_links:
    - from: "create.go runCreate (step 11f) / slack equivalent"
      to: "provision*InboundQueue deps.DLQArn"
      via: "DLQArn = awspkg.DLQArn(region, accountID, awspkg.{GitHub,Slack}InboundDLQName(prefix))"
      pattern: "DLQArn"
    - from: "drainGitHubInbound / drainSlackInbound"
      to: "per-sandbox source queue only"
      via: "DeleteGitHubInboundQueue / DeleteSlackInboundQueue on deps.QueueURL; NO DLQ delete"
      pattern: "Delete(GitHub|Slack)InboundQueue"
---

<objective>
Wire the Plan-01 SQS layer into the warm `km create` provisioning path: add a `DLQArn` field
to `githubInboundDeps` / `slackInboundDeps`, thread it into the (now 4-arg) create helpers, and
populate it at the call sites in `create.go` by deriving the ARN from region + account ID +
`{GitHub,Slack}InboundDLQName(prefix)`. Repair the two callers broken by Plan 01's signature
change and restore a green module build. Add a teardown regression test proving the shared DLQ
is never deleted by `km destroy`.

Purpose: GH-DLQ-BOTHPATHS (RESEARCH Finding 3 collapses "both paths" to the single warm path —
the create-handler Lambda does NOT create the queue, so NO Lambda code change), GH-DLQ-SLACK
(lockstep), GH-DLQ-TEARDOWN (destroy leaves the shared DLQ).
Output: DLQ-aware provisioning + a green `make build` + teardown guard test.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-CONTEXT.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-RESEARCH.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-01-SUMMARY.md
@internal/app/cmd/create_github_inbound.go
@internal/app/cmd/create_slack_inbound.go
@internal/app/cmd/destroy_github_inbound.go
@internal/app/cmd/destroy_slack_inbound.go
@internal/app/cmd/create_slack_inbound_test.go

<interfaces>
<!-- Plan 01 shipped (pkg/aws/sqs.go): -->
func GitHubInboundDLQName(resourcePrefix string) string
func SlackInboundDLQName(resourcePrefix string) string
func DLQArn(region, accountID, dlqName string) string
func CreateGitHubInboundQueue(ctx, SQSClient, queueName, dlqARN string) (string, error)  // <- 4 args now
func CreateSlackInboundQueue(ctx, SQSClient, queueName, dlqARN string) (string, error)   // <- 4 args now

<!-- Call sites to update (RESEARCH Finding 2):
     create_github_inbound.go:71 provisionGitHubInboundQueue, :83 CreateGitHubInboundQueue call,
       githubInboundDeps struct lines 34-51, call site create.go:1155-1174 (step 11f).
     create_slack_inbound.go:92 provisionSlackInboundQueue, :105 CreateSlackInboundQueue call,
       slackInboundDeps struct lines 39-68.
     destroy_github_inbound.go drainGitHubInbound (deletes source queue only),
     destroy_slack_inbound.go drainSlackInbound (deletes source queue only).
     Account ID source: cfg.ApplicationAccountID (create.go:475) OR the STS GetCallerIdentity
       already called at create.go:1554-1559 (Claude's discretion which is cleaner).
     Region source: resolvedProfile.Spec.Runtime.Region (create.go:511) / cfg.PrimaryRegion. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED tests — DLQ-ARN threading + teardown guard</name>
  <files>internal/app/cmd/create_github_inbound_test.go, internal/app/cmd/create_slack_inbound_test.go</files>
  <behavior>
    Extend the existing inbound tests (VALIDATION Wave-0: extend create_github_inbound_test.go +
    create_slack_inbound_test.go; reuse the existing `fakeSQS.createAttrs` capture — RESEARCH
    line 538). Add:

    - TestCreate_GitHubInboundQueueWithDLQ: set `DLQArn: "arn:aws:sqs:us-east-1:123456789012:km-github-inbound-dlq.fifo"`
      on githubInboundDeps, run provisionGitHubInboundQueue with an enabled inbound profile →
      assert fakeSQS.createAttrs["RedrivePolicy"] unmarshals to maxReceiveCount=3 + that exact
      deadLetterTargetArn.
    - TestCreate_GitHubInboundQueueNoDLQ: DLQArn == "" → assert NO RedrivePolicy key (dormancy).
    - TestCreate_SlackInboundQueueWithDLQ / NoDLQ: identical for the Slack path.
    - TestDrainGitHubInbound_NoSharedDLQDelete (VALIDATION line 526): run drainGitHubInbound with
      a per-sandbox QueueURL set → assert fakeSQS.deleteCalled == 1 and fakeSQS.deleteURL is the
      per-sandbox source queue URL (NOT a *-dlq.fifo URL). Mirror for the Slack drain.

    These reference VALIDATION lines 519-526 + RESEARCH Finding 2 (lines 99-143).
    Run and CONFIRM RED (the DLQArn field does not exist; the Create* call is still 3-arg).
  </behavior>
  <action>
    Edit the two test files. Reuse the in-file `fakeSQS` + `testState` helpers. Commit as
    `test(99.1-02): RED DLQ-ARN threading + teardown guard`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCreate_(GitHub|Slack)InboundQueue(With|No)DLQ|TestDrain(GitHub|Slack)Inbound_NoSharedDLQDelete' -count=1 2>&1 | grep -qiE 'undefined|unknown field|FAIL|build failed' && echo RED-OK</automated>
  </verify>
  <done>New tests exist and are RED (DLQArn field missing / signature mismatch).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread DLQ ARN through provisioning + repair callers (GREEN) + rebuild km</name>
  <files>internal/app/cmd/create_github_inbound.go, internal/app/cmd/create_slack_inbound.go, internal/app/cmd/create.go</files>
  <behavior>
    - Add `DLQArn string` to `githubInboundDeps` (after the SQS field, line ~42) and
      `slackInboundDeps` (line ~39-68).
    - In provisionGitHubInboundQueue (line 83) and provisionSlackInboundQueue (line 105), pass
      `deps.DLQArn` as the new 4th arg to CreateGitHubInboundQueue / CreateSlackInboundQueue.
      Empty string ⇒ no RedrivePolicy (dormancy preserved, RESEARCH Finding 1 lines 91-93).
    - At the create.go call sites, populate DLQArn by deriving the ARN:
        accountID := cfg.ApplicationAccountID (or reuse the STS callerID at create.go:1554-1559)
        region    := the create-flow region (resolvedProfile.Spec.Runtime.Region / cfg.PrimaryRegion)
        DLQArn = awspkg.DLQArn(region, accountID, awspkg.GitHubInboundDLQName(prefix))   // github step 11f
        DLQArn = awspkg.DLQArn(region, accountID, awspkg.SlackInboundDLQName(prefix))    // slack step
      If accountID OR region is empty, leave DLQArn == "" (dormant — do NOT fabricate a partial
      ARN). RESEARCH Finding 4 sub-option A3 (derive, no SSM read).
  </behavior>
  <action>
    Edit the three files. Reuse whatever account-ID source is already in scope at each call site
    (prefer cfg.ApplicationAccountID; fall back to the existing STS call). After the edits run
    `make build` (NOT bare `go build` — memory `feedback_rebuild_km`; includes ldflags). Commit as
    `feat(99.1-02): thread derived DLQ ARN into warm km create provisioning`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCreate_(GitHub|Slack)InboundQueue(With|No)DLQ' -count=1 && make build</automated>
  </verify>
  <done>WithDLQ tests GREEN (RedrivePolicy + maxReceiveCount=3), NoDLQ tests GREEN (dormant). `make build` succeeds — module compiles with the Plan-01 signature change. No create-handler Lambda change (RESEARCH Finding 3).</done>
</task>

<task type="auto">
  <name>Task 3: Teardown guard — destroy leaves the shared DLQ</name>
  <files>internal/app/cmd/destroy_github_inbound.go, internal/app/cmd/destroy_slack_inbound.go</files>
  <behavior>
    Per RESEARCH Finding 2 (lines 130-143): drainGitHubInbound / drainSlackInbound today already
    delete ONLY the per-sandbox source queue (DeleteGitHubInboundQueue / DeleteSlackInboundQueue
    on deps.QueueURL) and never touch a shared DLQ. The correct behavior already exists — this
    task makes it INTENTIONAL and regression-locked. Confirm (read) that neither drain references
    GitHubInboundDLQName / SlackInboundDLQName / any *-dlq queue; add a one-line code comment in
    each drain noting "shared DLQ is install-scoped and never deleted by km destroy (CONTEXT D5;
    km uninit owns its lifecycle)". The Task-1 teardown tests are the executable guard.
  </behavior>
  <action>
    Add the clarifying comment to each drain function. Do NOT add DLQ-delete logic. If a read
    reveals any stray DLQ reference, remove it. Commit as
    `docs(99.1-02): annotate destroy leaves shared DLQ (D5)`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestDrain(GitHub|Slack)Inbound_NoSharedDLQDelete' -count=1</automated>
  </verify>
  <done>Teardown tests GREEN: destroy deletes only the per-sandbox source queue; shared DLQ untouched (GH-DLQ-TEARDOWN).</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'TestCreate_(GitHub|Slack)Inbound|TestDrain' -count=1` GREEN.
- `make build` succeeds (module-wide build repaired after Plan 01 signature change).
- create-handler Lambda source unchanged (RESEARCH Finding 3 — verified, not modified).
- Dormancy: DLQArn == "" ⇒ no RedrivePolicy at provision time.
</verification>

<success_criteria>
The warm `km create` path threads a derived DLQ ARN into both inbound source queues
(GH-DLQ-BOTHPATHS, GH-DLQ-SLACK); teardown leaves the shared DLQ (GH-DLQ-TEARDOWN); module
builds; dormancy invariant preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-02-SUMMARY.md`
</output>
