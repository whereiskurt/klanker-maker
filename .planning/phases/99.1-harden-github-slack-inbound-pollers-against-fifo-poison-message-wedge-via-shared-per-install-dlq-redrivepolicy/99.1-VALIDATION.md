---
phase: 99.1
slug: harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-08
---

# Phase 99.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go testing (`testing` stdlib, table-driven, mock SQS via existing `fakeSQS`) |
| **Config file** | none — `go test ./...` |
| **Quick run command** | `go test ./pkg/aws/... ./internal/app/cmd/... -run 'DLQ\|Inbound\|RegionalModules' -count=1` |
| **Full suite command** | `go test ./pkg/aws/... ./internal/app/cmd/... -count=1` |
| **Estimated runtime** | ~20s (pure + mock SQS; no live AWS) |

---

## Sampling Rate

- **After every task commit:** quick command (DLQ/inbound/regionalModules scope)
- **After every plan wave:** full suite command
- **Before `/gsd:verify-work`:** full suite green
- **Max feedback latency:** ~20s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 99.1-01-* | 01 | 1 | GH-DLQ-SHARED, GH-DLQ-REDRIVE | unit (mock SQS) | `go test ./pkg/aws/... -run 'TestCreateSharedInboundDLQ\|TestCreate(GitHub\|Slack)InboundQueue_(With\|No)DLQ\|TestGetQueueARN' -count=1` | ❌ W0 | ⬜ pending |
| 99.1-02-* | 02 | 2 | GH-DLQ-BOTHPATHS, GH-DLQ-SLACK | unit (fakeSQS) | `go test ./internal/app/cmd/... -run 'TestCreate_(GitHub\|Slack)Inbound' -count=1` | partial (slack ✅) | ⬜ pending |
| 99.1-03-* | 03 | 2 | GH-DLQ-DEPLOY | unit | `go test ./internal/app/cmd/... -run 'TestRegionalModules_IncludesSQSInboundDLQ\|TestLambdaBuilds' -count=1` | ❌ W0 | ⬜ pending |
| 99.1-04-* | 04 | 3 | GH-DLQ-DOCTOR, GH-DLQ-TEARDOWN | unit (mock) | `go test ./internal/app/cmd/... -run 'TestCheckInboundDLQDepth\|TestDrain.*NoSharedDLQDelete' -count=1` | ❌ W0 | ⬜ pending |
| 99.1-04-* | 04 | 3 | GH-DLQ-DEPLOY | docs/deploy | `grep -q 'sqs-inbound-dlq' docs/* && grep -qi 'km destroy && km create' docs/*` | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. Final task IDs assigned by the planner.*

---

## Wave 0 Requirements

All test files below are net-new (or extend existing inbound tests):

- [ ] `pkg/aws/sqs_dlq_test.go` — `CreateSharedInboundDLQ` (FIFO attrs + idempotency), `GetQueueARN`/ARN-derivation, `GitHubInboundDLQName`/`SlackInboundDLQName`, RedrivePolicy injected in `CreateGitHubInboundQueue`/`CreateSlackInboundQueue` when `dlqARN != ""`, ABSENT when empty (dormant)
- [ ] extend `internal/app/cmd/create_github_inbound_test.go` + `create_slack_inbound_test.go` — `DLQArn` dep threading into the create attrs (assert `fakeSQS.createAttrs["RedrivePolicy"]`)
- [ ] `internal/app/cmd/doctor_inbound_dlq_test.go` — `checkInboundDLQDepth`: nil-client SKIP, not-exist SKIP, depth-0 OK, depth>0 WARN
- [ ] extend `internal/app/cmd/init_test.go` — `regionalModules()` includes `sqs-inbound-dlq`; `lambdaBuilds()` unchanged
- [ ] teardown test — `drain*Inbound`/destroy does NOT delete the shared DLQ

**Reuse:** existing `fakeSQS` in `create_slack_inbound_test.go` (captures `createAttrs`) covers RedrivePolicy assertions with no new fixture.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Poison message actually dead-letters after `maxReceiveCount` receives and un-wedges the FIFO group | GH-DLQ-REDRIVE | Requires live SQS FIFO redrive semantics + a real failing agent run | After `km init` (DLQ provisioned) + `km destroy && km create` a github-inbound sandbox: post a comment that fails the agent run repeatedly; confirm via `aws sqs get-queue-attributes` the message lands in `{prefix}-github-inbound-dlq.fifo` after ~3 receives and a subsequent valid comment in the same group is processed |
| Shared DLQ provisioned by `km init`; `km doctor` reports depth | GH-DLQ-SHARED, GH-DLQ-DOCTOR | Live SQS + terragrunt apply | `km init --dry-run=false`; confirm `{prefix}-{github,slack}-inbound-dlq.fifo` exist; `km doctor` shows DLQ depth check |
| `km uninit` destroys the shared DLQ without orphaning siblings | GH-DLQ-TEARDOWN | Live terragrunt destroy | On a teardown, confirm the `sqs-inbound-dlq` module destroys cleanly |

*All queue-construction, redrive-attachment, ARN, doctor-branch, and teardown-guard logic is unit-tested with mock SQS; only live SQS redrive behavior + terragrunt apply/destroy are manual.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (`-count=1` everywhere)
- [ ] Feedback latency < 20s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
