---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 04
subsystem: observability
tags: [km-doctor, sqs, fifo, dlq, poison-message, deploy-surface, docs]

# Dependency graph
requires:
  - phase: 99.1-01
    provides: GitHubInboundDLQName / SlackInboundDLQName helpers + QueueDepth
  - phase: 99.1-03
    provides: sqs-inbound-dlq module deploy-reachable (the DLQs this check probes)
provides:
  - "checkInboundDLQDepth — km doctor check: SKIP (nil/absent), OK (empty), WARN (poison messages present)"
  - "InboundDLQSQS + InboundResourcePrefix on DoctorDeps (production reuses the Slack inbound SQS client + prefix)"
  - "Operator docs for the Phase 99.1 FIFO poison-message wedge fix (github-bridge.md, slack-notifications.md, OPERATOR-GUIDE.md)"
  - "Recorded deploy-surface verification (reachability triple, IAM coverage, no create-handler change, recreate migration)"
affects: [km-doctor, github-inbound, slack-inbound]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Doctor check mirrors checkSlackInboundQueueExists: resolve queue URL via ListQueues prefix match (SQSClient has no GetQueueUrl), then QueueDepth"
    - "Dormant doctor check: nil client OR zero resolvable DLQs ⇒ CheckSkipped (never a spurious WARN when the feature is unconfigured)"
    - "Deploy-surface verification pass (memory feedback_verify_deploy_surface_not_just_code): reachability triple + IAM-vs-runtime + no-handler-change + migration story"

key-files:
  created:
    - internal/app/cmd/doctor_inbound_dlq.go
    - internal/app/cmd/doctor_inbound_dlq_test.go
  modified:
    - internal/app/cmd/doctor.go
    - internal/app/cmd/create_slack_inbound_test.go
    - docs/github-bridge.md
    - docs/slack-notifications.md
    - OPERATOR-GUIDE.md

key-decisions:
  - "checkInboundDLQDepth resolves each DLQ URL via a ListQueues prefix match (the SQSClient interface has no GetQueueUrl) and treats no-match / QueueDoesNotExist as dormant (SKIP), not error."
  - "Production wiring reuses deps.SlackInboundSQS + GetResourcePrefix() — the shared DLQs live in the same region/account as the Slack inbound queues, so no new client/prefix plumbing is needed."
  - "The check's closure demotes any ERROR to WARN in buildChecks (mirrors the Slack inbound checks) so a DLQ probe never fails km doctor."
  - "Added a minimal depthByName + listByPrefix knob to the in-package fakeSQS to distinguish the OK (all-zero) and WARN (non-empty) branches and resolve distinct github/slack DLQ URLs by prefix — no new mock type."

patterns-established:
  - "fakeSQS.depthByName: per-queue ApproximateNumberOfMessages keyed by trailing queue-name segment of the GetQueueAttributes QueueUrl"

requirements-completed: [GH-DLQ-DOCTOR, GH-DLQ-DEPLOY]

# Metrics
duration: 13min
completed: 2026-06-08
---

# Phase 99.1 Plan 04: km doctor Inbound DLQ Depth + Deploy-Surface Verification Summary

**`checkInboundDLQDepth` gives operators poison-message visibility (SKIP dormant / OK empty / WARN non-empty) over the shared per-install `{prefix}-{github,slack}-inbound-dlq.fifo` queues, wired live into `buildChecks` by reusing the Slack inbound SQS client; the FIFO-wedge fix's DLQ behavior + exact deploy sequence (NOT `--sidecars`) + recreate migration are documented across three operator docs and the deploy surface is verified (reachability triple, IAM-by-wildcard, no create-handler change).**

## Performance

- **Duration:** ~13 min
- **Started:** 2026-06-08T03:04:57Z
- **Completed:** 2026-06-08T03:18:13Z
- **Tasks:** 3 (Tasks 1–2 TDD RED→GREEN; Task 3 docs + verification)
- **Files modified:** 7 (2 created, 5 modified)

## Accomplishments

- **`checkInboundDLQDepth`** (new `doctor_inbound_dlq.go`): probes both shared DLQs by resolving each name to a URL via a `ListQueues` prefix match (the `SQSClient` interface exposes no `GetQueueUrl`), then reads `ApproximateNumberOfMessages` via the existing `QueueDepth`. nil client ⇒ SKIP; zero resolvable DLQs ⇒ SKIP (dormant); all depths 0 ⇒ OK; any depth > 0 ⇒ WARN with the count + an `aws sqs receive-message` / `purge-queue` / redrive remediation hint.
- **Registered live** in `buildChecks` after the Slack inbound checks, with the ERROR→WARN demotion so a DLQ probe never fails `km doctor`. Production `DoctorDeps` populates the new `InboundDLQSQS` / `InboundResourcePrefix` from the existing `SlackInboundSQS` client + `GetResourcePrefix()` — the check is wired, not dead.
- **Four TDD branches GREEN** (`TestCheckInboundDLQDepth_{Nil,NotExist,OK,Warn}`) via a minimal `depthByName` + `listByPrefix` extension to the in-package `fakeSQS`.
- **Docs:** Phase 99.1 sections added to `docs/github-bridge.md` and `docs/slack-notifications.md` (shared FIFO DLQ behavior, `maxReceiveCount=3`, `km doctor` depth table, deploy sequence NOT `--sidecars`, recreate migration, dormant invariant); `OPERATOR-GUIDE.md` Troubleshooting gains a FIFO poison-message-wedge diagnose/remediate entry.

## Deploy-Surface Verification (GH-DLQ-DEPLOY)

Per memory `feedback_verify_deploy_surface_not_just_code` — code-green ≠ deployable:

1. **Reachability triple (confirmed present):**
   - TF module: `infra/modules/sqs-inbound-dlq/v1.0.0/{main,variables,outputs}.tf`
   - Live unit: `infra/live/use1/sqs-inbound-dlq/terragrunt.hcl`
   - `regionalModules()` entry: `internal/app/cmd/init.go:310` (`name: "sqs-inbound-dlq"`)
   All three exist ⇒ `km init` applies the DLQs and `km uninit` reverse-destroys them. (No live AWS creds were used; verified via grep, per Plan-03's reachability-triple convention. `km init --plan` would enumerate `sqs-inbound-dlq` from this same registration.)

2. **IAM-vs-runtime coverage (confirmed by inspection):** `km-operator-policy` statements `sqs_slack_inbound` (main.tf:593) and `sqs_github_inbound` (:627) grant `CreateQueue / DeleteQueue / GetQueueAttributes / GetQueueUrl / ListQueues / SetQueueAttributes / TagQueue` on `{prefix}-{slack,github}-inbound-*.fifo` (:616 / :649). The `-inbound-*.fifo` wildcard matches `-inbound-dlq.fifo`, covering DLQ create/delete/get-attributes and the source-queue `SetQueueAttributes` (RedrivePolicy injection). The new doctor check uses only `ListQueues` + `GetQueueAttributes`, both granted. **No new (over-broad) IAM statement added** (RESEARCH Pitfall 6). The sandbox EC2 role needs **no** DLQ grant — the poller never reads the DLQ; SQS moves poison messages there automatically.

3. **No create-handler change (confirmed):** `cmd/create-handler/main.go` is **not** in `files_modified` of any 99.1 plan (01/02/03/04). Plan 02's frontmatter explicitly records "the create-handler Lambda is NOT modified — it drains into a pre-existing queue (RESEARCH Finding 3)." The only references to the file in 99.1 plan text are verification-instruction prose, not modifications.

4. **Migration story (documented):** existing sandboxes do **not** gain the `RedrivePolicy` retroactively (no silent backfill); operators recreate with `km destroy <id> --remote --yes && km create <profile>`. Recorded in all three docs.

## Task Commits

1. **Task 1: RED checkInboundDLQDepth branches** - `66a64224` (test) — net-new test file + `fakeSQS` `depthByName`/`listByPrefix` knobs; confirmed RED (undefined).
2. **Task 2: km doctor inbound DLQ-depth check** - `eaa837f6` (feat) — `checkInboundDLQDepth` + `DoctorDeps` extension + `buildChecks` registration + production wiring; all four branches GREEN, `make build` succeeds.
3. **Task 3: docs + deploy-surface verification** - `c2a20ea9` (docs) — three operator docs + recorded verification.

## Files Created/Modified

- `internal/app/cmd/doctor_inbound_dlq.go` — `checkInboundDLQDepth` + `resolveQueueURL` helper.
- `internal/app/cmd/doctor_inbound_dlq_test.go` — four-branch RED→GREEN tests.
- `internal/app/cmd/doctor.go` — `InboundDLQSQS` + `InboundResourcePrefix` on `DoctorDeps`; `buildChecks` registration (ERROR→WARN demotion); production deps wiring (reuse Slack SQS + prefix).
- `internal/app/cmd/create_slack_inbound_test.go` — `fakeSQS` gains `depthByName` (per-queue depth) + `listByPrefix` (prefix-filtered ListQueues).
- `docs/github-bridge.md` — § Phase 99.1 (DLQ behavior, doctor check, deploy sequence, dormant invariant).
- `docs/slack-notifications.md` — § Phase 99.1 (same content, slack-first framing).
- `OPERATOR-GUIDE.md` — Troubleshooting: FIFO poison-message wedge diagnose/remediate.

## Decisions Made

- Resolve DLQ URLs via `ListQueues` prefix match (no `GetQueueUrl` on the interface); no-match / `QueueDoesNotExist` ⇒ dormant SKIP, not error.
- Reuse `SlackInboundSQS` + `GetResourcePrefix()` for production wiring (same region/account) — no new client/prefix plumbing.
- Closure demotes ERROR→WARN so the DLQ probe never fails `km doctor`.
- Minimal `fakeSQS` knobs (`depthByName` / `listByPrefix`) instead of a new mock type.

## Deviations from Plan

### Auto-fixed Issues

None — plan executed as written for all three tasks.

## Deferred Issues

- **`TestUnlockCmd_RequiresStateBucket`** (`internal/app/cmd/unlock_test.go:62`) fails in this execution environment because live AWS credentials are present: the unlock path reaches a real DynamoDB call (returns "not locked") before the empty-`StateBucket` guard fires. **Pre-existing and unrelated to this plan** — confirmed identical failure on the clean Plan-03 tree via `git stash`; touches no SQS/doctor/DLQ code. Logged to `deferred-items.md`; not fixed (SCOPE BOUNDARY). The four `TestCheckInboundDLQDepth` branches and `make build` are GREEN.

## Issues Encountered

- The full `go test ./internal/app/cmd/...` suite takes ~7.5 min in this environment and surfaced only the pre-existing, environment-induced unlock failure above. The plan's targeted gate (`-run TestCheckInboundDLQDepth`) and `make build` both pass.

## User Setup Required

None for this plan. Operator deploy (per docs): `make build && make build-lambdas && km init --dry-run=false` (NOT `--sidecars`); recreate existing sandboxes to attach the RedrivePolicy.

## Next Phase Readiness

- GH-DLQ-DOCTOR and GH-DLQ-DEPLOY satisfied. The DLQ-depth check is live and the deploy/migration surface is documented + verified. The Phase 99.1 DLQ stack (SQS layer → callers → TF module → doctor + docs) is complete.

## Self-Check: PASSED

- FOUND: internal/app/cmd/doctor_inbound_dlq.go
- FOUND: internal/app/cmd/doctor_inbound_dlq_test.go
- FOUND: internal/app/cmd/doctor.go (modified)
- FOUND: docs/github-bridge.md (§ Phase 99.1)
- FOUND: docs/slack-notifications.md (§ Phase 99.1)
- FOUND: OPERATOR-GUIDE.md (FIFO wedge troubleshooting)
- FOUND commit: 66a64224 (Task 1, test)
- FOUND commit: eaa837f6 (Task 2, feat)
- FOUND commit: c2a20ea9 (Task 3, docs)

---
*Phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy*
*Completed: 2026-06-08*
