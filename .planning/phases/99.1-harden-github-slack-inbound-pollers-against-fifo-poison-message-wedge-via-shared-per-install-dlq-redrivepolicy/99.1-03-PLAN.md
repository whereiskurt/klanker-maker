---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - internal/app/cmd/init_test.go
  - infra/modules/sqs-inbound-dlq/v1.0.0/main.tf
  - infra/modules/sqs-inbound-dlq/v1.0.0/variables.tf
  - infra/modules/sqs-inbound-dlq/v1.0.0/outputs.tf
  - infra/live/use1/sqs-inbound-dlq/terragrunt.hcl
  - internal/app/cmd/init.go
  - infra/modules/km-operator-policy/v1.0.0/main.tf
autonomous: true
requirements: [GH-DLQ-SHARED, GH-DLQ-DEPLOY]
must_haves:
  truths:
    - "A new Terraform module sqs-inbound-dlq creates the two shared FIFO DLQs ({prefix}-github-inbound-dlq.fifo, {prefix}-slack-inbound-dlq.fifo)"
    - "The module is reachable by km init: it has a live terragrunt.hcl unit AND an entry in regionalModules() — module-on-disk alone is invisible (memory project_new_lambda_needs_live_unit_and_init_list)"
    - "km uninit destroys it automatically via reverse-order traversal of regionalModules()"
    - "The km-operator-policy IAM already covers Create/Delete/GetAttributes/SetQueueAttributes on the DLQ ARN pattern via the existing {prefix}-{github,slack}-inbound-*.fifo wildcard (RESEARCH Pitfall 6) — verified, no over-broad new grant added unless a gap is found"
  artifacts:
    - path: "infra/modules/sqs-inbound-dlq/v1.0.0/main.tf"
      provides: "two aws_sqs_queue FIFO DLQ resources; no required_providers (root.hcl owns it)"
      contains: "fifo_queue"
    - path: "infra/live/use1/sqs-inbound-dlq/terragrunt.hcl"
      provides: "live unit wiring the module into the use1 region"
      contains: "sqs-inbound-dlq"
    - path: "internal/app/cmd/init.go"
      provides: "regionalModules() entry for sqs-inbound-dlq"
      contains: "sqs-inbound-dlq"
  key_links:
    - from: "regionalModules() in init.go"
      to: "infra/live/use1/sqs-inbound-dlq/terragrunt.hcl"
      via: "{name: \"sqs-inbound-dlq\", dir: filepath.Join(regionDir, \"sqs-inbound-dlq\")}"
      pattern: "sqs-inbound-dlq"
---

<objective>
Provision the shared per-install FIFO DLQs as a Terraform module reachable by `km init`. Create
`infra/modules/sqs-inbound-dlq/v1.0.0` (two FIFO DLQ queues), the live unit
`infra/live/use1/sqs-inbound-dlq/terragrunt.hcl`, register it in `regionalModules()`, and verify
(extend, only if a real gap) the `km-operator-policy` IAM coverage for the DLQ ARN pattern.

Purpose: GH-DLQ-SHARED (the DLQs must EXIST, created once at `km init`, idempotent via TF state —
RESEARCH Finding 4 Option A) and GH-DLQ-DEPLOY (module reachability — the deploy-reachability
triple: module + live unit + regionalModules; memory project_new_lambda_needs_live_unit_and_init_list).
Output: a km-init-reachable shared-DLQ TF module + a green init_test asserting registration.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-CONTEXT.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-RESEARCH.md
@internal/app/cmd/init.go
@infra/modules/dynamodb-github-threads/v1.0.0/main.tf
@infra/live/use1/dynamodb-github-threads/terragrunt.hcl
@infra/modules/km-operator-policy/v1.0.0/main.tf

<interfaces>
<!-- Pattern to mirror EXACTLY (simple module, NO dependency blocks → no mock_outputs concern):
     infra/modules/dynamodb-github-threads/v1.0.0/{main.tf,variables.tf,outputs.tf}
     infra/live/use1/dynamodb-github-threads/terragrunt.hcl
     regionalModules() in init.go:205-326 — append a regionalModule struct after the DynamoDB
       block / lambda blocks and BEFORE the "ses" entry (RESEARCH line 174, 338).
     IAM: km-operator-policy main.tf sqs_slack_inbound (line ~593) + sqs_github_inbound (line ~627)
       already grant CreateQueue/DeleteQueue/GetQueueAttributes/SetQueueAttributes/TagQueue on
       arn:...:{prefix}-slack-inbound-*.fifo and {prefix}-github-inbound-*.fifo — the "*" already
       matches "-dlq.fifo" (RESEARCH Pitfall 6, lines 391-392). VERIFY, do not blindly widen. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 RED test — regionalModules includes sqs-inbound-dlq</name>
  <files>internal/app/cmd/init_test.go</files>
  <behavior>
    Extend init_test.go (VALIDATION Wave-0: extend init_test.go). Add:
    - TestRegionalModules_IncludesSQSInboundDLQ: call regionalModules("regiondir") and assert a
      module with name == "sqs-inbound-dlq" is present, ordered BEFORE the "ses" entry (so the
      DLQ exists alongside other regional infra; RESEARCH line 174/338).
    - TestLambdaBuilds (assert unchanged / present, VALIDATION line 528): assert lambdaBuilds()
      still returns the existing count with NO sqs-inbound-dlq Lambda (this phase adds NO Lambda —
      RESEARCH Finding 8 line 343). If a TestLambdaBuilds already exists, leave it; only add the
      regionalModules assertion.
    Run and CONFIRM RED (no sqs-inbound-dlq entry yet).
  </behavior>
  <action>
    Edit internal/app/cmd/init_test.go. Commit as `test(99.1-03): RED regionalModules includes sqs-inbound-dlq`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestRegionalModules_IncludesSQSInboundDLQ' -count=1 2>&1 | grep -qiE 'FAIL|not found|undefined' && echo RED-OK</automated>
  </verify>
  <done>Test exists and is RED (sqs-inbound-dlq not yet in regionalModules()).</done>
</task>

<task type="auto">
  <name>Task 2: Create the sqs-inbound-dlq Terraform module + live unit</name>
  <files>infra/modules/sqs-inbound-dlq/v1.0.0/main.tf, infra/modules/sqs-inbound-dlq/v1.0.0/variables.tf, infra/modules/sqs-inbound-dlq/v1.0.0/outputs.tf, infra/live/use1/sqs-inbound-dlq/terragrunt.hcl</files>
  <action>
    Mirror infra/modules/dynamodb-github-threads/v1.0.0 structure.
    main.tf: two `aws_sqs_queue` resources — github_inbound_dlq and slack_inbound_dlq — each with
      `fifo_queue = true`, `content_based_deduplication = false`, `name` from a variable
      (e.g. "{label}-github-inbound-dlq.fifo" / "{label}-slack-inbound-dlq.fifo"), tags merged with
      Component "km-github-inbound" / "km-slack-inbound". You MAY set message_retention_seconds =
      1209600 for parity. Do NOT declare `required_providers` / a provider block — root.hcl's
      generate "provider" stanza is the single source (memory project_terragrunt_providers_in_root).
    variables.tf: github_dlq_name, slack_dlq_name, tags (mirror dynamodb-github-threads/variables.tf).
    outputs.tf: github_dlq_arn, slack_dlq_arn, github_dlq_url, slack_dlq_url (.arn / .id of each).
    live/use1/sqs-inbound-dlq/terragrunt.hcl: copy dynamodb-github-threads/terragrunt.hcl verbatim,
      change the remote_state key to ".../sqs-inbound-dlq/terraform.tfstate", point terraform.source
      at the new module, and set inputs.github_dlq_name = "${site.label}-github-inbound-dlq.fifo" /
      slack_dlq_name = "${site.label}-slack-inbound-dlq.fifo". This unit has NO `dependency` blocks
      (matches dynamodb-github-threads) → no mock_outputs_allowed concern (memory
      project_terragrunt_show_needs_mocks). Confirm there is no dependency block before finishing.
  </action>
  <verify>
    <automated>test -f infra/modules/sqs-inbound-dlq/v1.0.0/main.tf && test -f infra/live/use1/sqs-inbound-dlq/terragrunt.hcl && grep -q 'fifo_queue' infra/modules/sqs-inbound-dlq/v1.0.0/main.tf && ! grep -q 'required_providers' infra/modules/sqs-inbound-dlq/v1.0.0/main.tf && ! grep -q 'dependency ' infra/live/use1/sqs-inbound-dlq/terragrunt.hcl && echo OK</automated>
  </verify>
  <done>Module (main/variables/outputs.tf) + live unit exist; FIFO queues; NO required_providers; NO dependency block in the live unit.</done>
</task>

<task type="auto">
  <name>Task 3: Register in regionalModules() + verify IAM coverage (GREEN) + rebuild km</name>
  <files>internal/app/cmd/init.go, infra/modules/km-operator-policy/v1.0.0/main.tf</files>
  <behavior>
    - Add `{name: "sqs-inbound-dlq", dir: filepath.Join(regionDir, "sqs-inbound-dlq")}` to
      regionalModules() (init.go), positioned after the DynamoDB/lambda blocks and BEFORE the "ses"
      entry (RESEARCH lines 174/338). `km uninit` reverses this list automatically — no uninit edit.
    - IAM verification: read km-operator-policy sqs_slack_inbound (line ~593) + sqs_github_inbound
      (line ~627). Per RESEARCH Pitfall 6 the existing `{prefix}-slack-inbound-*.fifo` and
      `{prefix}-github-inbound-*.fifo` wildcards ALREADY match the `-dlq.fifo` names, granting
      Create/Delete/GetAttributes/SetQueueAttributes on the DLQs. CONFIRM this by inspection. ONLY
      if a needed action is genuinely uncovered, add a tightly-scoped statement for
      `arn:aws:sqs:*:{account}:{prefix}-*-inbound-dlq.fifo`. Document the conclusion in the SUMMARY
      either way (covered-by-wildcard vs. new-statement-added). Do NOT add a redundant statement if
      coverage already exists.
  </behavior>
  <action>
    Edit init.go regionalModules(). Inspect (and only-if-needed edit) km-operator-policy main.tf.
    Run `make build` (memory feedback_rebuild_km — the new module is invisible to km init unless the
    rebuilt binary carries the regionalModules entry; memory project_make_build_precedes_km_init).
    Commit as `feat(99.1-03): register sqs-inbound-dlq in regionalModules + verify IAM`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestRegionalModules_IncludesSQSInboundDLQ|TestLambdaBuilds' -count=1 && make build</automated>
  </verify>
  <done>regionalModules() includes sqs-inbound-dlq before "ses"; test GREEN; lambdaBuilds() unchanged; IAM coverage verified (conclusion recorded in SUMMARY); `make build` succeeds.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'TestRegionalModules_IncludesSQSInboundDLQ|TestLambdaBuilds' -count=1` GREEN.
- Deploy-reachability triple satisfied: module on disk + live unit + regionalModules entry + `make build`.
- Live unit has no dependency block (no mock_outputs concern); module has no required_providers.
- IAM coverage for the DLQ ARN pattern verified (wildcard already matches, per RESEARCH Pitfall 6).
</verification>

<success_criteria>
The shared FIFO DLQs are provisioned by a km-init-reachable Terraform module (GH-DLQ-SHARED),
satisfying the deploy-reachability triple (GH-DLQ-DEPLOY). No new Lambda. IAM verified.
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-03-SUMMARY.md`
</output>
