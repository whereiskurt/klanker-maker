---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 03
subsystem: infra
tags: [sqs, fifo, dlq, terraform, terragrunt, km-init, iam]

# Dependency graph
requires:
  - phase: 99.1-01
    provides: DLQ ARN derivation + redrivePolicy shape consumed by the per-sandbox inbound queues
provides:
  - "sqs-inbound-dlq Terraform module (two shared FIFO DLQs: {prefix}-github-inbound-dlq.fifo, {prefix}-slack-inbound-dlq.fifo)"
  - "use1 live unit wiring the module into the region"
  - "regionalModules() registration making the DLQs reachable by km init (and reverse-destroyed by km uninit)"
  - "verified IAM coverage for the DLQ ARN pattern (existing wildcards already match -dlq.fifo)"
affects: [99.1-04, github-inbound, slack-inbound, km-init, km-uninit]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared-infra-once-per-install: a poison-message DLQ created at km init (idempotent via TF state), targeted by per-sandbox FIFO queues' redrivePolicy"
    - "Deploy-reachability triple: TF module + live terragrunt unit + regionalModules() entry (all three required or km init silently skips)"

key-files:
  created:
    - infra/modules/sqs-inbound-dlq/v1.0.0/main.tf
    - infra/modules/sqs-inbound-dlq/v1.0.0/variables.tf
    - infra/modules/sqs-inbound-dlq/v1.0.0/outputs.tf
    - infra/live/use1/sqs-inbound-dlq/terragrunt.hcl
  modified:
    - internal/app/cmd/init.go
    - internal/app/cmd/init_test.go

key-decisions:
  - "FIFO DLQs (fifo_queue=true) — a FIFO source queue's redrivePolicy MUST target a FIFO DLQ (AWS constraint)"
  - "No new IAM grant added: existing {prefix}-{github,slack}-inbound-*.fifo wildcards already match -dlq.fifo (RESEARCH Pitfall 6)"
  - "14-day (1209600s) message retention so operators have the SQS-max window to inspect/redrive poison messages"
  - "Placed sqs-inbound-dlq after dynamodb-github-threads and before the lambda/ses blocks — plain regional infra, ses applies last"

patterns-established:
  - "Shared per-install DLQ module mirrors the dynamodb-github-threads module/live-unit skeleton (no dependency block → no mock_outputs concern)"

requirements-completed: [GH-DLQ-SHARED, GH-DLQ-DEPLOY]

# Metrics
duration: 2min
completed: 2026-06-08
---

# Phase 99.1 Plan 03: Shared Inbound FIFO DLQ Module Summary

**km-init-reachable `sqs-inbound-dlq` Terraform module provisioning two shared per-install FIFO DLQs ({prefix}-github-inbound-dlq.fifo / {prefix}-slack-inbound-dlq.fifo), registered in regionalModules() with IAM coverage verified against the existing inbound-queue wildcards.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-08T02:59:05Z
- **Completed:** 2026-06-08T03:01:31Z
- **Tasks:** 3
- **Files modified:** 6 (4 created, 2 modified)

## Accomplishments
- New `sqs-inbound-dlq/v1.0.0` Terraform module: two FIFO DLQ queues (github + slack inbound), 14-day retention, content-based-dedup off, tagged per component.
- `infra/live/use1/sqs-inbound-dlq/terragrunt.hcl` live unit naming the DLQs `{label}-{github,slack}-inbound-dlq.fifo`, no `dependency` block, no `required_providers` (root.hcl owns the provider).
- Registered in `regionalModules()` so `km init` applies the DLQs automatically and `km uninit` destroys them via the reverse-order traversal — closing the deploy-reachability triple (module + live unit + regionalModules entry).
- IAM coverage verified by inspection: the existing `km-operator-policy` `sqs_slack_inbound` / `sqs_github_inbound` statements already grant Create/Delete/GetQueueAttributes/SetQueueAttributes/TagQueue on `{prefix}-{slack,github}-inbound-*.fifo`, and the `-*.fifo` wildcard matches `-dlq.fifo`. No new (over-broad) statement added.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave-0 RED test** - `7e31dee8` (test) — `TestRegionalModules_IncludesSQSInboundDLQ` (RED) + `TestLambdaBuilds` (no new Lambda)
2. **Task 2: Module + live unit** - `ecea5747` (feat) — two FIFO DLQ queues + use1 live unit
3. **Task 3: Register + verify IAM + rebuild** - `335a119d` (feat) — regionalModules() entry, IAM verified, `make build` green

_Task 1 (RED) and Tasks 2–3 (module + GREEN) follow the TDD RED→GREEN flow without a refactor step._

## Files Created/Modified
- `infra/modules/sqs-inbound-dlq/v1.0.0/main.tf` - Two `aws_sqs_queue` FIFO DLQ resources (github_inbound_dlq, slack_inbound_dlq).
- `infra/modules/sqs-inbound-dlq/v1.0.0/variables.tf` - github_dlq_name / slack_dlq_name / tags inputs.
- `infra/modules/sqs-inbound-dlq/v1.0.0/outputs.tf` - {github,slack}_dlq_arn + {github,slack}_dlq_url outputs.
- `infra/live/use1/sqs-inbound-dlq/terragrunt.hcl` - Live unit; remote_state key `.../sqs-inbound-dlq/terraform.tfstate`; names DLQs from `site.label`.
- `internal/app/cmd/init.go` - regionalModules() entry for sqs-inbound-dlq (before lambda/ses blocks).
- `internal/app/cmd/init_test.go` - `TestRegionalModules_IncludesSQSInboundDLQ` + `TestLambdaBuilds`.

## Decisions Made
- **IAM: covered-by-wildcard, no new statement.** Confirmed `sqs_slack_inbound` (main.tf ~line 593) and `sqs_github_inbound` (~line 627) scope to `{prefix}-{slack,github}-inbound-*.fifo`; the `-*.fifo` wildcard matches `-dlq.fifo`. Adding a statement would be redundant and is deliberately avoided (RESEARCH Pitfall 6).
- **FIFO DLQs, dedup off.** Redrive-moved messages retain their source MessageGroupId/MessageDeduplicationId; the DLQ does not synthesize them, so `content_based_deduplication = false`.
- **14-day retention** (`message_retention_seconds = 1209600`, the SQS max) for the longest operator inspection/redrive window.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reworded a code comment that tripped the Task 2 verification grep**
- **Found during:** Task 2 (module creation)
- **Issue:** The Task 2 verify clause `! grep -q 'required_providers' main.tf` failed because an explanatory comment contained the literal string `required_providers` (the module correctly has NO `required_providers` block).
- **Fix:** Reworded the comment to "declares NO provider requirements block of its own" — preserving intent without the literal substring. The actual module still declares no provider block.
- **Files modified:** infra/modules/sqs-inbound-dlq/v1.0.0/main.tf
- **Verification:** Task 2 verify command returns `OK`; `terraform fmt -check` passes.
- **Committed in:** `ecea5747` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Cosmetic comment wording only; module structure unchanged, no scope creep.

## Issues Encountered
- `terraform validate` at the module level errors with "Missing required provider" — expected and identical to the mirrored `dynamodb-github-threads` module (the provider is injected by root.hcl at the live layer). `terraform fmt -check` passes, confirming syntactic validity.

## User Setup Required
None - no external service configuration required. Operator deploys via the existing `km init` flow (see Next Phase Readiness for the deploy note).

## Next Phase Readiness
- The shared DLQs are now deploy-reachable: a `make build` + `km init --dry-run=false` will create them (the rebuilt km carries the regionalModules entry — memory project_make_build_precedes_km_init).
- Plan 99.1-04 can rely on these DLQs existing at init time as the redrivePolicy targets for the per-sandbox inbound FIFO queues.
- No blockers.

## Self-Check: PASSED

All 6 files verified present; all 3 task commits (7e31dee8, ecea5747, 335a119d) verified in git history.

---
*Phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy*
*Completed: 2026-06-08*
