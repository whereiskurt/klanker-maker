---
phase: 99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/aws/sqs_dlq_test.go
  - pkg/aws/sqs.go
autonomous: true
requirements: [GH-DLQ-SHARED, GH-DLQ-REDRIVE, GH-DLQ-SLACK]
must_haves:
  truths:
    - "CreateGitHubInboundQueue / CreateSlackInboundQueue include a RedrivePolicy attribute pointing at the shared DLQ ARN with maxReceiveCount=3 when a non-empty dlqARN is supplied"
    - "When dlqARN is empty, the CreateQueue Attributes map is byte-identical to today (NO RedrivePolicy key) — dormancy invariant"
    - "CreateSharedInboundDLQ creates a FIFO DLQ and is idempotent (QueueNameExists returns the existing URL, not an error)"
    - "DLQ name helpers and ARN derivation are deterministic and require no SQS API call"
  artifacts:
    - path: "pkg/aws/sqs.go"
      provides: "GitHubInboundDLQName/SlackInboundDLQName, DLQArn derivation, GetQueueARN, CreateSharedInboundDLQ, dlqARN param on both Create*InboundQueue helpers"
      contains: "func CreateSharedInboundDLQ"
    - path: "pkg/aws/sqs_dlq_test.go"
      provides: "RED-first unit tests for all of the above"
      contains: "TestCreateSharedInboundDLQ"
  key_links:
    - from: "CreateGitHubInboundQueue / CreateSlackInboundQueue"
      to: "CreateQueue Attributes[\"RedrivePolicy\"]"
      via: "json-marshalled {deadLetterTargetArn, maxReceiveCount} string, only when dlqARN != \"\""
      pattern: "RedrivePolicy"
---

<objective>
Add the SQS-layer primitives for the shared-DLQ poison-message fix in `pkg/aws/sqs.go`:
DLQ name helpers, deterministic ARN derivation, a `GetQueueARN` helper, an idempotent
`CreateSharedInboundDLQ` (FIFO), and a new `dlqARN` parameter on `CreateGitHubInboundQueue`
and `CreateSlackInboundQueue` that injects a `RedrivePolicy` attribute ONLY when the ARN is
non-empty.

Purpose: This is the pure, mockable foundation every other plan builds on. It cures the
head-of-line-blocking FIFO wedge (CONTEXT D3) by letting SQS auto-evict poison messages to a
shared DLQ after `maxReceiveCount` receives — no poller-shell change required (RESEARCH Finding 7).
Output: Extended `pkg/aws/sqs.go` + new `pkg/aws/sqs_dlq_test.go`.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-CONTEXT.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-RESEARCH.md
@.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-VALIDATION.md
@pkg/aws/sqs.go
@internal/app/cmd/create_slack_inbound_test.go

<interfaces>
<!-- Existing SQSClient interface (pkg/aws/sqs.go:25-30) — DO NOT change for this plan.
     RESEARCH Finding 1 (Open Question 3): no interface change required; RedrivePolicy
     goes in the CreateQueue Attributes map (option b), and CreateSharedInboundDLQ resolves
     its URL via ListQueues prefix-match on QueueNameExists (same pattern as existing
     CreateSlackInboundQueue lines 70-84). -->
type SQSClient interface {
    CreateQueue(ctx, *sqs.CreateQueueInput, ...func(*sqs.Options)) (*sqs.CreateQueueOutput, error)
    DeleteQueue(ctx, *sqs.DeleteQueueInput, ...func(*sqs.Options)) (*sqs.DeleteQueueOutput, error)
    GetQueueAttributes(ctx, *sqs.GetQueueAttributesInput, ...func(*sqs.Options)) (*sqs.GetQueueAttributesOutput, error)
    ListQueues(ctx, *sqs.ListQueuesInput, ...func(*sqs.Options)) (*sqs.ListQueuesOutput, error)
}

<!-- RESEARCH Code Examples (lines 396-471): canonical signatures to implement. -->
func SlackInboundDLQName(resourcePrefix string) string   // "{prefix}-slack-inbound-dlq.fifo"
func GitHubInboundDLQName(resourcePrefix string) string  // "{prefix}-github-inbound-dlq.fifo"
func DLQArn(region, accountID, dlqName string) string    // "arn:aws:sqs:{region}:{accountID}:{dlqName}"
func GetQueueARN(ctx, SQSClient, queueURL) (string, error)
func CreateSharedInboundDLQ(ctx, SQSClient, dlqName string) (string, error)
// Signature change (5th positional arg, end): both gain dlqARN string.
func CreateGitHubInboundQueue(ctx, SQSClient, queueName, dlqARN string) (string, error)
func CreateSlackInboundQueue(ctx, SQSClient, queueName, dlqARN string) (string, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED test file for the SQS DLQ layer</name>
  <files>pkg/aws/sqs_dlq_test.go</files>
  <behavior>
    Net-new test file (VALIDATION Wave-0 gap: `pkg/aws/sqs_dlq_test.go`). Define a local
    in-memory fake SQSClient (mirror the `fakeSQS` shape in
    internal/app/cmd/create_slack_inbound_test.go:25-74 — capture `createName`,
    `createAttrs`, support a `QueueNameExists` injection and `listResult` for URL lookup).
    Write these RED tests (all must compile against the signatures in <interfaces>):

    - TestGitHubInboundDLQName / TestSlackInboundDLQName: assert exact strings
      "km-github-inbound-dlq.fifo" / "km-slack-inbound-dlq.fifo" for prefix "km".
    - TestDLQArn: assert "arn:aws:sqs:us-east-1:123456789012:km-github-inbound-dlq.fifo".
    - TestGetQueueARN: fake returns Attributes{"QueueArn": "<arn>"} → helper returns it;
      missing attribute → error.
    - TestCreateSharedInboundDLQ_Attributes: fake captures createAttrs; assert
      FifoQueue=="true" and ContentBasedDeduplication=="false" (FIFO DLQ; RESEARCH Pitfall 1).
    - TestCreateSharedInboundDLQ_Idempotent: fake CreateQueue returns *sqstypes.QueueNameExists,
      listResult contains ".../km-github-inbound-dlq.fifo" → helper returns that URL, nil error.
    - TestCreateGitHubInboundQueue_WithDLQ: call with dlqARN != "" → assert
      createAttrs["RedrivePolicy"] is present and unmarshals to
      {"deadLetterTargetArn": dlqARN, "maxReceiveCount": 3}.
    - TestCreateGitHubInboundQueue_NoDLQ: call with dlqARN == "" → assert
      createAttrs has NO "RedrivePolicy" key and the other 4 keys are byte-identical to today
      (FifoQueue, ContentBasedDeduplication, VisibilityTimeout=30, MessageRetentionPeriod=1209600).
    - TestCreateSlackInboundQueue_WithDLQ / _NoDLQ: identical assertions for the Slack helper.

    These reference RESEARCH Findings 1 (lines 58-95) and Code Examples (lines 396-471) and
    VALIDATION lines 515-518. Run the suite and CONFIRM IT FAILS TO COMPILE / fails (RED) —
    the helpers and new signatures do not exist yet.
  </behavior>
  <action>
    Create pkg/aws/sqs_dlq_test.go per the behavior block. Use stdlib `testing` +
    `encoding/json` for the RedrivePolicy unmarshal assertion. Do NOT add any production code
    in this task — the file must be RED. Commit as `test(99.1-01): RED sqs DLQ layer`.
  </action>
  <verify>
    <automated>go test ./pkg/aws/ -run 'DLQ|GetQueueARN|InboundQueue_(With|No)DLQ' -count=1 2>&1 | grep -qiE 'undefined|FAIL|build failed' && echo RED-OK</automated>
  </verify>
  <done>sqs_dlq_test.go exists; `go test ./pkg/aws/` fails to build or fails because the helpers/signatures are missing (RED confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DLQ helpers + RedrivePolicy injection (GREEN)</name>
  <files>pkg/aws/sqs.go</files>
  <behavior>
    Make every Task-1 test pass:
    - GitHubInboundDLQName / SlackInboundDLQName / DLQArn return the deterministic strings.
    - GetQueueARN does one GetQueueAttributes call for QueueArn (RESEARCH lines 432-448).
    - CreateSharedInboundDLQ creates a FIFO DLQ (FifoQueue=true, ContentBasedDeduplication=false)
      and is idempotent via the same QueueNameExists→ListQueues→suffix-match pattern already in
      CreateSlackInboundQueue (sqs.go:67-84). FIFO DLQ does NOT need VisibilityTimeout/retention
      knobs but you MAY mirror MessageRetentionPeriod=1209600 for parity (Claude's discretion).
    - Add `dlqARN string` as the final parameter to BOTH CreateGitHubInboundQueue and
      CreateSlackInboundQueue. Build the existing 4-attr map, then `if dlqARN != ""` add
      attrs["RedrivePolicy"] = json.Marshal({"deadLetterTargetArn": dlqARN, "maxReceiveCount": 3})
      (RESEARCH Code Examples lines 398-429, Pitfall 2: value MUST be a JSON string).
      When dlqARN == "", the map is byte-identical to today (dormancy invariant — must_have).
  </behavior>
  <action>
    Implement in pkg/aws/sqs.go. Add `encoding/json` import. Keep the existing idempotent
    QueueNameExists handling intact in both Create*InboundQueue functions. Do NOT touch the
    SQSClient interface (RESEARCH Open Question 3). NOTE: this changes the signatures of two
    exported functions — their existing callers in internal/app/cmd/create_{github,slack}_inbound.go
    will no longer compile; that is EXPECTED and is fixed in Plan 02 (this plan only ships the
    pkg/aws layer + its own tests). To keep `go test ./pkg/aws/` green without breaking the
    module build, pass `""` is NOT applicable here (callers are in another package). Confirm
    only the pkg/aws package tests are green in <verify>; the full-module build is repaired in
    Plan 02. Run `make build` is deferred to Plan 02. Commit as `feat(99.1-01): sqs DLQ helpers + RedrivePolicy injection`.
  </action>
  <verify>
    <automated>go test ./pkg/aws/ -run 'DLQ|GetQueueARN|InboundQueue_(With|No)DLQ|TestCreateSharedInboundDLQ' -count=1</automated>
  </verify>
  <done>All Task-1 tests GREEN. RedrivePolicy present only when dlqARN != ""; absent (byte-identical 4-attr map) when empty. CreateSharedInboundDLQ idempotent. `go test ./pkg/aws/ -count=1` passes.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/aws/ -count=1` is GREEN.
- Dormancy invariant proven by TestCreate*InboundQueue_NoDLQ (no RedrivePolicy key when dlqARN empty).
- No SQSClient interface change; no new SDK calls beyond GetQueueAttributes(QueueArn) + CreateQueue.
- Plan 02 will repair the two callers (signature change) and run `make build`.
</verification>

<success_criteria>
RedrivePolicy injection (GH-DLQ-REDRIVE), shared FIFO DLQ creation + idempotency
(GH-DLQ-SHARED), and Slack/GitHub parity (GH-DLQ-SLACK) are unit-proven against mock SQS.
Dormancy invariant holds.
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-harden-github-slack-inbound-pollers-against-fifo-poison-message-wedge-via-shared-per-install-dlq-redrivepolicy/99.1-01-SUMMARY.md`
</output>
