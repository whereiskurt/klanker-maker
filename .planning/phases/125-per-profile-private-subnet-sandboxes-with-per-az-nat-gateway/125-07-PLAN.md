---
phase: 125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway
plan: 07
type: execute
wave: 2
depends_on: ["125-03", "125-04"]
files_modified:
  - internal/app/cmd/doctor_network.go
  - internal/app/cmd/doctor_network_test.go
  - internal/app/cmd/doctor.go
autonomous: true
requirements: [REQ-125-DOCTOR]
must_haves:
  truths:
    - "km doctor WARNs, with the monthly cost and a safe-to-disable note, when NAT is enabled and no private sandbox is running."
    - "km doctor WARNs when a private sandbox is running but NAT is absent."
    - "Both checks SKIP silently on an install that has never touched either toggle, so a Phase 124 install sees no new noise."
    - "Neither check ever deletes or mutates anything — they are read-only reporting."
  artifacts:
    - internal/app/cmd/doctor_network.go
    - internal/app/cmd/doctor_network_test.go
  key_links:
    - "cfg.Network.NATGateway + SandboxMetadata.NetworkPlacement -> checkNATIdle / checkPrivateWithoutNAT -> CheckResult -> doctor.go closure registration"
---

<objective>
Add two `km doctor` checks: NAT enabled but nothing using it (you are paying about
$132/month for nothing), and private sandboxes running with no NAT (their egress is
broken).

Purpose: NAT is the expensive, reversible half of this phase. The operator needs a standing
reminder when it is idle, and a loud signal when the two toggles have drifted out of sync
in the dangerous direction.

Output: a `doctor_network.go` / `doctor_network_test.go` pair following the repo's
one-file-per-concern doctor convention, plus their registration in `doctor.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway/125-CONTEXT.md
@.planning/phases/125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway/125-PATTERNS.md
@.planning/phases/125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway/125-03-SUMMARY.md
@.planning/phases/125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway/125-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the two pure doctor check functions and their table tests</name>
  <files>internal/app/cmd/doctor_network.go, internal/app/cmd/doctor_network_test.go</files>
  <read_first>
    - internal/app/cmd/doctor_slack.go lines 605-649 (`checkSlackPeerBridges` — the pure-function `CheckResult` shape: `Name`, `Status`, `Message`, `Remediation`, and the `CheckSkipped` early return for an unconfigured feature)
    - internal/app/cmd/doctor_slack.go lines 539-551 (`listSandboxesWithInboundImpl` — how a doctor check pulls `[]SandboxMetadata` and filters on one field)
    - internal/app/cmd/doctor_slack_peer_bridges_test.go lines 1-40 (the table-driven test skeleton: `wantStatus`, `wantMsgSub`, `wantRemSub`)
    - pkg/aws/metadata.go (`SandboxMetadata.NetworkPlacement` and `Status` fields, from Plan 04)
  </read_first>
  <behavior>
    - `checkNATIdle` Test 1: NAT enabled, zero running private sandboxes -> `CheckWarn`, message names the approximate monthly cost and says it is safe to disable, remediation names the `network.nat_gateway` key and the `km init --dry-run=false` command.
    - `checkNATIdle` Test 2: NAT enabled, at least one running private sandbox -> `CheckOK`.
    - `checkNATIdle` Test 3: NAT not enabled -> `CheckSkipped` (silent on a Phase 124 install).
    - `checkPrivateWithoutNAT` Test 4: NAT not enabled, one or more running private sandboxes -> `CheckWarn` naming the sandbox ids and the fix.
    - `checkPrivateWithoutNAT` Test 5: NAT not enabled, no private sandboxes -> `CheckSkipped`.
    - `checkPrivateWithoutNAT` Test 6: NAT enabled -> `CheckOK` or `CheckSkipped` (the condition cannot occur), never a WARN.
    - Test 7 (both checks): sandbox rows that are private but NOT running, and rows with an empty `NetworkPlacement`, are excluded from every count.
  </behavior>
  <action>
Create `internal/app/cmd/doctor_network.go` with two PURE functions taking
`(natEnabled bool, metas []kmaws.SandboxMetadata)` and returning `CheckResult`:
`checkNATIdle` (Name: "NAT gateway idle") and `checkPrivateWithoutNAT` (Name: "Private
sandboxes without NAT"). No AWS clients in either signature — keep them table-testable,
exactly like `checkSlackPeerBridges`.

Share one small helper that filters `metas` down to rows where `Status` is running AND
`NetworkPlacement` equals the private literal. Rows with an empty placement are pre-125
sandboxes and are implicitly public — they must never count as private.

`checkNATIdle`'s WARN message must include the approximate monthly figure (about
$132/month for four AZs) and the phrase that it is safe to disable, because the entire
point of the toggle is that the operator can decide to stop paying whenever nothing is
using it. Its `Remediation` must name the `network.nat_gateway` km-config.yaml key and the
`km init --dry-run=false` command, matching how `checkSlackPeerBridges` phrases its own
remediation.

`checkPrivateWithoutNAT`'s WARN message must list the affected sandbox ids — a private
sandbox with no NAT has no egress at all, and the operator needs to know which boxes.

Create `internal/app/cmd/doctor_network_test.go` as a table-driven test over both
functions, following the `doctor_slack_peer_bridges_test.go` skeleton. Name the tests
`TestDoctorNetworkNATIdle` and `TestDoctorNetworkPrivateWithoutNAT`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestDoctorNetwork' -count=1 -timeout 600s -v; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `internal/app/cmd/doctor_network.go` and `internal/app/cmd/doctor_network_test.go` both exist.
    - `grep -c 'func checkNATIdle' internal/app/cmd/doctor_network.go` prints `1`; same for `func checkPrivateWithoutNAT`.
    - Neither function's signature contains an AWS client or a `context.Context` — verify by reading the two signatures.
    - `go test ./internal/app/cmd/ -run 'TestDoctorNetwork' -count=1 -timeout 600s -v` shows all seven behaviour cases passing and exits 0.
    - The `checkNATIdle` WARN message contains a dollar figure and the `Remediation` contains both `network.nat_gateway` and `km init`.
  </acceptance_criteria>
  <done>Two pure, table-tested doctor checks exist that WARN on idle NAT with its cost, WARN on private sandboxes with no NAT while naming them, and SKIP silently when neither toggle has been used.</done>
</task>

<task type="auto">
  <name>Task 2: Register both checks in doctor.go</name>
  <files>internal/app/cmd/doctor.go</files>
  <read_first>
    - internal/app/cmd/doctor.go lines 4468-4481 (the `checks = append(checks, func(ctx context.Context) CheckResult { ... })` closure-registration pattern, including the CheckError-to-CheckWarn downgrade)
    - internal/app/cmd/doctor.go lines 4880-4900 and 4990-5000 (existing `kmaws.ListAllSandboxMetadataDynamo` call sites in this file — reuse whichever client and table-name variables are already in scope rather than constructing new ones)
    - internal/app/cmd/doctor_network.go (the two functions to call, from Task 1)
  </read_first>
  <action>
Register both checks with the same `checks = append(checks, func(ctx context.Context) CheckResult { ... })`
closure shape used by the Slack peer-bridge check. Inside the closure, fetch sandbox
metadata via `kmaws.ListAllSandboxMetadataDynamo` against the sandboxes table, reusing a
DynamoDB client already constructed in this function if one is in scope. Pass
`cfg.GetNATGatewayEnabled()` as the `natEnabled` argument.

If the metadata fetch errors, return a `CheckSkipped` rather than a `CheckError` — a doctor
check that cannot read state should not report a false alarm about infrastructure it did
not observe. Apply the same CheckError-to-CheckWarn downgrade the neighbouring checks use.

Do NOT add any orphan-row scan or deletion. Both checks are strictly read-only reporting.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestDoctor' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'checkNATIdle' internal/app/cmd/doctor.go` prints `1`; same for `checkPrivateWithoutNAT`.
    - `go test ./internal/app/cmd/ -count=1 -timeout 600s` exits 0.
    - Neither registration closure calls a Delete, Put, or Update DynamoDB operation — verify by reading the two closures.
  </acceptance_criteria>
  <done>Both checks run as part of `km doctor`, degrade to SKIP when state is unreadable, and mutate nothing.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**New files**
- `internal/app/cmd/doctor_network.go`
- `internal/app/cmd/doctor_network_test.go`

**New Go functions**
- `checkNATIdle(natEnabled bool, metas []aws.SandboxMetadata) CheckResult` — check name "NAT gateway idle"
- `checkPrivateWithoutNAT(natEnabled bool, metas []aws.SandboxMetadata) CheckResult` — check name "Private sandboxes without NAT"
- a shared running-and-private filter helper

**New CLI output**
- Two additional `km doctor` check lines, both SKIP on an install that has used neither toggle

**New Go test symbols**
- `TestDoctorNetworkNATIdle`, `TestDoctorNetworkPrivateWithoutNAT`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DynamoDB sandboxes table -> doctor reporting | Live sandbox state is read to produce operator-facing warnings. Read-only in both directions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-21 | Denial of Service | undetected toggle drift | medium | mitigate | Private sandboxes running against an install with no NAT have no egress at all. `checkPrivateWithoutNAT` surfaces exactly that condition and names the affected sandbox ids. |
| T-125-22 | Tampering | doctor mutating state | low | mitigate | Both checks are read-only by construction — the pure functions take a metadata slice and return a `CheckResult`, and the acceptance criteria require the registration closures to perform no Delete, Put, or Update. This deliberately avoids the orphan-row auto-deletion trap that a prior phase had to rule out for alias-scoped rows. |
| T-125-23 | Information Disclosure | sandbox ids in doctor output | low | accept | `km doctor` already prints sandbox ids throughout; these are operator-local identifiers, not secrets. |

ASVS level 1. No high-severity threats — nothing here blocks. No package-manager installs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `go test ./internal/app/cmd/ -count=1 -timeout 600s` green (exit status read directly, not through a pipe).
- On an install with neither toggle set, both new checks report SKIP and add no noise.
</verification>

<success_criteria>
`km doctor` tells the operator when they are paying for idle NAT (with the figure and the
fact that disabling is safe) and when a running private sandbox has no NAT to egress
through (naming the sandbox), while staying silent and read-only on a Phase 124 install.
</success_criteria>

<output>
Create `.planning/phases/125-per-profile-private-subnet-sandboxes-with-per-az-nat-gateway/125-07-SUMMARY.md` when done
</output>
