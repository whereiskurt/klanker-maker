---
phase: 86-km-create-prompt-queue
plan: 05
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - internal/app/cmd/agent.go
  - internal/app/cmd/agent_test.go
  - CLAUDE.md
  - OPERATOR-GUIDE.md
autonomous: true
requirements:
  - PQ-07

must_haves:
  truths:
    - "`km agent list <sandbox> --queue` lists queue entries with: index, status, ISO timestamp, ~80-char prompt preview"
    - "Default `km agent list <sandbox>` (no --queue) is byte-for-byte unchanged from pre-Phase-86 output"
    - "When `--queue` is set and the queue dir doesn't exist (sandbox without prompts), output reads 'no queue entries' without erroring"
    - "Output is sorted ascending by NNN index"
    - "TestAgentListQueue (Plan 86-01 stub) flips to GREEN"
    - "OPERATOR-GUIDE.md gains a `km create --prompt` section with recovery procedure"
    - "CLAUDE.md CLI bullet for `km create --prompt` and `km agent list --queue` added"
  artifacts:
    - path: "internal/app/cmd/agent.go"
      provides: "--queue BoolVar on newAgentListCmd; alternate code path that reads queue/*.meta.json"
      contains: "cmd.Flags().BoolVar(&queueView, \"queue\""
    - path: "internal/app/cmd/agent_test.go"
      provides: "TestAgentListQueue GREEN; assertions for empty + mixed-statuses scenarios"
    - path: "OPERATOR-GUIDE.md"
      provides: "Section: `km create --prompt` queueing usage + recovery"
    - path: "CLAUDE.md"
      provides: "CLI bullet for `--prompt` + `--wait` under km create and `--queue` under km agent list"
  key_links:
    - from: "newAgentListCmd RunE"
      to: "queue-rendering branch (when --queue is set)"
      via: "shell command read of /workspace/.km-agent/queue/*.meta.json via sendSSMAndWait"
      pattern: "if queueView \\{ return runAgentListQueue\\("
    - from: "runAgentListQueue"
      to: "table-style output to cmd.OutOrStdout()"
      via: "parse NNN|status|created|prompt-preview, render aligned columns"
      pattern: "fmt\\.Fprintf\\(w, \"%-5s %-9s"
---

<objective>
Add the `--queue` flag to `km agent list <sandbox>` for queue observability (PQ-07). When set, an alternate code path renders the contents of `/workspace/.km-agent/queue/` as a table: index, status, timestamp, prompt preview.

Purpose: Close out the last code-level PQ requirement (PQ-07). Operators investigating why a sandbox isn't progressing can now `km agent list <sb> --queue` to see entry status without `km shell`ing in.

Also: documentation pass — CLAUDE.md CLI bullet + OPERATOR-GUIDE.md section for the new `--prompt` flag and its recovery procedure.

Output: `km agent list sb-abc123 --queue` against a sandbox with queue entries renders a table; against a sandbox without entries renders "no queue entries". CLAUDE.md + OPERATOR-GUIDE.md updated.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-km-create-prompt-queue/86-CONTEXT.md
@.planning/phases/86-km-create-prompt-queue/BRIEF.md
@.planning/phases/86-km-create-prompt-queue/86-RESEARCH.md

# Source files to modify
@internal/app/cmd/agent.go
@internal/app/cmd/agent_test.go
@CLAUDE.md
@OPERATOR-GUIDE.md

<interfaces>
Existing patterns Plan 86-05 mirrors.

From internal/app/cmd/agent.go (existing, line 812+):
- `func newAgentListCmd(cfg *config.Config, fetcher SandboxFetcher, ssmClient SSMSendAPI, s3Client S3GetAPI) *cobra.Command`
- Currently lists `/workspace/.km-agent/runs/<runID>/` entries with their status + output.json size.
- Existing listCmd shell string at line 1067 iterates over `runs/*/` directories.

Plan 86-05's queue-listing shell command (read by alternate code path when --queue is set):

```bash
sudo -u sandbox bash -c '
for meta in $(ls -1 /workspace/.km-agent/queue/*.meta.json 2>/dev/null | sort); do
    [ -f "$meta" ] || continue
    num=$(basename "$meta" .meta.json)
    status=$(jq -r .status "$meta" 2>/dev/null || echo "unknown")
    created=$(jq -r .created_at "$meta" 2>/dev/null || echo "")
    prompt_preview=$(head -c 80 "${meta%.meta.json}.prompt" 2>/dev/null | tr -d "\n\r")
    echo "${num}|${status}|${created}|${prompt_preview}"
done'
```

Output format:
```
001|done|2026-05-19T14:30:00Z|clone repo X and pull main
002|running|2026-05-19T14:30:01Z|open PR with linter fix
003|pending|2026-05-19T14:30:02Z|publish release once CI green
```

Go-side rendering target:
```
INDEX  STATUS    CREATED                PROMPT
001    done      2026-05-19T14:30:00Z   clone repo X and pull main
002    running   2026-05-19T14:30:01Z   open PR with linter fix
003    pending   2026-05-19T14:30:02Z   publish release once CI green
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --queue flag + runAgentListQueue alternate path in agent.go; flip TestAgentListQueue GREEN</name>
  <files>internal/app/cmd/agent.go, internal/app/cmd/agent_test.go</files>
  <behavior>
    1. In `newAgentListCmd` (~line 813), add the flag registration before `return cmd`:
       ```go
       var queueView bool
       cmd.Flags().BoolVar(&queueView, "queue", false,
           "List on-box prompt queue entries from /workspace/.km-agent/queue/ instead of agent runs")
       ```

    2. Modify the RunE to branch on `queueView`:
       ```go
       RunE: func(cmd *cobra.Command, args []string) error {
           ctx := cmd.Context()
           if ctx == nil { ctx = context.Background() }
           sandboxID, err := ResolveSandboxID(ctx, cfg, args[0])
           if err != nil { return err }
           if queueView {
               return runAgentListQueue(ctx, cmd, cfg, fetcher, ssmClient, sandboxID)
           }
           return runAgentList(ctx, cmd, cfg, fetcher, ssmClient, s3Client, sandboxID)
       },
       ```

    3. Add `runAgentListQueue` helper (group near `runAgentList`):
       - Initialize real clients if not injected (mirror existing pattern in runAgentList).
       - Fetch sandbox via fetcher; require Status != "stopped".
       - Extract instance ID via `extractResourceID(rec.Resources, ":instance/")`.
       - Build the shell command shown in <interfaces>, dispatch via `sendSSMAndWait`.
       - Parse output as pipe-delimited records; render with fixed-width columns to `cmd.OutOrStdout()`.
       - If output is empty after TrimSpace, print "no queue entries" and return nil.
       - Truncate any prompt preview > 80 chars defensively.

    4. Flip Wave 0 stub `TestAgentListQueue` to real assertions:
       - **Empty-dir scenario:** fake SSM returns empty stdout. Run with `--queue` set. Assert: output contains `"no queue entries"`, no error.
       - **Mixed-status scenario:** fake SSM returns the 5-line example payload. Assert: output has header + 5 entries, all in numeric order, each status string present, preview text truncated.
       - **Flag-presence:** `cmd.Flags().Lookup("queue").Value.Type() == "bool"`.

    5. Add `TestAgentListNoQueueFlag_UnchangedPath` as a regression guard: run the list cmd WITHOUT `--queue` against the existing runs-listing fake SSM payload. Assert the existing list output renders unchanged (no `INDEX STATUS CREATED` header — that's only the queue view).
  </behavior>
  <action>
    1. Read `internal/app/cmd/agent.go` lines 812-840 (newAgentListCmd) + lines 1067-1093 (existing runAgentList listCmd shell) to confirm exact patterns + fake SSM struct.
    2. Add `var queueView bool` + `cmd.Flags().BoolVar(...)` registration.
    3. Modify RunE to branch on queueView.
    4. Add `runAgentListQueue` helper (~50 lines).
    5. Flip `TestAgentListQueue` to real assertions in `agent_test.go`. Reuse existing fake SSM.
    6. Add `TestAgentListNoQueueFlag_UnchangedPath` regression test.
    7. Run `make build && go test ./internal/app/cmd/ -run "TestAgentList" -v -count=1`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue &amp;&amp; make build 2>&amp;1 | tail -3 &amp;&amp; go test ./internal/app/cmd/ -run "TestAgentList" -v -count=1 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    `make build` succeeds. `TestAgentListQueue` PASSES with empty + mixed-status scenarios. `TestAgentListNoQueueFlag_UnchangedPath` PASSES. Existing `TestAgentList*` tests still PASS (no regressions).
    `./km agent list --help` includes `--queue` description.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update CLAUDE.md CLI bullets + OPERATOR-GUIDE.md section</name>
  <files>CLAUDE.md, OPERATOR-GUIDE.md</files>
  <behavior>
    **CLAUDE.md update:** In the `## CLI` section (~line 28), modify two bullets:

    1. `km create` — append `--prompt <text-or-@file>` (repeatable) + `--wait` to its flag list.
    2. `km agent list` — append `--queue` to its description.

    Suggested final lines:
    ```
    - `km create <profile.yaml>` — provision a sandbox (`--no-bedrock`, `--docker`, `--alias`, `--on-demand`, `--prompt <text-or-@file>` repeatable, `--wait`)
    - `km agent list <sandbox-id>` — list all agent runs with status and output size (`--queue` to list on-box prompt queue entries instead)
    ```

    **OPERATOR-GUIDE.md update:** Find the natural section near `km create` usage (use `grep -n "## \|### " OPERATOR-GUIDE.md` to locate). Add a new subsection titled `### km create --prompt — provision + queue prompts` covering:

    1. **Basic usage** — code block showing single prompt, multi-prompt chain, `--wait`, `--no-bedrock` direct-API mode.
    2. **Prompt syntax** — `@file` reads UTF-8 verbatim, `@@x` becomes literal `@x`, missing file fails before any AWS call.
    3. **Observability** — `km agent list <sb> --queue`, `km agent list <sb>` (runs), `cat /workspace/.km-agent/km-queue.log`, `journalctl -u km-queue -f`.
    4. **Failure model** — first failure marks remaining `skipped`; auth wait is indefinite; reboot reconciles `running` -> `pending`.
    5. **Recovery procedure** — manual queue clear (`rm /workspace/.km-agent/queue/*`), manual retry (edit meta.json status back to `pending`, then `systemctl start km-queue`).
    6. **Constraints** — EC2-only; `--prompt + --docker` fails fast; no per-prompt timeout/retry/conditional in v1; Bedrock probe cost note (~$0.05/day for stuck queue).
    7. Bottom reference line linking to the spec at `docs/superpowers/specs/2026-05-19-km-create-prompt-queue-design.md` and BRIEF at `.planning/phases/86-km-create-prompt-queue/BRIEF.md`.

    Length: ~50-90 lines of markdown.
  </behavior>
  <action>
    1. Run `grep -n "km create\|## CLI\|km agent list" CLAUDE.md | head -10` to find exact insertion points.
    2. Edit CLAUDE.md — modify both bullets.
    3. Run `grep -n "## \|### " OPERATOR-GUIDE.md | head -40` to find the natural section.
    4. Add the new `### km create --prompt` subsection per <behavior>.
    5. Verify by running `grep "km create --prompt\|km agent list.*--queue" CLAUDE.md OPERATOR-GUIDE.md`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue &amp;&amp; grep -c "\-\-prompt" CLAUDE.md OPERATOR-GUIDE.md &amp;&amp; grep "km create --prompt\|km agent list.*--queue" CLAUDE.md OPERATOR-GUIDE.md | head -5</automated>
  </verify>
  <done>
    `grep -c "\\-\\-prompt" CLAUDE.md OPERATOR-GUIDE.md` shows both files have >= 1 match (CLAUDE.md typically 1, OPERATOR-GUIDE.md >= 5 with all the usage examples). New OPERATOR-GUIDE.md section covers usage + recovery + constraints + reference link.
  </done>
</task>

</tasks>

<verification>
1. **Build + tests green:**
   `make build` exits 0. `go test ./internal/app/cmd/ -count=1` — all tests PASS (zero regressions).
   PQ-07 test (`TestAgentListQueue`) now GREEN.

2. **Documentation grep checks:**
   `grep -c "\\-\\-prompt" CLAUDE.md` returns >= 1.
   `grep -c "\\-\\-prompt" OPERATOR-GUIDE.md` returns >= 5 (usage examples).
   `grep "km agent list.*--queue" CLAUDE.md OPERATOR-GUIDE.md` returns >= 1 match per file.

3. **Existing behavior unchanged:**
   `./km agent list --help` shows the new `--queue` flag.
   `./km agent list <sb>` without `--queue` continues to render the runs table as before.
</verification>

<success_criteria>
- `internal/app/cmd/agent.go` has the `--queue` flag registered on `newAgentListCmd` and a `runAgentListQueue` helper.
- `internal/app/cmd/agent_test.go` has `TestAgentListQueue` GREEN with empty + mixed-status assertions plus a `TestAgentListNoQueueFlag_UnchangedPath` regression test.
- CLAUDE.md `km create` and `km agent list` bullets list the new flags.
- OPERATOR-GUIDE.md has a `### km create --prompt — provision + queue prompts` section with usage + recovery + constraints + reference link.
- `make build` + full `go test ./internal/app/cmd/` GREEN.
- All code-level PQ-NN IDs (PQ-01..PQ-08) now GREEN. Only operator-UAT IDs (PQ-09..PQ-13, R1) remain for Plan 86-06.
</success_criteria>

<output>
After completion, create `.planning/phases/86-km-create-prompt-queue/86-05-SUMMARY.md` covering:
- Files touched + line counts
- All PQ-01..PQ-08 ID statuses (should all be GREEN after this plan)
- CLAUDE.md / OPERATOR-GUIDE.md diff summary
- Setup for Plan 86-06 — UAT scenarios (PQ-09..PQ-13 + R1) require operator-in-loop testing against real AWS
</output>
