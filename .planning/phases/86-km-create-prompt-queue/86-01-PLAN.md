---
phase: 86-km-create-prompt-queue
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/app/cmd/create_prompt_test.go
  - internal/app/cmd/agent_test.go
  - pkg/profile/configfiles/km-queue-runner_test.sh
autonomous: true
requirements: []
# NOTE: Plan 86-01 delivers RED-state STUBS only (Nyquist gate / Wave 0 scaffolding).
# Per phase-86 convention, `requirements` lists IDs delivered end-to-end (impl + GREEN
# tests). Stub-only IDs are claimed by the implementing plan, not this scaffolding plan.
# Requirement → implementing-plan mapping for traceability:
#   PQ-01..PQ-04, R1 → 86-02
#   PQ-08            → 86-03
#   PQ-05, PQ-06     → 86-04
#   PQ-07            → 86-05
#   PQ-09..PQ-13, R1 → 86-06 (UAT)

must_haves:
  truths:
    - "Every PQ-NN behavior has at least one RED-state test stub before implementation begins"
    - "Tests fail with clear 'not implemented' messages (not compile errors)"
    - "Bash test harness for runner state machine exists and is invocable via plain bash"
    - "Nyquist gate flips to nyquist_compliant: true after this plan lands"
  artifacts:
    - path: "internal/app/cmd/create_prompt_test.go"
      provides: "RED-state Go test stubs for PQ-01..PQ-06 and PQ-08"
      contains: "TestCreatePromptFlag, TestResolvePrompts, TestCreatePromptDockerReject, TestPushQueueFiles, TestCreatePromptWait, TestCreatePromptWaitFail, TestQueueRunnerStateMachine"
    - path: "internal/app/cmd/agent_test.go"
      provides: "TestAgentListQueue stub appended (PQ-07)"
      contains: "TestAgentListQueue"
    - path: "pkg/profile/configfiles/km-queue-runner_test.sh"
      provides: "Bash test harness skeleton for runner state machine (PQ-08 deep coverage)"
      contains: "test_reconcile_running_to_pending, test_lowest_pending_picked_first, test_failure_marks_remaining_skipped, test_auth_probe_bedrock_*, test_auth_probe_direct_api_*"
  key_links:
    - from: "internal/app/cmd/create_prompt_test.go"
      to: "internal/app/cmd/create.go (NewCreateCmd)"
      via: "Cobra command construction + flag introspection"
      pattern: "NewCreateCmd\\(.*\\).Flags\\(\\).Lookup\\(\"prompt\"\\)"
    - from: "internal/app/cmd/agent_test.go"
      to: "internal/app/cmd/agent.go (newAgentListCmd)"
      via: "Cobra subcommand introspection"
      pattern: "newAgentListCmd\\(.*\\).Flags\\(\\).Lookup\\(\"queue\"\\)"
---

<objective>
Create RED-state test stubs for every Phase 86 acceptance behavior (PQ-01..PQ-08 + R1) before any implementation lands. This is the Nyquist gate — Wave 0 must exist and fail meaningfully before Waves 1-4 can write code.

Purpose: Lock the test surface area so subsequent waves implement against fixed contracts, not moving targets. Each test stub names a concrete function/flag/behavior the implementation must satisfy.

Output: One new test file (`create_prompt_test.go`), one augmented test file (`agent_test.go`), and one bash harness skeleton. All stubs compile, run, and fail with explicit `t.Skip("not implemented — Wave N")` or table assertions against not-yet-existent helpers.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/86-km-create-prompt-queue/86-CONTEXT.md
@.planning/phases/86-km-create-prompt-queue/BRIEF.md
@.planning/phases/86-km-create-prompt-queue/86-RESEARCH.md
@.planning/phases/86-km-create-prompt-queue/86-VALIDATION.md

# Existing test patterns to mirror
@internal/app/cmd/agent_test.go
@internal/app/cmd/create.go
@CLAUDE.md

<interfaces>
<!-- Key contracts the test stubs reference. -->
<!-- Wave 1+ will implement the production code these stubs assert against. -->

From internal/app/cmd/agent.go (existing, lines 1146-1175):
```go
// AgentRunOptions bundles per-run options for BuildAgentShellCommands.
type AgentRunOptions struct {
    NoBedrock       bool
    ArtifactsBucket string
    // (other fields exist; see source)
}

func BuildAgentShellCommands(prompt string, opts AgentRunOptions) (cmds []string, runID string)
func sendSSMAndWait(ctx context.Context, ssmClient SSMSendAPI, instanceID, shellCmd string) (string, error)
```

From internal/app/cmd/create.go (existing, lines 114-212):
```go
func NewCreateCmd(cfg *config.Config) *cobra.Command
// Existing flags: --on-demand, --aws-profile, --remote, --local, --sandbox-id,
// --alias, --substrate, --docker, --no-bedrock, --ttl, --idle, --compute, --ai, --verbose.
// Phase 86 adds: --prompt (repeatable StringArrayVar), --wait (BoolVar).
```

Phase 86 contracts the tests assert against (Wave 1+ must satisfy):
```go
// In package cmd (create.go or create_prompt.go — Wave 1 picks the file):
func resolvePrompts(raw []string) (resolved []string, err error)
// @file -> read file UTF-8; @@x -> literal @x; missing file -> error wrapping the path.

func pushQueueFiles(ctx context.Context, ssmClient SSMSendAPI, instanceID string, prompts []string, noBedrock bool) error
// Pushes /workspace/.km-agent/queue/NNN.prompt + NNN.meta.json via one SSM call.

func waitForQueueDrain(ctx context.Context, ssmClient SSMSendAPI, instanceID string, expectedCount int) (exitCode int, err error)
// Polls *.meta.json; returns 0 on all-done, first-failed exit code on failure.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create create_prompt_test.go with RED-state stubs for PQ-01..PQ-06 + PQ-08</name>
  <files>internal/app/cmd/create_prompt_test.go</files>
  <behavior>
    Stubs are SHAPE-COMPLETE (compile + run + fail meaningfully). Each is a real `func TestX(t *testing.T)` that either calls a not-yet-existent helper (compile failure = RED) OR uses `t.Skip("Wave N implements this")` with a clear marker.

    The recommended approach: write tests that call the Wave 1+ helpers (`resolvePrompts`, `pushQueueFiles`, `waitForQueueDrain`) and let them fail to compile UNTIL Wave 1 lands. To keep the package compiling in the meantime, declare local stub functions at the BOTTOM of this test file guarded by a build tag or use `t.Skip` with a TODO comment naming the wave.

    Pick the lighter path: use `t.Skip("not implemented — Wave 1")` for behavior tests and `t.Run` subtests for table-driven cases. The skip messages must name the wave + the production helper so the executor in that wave knows exactly what to wire.

    Tests to include (one per PQ ID):

    1. `TestCreatePromptFlag` (PQ-01) — uses `NewCreateCmd(testCfg).Flags().Lookup("prompt")` to assert: flag exists, type is `stringArray` (Cobra's name for StringArrayVar — `flag.Value.Type() == "stringArray"`), Shorthand is empty, default is `nil`. Also assert `Lookup("wait")` exists as BoolVar.

    2. `TestResolvePrompts` (PQ-02) — table-driven over `resolvePrompts([]string)`:
       - `{"literal"}` -> `{"literal"}`, no error.
       - `{"@@literal"}` -> `{"@literal"}`, no error.
       - `{"@/tmp/<TMP>"}` with a tmp file containing `"file contents\n"` -> `{"file contents\n"}`, no error. Use `t.TempDir()` to create the file.
       - `{"@/does/not/exist"}` -> error must contain `"/does/not/exist"` (path surfaced in error).
       - `{"literal", "@@x", "@<TMP>"}` mixed -> all three resolved in order.
       Stub line: `prompts, err := resolvePrompts(input)` — when helper doesn't exist, compile fails. Use `t.Skip("Wave 1: resolvePrompts not yet implemented")` if compile must pass.

    3. `TestCreatePromptDockerReject` (PQ-03) — table-driven: invoke `NewCreateCmd` with args `["--docker", "--prompt", "x", "fake.yaml"]` and a Mock that prevents any AWS call. Assert: RunE returns an error whose message contains `"queue requires EC2"` AND no SSM call was made. Also test `--substrate=docker --prompt x` returns the same error.

    4. `TestPushQueueFiles` (PQ-04) — uses a fake `SSMSendAPI` (already mocked in `agent_test.go` — reuse the pattern). Calls `pushQueueFiles(ctx, fakeSSM, "i-abc", []string{"first", "second"}, true /* noBedrock */)`. Asserts:
       - Exactly ONE `SendCommand` call was made (atomic batch).
       - Command document is `AWS-RunShellScript`.
       - Command body contains `001.prompt` AND `002.prompt`.
       - Command body contains `001.meta.json` AND `002.meta.json`.
       - Prompts are base64-encoded in the command body (verify by decoding the captured base64 substring back to `"first"` / `"second"`).
       - Meta JSON includes `"no_bedrock":true` and `"status":"pending"`.
       - Command body ends with `chown -R sandbox:sandbox /workspace/.km-agent/queue` (per RESEARCH.md).

    5. `TestCreatePromptWait` (PQ-05) — fake SSM whose `GetCommandInvocation` returns a script outputting status strings. Sequence the mock to return `"pending|pending"` then `"running|pending"` then `"done|done"`. Call `waitForQueueDrain(ctx, fakeSSM, "i-abc", 2)`. Assert: returns `(0, nil)` after the third poll.

    6. `TestCreatePromptWaitFail` (PQ-06) — fake SSM returning `"failed|skipped"` on first poll. Call `waitForQueueDrain(ctx, fakeSSM, "i-abc", 2)`. Assert: returns non-zero exit code AND error mentions the failed prompt index/exit.

    7. `TestQueueRunnerStateMachine` (PQ-08) — Go-side table-driven test of a `reconcileEntry(meta)` pure function that takes a meta.json status and returns the next status. Cases:
       - `"running"` -> `"pending"` (reconcile)
       - `"pending"` -> `"pending"` (no change at probe time)
       - `"done"` -> `"done"` (idempotent)
       - `"failed"` -> `"failed"` (idempotent)
       - `"skipped"` -> `"skipped"` (idempotent)
       This is the GO-side mirror of what the BASH runner does on startup. The bash harness in Task 3 tests the actual bash invocation.

    File header MUST include the standard project header. Use `package cmd` (same as existing tests). Import `testing`, `os`, `path/filepath`, `strings`, `context`, `encoding/base64`, and reuse `SSMSendAPI` + the fake from `agent_test.go`. If `agent_test.go` does not export the fake, copy the minimal struct inline (it's a few lines).

    Add a comment block at the top:
    ```
    // Phase 86 RED-state stubs. Each test asserts a contract from
    // .planning/phases/86-km-create-prompt-queue/86-VALIDATION.md.
    // Implementation lands in Waves 1-4. Until then, tests call
    // helpers that may not yet exist; this file is gated by
    // `go test` returning FAIL until Waves 1-4 ship.
    ```
  </behavior>
  <action>
    1. Read `internal/app/cmd/agent_test.go` end-to-end to identify the fake SSM struct(s) already in scope. Reuse them.
    2. Read `internal/app/cmd/create.go` lines 114-212 to confirm exact flag registration patterns (so test assertions match Cobra's behavior).
    3. Write `internal/app/cmd/create_prompt_test.go` per the <behavior> spec above. Compile must succeed: forward-declare helper signatures inline at the bottom of the file under a build tag OR use `t.Skip` patterns so the file is valid Go even before Wave 1.
    4. Run `go test ./internal/app/cmd/ -run TestCreatePrompt -v -count=1` and confirm: tests RUN (don't error compile), and either skip with the wave marker or fail with a clear "not implemented" message. RED-state by design.
    5. Run `go vet ./internal/app/cmd/` — must pass.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue &amp;&amp; go vet ./internal/app/cmd/ &amp;&amp; go test ./internal/app/cmd/ -run TestCreatePrompt -v -count=1 2>&amp;1 | grep -E "(SKIP|FAIL|PASS|RUN)" | head -40</automated>
  </verify>
  <done>
    `go vet ./internal/app/cmd/` exits 0.
    `go test ./internal/app/cmd/ -run TestCreatePrompt` either reports SKIP for every PQ-XX test (acceptable RED state via skip markers) OR FAIL with implementation-missing messages (preferred — proves wave 1+ needs to land). NO PASS lines for behaviors that aren't implemented yet.
    The test file is 100-250 lines, uses `package cmd`, and references each of PQ-01..PQ-06 + PQ-08 in test name comments.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append TestAgentListQueue stub to agent_test.go (PQ-07)</name>
  <files>internal/app/cmd/agent_test.go</files>
  <behavior>
    A single new test function appended to the existing `agent_test.go`, asserting:
    1. Flag introspection — `newAgentListCmd(testCfg, nil, nil, nil).Flags().Lookup("queue")` exists, is a BoolVar, defaults to `false`.
    2. Empty-dir scenario — fake SSM returns empty stdout. Run the list command with `--queue` flag set. Assert: no error, output contains a "no queue entries" sentinel string OR an empty table header (depending on the Wave 4 rendering choice; test should accept either by checking absence of any `NNN|status|` lines).
    3. Mixed-status scenario — fake SSM returns:
       ```
       001|done|echo first prompt text
       002|running|echo second longer prompt that might be truncated
       003|pending|@plan.txt
       004|failed|exit 1
       005|skipped|never runs
       ```
       Assert: rendered output contains all five status labels, in order (`001` appears before `002`, etc.). Output truncates prompt preview to <= 80 chars per BRIEF.
  </behavior>
  <action>
    1. Read existing `agent_test.go` to identify the fake SSM and how it's wired into `newAgentListCmd`.
    2. Append `TestAgentListQueue` to the END of `agent_test.go`. Use the same package `cmd` and existing fakes.
    3. Mark with `t.Skip("Wave 3: --queue flag on agent list not yet implemented")` if the flag doesn't yet exist (avoid compile errors).
    4. Run `go vet` + `go test ./internal/app/cmd/ -run TestAgentListQueue -v`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue &amp;&amp; go vet ./internal/app/cmd/ &amp;&amp; go test ./internal/app/cmd/ -run TestAgentListQueue -v -count=1 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `TestAgentListQueue` appears in test output as SKIP or FAIL (RED). No new PASS. Existing tests in `agent_test.go` continue passing. File grows by ~30-60 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create bash test harness skeleton for runner state machine</name>
  <files>pkg/profile/configfiles/km-queue-runner_test.sh</files>
  <behavior>
    A bash test script that defines (but doesn't run, until Wave 2 lands the actual runner) tests for the bash runner's state-machine behavior. Uses plain bash + `[[ ]]` asserts + `exit 1` on failure — no `bats` or `shellspec` dependency (lightest fit per CONTEXT.md discretion).

    Test cases (per BRIEF.md):
    - `test_reconcile_running_to_pending` — Set up a tmp queue dir with `001.meta.json` containing `{"status":"running"}`. Source the runner script's reconcile function (or invoke runner with a `--reconcile-only` mode if Wave 2 builds it). Assert: after reconcile, `001.meta.json` has `{"status":"pending"}`.
    - `test_lowest_pending_picked_first` — Set up `001` and `002` both `pending`. Mock the `claude` invocation to write `done` status and exit 0. Run one pass of the runner loop (or call its pick-next helper). Assert: `001.meta.json` is now `running` (or `done`) before `002` is touched.
    - `test_failure_marks_remaining_skipped` — Set up 001/002/003 all `pending`. Mock `claude` to exit 1 on the first call. Run the loop. Assert: `001.meta.json` = `failed`, `002.meta.json` = `skipped`, `003.meta.json` = `skipped`. Runner exits non-zero.
    - `test_auth_probe_bedrock_success_proceeds` — Mock `aws bedrock-runtime invoke-model` (PATH-shimmed `aws` script exiting 0). Verify: probe returns success, runner proceeds to entry pick.
    - `test_auth_probe_bedrock_failure_loops` — Mock `aws` exiting 1. Verify: probe returns failure, runner does NOT pick an entry; (test passes if runner sleeps and re-probes — use a counter + max-iterations cap to avoid infinite loop in test).
    - `test_auth_probe_direct_api_creds_present` — Create `$HOME/.claude/credentials.json` with valid JSON; ensure `jq -e .` passes. Probe succeeds.
    - `test_auth_probe_direct_api_creds_missing` — No file. Probe fails; runner waits.

    For Wave 0, every test function body should be:
    ```bash
    test_reconcile_running_to_pending() {
        echo "SKIP: Wave 2 implements /opt/km/bin/km-queue-runner"
        return 0  # SKIP semantics; not a hard fail in Wave 0
    }
    ```
    A `main()` at the bottom runs each test and prints SKIP/PASS/FAIL counts. Exit 0 if all SKIP or all PASS; exit 1 if any FAIL.
  </behavior>
  <action>
    1. Ensure `pkg/profile/configfiles/` directory exists (Bash: `mkdir -p /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue/pkg/profile/configfiles/`). It may already exist or be new — RESEARCH.md mentions it as the original spec landing but the final implementation pushes runner script via userdata.go; the test harness still lives in `pkg/profile/configfiles/` for organizational clarity.
    2. Write `km-queue-runner_test.sh` per the <behavior> spec above. Mark file `chmod +x`.
    3. The file MUST be self-contained — no external bats/shellspec dependency.
    4. The file MUST exit 0 in Wave 0 (all tests SKIP). Wave 2 flips them to real tests.
    5. Run it: `bash pkg/profile/configfiles/km-queue-runner_test.sh` — expect "7 SKIP, 0 PASS, 0 FAIL" output and exit 0.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr/.claude/worktrees/phase-86-prompt-queue &amp;&amp; bash pkg/profile/configfiles/km-queue-runner_test.sh 2>&amp;1 | tail -20 &amp;&amp; echo "EXIT=$?"</automated>
  </verify>
  <done>
    Script exists, is executable, runs without errors, prints SKIP counts for all 7 cases, exits 0. File is ~80-150 lines of bash. Wave 2 will replace the `echo SKIP` bodies with real test logic.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. **Go test surface exists in RED:**
   `go vet ./internal/app/cmd/` exits 0 (file compiles).
   `go test ./internal/app/cmd/ -run "TestCreatePrompt|TestAgentListQueue|TestResolvePrompts|TestPushQueueFiles|TestQueueRunnerStateMachine" -v -count=1` runs without panics. Output shows SKIP or FAIL lines for every PQ-NN behavior — zero PASS for unimplemented work.

2. **Bash harness invokable:**
   `bash pkg/profile/configfiles/km-queue-runner_test.sh` exits 0 with SKIP markers for all 7 test functions.

3. **Existing tests still pass:**
   `make build` succeeds (per CLAUDE.md ldflags requirement).
   `go test ./internal/app/cmd/ -count=1` — pre-existing tests still PASS (regression guard for Wave 0 not breaking anything).

4. **Nyquist gate:**
   Update `.planning/phases/86-km-create-prompt-queue/86-VALIDATION.md` frontmatter: flip `nyquist_compliant: false` to `true` and `wave_0_complete: false` to `true`. (Wave 1+ executors will check these flags before implementing.)
</verification>

<success_criteria>
- `internal/app/cmd/create_prompt_test.go` exists with test functions named for PQ-01, PQ-02, PQ-03, PQ-04, PQ-05, PQ-06, PQ-08.
- `internal/app/cmd/agent_test.go` has new `TestAgentListQueue` function for PQ-07.
- `pkg/profile/configfiles/km-queue-runner_test.sh` exists, executable, self-contained, runs clean.
- All stubs are RED — they don't pass yet (skip or fail). They cannot pass until Waves 1-4 implement the production code.
- `make build` continues to succeed (no broken compilation).
- `.planning/phases/86-km-create-prompt-queue/86-VALIDATION.md` frontmatter updated: `nyquist_compliant: true`, `wave_0_complete: true`.
</success_criteria>

<output>
After completion, create `.planning/phases/86-km-create-prompt-queue/86-01-SUMMARY.md` covering:
- Test files created with line counts
- Which PQ-NN IDs each test asserts
- RED-state confirmation (`go test` output summary showing SKIP/FAIL, zero PASS for unimplemented behaviors)
- VALIDATION.md frontmatter updated to mark Nyquist-compliant
</output>
