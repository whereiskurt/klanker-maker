---
phase: 67.1
slug: slack-inbound-ack-reaction
status: planned
nyquist_compliant: true
wave_0_complete: true   # Wave 0 == Task 1 of Plan 01 (test scaffold) — handled in-plan, not separate phase
created: 2026-05-03
---

# Phase 67.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test 1.22 |
| **Config file** | none — uses standard `go test` |
| **Quick run command** | `go test ./pkg/slack/bridge/... ./internal/app/cmd/... -run "Reactor\|EventsScopes" -count=1` |
| **Full suite command** | `go test ./pkg/slack/... ./internal/app/cmd/... ./cmd/km-slack-bridge/... -count=1` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green + manual UAT smoke test (Slack scope add + 👀 reaction confirmed in real workspace)
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

*Populated by planner after PLAN.md tasks are written. Reference RESEARCH.md
§ Validation Architecture for the full test plan. Expected task IDs follow
the pattern `67.1-{plan}-{task}` matching plan numbering 01, 02, 03.*

| Task ID    | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|------------|------|------|-------------|-----------|-------------------|-------------|--------|
| 67.1-01-01 | 01   | 1    | UAT-1,2,5   | scaffold  | `grep -c 'newHandler(' pkg/slack/bridge/events_handler_test.go` (signature ripple) | ✅ extends existing | ⬜ pending |
| 67.1-01-02 | 01   | 1    | UAT-1,2     | unit      | `go test ./pkg/slack/bridge/ -run TestSlackReactorAdapter -count=1` | ❌ created here | ⬜ pending |
| 67.1-01-03 | 01   | 1    | UAT-1,2,5   | unit      | `go test ./pkg/slack/bridge/ -run TestEventsHandler_Reactor -count=1 -race` | ❌ created here | ⬜ pending |
| 67.1-02-01 | 02   | 1    | UAT-4       | unit      | `go test ./internal/app/cmd/ -run TestSlackInit_ScopeCheck -count=1` | ✅ extends existing | ⬜ pending |
| 67.1-02-02 | 02   | 1    | UAT-3       | unit      | `go test ./internal/app/cmd/ -run TestDoctor_SlackInboundEventsSubscription -count=1` | ✅ extends existing | ⬜ pending |
| 67.1-03-01 | 03   | 2    | UAT-1..5    | static    | `grep -q 'KM_SLACK_ACK_EMOJI' infra/modules/lambda-slack-bridge/v1.0.0/main.tf` | ❌ created here | ⬜ pending |
| 67.1-03-02 | 03   | 2    | UAT-1..5    | static    | `grep -q 'reactions:write' docs/slack-notifications.md && grep -q 'KM_SLACK_ACK_EMOJI' CLAUDE.md` | ✅ extends existing | ⬜ pending |
| 67.1-03-03 | 03   | 2    | UAT-1..5    | manual    | operator UAT checklist (see Manual-Only Verifications) | n/a   | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `pkg/slack/bridge/events_handler_test.go` — extend with 4 new test funcs:
      `TestEventsHandler_Reactor_HappyPath`,
      `TestEventsHandler_Reactor_FailureDoesNotBlock`,
      `TestEventsHandler_Reactor_BotLoopSkips`,
      `TestEventsHandler_Reactor_NilReactorIsNoop`
- [ ] `pkg/slack/bridge/events_handler_test.go` — `newHandler()` helper signature
      extended to return a `*fakeReactor` (ripples to ~15 existing call sites,
      all in same file — global find/replace)
- [ ] `pkg/slack/bridge/events_handler_test.go` — `fakeReactor` struct with
      `calls []reactorCall` recording channel/ts/emoji and optional
      `errToReturn` field
- [ ] `internal/app/cmd/doctor_slack_inbound_test.go` — extend existing
      `TestDoctor_SlackInboundEventsSubscription_HasAllScopes` and
      `_MissingScope` to include `reactions:write` in expected required-set
- [ ] `internal/app/cmd/slack_test.go` (or wherever `VerifyEventsAPIScopes` is
      tested) — extend test cases with `reactions:write` in required list
- [ ] No new framework install needed — existing `go test` covers all paths

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 👀 reaction appears in real Slack within ~1s of message post | UAT-1 | Requires real Slack workspace + provisioned inbound sandbox; cannot mock the round-trip latency or Slack's emoji rendering | 1. Add `reactions:write` scope to Slack App, reinstall<br>2. `make build && km init --lambdas`<br>3. `km doctor` — confirm `slack_app_events_scopes` PASS<br>4. Post any text in `#sb-{id}` of an existing inbound-enabled sandbox<br>5. Verify 👀 reaction appears within 1-2s |
| Reaction missing-scope failure logs warning, does NOT block dispatch | UAT-2 | Requires inducing a real Slack `missing_scope` error in production; safer to verify in real workspace by deploying without scope first then adding | 1. Deploy bridge code WITHOUT adding scope<br>2. Post message in `#sb-{id}`<br>3. Verify SQS message arrives + agent runs (no regression)<br>4. Verify CloudWatch log shows `events: reaction failed` warning with `missing_scope`<br>5. Add scope, re-test, verify 👀 now appears |
| `KM_SLACK_ACK_EMOJI=hourglass` env override works | UAT-config | Bridge env var change requires deploy; verifies the operator-configurability requirement | 1. Set `KM_SLACK_ACK_EMOJI=hourglass_flowing_sand` in Lambda env<br>2. Re-deploy<br>3. Post message<br>4. Verify ⏳ instead of 👀 |
| `km doctor` flags missing scope before fix | UAT-3 | Confirms the warning UX — automated tests cover the function but not the operator-experience path | 1. Pre-flight: confirm `reactions:write` is in the existing scope list (mock the auth.test response if needed)<br>2. Remove scope from Slack App temporarily<br>3. Run `km doctor`<br>4. Verify FAIL output mentions `reactions:write` with "Add at: Slack App config..." hint |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter (planner sets after task IDs assigned)

**Approval:** task IDs assigned by planner 2026-05-03; nyquist_compliant set to true.
