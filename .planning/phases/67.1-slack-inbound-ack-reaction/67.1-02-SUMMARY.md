---
phase: 67.1-slack-inbound-ack-reaction
plan: "02"
subsystem: slack
tags: [slack, reactions, scope-check, km-doctor, km-slack-init]

requires:
  - phase: 67.1-slack-inbound-ack-reaction plan 01
    provides: Reactor interface + SlackReactorAdapter + EventsHandler wiring (the bridge code that uses reactions:write at runtime)

provides:
  - VerifyEventsAPIScopes now requires reactions:write (km slack init warns at setup time)
  - checkSlackAppEventsScopes now requires reactions:write (km doctor FAIL when scope missing)
  - Success message for km doctor events-scopes check names all three required scopes
  - 4 TestSlackInit_ScopeCheck_* tests (3 updated + 1 new MissingReactionsWrite)
  - 6 TestDoctor_SlackInboundEventsSubscription_* tests (3 updated + 1 new MissingReactionsWrite + 2 unchanged)

affects:
  - 67.1-slack-inbound-ack-reaction plan 03 (operator runbook + UAT references these checks)

tech-stack:
  added: []
  patterns:
    - "Scope-check extension: append new scope to required := []string{...} in both VerifyEventsAPIScopes and checkSlackAppEventsScopes; update success message; extend tests"

key-files:
  created: []
  modified:
    - internal/app/cmd/slack.go
    - internal/app/cmd/slack_test.go
    - internal/app/cmd/doctor_slack.go
    - internal/app/cmd/doctor_slack_inbound_test.go

key-decisions:
  - "Remediation text still says 'run km slack rotate-token to refresh the SSM-cached token' — per CONTEXT.md the bot token does NOT change on reinstall, so token rotation is NOT needed for a scope add; this wording should be softened in Plan 03 doc update (flagged, not fixed here)"
  - "Ordered reactions:write third in the required slice (after the two Phase 63 scopes) so missing-scope ordering in test assertions is deterministic"
  - "Four pre-existing test failures (TestAtList_WithRecords, TestConfigureInteractivePromptsUseNewNames, TestCreateDockerWritesComposeFile, TestApplyLifecycleOverrides_RunCreateRemoteSignature) confirmed pre-existing on HEAD before our changes — not caused by Plan 02; documented here, deferred"

patterns-established:
  - "TDD scope extension: update required slice in production code, update existing tests to include new scope in fixtures, add a targeted MissingReactionsWrite test covering the realistic upgrade scenario (Phase 63 ops who have the old scopes but not the new one)"

requirements-completed:
  - UAT-3
  - UAT-4

duration: 11min
completed: 2026-05-03
---

# Phase 67.1 Plan 02: Scope Verification for reactions:write Summary

**`reactions:write` added to required-scope lists in `km slack init` and `km doctor`, with 8 tests (4+4) covering the realistic Phase 63→67.1 upgrade scenario**

## Performance

- **Duration:** 11 min
- **Started:** 2026-05-03T18:32:55Z
- **Completed:** 2026-05-03T18:43:29Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- `VerifyEventsAPIScopes` (slack.go:767) now requires `["channels:history", "groups:history", "reactions:write"]` — `km slack init` warns when the new scope is missing
- `checkSlackAppEventsScopes` (doctor_slack.go:374) now requires same three scopes — `km doctor` FAIL output names `reactions:write` specifically; success message names all three
- All 4 `TestSlackInit_ScopeCheck_*` tests green: updated AllPresent/MissingOne/MissingBoth + new `MissingReactionsWrite`
- All 6 `TestDoctor_SlackInboundEventsSubscription_*` tests green: updated HasAllScopes/MissingScope/PartialScopes + new `MissingReactionsWrite` + unchanged AuthTestError/NilDeps

## Task Commits

1. **Task 1: Extend VerifyEventsAPIScopes + tests for reactions:write** - `9e8b0e3` (feat)
2. **Task 2: Extend checkSlackAppEventsScopes (km doctor) + tests for reactions:write** - `f5d670a` (feat)

## Files Created/Modified

- `internal/app/cmd/slack.go` — `VerifyEventsAPIScopes`: required slice extended to 3 scopes; doc-comment updated to mention Phase 67.1 and reactions:write
- `internal/app/cmd/slack_test.go` — 3 existing tests updated; `TestSlackInit_ScopeCheck_MissingReactionsWrite` added
- `internal/app/cmd/doctor_slack.go` — `checkSlackAppEventsScopes`: required slice extended to 3 scopes; success message updated; doc-comment updated
- `internal/app/cmd/doctor_slack_inbound_test.go` — 3 existing tests updated; `TestDoctor_SlackInboundEventsSubscription_MissingReactionsWrite` added

## Decisions Made

1. **Remediation text softening deferred to Plan 03.** The existing `Remediation` string says "run 'km slack rotate-token' to refresh the SSM-cached token" — per CONTEXT.md locked decision, "Bot token does NOT change on reinstall, so no token rotation needed." The token-rotation guidance is technically inaccurate for a scope-add scenario. Flagged for Plan 03 doc update rather than changed here to keep this plan's scope minimal.

2. **reactions:write placed third in required slice.** Preserves backward-compatible missing-scope ordering: if all three are missing, the order is `channels:history, groups:history, reactions:write` — matching the two existing unit tests that verify specific order of missing values.

3. **Pre-existing test failures documented, not fixed.** Four tests failing before our changes: `TestAtList_WithRecords`, `TestConfigureInteractivePromptsUseNewNames`, `TestCreateDockerWritesComposeFile`, `TestApplyLifecycleOverrides_RunCreateRemoteSignature`. Confirmed pre-existing on HEAD before Plan 02 changes. Scope boundary rule: only fix issues directly caused by current task's changes. Logged for separate fix.

## No Call Sites Assume Old Required-Slice Length

Confirmed by grep — only two production call sites exist, neither assumes a fixed-length required set:
- `doctor.go:2127` — passes a `getScopes` func; no length assumption
- `slack.go:314,325` — calls `VerifyEventsAPIScopes` and checks boolean result only

## Remediation Text Note

The `Remediation` field in `checkSlackAppEventsScopes` still says: "run 'km slack rotate-token' to refresh the SSM-cached token". Per CONTEXT.md: "Bot token does NOT change on reinstall, so no token rotation needed." This guidance is misleading for the scope-add workflow. Recommend softening in Plan 03 operator runbook to something like: "After adding the scope and reinstalling the app in Slack, run 'km doctor' again to verify — no token rotation required."

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None — both functions had clear verified insertion points from RESEARCH.md. Tests passed on first run after implementing changes.

## User Setup Required

None — no external service configuration required by this plan. Plan 03 covers the operator scope-add step and UAT checkpoint.

## Next Phase Readiness

Plan 02 is complete. Plan 01 (bridge code) and Plan 02 (scope checks) are both Wave 1 complete. Plan 03 (operator runbook, Terraform passthrough for `KM_SLACK_ACK_EMOJI`, UAT checkpoint) can proceed as the Wave 2 gate.

The `reactions:write` scope is now verified at both operator setup time (`km slack init`) and continuous health monitoring (`km doctor`). Operators who deploy the Plan 01 bridge code without adding the scope will get an immediate, actionable `km doctor` FAIL rather than silent CloudWatch warnings.

---
*Phase: 67.1-slack-inbound-ack-reaction*
*Completed: 2026-05-03*
