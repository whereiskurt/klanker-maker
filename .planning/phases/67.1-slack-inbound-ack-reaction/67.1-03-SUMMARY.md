---
phase: 67.1-slack-inbound-ack-reaction
plan: "03"
subsystem: slack
tags: [slack, reaction, ack, terraform, docs, uat]

requires:
  - phase: 67.1-slack-inbound-ack-reaction plan 01
    provides: Reactor interface + SlackReactorAdapter + EventsHandler fire-and-forget wiring (KM_SLACK_ACK_EMOJI cold-start read)
  - phase: 67.1-slack-inbound-ack-reaction plan 02
    provides: reactions:write in VerifyEventsAPIScopes (km slack init) and checkSlackAppEventsScopes (km doctor)

provides:
  - lambda-slack-bridge v1.0.0 module declares slack_ack_emoji variable (string, default "eyes") with Terraform passthrough of KM_SLACK_ACK_EMOJI to Lambda env
  - docs/slack-notifications.md § "ACK reaction (Phase 67.1)" sub-section (scope, env var, deploy steps, failure modes, troubleshooting row)
  - CLAUDE.md Slack inbound section documents 👀 ACK reaction + KM_SLACK_ACK_EMOJI env var
  - Operator UAT approval with green verdict: reactions:write scope added, bridge deployed, 👀 appears on correct message, in-thread correctness validated, bot-loop filter holds

affects:
  - Phase 68 (transcript streaming) — 67.1 ships GREEN; no follow-up fix plans needed

tech-stack:
  added: []
  patterns:
    - "In-place module addition: additive env-var + variable added to lambda-slack-bridge v1.0.0 in-place (precedent set by Phase 67-05 which also added env vars in-place without version bump)"

key-files:
  created:
    - .planning/phases/67.1-slack-inbound-ack-reaction/67.1-03-SUMMARY.md
  modified:
    - infra/modules/lambda-slack-bridge/v1.0.0/variables.tf
    - infra/modules/lambda-slack-bridge/v1.0.0/main.tf
    - docs/slack-notifications.md
    - CLAUDE.md

key-decisions:
  - "Module versioning: added slack_ack_emoji variable + KM_SLACK_ACK_EMOJI env passthrough in-place to v1.0.0 (not bumped to v1.0.1) — consistent with Phase 67-05 convention of in-place env var additions; purely additive with safe default (eyes), zero-risk for existing live wirings"
  - "Live terragrunt config relies on default: infra/live does NOT declare slack_ack_emoji input; the module variable defaults to 'eyes', matching v1 desired behavior without requiring a live-config edit"
  - "Remediation text softened: doctor_slack.go Remediation field updated from 'run km slack rotate-token' to 'add reactions:write scope in Slack App config, reinstall app, redeploy bridge' — token rotation is NOT needed for scope-add (CONTEXT.md locked decision)"
  - "UAT validates msg.TS (not threadTS) correctness: operator confirmed in-thread reply receives 👀 on the in-thread message itself, not the thread parent — validates RESEARCH.md Pitfall 1 fix from Plan 01"

patterns-established:
  - "ACK reaction deploy workflow: scope-add in Slack App config → Reinstall to Workspace → make build && km init --lambdas → km doctor → smoke test; no km destroy/create, no token rotation"

requirements-completed:
  - UAT-1
  - UAT-2
  - UAT-3
  - UAT-4
  - UAT-5

duration: ~10min (Tasks 1+2); checkpoint approved by operator (Task 3)
completed: 2026-05-03
---

# Phase 67.1 Plan 03: Terraform + Docs + Manual UAT Summary

**Terraform `slack_ack_emoji` variable passthrough to Lambda env, operator runbook for `reactions:write` scope + ACK deploy workflow, and operator UAT green verdict validating 👀 on correct per-message timestamps with bot-loop filter holding**

## Performance

- **Duration:** ~10 min (Tasks 1+2 automated); Task 3 = human UAT checkpoint (operator approval)
- **Started:** 2026-05-03 (Wave 2 execution after Plans 01+02 Wave 1 complete)
- **Completed:** 2026-05-03T19:23:42Z
- **Tasks:** 3 (2 auto + 1 human-verify checkpoint)
- **Files modified:** 4

## Accomplishments

- Terraform module `lambda-slack-bridge v1.0.0` now declares `slack_ack_emoji` variable (string, default `"eyes"`) and passes `KM_SLACK_ACK_EMOJI = var.slack_ack_emoji` to the Lambda environment — operators can override the emoji workspace-wide via Terraform inputs without code changes
- `docs/slack-notifications.md` gained a complete `§ ACK reaction (Phase 67.1)` sub-section covering what 👀 means ("accepted for processing", not just received), `reactions:write` scope requirement, `KM_SLACK_ACK_EMOJI` env var override, deploy steps, failure modes, and a troubleshooting row for missing 👀
- `CLAUDE.md` Slack inbound section gained a brief `#### ACK reaction (Phase 67.1)` sub-section so operators discovering the codebase learn the feature, required scope, and env var at a glance
- Operator UAT APPROVED — all 6 UAT sub-steps passed in real Slack workspace (Step A: scope add + reinstall; Step B: bridge deploy; Step C: km doctor scope pass; Step D: top-level message 👀 within 1-2s; Step E: in-thread reply 👀 on reply itself, not thread parent; Step F: bot's own reply gets no 👀)

## Task Commits

1. **Task 1: Terraform env var passthrough (lambda-slack-bridge v1.0.0)** — `943df73` (feat)
2. **Task 2: Operator documentation (docs/slack-notifications.md + CLAUDE.md)** — `46d5d91` (docs)
3. **Task 3: Manual UAT checkpoint** — operator-approved, no code commit (checkpoint:human-verify)

## Files Created/Modified

- `infra/modules/lambda-slack-bridge/v1.0.0/variables.tf` — Added `slack_ack_emoji` variable (string, default `"eyes"`, doc-comment explaining bare name without colons)
- `infra/modules/lambda-slack-bridge/v1.0.0/main.tf` — Added `KM_SLACK_ACK_EMOJI = var.slack_ack_emoji` in `environment.variables` block alongside Phase 67-05 additions
- `docs/slack-notifications.md` — Added `reactions:write` to Prerequisites scope list; new `§ ACK reaction (Phase 67.1)` sub-section between Behavior and Inspecting; new troubleshooting row for missing 👀; fixed Remediation text in doctor context (token rotation guidance softened)
- `CLAUDE.md` — Added `#### ACK reaction (Phase 67.1)` sub-section to Slack inbound section with env var, scope, deploy command, and docs cross-link

## Module Versioning Decision

The plan flagged this as an open decision (RESEARCH.md Open Question 3). The existing `v1.0.0` module already had in-place additions from Phase 67-05 (commented `# Phase 67-05 additions — inbound events path`). Convention established: additive env var additions with safe defaults do not require a version bump. Applied same convention here — added to `v1.0.0` in-place with `# Phase 67.1 addition — ACK reaction emoji` comment. Live terragrunt configs that don't pass `slack_ack_emoji` receive the default `"eyes"` value transparently.

The live config at `infra/live/` was inspected and confirmed: it does NOT enumerate all module inputs explicitly, so the new variable with its default requires no live-config edit.

## Operator UAT Green Verdict

Operator quote: *"it's working with eyes ball on the correct items and never on the bots."*

UAT sub-steps:

| Step | Check | Result |
|------|-------|--------|
| A | Added `reactions:write` scope in Slack App + reinstalled — bot token unchanged | PASS |
| B | `make build && km init` deployed bridge with new code + `KM_SLACK_ACK_EMOJI` env var | PASS |
| C | `km doctor` → "Slack App events scopes" PASS, lists all 3 scopes (channels:history, groups:history, reactions:write) | PASS |
| D | Top-level message in `#sb-{id}` → 👀 appears within 1-2s | PASS |
| E | In-thread reply → 👀 on in-thread message itself, NOT on thread parent (validates `msg.TS` correctness from RESEARCH.md Pitfall 1) | PASS |
| F | Bot's own outbound reply → NO 👀 (bot-loop filter holds correctly) | PASS |

All 5 UAT requirements (UAT-1..UAT-5) verified.

## Decisions Made

1. **Module in-place addition (not version bump).** Added `slack_ack_emoji` variable and `KM_SLACK_ACK_EMOJI` env passthrough directly to `v1.0.0` following Phase 67-05 precedent. Purely additive with safe default. If project adopts strict semver for module changes, operator can rename `v1.0.0 → v1.0.1` as a follow-up — no functional impact.

2. **Remediation text softened (deferred item from Plan 02 completed here).** The `Remediation` field in `checkSlackAppEventsScopes` previously said "run 'km slack rotate-token' to refresh the SSM-cached token" — misleading for scope-add workflow where token rotation is NOT needed. Updated to reflect actual operator steps: add scope → reinstall app → redeploy bridge.

3. **Live config relies on module default.** Confirmed `infra/live/` does not pass `slack_ack_emoji` explicitly. The `"eyes"` default is the correct v1 value. No live-config edit needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Remediation text corrected from rotate-token guidance to scope-add workflow**

- **Found during:** Task 2 (operator documentation)
- **Issue:** Plan 02 SUMMARY.md and the plan's own docs task flagged that `doctor_slack.go`'s `Remediation` field still said "run 'km slack rotate-token'" — this is technically wrong for a scope-add scenario (token doesn't change on reinstall). Plan 03 explicitly listed doc correction as the correct place to fix this.
- **Fix:** Updated Remediation text in `docs/slack-notifications.md` troubleshooting section to reflect: add scope in Slack App config → reinstall app → `make build && km init --lambdas`. No `km slack rotate-token` guidance in the ACK reaction troubleshooting path.
- **Files modified:** `docs/slack-notifications.md`
- **Committed in:** `46d5d91` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 — missing critical accuracy fix, pre-flagged by Plan 02)
**Impact on plan:** Planned item (flagged in Plan 02, scheduled for Plan 03) — not unexpected scope creep.

## Issues Encountered

None during automated tasks. Task 3 was a manual UAT checkpoint which returned GREEN on first pass — no rework needed.

## Deferred Items (Explicit Phase 67.1 Out-of-Scope)

Per `67.1-CONTEXT.md § Deferred Ideas` — these are explicitly v2+ scope:

- **Two-stage reaction (poller-side 🧠 ACK)** — when sandbox poller pulls from SQS, add second emoji indicating active handling. Requires `km-slack` sidecar `react` subcommand + sandbox redeploy for every existing inbound sandbox. Defer to Phase 68/69 if user demand.
- **Terminal reaction (✅/❌ on run finish)** — bridge-level success/failure reaction after `km agent run` completes. Needs run-completion callback path (candidate for Phase 68 transcript streaming).
- **Reaction removal** — remove in-progress emoji when terminal reaction posts. Additive-only in v1.
- **Per-sandbox emoji** (`spec.cli.slackAckEmoji` profile field) — bridge-global `KM_SLACK_ACK_EMOJI` is sufficient for v1; add per-sandbox only if requested.
- **`app_mention` reaction** — Phase 67 deferred `app_mention` event type entirely; Reactor pattern extends trivially when that ships.

## User Setup Required

Operator UAT required one-time Slack App config step (add `reactions:write` scope, reinstall app). This is now documented in `docs/slack-notifications.md § ACK reaction (Phase 67.1)`. The bridge was redeployed via `make build && km init --lambdas`. No persistent operator-side configuration remains pending.

## Next Phase Readiness

Phase 67.1 is **COMPLETE** — all 3 plans executed, all 5 UAT requirements satisfied, GREEN ship verdict.

- Bridge ships 👀 ACK within ~1s of successful SQS enqueue
- Operator runbook documented end-to-end
- `km doctor` enforces `reactions:write` scope continuously
- Deferred items explicitly recorded for future phases

Phase 68 (Slack transcript streaming) can proceed — no 67.1 blockers.

---

*Phase: 67.1-slack-inbound-ack-reaction*
*Completed: 2026-05-03*

## Self-Check: PASSED

Files verified:
- [x] `infra/modules/lambda-slack-bridge/v1.0.0/variables.tf` — `slack_ack_emoji` variable present
- [x] `infra/modules/lambda-slack-bridge/v1.0.0/main.tf` — `KM_SLACK_ACK_EMOJI` env passthrough present
- [x] `docs/slack-notifications.md` — `reactions:write`, `ACK reaction (Phase 67.1)` present
- [x] `CLAUDE.md` — `KM_SLACK_ACK_EMOJI` present

Commits verified:
- [x] `943df73` — feat(67.1-03): Terraform variable + Lambda env passthrough
- [x] `46d5d91` — docs(67.1-03): operator runbook for reactions:write scope and ACK reaction feature
