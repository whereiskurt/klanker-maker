---
phase: 67.1-slack-inbound-ack-reaction
verified: 2026-05-03T20:00:00Z
status: passed
score: 5/5 UAT criteria verified
re_verification: false
---

# Phase 67.1: Slack Inbound ACK Reaction Verification Report

**Phase Goal:** Bridge Lambda adds 👀 reaction (configurable via `KM_SLACK_ACK_EMOJI`) to inbound Slack message via `reactions.add` AFTER successful SQS enqueue, providing immediate (<1s) visual confirmation — without blocking the 200 response or the dispatch path. Mirrors the PauseHinter fire-and-forget pattern.
**Verified:** 2026-05-03T20:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (UAT Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| UAT-1 | Posting a message in #sb-{id} produces 👀 within ~1s, every time | VERIFIED (human) | Operator quote: "it's working with eyes ball on the correct items and never on the bots." Bridge fires Reactor goroutine with fresh 5s context immediately after SQS.Send returns nil (events_handler.go:228-241). |
| UAT-2 | Reaction failure does NOT block SQS enqueue or 200 response | VERIFIED | Reactor runs in goroutine (go func() block at line 234), 200 is returned unconditionally at line 243. TestEventsHandler_Reactor_FailureDoesNotBlock passes with -race; SQS gets the message even when Reactor.Add returns error. |
| UAT-3 | km doctor flags the new scope when missing | VERIFIED | `checkSlackAppEventsScopes` in doctor_slack.go:375 includes "reactions:write" in required slice. TestDoctor_SlackInboundEventsSubscription_MissingReactionsWrite passes. Remediation text correctly reads "add scopes ... reinstall app ... no 'km slack rotate-token' needed." |
| UAT-4 | km slack init warns about the new scope on bootstrap | VERIFIED | `VerifyEventsAPIScopes` in slack.go:768 includes "reactions:write" in required slice. TestSlackInit_ScopeCheck_MissingReactionsWrite passes. |
| UAT-5 | Bot's own messages and channel_join system events do NOT get reactions | VERIFIED | isBotLoop filter runs at step 4 (events_handler.go:145-148), which is BEFORE the SQS write (step 8) and Reactor goroutine (step 10). TestEventsHandler_Reactor_BotLoopSkips confirms: bot_id set → 0 reactor calls, 0 SQS sends. |

**Score:** 5/5 UAT criteria verified (UAT-1 human-approved, UAT-2/3/4/5 automated)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `pkg/slack/bridge/events_interfaces.go` | `Reactor` interface with `Add(ctx, channel, ts, emoji) error` | VERIFIED | Lines 73-79. Doc comment specifies `already_reacted` MUST be treated as nil. |
| `pkg/slack/bridge/aws_adapters.go` | `SlackReactorAdapter` with `already_reacted=nil` semantics | VERIFIED | Lines 405-480. Token fetch, JSON POST to `/reactions.add`, `already_reacted` → return nil at line 475. `checkRateLimit` reused for 429 handling. |
| `pkg/slack/bridge/events_handler.go` | `Reactor` + `AckEmoji` fields; goroutine fires after step 8 on `msg.TS` (not threadTS) | VERIFIED | Fields at lines 41, 45. Goroutine at lines 228-241. Uses `msg.TS` (line 229), not `threadTS`. Fresh `context.WithTimeout(context.Background(), 5*time.Second)` at line 235 mirrors PauseHinter pattern at line 215. |
| `cmd/km-slack-bridge/main.go` | Reads `KM_SLACK_ACK_EMOJI` (default "eyes"), wires `SlackReactorAdapter` | VERIFIED | Lines 173-181. `os.Getenv` with empty-string fallback to "eyes". Same `httpClient` and `tokenFetcher` instances as SlackPosterAdapter (15-min SSM cache shared). Also logged at cold-start via `envOr` at line 108. |
| `internal/app/cmd/slack.go` | `VerifyEventsAPIScopes` includes "reactions:write" | VERIFIED | Line 768: `required := []string{"channels:history", "groups:history", "reactions:write"}`. |
| `internal/app/cmd/doctor_slack.go` | `checkSlackAppEventsScopes` includes "reactions:write"; remediation text not "rotate-token" | VERIFIED | Line 375 for required slice. Line 392: Remediation correctly says "add scopes... reinstall app... no 'km slack rotate-token' needed." |
| `infra/modules/lambda-slack-bridge/v1.0.0/variables.tf` | `slack_ack_emoji` variable, default "eyes" | VERIFIED | Line 83-87. Type string, default "eyes". |
| `infra/modules/lambda-slack-bridge/v1.0.0/main.tf` | `KM_SLACK_ACK_EMOJI = var.slack_ack_emoji` env passthrough | VERIFIED | Line 257. |
| `docs/slack-notifications.md` | `reactions:write` scope documented; ACK reaction sub-section; troubleshooting row | VERIFIED | Line 459 (scope in prerequisites), line 515 (`§ ACK reaction (Phase 67.1)`), line 568 (troubleshooting row for missing 👀). |
| `CLAUDE.md` | `#### ACK reaction (Phase 67.1)` sub-section with env var and scope | VERIFIED | Lines 215-223. Includes `KM_SLACK_ACK_EMOJI`, `reactions:write` scope, deploy command, docs cross-link. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `events_handler.go` step 10 | `Reactor.Add(ctx, ch, msgTS, emoji)` | goroutine after line 195 SQS.Send success | VERIFIED | Reactor goroutine at line 228 is unreachable on SQS error (early return at line 200). Reaction only fires when SQS succeeds. |
| `main.go` cold-start | `SlackReactorAdapter` | `eventsHandler.Reactor = &bridge.SlackReactorAdapter{...}` line 177 | VERIFIED | Wired unconditionally. `eventsHandler.AckEmoji` set at line 181. |
| `SlackReactorAdapter.Add` | `reactions.add` Slack API | `POST https://slack.com/api/reactions.add` line 440 | VERIFIED | Token from `Tokens.Fetch`, JSON payload `{channel, timestamp, name}`. |
| `isBotLoop` filter | blocks Reactor for bot/system messages | step 4 at line 145 returns before step 10 at line 228 | VERIFIED | Early return at line 147 skips all subsequent steps including Reactor goroutine. |
| `VerifyEventsAPIScopes` | km slack init scope warning | `required` slice at slack.go:768 | VERIFIED | Called from slack.go:314,325; boolean result drives warning UX. |
| `checkSlackAppEventsScopes` | km doctor FAIL output | `required` slice at doctor_slack.go:375 | VERIFIED | Called from doctor.go:2127. |

---

### Test Coverage

| Test | File | Status | What It Verifies |
|------|------|--------|-----------------|
| `TestSlackReactorAdapter_HappyPath` | `aws_adapters_test.go:971` | PASS | Path `/reactions.add`, body `"name":"eyes"`, correct timestamp, nil error |
| `TestSlackReactorAdapter_AlreadyReacted_IsIdempotentSuccess` | `aws_adapters_test.go:1004` | PASS | `already_reacted` Slack error → nil returned |
| `TestSlackReactorAdapter_GenericError_Propagates` | `aws_adapters_test.go:1024` | PASS | Non-`already_reacted` error → propagated |
| `TestSlackReactorAdapter_RateLimited` | `aws_adapters_test.go:1047` | PASS | HTTP 429 + Retry-After → `*ErrSlackRateLimited` |
| `TestEventsHandler_Reactor_HappyPath` | `events_handler_test.go:661` | PASS | Valid message → Reactor.Add called once with (channel, msg.TS, "eyes") |
| `TestEventsHandler_Reactor_FailureDoesNotBlock` | `events_handler_test.go:701` | PASS | Reactor.Add error → SQS still has message, response is 200 |
| `TestEventsHandler_Reactor_BotLoopSkips` | `events_handler_test.go:730` | PASS | bot_id set → isBotLoop → 0 reactor calls |
| `TestEventsHandler_Reactor_NilReactorIsNoop` | `events_handler_test.go:760` | PASS | nil Reactor → no panic, SQS write succeeds |
| `TestSlackInit_ScopeCheck_MissingReactionsWrite` | `slack_test.go:680` | PASS | Phase 63 scopes present but no reactions:write → missing=[reactions:write] |
| `TestDoctor_SlackInboundEventsSubscription_MissingReactionsWrite` | `doctor_slack_inbound_test.go:217` | PASS | Has channels:history + groups:history but not reactions:write → FAIL listing reactions:write |

**Test suite runs (with -race):**
- `pkg/slack/bridge`: PASS (1.641s)
- `cmd/km-slack-bridge`: PASS (1.374s)
- `internal/app/cmd`: PASS (1.995s, scoped to ScopeCheck + SlackInboundEventsSubscription tests)

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments in changed files. No stub implementations. No empty handlers. No console-log-only logic. All goroutines have proper context cancellation (`defer cancel()`).

---

### Human Verification

UAT-1 (visual, real-time, <1s timing) and UAT-5 (bot-loop in real Slack) were validated by the operator in a real Slack workspace during Plan 03 UAT checkpoint.

**Operator approval:** "it's working with eyes ball on the correct items and never on the bots."

UAT sub-steps D (top-level message 👀 within 1-2s) and F (bot reply → no 👀) satisfy UAT-1 and UAT-5 respectively. UAT-2/3/4 are fully covered by automated tests.

---

## Summary

Phase 67.1 goal is fully achieved. The bridge Lambda adds a configurable 👀 ACK reaction to inbound Slack messages immediately after successful SQS enqueue, without blocking the 200 response path. Every implementation requirement from CONTEXT.md is satisfied:

- `Reactor` interface and `SlackReactorAdapter` production adapter are substantive and wired
- Fire-and-forget goroutine mirrors PauseHinter pattern exactly (fresh 5s background context)
- Reaction targets `msg.TS` (not `threadTS`), preventing `message_not_found` for in-thread replies
- `already_reacted` is treated as idempotent nil — Lambda retries are safe
- `KM_SLACK_ACK_EMOJI` env var with "eyes" default is wired end-to-end (cold-start → Lambda env → Terraform variable)
- `reactions:write` scope is enforced by both `km slack init` and `km doctor`
- Remediation text correctly guides scope-add workflow (no token rotation)
- Documentation updated in both `docs/slack-notifications.md` and `CLAUDE.md`
- 10 new tests (8 bridge + 2 scope-check) all pass with `-race`
- Operator UAT approved in real Slack workspace

---

_Verified: 2026-05-03T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
