---
phase: 96-slack-default-router-orphan-channel-mention-reply
plan: 03
type: execute
wave: 3
depends_on: [96-02]
files_modified:
  - pkg/slack/bridge/events_interfaces.go
  - pkg/slack/bridge/events_handler.go
  - pkg/slack/bridge/events_handler_test.go
  - cmd/km-slack-bridge/main.go
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: false
requirements: [SLACK-RTR-ORPHAN, SLACK-RTR-REPLY, SLACK-RTR-COOLDOWN, SLACK-RTR-SAFE, SLACK-RTR-E2E]
must_haves:
  truths:
    - "On a front-door FetchByChannel miss with zero peer claims, default_router=true, a bot @-mention, and cooldown clear, exactly one threaded reply is posted"
    - "Any claimed:true in the gather suppresses the router reply (owner handled it)"
    - "A non-mention message in an orphan channel never triggers the router reply"
    - "default_router=false (or RunningChannels/RouterCooldown unwired) => byte-identical to Phase 95 (no reply)"
    - "A second @-mention within the cooldown window posts no second reply; after TTL it replies again"
    - "The bot's own reply is filtered by isBotLoop and cannot re-trigger the router"
    - "The reply aggregates front-door + all peers' running channels rendered as <#CID>; empty list => guidance-only variant"
  artifacts:
    - path: "pkg/slack/bridge/events_handler.go"
      provides: "Front-door orphan detection + gated threaded reply via h.Slack.PostMessage"
      contains: "DefaultRouter"
    - path: "pkg/slack/bridge/events_interfaces.go"
      provides: "RouterCooldownStore interface"
      contains: "RouterCooldownStore"
    - path: "cmd/km-slack-bridge/main.go"
      provides: "KM_SLACK_DEFAULT_ROUTER read + RunningChannels + RouterCooldown wiring (default-off, nil-safe)"
      contains: "KM_SLACK_DEFAULT_ROUTER"
    - path: "docs/slack-notifications.md"
      provides: "Phase 96 default-router operator section"
      contains: "default_router"
  key_links:
    - from: "events_handler.go front-door miss"
      to: "h.Slack.PostMessage threaded reply"
      via: "orphan gates (default_router + mention + cooldown + zero claims)"
      pattern: "PostMessage"
    - from: "events_handler.go cooldown gate"
      to: "RouterCooldownStore.Reserve(router-cooldown:{channel}, 3600)"
      via: "nonces-table TTL"
      pattern: "Reserve"
    - from: "cmd/km-slack-bridge/main.go"
      to: "eventsHandler.DefaultRouter/RunningChannels/RouterCooldown"
      via: "KM_SLACK_DEFAULT_ROUTER env read"
      pattern: "DefaultRouter"
---

<objective>
Wire the front-door orphan reply on top of Plan 02's scatter-gather + lister. This
plan owns the front-door MISS-PATH behavior change, the cooldown, the cold-start
wiring, the docs, and the manual E2E checkpoint.

Behavior: on a front-door `FetchByChannel` miss, the handler calls Plan 02's
`Broadcast` (now returning `[]PeerClaimResult`), tallies claims, and — only when
ALL of (default_router enabled, zero claims, bot @-mention, per-channel cooldown
clear) hold — posts exactly one threaded reply listing running sandbox channels
aggregated from the front door's own lister + all peers' `Channels`, rendered as
`<#CID>` mentions. Cooldown reuses the nonces table via a `router-cooldown:{channel}`
TTL key (no km-sandboxes row exists for an orphan channel).

`default_router` defaults false ⇒ Phase 96 dormant; with it off the path returns
200 exactly as Phase 95.

Purpose: Deliver the user-visible router reply + cooldown + deploy wiring; verify
the cross-install E2E manually.
Output: The orphan-reply logic, RouterCooldownStore + adapter, main.go wiring,
docs, CLAUDE.md note, and operator-verified E2E.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/96-slack-default-router-orphan-channel-mention-reply/96-CONTEXT.md
@.planning/phases/96-slack-default-router-orphan-channel-mention-reply/96-RESEARCH.md
@.planning/phases/96-slack-default-router-orphan-channel-mention-reply/96-02-SUMMARY.md

# Files to read + EXTEND (AS-MERGED Phase 95 + Plan 02 output):
@pkg/slack/bridge/events_interfaces.go
@pkg/slack/bridge/events_handler.go
@pkg/slack/bridge/aws_adapters.go
@cmd/km-slack-bridge/main.go
@docs/slack-notifications.md

<interfaces>
<!-- From 96-RESEARCH.md §§ 2, 4, 5, 8. Plan 02 already added PeerClaimResult, SandboxChannelInfo, RunningChannelLister, DDBRunningChannelLister, the new Broadcast signature, and h.RunningChannels. -->

Plan 02 outputs available here:
  - PeerClaimResult{ PeerURL string; Claimed bool; Channels []SandboxChannelInfo }
  - h.Relayer.Broadcast(ctx, body, headers) ([]PeerClaimResult, error)
  - h.RunningChannels RunningChannelLister (ListRunning(ctx) ([]SandboxChannelInfo, error))
  - DDBRunningChannelLister{ Client DDBScanAPI; TableName string }

events_handler.go (AS-MERGED — VERIFY exact lines on read):
  - Front-door miss path (~lines 222-233): the `else if h.Relayer != nil` branch that calls Broadcast and returns 200. THIS is where the tally + orphan reply goes.
  - Mention detection precedent (~lines 266-275): `uid, err := h.BotUserID.Fetch(ctx)` then `strings.Contains(msg.Text, "<@"+uid+">")`. Reuse this as a REQUIRE-mention gate (not a filter-out).
  - isBotLoop (lines 469-495): returns true when m.BotID != "" — runs at ~line 185 BEFORE the miss site. STRUCTURAL guarantee: the bot's own reply cannot re-trigger. No new guard needed; add a regression assertion only.
  - h.Slack SlackPoster field (line 57): PostMessage(ctx, channel, subject, body, threadTS) (string, error). Empty subject => body alone. mrkdwn true.
  - Thread anchor (lines 290-294 pattern): replyThreadTS := msg.ThreadTS; if "" { replyThreadTS = msg.TS }.
  - h.BotUserID wired at main.go line 242.

RouterCooldownStore (ADD to events_interfaces.go — 96-RESEARCH.md § 5):
  type RouterCooldownStore interface { Reserve(ctx context.Context, channelID string, cooldownSeconds int) error }
  // Reserve returns nil on first call (post the reply); bridge.ErrNonceReplayed within the window (suppress).

cmd/km-slack-bridge/main.go (AS-MERGED — 96-RESEARCH.md § 8):
  - Phase 95 relay wiring lines 268-282 (KM_SLACK_PEER_BRIDGES parse -> eventsHandler.Relayer)
  - initNonces package-level (declared line 70, constructed line 94) -> wrap for routerCooldownAdapter
  - initDDB + sandboxesTable available for DDBRunningChannelLister
  - nonceStoreAdapter precedent at lines 571-584 -> model routerCooldownAdapter on it
  - DynamoNonceStore.Reserve(ctx, nonce, ttlSeconds) at aws_adapters.go line 142; returns nil first time, ErrNonceReplayed within TTL

Cooldown key: "router-cooldown:" + msg.Channel, ttl 3600s.

ANTI-PATTERNS (96-RESEARCH.md § 9):
  - DO NOT post the reply in a goroutine (Lambda freeze; Pitfall 3). Post SYNCHRONOUSLY with a bounded context before returning.
  - DO NOT post if msg.Channel == "" (defensive guard).
  - Wait for Broadcast to return (wg.Wait already synchronous) before tally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RouterCooldownStore interface + front-door orphan detection, gates, and threaded reply</name>
  <files>pkg/slack/bridge/events_interfaces.go, pkg/slack/bridge/events_handler.go, pkg/slack/bridge/events_handler_test.go</files>
  <behavior>
    - TestEventsHandler_DefaultRouter_OrphanReply: front-door miss (no x-km-relayed), Broadcast returns all Claimed:false with peer channels [{C2,wrkr,hardened}], h.RunningChannels returns [{C1,orc,patch}], default_router on, msg mentions the bot, cooldown clear => h.Slack.PostMessage called EXACTLY once with channel=msg.Channel, threadTS=msg.TS, body containing both `<#C1>` and `<#C2>` and the `#sb-{alias}-{profile}` convention text.
    - TestEventsHandler_DefaultRouter_ClaimShortCircuit: any PeerClaimResult.Claimed:true => PostMessage NOT called (owner handled it).
    - TestEventsHandler_DefaultRouter_NonMention: orphan + zero claims but msg.Text has no `<@uid>` => PostMessage NOT called.
    - TestEventsHandler_DefaultRouter_Off: default_router=false (h.DefaultRouter false) => PostMessage NOT called; response 200 (Phase 95 byte-identical). Pair with the existing TestEventsHandler_NilRelayer_MissReturns200.
    - TestEventsHandler_DefaultRouter_EmptyList: zero claims, both front-door and peers return empty channels => PostMessage called once with the guidance-only variant (no `<#` mentions, still mentions the naming convention).
    - TestEventsHandler_RouterCooldown_Suppress: cooldown store returns ErrNonceReplayed => PostMessage NOT called.
    - TestEventsHandler_RouterCooldown_Allow: cooldown store returns nil => PostMessage called once.
    - TestEventsHandler_DefaultRouter_BotLoopNoRetrigger: a message with BotID set (the bot's own reply) is dropped by isBotLoop before the miss site => Broadcast NOT called, PostMessage NOT called (regression assertion).
    - TestEventsHandler_DefaultRouter_EmptyChannelGuard: msg.Channel == "" => no PostMessage, no panic.
  </behavior>
  <action>
    Per 96-RESEARCH.md §§ 2, 4, 5:
    1. events_interfaces.go: add `RouterCooldownStore` interface (signature in <interfaces>).
    2. events_handler.go: add fields on EventsHandler — `DefaultRouter bool` (plain bool; false zero value = off) and `RouterCooldown RouterCooldownStore` (near Relayer/RunningChannels).
    3. Front-door miss path (the `else if h.Relayer != nil` branch, ~lines 222-233): replace the Plan-02 placeholder marker with:
       - Capture `results, err := h.Relayer.Broadcast(ctx, req.Body, req.Headers)`. On err, log and return 200 (do NOT reply — uncertain).
       - Tally: if ANY result.Claimed => return 200 (owner handled; today's behavior).
       - Zero claims => orphan candidate. Apply gates IN ORDER (cheapest first), each failure returns 200 silently:
         a. `if !h.DefaultRouter { return 200 }`
         b. `if msg.Channel == "" { return 200 }` (defensive)
         c. mention gate: `uid, ferr := h.BotUserID.Fetch(ctx)`; if ferr or uid=="" or `!strings.Contains(msg.Text, "<@"+uid+">")` => return 200.
         d. cooldown gate: if `h.RouterCooldown != nil` and `h.RouterCooldown.Reserve(ctx, "router-cooldown:"+msg.Channel, 3600)` returns ErrNonceReplayed => return 200. (If RouterCooldown==nil, treat as no-cooldown but still reply — though main.go wires it whenever DefaultRouter is on.)
       - Build the channel list: front door's own `h.RunningChannels.ListRunning(ctx)` (nil-guard => empty) + every `result.Channels` from the gather; dedupe by channel ID (96-RESEARCH.md § 6 — defensive dedup).
       - Render reply text (96-RESEARCH.md § 4): non-empty => "No sandbox is bound to this channel. To work with a bot, join one of its channels — they're named `#sb-{alias}-{profile}`. Currently running:\n" + bullet list of `• <#CID> — {alias} ({profile})`. Empty => guidance-only ("…they're named `#sb-{alias}-{profile}`. None are currently running; …").
       - replyThreadTS := msg.ThreadTS; if "" => msg.TS.
       - Post SYNCHRONOUSLY (NOT a goroutine — Pitfall 3) with a bounded context (e.g. context.WithTimeout(ctx, 5*time.Second)): `h.Slack.PostMessage(replyCtx, msg.Channel, "", text, replyThreadTS)`. Log on error; never fail the 200.
       - Return 200.
    4. events_handler_test.go: add a fakeCooldownStore (Reserve returns configurable nil/ErrNonceReplayed) and reuse the fakeRunningChannelLister + fakePeerRelayer (with configurable results) from Plan 02. Implement all behavior tests above. Assert PostMessage call count + args via a recording fake SlackPoster (one likely already exists — reuse it).
  </action>
  <verify>
    <automated>go build ./pkg/slack/bridge/... && go test ./pkg/slack/bridge/... -run 'EventsHandler_DefaultRouter|RouterCooldown|NilRelayer' -count=1 -race</automated>
  </verify>
  <done>Front-door orphan reply fires exactly once only when all gates pass; any claim, non-mention, default-off, or active cooldown suppresses it; empty list uses the guidance-only variant; reply is threaded and synchronous; bot-loop regression holds.</done>
</task>

<task type="auto">
  <name>Task 2: Cold-start wiring in main.go (default-off, nil-safe) + routerCooldownAdapter</name>
  <files>cmd/km-slack-bridge/main.go</files>
  <action>
    Per 96-RESEARCH.md § 8:
    1. Add a private `routerCooldownAdapter` type (model on `nonceStoreAdapter` lines 571-584):
       ```go
       type routerCooldownAdapter struct { inner *bridge.DynamoNonceStore }
       func (r *routerCooldownAdapter) Reserve(ctx context.Context, channelID string, cooldownSeconds int) error {
           return r.inner.Reserve(ctx, "router-cooldown:"+channelID, cooldownSeconds)
       }
       ```
    2. In `wireEventsHandler`, after the Phase 95 relay block (lines 268-282), add:
       ```go
       // Phase 96: default router. Off by default; only meaningful on the front-door install.
       if os.Getenv("KM_SLACK_DEFAULT_ROUTER") == "true" {
           eventsHandler.DefaultRouter = true
           eventsHandler.RunningChannels = &bridge.DDBRunningChannelLister{ Client: initDDB, TableName: sandboxesTable }
           eventsHandler.RouterCooldown = &routerCooldownAdapter{inner: initNonces}
           slog.Info("km-slack-bridge: default-router enabled")
       }
       ```
       Use the actual local names for the DDB client + sandboxes table name + nonce store as they appear in main.go (VERIFY on read: initDDB / sandboxesTable / initNonces per § 8). When the env var is absent/false, none of these fields are set => h.DefaultRouter is the false zero value => router dormant (nil-safe).
    3. Ensure `bridge.ErrNonceReplayed` is the error the handler checks (already exported by the bridge package).
  </action>
  <verify>
    <automated>go build ./cmd/km-slack-bridge/... && make build</automated>
  </verify>
  <done>main.go reads KM_SLACK_DEFAULT_ROUTER; when "true" wires DefaultRouter + RunningChannels (DDBRunningChannelLister) + RouterCooldown (nonces-backed); when absent/false leaves all router fields zero (dormant); `make build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Docs — docs/slack-notifications.md § Phase 96 + CLAUDE.md note</name>
  <files>docs/slack-notifications.md, CLAUDE.md</files>
  <action>
    1. docs/slack-notifications.md: add a "§ Phase 96 — Default router (orphan-channel mention reply)" section covering:
       - What it does: when the shared bot is @-mentioned in a channel no install owns, the designated front-door install posts ONE threaded reply with the `#sb-{alias}-{profile}` convention + running channels across all installs as `<#CID>`.
       - Config: `slack.default_router: true` in km-config.yaml on the FRONT-DOOR install only (no-op elsewhere). Default false = dormant (byte-identical to Phase 95).
       - Mechanism summary: claim-aware scatter-gather; zero claims = true orphan; legacy/error peer responses treated as claimed:true (rollout-safe mixed fleet).
       - Cooldown: per-channel 3600s via the nonces table.
       - No new Slack scopes; member-channels only.
       - Deploy: `make build-lambdas` (clean) + `km init --dry-run=false` (NOT `--sidecars`); deploy ALL installs before relying on cross-install channel lists (cite project_km_init_lambdas_doesnt_deploy + project_km_init_skips_existing_lambda_zips).
       - Out of scope (deferred): agentic self-serve create, non-member channels, DM fallback.
       Use generic placeholders (example.com / synthetic IDs) per project doc convention.
    2. CLAUDE.md: add a brief "Phase 96 (2026-06-05) — Slack default router" note in the phase-history area near the Phase 91/95 Slack notes, plus a "Where to look" row pointing at docs/slack-notifications.md § Phase 96. Mention the deploy reality (`make build-lambdas` + `km init --dry-run=false`, NOT `--sidecars`) and that no SandboxProfile schema change / sandbox recreate is needed.
  </action>
  <verify>
    <automated>grep -q "default_router" docs/slack-notifications.md && grep -qi "Phase 96" CLAUDE.md && echo OK</automated>
  </verify>
  <done>docs/slack-notifications.md has a Phase 96 default-router section; CLAUDE.md has a Phase 96 note + Where-to-look pointer; both mention the deploy constraint and the dormant-by-default behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual E2E — cross-install orphan reply (SLACK-RTR-E2E)</name>
  <what-built>
    The full default-router path: claim-aware scatter-gather, front-door orphan
    detection, gated threaded reply aggregating running channels across installs,
    per-channel cooldown, and the KM_SLACK_DEFAULT_ROUTER wiring + dynamodb:Scan
    IAM grant. This cannot be automated — it needs two real installs sharing one
    Slack App, live DynamoDB/SQS, and real Slack message delivery.
  </what-built>
  <how-to-verify>
    PRE-DEPLOY (operator, all installs):
    1. Set `slack:\n  default_router: true` in km-config.yaml on the FRONT-DOOR install only.
    2. On EACH install: `make build-lambdas` (clean) then `km init --dry-run=false` (NOT `--sidecars` — env-block + IAM changes require a full apply). Deploy ALL installs before testing cross-install lists.
    3. Confirm the bridge Lambda execution role policy now includes `dynamodb:Scan` on `{prefix}-sandboxes` (AWS console → IAM → the bridge role, or `aws iam get-role-policy`). Confirm `KM_SLACK_DEFAULT_ROUTER=true` in the front-door Lambda's environment tab.

    TEST (Slack):
    4. Have at least one running sandbox with a bound `#sb-*` channel on EACH install (so the aggregate list is non-empty across installs).
    5. Invite the shared bot to a channel with NO bound sandbox (a true orphan).
    6. @-mention the bot in that orphan channel.
       EXPECT: exactly ONE threaded reply on your message, naming the `#sb-{alias}-{profile}` convention and listing running sandbox channels from BOTH installs as `<#CID>` mentions.
    7. @-mention the bot AGAIN in the same channel within ~5 minutes.
       EXPECT: NO second reply (per-channel cooldown, 3600s).
    8. In a channel an install DOES own (a live `#sb-*`), @-mention the bot.
       EXPECT: NO router reply (the owner handles it via the normal SQS dispatch).
    9. Post a non-mention message in the orphan channel.
       EXPECT: NO router reply.

    Capture the actual deploy commands run and any cooldown-window timing in 96-UAT.md.
  </how-to-verify>
  <files>.planning/phases/96-slack-default-router-orphan-channel-mention-reply/96-UAT.md</files>
  <action>
    This is a manual operator checkpoint — SLACK-RTR-E2E cannot be automated (it needs
    two live installs sharing one Slack App + real Slack delivery). Pause and present the
    <how-to-verify> steps to the operator. Do the deploy (build-lambdas + km init
    --dry-run=false on all installs) and walk the operator through the Slack test. Record
    the actual commands, timings, and outcomes in 96-UAT.md. Do NOT mark the phase complete
    until the operator confirms all four EXPECT outcomes.
  </action>
  <verify>
    <automated>MANUAL — operator confirms in Slack; record results in 96-UAT.md. No automated command (live AWS + Slack required).</automated>
  </verify>
  <done>Operator confirms: exactly one threaded reply on the orphan @-mention listing both installs' running channels; no second reply within the cooldown; no reply in an owned channel; no reply on a non-mention. Results recorded in 96-UAT.md.</done>
  <resume-signal>Type "approved" if all four EXPECT outcomes held, or describe the deviation (e.g. "no reply appeared", "two replies", "owner channel also replied").</resume-signal>
</task>

</tasks>

<verification>
- `make build` succeeds; `go test ./pkg/slack/bridge/... -count=1 -race` green.
- Automated: orphan reply fires only on all-gates-pass; claim/non-mention/default-off/cooldown all suppress; empty-list guidance variant; bot-loop regression.
- Manual E2E (Task 4): exactly one reply on orphan mention listing both installs' channels; cooldown suppresses the repeat; owned channel + non-mention produce no reply.
</verification>

<success_criteria>
- A true-orphan @-mention on the front-door install (default_router on) yields one threaded reply aggregating running channels across all installs.
- Any peer claim, non-mention, default-off, or active cooldown suppresses the reply.
- default_router off => byte-identical to Phase 95.
- Operator E2E confirms cross-install behavior end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/96-slack-default-router-orphan-channel-mention-reply/96-03-SUMMARY.md`
(and record the manual E2E results in 96-UAT.md per the checkpoint).
</output>
