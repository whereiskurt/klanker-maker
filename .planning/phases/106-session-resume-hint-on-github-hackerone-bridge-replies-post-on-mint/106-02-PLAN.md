---
phase: 106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint
plan: 02
type: execute
wave: 1
depends_on: [106-01]
files_modified:
  - pkg/compiler/userdata.go
autonomous: true
requirements: [RESUME-HINT-FORMAT, RESUME-HINT-MINT, RESUME-HINT-GITHUB, RESUME-HINT-SLACK-EXCLUDED]
must_haves:
  truths:
    - "After a GitHub bridge turn that mints a new session id, the poller posts exactly ONE collapsed <details> resume-hint comment via km-github comment"
    - "The hint names the sandbox id, the /workspace run-from dir, and the agent-correct resume command (claude --resume / codex exec resume)"
    - "The hint fires only on mint: when NEW_GITHUB_SESSION differs from the prior GITHUB_SESSION (first turn or Gap-E re-mint)"
    - "The hint post is best-effort (|| true) and never blocks the SQS ack"
    - "The Slack poller remains byte-identical (dormancy goldens unchanged, no recapture)"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "GitHub poller post-on-mint resume-hint block"
      contains: "claude --resume"
  key_links:
    - from: "pkg/compiler/userdata.go (GITHUBINBOUND heredoc, post-write-back ~line 2391)"
      to: "/opt/km/bin/km-github comment --repo $REPO --number $NUMBER --body $HINT_BODY"
      via: "post-on-mint if guard + printf-built HINT_BODY"
      pattern: "km-github comment .*--body .*HINT_BODY"
    - from: "GitHub hint if-guard"
      to: "NEW_GITHUB_SESSION vs GITHUB_SESSION comparison"
      via: "post-on-mint condition"
      pattern: "NEW_GITHUB_SESSION.*!=.*GITHUB_SESSION"
---

<line_number_refresh date="2026-06-11">
Commit `73c64d53` (PR #18, "export HOME=/home/sandbox in bridge poller agent dispatch")
merged AFTER this plan was written and inserted ~11 `export HOME=/home/sandbox` lines into
the poller dispatch blocks, shifting every line below. **Textual anchors are authoritative
over the line numbers in this plan.** Current GitHub site (re-pinned 2026-06-11):
- `GITHUBINBOUND` heredoc opens at line **2094** (`cat > /opt/km/bin/km-github-inbound-poller << 'GITHUBINBOUND'`).
- `NEW_GITHUB_SESSION` is extracted at lines **2382–2386**.
- The DDB write-back block is `if [ -n "$NEW_GITHUB_SESSION" ] && [ -n "$REPO" ] && [ -n "$NUMBER" ]; then` at **2389**, with `update-item` + the "Session updated" echo ending at **2398**, closing `fi` at **2399**.
- **Insert the resume-hint block after the echo at line 2398, before the `fi` at 2399** (i.e. inside the mint-guarded write-back block, so it only runs when a new session id exists).
`#18` also makes `HOME=/home/sandbox` explicit inside each dispatch block — this REINFORCES
the design (run-from is still `cd /workspace`; storage is `/home/sandbox/.claude`). No design
change. Slack bounds remain **1535–2085** (unchanged — byte-identity guard still valid).
</line_number_refresh>

<objective>
Implement the GitHub inbound poller resume-hint block in `pkg/compiler/userdata.go`,
inside the `<< 'GITHUBINBOUND'` heredoc, immediately after the DDB session write-back
(the "Session updated" echo, currently line 2398 — see line_number_refresh above). The block posts a collapsed `<details>` fold (sandbox id + /workspace +
agent-correct resume command) via `km-github comment`, gated by a post-on-mint `if` and a
best-effort `|| true`. Drives Plan 01's GitHub test from RED to GREEN. Slack poller MUST
stay byte-identical.

Purpose: RESUME-HINT-GITHUB / RESUME-HINT-FORMAT / RESUME-HINT-MINT for the GitHub surface.
Output: ~12 added lines inside one heredoc; GitHub resume-hint test green; goldens unchanged.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-CONTEXT.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-RESEARCH.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-VALIDATION.md

@pkg/compiler/userdata.go
@pkg/compiler/userdata_github_inbound_test.go

<interfaces>
<!-- Exact injection site + in-scope variables (RESEARCH.md §GitHub poller). -->
<!-- The GITHUBINBOUND heredoc uses a single-quoted delimiter ⇒ backticks are LITERAL inside it. -->

Injection point: pkg/compiler/userdata.go ~line 2391, INSIDE the existing
  `if [ -n "$NEW_GITHUB_SESSION" ] && [ -n "$REPO" ] && [ -n "$NUMBER" ]` block,
  AFTER the `aws dynamodb update-item ...` call and the
  `echo "[km-github-inbound-poller] Session updated ..."` log line.

Variables already in scope at that site:
- SANDBOX_ID        — "${KM_SANDBOX_ID:-}" (line ~2100), e.g. sb-1a2b3c4d
- EFFECTIVE_AGENT   — "claude" | "codex" (computed ~2271-2280)
- NEW_GITHUB_SESSION— jq .session_id/.thread_id from output.json (~2375-2380); non-empty here
- GITHUB_SESSION    — prior stored id from DDB get-item (~2200); empty on first turn / Gap-E
- REPO              — jq .repo from envelope (~2155), "owner/repo"
- NUMBER            — jq .number from envelope (~2156)

km-github comment call surface (cmd/km-github/main.go): `--body` takes an INLINE string.
KM_GITHUB_REPLY_AGENT is NOT set in the poller's outer shell (only inside `sudo -u sandbox`
strings), so the hint receives NO attribution footer — correct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert GitHub post-on-mint resume-hint block</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    In the `<< 'GITHUBINBOUND'` heredoc, locate the GitHub session write-back: the
    `aws dynamodb update-item --table-name "$GITHUB_THREADS_TABLE" ...` call followed by
    `echo "[km-github-inbound-poller] Session updated ...`. This sits inside the existing
    `if [ -n "$NEW_GITHUB_SESSION" ] && [ -n "$REPO" ] && [ -n "$NUMBER" ]` guard (~line 2382-2392).

    Immediately AFTER that echo line (still inside that outer guard), insert the Phase 106
    resume-hint block, matching the locked format from RESEARCH.md §"GitHub Poller — Post-on-Mint Block":

      # Phase 106: post resume-hint fold on session mint (first turn or Gap-E re-mint).
      # Post-on-mint: fires only when the session id is new or changed. Best-effort
      # (|| true) — a failed hint post MUST NOT block the SQS ack or turn completion.
      if [ -n "$NEW_GITHUB_SESSION" ] && [ "$NEW_GITHUB_SESSION" != "${GITHUB_SESSION:-}" ]; then
        if [ "$EFFECTIVE_AGENT" = "codex" ]; then
          RESUME_CMD="codex exec resume $NEW_GITHUB_SESSION"
        else
          RESUME_CMD="claude --resume $NEW_GITHUB_SESSION"
        fi
        HINT_BODY=$(printf '<details>\n<summary>🔧 Resume this agent session</summary>\n\nOn sandbox `%s`, from the `/workspace` folder use `%s`\n</details>' \
          "$SANDBOX_ID" "$RESUME_CMD")
        /opt/km/bin/km-github comment --repo "$REPO" --number "$NUMBER" --body "$HINT_BODY" || true
      fi

    Critical constraints:
    - The `<< 'GITHUBINBOUND'` delimiter is single-quoted ⇒ NO Go/shell expansion of the
      heredoc text; the backticks in the printf format string are literal. Keep the printf
      format SINGLE-quoted so the backticks-around-%s render literally in the posted Markdown.
    - The summary text `🔧 Resume this agent session` and the literal mint guard
      `"$NEW_GITHUB_SESSION" != "${GITHUB_SESSION:-}"` MUST match Plan 01's assertions exactly.
    - Do NOT add `--body @file` — inline string per km-github surface.
    - Do NOT set KM_GITHUB_REPLY_AGENT (no attribution footer on the hint).
    - Touch ONLY the GITHUBINBOUND heredoc. Do NOT modify the Slack poller
      (`<< 'SLACKINBOUND'` / lines ~1535-2085) — it MUST stay byte-identical.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run TestUserdata_GitHubInboundPoller_ResumeHint -count=1</automated>
  </verify>
  <done>GitHub resume-hint test is GREEN; the GITHUBINBOUND heredoc contains the post-on-mint block with both agent branches, /workspace, SANDBOX_ID, || true, and the literal mint guard.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Slack-exclusion + full GitHub/compiler suite green</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    Verification-only task (no further edits unless a failure surfaces). Run the
    Slack-exclusion / dormancy byte-identity guards and the full GitHub inbound test set
    to prove the Slack poller and km-prefix goldens did NOT drift. If any byte-identity
    test fails, the Task 1 edit leaked outside the GITHUBINBOUND heredoc — revert the stray
    change; NO golden recapture is permitted for this phase (the dormancy goldens use
    GitHub-inbound-free / H1-free profiles and MUST be unchanged).
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run 'TestUserdataH1ByteIdentity|TestUserdataKmPrefixByteIdentity|TestUserdata_SlackPollerUnaffectedByGitHubInbound|TestUserdata_GitHubInbound' -count=1</automated>
  </verify>
  <done>`TestUserdataH1ByteIdentity`, `TestUserdataKmPrefixByteIdentity`, `TestUserdata_SlackPollerUnaffectedByGitHubInbound`, and all `TestUserdata_GitHubInbound*` tests PASS with no golden recapture.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/ -run TestUserdata_GitHubInboundPoller_ResumeHint -count=1` — GREEN.
- `go test ./pkg/compiler/ -run 'TestUserdataH1ByteIdentity|TestUserdataKmPrefixByteIdentity' -count=1` — GREEN, no recapture (Slack-exclusion / dormancy invariant).
- `go test ./pkg/compiler/ -count=1 -timeout 120s` — full compiler suite green (wave-merge gate).
</verification>

<success_criteria>
- GitHub poller emits the post-on-mint `<details>` resume-hint via `km-github comment`.
- Hint names sandbox id, /workspace, and the agent-correct resume command.
- Mint guard `NEW_GITHUB_SESSION != ${GITHUB_SESSION:-}` present; `|| true` non-blocking.
- Slack poller + dormancy goldens byte-identical (no recapture).
- Plan 01 GitHub test GREEN; full pkg/compiler suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-02-SUMMARY.md`
</output>
