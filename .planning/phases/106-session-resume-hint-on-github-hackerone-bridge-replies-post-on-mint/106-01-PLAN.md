---
phase: 106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/compiler/userdata_github_inbound_test.go
  - pkg/compiler/userdata_h1_byte_identity_test.go
autonomous: true
requirements: [RESUME-HINT-TESTS]
must_haves:
  truths:
    - "A failing GitHub resume-hint test exists that asserts the <details> fold, both agent branches, /workspace, SANDBOX_ID, || true, and the mint condition"
    - "The H1 enabled-poller test asserts the resume-hint markers (no --reply-to-researcher)"
    - "Both new/extended assertions FAIL against current HEAD (RED) — proving they pin Phase 106 behavior not pre-existing behavior"
  artifacts:
    - path: "pkg/compiler/userdata_github_inbound_test.go"
      provides: "TestUserdata_GitHubInboundPoller_ResumeHint (+ MintCondition assertions)"
      contains: "TestUserdata_GitHubInboundPoller_ResumeHint"
    - path: "pkg/compiler/userdata_h1_byte_identity_test.go"
      provides: "Extended TestUserdataH1EnabledRendersPoller wantSubstrings with hint markers"
      contains: "codex exec resume"
  key_links:
    - from: "pkg/compiler/userdata_github_inbound_test.go"
      to: "extractGitHubInboundPoller"
      via: "poller-scoped substring assertions"
      pattern: "extractGitHubInboundPoller"
    - from: "pkg/compiler/userdata_h1_byte_identity_test.go"
      to: "TestUserdataH1EnabledRendersPoller wantSubstrings"
      via: "appended hint-marker substrings"
      pattern: "wantSubstrings"
---

<objective>
Create the Wave 0 RED test scaffold that pins Phase 106's resume-hint behavior BEFORE
any production edit to `userdata.go`. This plan adds assertions only — no production
change. The tests MUST fail against current HEAD (proving they assert the new behavior),
then go green once Wave 1 implements the hint blocks.

Purpose: Nyquist gate. Every Wave 1 implementation task verifies against these tests.
Output: One new GitHub test function + extended H1 `wantSubstrings` — both RED at HEAD.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-CONTEXT.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-RESEARCH.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-VALIDATION.md

@pkg/compiler/userdata_github_inbound_test.go
@pkg/compiler/userdata_h1_byte_identity_test.go

<interfaces>
<!-- Existing test helpers the new assertions reuse — no codebase exploration needed. -->

From pkg/compiler/userdata_github_inbound_test.go:
- `minimalGitHubInboundProfile(t, inbound bool) *profile.SandboxProfile` — builds a profile with NotificationGitHubInboundSpec.Enabled = inbound
- `compileGitHubInboundUserData(t, p) string` — wraps generateUserData(p, "sb-gh-test", nil, "my-bucket", false, nil)
- `extractGitHubInboundPoller(t, out) string` — slices the `<< 'GITHUBINBOUND'` ... `\nGITHUBINBOUND\n` heredoc body
- `abbreviateUD(out)` — truncation helper for failure messages

From pkg/compiler/userdata_h1_byte_identity_test.go:
- `TestUserdataH1EnabledRendersPoller(t)` — flips notification.h1.inbound.enabled=true in-struct, renders userdata as "sb-phase103-h1on", asserts each entry in `wantSubstrings`. Existing entries include `km-h1 comment --report $REPORT_ID`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED GitHub resume-hint test</name>
  <files>pkg/compiler/userdata_github_inbound_test.go</files>
  <action>
    Append a new test function `TestUserdata_GitHubInboundPoller_ResumeHint` to
    `pkg/compiler/userdata_github_inbound_test.go`. It must mirror the existing
    poller-scoped pattern: build `minimalGitHubInboundProfile(t, true)`, compile via
    `compileGitHubInboundUserData`, extract the poller with `extractGitHubInboundPoller`,
    then assert (using the existing `strings.Contains` + `abbreviateUD` failure idiom)
    that the poller body contains ALL of these substrings:
      - `<details>`                          (the collapsible fold opener)
      - `🔧 Resume`                          (the locked summary emoji+wording; if the
                                              implementer's exact wording differs, this
                                              assertion is the contract — Wave 1 must match it)
      - `claude --resume`                    (Claude resume branch)
      - `codex exec resume`                  (Codex resume branch)
      - `/workspace`                         (run-from directory in the hint body — NOT /home/sandbox)
      - `SANDBOX_ID`                         (sandbox id referenced in the hint)
      - `|| true`                            (best-effort non-blocking guard on the hint post call)
      - `"$NEW_GITHUB_SESSION" != "${GITHUB_SESSION:-}"`  (the post-on-mint condition; this
                                              exact guard is what the research locked — assert
                                              the literal so MINT semantics can't silently drift)

    Add a doc comment above the function naming Phase 106 + the post-on-mint contract.
    Keep all assertions scoped to the extracted `poller` string (not whole `out`) so the
    test proves the hint lives inside the GitHub poller, not elsewhere.

    DO NOT touch userdata.go in this plan — this is a deliberately RED test.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run TestUserdata_GitHubInboundPoller_ResumeHint -count=1; test $? -ne 0 && echo "RED-AS-EXPECTED"</automated>
  </verify>
  <done>`go test ./pkg/compiler/ -run TestUserdata_GitHubInboundPoller_ResumeHint -count=1` FAILS at HEAD (RED), and the failure is a missing-substring assertion (compiles + runs, asserts the Phase 106 markers).</done>
</task>

<task type="auto">
  <name>Task 2: Extend H1 enabled-poller test with hint markers</name>
  <files>pkg/compiler/userdata_h1_byte_identity_test.go</files>
  <action>
    Extend the existing `wantSubstrings` slice inside `TestUserdataH1EnabledRendersPoller`
    (do NOT add a new test function — append entries to the existing slice, preserving the
    current 7 entries). Add these Phase 106 hint markers:
      - `<details>`                          (the fold opener in the H1 poller)
      - `🔧 Resume`                          (locked summary wording)
      - `claude --resume`                    (Claude branch)
      - `codex exec resume`                  (Codex branch)
      - `/workspace`                         (run-from dir)
      - `"$NEW_H1_SESSION" != "${H1_SESSION:-}"`  (post-on-mint condition for H1)

    Note the existing slice already asserts `km-h1 comment --report $REPORT_ID` — that
    same INTERNAL-by-default call surface is reused for the hint (no `--reply-to-researcher`).
    Do NOT add an assertion for the ABSENCE of `--reply-to-researcher` here (negative
    substring matching against the whole userdata is brittle — many call sites). The
    internal-only safety is enforced by the implementation reusing the bare
    `km-h1 comment --report` form; the byte-identity dormancy golden + code review cover it.

    Add a short inline comment tagging the new entries `// Phase 106 resume-hint markers`.

    DO NOT touch userdata.go in this plan.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run TestUserdataH1EnabledRendersPoller -count=1; test $? -ne 0 && echo "RED-AS-EXPECTED"</automated>
  </verify>
  <done>`go test ./pkg/compiler/ -run TestUserdataH1EnabledRendersPoller -count=1` FAILS at HEAD (RED) on the newly-added Phase 106 hint substrings; the 7 pre-existing assertions still pass (failure is only the new markers).</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/ -run TestUserdata_GitHubInboundPoller_ResumeHint -count=1` — RED (missing-substring failures, compiles cleanly).
- `go test ./pkg/compiler/ -run TestUserdataH1EnabledRendersPoller -count=1` — RED on Phase 106 markers only.
- `go vet ./pkg/compiler/` — clean (no compile errors in the new/edited test code).
- The byte-identity dormancy tests are UNCHANGED by this plan (test-only edits): `go test ./pkg/compiler/ -run 'TestUserdataH1ByteIdentity|TestUserdataKmPrefixByteIdentity' -count=1` still GREEN.
</verification>

<success_criteria>
- New GitHub resume-hint test exists and is RED at HEAD with missing-substring assertions.
- H1 enabled-poller test extended with Phase 106 markers and is RED at HEAD.
- No production code (`userdata.go`) changed. Dormancy goldens still green.
- Tests compile (`go vet` clean) so Wave 1 can drive them to GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-01-SUMMARY.md`
</output>
