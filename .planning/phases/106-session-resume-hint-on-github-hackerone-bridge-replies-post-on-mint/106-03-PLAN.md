---
phase: 106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint
plan: 03
type: execute
wave: 1
depends_on: [106-01, 106-02]
files_modified:
  - pkg/compiler/userdata.go
autonomous: true
requirements: [RESUME-HINT-FORMAT, RESUME-HINT-MINT, RESUME-HINT-H1, RESUME-HINT-SLACK-EXCLUDED]
must_haves:
  truths:
    - "After a HackerOne bridge turn that mints a new session id, the poller posts exactly ONE collapsed <details> resume-hint comment via km-h1 comment"
    - "The H1 hint is INTERNAL by default (no --reply-to-researcher) — never visible to the external researcher"
    - "The hint names the sandbox id, /workspace, and agent-correct resume command (claude --resume / codex exec resume)"
    - "The hint fires only on mint: NEW_H1_SESSION differs from the prior H1_SESSION"
    - "Best-effort (|| true); Slack poller stays byte-identical (no golden recapture)"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "H1 poller post-on-mint resume-hint block (internal-only)"
      contains: "codex exec resume"
  key_links:
    - from: "pkg/compiler/userdata.go (H1INBOUND heredoc, post-write-back ~line 2697)"
      to: "/opt/km/bin/km-h1 comment --report $REPORT_ID --body $HINT_BODY"
      via: "post-on-mint if guard + printf-built HINT_BODY, NO --reply-to-researcher"
      pattern: "km-h1 comment .*--report .*--body"
    - from: "H1 hint if-guard"
      to: "NEW_H1_SESSION vs H1_SESSION comparison"
      via: "post-on-mint condition"
      pattern: "NEW_H1_SESSION.*!=.*H1_SESSION"
---

<line_number_refresh date="2026-06-11">
Commit `73c64d53` (PR #18, "export HOME=/home/sandbox in bridge poller agent dispatch")
merged AFTER this plan was written and shifted every line below the dispatch blocks.
**Textual anchors are authoritative over the line numbers in this plan.** Current H1 site
(re-pinned 2026-06-11):
- `H1INBOUND` heredoc opens at line **2421** (`cat > /opt/km/bin/km-h1-inbound-poller << 'H1INBOUND'`).
- `NEW_H1_SESSION` is extracted at lines **2692–2696**.
- The DDB write-back block is `if [ -n "$NEW_H1_SESSION" ]; then` at **2699**, with `update-item` + the "Session updated" echo ending at **2708**, closing `fi` at **2709**.
- **Insert the resume-hint block after the echo at line 2708, before the `fi` at 2709** (mint-guarded write-back block).
H1 keys are `REPORT_ID` / `TARGET` (see `--key` at 2704). `#18` makes `HOME=/home/sandbox`
explicit per dispatch block — REINFORCES the design (run-from `cd /workspace`; storage
`/home/sandbox/.claude`); no design change. The internal-only `km-h1 comment` (NO
`--reply-to-researcher`) safety lock is unchanged. Slack bounds remain **1535–2085**.
</line_number_refresh>

<objective>
Implement the HackerOne inbound poller resume-hint block in `pkg/compiler/userdata.go`,
inside the `<< 'H1INBOUND'` heredoc, immediately after the H1 DDB session write-back
(the "Session updated" echo, currently line 2708 — see line_number_refresh above). The block posts a collapsed `<details>` fold via `km-h1 comment` — INTERNAL by
default (NO `--reply-to-researcher`), gated by a post-on-mint `if` and best-effort `|| true`.
Drives Plan 01's H1 assertions from RED to GREEN. Sequenced after Plan 02 because both edit
the same file (userdata.go); the H1 heredoc is a distinct, non-overlapping region.

Purpose: RESUME-HINT-H1 / RESUME-HINT-FORMAT / RESUME-HINT-MINT for the HackerOne surface,
with the internal-only safety property preserved.
Output: ~12 added lines inside the H1INBOUND heredoc; H1 test green; goldens unchanged.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-CONTEXT.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-RESEARCH.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-VALIDATION.md

@pkg/compiler/userdata.go
@pkg/compiler/userdata_h1_byte_identity_test.go

<interfaces>
<!-- Exact injection site + in-scope variables (RESEARCH.md §H1 poller). -->
<!-- The H1INBOUND heredoc uses a single-quoted delimiter ⇒ backticks are LITERAL inside it. -->

Injection point: pkg/compiler/userdata.go ~line 2697, INSIDE the existing
  `if [ -n "$NEW_H1_SESSION" ]` block, AFTER the
  `aws dynamodb update-item --table-name "$H1_THREADS_TABLE" ...` call and the
  `echo "[km-h1-inbound-poller] Session updated ..."` log line.

Variables already in scope at that site:
- SANDBOX_ID      — "${KM_SANDBOX_ID:-}" (line ~2425), e.g. sb-1a2b3c4d (NOT $TARGET — operator needs the box id)
- EFFECTIVE_AGENT — "claude" | "codex" (computed ~2587-2596)
- NEW_H1_SESSION  — jq from output.json (~2681-2686); non-empty here
- H1_SESSION      — prior stored id from DDB get-item (~2527); empty on first turn
- REPORT_ID       — jq .report_id from envelope (~2484)

km-h1 comment call surface (cmd/km-h1/main.go): defaults internal:true. The existing
poller already calls `km-h1 comment --report $REPORT_ID` (no --reply-to-researcher) for the
codex-missing guard — reuse that exact internal form. `--body` accepts an inline literal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert H1 post-on-mint internal resume-hint block</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    In the `<< 'H1INBOUND'` heredoc, locate the H1 session write-back: the
    `aws dynamodb update-item --table-name "$H1_THREADS_TABLE" ...` call followed by
    `echo "[km-h1-inbound-poller] Session updated ...`, inside the existing
    `if [ -n "$NEW_H1_SESSION" ]` guard (~line 2688-2698).

    Immediately AFTER that echo line (still inside the `if [ -n "$NEW_H1_SESSION" ]` guard),
    insert the Phase 106 resume-hint block, matching RESEARCH.md §"H1 Poller — Post-on-Mint Block":

      # Phase 106: post resume-hint fold on session mint (internal by default — the hint
      # never goes external; safety layer preserved). Best-effort (|| true) — a failed
      # hint post MUST NOT block the SQS ack or turn completion.
      if [ -n "$NEW_H1_SESSION" ] && [ "$NEW_H1_SESSION" != "${H1_SESSION:-}" ]; then
        if [ "$EFFECTIVE_AGENT" = "codex" ]; then
          RESUME_CMD="codex exec resume $NEW_H1_SESSION"
        else
          RESUME_CMD="claude --resume $NEW_H1_SESSION"
        fi
        HINT_BODY=$(printf '<details>\n<summary>🔧 Resume this agent session</summary>\n\nOn sandbox `%s`, from the `/workspace` folder use `%s`\n</details>' \
          "$SANDBOX_ID" "$RESUME_CMD")
        /opt/km/bin/km-h1 comment --report "$REPORT_ID" --body "$HINT_BODY" || true
      fi

    Critical constraints:
    - INTERNAL-ONLY SAFETY LOCK: the hint MUST NOT pass `--reply-to-researcher`. km-h1
      defaults internal:true; the bare `km-h1 comment --report ... --body ...` form keeps
      the hint on the internal/team comment, never visible to the external researcher.
    - The `<< 'H1INBOUND'` delimiter is single-quoted ⇒ heredoc text is literal; keep the
      printf format SINGLE-quoted so backticks render literally in the posted Markdown.
    - Summary text `🔧 Resume this agent session` and the literal mint guard
      `"$NEW_H1_SESSION" != "${H1_SESSION:-}"` MUST match Plan 01's H1 assertions exactly.
    - Use SANDBOX_ID (the EC2 box id), not $TARGET (the thread-continuity alias key).
    - Touch ONLY the H1INBOUND heredoc. Do NOT modify the Slack poller (byte-identical) and
      do NOT re-touch the GITHUBINBOUND block from Plan 02.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -run TestUserdataH1EnabledRendersPoller -count=1</automated>
  </verify>
  <done>`TestUserdataH1EnabledRendersPoller` is GREEN (all Phase 106 markers + the 7 pre-existing entries pass); the H1INBOUND heredoc contains the internal-only post-on-mint hint with both agent branches, /workspace, SANDBOX_ID, || true, and the literal mint guard; NO --reply-to-researcher on the hint call.</done>
</task>

<task type="auto">
  <name>Task 2: Prove H1 dormancy + Slack-exclusion + full compiler suite green</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    Verification-only task (no edits unless a failure surfaces). Run the H1 dormancy
    byte-identity guard (`ec2-basic.yaml`, H1-inbound disabled — adding lines inside
    `{{- if .H1InboundEnabled }}` MUST NOT change it), the km-prefix golden, and the full
    pkg/compiler suite. If `TestUserdataH1ByteIdentity` fails, the Task 1 edit leaked
    outside the H1-enabled conditional block — fix the placement; NO golden recapture is
    permitted for this phase.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/ -count=1 -timeout 120s</automated>
  </verify>
  <done>Full `go test ./pkg/compiler/ -count=1 -timeout 120s` is GREEN. `TestUserdataH1ByteIdentity` and `TestUserdataKmPrefixByteIdentity` pass with no golden recapture (Slack-exclusion / dormancy invariant holds).</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/ -run TestUserdataH1EnabledRendersPoller -count=1` — GREEN.
- `go test ./pkg/compiler/ -run TestUserdataH1ByteIdentity -count=1` — GREEN, no recapture (H1 dormancy invariant).
- `go test ./pkg/compiler/ -count=1 -timeout 120s` — full suite green, including the GitHub resume-hint test from Plan 02.
</verification>

<success_criteria>
- H1 poller emits the post-on-mint `<details>` resume-hint via `km-h1 comment` INTERNAL-only.
- No `--reply-to-researcher` on the hint — the safety property is preserved.
- Hint names sandbox id, /workspace, agent-correct resume command; mint guard present; `|| true`.
- H1 dormancy + km-prefix goldens byte-identical (no recapture); Slack poller unchanged.
- Plan 01 H1 assertions GREEN; full pkg/compiler suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-03-SUMMARY.md`
</output>
