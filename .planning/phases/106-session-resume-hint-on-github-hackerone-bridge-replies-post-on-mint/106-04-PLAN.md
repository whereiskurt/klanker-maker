---
phase: 106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint
plan: 04
type: execute
wave: 2
depends_on: [106-02, 106-03]
files_modified:
  - docs/github-bridge.md
  - docs/h1-bridge.md
  - CLAUDE.md
  - skills/init/SKILL.md
autonomous: true
requirements: [RESUME-HINT-DOCS]
must_haves:
  truths:
    - "docs/github-bridge.md has a Phase 106 section describing the post-on-mint resume-hint fold"
    - "docs/h1-bridge.md has a Phase 106 section noting the INTERNAL-only safety property"
    - "Both doc sections state run-from /workspace, post-on-mint semantics, and the make build-lambdas + km init --dry-run=false + recreate deploy line"
    - "CLAUDE.md carries a Phase 106 (complete) phase note consistent with the docs"
    - "skills/init/SKILL.md fast-path/rollout guidance no longer tells operators a userdata-template change deploys via km init --sidecars; it points create-handler-embedded userdata edits (Phase 106 class) at make build-lambdas + full km init --dry-run=false"
  artifacts:
    - path: "docs/github-bridge.md"
      provides: "Phase 106 operator section"
      contains: "## Phase 106"
    - path: "docs/h1-bridge.md"
      provides: "Phase 106 operator section (internal-only note)"
      contains: "## Phase 106"
    - path: "CLAUDE.md"
      provides: "Phase 106 phase note"
      contains: "Phase 106"
    - path: "skills/init/SKILL.md"
      provides: "Corrected create-handler-userdata deploy guidance in the rollout sequence"
      contains: "create-handler"
  key_links:
    - from: "docs/github-bridge.md Phase 106"
      to: "deploy = make build-lambdas + km init --dry-run=false + km destroy && km create"
      via: "operator deploy sequence"
      pattern: "make build-lambdas"
    - from: "docs/h1-bridge.md Phase 106"
      to: "INTERNAL-only (no --reply-to-researcher)"
      via: "safety property note"
      pattern: "internal"
    - from: "skills/init/SKILL.md rollout sequence"
      to: "make build-lambdas + km init --dry-run=false for create-handler-embedded userdata"
      via: "corrected fast-path guidance (NOT --sidecars)"
      pattern: "create-handler"
---

<objective>
Document Phase 106 for operators: add a `## Phase 106` section to `docs/github-bridge.md`
and `docs/h1-bridge.md`, add a Phase 106 phase note to `CLAUDE.md`, and correct the
`skills/init/SKILL.md` fast-path/rollout guidance so the klanker:init plugin gives accurate
deploy advice. All four must be mutually consistent and consistent with the shipped behavior
(Plans 02/03): post-on-mint, collapsed `<details>` fold, run-from `/workspace`, agent-correct
resume command, H1 internal-only, and the exact deploy sequence (`make build-lambdas` +
`km init --dry-run=false` + existing sandboxes need `km destroy && km create`; bridge Lambdas /
IAM / TF unaffected).

The SKILL.md correction closes a real inaccuracy: the rollout-sequence template currently says
`km init --sidecars # if sidecars/* or userdata template changed`, but the poller userdata is
embedded in the create-handler Lambda zip — a userdata edit (Phase 106's class) needs
`make build-lambdas` + full `km init --dry-run=false`, NOT `--sidecars` (and NOT scoped
`--github`/`--h1`). This is the "operator-guide note in klanker:init SKILL.md fast-path section"
item from RESEARCH.md's RESUME-HINT-DOCS definition.

Purpose: RESUME-HINT-DOCS — operator-facing accuracy and deploy-surface consistency.
Output: four edits; `grep '## Phase 106'` + `/workspace` hits in both bridge docs; CLAUDE.md
note; corrected create-handler-userdata guidance in skills/init/SKILL.md.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-CONTEXT.md
@.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-RESEARCH.md

@docs/github-bridge.md
@docs/h1-bridge.md
@CLAUDE.md
@skills/init/SKILL.md

<interfaces>
<!-- Doc structure to mirror. -->
- docs/github-bridge.md ends with the `## Phase 102 — Agent verbs (/claude, /codex)` section (~line 875).
  Append `## Phase 106 — Resume-hint on bridge replies (post-on-mint)` after the last existing section.
- docs/h1-bridge.md uses the same `## Phase NNN` heading convention; append the Phase 106 section.
- CLAUDE.md phase notes are reverse-chronological bold lines at the top (Phase 105 is first, ~line 21).
  Insert a `**Phase 106 (2026-06-11) — ...(complete):**` note ABOVE the Phase 105 note.
- skills/init/SKILL.md "Rollout sequence template" (~line 232-244) currently reads:
    make build                                  # always first
    km init --sidecars                          # if sidecars/* or userdata template changed   <-- INACCURATE for create-handler-embedded userdata
    km init --dry-run=false                     # if Terraform modules or Lambda code changed
  The poller userdata is compiled into the create-handler Lambda zip; a userdata edit needs
  `make build-lambdas` + `km init --dry-run=false`. Correct the comment so "userdata template
  changed" routes to the lambdas/full-init path, not `--sidecars`. (`--sidecars` only rebuilds
  + uploads the sidecar binaries; it does NOT re-upload the create-handler zip — see the
  Fast-path table at ~line 97-100 which already says "Rebuilds a stale Lambda code zip → use
  make build-lambdas + full km init".)
- Deploy line is fixed by RESEARCH.md §Deploy: `make build-lambdas` + `km init --dry-run=false`
  (NOT --sidecars, NOT km init --github/--h1 — those don't re-upload the create-handler zip);
  existing sandboxes need `km destroy && km create`; bridge Lambdas/IAM/TF UNAFFECTED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 106 sections to github-bridge.md and h1-bridge.md</name>
  <files>docs/github-bridge.md, docs/h1-bridge.md</files>
  <action>
    Append a `## Phase 106 — Resume-hint on bridge replies (post-on-mint)` section to the
    END of each doc, matching the surrounding section's prose style. Each section must cover:
    - WHAT: after a bridge agent turn, the POLLER (not the agent) posts ONE extra collapsed
      GitHub-Markdown `<details>` comment carrying the operator resume handle.
    - CONTENT: sandbox id + run-from `/workspace` (NOT /home/sandbox — note session transcripts
      live at /home/sandbox/.claude/projects/-workspace/<id>.jsonl but `--resume` keys off CWD,
      so it must run from /workspace) + agent-correct command (Claude `claude --resume <id>`,
      Codex `codex exec resume <id>`, branched on EFFECTIVE_AGENT). The literal string
      `/workspace` MUST appear in each section (it is the locked run-from decision).
    - FREQUENCY: post-on-mint — fires only when the session id is new or changed (first turn,
      or Gap-E cross-box re-mint). Stable common case ⇒ exactly once per thread.
    - ROBUSTNESS: best-effort `|| true`; never blocks the SQS ack.
    - github-bridge.md ONLY: note the fold is collapsed because PR comments are visible to all
      repo collaborators (the ids are not exploitable without AWS/SSM access).
    - h1-bridge.md ONLY: note the hint is posted via `km-h1 comment` INTERNAL by default (NO
      `--reply-to-researcher`) ⇒ it lands on the internal/team comment, NEVER visible to the
      external researcher.
    - DEPLOY (both): this is a `pkg/compiler/userdata.go` change embedded in the create-handler
      Lambda. Deploy = `make build-lambdas` + `km init --dry-run=false` (NOT `--sidecars`, NOT
      `km init --github`/`--h1` — those refresh env+IAM but do not re-upload the create-handler
      zip). Existing sandboxes need `km destroy && km create`. Bridge Lambdas / IAM / TF UNAFFECTED;
      no SandboxProfile schema change, no new TF resource, no new DDB column.
    - SCOPE: Slack poller deliberately EXCLUDED (operator can ask interactively in chat).
  </action>
  <verify>
    <automated>grep -q '## Phase 106' docs/github-bridge.md && grep -q '## Phase 106' docs/h1-bridge.md && grep -q '/workspace' docs/github-bridge.md && grep -q '/workspace' docs/h1-bridge.md && grep -q 'make build-lambdas' docs/github-bridge.md && grep -iq 'internal' docs/h1-bridge.md && echo OK</automated>
  </verify>
  <done>Both docs have a `## Phase 106` section AND state the locked run-from directory `/workspace`; github-bridge.md mentions the `make build-lambdas` deploy line; h1-bridge.md mentions the internal-only safety property. Both state post-on-mint semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 106 note to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
    Insert a `**Phase 106 (2026-06-11) — Session-resume hint on GitHub + HackerOne bridge
    replies (post-on-mint) (complete):**` bullet note ABOVE the existing Phase 105 note
    (reverse-chronological order, ~line 21). Keep it concise and consistent with the docs:
    - Poller posts ONE collapsed `<details>` resume-hint comment after a bridge turn, carrying
      sandbox id + run-from `/workspace` + agent-correct resume command (Claude `claude --resume`,
      Codex `codex exec resume`, branched on EFFECTIVE_AGENT) + the freshly minted session id.
    - Post-on-mint: fires only when the session id is new or changed (first turn / Gap-E re-mint)
      ⇒ exactly once per stable thread. Best-effort `|| true` (never blocks SQS ack).
    - H1 hint is INTERNAL by default (km-h1, no `--reply-to-researcher`) — never external.
    - GitHub + HackerOne pollers only; Slack poller deliberately EXCLUDED + byte-identical.
    - Pure `pkg/compiler/userdata.go` change; no schema/TF/DDB/binary change.
    - Deploy = `make build-lambdas` + `km init --dry-run=false` (NOT `--sidecars` / `--github` /
      `--h1`); existing sandboxes need `km destroy && km create`; bridge Lambdas/IAM/TF unaffected.
    - See `docs/github-bridge.md` § Phase 106 and `docs/h1-bridge.md` § Phase 106.
  </action>
  <verify>
    <automated>grep -q 'Phase 106' CLAUDE.md && grep -q 'make build-lambdas' CLAUDE.md && echo OK</automated>
  </verify>
  <done>CLAUDE.md carries a Phase 106 (complete) note above the Phase 105 note, consistent with the two bridge docs (post-on-mint, /workspace, H1 internal-only, deploy = make build-lambdas + km init --dry-run=false).</done>
</task>

<task type="auto">
  <name>Task 3: Correct create-handler-userdata deploy guidance in skills/init/SKILL.md</name>
  <files>skills/init/SKILL.md</files>
  <action>
    Fix the inaccurate rollout-sequence guidance in the "Rollout sequence template" section
    (~line 232-244). The current `km init --sidecars # if sidecars/* or userdata template changed`
    line is WRONG for create-handler-embedded userdata: the poller userdata is compiled into the
    create-handler Lambda zip, so a `pkg/compiler/userdata.go` edit deploys via `make build-lambdas`
    + full `km init --dry-run=false`, NOT `--sidecars` (and NOT scoped `--github`/`--h1`).

    Make these edits:
    1. Change the `km init --sidecars` comment so it no longer claims to cover "userdata template"
       changes — scope it to `sidecars/*` only (the sidecar binaries it actually re-uploads).
    2. Add a line/note routing userdata-template (create-handler-embedded poller) changes to
       `make build-lambdas` + `km init --dry-run=false`. For example, extend the template block:
         make build-lambdas                          # if create-handler-embedded userdata (e.g. pollers in pkg/compiler/userdata.go) or Lambda code changed
         km init --dry-run=false                     # uploads the new create-handler zip (and any TF/Lambda changes) via terragrunt
       and clarify in prose that `--sidecars` rebuilds + uploads ONLY the sidecar binaries and does
       NOT re-upload the create-handler zip, so a userdata edit is invisible to running create
       paths until a full `km init --dry-run=false`. Existing sandboxes still need
       `km destroy && km create` to pick up the new poller heredoc.
    3. Keep it consistent with the Fast-path table (~line 97-100: "Rebuilds a stale Lambda code
       zip → No, use make build-lambdas + full km init"), the CLAUDE.md Phase 106 note (Task 2),
       and the github-bridge.md / h1-bridge.md Phase 106 sections (Task 1) — same deploy verbs,
       same NOT-`--sidecars`/NOT-`--github`/`--h1` caveat.

    Cite Phase 106 as the concrete example of this change class (poller resume-hint in
    pkg/compiler/userdata.go) so the guidance is grounded.
  </action>
  <verify>
    <automated>grep -q 'create-handler' skills/init/SKILL.md && grep -q 'make build-lambdas' skills/init/SKILL.md && ! grep -q 'sidecars                          # if sidecars/\* or userdata template changed' skills/init/SKILL.md && echo OK</automated>
  </verify>
  <done>The rollout-sequence template no longer routes a "userdata template" change to `km init --sidecars`; create-handler-embedded userdata edits (Phase 106 class) are routed to `make build-lambdas` + `km init --dry-run=false`, with the `--sidecars`-does-not-re-upload-create-handler caveat stated, consistent with CLAUDE.md and the bridge docs.</done>
</task>

</tasks>

<verification>
- `grep -n '## Phase 106' docs/github-bridge.md docs/h1-bridge.md` — one hit each.
- `grep -n '/workspace' docs/github-bridge.md docs/h1-bridge.md` — hit in each Phase 106 section.
- `grep -n 'Phase 106' CLAUDE.md` — hit above the Phase 105 note.
- `grep -n 'create-handler' skills/init/SKILL.md` — corrected rollout guidance present; the old
  `--sidecars ... userdata template changed` line is gone.
- Reviewer confirms run-from `/workspace`, post-on-mint, H1-internal note, and the
  `make build-lambdas` + `km init --dry-run=false` + recreate deploy line are present and
  consistent across all four files.
</verification>

<success_criteria>
- Phase 106 documented in docs/github-bridge.md, docs/h1-bridge.md, and CLAUDE.md.
- skills/init/SKILL.md rollout guidance corrected: create-handler-embedded userdata edits deploy
  via make build-lambdas + km init --dry-run=false, NOT --sidecars.
- All four are mutually consistent and match shipped behavior (Plans 02/03).
- Both bridge docs assert the locked `/workspace` run-from directory.
- Deploy surface (make build-lambdas + km init --dry-run=false; recreate sandboxes; bridge
  Lambdas/IAM/TF unaffected) stated correctly; H1 internal-only safety property documented.
</success_criteria>

<output>
After completion, create `.planning/phases/106-session-resume-hint-on-github-hackerone-bridge-replies-post-on-mint/106-04-SUMMARY.md`
</output>
