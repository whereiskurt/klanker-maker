---
phase: 63-slack-notify-hook-for-claude-code-permission-and-idle-events
plan: 10
type: execute
wave: 4
depends_on: [63-04, 63-05, 63-06, 63-07, 63-08, 63-09]
files_modified:
  - test/e2e/slack/slack_e2e_test.go
  - test/e2e/slack/helpers.go
  - profiles/slack-test-shared.yaml
  - profiles/slack-test-per-sandbox.yaml
  - docs/slack-notifications.md
  - CLAUDE.md
  - .planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-10-UAT.md
autonomous: false
requirements: [SLCK-09, SLCK-10]
must_haves:
  truths:
    - "test/e2e/slack/ contains an opt-in CI test harness gated on RUN_SLACK_E2E=1 that exercises the full Slack pipeline against a real klankermaker.ai workspace; default `go test ./...` skips this directory entirely."
    - "All five Manual-Only verifications from 63-VALIDATION.md (Slack Connect invite delivery, end-to-end notification delivery, per-sandbox lifecycle + archive, Phase 62 backward compat, bot token rotation) are exercised either as automated E2E tests or as documented UAT scenarios with sign-off in 63-10-UAT.md."
    - "Slack rate-limit propagation (Slack 429 → bridge 503 → km-slack retry) is smoke-tested against the real API with a burst sender."
    - "docs/slack-notifications.md is the operator guide: workspace prerequisites (Pro tier for Slack Connect, bot scopes, app install), `km slack init` walkthrough, profile field reference, troubleshooting matrix, security model summary, rotation procedures."
    - "CLAUDE.md is updated with: new commands `km slack init/test/status`, new env vars (KM_NOTIFY_SLACK_ENABLED, KM_SLACK_CHANNEL_ID, KM_SLACK_BRIDGE_URL, KM_NOTIFY_EMAIL_ENABLED), new SSM paths (`/km/slack/*`), and the `km init --sidecars` requirement after schema-driven changes."
    - "Two reusable test profiles (`profiles/slack-test-shared.yaml` and `profiles/slack-test-per-sandbox.yaml`) are committed for repeatable UAT."
    - "63-10-UAT.md captures the signed-off live UAT outcomes per Phase 62 precedent (62-05-PLAN.md)."
  artifacts:
    - path: "test/e2e/slack/slack_e2e_test.go"
      provides: "Opt-in E2E test harness gated by RUN_SLACK_E2E=1; covers all SLCK-09 manual-only scenarios"
      contains: "TestE2ESlack_SharedMode_NotificationDelivery"
    - path: "test/e2e/slack/helpers.go"
      provides: "Test fixtures: provision sandbox, fire test prompts, poll Slack for messages via bot token"
      contains: "WaitForSlackMessage"
    - path: "profiles/slack-test-shared.yaml"
      provides: "Test profile with notifySlackEnabled+notifyOnIdle in shared mode for repeatable UAT"
      contains: "notifySlackEnabled"
    - path: "profiles/slack-test-per-sandbox.yaml"
      provides: "Test profile with notifySlackPerSandbox+notifyOnPermission for per-sandbox channel UAT"
      contains: "notifySlackPerSandbox"
    - path: "docs/slack-notifications.md"
      provides: "Operator-facing guide: setup, config, troubleshooting, rotation"
      contains: "km slack init"
    - path: "CLAUDE.md"
      provides: "Updated CLI command list, env var conventions, SSM path conventions for Slack"
      contains: "km slack"
    - path: ".planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-10-UAT.md"
      provides: "Signed-off UAT outcome table per Phase 62 precedent"
      contains: "UAT Outcomes"
  key_links:
    - from: "test/e2e/slack/slack_e2e_test.go"
      to: "All Wave 3 plans (07/08/09) — exercises km slack init/test/status, km create with Slack profiles, km destroy archive"
      via: "real CLI invocations against a live AWS account + Slack workspace"
      pattern: "RUN_SLACK_E2E"
    - from: "docs/slack-notifications.md + CLAUDE.md"
      to: "Operator workflow"
      via: "Single source of truth for new commands, env vars, and SSM paths"
      pattern: "km slack init"
---

<objective>
Cap Phase 63 with end-to-end live verification + operator-facing documentation.

The E2E harness lives at `test/e2e/slack/` and is gated on `RUN_SLACK_E2E=1` so it does NOT run during default `go test ./...` (CI safe). It exercises the entire pipeline — `km slack init` → `km create` (both shared and per-sandbox modes) → `km agent run` to trigger Notification + Stop events → poll Slack for delivered messages → `km destroy` → confirm archive — against a real klankermaker.ai workspace.

Documentation has two surfaces: `docs/slack-notifications.md` (operator-facing — setup, config, troubleshooting) and `CLAUDE.md` updates (CLI quick reference, env var/SSM conventions). Per CLAUDE.md memory `feedback_rebuild_km`, the doc explicitly notes `make build` and `km init --sidecars` as gates for new sandboxes to pick up the km-slack binary.

Per Phase 62 precedent (62-05-PLAN.md), live UAT outcomes are captured in `63-10-UAT.md` as a signed-off table — even when individual scenarios are automated, the human-in-the-loop sign-off matters for Slack Connect invite acceptance and Pro-tier confirmation.

This plan is `autonomous: false` because Slack Connect invite acceptance is genuinely manual (operator clicks the email link from a different workspace, accepts the invite, confirms channel visibility) and bot token rotation requires the operator to revoke + reissue from the Slack App admin UI. Both gates are checkpoint:human-action.

Output:
- `test/e2e/slack/slack_e2e_test.go` + `test/e2e/slack/helpers.go` — opt-in CI test harness.
- `profiles/slack-test-shared.yaml` + `profiles/slack-test-per-sandbox.yaml` — reusable test profiles.
- `docs/slack-notifications.md` — operator guide.
- `CLAUDE.md` — updated CLI / env / SSM reference.
- `63-10-UAT.md` — UAT outcome table.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-CONTEXT.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-RESEARCH.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-VALIDATION.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-04-PLAN.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-05-PLAN.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-07-PLAN.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-08-PLAN.md
@.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-09-PLAN.md
@.planning/phases/62-claude-code-operator-notify-hook-for-permission-and-idle-events/62-05-PLAN.md
@CLAUDE.md
@profiles/open-dev.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build test/e2e/slack/ harness with opt-in gate, helpers, automated scenarios + reusable test profiles</name>
  <files>
    test/e2e/slack/slack_e2e_test.go
    test/e2e/slack/helpers.go
    profiles/slack-test-shared.yaml
    profiles/slack-test-per-sandbox.yaml
  </files>
  <action>
**1. Read existing E2E test patterns** (if any) to match conventions:

```bash
ls test/ 2>/dev/null || ls tests/ 2>/dev/null
grep -rn "RUN_E2E\|os.Getenv.*Skip\|t.Skip.*RUN_" --include="*.go" | head -10
grep -rn "//go:build e2e\|//go:build integration" --include="*.go" | head -5
```

If the project has an existing opt-in pattern (env var or build tag), use it. Otherwise use `RUN_SLACK_E2E=1`. Document the choice in 63-10-SUMMARY.md.

**2. Create `profiles/slack-test-shared.yaml`** based on `profiles/open-dev.yaml`:

```yaml
apiVersion: klankermaker.ai/v1
kind: SandboxProfile
metadata:
  name: slack-test-shared
spec:
  extends: open-dev
  lifecycle:
    ttl: 30m
  cli:
    notifyOnIdle: true
    notifyCooldownSeconds: 0
    notifyEmailEnabled: false
    notifySlackEnabled: true
```

Confirm exact apiVersion + extends syntax by reading `profiles/open-dev.yaml` first; mirror it.

**3. Create `profiles/slack-test-per-sandbox.yaml`:**

```yaml
apiVersion: klankermaker.ai/v1
kind: SandboxProfile
metadata:
  name: slack-test-per-sandbox
spec:
  extends: open-dev
  lifecycle:
    ttl: 30m
  cli:
    notifyOnPermission: true
    notifyOnIdle: true
    notifyCooldownSeconds: 0
    notifyEmailEnabled: false
    notifySlackEnabled: true
    notifySlackPerSandbox: true
    slackArchiveOnDestroy: true
```

**4. Create `test/e2e/slack/helpers.go`:**

- `loadE2EConfig(t)` — read `KM_SLACK_E2E_BOT_TOKEN`, `KM_SLACK_E2E_INVITE_EMAIL`, `KM_SLACK_E2E_REGION`, optional `KM_SLACK_E2E_SHARED_CHANNEL`. `t.Skip` with clear message if any required vars missing.
- `runKM(t, args ...) (stdout string, exitCode int)` — exec `./km <args...>` with combined stdout/stderr.
- `WaitForSlackMessage(t, ctx, cfg, channelID, bodySubstr, timeout)` — poll Slack `conversations.history` via thin HTTP using bot token, return ts when match found.
- `AssertSandboxArchivedInSlack(t, ctx, cfg, channelID) bool` — call `conversations.info`, return `is_archived`.
- `cleanupSandbox(t, sandboxID)` — `km destroy --remote --yes`, log on failure but never fail the test.
- `extractSandboxID(kmOutput)` — regex `sb-[a-f0-9]{8}` from `km create` output.
- `extractSlackChannelID(t, sandboxID)` — call `km status <id>`, regex `C[A-Z0-9]+`.
- `getSharedChannelID(t)` — call `km slack status`, regex shared-channel-id row.

If pkg/slack.Client doesn't expose `conversations.history` or `conversations.info`-with-`is_archived`, do thin direct HTTP in helpers.go (don't bloat pkg/slack for one-off test reads).

**5. Create `test/e2e/slack/slack_e2e_test.go`** with:

```go
package slack_e2e

func TestMain(m *testing.M) {
    if os.Getenv("RUN_SLACK_E2E") != "1" {
        fmt.Println("RUN_SLACK_E2E not set; skipping live Slack E2E tests")
        os.Exit(0)
    }
    os.Exit(m.Run())
}
```

Required test functions (one per VALIDATION.md Manual-Only scenario):

- `TestE2ESlack_InitFlow_StoresAllSSMParams` — runs `km slack init --bot-token --invite-email --shared-channel km-e2e-notifications`; asserts `km slack status` output contains all five `/km/slack/*` paths. Covers SLCK-CONNECT (partial — invite delivery confirmed in UAT, not automation).

- `TestE2ESlack_SharedMode_NotificationDelivery` — `km create profiles/slack-test-shared.yaml`, `km agent run --prompt "What's 2+2?" --wait` (Stop event), poll Slack for `[<sb-id>] idle` message in shared channel. defer cleanup. Covers SLCK-E2E-NOTIFY (Stop path).

- `TestE2ESlack_SharedMode_PermissionEvent` — variant prompt that triggers a tool-permission Notification; assert `[<sb-id>] needs permission` message arrives. (Note from 62-VERIFICATION lessons: km agent run uses `--dangerously-skip-permissions` by default — may need a different invocation, e.g., `km shell` without that flag, or fire the hook manually via SSM SendCommand. Document the workaround.)

- `TestE2ESlack_PerSandboxMode_LifecycleAndArchive` — `km create profiles/slack-test-per-sandbox.yaml --alias e2e-demo`, fire idle event, confirm message in `#sb-e2e-demo`, `km destroy --remote --yes`, confirm channel archived via conversations.info `is_archived=true`. Covers SLCK-PER-SANDBOX.

- `TestE2ESlack_Phase62Compat_EmailWhenSlackOff` — uses a Phase 62 profile (no Slack fields). Provision, fire idle, confirm operator inbox receives email via `km email read --json`, confirm shared Slack channel receives nothing for that sandbox-id. Covers SLCK-PHASE62-COMPAT (token rotation portion is UAT-only).

- `TestE2ESlack_RateLimit_BurstBackoff` — gated on additional `KM_SLACK_E2E_RATELIMIT=1` env var (avoids workspace spam in default E2E runs). Sends N rapid posts via `km-slack` directly until Slack returns 429; asserts the retry loop honors Retry-After and eventually succeeds. Covers SLCK-RATE-LIMIT.

- Bot token rotation (SLCK-TOKEN-ROTATION) is too time-bound to script (requires a >15min wait for Lambda cache TTL): document as UAT-only in 63-10-UAT.md instead.

**6. Validate test profiles** with the freshly-built binary:

```bash
make build
./km validate profiles/slack-test-shared.yaml
./km validate profiles/slack-test-per-sandbox.yaml
```

**7. Confirm the gate works** — default `go test ./...` MUST NOT run E2E:

```bash
go test ./test/e2e/slack/... -count=1 2>&1 | grep -q "skipping live Slack E2E tests"
```

(That string proves TestMain returned before any test ran.)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; make build &amp;&amp; ./km validate profiles/slack-test-shared.yaml &amp;&amp; ./km validate profiles/slack-test-per-sandbox.yaml &amp;&amp; (go test ./test/e2e/slack/... -count=1 2>&amp;1 | grep -q "skipping live Slack E2E tests")</automated>
  </verify>
  <done>
    - test/e2e/slack/ has TestMain gate, helpers, and ≥5 automated scenarios + ≥1 documented manual-only scenario (token rotation).
    - Default `go test ./...` does not run the E2E suite.
    - Two profiles validate against the live `km validate`.
    - Helpers cover Slack history poll, archive check, and km CLI invocation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write docs/slack-notifications.md operator guide; update CLAUDE.md with new commands, env vars, SSM paths</name>
  <files>
    docs/slack-notifications.md
    CLAUDE.md
  </files>
  <action>
**1. Read existing docs to match style** (especially `docs/multi-agent-email.md` per CLAUDE.md — closest comparable doc):

```bash
ls docs/
head -80 docs/multi-agent-email.md 2>/dev/null
```

**2. Create `docs/slack-notifications.md`** — operator guide. Sections:

- **Overview** — Phase 63 extends Phase 62 email with parallel Slack delivery; bot token never leaves AWS; sandboxes call signed bridge Lambda.
- **Channel modes table** — shared / per-sandbox / override (when each applies, what the channel name is).
- **Prerequisites** — Pro Slack workspace; required bot scopes (`chat:write`, `channels:manage`, `conversations.connect:write`, `groups:write`); custom Slack App installed; bot token captured; operator's separate Slack workspace able to receive Connect invites.
- **One-time setup** — `make build` → `km init --sidecars` → `km init` → `km slack init` (interactive or with flags). Followed by accepting the Slack Connect invite in operator's separate workspace. Verify via `km slack status` and `km slack test`.
- **Profile fields table** — five new spec.cli fields, types, defaults, purposes (mirror CONTEXT.md).
- **Validation rules** — four rules from CONTEXT.md (mutual exclusion error, two no-op warnings, regex error, neither-channel warning); note `km validate` exits 0 on warnings (WARN: prefix), exits 1 on errors.
- **Example profiles** — three snippets: shared mode, per-sandbox, override.
- **Architecture** — ASCII diagram: sandbox → /opt/km/bin/km-slack → bridge Lambda Function URL → DynamoDB verify chain → Slack Web API.
- **SSM parameters reference** — `/km/slack/{bot-token,workspace,invite-email,shared-channel-id,bridge-url,last-test-timestamp}` table.
- **Sandbox env vars reference** — KM_NOTIFY_EMAIL_ENABLED, KM_NOTIFY_SLACK_ENABLED, KM_SLACK_CHANNEL_ID, KM_SLACK_BRIDGE_URL.
- **Bot token rotation** — `km slack init --force --bot-token <new>`; Lambda picks up on cold start or 15-min cache TTL.
- **Troubleshooting matrix** — failures + causes + fixes:
  - `not_allowed_token_type` from `km slack init` → free-tier workspace → upgrade to Pro.
  - `name_taken` from `km create` per-sandbox → use `--alias` or `notifySlackChannelOverride`.
  - "bot is not a member" override mode → invite km-bot to the channel.
  - Hook fires, no Slack message → bridge not deployed → run `km init` then `km slack init`.
  - Hook fires on existing sandbox, no Slack → existing sandboxes don't get km-slack retroactively → destroy + recreate.
  - `km doctor` reports stale Slack channel → manually archive in Slack UI or remove from DynamoDB.
  - `km slack test` returns 401 → bot token expired → rotate.
- **Security model summary** — Ed25519 signing (sandbox + operator keys); replay protection (timestamp + nonce); channel-mismatch authorization; action authorization (sandbox can only `post` to own channel; `archive`/`test` operator-only); bot token KMS-encrypted, only bridge Lambda + operator can read.
- **See also** — `docs/multi-agent-email.md`, the design spec at `docs/superpowers/specs/2026-04-29-slack-notify-hook-design.md`, the Phase 62 spec, CLAUDE.md.

The full prose was drafted in this plan's earlier draft — see the previous file revision for the complete markdown body. The execution can copy that draft verbatim once the structure-and-section list above is verified against the actual codebase paths.

**3. Update `CLAUDE.md`**:

3a. **CLI command list** — append new commands near the existing `km email send / km otel / km init` block:

```markdown
- `km slack init` — bootstrap Slack integration: validate bot token, write SSM params, create shared channel, send Slack Connect invite, deploy bridge Lambda (`--bot-token`, `--invite-email`, `--shared-channel`, `--force`)
- `km slack test` — end-to-end smoke test through the bridge using operator signing key
- `km slack status` — print SSM-backed Slack config (workspace, channel, bridge URL, last test)
```

3b. **Add a new top-level section** after the "Email" section (mirror that section's structure):

```markdown
## Slack Notifications

Phase 63 extends Phase 62's operator-notify hook with parallel Slack delivery. Sandboxes call a `km-slack-bridge` Lambda with Ed25519-signed payloads (same trust model as `km-send`); operators are invited to channels via Slack Connect.

### One-time setup

\`\`\`bash
make build               # Always rebuild km after edits (memory: feedback_rebuild_km)
km init --sidecars       # Upload km-slack binary to S3
km init                  # Deploy bridge Lambda + nonce table
km slack init            # Interactive bootstrap (or pass --bot-token + --invite-email)
\`\`\`

### Profile fields (\`spec.cli\`)

| Field | Type | Default | Purpose |
|---|---|---|---|
| \`notifyEmailEnabled\` | bool* | true | Phase 62 compat; set false to skip email when Slack is on |
| \`notifySlackEnabled\` | bool* | false | Enable Slack delivery |
| \`notifySlackPerSandbox\` | bool | false | Create #sb-{id} channel; archive at destroy |
| \`notifySlackChannelOverride\` | string | empty | Pin to channel ID (^C[A-Z0-9]+$) |
| \`slackArchiveOnDestroy\` | bool* | true | Per-sandbox only; false preserves channel |

### Sandbox env vars

| Variable | Source |
|---|---|
| \`KM_NOTIFY_EMAIL_ENABLED\` | profile (omit = default 1) |
| \`KM_NOTIFY_SLACK_ENABLED\` | profile (omit = default 0) |
| \`KM_SLACK_CHANNEL_ID\` | runtime, injected by km create |
| \`KM_SLACK_BRIDGE_URL\` | runtime, injected by km create |

### SSM parameters

\`/km/slack/bot-token\` (SecureString), \`/km/slack/workspace\`, \`/km/slack/invite-email\`, \`/km/slack/shared-channel-id\`, \`/km/slack/bridge-url\`.

### Important workflow notes

- **\`km init --sidecars\` is required** after this phase ships so management Lambdas pick up the schema additions and the new \`km-slack\` sidecar binary lands in S3.
- Existing sandboxes do NOT get km-slack retroactively — \`km destroy\` + \`km create\` to provision with the binary.
- Slack Connect (\`conversations.inviteShared\`) requires a Pro Slack workspace.
- Bot token rotation: \`km slack init --force --bot-token <new>\`. Lambda picks up new token on cold start or after 15-min cache TTL.

See \`docs/slack-notifications.md\` for the full operator guide.
```

3c. **Confirm the existing CLI quick-reference** (near the top of CLAUDE.md) gets the three `km slack` commands inserted in alphabetical / section order.

3d. **Build and validate:**

```bash
cd /Users/khundeck/working/klankrmkr
make build
./km slack --help
./km slack init --help
```
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; make build &amp;&amp; grep -q "km slack init" CLAUDE.md &amp;&amp; grep -q "/km/slack/bot-token" CLAUDE.md &amp;&amp; grep -q "KM_NOTIFY_SLACK_ENABLED" CLAUDE.md &amp;&amp; [ -f docs/slack-notifications.md ] &amp;&amp; grep -q "km slack init" docs/slack-notifications.md &amp;&amp; grep -q "Pro Slack workspace" docs/slack-notifications.md</automated>
  </verify>
  <done>
    - docs/slack-notifications.md exists with all sections from the outline (Overview through See Also).
    - Troubleshooting matrix covers ≥7 known failure modes.
    - Security model section mentions DynamoDB-backed public keys (RESEARCH.md correction #1) and channel-mismatch authorization explicitly.
    - CLAUDE.md has three new km slack commands in the CLI list.
    - CLAUDE.md has a "Slack Notifications" section after Email with the same structural pattern.
    - All four new env vars and five SSM paths are documented.
    - `make build` succeeds; `./km slack --help` shows the subcommand tree.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — operator runs the full Slack pipeline end-to-end against the real klankermaker.ai workspace and signs off in 63-10-UAT.md</name>
  <what-built>
    Full Phase 63 implementation: schema, compiler, km-slack binary, bridge Lambda, km slack init/test/status, km create channel provisioning, km destroy archive flow, km doctor checks, E2E harness, operator docs.
  </what-built>
  <how-to-verify>
**Pre-flight (operator's local machine):**

```bash
cd /Users/khundeck/working/klankrmkr
make build
./km --version
./km init --sidecars      # Ship km-slack binary to S3
./km init                 # Deploy bridge Lambda + nonce DynamoDB table
```

**Provision Slack workspace prerequisites (out-of-band, one time):**

1. Pro Slack workspace at klankermaker.ai (or test workspace).
2. Custom Slack App with bot scopes: `chat:write`, `channels:manage`, `conversations.connect:write`, `groups:write`.
3. Install app to workspace; capture bot token (`xoxb-...`).

**Scenario 1 — `km slack init` happy path + Slack Connect invite acceptance:**

```bash
./km slack init \
  --bot-token "$SLACK_BOT_TOKEN" \
  --invite-email "$YOUR_OPERATOR_EMAIL" \
  --shared-channel "km-notifications"
./km slack status
```

Expected: status shows all five `/km/slack/*` SSM paths populated. Operator's email receives a Slack Connect invite. Operator clicks accept in their separate Slack workspace; `#km-notifications` becomes visible there. Record outcome in 63-10-UAT.md.

**Scenario 2 — `km slack test` end-to-end:**

```bash
./km slack test
```

Expected: command exits 0; "If you see this, the bridge is wired." appears in `#km-notifications`. Record ts and outcome.

**Scenario 3 — Shared-mode notification delivery:**

```bash
./km create profiles/slack-test-shared.yaml
# Note the new sandbox ID
./km agent run sb-XXXXXXXX --prompt "What's 2+2?" --wait
```

Expected: idle hook fires; `#km-notifications` receives `*[sb-XXXXXXXX] idle*` message with body containing the assistant's last text. Record outcome. `./km destroy sb-XXXXXXXX --remote --yes`.

**Scenario 4 — Per-sandbox channel lifecycle:**

```bash
./km create profiles/slack-test-per-sandbox.yaml --alias e2e-demo
# Note the new sandbox ID
```

Expected: `#sb-e2e-demo` channel appears in operator's Slack via Slack Connect (or accept new invite if Slack Connect requires per-channel acceptance). Channel ID stored in DynamoDB sandbox metadata.

```bash
./km agent run sb-XXXXXXXX --prompt "What's 2+2?" --wait
```

Expected: idle message in `#sb-e2e-demo`.

```bash
./km destroy sb-XXXXXXXX --remote --yes
```

Expected: final "destroyed at <ts>" message posted, then channel archived. Verify in Slack UI that the channel is archived. Record outcome.

**Scenario 5 — Per-sandbox without archive:**

Repeat Scenario 4 with `slackArchiveOnDestroy: false` in the profile. Expected: final post says "Channel preserved per profile"; channel persists post-destroy.

**Scenario 6 — Phase 62 backward compat:**

```bash
./km create profiles/notify-test.yaml   # Phase 62 profile, no Slack
./km agent run sb-XXXXXXXX --prompt "What's 2+2?" --wait
```

Expected: operator inbox receives email per Phase 62; no Slack message for that sandbox-id arrives in any channel.

**Scenario 7 — Bot token rotation:**

1. In Slack App admin UI, revoke current bot token.
2. Generate new token.
3. Within 15 minutes, send `./km slack test` (against still-warm Lambda) — expect failure (cached invalid token).
4. Wait >15 minutes (or force a cold start by deploying a no-op Lambda env var change).
5. `./km slack init --force --bot-token "$NEW_TOKEN"` (updates SSM).
6. `./km slack test` — expect success.

Record outcome.

**Scenario 8 — `km doctor`:**

```bash
./km doctor
```

Expected: `slack-token` check returns OK (or WARN if known issue). `slack-stale-channels` returns OK (no stale).

After all scenarios, write outcomes into `63-10-UAT.md` per Phase 62 precedent.
  </how-to-verify>
  <action>
Operator follows the eight scenarios above in `<how-to-verify>` against a live klankermaker.ai Slack workspace and a live AWS account, then writes the outcome of each scenario (PASS / FAIL / N/A with notes) into a new `63-10-UAT.md` UAT outcome table. The 63-10-UAT.md file follows the Phase 62 `62-05-SUMMARY.md` precedent — header + scenario table + sign-off line with operator name + date.

Why this is a checkpoint and not automated:
- Slack Connect invite acceptance (Scenarios 1, 4) requires a human click in the operator's separate Slack workspace.
- Bot token rotation (Scenario 7) requires Slack App admin UI access + a >15-minute wait for Lambda cache TTL.
- Pro-tier workspace confirmation (Scenarios 1, 4) is a billing/tier verification a stub cannot reproduce.

Automation that does run as part of this plan covers everything else (Tasks 1 + 2): E2E test harness gated by `RUN_SLACK_E2E=1` exercises Scenarios 2, 3, 4, 6 against any environment with the four `KM_SLACK_E2E_*` env vars set; `grep`-based docs/CLAUDE.md verification covers Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-10-UAT.md &amp;&amp; grep -q "UAT Outcomes" .planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-10-UAT.md</automated>
  </verify>
  <done>
    - 63-10-UAT.md exists with an outcome row for each of the eight scenarios.
    - Sign-off line records operator name + UAT date.
    - Any FAIL outcomes have linked follow-up issues or gap-closure plan references.
  </done>
  <resume-signal>Type "approved" once 63-10-UAT.md outcome table is filled in and signed off, or describe issues encountered.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./test/e2e/slack/... -count=1` exits 0 with the gate message (no actual tests run without RUN_SLACK_E2E=1).
- `make build` is green.
- `./km validate profiles/slack-test-shared.yaml` and `./km validate profiles/slack-test-per-sandbox.yaml` pass.
- `./km slack --help` shows `init`, `test`, `status` subcommands.
- `grep -q "km slack init" docs/slack-notifications.md` and `grep -q "km slack init" CLAUDE.md` succeed.
- `grep -q "/km/slack/bot-token" CLAUDE.md` succeeds.
- `grep -q "KM_NOTIFY_SLACK_ENABLED" CLAUDE.md` succeeds.
- `63-10-UAT.md` exists with at minimum the section headers (operator fills in outcomes during the live UAT).
</verification>

<success_criteria>
- E2E test harness covers all five Manual-Only verifications from 63-VALIDATION.md as automated scenarios where possible (≥5 scenarios) + documented UAT path for the unscriptable two (token rotation, Connect invite acceptance).
- Default `go test ./...` does NOT run E2E tests (gate verified).
- Two reusable test profiles committed and validated.
- Operator-facing documentation comprehensively covers setup, config, troubleshooting, security model, rotation.
- CLAUDE.md is the single source of truth for new CLI commands, env vars, SSM paths.
- Live UAT signed off in 63-10-UAT.md per Phase 62 precedent.
- Phase 63 is complete and verifiable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/63-slack-notify-hook-for-claude-code-permission-and-idle-events/63-10-SUMMARY.md` documenting:
- New: test/e2e/slack/ harness with TestMain gate.
- New: profiles/slack-test-shared.yaml + profiles/slack-test-per-sandbox.yaml.
- New: docs/slack-notifications.md operator guide.
- Modified: CLAUDE.md (new CLI commands, env vars, SSM paths, "Slack Notifications" section).
- Created during UAT: 63-10-UAT.md outcome table.
- Decision: Bot token rotation and Slack Connect invite acceptance are UAT-only (cannot be reasonably scripted in CI without long waits or human-in-the-loop email reading).
- Decision: Rate-limit smoke test gated behind additional KM_SLACK_E2E_RATELIMIT=1 to avoid workspace spam in default E2E runs.
- Phase 63 complete: requirements SLCK-01..SLCK-10 closed across 10 plans; ready for `/gsd:verify-work` and milestone close.
</output>
