---
phase: 63
plan: 10
type: uat
created: 2026-04-30
status: pending
---

# Phase 63 Plan 10 — UAT Outcomes

Per Phase 62 precedent (62-05-PLAN.md), live UAT outcomes are captured here as a signed-off table.

## Prerequisites

Before running UAT scenarios, confirm:

- [ ] `make build` succeeds and `./km --version` shows current build
- [ ] `km init --sidecars` has been run (km-slack binary in S3)
- [ ] `km init` has been run (bridge Lambda + nonce table deployed)
- [ ] `km slack init` completed (all five `/km/slack/*` SSM params populated)
- [ ] `km slack status` shows no `(unset)` values
- [ ] `km slack test` exits 0 (smoke test message visible in `#km-notifications`)
- [ ] Operator's separate Slack workspace has accepted the Slack Connect invite

---

## UAT Outcomes

| Scenario | Description | Expected | Actual | Status | Notes |
|----------|-------------|----------|--------|--------|-------|
| 1 | `km slack init` happy path + Slack Connect invite delivery | `km slack status` shows all 5 SSM paths; operator email receives Slack Connect invite | — | PENDING | Invite acceptance is manual (click email link in separate workspace) |
| 2 | `km slack test` end-to-end smoke test | Exit 0; "If you see this, the bridge is wired." visible in `#km-notifications` | — | PENDING | |
| 3 | Shared-mode notification delivery | `km agent run --wait` triggers idle hook; `#km-notifications` receives `*[sb-XXX] idle*` message | — | PENDING | |
| 4 | Per-sandbox channel lifecycle + archive | `km create profiles/slack-test-per-sandbox.yaml --alias e2e-demo`; idle fires; message in `#sb-e2e-demo`; `km destroy` archives channel | — | PENDING | Slack Connect per-channel invite may require separate acceptance |
| 5 | Per-sandbox without archive | Repeat Scenario 4 with `slackArchiveOnDestroy: false`; channel persists post-destroy | — | PENDING | |
| 6 | Phase 62 backward compat | `km create profiles/notify-test.yaml`; idle fires; operator email received; no Slack message for that sandbox-id | — | PENDING | |
| 7 | Bot token rotation | Revoke token in Slack App admin UI; warm Lambda returns failure; wait >15min (or force cold start); `km slack init --force --bot-token <new>`; `km slack test` succeeds | — | PENDING | Requires >15min wait for Lambda cache TTL |
| 8 | `km doctor` Slack health checks | `km doctor` returns OK for `slack-token` and `slack-stale-channels` | — | PENDING | |

---

## Sign-off

**Operator:** _________________

**UAT date:** _________________

**Outcome summary:** _________________

---

*Fill in this table after running the eight scenarios in Task 3 of 63-10-PLAN.md.*
*Change PENDING rows to PASS / FAIL / N/A. Add notes for any FAIL outcomes (linked issue or gap-closure plan).*
