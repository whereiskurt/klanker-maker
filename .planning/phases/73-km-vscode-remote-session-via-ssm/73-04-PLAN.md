---
phase: 73-km-vscode-remote-session-via-ssm
plan: 04
type: execute
wave: 3
depends_on: ["73-03"]
files_modified:
  - docs/vscode.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "An operator with no prior knowledge of Phase 73 can install the `code` binary, set `vscodeEnabled: true` (or omit it), provision a sandbox, and connect to VS Code Web following the steps in `docs/vscode.md`."
    - "`docs/vscode.md` documents the known limitations: re-running `start` without `stop` (port conflict), local port collision (`--local-port`), the `code` binary not being installed (`journalctl` excerpt + hint), `vscodeEnabled: false` clean error."
    - "CLAUDE.md's CLI command list includes `km vscode start | stop | status` so the file functions as a consolidated entry-point reference."
  artifacts:
    - path: "docs/vscode.md"
      provides: "Operator setup guide: prereqs, profile fields, sample initCommands, troubleshooting"
      min_lines: 80
    - path: "CLAUDE.md"
      provides: "km vscode entry in the CLI section"
      contains: "km vscode start"
  key_links:
    - from: "docs/vscode.md (sample initCommands)"
      to: "Microsoft VS Code CLI download (cli-alpine-x64 for Amazon Linux 2023)"
      via: "curl + tar + chmod"
      pattern: "cli-alpine-x64"
    - from: "docs/vscode.md (troubleshooting)"
      to: "Journalctl excerpt produced by `km vscode start` failure path"
      via: "Operator workflow: read error → consult docs"
      pattern: "code binary not installed"
---

<objective>
Operator-facing documentation. Two files:

1. `docs/vscode.md` — new file. Setup guide, sample profile snippet,
   troubleshooting matrix.
2. `CLAUDE.md` — append `km vscode start | stop | status` to the CLI
   command list near the existing entries.

Purpose:
- Phase 73 ships behind a clean operator UX. Without docs, the
  "VS Code Web ready for sb-x" output drops the operator into a
  browser they don't yet know how to drive — and the `code` binary
  install step is non-obvious.
- CLAUDE.md is the consolidated entry-point reference; missing the
  vscode commands there breaks Claude's ability to guide operators
  through the new workflow.

Output:
- New `docs/vscode.md` (~120 lines covering prereqs, install,
  start/stop/status, flags, troubleshooting, security model summary).
- One-paragraph addition + 3 lines in CLAUDE.md's CLI command list.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-CONTEXT.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-RESEARCH.md
@docs/superpowers/specs/2026-05-06-km-vscode-design.md
@docs/slack-notifications.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/vscode.md operator guide</name>
  <files>docs/vscode.md</files>
  <action>
    Create `docs/vscode.md` (new file). Use `docs/slack-notifications.md`
    as the structural reference (existing operator-facing doc).

    Required sections (use `##` headings):

    1. **Overview** — One paragraph: "Phase 73 adds `km vscode start | stop | status` for companion-style VS Code Web sessions on a running sandbox. The session is reachable through an SSM port-forward; nothing is exposed publicly."
    2. **Prerequisites**
       - `km` built from a Phase-73-or-later commit (`make build`).
       - `km init --sidecars` run after the schema change (per `memory/project_schema_change_requires_km_init.md`).
       - The `code` CLI installed in the sandbox profile's `initCommands`.
    3. **Sample profile** — Working YAML snippet with `spec.cli.vscodeEnabled: true` (note: default; can omit) and `spec.execution.initCommands` for the Microsoft `code` CLI download. Use `cli-alpine-x64` (works on Amazon Linux 2023 default AMI per RESEARCH.md). Recommend `latest` with a clearly-flagged note on how to pin via commit hash.
    4. **Usage** — Three subsections, one per subcommand:
       - `km vscode start <sandbox-id>` — what it does (rotate token, start unit, port-forward), expected output (sample connection block), `--local-port` and `--no-forward` flags.
       - `km vscode stop <sandbox-id>` — what it does, when to use.
       - `km vscode status <sandbox-id>` — what it does, expected output.
    5. **Security model** — Short bulleted list:
       - SSM tunnel is the security boundary (IAM-authenticated, encrypted).
       - Token in URL is defense-in-depth against other processes on the operator's laptop.
       - Server binds 127.0.0.1 only — nothing on the EC2 instance can reach it; no SG changes needed.
       - Token rotates on every `km vscode start`.
    6. **Troubleshooting** — Table or section per known issue:
       - **`VS Code not enabled in this sandbox's profile`** → set `spec.cli.vscodeEnabled: true` in the profile and reprovision (`km destroy && km create`).
       - **`code: command not found` in journalctl** → `code` binary is not installed; add the `initCommands` snippet from §3.
       - **`bind: address already in use` on port 8443** → either stop the previous tunnel or pass `--local-port 9443`.
       - **Re-running `start` while a tunnel is open** → the second `restart` rotates the token, breaking the old session. Run `km vscode stop` first, OR use `km vscode start --no-forward` to print the new URL/token without starting a second tunnel.
       - **Browser shows "Invalid token" 401** → may indicate an old `code` CLI version where `--connection-token-file` had a bug. Phase 73 mitigates by passing `--connection-token "$TOKEN"` directly, so this should be rare. Workaround: rerun `km vscode start` to force a fresh token.
    7. **Limitations (deferred to future phases)** — bulleted list mirroring 73-CONTEXT.md `<deferred>`:
       - No `--duration` flag / auto-stop on idle (compose with `km at` if needed).
       - No `km doctor` checks for VS Code yet.
       - One concurrent session per sandbox (run `code serve-web` directly via `km shell` for power use cases).
       - No automatic session persistence across `km destroy` + `km create` (use `additionalVolume` if needed).
       - Documented for Microsoft `code` only — `code-server` and `openvscode-server` work but require tweaking the launch wrapper.
    8. **Internals** (optional, end of doc) — pointer to the design spec at `docs/superpowers/specs/2026-05-06-km-vscode-design.md` and a one-line summary of the on-sandbox file layout (unit, wrapper, token dir).

    Tone: matches `docs/slack-notifications.md` — operator-friendly,
    code blocks where useful, no hand-waving. Do NOT use emoji.

    File length target: 100-150 lines.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f docs/vscode.md && wc -l docs/vscode.md | awk '$1 >= 80 { exit 0 } { exit 1 }' && grep -q "km vscode start" docs/vscode.md && grep -q "vscodeEnabled" docs/vscode.md && grep -q "cli-alpine-x64" docs/vscode.md && grep -q "Troubleshooting\|troubleshooting" docs/vscode.md</automated>
  </verify>
  <done>
    `docs/vscode.md` exists with at least 80 lines. Contains
    `km vscode start`, `vscodeEnabled`, `cli-alpine-x64`, and a
    troubleshooting section. Sample profile snippet is valid YAML
    that `km validate` would accept (verify mentally; no automated
    YAML check needed because we don't ship the snippet as a
    standalone file).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md CLI section</name>
  <files>CLAUDE.md</files>
  <action>
    Edit `CLAUDE.md`. Find the `## CLI` section (currently lists every
    `km` subcommand). Find a logical insertion point — the existing
    list groups commands roughly by topic; insert the vscode entries
    after the `km slack ...` entries and before `km init`, OR after
    `km shell` and before `km ami list` (whichever fits the existing
    structure better — the slack block is around line 50-70 in CLAUDE.md
    and the shell/ami block is around line 90+).

    Add these three lines (matching the existing markdown bullet style):

    ```markdown
    - `km vscode start <sandbox-id>` — launch a VS Code Web session inside the sandbox via SSM port-forward (`--local-port N` to override 8443, `--no-forward` to print URL/token only)
    - `km vscode stop <sandbox-id>` — stop the VS Code session (`systemctl stop km-vscode` over SSM)
    - `km vscode status <sandbox-id>` — print is-active + last 20 lines of `journalctl -u km-vscode`
    ```

    Also add a new top-level section AFTER the existing "## Slack
    Notifications" section and BEFORE the "## Architecture" section
    (where every other major feature has its own block). Use this
    structure:

    ```markdown
    ## VS Code Web sessions

    Phase 73 adds companion-style VS Code Web sessions on running
    sandboxes via `km vscode start | stop | status`. Operator installs
    the `code` CLI in the profile's `initCommands`; km provisions the
    systemd unit, launch wrapper, and token directory at sandbox boot
    (gated by `spec.cli.vscodeEnabled`, default `true`).

    ### Profile field

    | Field | Type | Default | Effect |
    |---|---|---|---|
    | `vscodeEnabled` | `bool*` | `true` | Provision `km-vscode.service` + `/opt/km/bin/km-vscode-launch` + `/etc/km/vscode/` at boot. Set `false` to opt out. |

    ### Sandbox-side contract

    | Path | Purpose | Mode | Owner |
    |---|---|---|---|
    | `/etc/systemd/system/km-vscode.service` | Systemd unit (NOT enabled — explicit start only) | 0644 | root |
    | `/opt/km/bin/km-vscode-launch` | Rotates token, execs `code serve-web --connection-token "$TOKEN"` | 0755 | root |
    | `/etc/km/vscode/` | Token directory | 0700 | sandbox |
    | `/etc/km/vscode/token` | 64-hex-char token, regenerated on every `start` | 0600 | sandbox |

    ### Important workflow notes

    - **Schema change ⇒ `km init --sidecars`** so the management
      Lambda's embedded km binary picks up `vscodeEnabled`.
    - **Existing sandboxes need destroy + create** to adopt the new
      userdata template.
    - **Operator installs the `code` CLI** in `initCommands` — km does
      not install the binary. See `docs/vscode.md` for the canonical
      `cli-alpine-x64` snippet (Amazon Linux 2023 default).
    - **Token rotates on every `start`** — re-running `start` while a
      tunnel is open invalidates the previous session. `stop` first,
      or use `--no-forward`.

    See `docs/vscode.md` for the full operator guide.
    ```

    Place this block immediately after the existing "## Slack
    Notifications" / "### Slack transcript streaming (Phase 68)"
    block and before "## Architecture".

    Do NOT touch any unrelated section of CLAUDE.md. Do NOT reformat
    existing markdown. Use `Edit` for in-place changes.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q "km vscode start" CLAUDE.md && grep -q "## VS Code Web sessions" CLAUDE.md && grep -q "vscodeEnabled" CLAUDE.md && grep -q "km-vscode-launch" CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md contains the three new CLI bullets and the new "## VS
    Code Web sessions" section. References `docs/vscode.md` for the
    full operator guide. The new section sits between the existing
    "## Slack Notifications" block and "## Architecture".
  </done>
</task>

</tasks>

<verification>
- `docs/vscode.md` is at least 80 lines and contains `km vscode start`,
  `vscodeEnabled`, `cli-alpine-x64`, troubleshooting.
- `CLAUDE.md` contains the three new CLI bullets and a new
  `## VS Code Web sessions` top-level section.
- `git diff --stat docs/vscode.md CLAUDE.md` shows new file +
  surgical CLAUDE.md edits.
</verification>

<success_criteria>
- A new operator can read `docs/vscode.md` and successfully provision
  + connect to VS Code Web on a sandbox.
- CLAUDE.md surfaces the new commands in the CLI list AND has a
  dedicated section for VS Code Web sessions matching the existing
  feature-doc convention.
</success_criteria>

<output>
After completion, create `.planning/phases/73-km-vscode-remote-session-via-ssm/73-04-SUMMARY.md`
listing: docs/vscode.md section outline, CLAUDE.md edit positions
(line numbers), and a one-line note pointing Plan 73-05 (closeout)
at the manual-smoke-test checklist that the docs make possible.
</output>
