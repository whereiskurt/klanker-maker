---
phase: 73-km-vscode-remote-session-via-ssm
plan: 02
type: execute
wave: 1
depends_on: ["73-00"]
files_modified:
  - pkg/compiler/userdata.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Userdata generated for a profile with `vscodeEnabled: true` (or omitted/nil) contains the km-vscode systemd unit, the launch wrapper, and the token directory mkdir/chown/chmod sequence."
    - "Userdata generated for `vscodeEnabled: false` contains NONE of those four artifacts."
    - "After the unit file is written, `systemctl daemon-reload` runs at provision time so the unit is visible to the FIRST `km vscode start` (no auto-start, no enable)."
    - "AMI-baked sandboxes get the unit fresh from cloud-init each boot — same pattern as every other systemd unit km manages."
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "VSCodeEnabled bool field on userDataParams + populated from profile + conditional template block"
      contains: "VSCodeEnabled, km-vscode.service, km-vscode-launch"
  key_links:
    - from: "userDataParams.VSCodeEnabled"
      to: "Conditional `{{- if .VSCodeEnabled }}` block in userDataTemplate"
      via: "Go text/template gating"
      pattern: "{{- if .VSCodeEnabled }}"
    - from: "generateUserData"
      to: "p.Spec.CLI.VSCodeEnabled (Wave 1 schema field)"
      via: "Inline nil-check pattern (RESEARCH.md Code Examples lines 398-404). Avoids cross-plan dependency on DefaultVSCodeEnabled — both plans are Wave 1 and could execute in parallel."
      pattern: "p.Spec.CLI.VSCodeEnabled"
    - from: "Launch wrapper inside the template"
      to: "/etc/km/vscode/token (read by km vscode start via SSM)"
      via: "`code serve-web --connection-token \"$TOKEN\"` after writing token file 0600"
      pattern: "connection-token \"\\$TOKEN\""
---

<objective>
Add the `vscodeEnabled` userdata template branch: when the profile flag
is true (default), provision the km-vscode systemd unit, launch wrapper,
token directory, and a daemon-reload. When false, omit everything. This
plan turns the four `TestUserDataVSCode*` Wave 0 stubs GREEN.

Purpose:
- Single source of truth for sandbox-side runtime contract. The unit is
  NOT enabled (no `WantedBy=multi-user.target`); explicit start only.
- Honor the corrected design (Plan 73-00 Task 3): the launch wrapper
  uses `--connection-token "$TOKEN"` (NOT `--connection-token-file`,
  NOT `--without-connection-token=false`).
- Run `systemctl daemon-reload` at provision time so the FIRST
  `km vscode start` finds the freshly-written unit (matches the
  AMI-bake-then-cloud-init pattern from commit 4030fce).
- **Use the inline nil-check pattern, NOT `profile.DefaultVSCodeEnabled`.**
  73-01 (which adds `DefaultVSCodeEnabled`) is also Wave 1 and may
  execute in parallel; calling it here would create a parallel-safety
  hazard. The inline pattern (RESEARCH.md Code Examples lines 398-404)
  achieves identical semantics with only a direct dependency on the
  `VSCodeEnabled *bool` field — which 73-01 lands in the same wave but
  via the Wave 0 stub tests acts as a stable contract.

Output:
- `userDataParams.VSCodeEnabled bool` field.
- One inline nil-check block in `generateUserData` populating the field.
- One `{{- if .VSCodeEnabled }} … {{- end }}` block in the template,
  placed near the other systemd-unit-writing blocks (after the
  slack-inbound block, before the systemd enable/restart fan-out).
- Four Wave 0 userdata tests GREEN.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-CONTEXT.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-RESEARCH.md
@pkg/compiler/userdata.go
@pkg/compiler/userdata_test.go
@CLAUDE.md

<interfaces>
<!-- The two integration points: userDataParams struct + generateUserData -->
<!-- population block. Existing patterns from Phase 67 (SlackInboundEnabled) -->
<!-- show exactly how to add a new conditional block. -->

From pkg/compiler/userdata.go (line 2766+, struct):
```go
type userDataParams struct {
    SandboxID    string
    // …many fields…
    SlackInboundEnabled bool   // Phase 67
    NotifySlackEnabled  bool   // Phase 67
    SlackThreadsTableName string
    SlackStreamMessagesTableName string
}
```

From pkg/compiler/userdata.go (line ~3175, population):
```go
// Phase 67: wire SlackInboundEnabled for template conditionals
if p.Spec.CLI.NotifySlackInboundEnabled {
    params.SlackInboundEnabled = true
    // …
}
```

From pkg/compiler/userdata.go (line 1207, template gate pattern):
```
{{- if .SlackInboundEnabled }}
…systemd unit + bash poller heredoc…
{{- end }}
```

From pkg/profile (Wave 1 — schema field added by Plan 73-01):
```go
// On CLISpec — pointer to bool, nil ⇒ default true.
VSCodeEnabled *bool `yaml:"vscodeEnabled,omitempty" json:"vscodeEnabled,omitempty"`
```

Inline nil-check semantics (RESEARCH.md Code Examples lines 398-404):
- Default true. Only `Spec.CLI != nil && Spec.CLI.VSCodeEnabled != nil && !*Spec.CLI.VSCodeEnabled` flips the field to false.
- This intentionally MIRRORS what 73-01's `DefaultVSCodeEnabled` does, but inlined here so 73-02 has zero compile-time dependency on 73-01's helper. Both plans can ship in either order within Wave 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VSCodeEnabled to userDataParams + populate via inline nil-check</name>
  <files>pkg/compiler/userdata.go</files>
  <behavior>
    After this task:
    - `params.VSCodeEnabled == true` when:
      - `p.Spec.CLI == nil` (no cli block)
      - `p.Spec.CLI != nil && p.Spec.CLI.VSCodeEnabled == nil` (cli block, no flag)
      - `p.Spec.CLI != nil && p.Spec.CLI.VSCodeEnabled != nil && *p.Spec.CLI.VSCodeEnabled == true`
    - `params.VSCodeEnabled == false` ONLY when:
      - `p.Spec.CLI != nil && p.Spec.CLI.VSCodeEnabled != nil && *p.Spec.CLI.VSCodeEnabled == false`
    - Compile clean: `go vet ./pkg/compiler/...`
  </behavior>
  <action>
    1. **Add the struct field.** In `pkg/compiler/userdata.go`, locate
       the `userDataParams` struct (line ~2766). At the END of the
       struct (after `SlackStreamMessagesTableName`), add:

       ```go
       // Phase 73: VSCodeEnabled gates the km-vscode systemd unit, the
       // /opt/km/bin/km-vscode-launch wrapper, and the /etc/km/vscode/
       // token directory. True when the profile sets
       // spec.cli.vscodeEnabled to true OR omits the field (nil ⇒
       // default true). False only when the profile explicitly sets
       // vscodeEnabled: false.
       VSCodeEnabled bool
       ```

    2. **Populate the field — INLINE nil-check, not via helper.** In
       `generateUserData`, find a spot AFTER the existing
       `if p.Spec.CLI != nil { … }` block (which currently handles all
       the notify* fields, around line 3140-3215). At the end of
       `generateUserData`, JUST BEFORE the final
       `return executeTemplate(...)` call (or wherever the template is
       executed), add the EXACT block below:

       ```go
       // Phase 73: vscodeEnabled is default-true. Inline nil-check
       // (NOT profile.DefaultVSCodeEnabled) — Plan 73-02 must not
       // take a compile-time dependency on Plan 73-01's helper because
       // both plans are Wave 1 and may execute in parallel. Semantics
       // mirror the helper exactly (RESEARCH.md Code Examples 398-404).
       if p.Spec.CLI != nil && p.Spec.CLI.VSCodeEnabled != nil && !*p.Spec.CLI.VSCodeEnabled {
           params.VSCodeEnabled = false
       } else {
           params.VSCodeEnabled = true
       }
       ```

       Note carefully:
       - The else branch sets true unconditionally when the explicit
         false condition fails — covering nil-CLI, nil-pointer, and
         &true cases in one branch.
       - This populates AFTER the `if p.Spec.CLI != nil` block on
         purpose so the nil-CLI path produces true (matching
         TestUserDataVSCodeDefault).
       - Do NOT call `profile.DefaultVSCodeEnabled` here even though
         it exists in 73-01. Keeping this plan inline-only preserves
         the parallel Wave 1 layout (schema + userdata both ship in
         Wave 1, no ordering between them).

    3. **No other code changes.** Do NOT add the field to the existing
       `if p.Spec.CLI != nil` block — that would re-introduce the
       nil-CLI bug and cause `TestUserDataVSCodeDefault` (which uses a
       profile without a `cli:` block) to fail.

    4. Run `go vet ./pkg/compiler/...` after editing — ensure no
       unused-import / shadowing issues. (No new imports are required;
       the `profile` package was previously needed for the helper-call
       form but is no longer referenced from this new code.)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go vet ./pkg/compiler/... && go build ./pkg/compiler/...</automated>
  </verify>
  <done>
    `userDataParams` struct has a `VSCodeEnabled bool` field with
    docstring. `generateUserData` populates it via the inline
    `if p.Spec.CLI != nil && p.Spec.CLI.VSCodeEnabled != nil && !*p.Spec.CLI.VSCodeEnabled` /
    else pattern. The file does NOT call `profile.DefaultVSCodeEnabled`.
    Compile and vet clean. (The four userdata tests are still RED at
    this point — the template block is added in Task 2.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the conditional userdata template block</name>
  <files>pkg/compiler/userdata.go</files>
  <behavior>
    After this task:
    - All 4 Wave 0 userdata tests pass:
      - `TestUserDataVSCodeEnabled` (vscodeEnabled: true → all four artifacts present)
      - `TestUserDataVSCodeDisabled` (vscodeEnabled: false → none present)
      - `TestUserDataVSCodeDefault` (cli: {} or no cli block → all four present)
      - `TestUserDataVSCodeDaemonReload` (daemon-reload appears AFTER the unit write)
    - The 4 pre-existing known-failing tests remain at the same RED state
      (no new regressions).
    - Generated userdata launch wrapper uses
      `--connection-token "$TOKEN"` (NOT `--connection-token-file`,
      NOT `--without-connection-token=false`).
  </behavior>
  <action>
    1. **Locate the insertion point.** Find the slack-inbound systemd
       unit block at `userdata.go:1718` (`{{- if .SlackInboundEnabled }}`
       writing `/etc/systemd/system/km-slack-inbound-poller.service`).
       Find its matching `{{- end }}` (likely around line 1740-1800 —
       grep for the next `{{- end }}` after line 1718). The vscode
       block goes IMMEDIATELY AFTER that `{{- end }}`, BEFORE the next
       block or the systemctl enable/restart fan-out at line 2289.

    2. **Insert the block.** Add this exact block (note the
       single-quoted heredocs — they prevent shell expansion of
       `$TOKEN` inside the wrapper, which is correct because the
       wrapper script must contain the literal `$TOKEN` reference and
       only resolve it when the bash runs at unit-start time). Use
       Go's raw-string Go template — placement is inside
       `userDataTemplate`'s string literal:

       ```
       {{- if .VSCodeEnabled }}
       # ----------------------------------------------------------------
       # Phase 73: km-vscode — VS Code Web session systemd unit + launcher
       # The unit is NOT enabled (no WantedBy) — `km vscode start` issues
       # an explicit `systemctl restart` that rotates the token each time.
       # ----------------------------------------------------------------
       cat > /etc/systemd/system/km-vscode.service << 'KMVSCODEUNIT'
       [Unit]
       Description=Klanker Maker VS Code Web session
       After=network-online.target
       Wants=network-online.target

       [Service]
       Type=simple
       User=sandbox
       Group=sandbox
       ExecStart=/opt/km/bin/km-vscode-launch
       Restart=on-failure
       RestartSec=2
       # Note: deliberately NOT WantedBy=multi-user.target — explicit start only.
       KMVSCODEUNIT
       chmod 0644 /etc/systemd/system/km-vscode.service

       cat > /opt/km/bin/km-vscode-launch << 'KMVSCODELAUNCH'
       #!/usr/bin/env bash
       # /opt/km/bin/km-vscode-launch — rotate token + exec `code serve-web`.
       # Bind 127.0.0.1 only — SSM port-forward is the security boundary.
       set -euo pipefail
       umask 077
       TOKEN=$(openssl rand -hex 32)
       printf '%s' "$TOKEN" > /etc/km/vscode/token
       chown sandbox:sandbox /etc/km/vscode/token
       chmod 0600 /etc/km/vscode/token
       exec /usr/local/bin/code serve-web \
           --host 127.0.0.1 \
           --port 8443 \
           --connection-token "$TOKEN" \
           --accept-server-license-terms
       KMVSCODELAUNCH
       chmod 0755 /opt/km/bin/km-vscode-launch

       mkdir -p /etc/km/vscode
       chown sandbox:sandbox /etc/km/vscode
       chmod 0700 /etc/km/vscode

       # Reload systemd so the FIRST `km vscode start` finds the unit.
       # The unit is NOT enabled and NOT started here — explicit-start only.
       # AMI-baked sandboxes hit this same path on every cloud-init.
       systemctl daemon-reload
       echo "[km-bootstrap] km-vscode unit + launch wrapper installed (explicit start only)"
       {{- end }}
       ```

       **Critical details — verify each:**
       - The launch wrapper uses `--connection-token "$TOKEN"` (NOT
         `--connection-token-file`, NOT `--without-connection-token`).
       - The unit has NO `[Install]` section (NO `WantedBy=multi-user.target`).
       - The systemd block uses `cat > … << 'KMVSCODEUNIT'` (single-quoted
         heredoc tag) so neither template expansion nor shell expansion
         touches the unit body.
       - The wrapper block uses `cat > … << 'KMVSCODELAUNCH'` for the
         same reason — the `$TOKEN` inside must remain literal.
       - The block contains exactly one `systemctl daemon-reload` call.

    3. **Do NOT add `km-vscode` to the systemctl enable / restart
       fan-out lines** at userdata.go:2289-2297. Those lines are
       `systemctl enable …` + `systemctl restart …` for sidecars that
       must auto-start at every boot. The vscode unit MUST NOT
       auto-start.

    4. After editing, the four RED tests must turn GREEN. Run them.
       If any fails, inspect `out` from `generateUserData` and adjust
       the template — likely candidates: ordering inside the template,
       template-tag whitespace (`{{-` vs `{{`), or heredoc tag literal.

    5. Sanity-check: `git grep -n "without-connection-token" pkg/compiler/`
       must return no hits. The string must NOT appear anywhere in the
       generated userdata.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/compiler/... -run "TestUserDataVSCode" -count=1 && ! grep -q "without-connection-token" pkg/compiler/userdata.go</automated>
  </verify>
  <done>
    `go test ./pkg/compiler/... -run TestUserDataVSCode -count=1` is GREEN
    (4 tests). `grep` confirms `without-connection-token` does not
    appear in `pkg/compiler/userdata.go`. Generated userdata for a
    `vscodeEnabled: true` profile contains all four artifacts (unit,
    wrapper, dir mkdir, daemon-reload) and the wrapper's exec line uses
    `--connection-token "$TOKEN"`. Pre-existing failing tests remain at
    the same RED state.
  </done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/... -run TestUserDataVSCode -count=1` → 4 tests GREEN
- `go test ./pkg/compiler/... -count=1` → only the 4 pre-existing
  unrelated failures remain (no new regressions)
- `go vet ./pkg/compiler/...` → clean
- `! grep "without-connection-token" pkg/compiler/userdata.go` → no hits
- `! grep "DefaultVSCodeEnabled" pkg/compiler/userdata.go` → no hits
  (this plan deliberately uses the inline nil-check, not 73-01's helper)
- `! grep "WantedBy=multi-user.target" -A0 pkg/compiler/userdata.go | grep "km-vscode"` →
  the vscode block does not auto-start
</verification>

<success_criteria>
- userDataParams has VSCodeEnabled bool, populated via inline nil-check
  (NOT via profile.DefaultVSCodeEnabled — Wave 1 parallel safety).
- Conditional template block emits unit + wrapper + token dir +
  daemon-reload exactly when VSCodeEnabled is true.
- Launch wrapper inside the template uses `--connection-token "$TOKEN"`.
- All 4 Wave 0 userdata tests GREEN.
- No regressions to other compiler tests.
</success_criteria>

<output>
After completion, create `.planning/phases/73-km-vscode-remote-session-via-ssm/73-02-SUMMARY.md`
listing: line ranges of the changes, a copy of the template block as
shipped, and confirmation of `! grep without-connection-token`,
`! grep DefaultVSCodeEnabled`, and the 4 GREEN tests.
</output>
</content>
</invoke>