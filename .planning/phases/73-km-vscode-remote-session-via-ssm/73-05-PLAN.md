---
phase: 73-km-vscode-remote-session-via-ssm
plan: 05
type: execute
wave: 4
depends_on: ["73-01", "73-02", "73-03", "73-04"]
files_modified:
  - .planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md
  - .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "VALIDATION.md's Per-Task Verification Map is fully populated for every Plan 73-00..73-04 task with type, command, file-exists state."
    - "VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`."
    - "73-UAT.md exists with the four manual smoke scenarios already listed in VALIDATION.md (end-to-end session; token rotation; vscodeEnabled:false clean error; local port collision)."
    - "Operator runs the UAT checklist and signs off OR opens follow-up issues for any gaps."
  artifacts:
    - path: ".planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md"
      provides: "Per-Task Verification Map populated; nyquist_compliant: true; wave_0_complete: true"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md"
      provides: "Operator-facing manual test checklist with results column"
      min_lines: 40
  key_links:
    - from: "73-VALIDATION.md Per-Task Verification Map"
      to: "Each PLAN.md task's <verify><automated> command"
      via: "Mirrored 1:1 for sampling continuity audit"
      pattern: "go test"
    - from: "73-UAT.md scenarios"
      to: "VALIDATION.md Manual-Only Verifications section"
      via: "1:1 reproduction of the 4 manual scenarios with checkboxes for sign-off"
      pattern: "vscode start"
---

<objective>
Closeout. Two related artifacts:

1. **VALIDATION.md update** — Populate the Per-Task Verification Map
   so the validation strategy doc reflects the actual plan structure.
   Set `nyquist_compliant: true` once every task has an `<automated>`
   verify command (which they all do per Plan 73-00..73-04). Set
   `wave_0_complete: true` once Plans 73-01..73-03 are GREEN.
2. **UAT.md creation** — Materialize the four manual scenarios
   already listed in VALIDATION.md into an operator-facing checklist
   with reproducible steps and result-recording columns.

Then a checkpoint pauses execute-phase for the operator to actually
run the four manual scenarios and sign off (or surface gaps for
follow-up work).

Purpose:
- VALIDATION.md is the audit trail for "every task has feedback,
  feedback latency < 90s." Without the populated map, the next
  `/gsd:verify-work` run can't sample correctly.
- UAT.md is the human-required closing gate. Phase 73 ships features
  that interact with a live EC2 + browser; only the operator can
  observe "did the browser actually load and let me edit a file?"

Output:
- VALIDATION.md with a populated Per-Task Verification Map (rows for
  every task in 73-00 through 73-04).
- VALIDATION.md frontmatter flips: `nyquist_compliant: true`,
  `wave_0_complete: true`.
- New file `.planning/phases/73-…/73-UAT.md` with 4 scenarios and a
  results table.
- Manual sign-off checkpoint blocks execute-phase until operator
  reports back.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-CONTEXT.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-RESEARCH.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-00-PLAN.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-01-PLAN.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-02-PLAN.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-03-PLAN.md
@.planning/phases/73-km-vscode-remote-session-via-ssm/73-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate VALIDATION.md Per-Task Verification Map + flip flags</name>
  <files>.planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md</files>
  <action>
    Edit `73-VALIDATION.md`:

    1. **Replace the placeholder Per-Task Verification Map.** The
       current table (lines ~41-43) has one placeholder row. Replace
       with a row for every task in Plans 73-00 through 73-04. Mirror
       this format (use the actual `<automated>` command from each
       PLAN.md):

       ```
       | Task ID | Plan | Wave | Behavior | Test Type | Automated Command | Status |
       |---------|------|------|----------|-----------|-------------------|--------|
       | 73-00-01 | 00   | 0    | vscode_test.go stubs | wave-0 | `go vet ./internal/app/cmd/... 2>&1 \| grep undefined` | ✅ green |
       | 73-00-02 | 00   | 0    | profile + userdata stubs | wave-0 | `go test ./pkg/profile/... -run TestVSCodeEnabled -count=1` (RED expected) | ✅ green |
       | 73-00-03 | 00   | 0    | CONTEXT/spec amendments | docs | `! grep "without-connection-token=false" .planning/.../73-CONTEXT.md docs/.../km-vscode-design.md` | ✅ green |
       | 73-01-01 | 01   | 1    | VSCodeEnabled field + helper | unit | `go test ./pkg/profile/... -run "TestVSCodeEnabled" -count=1` | ✅ green |
       | 73-01-02 | 01   | 1    | JSON schema entry | unit | `python3 -c "..."; go test ./pkg/profile/... -count=1` | ✅ green |
       | 73-02-01 | 02   | 1    | userDataParams + populate | compile | `go vet ./pkg/compiler/... && go build ./pkg/compiler/...` | ✅ green |
       | 73-02-02 | 02   | 1    | userdata template block | unit | `go test ./pkg/compiler/... -run TestUserDataVSCode -count=1 && ! grep "without-connection-token" pkg/compiler/userdata.go` | ✅ green |
       | 73-03-01 | 03   | 2    | vscode.go (start/stop/status) | unit | `go test ./internal/app/cmd/... -run TestVSCode -count=1 && go vet ./internal/app/cmd/...` | ✅ green |
       | 73-03-02 | 03   | 2    | root.go wiring | smoke | `go build ./cmd/km && ./km vscode --help \| grep -E start\|stop\|status` | ✅ green |
       | 73-04-01 | 04   | 3    | docs/vscode.md | docs | `test -f docs/vscode.md && wc -l docs/vscode.md \| awk '$1>=80 {exit 0} {exit 1}'` | ✅ green |
       | 73-04-02 | 04   | 3    | CLAUDE.md update | docs | `grep -q "km vscode start" CLAUDE.md && grep -q "## VS Code Web sessions" CLAUDE.md` | ✅ green |
       | 73-05-01 | 05   | 4    | populate VALIDATION.md (this task) | meta | `grep -q "nyquist_compliant: true" .planning/.../73-VALIDATION.md` | ✅ green |
       | 73-05-02 | 05   | 4    | manual UAT sign-off | manual | N/A — operator | ⬜ pending |
       ```

    2. **Update the frontmatter:** change `nyquist_compliant: false`
       to `nyquist_compliant: true` AND `wave_0_complete: false` to
       `wave_0_complete: true`. Update `status: draft` to
       `status: ready` (or whatever the workflow's expected
       executable status is).

    3. **Update the "Validation Sign-Off" checklist** at the bottom
       — tick every box EXCEPT the last (manual UAT) and the
       `nyquist_compliant: true` final-set box (which gets ticked
       automatically when this task lands).

    Do NOT delete or rewrite any other section. Manual-Only
    Verifications stays as-is — it's the source for Task 2.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q "nyquist_compliant: true" .planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md && grep -cE "^\| 73-(00|01|02|03|04|05)-" .planning/phases/73-km-vscode-remote-session-via-ssm/73-VALIDATION.md | awk '$1 >= 12 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    VALIDATION.md frontmatter has `nyquist_compliant: true` and
    `wave_0_complete: true`. Per-Task Verification Map has at least
    12 task rows (one per task in 73-00..73-05). All non-manual
    rows show `✅ green` status; the manual UAT row stays
    `⬜ pending`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 73-UAT.md with manual smoke checklist</name>
  <files>.planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md</files>
  <action>
    Create `.planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md`
    (new file). Use the four scenarios from VALIDATION.md
    Manual-Only Verifications as the source of truth.

    Required structure:

    ```markdown
    ---
    phase: 73
    slug: km-vscode-remote-session-via-ssm
    status: pending
    operator: <fill at sign-off>
    completed: <fill at sign-off>
    ---

    # Phase 73 — Operator UAT

    > Run after Plans 73-00 through 73-04 are merged and the binary
    > is rebuilt. Pre-req: `make build && km init --sidecars` to
    > propagate the new schema to the management Lambda.

    ## Scenario 1: End-to-end VS Code session

    **Pre-conditions:**
    - Phase 73 binary built (`make build`); `make build` exit 0.
    - `km init --sidecars` exit 0.
    - A test profile `profiles/vscode-smoke.yaml` with:
      - `spec.cli.vscodeEnabled: true` (or omitted)
      - `spec.execution.initCommands` per `docs/vscode.md` (cli-alpine-x64 install)

    **Steps:**
    1. `km validate profiles/vscode-smoke.yaml` → exit 0
    2. `km create profiles/vscode-smoke.yaml` → returns sandbox id `sb-XXX`
    3. Wait until `km status sb-XXX` shows `running`
    4. `km vscode start sb-XXX` → prints connection block including a URL
    5. Open the URL in a browser
    6. VS Code Web loads; the workspace tree is visible
    7. Edit `/workspace/test.txt`; save with Ctrl-S
    8. From a separate terminal: `km shell sb-XXX -- cat /workspace/test.txt` → shows the edit
    9. Ctrl-C the `km vscode start` terminal → tunnel closes (server keeps running)
    10. `km vscode stop sb-XXX` → exit 0
    11. `km vscode status sb-XXX` → reports `inactive`, exit non-zero

    **Result:** [ ] PASS / [ ] FAIL — notes:

    ## Scenario 2: Token rotation across `start` calls

    **Steps:**
    1. `km vscode start sb-XXX --no-forward` → record `token1` from URL
    2. `km vscode stop sb-XXX`
    3. `km vscode start sb-XXX --no-forward` → record `token2` from URL
    4. Assert `token1 != token2` (eyeball compare)

    **Result:** [ ] PASS / [ ] FAIL — notes:

    ## Scenario 3: `vscodeEnabled: false` produces a clean error

    **Pre-conditions:**
    - A second test profile `profiles/vscode-disabled.yaml` with
      `spec.cli.vscodeEnabled: false` and otherwise identical
      contents (no `code` install needed).

    **Steps:**
    1. `km create profiles/vscode-disabled.yaml` → returns `sb-YYY`
    2. Wait until running.
    3. `km vscode start sb-YYY` → exits non-zero with message
       containing "VS Code not enabled in this sandbox's profile"
    4. NO journalctl excerpt should be printed (this is the
       distinguishing feature vs. a real failure).

    **Result:** [ ] PASS / [ ] FAIL — notes:

    ## Scenario 4: Local port collision behavior

    **Steps:**
    1. On the operator laptop: `nc -l 8443 &` (or any process
       binding 8443).
    2. `km vscode start sb-XXX` → expect bind error from the
       port-forward step (server still rotated and ran on the
       remote — that's expected; the error is local-side).
    3. `km vscode start sb-XXX --local-port 9443` → succeeds;
       URL shows port 9443.
    4. Open `http://localhost:9443/?tkn=…` in browser → loads.
    5. Cleanup: `kill %1` (the nc) and Ctrl-C the tunnel.

    **Result:** [ ] PASS / [ ] FAIL — notes:

    ---

    ## Sign-off

    - **Operator:** _________________________
    - **Date:** ____________
    - **All scenarios PASS:** [ ] yes / [ ] no — see notes
    - **Follow-up issues:** ____________________________________

    ## Cleanup

    `km destroy sb-XXX --remote --yes`
    `km destroy sb-YYY --remote --yes`
    ```

    Tone: clear, reproducible, no hand-waving. Do NOT use emoji.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && test -f .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md && wc -l .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md | awk '$1 >= 40 { exit 0 } { exit 1 }' && grep -q "Scenario 1" .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md && grep -q "Scenario 4" .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md && grep -q "Sign-off" .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md</automated>
  </verify>
  <done>
    `73-UAT.md` exists, ≥40 lines, has Scenarios 1-4 plus a Sign-off
    section. Frontmatter has `status: pending`. Each scenario has
    explicit `Result: [ ] PASS / [ ] FAIL` checkboxes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs UAT and signs off</name>
  <files>.planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md</files>
  <action>
    PAUSE for operator. This task is a manual sign-off gate — Claude
    does NOT execute it. The operator (you) opens 73-UAT.md and walks
    through all four scenarios on a real AWS account, ticking
    PASS / FAIL boxes and filling in operator name + date in the
    Sign-off section. Commit the updated 73-UAT.md when complete.

    What's been built (for context):
    - Phase 73 in entirety: schema field, JSON schema entry, userdata
      template, three CLI subcommands, operator docs, and a complete
      test suite (13 unit tests + the 4 manual scenarios).

    How to verify (operator workflow):
    1. Open `.planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md`.
    2. Walk through Scenarios 1-4 on a live AWS account.
    3. For each scenario, tick `[x] PASS` or `[x] FAIL` and fill the
       notes field.
    4. Fill in operator name + date in the Sign-off section.

    If any scenario FAILS:
    - Capture the failure mode in the Notes column of that scenario.
    - Decide: (a) fix in this phase via `/gsd:plan-phase 73 --gaps`,
      or (b) defer to a Phase 73.1 follow-up. Document the choice in
      the Follow-up issues field of Sign-off.

    On approval, change the frontmatter `status: pending` →
    `status: signed-off` and run `/gsd:close-phase 73`.

    Estimated total wall-clock: ~25-40 minutes (most of it waiting
    for the two test sandboxes to come up; ~5 min of actual UAT).

    Resume signal: type "approved" with operator name, OR
    "fail: <scenario-N>: <reason>".
  </action>
  <verify>
    <automated>grep -q "status: signed-off" .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md && grep -E "All scenarios PASS:.*\[x\] yes" .planning/phases/73-km-vscode-remote-session-via-ssm/73-UAT.md</automated>
  </verify>
  <done>
    73-UAT.md frontmatter has `status: signed-off`. All four
    scenarios have a PASS or FAIL box ticked. Operator name + date
    filled in. The "All scenarios PASS: [x] yes" box is ticked OR
    Follow-up issues are documented if any scenario failed.
  </done>
</task>

</tasks>

<verification>
- VALIDATION.md frontmatter flipped to `nyquist_compliant: true` /
  `wave_0_complete: true`.
- VALIDATION.md Per-Task Verification Map has ≥12 task rows.
- 73-UAT.md exists with 4 scenarios + sign-off section.
- Manual UAT checkpoint blocks plan completion until operator signs off.
</verification>

<success_criteria>
- All Per-Task Verification Map rows except the manual UAT row show
  GREEN status.
- 73-UAT.md is reproducible by another operator with no Phase-73
  context (every step is explicit, every input file path or command
  is named).
- The phase is unambiguously DONE: every task in 73-00..73-04 has
  passed automated verification, and 73-05 has captured operator
  sign-off (or specific gap descriptions).
</success_criteria>

<output>
After operator sign-off, create
`.planning/phases/73-km-vscode-remote-session-via-ssm/73-05-SUMMARY.md`
covering: VALIDATION.md flips, UAT.md scenarios + their PASS/FAIL
states, any gaps deferred to a future phase, and the operator's
sign-off date and name.
</output>
