---
phase: 61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox
plan: 03
type: execute
wave: 3
depends_on: [61-01, 61-02]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "After km init <region>, aws ssm describe-document --name KM-Sandbox-Session returns Status: Active, SessionType: Standard_Stream"
    - "km shell <id> non-root: whoami returns 'sandbox'; sleep 100 + Ctrl+C interrupts and returns prompt; session stays open"
    - "km agent --claude <id>: Ctrl+C interrupts Claude generation without terminating the SSM session; Ctrl+D exits cleanly"
    - "km agent attach <id>: Ctrl+C inside the tmux pane interrupts the foreground process; Ctrl-B d detaches and ends the SSM session"
    - "km agent run <id> --prompt ... --interactive: Ctrl+C interrupts the running command; Ctrl-B d detaches"
    - "km shell --root <id>: regression — Ctrl+C still interrupts foreground processes (root path is unchanged)"
  artifacts: []
  key_links: []
---

<objective>
Run the manual UAT (User Acceptance Testing) defined in 61-CONTEXT.md and 61-VALIDATION.md against a live sandbox to confirm Ctrl+C now forwards SIGINT to remote foreground processes for all four affected callsites without tearing down the SSM session.

Purpose: The Ctrl+C signal forwarding behavior cannot be unit-tested — it requires a real PTY, real AWS SSM data plane, and live operator keystrokes. This plan is the goal-backward verification that the bug is actually fixed end-to-end. It runs LAST after both infra (Plan 01) and CLI (Plan 02) are merged.

Output: A signed-off UAT log appended to the SUMMARY documenting outcomes for each scenario.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-CONTEXT.md
@.planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-VALIDATION.md
@.planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-01-SUMMARY.md
@.planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm km binary is built from main and KM-Sandbox-Session is provisioned</name>
  <files>(none — read-only verification)</files>
  <action>
Before kicking the operator into manual testing, confirm:

1. The local `km` binary embeds the new code:

```bash
cd /Users/khundeck/working/klankrmkr
make build
./km --version
grep -n 'KM-Sandbox-Session' internal/app/cmd/shell.go internal/app/cmd/agent.go | wc -l   # expect ≥ 4
```

2. The `KM-Sandbox-Session` document is provisioned in the operator's primary region:

```bash
aws ssm describe-document --name KM-Sandbox-Session --region us-east-1 --profile klanker-terraform \
  --query '{Name:Document.Name,Status:Document.Status,SessionType:Document.SessionType,DocumentType:Document.DocumentType}' \
  --output table
```

Expected output:
```
Name           = KM-Sandbox-Session
Status         = Active
SessionType    = Standard_Stream
DocumentType   = Session
```

If the doc is missing, run `km init us-east-1 --dry-run=false` and re-check.

3. Identify (or create) a running EC2 sandbox suitable for UAT:

```bash
./km list
# If none running:
./km create profiles/open-dev.yaml
```

Note the sandbox-id for use in Tasks 2-7 of the UAT.

If any of these pre-flight checks fails, surface the failure and STOP — do not proceed to manual UAT until pre-flight is green.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; make build &amp;&amp; aws ssm describe-document --name KM-Sandbox-Session --region us-east-1 --profile klanker-terraform --query 'Document.Status' --output text | grep -q '^Active$'</automated>
  </verify>
  <done>
    - `km` binary builds.
    - `aws ssm describe-document --name KM-Sandbox-Session` returns `Status: Active` and `SessionType: Standard_Stream` in the operator's primary region.
    - At least one running EC2 sandbox is identified for UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — km shell non-root Ctrl+C</name>
  <files>(none — manual interactive verification)</files>
  <action>
Plan 01 created the `KM-Sandbox-Session` SSM document. Plan 02 switched `km shell <id>` (non-root) to use it. The non-root SSM session now uses Standard_Stream sessionType which forwards Ctrl+C as a PTY byte instead of tearing down the session.

**Operator steps:**

1. Pick a running EC2 sandbox from `./km list`. Substitute the sandbox-id below.
2. Run: `./km shell <sandbox-id>`
3. Once landed, run: `whoami` — expect output `sandbox` (NOT `root`, NOT `ssm-user`).
4. Run: `echo $KM_SANDBOX_ID` — expect a non-empty value (confirms `/etc/profile.d/` sourced via the conditional `exec bash -l` branch of the shellProfile).
5. Run: `sleep 100`
6. Press **Ctrl+C** (one time).
   - Expected: `^C` echoes, the sleep is interrupted, a fresh `$ ` prompt appears.
   - The SSM session MUST remain open. (Bug behavior would close the session and dump you back at the local terminal prompt.)
7. Run: `exit` to end the session normally.

After completing the steps, type "approved" to resume execution, or describe the divergence (which step, exact error text, observed behavior).
  </action>
  <verify>
    <manual>Operator confirms: whoami=sandbox, $KM_SANDBOX_ID non-empty, Ctrl+C interrupts sleep without ending session, exit returns to local prompt cleanly.</manual>
    <resume-signal>Type "approved" if all expectations met; otherwise describe what happened.</resume-signal>
  </verify>
  <done>
    - whoami returns `sandbox`.
    - $KM_SANDBOX_ID is set.
    - Ctrl+C interrupts the sleep, prompt returns, SSM session stays open.
    - `exit` ends the session cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — km agent --claude Ctrl+C during generation</name>
  <files>(none — manual interactive verification)</files>
  <action>
Plan 02 switched `km agent <id> --claude` to use `KM-Sandbox-Session`. Ctrl+C should now interrupt Claude generation without terminating the SSM session.

**Operator steps:**

Pre-req: `claude login` completed on the sandbox (direct API), OR the profile has `cli.noBedrock: false` (Bedrock-routed Claude).

1. Run: `./km agent <sandbox-id> --claude`
2. Wait for the Claude prompt.
3. Send: `count to 1000 slowly`  (any prompt that produces a long token stream).
4. While Claude is generating, press **Ctrl+C** (one time).
   - Expected: Claude shows its interrupt indicator (e.g., `[Interrupted]`), generation halts, the Claude REPL continues to accept new input.
   - The SSM session MUST remain open — you should still be inside Claude.
5. Press **Ctrl+D** to exit Claude. The SSM session ends normally.

After completing the steps, type "approved" to resume execution, or describe the divergence.
  </action>
  <verify>
    <manual>Operator confirms: Ctrl+C interrupts Claude generation, Claude REPL stays alive, Ctrl+D exits cleanly.</manual>
    <resume-signal>Type "approved" if all expectations met; otherwise describe what happened.</resume-signal>
  </verify>
  <done>
    - Ctrl+C produces an interrupt indicator inside Claude.
    - Claude continues to accept input after interrupt.
    - Ctrl+D ends the session cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — km agent attach Ctrl+C in tmux pane</name>
  <files>(none — manual interactive verification)</files>
  <action>
Plan 02 switched `km agent attach <id>` to use `KM-Sandbox-Session`. Ctrl+C inside the attached tmux pane should affect the foreground process inside the pane, NOT terminate the SSM session.

**Operator steps:**

1. Spawn a tmux session on the sandbox first:
   `./km agent run <sandbox-id> --prompt "sleep 600"`
   (Returns immediately. The agent runs `sleep 600` inside a `km-agent-*` tmux session.)
2. Attach: `./km agent attach <sandbox-id>`
   (You're now inside the tmux pane watching the running agent.)
3. Press **Ctrl+C** (one time) inside the pane.
   - Expected: the foreground process (sleep / agent) is interrupted in the pane; you stay attached to tmux.
   - The SSM session MUST remain open.
4. Press **Ctrl-B d** to detach from tmux.
   - Expected: tmux releases the pane, the SSM session ends, you return to your local prompt.

After completing the steps, type "approved" to resume execution, or describe the divergence.
  </action>
  <verify>
    <manual>Operator confirms: Ctrl+C interrupts the pane's foreground process, tmux stays attached, Ctrl-B d detaches and ends SSM cleanly.</manual>
    <resume-signal>Type "approved" if all expectations met; otherwise describe what happened.</resume-signal>
  </verify>
  <done>
    - Ctrl+C interrupts the pane's foreground process without ending the SSM session.
    - Ctrl-B d detaches from tmux and ends the SSM session.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT — km agent run --interactive Ctrl+C in tmux pane</name>
  <files>(none — manual interactive verification)</files>
  <action>
Plan 02 switched `km agent run <id> --prompt ... --interactive` to use `KM-Sandbox-Session`. Ctrl+C inside the live tmux pane should affect the running command without ending the SSM session.

**Operator steps:**

1. Run: `./km agent run <sandbox-id> --prompt "sleep 60" --interactive`
   (Opens a tmux session and attaches you live.)
2. Press **Ctrl+C** (one time) while the sleep is running.
   - Expected: sleep is interrupted; the bash prompt appears in the pane (because the tmux command is `'/tmp/km-agent-run.sh; exec bash'`).
   - The SSM session MUST remain open.
3. Press **Ctrl-B d** to detach. SSM session ends; you return to local prompt.

After completing the steps, type "approved" to resume execution, or describe the divergence.
  </action>
  <verify>
    <manual>Operator confirms: Ctrl+C interrupts the sleep command, the exec bash prompt appears in the pane, Ctrl-B d detaches cleanly.</manual>
    <resume-signal>Type "approved" if all expectations met; otherwise describe what happened.</resume-signal>
  </verify>
  <done>
    - Ctrl+C interrupts the sleep, bash prompt appears.
    - Ctrl-B d detaches and ends the SSM session.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: UAT — km shell --root regression check</name>
  <files>(none — manual interactive verification)</files>
  <action>
Phase 61 deliberately leaves the root path unchanged (it already uses Standard_Stream via the default doc). This task is a regression guard: confirm root path Ctrl+C still works as before.

**Operator steps:**

1. Run: `./km shell --root <sandbox-id>`
2. Run: `whoami` — expect `root` (or `ssm-user`, depending on profile).
3. Run: `sleep 100`
4. Press **Ctrl+C** (one time).
   - Expected: `^C` echoes, sleep interrupted, prompt returns.
   - SSM session MUST remain open.
5. Run: `exit`.

If anything diverges here it is a P0 regression because the root path was supposed to be untouched by this phase.
  </action>
  <verify>
    <manual>Operator confirms: root path behaves identically to pre-Phase-61 baseline (Ctrl+C interrupts foreground, session stays open, exit ends cleanly).</manual>
    <resume-signal>Type "approved" if all expectations met; otherwise describe what happened (any regression here is P0).</resume-signal>
  </verify>
  <done>
    - whoami returns root or ssm-user (per profile).
    - Ctrl+C interrupts sleep, session stays open.
    - `exit` ends the session cleanly.
    - No new regression vs. pre-Phase-61 behavior.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: UAT — Backwards-compat fail-fast in a doc-less region</name>
  <files>(none — manual verification with optional AWS state mutation)</files>
  <action>
Plan 02 added a fail-fast handler: when the SSM CLI returns InvalidDocument for KM-Sandbox-Session, km surfaces an actionable message instructing `km init <region>`. This is the operator's safety net for the cross-region rollout.

**Operator steps:** Choose ONE method.

**Method A (preferred — real AWS):** Find a region where the operator has at least one sandbox but has NOT yet run `km init <region>` since this phase merged.

```bash
./km list --region <region>     # confirm sandboxes exist
./km shell <sandbox-id>          # expect actionable error
```

Expected error message MUST contain all three substrings: `KM-Sandbox-Session not provisioned`, the region name, and `run \`km init`.

Then remediate and re-verify:
```bash
./km init <region>               # provisions the doc via Terragrunt
./km shell <sandbox-id>          # expect successful session
```

**Method B (synthetic — temporary delete):** If no doc-less region is available:

```bash
# Backup current doc content for rollback safety
aws ssm get-document --name KM-Sandbox-Session --region us-east-1 \
  --profile klanker-terraform > /tmp/km-sandbox-session-backup.json

# Delete the doc
aws ssm delete-document --name KM-Sandbox-Session --region us-east-1 \
  --profile klanker-terraform

# Confirm fail-fast
./km shell <sandbox-id>          # expect actionable error

# Restore via Terragrunt
cd /Users/khundeck/working/klankrmkr/infra/live/use1/ssm-session-doc
terragrunt apply -auto-approve

# Confirm restoration
./km shell <sandbox-id>          # expect successful session
```

Either method must surface an error message containing all three: `KM-Sandbox-Session not provisioned`, the region, and `km init`.

After completing the steps, type "approved" with the actual error string captured, or describe the divergence.
  </action>
  <verify>
    <manual>Operator confirms: error message contains "KM-Sandbox-Session not provisioned", the region name, and the suggestion "run `km init"; remediation via `km init` (or `terragrunt apply`) restores normal behavior.</manual>
    <resume-signal>Type "approved" with the captured error string; otherwise describe what was actually returned.</resume-signal>
  </verify>
  <done>
    - Missing-doc error message contains all three required substrings.
    - Re-running km init / terragrunt apply restores the doc.
    - Subsequent `km shell` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 8: Update phase SUMMARY with UAT log</name>
  <files>
    .planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-03-SUMMARY.md
  </files>
  <action>
Append a UAT outcome table to the plan SUMMARY:

```markdown
## UAT Outcomes

| UAT Scenario | Outcome | Notes |
|--------------|---------|-------|
| 2. km shell non-root + Ctrl+C | ✅ pass / ❌ fail | <evidence from operator response> |
| 3. km agent --claude + Ctrl+C | ✅ pass / ❌ fail | <evidence> |
| 4. km agent attach + Ctrl+C | ✅ pass / ❌ fail | <evidence> |
| 5. km agent run --interactive + Ctrl+C | ✅ pass / ❌ fail | <evidence> |
| 6. km shell --root + Ctrl+C (regression) | ✅ pass / ❌ fail | <evidence> |
| 7. Missing-doc fail-fast | ✅ pass / ❌ fail | <evidence + captured error string> |
```

Pull each row's outcome from the operator's `approved`/diagnosis response on Tasks 2-7.

If ANY scenario fails:
- Mark the phase status as `verification_gap` in `.planning/STATE.md` (do NOT mark the phase complete).
- Capture the failure as a gap in `.planning/phases/61-.../61-VERIFICATION.md` for `/gsd:plan-phase 61 --gaps` to address. Use the template:

```yaml
- truth: "<failed UAT truth>"
  reason: "<what diverged>"
  artifacts: ["<file paths likely involved>"]
  missing: ["<what to add/fix>"]
```

If ALL scenarios pass, append a one-line `UAT signed off YYYY-MM-DD by <operator>` footer.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-03-SUMMARY.md &amp;&amp; grep -q 'UAT' .planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-03-SUMMARY.md</automated>
  </verify>
  <done>
    - 61-03-SUMMARY.md contains a populated UAT outcome table.
    - All six scenarios are accounted for (pass or fail with evidence).
    - If failures exist, a VERIFICATION.md gap file is created for follow-up.
  </done>
</task>

</tasks>

<verification>
- All six manual UAT scenarios (Tasks 2-7) produce the expected behavior.
- Pre-flight automated check (Task 1) confirms infra and CLI are aligned.
- 61-03-SUMMARY.md is updated with a UAT outcome table and operator sign-off.
</verification>

<success_criteria>
- All four affected callsites verified end-to-end: Ctrl+C forwards as PTY byte (does NOT terminate the SSM session).
- Root path regression check passes (unchanged behavior).
- Missing-doc fail-fast surfaces an actionable message.
- UAT outcomes documented in the plan SUMMARY.
- If any scenario fails, gap closure is queued via VERIFICATION.md (not silently swallowed).
</success_criteria>

<output>
Create `.planning/phases/61-km-shell-ctrl-c-fix-switch-interactive-ssm-sessions-from-aws-startinteractivecommand-to-a-parameterized-standard-stream-document-with-runasdefaultuser-sandbox/61-03-SUMMARY.md` with the UAT outcome table and operator sign-off (or a VERIFICATION.md sibling if any scenario fails).
</output>
