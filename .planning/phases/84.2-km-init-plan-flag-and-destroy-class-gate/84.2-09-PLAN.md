---
phase: 84.2-km-init-plan-flag-and-destroy-class-gate
plan: 09
type: execute
wave: 2
depends_on:
  - 84.2-08
files_modified:
  - .planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md
autonomous: false
gap_closure: true
requirements:
  - DESTROY-CLASS-GATE
  - ACCEPT-DESTROYS-OVERRIDE
  - BOOTSTRAP-PLAN-PARITY
  - PLAN-FLAG
  - PLAN-OUTPUT-FORMAT
  - PLAN-ERROR-HANDLING
  - PROTECTED-TYPES-LIST

must_haves:
  truths:
    - "km init --plan exits 1 with trip block when aws_ses_receipt_rule would be destroyed (Scenario 2)"
    - "km init --plan --i-accept-destroys exits 0 with trip block printed (Scenario 3)"
    - "km bootstrap --shared-ses --plan trips gate on a foundation-owned protected resource without prevent_destroy (Scenario 4b)"
    - "km bootstrap --shared-ses --plan --i-accept-destroys exits 0 with trip block printed (Scenario 4c)"
    - "Validation doc Scenario 4b targets a resource without prevent_destroy (not aws_ses_receipt_rule_set)"
    - "Validation doc Scenario 6 trigger is km-config.yaml field absence, not shell env unset"
  artifacts:
    - path: ".planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md"
      provides: "Corrected Scenario 4b target + corrected Scenario 6 trigger"
      contains: "km-config.yaml"
  key_links:
    - from: "Scenario 2 UAT"
      to: "aws_ses_receipt_rule in ProtectedTypes (Plan 08)"
      via: "km init --plan against modified ses/v2.0.0/main.tf"
      pattern: "aws_ses_receipt_rule"
    - from: "Scenario 4b UAT"
      to: "foundation-owned resource without prevent_destroy"
      via: "km bootstrap --shared-ses --plan against modified foundation module"
      pattern: "aws_ses_active_receipt_rule_set|aws_ses_domain_dkim"
---

<objective>
Re-run UAT Scenarios 2, 3, 4b, 4c (deferred in 84.2-07 due to GAP-1) and correct two
validation doc prose errors (GAP-2, GAP-3).

Purpose: Confirm the end-to-end gate behavior works after the Plan 08 fix. Scenarios 2 and 3
test the blocking GAP-1 fix (aws_ses_receipt_rule now trips the gate). Scenarios 4b and 4c
test bootstrap parity using a foundation resource that does NOT have prevent_destroy, working
around the terraform error that blocked the original attempt. The doc corrections prevent future
operators from following incorrect instructions.

Output:
- UAT log confirming Scenarios 2, 3, 4b, 4c all PASS
- 84.2-VALIDATION.md with corrected Scenario 4b target and Scenario 6 trigger
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-CONTEXT.md
@.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md
@.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-07-UAT.md
@.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct validation doc prose (GAP-2 + GAP-3)</name>
  <files>.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md</files>
  <action>
Read the full 84.2-VALIDATION.md. Make two targeted prose corrections:

CORRECTION 1 — Scenario 4b (GAP-2):
Current: "Edit infra/modules/ses-shared-rule-set/v1.0.0/main.tf; km bootstrap --shared-ses --plan"
Problem: aws_ses_receipt_rule_set.shared has lifecycle.prevent_destroy=true, so a synthetic delete
edit causes terraform to ERROR before the gate evaluates — a different failure mode than the validation
doc projects (gate trip).

Read infra/modules/ses-shared-rule-set/v1.0.0/main.tf to find the right target. Choose a resource in
that file that (a) is a ProtectedType AND (b) does NOT have lifecycle.prevent_destroy=true. Likely
candidates: aws_ses_active_receipt_rule_set or aws_ses_domain_dkim. Document which one you picked.

Update the Scenario 4b command row in the validation table:
- Command: "Edit ses-shared-rule-set/v1.0.0/main.tf to comment out the <chosen resource> block
  (NOT aws_ses_receipt_rule_set — it has lifecycle.prevent_destroy=true); run
  km bootstrap --shared-ses --plan"
- Add a prose note in the row: "Note: aws_ses_receipt_rule_set.shared has lifecycle.prevent_destroy=true
  and cannot be used for a synthetic destroy test — terraform errors at plan time before the gate
  evaluates. Use <chosen resource> instead. The prevent_destroy is functioning as a first-line
  defense; the gate is a second-tier net."

CORRECTION 2 — Scenario 6 (GAP-3):
Current: "unset KM_ROUTE53_ZONE_ID; km init --plan"
Problem: km loads route53_zone_id from km-config.yaml via ExportTerragruntEnvVars, not from the
shell env. The shell unset has no effect on this install.

Update the Scenario 6 command row:
- Command: "Temporarily clear route53_zone_id in km-config.yaml (set to empty string or comment
  out the field); run km init --plan. Restore the field after the scenario."
- Add a prose note: "Note: km loads route53_zone_id from km-config.yaml via ExportTerragruntEnvVars,
  not from the shell env. Unsetting the KM_ROUTE53_ZONE_ID shell variable has no effect on this path."

Also update VALIDATION.md frontmatter:
- status: "updated-2026-05-17" (was "draft")
- nyquist_compliant: true (tests now exist for all validated behaviors)

Do NOT change any other rows or sections.
  </action>
  <verify>
    <automated>grep -c "km-config.yaml" .planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md</automated>
  </verify>
  <done>
    - Scenario 4b in VALIDATION.md references a ProtectedType resource without prevent_destroy
    - Scenario 6 in VALIDATION.md describes km-config.yaml field absence as the skip trigger
    - nyquist_compliant: true set in frontmatter
    - No other rows changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — re-run deferred Scenarios 2, 3, 4b, 4c</name>
  <action>
Claude runs make build and ./bin/km version to confirm the binary is fresh from Plan 08.
All four scenarios require real AWS state — only the operator can complete them.
  </action>
  <verify>
    <automated>./bin/km version 2>&1 | head -1</automated>
  </verify>
  <done>UAT log created at 84.2-09-UAT.md with all 4 scenarios PASS</done>
  <what-built>
Plan 08 fixed the GAP-1 blocking regression: aws_ses_receipt_rule is now in ProtectedTypes.
make build has been run (Plan 08 Task 3). The binary at ./bin/km is ready.

Task 1 (above) corrected the VALIDATION.md prose for Scenarios 4b and 6.

Now re-run the four deferred UAT scenarios. These require real AWS state and terragrunt invocations.
  </what-built>
  <how-to-verify>
**Pre-conditions (confirm before starting):**
- `make build` completed successfully (Plan 08 Task 3)
- `./bin/km version` shows the updated build
- Working tree is clean: `git status`

**Scenario 2 — Synthetic aws_ses_receipt_rule destroy trips gate:**

1. Edit `infra/modules/ses/v2.0.0/main.tf` — comment out the `aws_ses_receipt_rule.sandbox_catchall`
   resource block AND its corresponding output in `infra/modules/ses/v2.0.0/outputs.tf`.
2. Run: `km init --plan`
3. Expected:
   - Per-module summary shows `ses: 0 to add, 0 to change, 1 to destroy ⚠`
   - Trip block printed containing `aws_ses_receipt_rule.sandbox_catchall [DESTROY]`
   - Exit code: 1 (gate blocked)
4. Restore: `git checkout -- infra/modules/ses/v2.0.0/main.tf infra/modules/ses/v2.0.0/outputs.tf`

**Scenario 3 — Override clears exit code, trip block still printed:**

1. Apply the same edit as Scenario 2 (comment out sandbox_catchall + its output).
2. Run: `km init --plan --i-accept-destroys`
3. Expected:
   - Trip block printed (same addresses as Scenario 2)
   - Footer includes "override active via --i-accept-destroys — exit 0; no apply will run"
   - Exit code: 0
4. Restore: `git checkout -- infra/modules/ses/v2.0.0/main.tf infra/modules/ses/v2.0.0/outputs.tf`

**Scenario 4b — Bootstrap parity, synthetic destroy trips gate:**

1. Use the resource identified in Task 1 (a ProtectedType without prevent_destroy in
   infra/modules/ses-shared-rule-set/v1.0.0/main.tf). Comment out that resource block.
2. Run: `km bootstrap --shared-ses --plan`
3. Expected:
   - Trip block printed listing the targeted resource address and type
   - Exit code: 1
4. Restore the foundation module file.

**Scenario 4c — Bootstrap override:**

1. Apply the same Scenario 4b edit.
2. Run: `km bootstrap --shared-ses --plan --i-accept-destroys`
3. Expected:
   - Trip block printed
   - Override notice printed
   - Exit code: 0
4. Restore the foundation module file.

**Capture results** in `.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-09-UAT.md`
with the exact terminal output for each scenario (copy-paste the km output and exit codes).

**Phase closes when all 4 scenarios PASS.**
  </how-to-verify>
  <resume-signal>
Type "approved" with the UAT results, or describe any scenario failures with terminal output.
If Scenario 4b fails because no suitable non-prevent_destroy protected resource exists in the
foundation module, document which resource was tried and what happened — the gap may need a
different test approach (e.g., a dedicated unit test for the bootstrap path using a mock runner).
  </resume-signal>
</task>

</tasks>

<verification>
Doc correction automated check:
```bash
grep -c "km-config.yaml" .planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md
# Expected: >= 1 (Scenario 6 updated)

grep -c "prevent_destroy" .planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-VALIDATION.md
# Expected: >= 1 (Scenario 4b note added)
```

UAT scenarios verified by operator checkpoint (Scenarios 2, 3, 4b, 4c all PASS).
</verification>

<success_criteria>
- 84.2-VALIDATION.md has correct Scenario 4b target (not aws_ses_receipt_rule_set) and Scenario 6 trigger (km-config.yaml)
- UAT Scenario 2 PASS: km init --plan exits 1 with trip block for aws_ses_receipt_rule.sandbox_catchall
- UAT Scenario 3 PASS: km init --plan --i-accept-destroys exits 0, trip block printed with override notice
- UAT Scenario 4b PASS: km bootstrap --shared-ses --plan trips gate for a foundation protected resource
- UAT Scenario 4c PASS: km bootstrap --shared-ses --plan --i-accept-destroys exits 0 with override notice
- All modified module files restored to git HEAD after each scenario
</success_criteria>

<output>
After completion, create `.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-09-SUMMARY.md`
Also create `.planning/phases/84.2-km-init-plan-flag-and-destroy-class-gate/84.2-09-UAT.md` capturing the operator UAT results.
</output>
