---
phase: 84.2-km-init-plan-flag-and-destroy-class-gate
plan: 09
type: uat
status: passed
date_started: 2026-05-17
date_closed: 2026-05-17
operator: KPH (driven autonomously by Claude with user authorization)
aws_account: sandboxes.klankermaker.ai (Phase-84-completed)
aws_region: us-east-1
binary: km v0.2.656 (0b6ea1e — includes Plan 08 GAP-1 fix)
predecessor: 84.2-07-UAT.md (4 scenarios deferred under GAP-1)
---

# Phase 84.2 Operator UAT — Gap-Closure Re-Run

Re-runs the four scenarios deferred from `84.2-07-UAT.md` after Plan 08 closed GAP-1 (`aws_ses_receipt_rule` missing from ProtectedTypes).

## Environment

- km version: v0.2.656 (0b6ea1e) — rebuilt after Plan 08 lands `aws_ses_receipt_rule` in protected.go
- Working tree: clean before each scenario; restored after via `git checkout --`
- All edits are to module-source `.tf` files only; no AWS resources were applied

## Scenarios

### Scenario 2 — Synthetic protected destroy trips gate (re-run)

**Edit:** Commented out `aws_ses_receipt_rule.sandbox_catchall` in `infra/modules/ses/v2.0.0/main.tf` plus its matching output in `infra/modules/ses/v2.0.0/outputs.tf`.

**Command:** `./bin/km init --plan`

**Result:** PASS — exit 1, trip block fires correctly.

```
  Planning ses... 0 to add, 0 to change, 1 to destroy ⚠

✗ km init --plan would destroy 1 protected resources:

  ses:
    - aws_ses_receipt_rule.sandbox_catchall    [DESTROY]

These resource types are on the protected list because past incidents
caused unrecoverable data loss (see pkg/terragrunt/planreport/protected.go).

To proceed anyway, re-run with --i-accept-destroys (you must understand
why each resource is destroying — terragrunt apply will not ask again).

Error: destroy-class gate tripped (re-run with --i-accept-destroys to override)
exit=1
```

**Closes:** UAT GAP-1 from 84.2-07-UAT.md. Gate now catches the exact Phase 82→84 incident resource type.

---

### Scenario 3 — Override clears exit code (re-run)

**Edit:** Same as Scenario 2 (catchall still commented).

**Command:** `./bin/km init --plan --i-accept-destroys`

**Result:** PASS — exit 0, full trip block printed, explicit override notice, aggregate footer prints normally.

```
  Planning ses... 0 to add, 0 to change, 1 to destroy ⚠

✗ km init --plan would destroy 1 protected resources:

  ses:
    - aws_ses_receipt_rule.sandbox_catchall    [DESTROY]
  ...
  (override active via --i-accept-destroys — exit 0; no apply will run)

Total across 16 modules: 0 to add, 3 to change, 1 to destroy

Run 'km init --dry-run=false' to apply.
exit=0
```

Then `git checkout -- infra/modules/ses/v2.0.0/main.tf infra/modules/ses/v2.0.0/outputs.tf` → `git status infra/` clean.

---

### Scenario 4b — Bootstrap parity, synthetic destroy (re-run on `aws_ses_active_receipt_rule_set`)

**Plan 09 Task 1 correction:** Validation prose retargeted from `aws_ses_receipt_rule_set` (blocked by `lifecycle.prevent_destroy = true`) to `aws_ses_active_receipt_rule_set` — also in ProtectedTypes but without `prevent_destroy`.

**Edit:** Commented out `resource "aws_ses_active_receipt_rule_set" "shared"` (lines 100-105) AND its matching `import {}` block (lines 55-58) in `infra/modules/ses-shared-rule-set/v1.0.0/main.tf`.

**Command:** `./bin/km bootstrap --shared-ses --plan`

**Result:** PASS — exit 1, foundation-path gate trips correctly.

```
  Planning ses-shared-rule-set... 0 to add, 0 to change, 1 to destroy ⚠

✗ km init --plan would destroy 1 protected resources:

  ses-shared-rule-set:
    - aws_ses_active_receipt_rule_set.shared[0] [DESTROY]
  ...
Error: destroy-class gate tripped (re-run with --i-accept-destroys to override)
exit=1
```

---

### Scenario 4c — Bootstrap override (re-run)

**Edit:** Same as Scenario 4b.

**Command:** `./bin/km bootstrap --shared-ses --plan --i-accept-destroys`

**Result:** PASS — exit 0, trip block + override notice + apply-prompt footer.

```
  Planning ses-shared-rule-set... 0 to add, 0 to change, 1 to destroy ⚠

✗ km init --plan would destroy 1 protected resources:

  ses-shared-rule-set:
    - aws_ses_active_receipt_rule_set.shared[0] [DESTROY]
  ...
  (override active via --i-accept-destroys — exit 0; no apply will run)

Total across 1 modules: 0 to add, 0 to change, 1 to destroy

Run 'km bootstrap --shared-ses' (without --plan) to apply.
exit=0
```

Then `git checkout -- infra/modules/ses-shared-rule-set/v1.0.0/main.tf` → `git status infra/` clean.

---

## Cosmetic finding (non-blocking)

The trip-block headline reads `✗ km init --plan would destroy …` even when invoked via `km bootstrap --shared-ses --plan`. The aggregate footer correctly says `Run 'km bootstrap --shared-ses' …`, so the operator gets the right apply-time guidance. Filing as a small follow-up:

- **COSMETIC-1:** `printTripBlock` headline is hard-coded to "km init --plan". Should accept the invoking command name or be neutralized to "destroy-class gate would destroy …". One-line code change. Not gating Phase 84.2 close.

## Sign-off

**Operator:** KPH (work driven autonomously by Claude with explicit user authorization at start of UAT)
**Date:** 2026-05-17

| Scenario | Result |
|----------|--------|
| 2 — `aws_ses_receipt_rule` destroy trips gate | PASS |
| 3 — `--i-accept-destroys` clears exit code | PASS |
| 4b — Bootstrap parity, synthetic destroy on `aws_ses_active_receipt_rule_set` | PASS |
| 4c — Bootstrap parity, override | PASS |

**Verdict:** All four deferred scenarios PASS. The destroy-class gate now correctly catches the Phase 82→84 incident resource type at both the init and bootstrap paths, with the override behaving as designed. Phase 84.2 load-bearing promise (next 82→84-style incident cannot silently happen) is delivered.

The only outstanding item is the cosmetic `printTripBlock` headline wording (COSMETIC-1 above) — not a gate to phase close.
