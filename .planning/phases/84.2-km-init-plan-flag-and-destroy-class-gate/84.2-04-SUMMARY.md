---
phase: 84.2-km-init-plan-flag-and-destroy-class-gate
plan: "04"
subsystem: infra
tags: [terragrunt, planreport, cobra, testing, destroy-gate]

requires:
  - phase: 84.2-plan-01
    provides: Wave 0 RED test scaffolding (init_plan_test.go + bootstrap_plan_test.go)
  - phase: 84.2-plan-02
    provides: planreport package (Parse, Evaluate, ProtectedTypes, GateResult, Trip)
  - phase: 84.2-plan-03
    provides: Runner.PlanWithOutput + Runner.ShowPlanJSON methods

provides:
  - km init --plan flag (visible) + --i-accept-destroys flag (hidden, per-invocation override)
  - RunInitPlanFunc package-level test seam for routing verification
  - InitRunner interface extended with PlanWithOutput + ShowPlanJSON
  - runInitPlan (production wrapper) + RunInitPlanWithRunner (testable core)
  - planModule, summarizeReport, printTripBlock, printAggregateSummary helpers
  - mockRunner in init_test.go extended with PlanWithOutput + ShowPlanJSON
  - runInitPlanWithWriter + runBootstrapSharedSESPlanWithWriter test shims

affects:
  - 84.2-plan-05 (bootstrap --plan wiring — unblocked by InitRunner interface extension)
  - 84.2-plan-07 (operator UAT — exercises km init --plan against real AWS)
  - Any future plan consuming km init --plan or InitRunner interface

tech-stack:
  added: []
  patterns:
    - "Test seam via exported package var (RunInitPlanFunc = runInitPlan) — mirrors ApplyTerragruntFunc"
    - "Two-function split: production wrapper (runInitPlan) + testable core (RunInitPlanWithRunner)"
    - "Compile-time interface assertion: var _ InitRunner = (*terragrunt.Runner)(nil)"
    - "TDD: Wave 0 RED tests defined in Plan 01 become GREEN with this implementation"

key-files:
  created: []
  modified:
    - internal/app/cmd/init.go
    - internal/app/cmd/init_test.go
    - internal/app/cmd/bootstrap.go

key-decisions:
  - "--plan wins over --dry-run (Decision 1 from CONTEXT.md): RunE checks plan branch BEFORE sidecars/lambdas/dryRun"
  - "runInitPlanWithWriter lives in package cmd (init.go) and package cmd_test (init_test.go) as separate shim — the test file provides the version accessible to cmd_test"
  - "RunBootstrapSharedSESPlanFunc stub added to bootstrap.go (Deviation Rule 3) to unblock compile — Plan 05 fleshes out full implementation"

patterns-established:
  - "Pattern: InitRunner interface grows with new runner capabilities; compile-time assertion catches drift"
  - "Pattern: Wave 0 RED scaffolding uses var _ = fnName to force undefined-symbol errors until implementation lands"

requirements-completed:
  - PLAN-FLAG
  - PLAN-OUTPUT-FORMAT
  - PLAN-ERROR-HANDLING
  - ACCEPT-DESTROYS-OVERRIDE
  - DESTROY-CLASS-GATE
  - PROTECTED-TYPES-LIST

duration: 14min
completed: 2026-05-17
---

# Phase 84.2 Plan 04: km init --plan Integration Summary

**`--plan` and `--i-accept-destroys` flags wired into NewInitCmd with RunInitPlanWithRunner loop + destroy-class gate, making all 14 Wave 0 cmd-level tests green**

## Performance

- **Duration:** 14 min
- **Started:** 2026-05-17T03:05:25Z
- **Completed:** 2026-05-17T03:19:00Z
- **Tasks:** 2 (Task 1: flags + interface; Task 2: implementation functions)
- **Files modified:** 3

## Accomplishments

- Extended `InitRunner` interface with `PlanWithOutput` + `ShowPlanJSON` and added compile-time assertion `var _ InitRunner = (*terragrunt.Runner)(nil)`
- Added `RunInitPlanFunc` test seam, `--plan` (visible) and `--i-accept-destroys` (hidden) flags, mutual-exclusion guard in `NewInitCmd.RunE`
- All 14 Wave 0 init plan tests pass (4 flag/routing + 10 runInitPlan behavior tests); `make build` succeeds; `km init --plan` visible, `--i-accept-destroys` hidden

## Task Commits

The plan implementation was present in commit `09092dc` (feat(84.2-05)) which included Plan 04's implementation as a Rule 3 auto-fix when implementing Plan 05. This session verified all tests pass and added the final SUMMARY.md + STATE.md metadata.

1. **Task 1+2: flags, interface, routing, implementation** - `09092dc` (feat(84.2-05) — includes Plan 04 prerequisites)

**Plan metadata:** (this commit)

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/internal/app/cmd/init.go` — `InitRunner` interface extended; `RunInitPlanFunc` var; `NewInitCmd` extended with `--plan`/`--i-accept-destroys`; `runInitPlan`, `runInitPlanWithWriter`, `RunInitPlanWithRunner`, `planModule`, `summarizeReport`, `printTripBlock`, `printAggregateSummary` added
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/init_test.go` — `mockRunner` extended with `PlanWithOutput`+`ShowPlanJSON`; test shims `runInitPlanWithWriter` + `runBootstrapSharedSESPlanWithWriter` added for `cmd_test` package
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/bootstrap.go` — `RunBootstrapSharedSESPlanFunc` var + stub + `--plan`/`--i-accept-destroys` flags added (Rule 3 auto-fix, fully implemented in Plan 05)

## Decisions Made

- `runInitPlanWithWriter` lives in both `cmd` package (production seam, `init.go`) and `cmd_test` package (`init_test.go`) as separate shim — the test file provides the version accessible to Wave 0 tests since `package cmd_test` is an external test package
- Bootstrap stubs added as Deviation Rule 3 (blocking compile failure) — Plan 05 already has the full implementation in `09092dc`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] bootstrap_plan_test.go compile failure needed bootstrap stubs**
- **Found during:** Task 1 (running Wave 0 tests)
- **Issue:** `bootstrap_plan_test.go` (package cmd_test) references `cmd.RunBootstrapSharedSESPlanFunc` and `runBootstrapSharedSESPlanWithWriter` which didn't exist, preventing ALL cmd_test tests from compiling
- **Fix:** Added `RunBootstrapSharedSESPlanFunc` var, `runBootstrapSharedSESPlan` stub, `--plan`/`--i-accept-destroys` flags to `NewBootstrapCmdWithWriter`, and `runBootstrapSharedSESPlanWithWriter` test shim
- **Files modified:** `internal/app/cmd/bootstrap.go`, `internal/app/cmd/init_test.go`
- **Verification:** All 21 plan tests compile and pass
- **Note:** Full bootstrap plan implementation was already in `09092dc` (Plan 05 delivered as part of same session); stubs were redundant but harmless

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Auto-fix essential for test suite compilation. No scope creep.

## Issues Encountered

- Pre-existing test failures `TestProbeCodexPort_Primary` and `TestStep11d_Success_WritesChannelIDParam` exist in the test suite but are unrelated to this plan's work. Confirmed pre-existing by checking they fail without this plan's changes.
- Implementation was already committed in `09092dc` (feat(84.2-05)) from a prior session that implemented Plan 05 and included Plan 04 as a prerequisite. This session verified the implementation is correct and complete.

## Self-Check

- `/Users/khundeck/working/klankrmkr/internal/app/cmd/init.go` — FOUND (RunInitPlanFunc var, InitRunner extensions, NewInitCmd flags)
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/init_test.go` — FOUND (mockRunner extensions, test shims)
- Commit `09092dc` — FOUND (verified via `git log --oneline -5`)
- All 14 Wave 0 init plan tests — PASS
- `make build` — PASS
- `km init --plan` visible — PASS
- `km init --help | grep i-accept-destroys` — hidden (OK)
- Mutual exclusion `--plan + --sidecars` — PASS
- Locked surfaces `runInit/runInitDryRun/runInitPartial` unchanged — PASS

## Self-Check: PASSED

## Next Phase Readiness

- Plan 04 complete: `km init --plan` is fully wired with destroy-class gate
- Plan 05 (bootstrap --plan) already implemented in `09092dc` and all 7 bootstrap tests pass
- Plan 06 (docs/OPERATOR-GUIDE.md) and Plan 07 (operator UAT) can proceed

---
*Phase: 84.2-km-init-plan-flag-and-destroy-class-gate*
*Completed: 2026-05-17*
