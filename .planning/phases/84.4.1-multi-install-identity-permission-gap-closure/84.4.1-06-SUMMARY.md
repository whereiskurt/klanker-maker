---
phase: 84.4.1-multi-install-identity-permission-gap-closure
plan: 06
status: complete
completed: 2026-05-19 ~02:30 UTC
result: PASS
tasks_complete: 4
gaps_surfaced: 5
---

# Phase 84.4.1 Plan 06: Wave 4 UAT-2 + OPERATOR-GUIDE.md Update — Summary

## One-liner

Phase 84.4.1 multi-install thesis empirically proven via live UAT-2: fresh-prefix `tg` install on shared-domain account completed full lifecycle (configure → bootstrap → bootstrap --shared-ses → init → create sandbox → destroy → uninit → unbootstrap) on a sibling `klanker-maker-tg/` working directory while canonical km install remained fully operational (both `learn` and locked sandboxes verified running after tg teardown). 5 non-blocking gaps surfaced for follow-on phase.

## Commits

- `2ed408d` — feat(84.4.1-06): initialize tg UAT-2 before-snapshot + log scaffold
- `f0e374e` — docs(84.4.1-06): replace Phase 84.4 gaps+workarounds with clean 84.4.1 runbook
- `341a2e7` — docs(84.4.1-06): note km init --plan lambda zip prereq on fresh clones
- `(this commit)` — feat(84.4.1-06): UAT-2 PASS — fill closure criteria + AFTER snapshot

## Tasks executed

### Task 1 (autonomous) — BEFORE snapshot + log scaffold

**tg-uat-before.json** created by copying `km-after-84.4.1.json` from plan 05 (SUMMARY
notes km install was unchanged between Wave 3 completion and UAT-2 start). Updated
`meta.captured_at` to UAT-2 start time and added `meta.source` annotation.

**tg-uat-log.md** initialized with full 12-step scaffold covering:
- Pre-UAT canonical km install resource list (11 IAM roles, 2 S3 buckets, 2 hosted zones, SSM doc, SCP)
- Per-step instructions with exact commands, expected outputs, HALT conditions
- Per-step verdict fields (operator fills in during live run)
- Steps 3-8 and Steps 9-11 summary verdict tables
- Phase 84.4.1 Closure Criteria Verification table (10 rows, criteria a-j)
- HALT conditions for each load-bearing assertion:
  - Step 4: `InvalidChangeBatch` means Wave 1 plan 03 fix did not fire
  - Step 6: `DocumentAlreadyExists` means Wave 1 plan 02 fix did not fire
  - Step 7: SCP-deny errors mean Wave 1 plan 01 + Wave 3 plan 05 fix did not work
  - Step 11: km-Sandbox-Session missing after tg uninit means 84.4.1 thesis failed

### Task 2 (checkpoint:human-action) — Paused

Awaiting operator to set up klanker-maker-tg/ sibling directory and execute
Steps 3-8 (km bootstrap, bootstrap --shared-ses, km init --plan, km init,
km create, km destroy). See checkpoint message below.

### Task 3 (checkpoint:human-action) — Paused

Awaiting Steps 9-12 (km uninit, km unbootstrap, canonical km smoke test,
AFTER inventory snapshot + diff). See checkpoint message below.

### Task 4 (autonomous) — OPERATOR-GUIDE.md replacement

**Replaced** `### Phase 84.4 — Multi-install module hardening` gaps+workarounds
content (153 lines deleted) with clean `### Phase 84.4.1 — Multi-install:
second/third install bootstrap` runbook (115 lines added).

Deleted prose included:
- SCP AND-composition gap subsection with manual workaround
- ssm-session-doc shared-resource teardown gap with recovery procedure
- SES auto-import gap with manual terragrunt import workaround
- Second-install bootstrap sequence with 8 manual steps and inline caveats
- Companion teardown with 6 manual recovery steps
- Phase 84.4.1 scope forward-reference list

New runbook includes:
- 7-step quick start (no manual imports anywhere)
- Per-install resource naming explanation
- Pattern-based SCP trust design + security trade-off subsection
- DKIM auto-import behavior on shared-domain second install
- Fresh-clone DX: state_bucket default, region.hcl auto-prereq, DDB cleanup
- AWS limits: 5-SCP limit, 5,120-byte SCP guard, 1-2s SSM outage window
- Companion teardown with SCP manual detach/delete commands
- History + commit reference table (Phase 84 through 84.4.1 with dates)
- Updated Phase 84.3 cross-reference (84.4.1 complete)

## Artifacts

| Path | Status | Notes |
|------|--------|-------|
| `tg-uat-before.json` | Created | Equals km-after-84.4.1.json; UAT-2 baseline |
| `tg-uat-log.md` | Filled in | All 12 steps annotated with actuals; PASS verdict |
| `tg-uat-after.json` | Captured | Live snapshot of canonical km install post-tg-teardown |
| `OPERATOR-GUIDE.md` | Updated | Phase 84.4.1 clean runbook + km init --plan zip prereq note |

## Closure Criteria Status

| Criterion | Status | Notes |
|-----------|--------|-------|
| (a) ssm-session-doc/v2.0.0 per-install rename | PASS | tg-Sandbox-Session created cleanly; km-Sandbox-Session untouched after tg teardown |
| (b) SCP cross-install design + canonical apply clean | PASS | tg sandbox provisioned after orphan rg SCP detached; tg + km SCPs both `*-*` |
| (c) SES auto-import on shared-domain second install | PASS | tg bootstrap --shared-ses completed without manual terragrunt import |
| (d) make build-lambdas builds 6 zips | PASS | Verified during UAT Step 5 |
| (e) km bootstrap writes region.hcl as prereq | PASS | Fresh klanker-maker-tg/ clone bootstrapped without manual region.hcl |
| (f) km configure state_bucket UX | PASS | Default displayed during Step 2 |
| (g) km unbootstrap deletes DDB lock table | PASS | tg unbootstrap completed cleanly |
| (h) downloadTerraform stale cache invalidation | PASS (unit test) | Plan 04 unit test |
| (i) tg UAT-2 ends green + km shell on canonical works | PASS | Both km `learn` and locked sandbox confirmed running post-tg-teardown |
| (j) OPERATOR-GUIDE.md gaps+workarounds prose removed | PASS | Committed f0e374e + 341a2e7 (zip prereq addendum) |

## Live UAT findings (Gaps for follow-on phase)

Five gaps surfaced during the live UAT-2 run. None blocked the load-bearing closure
criteria; all warrant a follow-on gap-closure phase (84.4.2 or similar).

1. **`km init --plan` lambda-zip prereq missing** — `runInitPlan` doesn't call `buildLambdaZips`; full apply (`runInit`) does. Doc-note interim fix shipped in `341a2e7`. Real fix: add `buildLambdaZips(repoRoot)` to `runInitPlan` before the module loop (init.go:1212).

2. **`km configure` doesn't auto-derive artifacts_bucket default** — Phase 84.3 partial-pass gap `gap-validate-artifacts-bucket-not-wired-into-load` re-confirmed. Operator typed `tg-km-artifacts-use1-abcd0123` (placeholder pattern), validator accepted it. Fix: wire `deriveArtifactsBucket(prefix, applicationAccountID)` as the prompt default at configure.go:592 + strengthen validation.

3. **Orphan SCPs not auto-detached by km uninit/unbootstrap** — `rg-sandbox-containment` (p-tbyi97x6) from Phase 84.4 UAT-1 was still attached and BLOCKED tg-create-handler. Same pattern: `tg-sandbox-containment` (p-8in7702y) is now orphaned post-tg-teardown. Fix: `km uninit` detaches+deletes the install's SCP (with confirmation), OR `km doctor` warns on orphan SCPs.

4. **`validateArtifactsBucket` too narrow** — Rejects `<angle-brackets>` + `km-artifacts-12345` sentinel but accepted obvious placeholder `tg-km-artifacts-use1-abcd0123`. Fix: enforce `${prefix}-artifacts-${digit-account-id}` shape, or reject names with `abcd…` / suspicious-digit suffixes.

5. **Suspected TTL / "system in use" detection bug during km uninit** — Operator-flagged during teardown; needs reproduction + log capture. Not blocking; investigation TBD.

## Post-UAT residual artifacts (cleanup-tracked)

- `tg-sandbox-containment` SCP (p-8in7702y) — orphan from tg unbootstrap; matches gap #3 pattern
- `tg-km-artifacts-use1-abcd0123` S3 bucket — if created by earlier bootstrap before the value was corrected, would be an orphan; operator should check `aws s3 ls | grep tg-` and remove if present

## Deviations from Plan

### Auto-deviation: Task 4 executed before Tasks 2+3 (checkpoints)

Per the execution order in the plan, Task 4 is listed after the two
checkpoint tasks (Tasks 2 and 3). However, the plan objective note states
"Do what you CAN autonomously" and Task 4 is autonomous. The OPERATOR-GUIDE.md
update is independent of the live UAT outcome — it documents the working
happy path based on the code shipped in Waves 1-3, not the UAT results. This
reordering is not a deviation from plan intent.

### Note: tg-uat-before.json captured via copy, not live AWS snapshot

The plan specifies running `scripts/inventory-diff.sh snapshot` to capture
tg-uat-before.json. The objective prompt allows "copy or re-snapshot". Since
AWS credentials are not available in this execution context and the context_note
confirms km install is unchanged since Wave 3, tg-uat-before.json was copied
from km-after-84.4.1.json with updated metadata. The operator SHOULD re-run
`scripts/inventory-diff.sh snapshot` to capture the live state before starting
the tg UAT if the canonical km install state may have changed.

## Performance Metrics

- Autonomous prep: ~290s (~5 min)
- Live UAT-2 duration: ~3.5 hr (operator-driven, klanker-maker-tg/ fresh install)
- Tasks completed: 4 of 4
- Files created: 3 (tg-uat-before.json, tg-uat-after.json, tg-uat-log.md)
- Files modified: 1 (OPERATOR-GUIDE.md)
- Gaps surfaced for follow-on phase: 5

## Self-Check: PASSED
