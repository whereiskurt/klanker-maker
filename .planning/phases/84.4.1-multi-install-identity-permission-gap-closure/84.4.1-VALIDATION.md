---
phase: 84.4.1
slug: multi-install-identity-permission-gap-closure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-18
---

# Phase 84.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

Per RESEARCH.md §"Validation Architecture" (lines 1138-1195) — this file is the executable contract; RESEARCH.md is the source.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib) — same as Phase 84.4 |
| **Config file** | None — runs via `go test ./...` |
| **Quick run command** | `go test ./pkg/terragrunt/... ./internal/app/cmd/... ./internal/app/config/... -count=1` |
| **Full suite command** | `make test` (excludes 5 known-broken packages per `.planning/phases/84.4-multi-install-module-hardening/deferred-items.md` lines 1-43) |
| **Estimated runtime** | ~30-60 seconds for quick, ~2-3 minutes for full |

---

## Sampling Rate

- **After every task commit:** Run `make test`
- **After every plan wave:** Run `make test` + manual `terragrunt validate` on `infra/modules/scp/v2.0.0/` and `infra/modules/ssm-session-doc/v2.0.0/`
- **Before `/gsd:verify-work`:** Full `make test` must be green + UAT-2 Steps 1-11 PASS + km install byte-identical before/after
- **Max feedback latency:** ~60 seconds (quick run) / ~3 minutes (full)

---

## Per-Task Verification Map

Per-task IDs are assigned by gsd-planner during plan creation. This table maps each phase requirement to its verification command and the test file's existence status as of phase start.

| Requirement | Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| SSM-SESSION-DOC-PREFIX-NAMESPACING | `ssm-session-doc/v2.0.0/` exists, audit passes | Unit (HCL static analysis) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix -count=1` | ✅ (Phase 84.4 Plan 01 — auto-picks up new v2.0.0) | ⬜ pending |
| SSM-SESSION-DOC-PREFIX-NAMESPACING | `cfg.GetSandboxSessionDocumentName()` returns `${prefix}-Sandbox-Session` | Unit (Go) | `go test ./internal/app/config/ -run TestGetSandboxSessionDocumentName -count=1` | ❌ W0 | ⬜ pending |
| SSM-SESSION-DOC-PREFIX-NAMESPACING | km shell / km agent use config-derived doc name (5 callsites) | Unit (Go) | `go test ./internal/app/cmd/ -run TestShellCmd_UsesDocNameFromConfig -count=1` | ✅ (extend existing) | ⬜ pending |
| SSM-SESSION-DOC-PREFIX-NAMESPACING | Active session survives doc rename, new session retries cleanly | Manual UAT | UAT-2 Step 6 (run `km shell` during apply window) | N/A — operator | ⬜ pending |
| SCP-CROSS-INSTALL-COMPOSITION | `BuildSCPPolicyFromPrefix(any-prefix)` renders `*-*` patterns | Unit (Go) | `go test ./internal/app/cmd/ -run TestBuildSCPPolicy -count=1` | ✅ (`bootstrap_scp_test.go:20-61` — REWRITTEN, not extended) | ⬜ pending |
| SCP-CROSS-INSTALL-COMPOSITION | Audit passes for scp/v2.0.0 after pattern edit | Unit (static analysis) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix -count=1` | ✅ | ⬜ pending |
| SCP-CROSS-INSTALL-COMPOSITION | Canonical km install `terragrunt plan` shows ~6 in-place updates, 0 destroys | Manual UAT | Wave 2 operator-driven `terragrunt plan` on `infra/live/management/scp/` | N/A — operator | ⬜ pending |
| SCP-CROSS-INSTALL-COMPOSITION | Second install's create-handler is trusted by canonical km install's SCP | UAT-2 (live) | UAT-2 Step 7 (`km create` on `tg` succeeds without SCP edit) | N/A — operator | ⬜ pending |
| SES-AUTO-IMPORT-SHARED-DOMAIN | Auto-import gate fires when stateReader claims foundation owns identity | Unit (Go with fakes) | `go test ./internal/app/cmd/ -run TestRunBootstrapSharedSES_AutoImportFiresWhenStateOwnsIdentity -count=1` | ❌ W0 | ⬜ pending |
| SES-AUTO-IMPORT-SHARED-DOMAIN | autoImportFoundationSESRecords idempotency (re-run is no-op) | Unit | `go test ./internal/app/cmd/ -run TestRunBootstrapSharedSESAutoImport -count=1` | ✅ (`bootstrap_dkim_import_test.go`) | ⬜ pending |
| SES-AUTO-IMPORT-SHARED-DOMAIN | Fail-fast error message includes recovery `terragrunt import` command(s) | Unit (error string assertion) | `go test ./internal/app/cmd/ -run TestRunBootstrapSharedSES_FailFastRecoveryMessage -count=1` | ❌ W0 | ⬜ pending |
| SES-AUTO-IMPORT-SHARED-DOMAIN | UAT-2 second install bootstrap on shared domain — no manual import workaround | UAT-2 (live) | UAT-2 Step 4 (no manual `terragrunt import` workaround needed) | N/A — operator | ⬜ pending |
| LAMBDA-ZIP-MAKEFILE-PARITY | `make build-lambdas` produces 6 zips | Shell gate | `make build-lambdas && [ $(ls build/*.zip \| wc -l) -eq 6 ]` | ❌ W0 (Makefile target) | ⬜ pending |
| BOOTSTRAP-REGION-HCL-PREREQ | `runBootstrap` / `runBootstrapSharedSES` write region.hcl when missing | Unit (Go) | `go test ./internal/app/cmd/ -run TestRunBootstrap_WritesRegionHCL -count=1` | ❌ W0 | ⬜ pending |
| CONFIGURE-STATE-BUCKET-UX | state_bucket prompt shows computed default `[tf-${prefix}-state-${region}]` | Unit (Go) | `go test ./internal/app/cmd/ -run TestConfigure_StateBucketDefault -count=1` | ✅ (extend `configure_84_3_test.go`) | ⬜ pending |
| CONFIGURE-STATE-BUCKET-UX | HeadBucket-on-403 triggers `[Y/edit/abort]` retry UX | Unit (Go with mock S3) | `go test ./internal/app/cmd/ -run TestConfigure_StateBucketHeadBucketRetry -count=1` | ✅ (extend) | ⬜ pending |
| UNBOOTSTRAP-DDB-LOCK-CLEANUP | DDB lock table deletion called during unbootstrap | Unit (Go with mock DynamoDB) | `go test ./internal/app/cmd/ -run TestRunUnbootstrap_DeletesDDBLockTable -count=1` | ❌ W0 (new `UnbootstrapDynamoDBAPI` interface needed) | ⬜ pending |
| TERRAFORM-VERSION-CACHE-INVALIDATION | Cached terraform binary re-downloaded when version mismatches sidecar file | Unit (Go with tempdir) | `go test ./internal/app/cmd/ -run TestDownloadTerraform_CacheInvalidation -count=1` | ❌ W0 | ⬜ pending |
| TERRAFORM-VERSION-CACHE-INVALIDATION | `isPlaceholderBucket` accepts `km-artifacts-12345` (d551bba regression) | Unit (Go) | `go test ./internal/app/config/ -run TestConfigLoad_AcceptsLegacyBucketLiteral -count=1` | ✅ (d551bba) | ⬜ pending |
| FRESH-PREFIX-UAT-2 | tg install end-to-end without workarounds; km install byte-identical | E2E UAT | UAT-2 Steps 1-11 + `scripts/inventory-diff.sh diff` | N/A — operator-driven, 2-3 hours | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Per-task assignment (e.g., `84.4.1-02-03`) is added by gsd-planner during plan creation; this table is updated as task IDs become known.*

---

## Wave 0 Requirements

New test scaffolding that must exist before implementation tasks can be verified:

- [ ] `internal/app/config/config_test.go` — `TestGetSandboxSessionDocumentName` (or extend existing). Covers SSM-SESSION-DOC-PREFIX-NAMESPACING method on `cfg.GetSandboxSessionDocumentName()`.
- [ ] `internal/app/cmd/bootstrap_dkim_import_test.go` — `TestRunBootstrapSharedSES_AutoImportFiresWhenStateOwnsIdentity`. Covers SES-AUTO-IMPORT-SHARED-DOMAIN gate-separation fix.
- [ ] `internal/app/cmd/bootstrap_dkim_import_test.go` — `TestRunBootstrapSharedSES_FailFastRecoveryMessage`. Asserts error message contains exact `terragrunt import` command(s) operator should run.
- [ ] `internal/app/cmd/bootstrap_test.go` — `TestRunBootstrap_WritesRegionHCL` and `TestRunBootstrapSharedSES_WritesRegionHCL`. Covers BOOTSTRAP-REGION-HCL-PREREQ.
- [ ] `internal/app/cmd/unbootstrap_test.go` — `TestRunUnbootstrap_DeletesDDBLockTable` with mock DynamoDB client. Covers UNBOOTSTRAP-DDB-LOCK-CLEANUP. Requires new `UnbootstrapDynamoDBAPI` interface in `unbootstrap.go`.
- [ ] `internal/app/cmd/init_test.go` — `TestDownloadTerraform_CacheInvalidation` exercising sidecar version file logic. Covers TERRAFORM-VERSION-CACHE-INVALIDATION.
- [ ] `internal/app/cmd/configure_84_3_test.go` — extend `TestConfigure_StateBucketDefault` to assert computed default and `[Y/edit/abort]` retry. Existing scaffolding from Phase 84.3.
- [ ] `internal/app/cmd/bootstrap_scp_test.go` — REWRITE `TestBuildSCPPolicyPrefix` (lines 47-61) and `TestBuildSCPPolicySize` (lines 20-45) for `*-*` patterns. Backward-compat assertions change.
- [ ] `pkg/terragrunt/modulehygiene_test.go` — extend with v1.0.0 WARN-level scan + case-insensitive `(?i)km-` matching. Covers regression-prevention for the next ssm-session-doc-shape gap.
- [ ] `Makefile` — extend `build-lambdas:` target to produce all 6 zips (currently 4). No new test scaffolding — shell gate is `ls build/*.zip | wc -l == 6`.
- [ ] `infra/modules/ssm-session-doc/v2.0.0/` — new directory with `main.tf`, `variables.tf`, `outputs.tf`, plus `moved {}` block. `TestModuleNamesUseResourcePrefix` auto-picks it up once it exists.
- [ ] `infra/live/management/scp/terragrunt.hcl` — flip `trusted_role_arns` from `${prefix}-*` literals to `*-*` patterns (operator-level wiring change, complements module-level fix).

---

## Manual-Only Verifications

Behaviors that cannot be automated and require operator-driven UAT-2:

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| Canonical km install SCP body swap (sub-10s window) | SCP-CROSS-INSTALL-COMPOSITION | Real AWS Organizations policy state change; no API to simulate the deployed policy without applying | Wave 2: `cd infra/live/management/scp/ && terragrunt plan` — expect ~6 `update in-place` lines on Statement[].Condition.ArnNotLike values, 0 destroys. Apply, time the window. |
| ssm-session-doc destroy/create gap (1-2s window) | SSM-SESSION-DOC-PREFIX-NAMESPACING | Real SSM API state change; active session client cache behavior not unit-testable | Wave 2: open `km shell km-...` in one terminal; in another, `terragrunt apply` ssm-session-doc/v2.0.0 on the same install. Verify active session survives; new sessions during gap retry cleanly. |
| Fresh-prefix `tg` end-to-end lifecycle | FRESH-PREFIX-UAT-2 | Requires fresh git clone, fresh `km configure`, fresh AWS resources across 17 regional modules + sandbox create/destroy | UAT-2 Plan steps 1-11 in `84.4.1-XX-PLAN.md` (planner-assigned plan number). 2-3 hour operator session. AWS-CLI inventory diff before/after to prove km install byte-identical. |
| Bootstrap with no manual SES workarounds | SES-AUTO-IMPORT-SHARED-DOMAIN | Real SES domain identity + Route53 record state on shared-domain account; unit tests use fakes | UAT-2 Step 4: `./km bootstrap --shared-ses --dry-run=false` on tg install completes without operator running `terragrunt import` manually. |

---

## Validation Sign-Off

- [ ] All requirements have automated verify, UAT-2 step, or explicit Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (verified during plan creation)
- [ ] Wave 0 covers all ❌ W0 references in the verification map
- [ ] No watch-mode flags (`go test -count=1` used everywhere to bypass test cache)
- [ ] Feedback latency < 60s for quick run
- [ ] `nyquist_compliant: true` set in frontmatter (after plan creation + Wave 0 scaffolding implemented)

**Approval:** pending
