---
phase: 98-github-bridge-expansion
plan: 01
type: execute
wave: 2
depends_on: ["98-00"]
files_modified:
  - cmd/km-github/main.go
  - cmd/km-github/check_test.go
  - cmd/km-github/prcreate_test.go
  - pkg/compiler/userdata.go
autonomous: true
requirements: [GH-X-CHECK, GH-X-PRCREATE, GH-X-PUSH]
must_haves:
  truths:
    - "km-github check posts a check run (name + conclusion + summary) and exits 0; bad conclusion exits non-zero"
    - "km-github pr create opens a PR from a branch and prints the PR html_url to stdout"
    - "The GitHub poller preamble tells the agent to use a dedicated git worktree per PR and lists check + pr create + push verbs"
  artifacts:
    - path: "cmd/km-github/main.go"
      provides: "check + pr create verbs in dispatch()"
      contains: "runCheck"
    - path: "pkg/compiler/userdata.go"
      provides: "worktree-per-PR + verb-list preamble for github envelopes"
      contains: "worktree"
  key_links:
    - from: "cmd/km-github/main.go runCheckWith"
      to: "POST /repos/{owner}/{repo}/check-runs"
      via: "addGitHubHeaders + loadToken"
      pattern: "check-runs"
    - from: "cmd/km-github/main.go runPRCreateWith"
      to: "POST /repos/{owner}/{repo}/pulls"
      via: "addGitHubHeaders + loadToken"
      pattern: "/pulls"
---

<objective>
Add the remaining `km-github` write verbs — `check` (CI-style check run) and `pr create` (open a PR from a branch) — and harden the push-commit path by updating the sandbox poller preamble to instruct worktree-per-PR isolation and advertise the full verb set.

This is sandbox-side only: no Lambda, no IAM, no new infra. The installation token already carries `checks:write` + `pull_requests:write` + `contents:write` (Phase 97 `GitHubInboundWritePerms()` in pkg/github/token.go:228). The work is two new verb pairs cloned from the existing `runReviewWith` pattern, plus a preamble string change.

Purpose: Let the agent post pass/fail check runs and open PRs (turning an issue @-mention into a PR), and keep concurrent PRs isolated in a long-lived shared box.
Output: extended cmd/km-github/main.go (GREEN tests from 98-00), updated userdata preamble.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/98-github-bridge-expansion-richer-km-github-write-backs-check-runs-pr-create-push-commit-hardening-thread-session-continuity-for-follow-up-mentions-shared-alias-across-multiple-repos/98-RESEARCH.md

# Implement-against — read these:
@cmd/km-github/main.go
@cmd/km-github/check_test.go
@cmd/km-github/prcreate_test.go

<interfaces>
Existing dispatch (cmd/km-github/main.go:61) — add cases following the verbatim run<Verb>/run<Verb>With pattern:
```go
// main.go:61 dispatch()
case "comment": return runComment(args[1:], stderr)   // line 67
case "review":  return runReview(args[1:], stderr)     // line 69
// ADD:
case "check":   return runCheck(args[1:], stderr)
case "pr":
    if len(args) < 2 { usage(stderr); return 2 }
    switch args[1] {
    case "create": return runPRCreate(args[2:], stderr)
    default: usage(stderr); return 2
    }
```
Reuse: loadToken(stderr) (main.go:260), addGitHubHeaders(req, token) (main.go:250).
Check-run payload (RESEARCH Code Examples): POST /repos/{o}/{r}/check-runs {name, head_sha, status:"completed", conclusion:"success|failure|neutral", output:{title,summary}} → 201.
PR-create payload: POST /repos/{o}/{r}/pulls {title, head, base, body} → 201 {html_url, number}.

Preamble location: pkg/compiler/userdata.go — github inbound poller block (the source-aware drain near line 1114 `{{- if .GitHubInboundEnabled }}` and the km-github symlink at 1115-1119). The preamble string that builds the agent prompt for a github envelope is where the worktree + verb-list guidance belongs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement km-github check + pr create verbs (GREEN the 98-00 tests)</name>
  <files>cmd/km-github/main.go, cmd/km-github/check_test.go, cmd/km-github/prcreate_test.go</files>
  <behavior>
    - runCheckWith(repo, number/headSHA, name, conclusion, summary, token) POSTs to /repos/{owner}/{repo}/check-runs, 201 → exit 0.
    - conclusion ∉ {success, failure, neutral} → exit non-zero, no HTTP call.
    - missing head_sha → stderr warning + non-zero exit (per RESEARCH Pitfall 7; check requires head_sha or GitHub returns 422).
    - runPRCreateWith(repo, title, head, base, body, token) POSTs to /repos/{owner}/{repo}/pulls, 201 → prints html_url to stdout, exit 0.
    - missing required --title/--head/--base → usage, exit 2.
  </behavior>
  <action>
    Remove the `//go:build phase98_wave0` tag from check_test.go and prcreate_test.go (the 98-00 RED stubs) so they run in the normal suite.

    In cmd/km-github/main.go:
    1. Add `check` + `pr create` cases to dispatch() (see interfaces).
    2. Add runCheck(args, stderr): parse flags `--repo --number --name --conclusion --summary --head-sha`; validate conclusion against the allowed set (reject early, no HTTP, non-zero); validate head-sha non-empty (warn + non-zero if empty); loadToken; call runCheckWith.
    3. Add runCheckWith(...): build checkRunPayload{Name, HeadSHA, Status:"completed", Conclusion, Output:{Title,Summary}}; clone the exact HTTP shape of runReviewWith (main.go:196) — http.NewRequest POST, addGitHubHeaders(req, token), client.Do, treat 201 as success, non-2xx → print body to stderr + non-zero.
    4. Add runPRCreate(args, stderr): parse `--repo --title --base --head --body`; require title/base/head (usage+2 if missing); loadToken; call runPRCreateWith.
    5. Add runPRCreateWith(...): POST prCreatePayload{Title,Head,Base,Body} to /pulls; on 201 decode {html_url} and Fprintln it to STDOUT (the agent reads stdout); non-2xx → stderr + non-zero.
    6. Update usage() (main.go:81) to list `check` and `pr create` with their flags.

    Use the existing apiBaseURL/host derivation already in main.go (so the httptest stub in the tests intercepts correctly — check how runReviewWith/runCommentWith get their base URL and follow it identically).
  </action>
  <verify>
    <automated>go test ./cmd/km-github/... -run 'TestCheck|TestPRCreate' -count=1 -timeout 60s</automated>
  </verify>
  <done>TestCheck + TestPRCreate (and subtests) pass. check posts a check run and rejects bad conclusion / missing head_sha; pr create opens a PR and prints html_url to stdout. usage() lists both verbs.</done>
</task>

<task type="auto">
  <name>Task 2: Harden push path — worktree-per-PR + verb-list preamble (GH-X-PUSH)</name>
  <files>pkg/compiler/userdata.go</files>
  <action>
    The git credential helper (km-git-askpass / km-git-credential-helper, userdata.go:514-571) already makes `git push` work with the per-sandbox token — no code change needed there. GH-X-PUSH hardening is about the AGENT being told to use a dedicated worktree per PR so concurrent PRs in a long-lived shared box don't cross-contaminate.

    In pkg/compiler/userdata.go, locate the github-inbound poller's prompt-preamble construction (the source-aware drain block; the km-github install/symlink is at lines 1114-1119 — the envelope→prompt assembly is in the same poller). Update the preamble text emitted for a github envelope to include (mirroring the design spec § Sandbox poller + prompt preamble, lines 154-166):
    - "Fetch the PR into a DEDICATED git worktree: `git fetch origin pull/{number}/head` then `git worktree add ../pr-{number} FETCH_HEAD` — never work directly in the main checkout."
    - The full verb list: "Reply with the km-github CLI: `km-github review`, `km-github comment`, `km-github check --name … --conclusion success|failure|neutral --summary … --head-sha {head_sha}`, `km-github pr create --title … --base … --head …`. `git push` works directly via the credential helper."
    - Pass the envelope's head_sha/branch/number into the preamble so the agent has them.

    If there is an existing compiler test that snapshots/asserts the github preamble, update its expectation. If TestUserdataGitHubCredentialHelper exists (98-VALIDATION marks it "may exist"), confirm it still passes — the credential helper block is unchanged.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... -count=1 -timeout 120s && go build ./...</automated>
  </verify>
  <done>The github poller preamble instructs worktree-per-PR (`git worktree add`) and lists check + pr create + push. pkg/compiler tests pass; credential-helper block unchanged.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/km-github/... -count=1` green (check + pr create + existing comment/review).
- `go test ./pkg/compiler/... -count=1` green.
- `go build ./...` green.
- No new IAM / no new infra (token perms from Phase 97 already cover checks/pulls/contents write).
</verification>

<success_criteria>
- `km-github check` posts a check run with conclusion success/failure/neutral + summary; bad conclusion / missing head_sha exit non-zero.
- `km-github pr create --title/--base/--head/--body` opens a PR and prints its html_url to stdout.
- The github-envelope agent preamble instructs worktree-per-PR isolation and advertises check + pr create + push.
</success_criteria>

<output>
After completion, create `.planning/phases/98-github-bridge-expansion-richer-km-github-write-backs-check-runs-pr-create-push-commit-hardening-thread-session-continuity-for-follow-up-mentions-shared-alias-across-multiple-repos/98-01-SUMMARY.md`
</output>
