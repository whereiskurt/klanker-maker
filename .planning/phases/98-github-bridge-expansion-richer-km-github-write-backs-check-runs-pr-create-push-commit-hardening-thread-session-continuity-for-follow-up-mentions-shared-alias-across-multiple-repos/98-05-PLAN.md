---
phase: 98-github-bridge-expansion
plan: 05
type: execute
wave: 5
depends_on: ["98-01", "98-02", "98-03", "98-04"]
files_modified:
  - docs/github-bridge.md
  - internal/app/cmd/init_test.go
autonomous: false
requirements: [GH-X-E2E]
must_haves:
  truths:
    - "The full suite is green and the deploy surface is verified: new DDB module in regionalModules, bridge IAM matches runtime API calls, lambda-zip build-list intact, env vars present"
    - "docs/github-bridge.md documents the Phase 98 verbs, continuity, shared-alias, resume, and the cold-create + SOPS cold-box auth deploy sequence"
    - "An operator-run manual E2E confirms follow-up continuity, check run + opened PR, shared-alias dispatch, and stopped-alias auto-resume on real AWS + GitHub"
  artifacts:
    - path: "docs/github-bridge.md"
      provides: "Phase 98 operator runbook section"
      contains: "Phase 98"
  key_links:
    - from: "deploy-surface checklist"
      to: "regionalModules + bridge IAM + lambdaBuilds + env"
      via: "automated cross-checks"
      pattern: "dynamodb-github-threads"
---

<objective>
Close the phase: (1) an automated deploy-surface verification pass that encodes every Phase-97 deploy footgun as an assertion, (2) operator runbook docs for the Phase 98 additions and the cold-create/SOPS deploy sequence, and (3) a human checkpoint for the manual E2E (GH-X-E2E) that only a live AWS + GitHub setup can exercise.

This is the "code-green != deployable" gate (MEMORY feedback_verify_deploy_surface_not_just_code): Phase 97 passed unit tests but shipped a Lambda that was never deployed and a cold path that never worked. This plan asserts the deploy surface BEFORE the human runs the E2E.

Purpose: Prevent a repeat of Phase 97's 7 post-verification deploy gaps; give the operator a tested runbook; verify the end-to-end behavior on real infrastructure.
Output: deploy-surface assertions, docs/github-bridge.md Phase 98 section, signed-off manual E2E.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/98-github-bridge-expansion-richer-km-github-write-backs-check-runs-pr-create-push-commit-hardening-thread-session-continuity-for-follow-up-mentions-shared-alias-across-multiple-repos/98-RESEARCH.md
@.planning/phases/98-github-bridge-expansion-richer-km-github-write-backs-check-runs-pr-create-push-commit-hardening-thread-session-continuity-for-follow-up-mentions-shared-alias-across-multiple-repos/98-VALIDATION.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-VERIFICATION.md
@docs/github-bridge.md

<deploy_surface_checklist>
From RESEARCH Pitfall 8 + MEMORY footguns — assert each:
1. dynamodb-github-threads in regionalModules() (TestRegionalModulesIncludesGitHubThreads — GREEN from 98-04; re-assert here as the phase gate).
2. km-github-bridge still in lambdaBuilds() and km-github in sidecarBuilds() (init.go:1751, 1952) — a phase that changes the bridge must not drop it from the hardcoded build list.
3. Bridge IAM <-> runtime cross-check: every AWS API the bridge code calls has a matching IAM statement in lambda-github-bridge/v1.1.0 — dynamodb GetItem/PutItem/UpdateItem on km-github-threads (98-02), ec2:DescribeInstances + ec2:StartInstances (98-04), plus pre-existing alias-index Query, SQS Send, EventBridge PutEvents, KMS Decrypt, SSM github config.
4. KM_ARTIFACTS_BUCKET present in the bridge Lambda env block (needed for cold-create artifact_bucket).
5. Live unit infra/live/use1/lambda-github-bridge/terragrunt.hcl sources v1.1.0 (not v1.0.0).
6. github-review.yaml validates with spec.secrets.sopsFile.
</deploy_surface_checklist>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated deploy-surface verification pass</name>
  <files>internal/app/cmd/init_test.go</files>
  <action>
    Add a TestDeploySurfaceGitHubBridgePhase98 (table/sub-test style) to init_test.go that asserts the in-process, file-level deploy invariants (no live AWS):
    - regionalModules(regionDir) contains dynamodb-github-threads AND lambda-github-bridge, with github-threads ordered before the bridge.
    - lambdaBuilds() contains {name:"km-github-bridge"} and sidecarBuilds() contains {name:"km-github"}.
    - The lambda-github-bridge regional module's envReqs includes "KM_ARTIFACTS_BUCKET".
    - Read infra/modules/lambda-github-bridge/v1.1.0/main.tf from disk; assert it contains the strings "km-github-threads", "ec2:StartInstances", "ec2:DescribeInstances", "dynamodb:GetItem", "dynamodb:UpdateItem" (IAM<->runtime cross-check by grep — pragmatic, deterministic).
    - Read infra/live/use1/lambda-github-bridge/terragrunt.hcl; assert it sources "v1.1.0".

    Then run the FULL suite + build as the phase gate. Fix any cross-package breakage introduced by the wave merges (the bridge interface additions ripple to any callers/mocks). Resolve until the whole suite is green.

    Run terraform fmt on the new/changed modules and fix any HCL formatting.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestDeploySurfaceGitHubBridgePhase98|TestRegionalModulesIncludesGitHubThreads' -count=1 -timeout 60s && go build ./... && go test ./... -count=1 -timeout 180s</automated>
  </verify>
  <done>TestDeploySurfaceGitHubBridgePhase98 passes (module list, build lists, env, IAM<->runtime grep, live-unit version). Full go test ./... + go build ./... green. terraform fmt clean.</done>
</task>

<task type="auto">
  <name>Task 2: docs/github-bridge.md — Phase 98 operator runbook</name>
  <files>docs/github-bridge.md</files>
  <action>
    Add a "Phase 98 — richer write-backs, continuity, shared-alias, auto-resume, cold-create" section to docs/github-bridge.md covering:
    - New verbs: km-github check --name --conclusion success|failure|neutral --summary --head-sha; km-github pr create --title --base --head --body (prints PR url). Note worktree-per-PR guidance in the agent preamble.
    - Thread continuity: km-github-threads DDB table; follow-up @-mentions continue the same agent session; replies in a known PR/issue thread bypass the re-mention requirement (mirrors Slack Phase 91.3).
    - Shared alias: multiple github.repos entries -> one alias (one larger shared box, worktree-per-PR isolation); km doctor WARNs on alias collisions / match overlap.
    - Auto-resume: a stopped/paused aliased box is auto-resumed by an @-mention (bridge ec2:StartInstances); the queued prompt drains after boot. "Configure-once, stop, GitHub wakes it."
    - Cold-create (now working): bridge generates a valid sandbox_id + artifact_prefix=github-profiles/{slug}; km init pre-stages each profile (+ SOPS bundle) to S3; the cold box self-authenticates via SOPS-injected Claude credentials (NOT Bedrock). Document the operator step: encrypt github-review-secrets.enc.yaml (Claude creds) with the shared SOPS KMS key and place it at the path declared in github-review.yaml's spec.secrets.sopsFile.
    - Deploy sequence (encode the footguns): make build-lambdas (clean — avoids the init-skips-existing-zips trap), then km init --dry-run=false (new dynamodb-github-threads table + bridge IAM/env require a FULL apply, NOT --sidecars), then km init --sidecars (refresh create-handler if the poller/preamble changed), then existing sandboxes: km destroy <id> --remote --yes && km create <profile> to gain the new poller/verbs/continuity write.
    - Troubleshooting: cold-create artifact_prefix doubling (fixed), stopped-alias enqueue-to-dead-queue (fixed by resume), SOPS bundle missing -> cold box can't auth.

    Keep examples generic (example.com / Corporate / synthetic IDs) per project doc convention.
  </action>
  <verify>
    <automated>grep -q "Phase 98" docs/github-bridge.md && grep -qiE "km-github check|pr create" docs/github-bridge.md && grep -qiE "auto-resume|StartInstances" docs/github-bridge.md && grep -qiE "SOPS|sopsFile" docs/github-bridge.md && grep -qiE "make build-lambdas" docs/github-bridge.md</automated>
  </verify>
  <done>docs/github-bridge.md has a Phase 98 section covering check/pr-create verbs, continuity + thread-bypass, shared-alias, auto-resume, cold-create + SOPS auth, and the full deploy sequence with footguns. Examples are generic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual E2E verification (GH-X-E2E)</name>
  <what-built>
    The full Phase 98 GitHub bridge expansion, deployed to real AWS + GitHub:
    - km-github check + pr create verbs (sandbox-side)
    - thread/session continuity + thread-bypass (km-github-threads table)
    - shared-alias across repos + km doctor warnings
    - stopped-sandbox auto-resume (bridge ec2:StartInstances)
    - fixed cold-create with S3-staged profile + SOPS Claude-cred self-auth
    All unit tests + deploy-surface assertions are green; docs/github-bridge.md documents the deploy sequence.
  </what-built>
  <files>docs/github-bridge.md</files>
  <action>Operator deploys and runs the live E2E (this is a human-verify checkpoint — see how-to-verify for the exact deploy + scenario steps).</action>
  <how-to-verify>
    Deploy first (operator workstation, AWS_PROFILE=klanker-application):
      1. make build-lambdas    (clean build of bridge + sidecars)
      2. km init --dry-run=false    (applies dynamodb-github-threads + bridge v1.1.0 IAM/env — FULL apply)
      3. km init --sidecars    (refresh create-handler/poller)
      4. km doctor    (expect github checks healthy; verify no unexpected alias-collision WARN for your config)
      5. Encrypt a SOPS bundle with Claude creds (github-review-secrets.enc.yaml) per docs/github-bridge.md and place it where github-review.yaml references it.

    Then run the four E2E scenarios (per 98-VALIDATION.md Manual-Only) against a real repo where the App is installed:
      A. CONTINUITY: @-mention the bot on a PR; after it replies, post a follow-up WITHOUT re-mentioning. Confirm the reply references prior-turn context (same agent_session_id in km-github-threads; thread-bypass dispatched without a mention).
      B. WRITE-BACKS: trigger a request that runs km-github check and km-github pr create. Confirm the check run AND the opened PR appear in the GitHub UI.
      C. SHARED-ALIAS: configure two github.repos entries -> one alias; @-mention in each repo; confirm both dispatch to the SAME sandbox in separate worktrees.
      D. AUTO-RESUME: stop an aliased sandbox (km pause/stop); @-mention its repo; confirm the bridge resumes it (CloudWatch logs show StartInstances) and the poller drains the queued prompt after boot — no manual km resume. Confirm a single sandbox (no duplicate cold-create).
      E. COLD-CREATE (optional but ideal): @-mention a repo whose alias has NO sandbox; confirm create-handler provisions from the S3-staged profile, the box self-authenticates via SOPS Claude creds, and a review posts — fully automated.

    Also confirm NO regression to Phase 97 warm-path comment-trigger review.
  </how-to-verify>
  <verify>Human confirms all five E2E scenarios (continuity, write-backs, shared-alias, auto-resume, cold-create) and no Phase 97 regression.</verify>
  <done>Operator types "approved" after all scenarios pass on real AWS + GitHub; failures are captured for a --gaps plan.</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe which scenario failed and the observed behavior (CloudWatch log lines / GitHub UI state) so a --gaps plan can be drafted.</resume-signal>
</task>

</tasks>

<verification>
- TestDeploySurfaceGitHubBridgePhase98 + full go test ./... + go build ./... green.
- docs/github-bridge.md Phase 98 section present and complete.
- Human E2E sign-off received for continuity, write-backs, shared-alias, auto-resume (cold-create ideally).
- Phase 97 warm path unregressed.
</verification>

<success_criteria>
- Deploy surface verified by automated assertions (module list, IAM<->runtime, build lists, env, live-unit version).
- Operator runbook documents every Phase 98 capability + the full deploy sequence with footguns.
- Live E2E confirms follow-up continuity, check run + opened PR, shared-alias dispatch, and stopped-alias auto-resume.
</success_criteria>

<output>
After completion, create `.planning/phases/98-github-bridge-expansion-richer-km-github-write-backs-check-runs-pr-create-push-commit-hardening-thread-session-continuity-for-follow-up-mentions-shared-alias-across-multiple-repos/98-05-SUMMARY.md`
</output>
