---
phase: 113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - docs/operational-gotchas.md
  - CLAUDE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "scripts/validate-all-profiles.sh stays green (no schema change introduced by this phase)"
    - "On a freshly created sandbox, /opt/km/.km-profile.yaml exists and is byte-identical to the S3 copy at artifacts/{id}/.km-profile.yaml"
    - "The klanker:sandbox six-section census runs clean end-to-end on a live box (capabilities enumerated, network boundary confirmed by the 1 allowed + 1 blocked curl, sudo state correct)"
    - "On a pre-Phase-113 box (no profile file), the census degrades gracefully — never errors"
    - "Operators know the deploy surface: make build-lambdas + km init --dry-run=false + plugin bump + recreate existing sandboxes"
  artifacts:
    - path: "docs/operational-gotchas.md"
      provides: "Phase 113 entry: on-box profile file + census + deploy surface"
      contains: "/opt/km/.km-profile.yaml"
    - path: "CLAUDE.md"
      provides: "Phase 113 summary block + Where-to-look row for self-census"
      contains: "Phase 113"
  key_links:
    - from: "on-box /opt/km/.km-profile.yaml"
      to: "s3://$BUCKET/artifacts/{id}/.km-profile.yaml"
      via: "byte-diff during UAT (same *profile.SandboxProfile source)"
      pattern: "km-profile\\.yaml"
---

<objective>
Document the Phase 113 capability and verify it end-to-end on a live sandbox. Confirm the
on-box profile file lands, the six-section census runs clean, the network boundary is empirically
confirmed by the single allowed/blocked curl pair, and graceful degradation holds on an old box.
Record the deploy surface so operators can ship it.

There is NO redaction spec-review checkpoint — the profile is written VERBATIM (locked decision).
Do not add any redaction/sanitization gate.

Purpose: Prove the feature works on real infrastructure and leave operators a runbook.
Output: docs updates (operational-gotchas.md + CLAUDE.md) and a live UAT sign-off.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-CONTEXT.md
@.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-RESEARCH.md
@docs/superpowers/specs/2026-06-14-sandbox-self-awareness-design.md
@skills/sandbox/SKILL.md
@docs/operational-gotchas.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profiles still valid + document the deploy surface</name>
  <files>docs/operational-gotchas.md, CLAUDE.md</files>
  <action>
    1. Run `bash scripts/validate-all-profiles.sh` and confirm it exits 0 (no schema change was
       introduced — this is the smoke gate that proves the phase did not break the profile inventory).

    2. Add a Phase 113 entry to `docs/operational-gotchas.md` (or the appropriate self-awareness
       section) covering:
       - Every sandbox now writes its rendered profile to `/opt/km/.km-profile.yaml` (mode 0644,
         owned sandbox:sandbox), sourced from the same *profile.SandboxProfile that drives userdata
         (byte-identical to the S3 copy at `artifacts/{id}/.km-profile.yaml`).
       - The file is written VERBATIM (no redaction) — safe because the agent runs as the sandbox
         user and secrets are SSM paths (not values) already readable by the role.
       - The klanker:sandbox skill now runs a six-section self-census (A–F) and degrades gracefully
         on pre-Phase-113 boxes (file absent → env + probes, never errors).
       - The census includes exactly ONE safe-active allowed-vs-blocked curl pair — the only
         traffic-generating step; operators will see it in `km otel`.
       - DEPLOY SURFACE: `make build-lambdas` + `km init --dry-run=false` (create-handler carries
         the new userdata — NOT `--sidecars`, which does not rebuild the create-handler zip).
         Plugin version bump in `.claude-plugin/plugin.json` + `marketplace.json` for the skill
         content change. Existing sandboxes gain the file only on `km destroy && km create`.
         NO schema change, NO new TF resource, NO new DDB column, NO bridge Lambda change, NO IAM change.

    3. Add a Phase 113 summary block to `CLAUDE.md` (matching the existing "Phase NNN (date) — title"
       format used for Phases 104-109) AND a "Where to look" table row pointing to the
       self-census skill + the deploy notes.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && bash scripts/validate-all-profiles.sh 2>&1 | tee /tmp/p113_validate.log; test ${PIPESTATUS[0]} -eq 0 && grep -q '/opt/km/.km-profile.yaml' docs/operational-gotchas.md && grep -q 'Phase 113' CLAUDE.md</automated>
  </verify>
  <done>validate-all-profiles.sh exits 0; operational-gotchas.md documents the on-box profile + deploy surface; CLAUDE.md has a Phase 113 block + where-to-look row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — on-box profile present + six-section census + graceful degradation</name>
  <action>This is a blocking human-verify checkpoint — follow the <how-to-verify> steps below on a live sandbox. Do NOT add any redaction/sanitization step (verbatim is locked). Record results in 113-UAT.md.</action>
  <what-built>
    Platform: every EC2 sandbox writes `/opt/km/.km-profile.yaml` at boot (Plan 01, deployed via
    `make build-lambdas` + `km init --dry-run=false`). Skill: klanker:sandbox six-section self-census
    with graceful fallback (Plan 02). This checkpoint verifies the end-to-end behavior on a LIVE box.
  </what-built>
  <how-to-verify>
    PRE-REQ: Plan 01's userdata change must be deployed first:
      1. `make build-lambdas`
      2. `km init --dry-run=false`

    Then provision a fresh sandbox from a Slack-enabled profile (so capability/Slack sections have
    signal). Use AWS_PROFILE=klanker-application for the default install (project memory):

      3. `AWS_PROFILE=klanker-application km create <slack-enabled-profile> --alias p113uat`
         (a profile with notification.slack.enabled: true; e.g. a github-review or slack-notify profile)

    VERIFY A — file present + byte-identical to S3:
      4. `km shell <id>` then on-box: `cat /opt/km/.km-profile.yaml` (must exist, non-empty)
      5. From operator host, byte-diff against the S3 copy:
         `diff <(km shell <id> -- cat /opt/km/.km-profile.yaml) <(aws s3 cp s3://$KM_ARTIFACTS_BUCKET/artifacts/<id>/.km-profile.yaml -)`
         (expect: no diff / identical)

    VERIFY B — census runs clean end-to-end (sections A–F):
      6. In `km shell <id>`, run the klanker:sandbox census block. Confirm:
         - Section A: identity/agent printed
         - Section B: sidecar helpers + bridges + pollers + runtime features enumerated
         - Section C: enforcement mode inferred; EXACTLY two curls fire (one allowed → connect/200,
           one blocked → block/timeout); boundary confirmed
         - Section D: sudo state correct (matches spec.execution.privileged); restrictions explained
         - Section E: Slack publish-back readiness reported
         - Section F: one-paragraph posture summary printed
         - NO errors anywhere

    VERIFY C — graceful degradation on a pre-Phase-113 box (if one is available):
      7. On an OLD sandbox lacking `/opt/km/.km-profile.yaml`, run census Section A — it must fall
         back to env + probes and NEVER error. (If no old box exists, simulate by temporarily
         renaming the file: `sudo mv /opt/km/.km-profile.yaml /tmp/p && <run census> ; sudo mv /tmp/p /opt/km/.km-profile.yaml`.)

    CLEANUP:
      8. `km destroy <id> --remote --yes`

    Record results (and any failures with the exact stderr) in 113-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" once the on-box file is present + byte-identical, the six-section census runs clean (incl. the 1 allowed + 1 blocked curl), and graceful degradation holds — or describe the failures.</resume-signal>
</task>

</tasks>

<verification>
- `scripts/validate-all-profiles.sh` green.
- Live: `/opt/km/.km-profile.yaml` present and byte-identical to the S3 artifacts copy.
- Live: six-section census runs clean; exactly two curls (1 allowed + 1 blocked); sudo state correct.
- Live: graceful degradation when the file is absent.
- Docs updated (operational-gotchas.md + CLAUDE.md) with the deploy surface.
- NO redaction checkpoint exists (verbatim is locked).
</verification>

<success_criteria>
- End-to-end self-census verified on a live sandbox.
- Operators have a documented deploy + UAT runbook.
</success_criteria>

<deploy_notes>
Full deploy sequence for this phase: `make build-lambdas` + `km init --dry-run=false` (create-handler
carries the new userdata — NOT `--sidecars`); plugin version bump in `.claude-plugin/plugin.json` +
`marketplace.json` (Plan 02); existing sandboxes need `km destroy && km create` to gain the on-box
file. NO schema change, NO new TF/DDB/IAM/bridge-Lambda change.
</deploy_notes>

<output>
After completion, create `.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-03-SUMMARY.md` and a `113-UAT.md` recording the live verification results.
</output>
