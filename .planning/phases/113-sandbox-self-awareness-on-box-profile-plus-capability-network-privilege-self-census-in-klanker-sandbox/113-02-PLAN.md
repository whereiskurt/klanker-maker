---
phase: 113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - skills/sandbox/SKILL.md
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "An on-box agent invoking klanker:sandbox runs a six-section self-census (A identity, B capabilities, C network, D privilege, E Slack readiness, F posture summary)"
    - "The skill reads /opt/km/.km-profile.yaml (now real after Plan 01) and degrades gracefully — never errors — when the file is absent (pre-Phase-113 box)"
    - "The capability census enumerates which sidecar helpers / bridges / pollers / runtime features are present without a new binary (inline bash only)"
    - "The network section infers enforcement mode from runtime signals (no KM_ENFORCEMENT env var exists) and runs exactly ONE allowed + ONE blocked curl to confirm the boundary"
    - "The privilege section probes sudo -n true, cross-checks spec.execution.privileged, and EXPLAINS each restriction so the agent stops fighting locked-down behavior"
    - "Plugin version is bumped in BOTH plugin.json and marketplace.json so clients fetch the new skill (not the cached 0.4.8)"
  artifacts:
    - path: "skills/sandbox/SKILL.md"
      provides: "Six-section self-census skill with graceful profile-absent fallback"
      min_lines: 120
    - path: ".claude-plugin/plugin.json"
      provides: "version bumped 0.4.8 -> 0.4.9"
      contains: "0.4.9"
    - path: ".claude-plugin/marketplace.json"
      provides: "version bumped 0.4.8 -> 0.4.9 (matching plugin.json)"
      contains: "0.4.9"
  key_links:
    - from: "skills/sandbox/SKILL.md section B/C/D"
      to: "/opt/km/.km-profile.yaml"
      via: "cat with 2>/dev/null fallback to PROFILE_AVAILABLE=0"
      pattern: "cat /opt/km/\\.km-profile\\.yaml"
    - from: ".claude-plugin/plugin.json"
      to: ".claude-plugin/marketplace.json"
      via: "identical version string"
      pattern: "0\\.4\\.9"
---

<objective>
Rewrite `skills/sandbox/SKILL.md` from its current 106-line "detect env + email tooling" scope
into a structured six-section self-census (A–F) so an on-box agent can answer, in a single skill
invocation: who am I, what can I do, where do I sit on the network, and what is locked down and
why. Fix the previously-broken `cat /opt/km/.km-profile.yaml` reference (the file now exists after
Plan 01) with a graceful "file absent → pre-Phase-113 sandbox → env + probes only, never error"
fallback. Bump the plugin version in both manifests so clients pick up the new skill.

Purpose: Give the agent full self-knowledge grounded in both the declarative profile and live
runtime probes, replacing scattered guesswork.
Output: Rewritten SKILL.md + version bump in plugin.json and marketplace.json.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-RESEARCH.md
@.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-CONTEXT.md
@docs/superpowers/specs/2026-06-14-sandbox-self-awareness-design.md
@skills/sandbox/SKILL.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json

<onbox_signals>
<!-- Verified runtime inventory from RESEARCH §5. Use these EXACT names in the census bash. -->

Sidecar helper binaries at /opt/km/bin/ (presence → which bridges this box serves):
  km-send (always)  km-recv (always)  km-slack (always)  km-presence (always)
  km-github (ONLY when GitHub inbound enabled)  km-h1 (ONLY when H1 inbound enabled)

Systemd units (probe with `systemctl is-active <unit>`):
  km-http-proxy.service, km-dns-proxy.service, km-audit-log.service, km-tracing.service,
  km-presence.service (always); km-mail-poller.service (always for EC2);
  km-slack-inbound-poller.service (Slack inbound), km-github-inbound-poller.service (GitHub
  inbound), km-h1-inbound-poller.service (H1 inbound);
  km-ebpf-enforcer.service (ONLY when enforcement is ebpf or both).

Enforcement mode — NO `KM_ENFORCEMENT` env var exists on-box. Infer from runtime signals:
  EBPF_ACTIVE = `systemctl is-active km-ebpf-enforcer`
  IPTABLES_DNAT = `iptables -t nat -L OUTPUT -n 2>/dev/null | grep -c DNAT`
  both signals → "both"; ebpf only → "ebpf"; dnat only → "proxy"; neither → "unknown".
  Cross-check against `spec.network.enforcement` from the profile when available.

Proxy/network passive signals:
  HTTP_PROXY/HTTPS_PROXY env (set to http://127.0.0.1:3128 for proxy/both modes)
  custom CA at /usr/local/share/ca-certificates/km-proxy-ca.crt
  eBPF cgroup at /sys/fs/cgroup/km.slice/km-${KM_SANDBOX_ID}.scope/
  /etc/resolv.conf

Privilege:
  `sudo -n true 2>/dev/null` → passwordless sudo iff spec.execution.privileged: true
  cross-check profile `spec.execution.privileged`

KM_* env vars (from /etc/profile.d/km-*.sh): KM_SANDBOX_ID, KM_SANDBOX_ALIAS, KM_SANDBOX_EMAIL,
  KM_OPERATOR_EMAIL, KM_AGENT, KM_NOTIFY_SLACK_ENABLED, KM_SLACK_CHANNEL_ID, KM_SLACK_BRIDGE_URL,
  KM_SLACK_THREAD_TS, KM_GITHUB_INBOUND_QUEUE_URL, KM_H1_INBOUND_QUEUE_URL, KM_ALLOWED_REFS.

Profile fields the census reads (all from /opt/km/.km-profile.yaml):
  spec.network.enforcement, spec.network.<egress allowlist>, spec.execution.privileged,
  spec.sourceAccess/git allowedRefs, spec.iam.allowedSecretPaths, spec.runtime.vscode.enabled,
  spec.runtime.desktop.enabled, spec.budget.*, spec.lifecycle ttl/idle,
  spec.runtime.additionalVolume / additionalSnapshots, spec.email.* (signing/encryption/verifyInbound).
</onbox_signals>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md into the six-section self-census (A–F) with graceful fallback</name>
  <files>skills/sandbox/SKILL.md</files>
  <action>
    Rewrite `skills/sandbox/SKILL.md` in its entirety, PRESERVING the existing frontmatter style,
    the cross-references block, and the skill's existing voice/structure (imperative steps, bash
    blocks, tables). Update the `description:` frontmatter to reflect the broader scope
    (e.g. "Self-census: identity, capabilities, network position, privilege/restrictions, Slack
    readiness, posture summary"). Keep the "Invoke this skill first" guidance and the cross-links
    to klanker:email / klanker:slack / klanker:operator / klanker:init.

    The existing 4 steps (identity, email policy, tooling, signing key) are NOT deleted — they are
    folded into Section A (identity) and the capability census (Section B). Do not regress any
    existing guidance (external-email --no-sign, KM-AUTH inbound filter, signing-key/Slack shared key).

    Open with a graceful profile-read preamble (RESEARCH code example):
    ```bash
    KM_PROFILE=$(cat /opt/km/.km-profile.yaml 2>/dev/null)
    if [ -z "$KM_PROFILE" ]; then
      echo "[INFO] /opt/km/.km-profile.yaml absent — pre-Phase-113 sandbox"
      echo "[INFO] Falling back to env-var census + live probes only"
      PROFILE_AVAILABLE=0
    else
      PROFILE_AVAILABLE=1
    fi
    ```
    EVERY profile-derived line in sections B–F must be guarded on `PROFILE_AVAILABLE` and must
    NEVER error when the file is absent — fall back to env vars + live probes. This is the locked
    graceful-degradation decision.

    Author the six sections (use the verified <onbox_signals> names EXACTLY):

    A. Identity & agent — KM_SANDBOX_ID, alias, emails, operator email, KM_AGENT default. (Folds in
       current Step 1.) Include the "if KM_SANDBOX_ID empty → not in a KM sandbox, stop" guard.

    B. Capability census (inline bash, NO new binary):
       - Sidecar helpers present: loop over km-send km-recv km-slack km-github km-h1, `test -x
         /opt/km/bin/$bin`. Explain: presence of km-github / km-h1 → this box serves that bridge.
       - Channels wired: email policy (from profile spec.email or env); Slack post-back
         (KM_NOTIFY_SLACK_ENABLED / KM_SLACK_CHANNEL_ID / KM_SLACK_BRIDGE_URL); inbound pollers via
         `systemctl is-active km-slack-inbound-poller / km-github-inbound-poller / km-h1-inbound-poller`;
         transcript streaming if enabled.
       - Runtime features (profile, guarded on PROFILE_AVAILABLE): VS Code
         (spec.runtime.vscode.enabled), desktop (spec.runtime.desktop.enabled), budget/TTL/idle,
         additionalVolume / additionalSnapshots.

    C. Network position (passive + EXACTLY ONE safe-active pair):
       - Passive: infer enforcement via EBPF_ACTIVE + IPTABLES_DNAT (RESEARCH example — there is
         NO KM_ENFORCEMENT env var); cross-check spec.network.enforcement from profile; detect proxy
         sidecar (systemctl is-active km-http-proxy + HTTP_PROXY env), custom CA at
         /usr/local/share/ca-certificates/km-proxy-ca.crt, eBPF cgroup dir, /etc/resolv.conf;
         surface the egress allowlist from the profile.
       - Safe-active: EXACTLY TWO curls, clearly labeled "the only traffic-generating step
         (expect it in km otel)": one to a profile-declared ALLOWED host (expect connect/200) and
         one to a KNOWN-BLOCKED host (expect block/timeout). Use short `curl --max-time 5 -sS -o
         /dev/null -w '%{http_code}'` style; never more than these two requests; never a port-scan
         or traceroute (out of scope, locked).

    D. Privilege & restrictions (each EXPLAINED so the agent stops fighting it):
       - `sudo -n true 2>/dev/null` → passwordless sudo or not; cross-check spec.execution.privileged
         and WARN on mismatch (RESEARCH privilege-probe example).
       - Filesystem enforcement (writable/read-only paths), git-ref enforcement (allowedRefs /
         KM_ALLOWED_REFS), injected secret paths (spec.iam.allowedSecretPaths) — each with a
         one-line "why" (e.g. "no sudo because privileged: false; this is intentional, do not
         attempt to escalate").

    E. Slack publish-back readiness (concise) — confirm post-back env is populated
       (KM_NOTIFY_SLACK_ENABLED / KM_SLACK_CHANNEL_ID / KM_SLACK_BRIDGE_URL / KM_SLACK_THREAD_TS),
       then CROSS-LINK klanker:slack for the how-to. Do NOT duplicate klanker:slack content
       (locked — no klanker:slack change, no duplication).

    F. Self-diagnosis summary — instruct the agent to end with a one-paragraph posture statement:
       who am I, what can I do, where do I sit on the network, what is locked and why.

    Preserve the trailing "Identity Summary" intent inside Section F. Keep total length reasonable
    (~120-220 lines). Do NOT change klanker:slack/SKILL.md.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q 'cat /opt/km/.km-profile.yaml 2>/dev/null' skills/sandbox/SKILL.md && grep -q 'PROFILE_AVAILABLE' skills/sandbox/SKILL.md && grep -q 'systemctl is-active km-ebpf-enforcer' skills/sandbox/SKILL.md && grep -q 'sudo -n true' skills/sandbox/SKILL.md && grep -q 'km-github' skills/sandbox/SKILL.md && grep -q 'klanker:slack' skills/sandbox/SKILL.md && test $(grep -c '^##' skills/sandbox/SKILL.md) -ge 6</automated>
  </verify>
  <done>SKILL.md contains the graceful profile-read preamble, all six section anchors (A–F), enforcement inference via systemctl/iptables (no KM_ENFORCEMENT), the sudo probe, the bridge-helper census, and a klanker:slack cross-link with no duplication. No klanker:slack edit.</done>
</task>

<task type="auto">
  <name>Task 2: Bump plugin version in plugin.json AND marketplace.json (lockstep)</name>
  <files>.claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
    Bump the plugin version from "0.4.8" to "0.4.9" in BOTH files (project memory
    plugin_version_gates_cache — clients cache the old skill otherwise). The version strings MUST
    match exactly.
    - `.claude-plugin/plugin.json` line 4: `"version": "0.4.8"` → `"0.4.9"`.
    - `.claude-plugin/marketplace.json` line 11 (`.plugins[0].version`): `"version": "0.4.8"` → `"0.4.9"`.
    Change ONLY the version fields. Do not touch any other key. If "0.4.9" is already taken by a
    concurrent phase (110/111/112 were in flight), use the next free patch (0.4.10...) and keep
    both files identical.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && P=$(jq -r .version .claude-plugin/plugin.json); M=$(jq -r '.plugins[0].version' .claude-plugin/marketplace.json); echo "plugin=$P marketplace=$M"; test "$P" = "$M" && test "$P" != "0.4.8"</automated>
  </verify>
  <done>jq reports identical, non-0.4.8 version strings in plugin.json and marketplace.json (.plugins[0].version).</done>
</task>

</tasks>

<verification>
- SKILL.md grep checks pass (preamble, six section anchors, enforcement inference, sudo probe,
  bridge census, klanker:slack cross-link).
- `jq` version-match: plugin.json `.version` == marketplace.json `.plugins[0].version`, both != 0.4.8.
- klanker:slack/SKILL.md unchanged (verify `git diff --name-only` does NOT list skills/slack/SKILL.md).
- NO redaction/sanitization logic anywhere (skill reads the verbatim on-box file).
</verification>

<success_criteria>
- klanker:sandbox is a six-section self-census that degrades gracefully on pre-Phase-113 boxes.
- Plugin version bumped in lockstep so clients fetch the rewritten skill.
</success_criteria>

<deploy_notes>
Skill content change requires the plugin version bump in BOTH manifests (done in Task 2) — clients
cache the old skill otherwise (project memory plugin_version_gates_cache). The skill itself ships
with the plugin; no Lambda/terragrunt deploy is needed for the skill, but the on-box file it reads
requires Plan 01's userdata change to be live (`make build-lambdas` + `km init --dry-run=false`,
then `km destroy && km create` for existing boxes — see Plan 03 UAT).
</deploy_notes>

<output>
After completion, create `.planning/phases/113-sandbox-self-awareness-on-box-profile-plus-capability-network-privilege-self-census-in-klanker-sandbox/113-02-SUMMARY.md`.
</output>
