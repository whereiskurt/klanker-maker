---
phase: 85-doctor-orphan-state-lock-digest-sweeper-report-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["85-03"]
files_modified:
  - .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md
autonomous: false
requirements:
  - ACCEPT-READ
  - ACCEPT-WRITE

must_haves:
  truths:
    - "On the operator's current account (~275 orphan state-lock digest rows), `time km doctor` completes in ≤30s wall clock (vs. ~1:41 baseline)."
    - "`km doctor` output for the digest check is a single readable summary line + up to 10 inline previewed items + '… and N more (use --json for full list)' continuation."
    - "`km doctor --json` emits the full uncapped list of digest items (every one of the ~275 orphans is in the JSON array)."
    - "`km doctor --with-deletes --dry-run=false` deletes the ~275 orphan rows AND leaves every row whose S3 state object still exists untouched AND leaves the currently-running sandbox's lock row untouched."
    - "A second immediate `km doctor` after the cleanup pass reports 0 orphan rows (or only newly-leaked rows from interim activity)."
  artifacts:
    - path: ".planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md"
      provides: "Operator UAT log with timed runs, before/after lock-table scan counts, screenshot/transcript of doctor output, sign-off verdict"
      contains: "PASS | PARTIAL-PASS | FAIL"
  key_links:
    - from: "Plan 03 binary (`make build` artifact)"
      to: "Operator's live AWS account (lock table + state buckets)"
      via: "km doctor + km doctor --with-deletes --dry-run=false"
      pattern: "time km doctor"
---

<objective>
Operator UAT for Phase 85 against the live AWS account with ~275 accumulated orphan state-lock digest rows. Validate ACCEPT-READ (≤30s wall clock + readable output) and ACCEPT-WRITE (deletes only orphans, leaves live state locks untouched). Produce 85-04-UAT.md documenting timed runs, before/after snapshots, and sign-off verdict.

Purpose: All Plan 02 unit tests pass with mocks; this plan is the only check against REAL AWS-side behavior. The agent has no way to verify either acceptance criterion without operator hands on the keyboard — the perf target (≤30s) and the destructive write-path both require the operator's specific account state.

Output:
- .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md (signed UAT log)
- Git commit of the UAT log + any incidental fixes surfaced during testing
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/BRIEF.md
@.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-01-SUMMARY.md
@.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-02-SUMMARY.md
@.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-03-SUMMARY.md
@OPERATOR-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-UAT setup — fresh binary + before-snapshot of the lock table</name>
  <files>.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-lock-table.json, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-state-objects.json</files>
  <action>
1. Build a fresh binary using the project's standard build:
   ```
   make build
   ```
   (NOT bare `go build` — see CLAUDE.md feedback note: ldflags carry the version.)

2. Verify the binary exposes the new flag:
   ```
   ./bin/km doctor --help | grep -E "delete-state-digests|with-deletes"
   ```
   Expected: both lines present; the `--with-deletes` description mentions `--delete-state-digests`.

3. Capture a BEFORE-snapshot of the lock table for diffing:
   ```
   # Resolve the lock-table name for this install. Look up from km-config.yaml resource_prefix + region label.
   # Example for default install in us-east-1: tf-km-locks-use1
   LOCK_TABLE=$(./bin/km doctor --json 2>/dev/null | jq -r '.checks[] | select(.name == "Terraform state lock digest") | .message' | head -1)
   # OR: derive manually from km-config.yaml:
   #   tf-${KM_RESOURCE_PREFIX}-locks-${region_label}

   aws dynamodb scan --table-name <LOCK_TABLE_NAME> \
     --projection-expression "LockID" \
     --output json \
     > .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-lock-table.json
   ```

4. Capture the BEFORE count of S3 state objects under the state-bucket key prefix for cross-reference:
   ```
   STATE_BUCKET="<resolve from km-config.yaml — e.g. tf-km-state-use1>"
   aws s3api list-objects-v2 --bucket "$STATE_BUCKET" --query 'Contents[].Key' --output json \
     > .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-state-objects.json
   ```

5. Initialize `.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md` with a YAML header:
   ```yaml
   ---
   phase: 85
   slug: doctor-orphan-state-lock-digest-sweeper-report-cleanup
   uat_run_date: <today's date>
   operator: <operator email — from /etc/passwd or env>
   binary_version: <output of ./bin/km --version>
   verdict: pending
   ---
   ```

6. Note the before-state in the UAT log:
   - Lock-table item count (from `jq '.Items | length' uat-before-lock-table.json`)
   - State-object count (from `jq 'length' uat-before-state-objects.json`)
   - Expected orphan count (lock-table count where LockID ends in `-md5` minus state-object count) — this is the rough ground truth.

Capture all of the above in `85-04-UAT.md` under a `## Setup` section.
  </action>
  <verify>
    <automated>test -f ./bin/km && ./bin/km doctor --help | grep -q "delete-state-digests" && test -f .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-lock-table.json</automated>
  </verify>
  <done>
- Fresh `./bin/km` exists with the new flag.
- BEFORE snapshots of lock table and state bucket saved to disk under the phase directory.
- 85-04-UAT.md header + `## Setup` section written, including baseline counts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — timed read run + write run + after-snapshot</name>
  <files>.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-readrun.txt, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-dryrun.txt, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-writerun.txt, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-secondrun.txt, .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-after-lock-table.json</files>
  <what-built>
A new `--delete-state-digests` flag on `km doctor`, folded into `--with-deletes`, that uses parallel HeadObject + BatchWriteItem to clean orphan DDB lock rows whose S3 state object is definitively gone (s3types.NotFound). Output is summary + 10-item inline preview + `--json` for full list.
  </what-built>
  <action>
Run the following 6 steps against the operator's live AWS account. Pause for the operator's resume-signal at the end. Capture each step's output to the corresponding file in `<files>`.

**STEP 1 — Timed read run (ACCEPT-READ — perf target ≤30s):**

```
time ./bin/km doctor 2>&1 | tee .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-readrun.txt
```

Expected:
- Wall-clock real time ≤ 30 seconds (vs ~1:41 baseline).
- The "Terraform state lock digest" check shows a summary line like:
  `state digest mismatch in 275 item(s) (275 orphan: state object missing, 0 other)`
- Followed by up to 10 indented LockID lines.
- Followed by: `… and 265 more (use --json for full list)` (or similar).
- Suggested remediation line: `Re-run with --dry-run=false --delete-state-digests to delete orphan rows.`

If the run exceeds 30s, capture the timing and proceed — note as PARTIAL PASS in the UAT log with a hypothesis for the bottleneck. (Most likely cause: AWS account is throttling HeadObject, increase headWorkers, OR the lock-table scan is paginated across many pages — neither blocks the write path.)

**STEP 2 — JSON full-list verification (ACCEPT-READ — format):**

```
./bin/km doctor --json | jq '.checks[] | select(.name == "Terraform state lock digest")'
```

Expected: The check object's `.full_items` (or equivalent — read the actual key from the JSON) lists EVERY orphan LockID (no inline cap of 10 in JSON output). Cross-check the count matches the warn message's summary number.

NOTE: If the JSON does NOT include a full list field (i.e. renderSweeperMessage returned the uncapped list but it wasn't plumbed into the JSON output), capture this as a GAP for follow-up — the inline output format is still readable, so ACCEPT-READ partially passes.

**STEP 3 — Dry-run write preview (safety check before destructive run):**

```
./bin/km doctor --with-deletes --dry-run=true 2>&1 | tee .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-dryrun.txt
```

Expected: Same output as STEP 1 (no rows are deleted because --dry-run=true overrides the destructive intent). Confirms the destructive path is gated.

**STEP 4 — Destructive write run (ACCEPT-WRITE):**

CRITICAL: This step deletes DDB rows. Confirm there is at least one running sandbox before proceeding so we can verify the live-lock-row safety guarantee.

```
# Pre-flight: capture the current live-sandbox lock-row LockID (so we can verify it survives).
RUNNING_SID=$(./bin/km list | grep running | head -1 | awk '{print $1}')
echo "Live sandbox under test: $RUNNING_SID"
# Capture its expected LockID prefix from the state bucket:
aws s3 ls "s3://<STATE_BUCKET>/tf-<prefix>/sandboxes/$RUNNING_SID/" --recursive
```

Then:
```
time ./bin/km doctor --with-deletes --dry-run=false 2>&1 | tee .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-writerun.txt
```

Expected output (in the writerun log):
- Wall-clock real time ≤ 30s.
- The digest check message now reports `→ N deleted, 0 failed` (where N is approximately the BEFORE orphan count).
- Status remains `Terraform state lock digest: WARN` (something was wrong, even if cleaned).

**STEP 5 — After-snapshot + live-row safety verification:**

```
aws dynamodb scan --table-name <LOCK_TABLE_NAME> \
  --projection-expression "LockID" \
  --output json \
  > .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-after-lock-table.json

# Diff the before/after item counts:
BEFORE=$(jq '.Items | length' .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-before-lock-table.json)
AFTER=$(jq '.Items | length' .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-after-lock-table.json)
echo "Before: $BEFORE; After: $AFTER; Deleted: $((BEFORE - AFTER))"
```

Expected:
- AFTER count is significantly lower than BEFORE (approximately BEFORE minus the orphan count).
- The live sandbox's LockID (from STEP 4 pre-flight) is STILL present in the AFTER snapshot:
  ```
  jq -r '.Items[].LockID.S' .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-after-lock-table.json | grep "$RUNNING_SID"
  ```
  Must produce at least one line.

**STEP 6 — Second doctor run (idempotency check):**

```
time ./bin/km doctor 2>&1 | tee .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-secondrun.txt
```

Expected:
- Digest check now reports `state digest consistent` OR `state digest mismatch in <small N>` (only items that have leaked since STEP 4, typically 0-2).

**STEP 7 — Update 85-04-UAT.md** with:
- All 6 step transcripts (link to the captured .txt files)
- Wall-clock times for STEPS 1, 4, 6
- Before/after counts from STEP 5
- Confirmation the live sandbox lock row survived
- VERDICT: PASS / PARTIAL PASS (with gap list) / FAIL (with reason and rollback plan)

**ROLLBACK if FAIL:**
- The destructive step deleted rows but failed safety guarantees → the operator MUST manually restore deleted lock rows from the BEFORE snapshot:
  ```
  jq -r '.Items[].LockID.S' uat-before-lock-table.json > deleted-lockids.txt
  # For each deleted LockID, recreate the row (Digest can be re-computed by terragrunt on next apply).
  ```
- File an issue tracking the regression; pause Plan 04 sign-off until fixed.
  </action>
  <how-to-verify>
1. Run STEP 1 and observe wall-clock time printed by `time`. Confirm it is ≤30s and the digest-check summary line matches the format `state digest mismatch in N item(s) (...)`.
2. Run STEP 2 and confirm `--json` output includes the full uncapped list.
3. Run STEP 3 and confirm dry-run preview matches STEP 1 (no rows deleted).
4. Run STEP 4 and observe `→ N deleted, 0 failed` in the digest check.
5. Run STEP 5 and confirm AFTER count is approximately BEFORE minus the orphan count AND the live sandbox's LockID is still present.
6. Run STEP 6 and confirm digest check is now consistent (or near-zero remaining orphans).
7. Update 85-04-UAT.md with transcripts, times, counts, and a verdict line.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-readrun.txt && test -f .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-doctor-writerun.txt && test -f .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-after-lock-table.json</automated>
  </verify>
  <resume-signal>
Type one of:
- `approved` — UAT PASS, all 6 acceptance criteria green.
- `partial: <gap list>` — UAT PARTIAL, list gaps for follow-up phase (e.g., `partial: --json full-list not plumbed; perf passes at 25s; deletion works correctly`).
- `fail: <reason>` — UAT FAIL, describe what went wrong and whether rollback was needed.
  </resume-signal>
  <done>
- 6 step transcripts saved under .planning/phases/85-.../uat-*.{txt,json}.
- Wall-clock time captured for STEP 1 (read run), STEP 4 (write run), STEP 6 (second run).
- Before/after lock-table counts captured.
- Operator has signaled approved / partial / fail.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize 85-04-UAT.md and commit</name>
  <files>.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md</files>
  <action>
Based on the operator's resume signal from Task 2:

1. **If `approved`:** Update the UAT frontmatter `verdict: pass`. Add a `## Sign-off` section confirming all 6 steps green with wall-clock numbers. Commit with `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(85.04): operator UAT PASS — sweeper validated against ~275-orphan account" --files .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-*.json .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/uat-*.txt`.

2. **If `partial: <gaps>`:** Update verdict to `partial-pass`. Document each gap in a `## Gaps` section with severity, expected behavior, observed behavior, and proposed follow-up phase (likely Phase 85.1 if scope is large, or operator-followup if small). Commit.

3. **If `fail: <reason>`:** Update verdict to `fail`. Document the failure mode, rollback steps taken, and required fixes. DO NOT commit destructive AWS state changes as resolved — leave the phase in a known-failed state and signal for a gap-closure planning round (`/gsd:plan-phase 85 --gaps`).

Final state:
- 85-04-UAT.md frontmatter `verdict` set to one of: `pass | partial-pass | fail`.
- All transcript artifacts (uat-*.json, uat-*.txt) committed alongside the UAT log.
- For PASS or PARTIAL-PASS: phase is considered DONE; subsequent retro can run.
- For FAIL: phase is BLOCKED until follow-up.
  </action>
  <verify>
    <automated>grep -E "^verdict: (pass|partial-pass|fail)$" .planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-UAT.md && git log -1 --format="%s" | grep -qE "85\.04|85-04"</automated>
  </verify>
  <done>
- 85-04-UAT.md verdict frontmatter is one of pass/partial-pass/fail.
- UAT transcripts (uat-*.json, uat-*.txt) and the log itself are committed.
- Commit message references plan 85-04.
- Phase 85 is closed (PASS), partially-closed-with-followup (PARTIAL), or BLOCKED (FAIL).
  </done>
</task>

</tasks>

<verification>
- 85-04-UAT.md exists with valid frontmatter verdict.
- Six step transcripts captured (read run, dry-run, write run, before/after lock-table snapshots, second run).
- For ACCEPT-READ: wall-clock time for step 1 ≤ 30s documented.
- For ACCEPT-WRITE: before-count - after-count ≈ orphan-count documented; live sandbox lock row survives confirmed via grep against after-snapshot.
- For ACCEPT-WRITE safety: 0 rows from the live state-bucket key set were deleted (rows whose S3 object still exists are still in DDB after the cleanup).
- Commit includes UAT log + transcripts.
</verification>

<success_criteria>
- ACCEPT-READ: `time km doctor` ≤30s on the ~275-orphan account; output is the new readable format; `--json` emits the full list.
- ACCEPT-WRITE: `km doctor --with-deletes --dry-run=false` deletes ~275 orphan rows; live state mismatches are untouched; running sandbox's lock row is untouched.
- ACCEPT-TEST already passed in Plan 02; this plan does not re-run unit tests except as smoke (`go test ./internal/app/cmd/ -run TestDigestSweeper -v` should still be green if the operator wants a sanity check).
- 85-04-UAT.md verdict reflects reality with full transcript backing.
- Phase 85 cleanly closes or surfaces a documented gap-closure scope for Phase 85.1.
</success_criteria>

<output>
After completion, create `.planning/phases/85-doctor-orphan-state-lock-digest-sweeper-report-cleanup/85-04-SUMMARY.md` documenting:
- Final UAT verdict
- Wall-clock numbers (before/after baseline)
- Cleanup volume (rows deleted, rows preserved)
- Any gaps surfaced for follow-up
- Whether the upstream-leak follow-up (km destroy / km uninit deleting tfstate-md5 row) is now urgent or can wait
</output>
