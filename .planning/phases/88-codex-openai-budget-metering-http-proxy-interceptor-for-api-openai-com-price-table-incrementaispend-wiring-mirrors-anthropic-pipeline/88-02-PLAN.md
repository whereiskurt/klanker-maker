---
phase: 88
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - sidecars/http-proxy/httpproxy/http_proxy_test.go
autonomous: true
requirements:
  - OAI-BUDGET-05
  - OAI-BUDGET-06

must_haves:
  truths:
    - "http_proxy_test.go grows three integration RED tests asserting end-to-end OpenAI metering"
    - "TestOpenAIAIByModelIntegration proves IncrementAISpend writes SK=BUDGET#ai#{gpt-modelID} for OpenAI rows"
    - "TestHTTPProxy_OpenAIMetered proves goproxy CONNECT to api.openai.com is MITM'd + metered"
    - "TestTransparent_OpenAI proves transparent listener routes openai responses through meter (parity with Bedrock/Anthropic transparent path)"
  artifacts:
    - path: sidecars/http-proxy/httpproxy/http_proxy_test.go
      provides: "3 integration tests added — TestOpenAIAIByModelIntegration, TestHTTPProxy_OpenAIMetered, TestTransparent_OpenAI"
      contains: "TestOpenAIAIByModelIntegration"
  key_links:
    - from: sidecars/http-proxy/httpproxy/http_proxy_test.go
      to: sidecars/http-proxy/httpproxy/openai.go
      via: "Tests reference ExtractOpenAITokens, OpenAIBlockedResponse → 88-04 must export these"
      pattern: "httpproxy\\.(Extract|OpenAI)"
    - from: sidecars/http-proxy/httpproxy/http_proxy_test.go
      to: sidecars/http-proxy/httpproxy/proxy.go
      via: "TestHTTPProxy_OpenAIMetered hits the new openaiHostRegex handler block — 88-05 must register it"
      pattern: "WithBudgetEnforcement"
---

<objective>
Add three integration RED tests covering the end-to-end metering pipeline for OpenAI traffic — both MITM (`proxy.go`) and transparent (`transparent.go`) paths. This is Wave 0 — tests MUST fail until plans 88-04 and 88-05 land.

Purpose: Pin down the wire-level contract before any production code exists. The DynamoDB-stub-based AIByModel integration test is especially load-bearing — it proves that the existing `IncrementAISpend(ctx, client, table, sandboxID, modelID, in, out, cost)` call shape works identically for OpenAI model IDs (the third pillar of the must_haves: existing enforcer paths fire identically for OpenAI rows as for Claude rows).

Output: 3 tests appended to `http_proxy_test.go`. No new file (sticks with the convention — anthropic_test.go houses the AIByModel integration test for Anthropic, but http_proxy_test.go is where the goproxy-end-to-end tests live).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-CONTEXT.md
@.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-RESEARCH.md
@.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-VALIDATION.md

# Test patterns to copy
@sidecars/http-proxy/httpproxy/anthropic_test.go
@sidecars/http-proxy/httpproxy/http_proxy_test.go
@sidecars/http-proxy/httpproxy/proxy.go
@sidecars/http-proxy/httpproxy/transparent.go

<interfaces>
<!-- AIByModel test reuses captureModelIDStub. Plan 88-01 defines it in openai_test.go — but it already exists in anthropic_test.go:217-238 in the same package. Use the existing one (Go test files in the same package share symbols). -->

```go
// Already defined in anthropic_test.go:217-238 (same package httpproxy_test)
type captureModelIDStub struct { capturedSK string }
func (s *captureModelIDStub) UpdateItem(ctx, input, optFns) (*dynamodb.UpdateItemOutput, error)
func (s *captureModelIDStub) GetItem(...) (...)
func (s *captureModelIDStub) Query(...) (...)

// From pkg/aws/budget.go:65
func IncrementAISpend(ctx, client BudgetAPI, tableName, sandboxID, modelID string, inputTokens, outputTokens int, costUSD float64) (float64, error)

// From sidecars/http-proxy/httpproxy/proxy.go (existing — already used for Anthropic/Bedrock integration tests)
func NewProxy(...ProxyOption) *goproxy.ProxyHttpServer
func WithBudgetEnforcement(client aws.BudgetAPI, tableName, sandboxID string, modelRates map[string]aws.BedrockModelRate, cache *budgetCache) ProxyOption

// Symbols that DO NOT exist yet (88-04 / 88-05 create them):
func ExtractOpenAITokens(...) (modelID, in, out, cachedIn, reasoning int, err error)  // 88-04
func OpenAIBlockedResponse(req, sandboxID, modelID string, spent, limit float64) *http.Response  // 88-04
var openaiHostRegex = regexp.MustCompile(`^api\.openai\.com`)  // 88-05 in proxy.go
// meterOpenAIResponse method on TransparentListener — 88-05 in transparent.go
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestOpenAIAIByModelIntegration to http_proxy_test.go</name>
  <files>sidecars/http-proxy/httpproxy/http_proxy_test.go</files>
  <behavior>
    One test: `TestOpenAIAIByModelIntegration` (name must match VALIDATION.md row 88-05-01 `-run` filter exactly).

    Mirror `TestAnthropicAIByModelIntegration` (anthropic_test.go:189-214) verbatim, swapping:
    - sandbox ID: "sb-test"
    - model ID: "gpt-5.3-codex" (the most important Phase 88 model — Codex CLI default)
    - input tokens: 1000
    - output tokens: 500
    - cost: 0.0099 (1000 input * $0.00175/1K + 500 output * $0.014/1K = 0.00175 + 0.007 = 0.00875, but use 0.0099 to allow a small cache-discount fudge so the assertion stays deterministic; OR pass costUSD=0.00875 — researcher does not constrain the exact value, only the SK shape)

    Test calls `aws.IncrementAISpend(ctx, stub, "km-budgets", "sb-test", "gpt-5.3-codex", 1000, 500, 0.00875)`, asserts `stub.capturedSK == "BUDGET#ai#gpt-5.3-codex"`.

    Pattern requirement: the test PROVES the third must_have (existing enforcer paths fire identically for OpenAI rows). The exact same DynamoDB SK shape works for OpenAI as for Claude — no schema change.
  </behavior>
  <action>
    Append `TestOpenAIAIByModelIntegration` to `sidecars/http-proxy/httpproxy/http_proxy_test.go`.

    Reuse `captureModelIDStub` from anthropic_test.go (same package — symbols share). Imports needed (if not already imported): `"context"`, `"github.com/aws/aws-sdk-go-v2/service/dynamodb"`, `"github.com/whereiskurt/klanker-maker/pkg/aws"`. Check existing imports in http_proxy_test.go first — most should already be present from the existing tests.

    This test has NO direct dependency on 88-04 (it does not call `ExtractOpenAITokens` or any unexported openai.go symbol). It only proves the DynamoDB SK shape. **It will GREEN immediately on its own** — that's intentional, because it's testing the agent-agnostic budget.go path, not the new OpenAI code. The "RED" Wave 0 contract is satisfied by Tasks 2 and 3 below.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./sidecars/http-proxy/httpproxy/ -run TestOpenAIAIByModelIntegration -count=1 2>&1 | tail -10</automated>
    Expected: PASS (this is the one Wave 0 test that GREENs immediately, proving the agent-agnostic schema).
  </verify>
  <done>
    `TestOpenAIAIByModelIntegration` exists, passes, captures `BUDGET#ai#gpt-5.3-codex` SK. Proves OpenAI model IDs slot into the existing DynamoDB schema without modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestHTTPProxy_OpenAIMetered end-to-end test</name>
  <files>sidecars/http-proxy/httpproxy/http_proxy_test.go</files>
  <behavior>
    One test: `TestHTTPProxy_OpenAIMetered` (name matches VALIDATION.md row 88-05-02).

    Pattern: pure MITM end-to-end through `httpproxy.NewProxy(...)` with `WithBudgetEnforcement(stub, "km-budgets", "sb-test", staticOpenAIRates, cache)` option:

    1. Stand up an httptest.Server emulating api.openai.com — responds to `POST /v1/responses` with the Responses API SSE body from RESEARCH.md § Code Examples lines 308-321.
    2. Stand up `NewProxy(WithBudgetEnforcement(...))`.
    3. Make a CONNECT-then-POST through the proxy to api.openai.com.
    4. Assert: the response body streams through (client receives the SSE), AND the captureModelIDStub captured `BUDGET#ai#gpt-5.5` SK (proving the OnResponse handler fired and called IncrementAISpend).

    Pattern reference: there is currently NO Anthropic-equivalent test in http_proxy_test.go (researcher noted this in VALIDATION.md Wave 0 Gaps: "mirror existing Anthropic budget integration test if one exists; otherwise net-new"). This test IS net-new. Use `TestHTTPProxy_GitHubAllowedRepo` (http_proxy_test.go:360+) as the closest existing end-to-end-through-NewProxy pattern.

    Test name MUST match `-run TestHTTPProxy_OpenAIMetered` exactly.
  </behavior>
  <action>
    Append `TestHTTPProxy_OpenAIMetered` to `sidecars/http-proxy/httpproxy/http_proxy_test.go`. Test will fail with `undefined: httpproxy.openaiHostRegex` or similar UNLESS the test is written to depend only on EXPORTED symbols (preferred):

    - Don't reference `openaiHostRegex` (unexported).
    - Don't reference `staticOpenAIRates` directly — use `httpproxy.StaticOpenAIRates()` (the exported accessor 88-04 will create).

    Then the test fails at runtime: with no openaiHostRegex handler registered in proxy.go (until 88-05), the response is NOT MITM'd, the meteringReader is NOT wrapped around the body, and the captureModelIDStub.capturedSK stays empty. RED state at runtime, not compile time.

    Specifically:
    1. Build a synthetic httpserver that returns the SSE body.
    2. Configure goproxy via `httpproxy.NewProxy(...)` with `WithBudgetEnforcement(stub, "km-budgets", "sb-test", httpproxy.StaticOpenAIRates(), httpproxy.NewBudgetCache(...))`.
    3. Use the goproxy server as an HTTP CONNECT proxy from a test http.Client.
    4. POST to `https://api.openai.com/v1/responses` (with rewriting tricks per existing tests — see TestHTTPProxy_GitHubAllowedRepo for the host-rewrite pattern).
    5. Drain response body to trigger meteringReader EOF.
    6. Assert `stub.capturedSK == "BUDGET#ai#gpt-5.5"` (will FAIL with empty string until 88-05 wires the handler).

    Implementation note: this test will be ~80 LoC. Cap budget — if exceeds 100 LoC, split out a helper function (`newOpenAIMockServer(t, body string) *httptest.Server`) and stash it in the same file just above the test.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./sidecars/http-proxy/httpproxy/ -run TestHTTPProxy_OpenAIMetered -count=1 2>&1 | tail -20</automated>
    Expected EITHER: compile failure (undefined `StaticOpenAIRates`) — fine, RED state; OR runtime assertion failure ("capturedSK = \"\", want \"BUDGET#ai#gpt-5.5\"") — also RED state.

    GREEN state requires BOTH 88-04 (StaticOpenAIRates accessor) AND 88-05 (proxy.go handler registration). This test gates both.
  </verify>
  <done>
    `TestHTTPProxy_OpenAIMetered` exists, fails in RED state. Test contract pins down the requirement that 88-05's proxy.go handler MUST register against `api.openai.com` AND wrap the response body in a meteringReader that fires IncrementAISpend.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TestTransparent_OpenAI for transparent-listener parity</name>
  <files>sidecars/http-proxy/httpproxy/http_proxy_test.go</files>
  <behavior>
    One test: `TestTransparent_OpenAI` (name matches VALIDATION.md row 88-06-01).

    Note from researcher (VALIDATION.md row 88-06-01): "transparent mode currently has no Anthropic test either — add at least one as defense". So this test is net-net new for OpenAI AND establishes the transparent-listener test pattern that future Anthropic regressions could use.

    Pattern:
    1. Construct a `TransparentListener` (the exported type — check transparent.go for the constructor name; likely `NewTransparentListener(...)`).
    2. Build a synthetic request representing an inbound TLS-terminated GET/POST to `api.openai.com:443`.
    3. Build a synthetic response with the OpenAI Responses API SSE body.
    4. Invoke the `meterOpenAIResponse` flow either directly (if exported as method) or via the public `relayWithInspection` entry point (if methods unexported, drive it through an inbound pipe pair like the other transparent tests).
    5. Assert: budget stub captured `BUDGET#ai#gpt-5.5` SK; response bytes wrapped correctly.

    Caveat: if the transparent listener uses entirely unexported methods AND there are no existing transparent_test.go integration tests, write the test against the smallest exported surface OR add a minimal exported test hook. Don't add a permanent test-only export to non-test code — instead, place the test in `package httpproxy` (not `httpproxy_test`) so it can reach unexported symbols.
  </behavior>
  <action>
    First, check the transparent.go surface: `grep -n "^func.*TransparentListener\|^func NewTransparent" /Users/khundeck/working/klankrmkr/sidecars/http-proxy/httpproxy/transparent.go`. Identify the constructor + the smallest entry point that triggers the meter path.

    Two options:

    **Option A (preferred):** Test `package httpproxy_test` (external), drive via the public listener Listen()+net.Dial dance. Mirrors how `relayWithInspection` would be exercised in production. ~120 LoC.

    **Option B (fallback):** Test `package httpproxy` (white-box), call the unexported `meterOpenAIResponse` method on a TransparentListener instance directly. ~50 LoC. Acceptable because transparent.go is internal package code, not public API.

    Implementer chooses based on the constructor surface. Document choice in a `// NOTE:` comment in the test.

    Test name MUST match `-run TestTransparent_OpenAI` exactly. Will fail at compile time (undefined `meterOpenAIResponse`) until 88-05 lands.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./sidecars/http-proxy/httpproxy/ -run TestTransparent_OpenAI -count=1 2>&1 | tail -20</automated>
    Expected: compile failure (`undefined: meterOpenAIResponse` if white-box, or runtime "capturedSK empty" if black-box). RED state.
  </verify>
  <done>
    `TestTransparent_OpenAI` exists. Pins down the requirement that 88-05's transparent.go edits MUST add an `isOpenAI := openaiHostRegex.MatchString(host)` branch + `meterOpenAIResponse` method mirroring `meterAnthropicResponse` (transparent.go:403-432).

    Wave 0 plan 88-02 complete: 3 integration tests scaffolded, 1 passes (AIByModel), 2 fail RED.
  </done>
</task>

</tasks>

<verification>
- `go test ./sidecars/http-proxy/httpproxy/ -run TestOpenAIAIByModelIntegration -count=1` → PASS (proves agent-agnostic schema)
- `go test ./sidecars/http-proxy/httpproxy/ -run TestHTTPProxy_OpenAIMetered -count=1` → FAIL (RED — handler not registered)
- `go test ./sidecars/http-proxy/httpproxy/ -run TestTransparent_OpenAI -count=1` → FAIL (RED — meterOpenAIResponse undefined)
- All three test names align with VALIDATION.md per-task verify-map rows 88-05-01, 88-05-02, 88-06-01
</verification>

<success_criteria>
- 3 integration tests added to `http_proxy_test.go` (file already exists — append only, do not rewrite)
- Test names match VALIDATION.md `-run` filters exactly
- TestOpenAIAIByModelIntegration GREENs on its own (no production-code dependency — proves schema)
- TestHTTPProxy_OpenAIMetered + TestTransparent_OpenAI are RED, pinning down 88-04 + 88-05 contracts
- No new files created — `http_proxy_test.go` is the canonical home for goproxy-end-to-end tests
</success_criteria>

<output>
After completion, create `.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-02-SUMMARY.md` per the GSD summary template.
</output>
