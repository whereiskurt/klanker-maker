---
phase: 88
plan: 07
type: execute
wave: 2
depends_on:
  - 88-04
  - 88-05
  - 88-06
files_modified:
  - CLAUDE.md
  - .planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-07-UAT.md
autonomous: false
requirements:
  - OAI-BUDGET-09

must_haves:
  truths:
    - "make sidecars produces a new http-proxy binary embedded into the S3 sidecar zip used by km create"
    - "km init --sidecars deploys the new binary into the live management region"
    - "A fresh Codex sandbox creating real api.openai.com traffic produces a BUDGET#ai#gpt-5.x DynamoDB row with spentUSD > 0"
    - "CLAUDE.md mentions OpenAI metering alongside the existing Bedrock + Anthropic metering"
  artifacts:
    - path: 88-07-UAT.md
      provides: "Operator runbook for live sandbox verification — 4 scenarios"
      contains: "Scenario 1"
    - path: CLAUDE.md
      provides: "Mentions Codex/OpenAI metering in the Budget Enforcement subsection"
      contains: "OpenAI"
  key_links:
    - from: 88-07-UAT.md
      to: DynamoDB km-budgets table
      via: "Operator runs km status post-test; UAT runbook calls out exact DDB query"
      pattern: "BUDGET#ai#gpt-"
---

<objective>
Deploy the new http-proxy sidecar binary, run a live UAT against a Codex sandbox, and update CLAUDE.md to mention OpenAI metering alongside Bedrock + Anthropic.

Purpose: Wave 0 + Wave 1 verify correctness via unit + integration tests with stubs. Only a live sandbox can prove the chain end-to-end: real Codex CLI → real api.openai.com → real proxy interception → real DynamoDB write. This plan delivers must_have #1 in production conditions.

Output: Deployed binary + 1 UAT runbook + 1 CLAUDE.md edit. NOT autonomous — Task 2 is a `checkpoint:human-verify` (user must run live sandbox and confirm DDB rows appear).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-CONTEXT.md
@.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-VALIDATION.md

# Reference: CLAUDE.md conventions (make sidecars, km init --sidecars, agent: codex)
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy sidecars via make sidecars + km init --sidecars</name>
  <files>(no source files modified — deploy step)</files>
  <action>
    Three sequential commands. Each must succeed before proceeding.

    1. **Build:** `cd /Users/khundeck/working/klankrmkr && make sidecars 2>&1 | tail -30`
       - This invokes the Makefile target that compiles all sidecar binaries (http-proxy, dns-proxy, audit-log, tracing, km-presence) and uploads them to the artifacts S3 bucket.
       - Verify the http-proxy binary timestamp updated: `aws s3 ls s3://${KM_ARTIFACTS_BUCKET}/sidecars/http-proxy/` (use the bucket from `km env | grep KM_ARTIFACTS_BUCKET`).
       - Per CLAUDE.md: do NOT use bare `go build` — Makefile has the correct ldflags and S3 upload.

    2. **Deploy Lambda + management infra:** `cd /Users/khundeck/working/klankrmkr && km init --sidecars 2>&1 | tail -30`
       - This is the fast-binary path (per CLAUDE.md CLI table) that redeploys only the sidecar-affecting infra without a full terragrunt apply.
       - Watch for errors. If `km init --sidecars` reports drift in upstream modules, fall back to plain `km init --plan` first to surface what changed.

    3. **Verify deploy artifact in S3:** `aws s3api head-object --bucket ${KM_ARTIFACTS_BUCKET} --key sidecars/http-proxy/http-proxy --query 'LastModified' 2>&1`
       - Confirms the binary is fresh (timestamp within the last few minutes).
       - If artifacts bucket is not exported, run `eval $(km env)` first.

    Do NOT run `km create` here — that's the UAT step (next task). This task is pure infra deploy.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && make sidecars 2>&1 | grep -E "(SUCCESS|FAIL|ERROR|http-proxy)" | tail -10 && eval $(km env 2>/dev/null) && aws s3api head-object --bucket "${KM_ARTIFACTS_BUCKET}" --key sidecars/http-proxy/http-proxy --query LastModified 2>&1 | tail -5</automated>
    Expected: `make sidecars` exits 0, http-proxy binary in the build output, S3 head-object returns a recent LastModified timestamp.

    If `KM_ARTIFACTS_BUCKET` is not set, the eval line populates it via `km env`.
  </verify>
  <done>
    `make sidecars` succeeded. `km init --sidecars` succeeded. Fresh http-proxy binary is in S3. Ready for live UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — Codex sandbox produces BUDGET#ai#gpt-* DynamoDB row</name>
  <files>(no source files — operator runs live sandbox commands; results captured in 88-07-UAT.md from Task 3)</files>
  <what-built>
    Phase 88 ships:
    - New file `sidecars/http-proxy/httpproxy/openai.go` (token extractor + cost calc + rate table + 403 blocked-response)
    - Third intercept block in `proxy.go` for `api.openai.com`
    - Transparent listener parity in `transparent.go`
    - L7 proxy host gate in `pkg/compiler/userdata.go` (Codex agent → append api.openai.com)
    - Extended `aws.BedrockModelRate` struct with `CachedInputPricePer1KTokens` field
    - Deployed via `make sidecars && km init --sidecars` (Task 1 above)

    Unit + integration tests (Wave 0/1) prove the code paths work with stubs. This UAT proves the live chain works end-to-end: Codex CLI → real api.openai.com call → http-proxy MITM intercept → DynamoDB write.
  </what-built>
  <action>
    Hand control to the operator to execute the 4 UAT scenarios below. Claude does NOT run these commands — they require a live OpenAI API key + AWS credentials + tolerance for real-money costs (Codex calls cost ~$0.001-$0.05 per turn). The operator runs the scenarios; Claude waits at the resume-signal.

    The full runbook lives in `88-07-UAT.md` (created by Task 3 of this plan, which runs in parallel with this checkpoint). The how-to-verify block below mirrors the runbook.
  </action>
  <how-to-verify>
    The full UAT runbook lives in `88-07-UAT.md` (Task 3 of this plan creates it). Execute the 4 scenarios below:

    **Pre-req:** A valid OpenAI API key set in SSM as `/km/sandboxes/openai-api-key` (or per the codex profile's secret reference). If the operator does not have one, this UAT cannot proceed — defer until a key is available.

    **Scenario 1 — Happy path metering:**
    1. `km create profiles/codex.yaml` (use whichever Codex profile is locally present; if none, use `profiles/learn.v2.codex.yaml.bak2` as a starting point and `cp` to a working name first).
    2. Wait for the sandbox to reach `running` state: `km list | grep <sandbox-id>`.
    3. Trigger a Codex call: `km agent run <sandbox-id> --prompt "Echo back the word hello." --wait`
    4. Query DynamoDB for the metering row:
       ```bash
       aws dynamodb get-item \
         --table-name "${KM_RESOURCE_PREFIX:-km}-budgets" \
         --key '{"PK":{"S":"SANDBOX#<sandbox-id>"},"SK":{"S":"BUDGET#ai#gpt-5.3-codex"}}' \
         --query 'Item' --output json
       ```
       (Or whichever model the Codex CLI ended up using — adjust SK accordingly. Run `aws dynamodb query --table-name ${KM_RESOURCE_PREFIX:-km}-budgets --key-condition-expression 'PK = :pk' --expression-attribute-values '{":pk":{"S":"SANDBOX#<sandbox-id>"}}'` to list all rows for the sandbox.)
    5. **Expected:** Item exists, has `spentUSD > 0`, `inputTokens > 0`, `outputTokens > 0`, `last_updated` recent.
    6. **Pass criterion:** `spentUSD` field present and non-zero. SK matches `BUDGET#ai#gpt-*` pattern.

    **Scenario 2 — km status surfaces the row:**
    1. `km status <sandbox-id> | grep -A 5 "AI"` should show a per-model breakdown including the gpt-5.x entry from Scenario 1.
    2. **Pass criterion:** km status renders the OpenAI model spend alongside any Claude spend (none expected for Codex-only sandbox).

    **Scenario 3 — Budget exhaustion 403 (optional, requires manual budget adjustment):**
    1. Edit the sandbox budget to a tiny limit: directly update DynamoDB or destroy + recreate with `spec.budget.ai.maxSpendUSD: 0.001`.
    2. Make another Codex call.
    3. **Expected:** Codex CLI receives a 403 from the proxy with the OpenAIBlockedResponse JSON envelope.
    4. **Pass criterion:** Codex tool surfaces the budget-exhausted error; sandbox CloudWatch logs show `event_type=ai_budget_exhausted_preflight` for an `api.openai.com` host.

    **Scenario 4 — Cleanup:**
    1. `km destroy <sandbox-id> --remote --yes` (per CLAUDE.md memory — use `--remote --yes`, NOT `--force`).
    2. Confirm sandbox gone: `km list | grep <sandbox-id>` → no rows.

    **If any scenario FAILS:** capture stderr, CloudWatch logs from the http-proxy sidecar (`/aws/lambda/<prefix>-http-proxy-<sandbox-id>` or per the sidecar log group convention), and the DynamoDB item state. File as `.planning/phases/88-.../88-VERIFICATION.md` for `/gsd:plan-phase 88 --gaps` cycle.
  </how-to-verify>
  <verify>Operator-reported pass/fail of Scenarios 1 + 2 (Scenario 3 optional, Scenario 4 cleanup). No Claude-automatable verify command — that's the whole point of the checkpoint.</verify>
  <done>Operator confirms "approved" via resume-signal AFTER Scenarios 1 + 2 PASS. On failure, branch to `/gsd:plan-phase 88 --gaps` from a written 88-VERIFICATION.md capturing the failed scenario details.</done>
  <resume-signal>Type "approved" once Scenarios 1 + 2 PASS (Scenario 3 is optional, Scenario 4 is cleanup). If any scenario fails, describe the failure and we'll branch to a gap-closure plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write UAT runbook + update CLAUDE.md to mention OpenAI metering</name>
  <files>.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-07-UAT.md, CLAUDE.md</files>
  <action>
    **Part A — Write `88-07-UAT.md`:**

    Create the file as a written runbook of Task 2's 4 scenarios. Use the structure:

    ```markdown
    # Phase 88 — UAT Runbook

    **Status:** pending (executed manually as part of plan 88-07 Task 2)
    **Goal:** Verify Phase 88 ships working OpenAI metering against a live Codex sandbox.

    ## Pre-requisites

    - Valid OpenAI API key in SSM under `/km/sandboxes/openai-api-key` (or per profile secret ref)
    - `km` CLI built and on PATH (post-`make build`)
    - Sidecars deployed (`make sidecars && km init --sidecars` — Task 1 of this plan)
    - AWS creds with read access to DynamoDB `<prefix>-budgets` table

    ## Scenarios

    ### Scenario 1 — Happy path metering

    [Steps 1-6 from Task 2 above, verbatim.]

    ### Scenario 2 — km status surfaces the row

    [Steps from Task 2 above, verbatim.]

    ### Scenario 3 — Budget exhaustion 403 (optional)

    [Steps from Task 2 above, verbatim.]

    ### Scenario 4 — Cleanup

    [Steps from Task 2 above, verbatim.]

    ## Pass / Fail

    - **PASS:** Scenarios 1 + 2 succeed. Scenario 3 optional. Mark `88-07-SUMMARY.md` status: PASS.
    - **FAIL:** Any scenario fails. Capture details, file `88-VERIFICATION.md` per gap-closure mode.
    ```

    Copy the exact steps from Task 2 — the runbook is the authoritative reference, Task 2 is a pointer.

    **Part B — Update `CLAUDE.md`:**

    Find the section that lists what the http-proxy sidecar does. Likely candidates (grep for relevant headers):

    ```bash
    grep -n "http-proxy\|Bedrock metering\|Anthropic metering\|BUDG-04\|BUDG-10" /Users/khundeck/working/klankrmkr/CLAUDE.md | head -10
    ```

    Find the existing mention of Bedrock + Anthropic budget metering. Append one parallel line for OpenAI. Likely target: the architecture bullet list (`sidecars/` line) OR the budget-related section if one exists.

    Suggested patch:

    Locate the section under "## Architecture" or similar that lists `sidecars/`:
    ```
    - `sidecars/` — HTTP proxy (MITM), DNS proxy, audit-log, tracing, km-presence
    ```

    Add a new subsection (or extend an existing one if there's already a budget enforcement section) with:

    ```markdown
    ## Budget Metering Coverage (Phase 88)

    http-proxy MITM intercepts and meters three AI provider endpoints into the same
    `BUDGET#ai#{modelID}` DynamoDB row shape:

    - **Bedrock InvokeModel** (`bedrock-runtime.*.amazonaws.com`) — Claude on Bedrock (Phase 6)
    - **Anthropic direct API** (`api.anthropic.com`) — Claude Code (Phase 20)
    - **OpenAI direct API** (`api.openai.com`) — Codex CLI + raw OpenAI SDK (Phase 88)

    L7 proxy hosts are gated per profile:
    - `spec.execution.useBedrock: true` → adds `.amazonaws.com,api.anthropic.com`
    - `spec.cli.agent: codex` → adds `api.openai.com` (Phase 88)

    Unknown model IDs in any provider write rows with `spentUSD=0` and log
    `event_type=*_unknown_model` so operators see the gap in `km status`.
    ```

    Keep the addition under 20 lines — CLAUDE.md is hot context and bloats fast.

    If a "Budget Enforcement" section already exists, extend it rather than adding a parallel section.
  </action>
  <verify>
    <automated>test -f /Users/khundeck/working/klankrmkr/.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-07-UAT.md && grep -c "Scenario" /Users/khundeck/working/klankrmkr/.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-07-UAT.md && grep -c "OpenAI" /Users/khundeck/working/klankrmkr/CLAUDE.md</automated>
    Expected:
    - UAT file exists
    - 4 (or more) "Scenario" mentions in the UAT file
    - CLAUDE.md has ≥1 "OpenAI" mention (more if the patch landed correctly)
  </verify>
  <done>
    `88-07-UAT.md` exists with 4 scenarios. `CLAUDE.md` mentions OpenAI metering. Both files committed in the phase wrap-up commit.
  </done>
</task>

</tasks>

<verification>
- Task 1: `make sidecars && km init --sidecars` exit 0; fresh http-proxy binary in S3
- Task 2: User-verified Scenarios 1 + 2 PASS (Scenario 3 optional)
- Task 3: `88-07-UAT.md` exists, `CLAUDE.md` mentions OpenAI metering
- Phase-wide final check: `cd /Users/khundeck/working/klankrmkr && go test ./... -count=1 2>&1 | tail -20` — full repo build + test passes (per VALIDATION.md § Sampling Rate "Phase gate" line)
</verification>

<success_criteria>
- Phase 88 deploys without errors
- Live Codex sandbox produces real `BUDGET#ai#gpt-*` DynamoDB rows with `spentUSD > 0`
- `km status` renders OpenAI model breakdown alongside any Claude breakdown
- CLAUDE.md updated to reflect three-provider metering coverage
- UAT runbook persisted for future regression checks
- Phase 88 ready for `/gsd:verify-work` then `/gsd:end-phase`
</success_criteria>

<output>
After completion, create `.planning/phases/88-codex-openai-budget-metering-http-proxy-interceptor-for-api-openai-com-price-table-incrementaispend-wiring-mirrors-anthropic-pipeline/88-07-SUMMARY.md` per the GSD summary template.
</output>
