---
phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff
plan: "01"
subsystem: cli
tags: [configure, resource_prefix, multi-instance, preserve, tdd]

# Dependency graph
requires:
  - phase: 82-multi-instance-resource-prefix-isolation
    provides: "resource_prefix preserve guard introduced in configure.go (outputDir != '' path only)"
provides:
  - "Bare-path km configure --non-interactive preserves resource_prefix from existing km-config.yaml"
  - "KM_REPO_ROOT-based findRepoRoot() resolution used in preserve guard, matching write-path logic"
affects:
  - configure.go
  - km configure operator workflow
  - multi-instance installs

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Preserve guard mirrors write-path logic: effectiveDir = outputDir || findRepoRoot()"

key-files:
  created: []
  modified:
    - internal/app/cmd/configure.go
    - internal/app/cmd/configure_test.go

key-decisions:
  - "Extend preserve guard by replacing `outputDir != \"\"` condition with effectiveDir resolution matching the existing write-path (findRepoRoot() fallback)"
  - "Two new TDD tests use KM_REPO_ROOT env var to direct findRepoRoot() to a temp directory — mirrors how the production configure command resolves output path"
  - "Pre-existing TestUnlockCmd_RequiresStateBucket failure is out-of-scope; logged to deferred-items.md"

patterns-established:
  - "Guard and write path use identical directory resolution logic to prevent drift"

requirements-completed: [CONFIG-PRESERVE-BARE]

# Metrics
duration: 8min
completed: 2026-05-16
---

# Phase 82.1 Plan 01: Bare-Path Configure Preserve Guard Summary

**Extended `km configure` preserve guard to cover bare invocations (no --output-dir) via `findRepoRoot()` fallback, closing Gap 1 from Phase 82 VERIFICATION.md**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-16T15:59:55Z
- **Completed:** 2026-05-16T16:07:55Z
- **Tasks:** 2 (RED + GREEN, TDD)
- **Files modified:** 2

## Accomplishments

- Bare `km configure --non-interactive` (no --output-dir) now preserves `resource_prefix` from an existing km-config.yaml rather than silently resetting to "km"
- Two new tests cover the bare-path scenario: preserve existing prefix and fresh-install default-to-km
- All four configure-preserve tests pass: `TestConfigureBarePath_PreservesResourcePrefix`, `TestConfigureBarePath_FreshInstallDefaultsToKm`, `TestConfigureRerunPreservesResourcePrefix`, `TestConfigureResetPrefixFlag`
- CLAUDE.md claim "Re-running `km configure` preserves the existing `resource_prefix`" is now true for ALL invocation modes

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — failing tests** - `ccceb09` (test)
2. **Task 2: GREEN — fix configure.go** - `061fdc2` (feat)

## Files Created/Modified

- `internal/app/cmd/configure.go` - Preserve guard extended: removed `outputDir != ""` condition, replaced with `effectiveDir` resolution via `findRepoRoot()` fallback (mirrors write-path logic at lines 335-338)
- `internal/app/cmd/configure_test.go` - Added `TestConfigureBarePath_PreservesResourcePrefix` and `TestConfigureBarePath_FreshInstallDefaultsToKm` using `KM_REPO_ROOT` env var

## Decisions Made

- Used `effectiveDir := outputDir; if effectiveDir == "" { effectiveDir = findRepoRoot() }` pattern to mirror the existing write-path resolution exactly — ensures guard and write always use the same directory
- Tests use `exec.Command` directly with `c.Env = append(os.Environ(), "KM_REPO_ROOT="+dir)` instead of `runKMArgs` helper so the env var can be set per-invocation

## Deviations from Plan

None — plan executed exactly as written. TDD RED/GREEN flow followed precisely.

## Issues Encountered

Pre-existing `TestUnlockCmd_RequiresStateBucket` failure found during full-package regression run. Confirmed unrelated to configure.go changes (was failing before my commits). Logged to `deferred-items.md`.

## Next Phase Readiness

- Gap 1 (CONFIG-PRESERVE-BARE) closed
- Ready for Plan 82.1-02 (Gap 2: service_hcl.go literal) and Plan 82.1-03 (Gap 3: SES active rule-set handoff)

---
*Phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff*
*Completed: 2026-05-16*
