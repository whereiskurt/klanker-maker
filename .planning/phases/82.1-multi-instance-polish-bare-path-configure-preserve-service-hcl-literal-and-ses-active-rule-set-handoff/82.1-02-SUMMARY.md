---
phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff
plan: "02"
subsystem: compiler
tags: [multi-instance, resource_prefix, dynamodb, slack, service_hcl, tdd]

# Dependency graph
requires:
  - phase: 82-multi-instance-resource-prefix-isolation
    provides: resource_prefix isolation across SES, email-handler, ECS modules; KM_RESOURCE_PREFIX env convention

provides:
  - service_hcl.go generateEC2ServiceHCL now derives SlackStreamMessagesTableName from KM_RESOURCE_PREFIX (fallback "km") instead of hardcoded literal
  - TDD test TestEC2ServiceHCL_StreamTableUsesResourcePrefix covering all three cases (custom prefix, default prefix, explicit override)

affects:
  - phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff
  - multi-instance-deploy
  - km-init

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ec2StreamPrefix := os.Getenv(\"KM_RESOURCE_PREFIX\"); if ec2StreamPrefix == \"\" { ec2StreamPrefix = \"km\" }; streamTable = ec2StreamPrefix + \"-slack-stream-messages\" — prefix-aware fallback pattern for DynamoDB table names"

key-files:
  created:
    - (no new files; test added to existing file)
  modified:
    - pkg/compiler/service_hcl.go
    - pkg/compiler/compiler_test.go

key-decisions:
  - "Named variable ec2StreamPrefix (not ec2ResourcePrefix) to avoid shadowing the ec2ResourcePrefix variable declared at line 792 of the same function"
  - "Three TDD subtests in one test function: custom prefix, default prefix, explicit KM_SLACK_STREAM_TABLE override — covers all paths through the fixed block"
  - "Pre-existing test failures (TestUserDataNotifyEnvVars_NoneSet_NoEnvBlock et al.) confirmed out-of-scope by reproducing them against unmodified code; not addressed in this plan"

patterns-established:
  - "KM_RESOURCE_PREFIX fallback pattern: os.Getenv -> default 'km' -> concatenate '-<resource-suffix>' — mirrors Phase 82 pattern used in userdata.go and ec2ResourcePrefix block in same function"

requirements-completed:
  - SVCHCL-PREFIX-FALLBACK

# Metrics
duration: 2min
completed: 2026-05-16
---

# Phase 82.1 Plan 02: service_hcl.go Stream-Table Prefix-Aware Derivation Summary

**EC2 ServiceHCL stream-table name now derived from KM_RESOURCE_PREFIX instead of hardcoded "km-slack-stream-messages", closing Gap 2 from Phase 82 multi-instance isolation**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-16T15:39:55Z
- **Completed:** 2026-05-16T15:41:31Z
- **Tasks:** 2 (RED test + GREEN implementation)
- **Files modified:** 2

## Accomplishments
- Replaced line 784 literal `"km-slack-stream-messages"` in `generateEC2ServiceHCL` with `ec2StreamPrefix + "-slack-stream-messages"` where `ec2StreamPrefix` reads `KM_RESOURCE_PREFIX` (default `"km"`)
- Wrote `TestEC2ServiceHCL_StreamTableUsesResourcePrefix` covering all three paths: non-default prefix `rg`, default prefix (empty env var), and explicit `KM_SLACK_STREAM_TABLE` override
- Confirmed no regression in `TestCompileEC2ServiceHCL` and `TestCompileEC2Spot`

## Task Commits

Each task was committed atomically:

1. **Task 1: TDD RED — failing test for prefix-aware stream table** - `069e6e8` (test)
2. **Task 2: TDD GREEN — replace literal with prefix-aware derivation** - `42f7f25` (feat)

_Note: TDD plan; two commits (test → feat). No refactor step needed — implementation is minimal and clean._

## Files Created/Modified
- `pkg/compiler/service_hcl.go` - Lines 782-786: stream table derivation replaced; +4 lines net
- `pkg/compiler/compiler_test.go` - `TestEC2ServiceHCL_StreamTableUsesResourcePrefix` added after TestCompileEC2Spot; +67 lines

## Decisions Made
- Named the new variable `ec2StreamPrefix` instead of reusing `ec2ResourcePrefix` to avoid shadowing the `ec2ResourcePrefix` variable at line 792 in the same function scope
- Chose a single table-driven test function with three named subtests rather than three separate top-level test functions — mirrors project's existing subtest pattern in this file

## Deviations from Plan

None - plan executed exactly as written.

The pre-existing test failures in `TestUserDataNotifyEnvVars_NoneSet_NoEnvBlock` and related tests were confirmed as pre-existing (reproduced on unmodified code via `git stash`) and are out of scope per the scope boundary rule. They are unrelated to the stream-table literal being fixed here.

## Issues Encountered
- Pre-existing test failures detected during `go test ./pkg/compiler/...`. Verified they existed before this plan's changes by stashing and re-running. No action taken; logged here for awareness.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Gap 2 (service_hcl.go literal) is now fully closed
- A second `km install` with `resource_prefix: rg` will generate EC2 service.hcl files referencing `rg-slack-stream-messages` — the correct per-install DynamoDB table name
- Phase 82.1 plans 01 and 02 together address both gaps deferred from Phase 82

---
*Phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff*
*Completed: 2026-05-16*
