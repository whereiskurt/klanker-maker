---
phase: 82.1-multi-instance-polish-bare-path-configure-preserve-service-hcl-literal-and-ses-active-rule-set-handoff
verified: 2026-05-16T17:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
human_verification:
  - test: "Run all four configure-preserve tests in the real binary"
    expected: "TestConfigureBarePath_PreservesResourcePrefix, TestConfigureBarePath_FreshInstallDefaultsToKm, TestConfigureRerunPreservesResourcePrefix, TestConfigureResetPrefixFlag all PASS"
    why_human: "Tests require building the km binary; cannot run in static verification; operator-confirmed passing per SUMMARY-01"
  - test: "km init --dry-run=true on primary install after SES count-gate change"
    expected: "Plan: 0 to add, 0 to change, 0 to destroy (Terraform auto-migrates non-indexed to count[0] address)"
    why_human: "Requires live AWS + Terraform state; operator confirmed this at Task 4 checkpoint per SUMMARY-03"
---

# Phase 82.1: Multi-Instance Polish Verification Report

**Phase Goal:** Close three multi-instance gap-closure items from Phase 82 — CONFIG-PRESERVE-BARE (bare km configure preserves resource_prefix), SVCHCL-PREFIX-FALLBACK (service_hcl.go stream-table name prefix-aware), SES-ACTIVE-RULESET (second installs must not steal SES activation).
**Verified:** 2026-05-16T17:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Bare `km configure --non-interactive` (no --output-dir) against a dir with resource_prefix: rg preserves resource_prefix: rg | VERIFIED | configure.go:149-161 — effectiveDir guard fires without outputDir condition; findRepoRoot() fallback mirrors write path |
| 2 | Bare `km configure --non-interactive --reset-prefix` resets resource_prefix to km | VERIFIED | configure.go:149 — guard gated by `!resetPrefix`; reset path unchanged |
| 3 | Bare `km configure` with NO existing km-config.yaml defaults to km (fresh-install path) | VERIFIED | configure.go:155 — ReadFile returns err for non-existent file; existingPrefix stays ""; TestConfigureBarePath_FreshInstallDefaultsToKm covers this |
| 4 | Two new TDD tests covering the bare-path scenario exist and are substantive | VERIFIED | configure_test.go:581-662 — TestConfigureBarePath_PreservesResourcePrefix and TestConfigureBarePath_FreshInstallDefaultsToKm; full exec.Command tests with KM_REPO_ROOT |
| 5 | generateEC2ServiceHCL with KM_RESOURCE_PREFIX=rg emits rg-slack-stream-messages | VERIFIED | service_hcl.go:784-789 — ec2StreamPrefix + "-slack-stream-messages" pattern; TestEC2ServiceHCL_StreamTableUsesResourcePrefix subtest "custom prefix rg" asserts this |
| 6 | generateEC2ServiceHCL with KM_RESOURCE_PREFIX unset emits km-slack-stream-messages | VERIFIED | service_hcl.go:785-786 — default "km" fallback; subtest "default prefix km" asserts this |
| 7 | Literal string "km-slack-stream-messages" no longer appears at service_hcl.go:784 | VERIFIED | grep -n '"km-slack-stream-messages"' pkg/compiler/service_hcl.go returns no results |
| 8 | SES module var.activate_rule_set (bool, default true) count-gates aws_ses_active_receipt_rule_set | VERIFIED | variables.tf:33-37 (bool, default true); main.tf:68-71 (count = var.activate_rule_set ? 1 : 0) |
| 9 | Operators understand the SES singleton constraint and handoff procedure | VERIFIED | CLAUDE.md:39-69 (Phase 82.1 subsection with full runbook); OPERATOR-GUIDE.md:646-677 (SES activation handoff section) |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/app/cmd/configure.go` | Preserve guard extended via findRepoRoot() when outputDir is empty | VERIFIED | Lines 149-161: effectiveDir resolution mirrors write-path at lines 344-346; `outputDir != ""` condition removed |
| `internal/app/cmd/configure_test.go` | Two new TDD tests covering bare-path cases | VERIFIED | TestConfigureBarePath_PreservesResourcePrefix (line 581) and TestConfigureBarePath_FreshInstallDefaultsToKm (line 634); substantive exec.Command integration tests with KM_REPO_ROOT |
| `pkg/compiler/service_hcl.go` | Line 784 literal replaced with prefix-aware derivation | VERIFIED | Lines 782-790: ec2StreamPrefix read from KM_RESOURCE_PREFIX, fallback "km", concatenated with "-slack-stream-messages"; old literal absent |
| `pkg/compiler/compiler_test.go` | TestEC2ServiceHCL_StreamTableUsesResourcePrefix | VERIFIED | Line 203: three subtests (custom prefix, default prefix, explicit override); substantive — checks string presence/absence in compiled HCL |
| `infra/modules/ses/v1.0.0/variables.tf` | New activate_rule_set variable (bool, default true) | VERIFIED | Lines 33-37: type bool, description explains singleton constraint, default true |
| `infra/modules/ses/v1.0.0/main.tf` | aws_ses_active_receipt_rule_set count-gated by var.activate_rule_set | VERIFIED | Lines 65-71: count = var.activate_rule_set ? 1 : 0; comment references OPERATOR-GUIDE.md |
| `infra/live/use1/ses/terragrunt.hcl` | activate_rule_set wired from KM_SES_ACTIVATE_RULESET env var | VERIFIED | Line 47: activate_rule_set = tobool(get_env("KM_SES_ACTIVATE_RULESET", "true")) |
| `CLAUDE.md` | Phase 82.1 subsection with SES handoff runbook | VERIFIED | Lines 39-69: full operator runbook, KM_SES_ACTIVATE_RULESET documented, rollback command present |
| `OPERATOR-GUIDE.md` | SES activation handoff section with step-by-step runbook | VERIFIED | Lines 646-677: second-install procedure, deliberate handoff steps, primary install safety note |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| configure.go:152 (preserve guard) | findRepoRoot() in create.go:2234 | fallback when effectiveDir == "" | WIRED | configure.go:150-152 calls findRepoRoot() in the guard; create.go:2234 defines it; both configure.go calls (guard + write path) use the same function |
| service_hcl.go:782-790 (streamTable resolution) | SlackStreamMessagesTableName template param | KM_RESOURCE_PREFIX env var read at HCL generation time | WIRED | os.Getenv("KM_RESOURCE_PREFIX") at line 784; assigned to params.SlackStreamMessagesTableName at line 790 |
| infra/live/use1/ses/terragrunt.hcl | infra/modules/ses/v1.0.0/variables.tf | activate_rule_set = tobool(get_env("KM_SES_ACTIVATE_RULESET", "true")) | WIRED | terragrunt.hcl:47 matches variables.tf:33-37 variable declaration |
| infra/modules/ses/v1.0.0/main.tf | aws_ses_active_receipt_rule_set.km_sandbox | count = var.activate_rule_set ? 1 : 0 | WIRED | main.tf:69 count-gates the resource; variable consumed directly |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CONFIG-PRESERVE-BARE | 82.1-01 | Bare km configure preserves resource_prefix via findRepoRoot() | SATISFIED | configure.go:149-161; configure_test.go:581-662; commits ccceb09 + 061fdc2 |
| SVCHCL-PREFIX-FALLBACK | 82.1-02 | service_hcl.go stream-table name derived from KM_RESOURCE_PREFIX | SATISFIED | service_hcl.go:782-790; compiler_test.go:203-268; commits 069e6e8 + 42f7f25 |
| SES-ACTIVE-RULESET | 82.1-03 | activate_rule_set bool (default true) count-gates aws_ses_active_receipt_rule_set; KM_SES_ACTIVATE_RULESET wired in live terragrunt | SATISFIED | variables.tf:33-37; main.tf:65-71; terragrunt.hcl:47; CLAUDE.md:39-69; OPERATOR-GUIDE.md:646-677; commits 3bf882e + 284b396 + fb8c39e |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| pkg/compiler/service_hcl.go | 322, 328 | TODO(plan-04): /km/ CloudWatch log group prefix not yet dynamic | Info | Pre-existing from Phase 82, out of scope for 82.1 |
| pkg/compiler/service_hcl.go | 843 | TODO Phase 66 plan 04: nil-network fallback email domain hardcoded | Info | Pre-existing, out of scope |

No blockers. No warnings. All pre-existing TODOs are unrelated to Phase 82.1 scope.

### Human Verification Required

#### 1. Configure-preserve test suite

**Test:** cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run "TestConfigureBarePath|TestConfigureRerunPreservesResourcePrefix|TestConfigureResetPrefixFlag" -v -count=1
**Expected:** All four tests PASS — TestConfigureBarePath_PreservesResourcePrefix, TestConfigureBarePath_FreshInstallDefaultsToKm, TestConfigureRerunPreservesResourcePrefix, TestConfigureResetPrefixFlag
**Why human:** Tests compile and execute the km binary; cannot run in static code analysis. Operator-confirmed PASS in SUMMARY-01.

#### 2. SES Terraform plan — zero diff for primary install

**Test:** unset KM_SES_ACTIVATE_RULESET && km init --dry-run=true 2>&1 | grep -E "aws_ses_active_receipt_rule_set|Plan:"
**Expected:** Plan: 0 to add, 0 to change, 0 to destroy. No destroy+recreate of aws_ses_active_receipt_rule_set.
**Why human:** Requires live AWS credentials + Terraform state. Operator confirmed at Task 4 checkpoint in SUMMARY-03: "Terraform 1.x auto-migrated the resource address (non-indexed → count[0]) as a state move — zero destroy+recreate, no moved {} block required."

### Gaps Summary

No gaps. All three requirements are fully implemented, wired, and documented.

- CONFIG-PRESERVE-BARE: The `outputDir != ""` guard condition is replaced with an `effectiveDir` resolution that mirrors the write path. Both guard and write path call `findRepoRoot()` when `outputDir` is empty. Two TDD integration tests exercise the bare-path scenario end-to-end.
- SVCHCL-PREFIX-FALLBACK: The literal `"km-slack-stream-messages"` at the former line 784 is replaced by `ec2StreamPrefix + "-slack-stream-messages"` where `ec2StreamPrefix` reads `KM_RESOURCE_PREFIX` with `"km"` as default. Three TDD subtests cover custom prefix, default prefix, and explicit override paths.
- SES-ACTIVE-RULESET: The `aws_ses_active_receipt_rule_set` resource is count-gated behind `var.activate_rule_set` (default `true`). The live terragrunt.hcl wires this from `KM_SES_ACTIVATE_RULESET` env var. Operator documentation in both CLAUDE.md and OPERATOR-GUIDE.md provides the full second-install runbook and rollback procedure. Primary-install zero-diff confirmed by operator at checkpoint.

---

_Verified: 2026-05-16T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
