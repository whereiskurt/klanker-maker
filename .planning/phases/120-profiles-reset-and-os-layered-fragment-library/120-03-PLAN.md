---
phase: 120-profiles-reset-and-os-layered-fragment-library
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/learn.v2.yaml
  - profiles/learn.v2.chatty.yaml
  - profiles/learn.v2.codex.yaml
  - profiles/learn.v2.polite.yaml
  - profiles/learn.v2.parallel.yaml
  - profiles/learn.v2.private-allow.yaml
  - profiles/learn.v2.desktop.yaml
  - profiles/dc34.yaml
  - profiles/dc34.ami.yaml
  - profiles/codex.yaml
  - profiles/locked.yaml
  - profiles/locked.ami.yaml
  - profiles/github-review.yaml
  - profiles/ao.yaml
  - profiles/goose.yaml
  - profiles/example-additional-snapshots.yaml
  - profiles/h1-triage.yaml
  - profiles/check-triage.yaml
  - profiles/desktop.yaml
  - pkg/compiler/userdata_phase92_byte_identity_test.go
  - pkg/compiler/agent_claude_golden_test.go
  - pkg/compiler/agent_codex_golden_test.go
  - pkg/profile/github_review_secrets_test.go
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "Every retired demo + frozen byte-identity input fixture is git-moved to testdata/profiles/ with bytes unchanged"
    - "The 6 test files / 7 path-edit sites point at testdata/profiles/ so byte-identity + golden tests stay green"
    - "go test ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... passes (capture the command's own exit, not a piped tail)"
    - "No commit in this plan leaves the test suite red — file move + path update land together"
    - "userdata_learn_v2_pre92_baseline.golden.sh is NOT re-captured; golden OUTPUT files stay in pkg/compiler/testdata/"
  artifacts:
    - path: "testdata/profiles/learn.v2.yaml"
      provides: "Archived frozen byte-identity input fixture (bytes identical to pre-move)"
      contains: "learn-v2-direct-api"
    - path: "testdata/profiles/github-review.yaml"
      provides: "Archived github-review profile read by github_review_secrets_test.go"
      contains: "github-review-secrets.enc.yaml"
    - path: "testdata/profiles/desktop.legacy.yaml"
      provides: "Archived old monolithic desktop profile (renamed from profiles/desktop.yaml)"
      contains: "desktop"
  key_links:
    - from: "pkg/compiler/userdata_phase92_byte_identity_test.go"
      to: "testdata/profiles/learn.v2.yaml"
      via: "profilesDir = filepath.Join(repoRoot, \"testdata\", \"profiles\")"
      pattern: "testdata.*profiles"
    - from: "pkg/compiler/agent_claude_golden_test.go"
      to: "testdata/profiles/{learn.v2,dc34,locked,codex}.yaml"
      via: "table fixture path strings ../../testdata/profiles/*.yaml"
      pattern: "../../testdata/profiles/"
    - from: "pkg/profile/github_review_secrets_test.go"
      to: "testdata/profiles/github-review.yaml"
      via: "filepath.Join(repoRoot, \"testdata\", \"profiles\", \"github-review.yaml\")"
      pattern: "testdata.*profiles.*github-review"
---

<objective>
Archive every retired demo profile and frozen byte-identity input fixture from profiles/ into
the existing testdata/profiles/ directory using `git mv` (history follows, bytes unchanged),
and update the SIX test files / SEVEN path-edit sites that reference those inputs IN THE SAME
PLAN so the test suite never goes red between commits.

Purpose: clear profiles/ down to the 4 leaves + base/** + checks/** + secrets/** + *.prompt.txt
while keeping the Phase 92 byte-identity contract and the agent goldens green.
Output: ~19 profiles relocated under testdata/profiles/; 4 test files updated to the new paths.

CRITICAL: This plan is independent of Plan 02 (different files) and runs in the same wave. The
new profiles/desktop.yaml and profiles/github.yaml are authored by Plan 02; here we move the OLD
profiles/desktop.yaml to testdata/profiles/desktop.legacy.yaml and the OLD
profiles/github-review.yaml to testdata/profiles/github-review.yaml. Coordinate ordering only on
desktop.yaml: do the `git mv profiles/desktop.yaml testdata/profiles/desktop.legacy.yaml` FIRST
(this plan), then Plan 02 writes a fresh profiles/desktop.yaml. If both have run, `ls
profiles/desktop.yaml` shows the NEW one and `ls testdata/profiles/desktop.legacy.yaml` shows the
OLD one. The executor must NOT delete a freshly-authored profiles/desktop.yaml — move only if the
file present is the OLD monolithic one (check for absence of an `extends:` line before moving;
the OLD desktop.yaml has no extends).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-CONTEXT.md
@.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-RESEARCH.md

# The exact test-path edit sites (verified line numbers — Research Task 1):
@pkg/compiler/userdata_phase92_byte_identity_test.go
@pkg/compiler/agent_claude_golden_test.go
@pkg/compiler/agent_codex_golden_test.go
@pkg/profile/github_review_secrets_test.go

<interfaces>
<!-- The 6-file / 7-site edit map (verified by research, line numbers may drift ±2). -->

1. pkg/compiler/userdata_phase92_byte_identity_test.go ~line 34:
     profilesDir := filepath.Join(repoRoot, "profiles")
   → filepath.Join(repoRoot, "testdata", "profiles")

2-5. pkg/compiler/agent_claude_golden_test.go ~lines 41-44 (table fixture path strings):
     "../../profiles/learn.v2.yaml" → "../../testdata/profiles/learn.v2.yaml"
     "../../profiles/dc34.yaml"     → "../../testdata/profiles/dc34.yaml"
     "../../profiles/locked.yaml"   → "../../testdata/profiles/locked.yaml"
     "../../profiles/codex.yaml"    → "../../testdata/profiles/codex.yaml"
   (The test derives profilesDir from filepath.Dir(path[len("../../"):]) — so "testdata/profiles"
    falls out automatically once the string is updated. Verify, do not hand-set profilesDir.)

6. pkg/compiler/agent_codex_golden_test.go ~line 32:
     const profilePath = "../../profiles/codex.yaml" → "../../testdata/profiles/codex.yaml"
   (uses profile.Parse, not Resolve — only the path constant changes.)

7. pkg/profile/github_review_secrets_test.go ~line 32:
     filepath.Join(repoRoot, "profiles", "github-review.yaml")
   → filepath.Join(repoRoot, "testdata", "profiles", "github-review.yaml")

NO CHANGE NEEDED: userdata_h1_byte_identity_test.go (loads ec2-basic.yaml from
pkg/compiler/testdata/, not a top-level profile). Golden OUTPUT files
(pkg/compiler/testdata/*.golden.{sh,json,toml}) do NOT move and do NOT change bytes.

Phantom (tempdir-created, NOT real files — ignore in audit):
  profiles/sealed.yaml, profiles/review.yaml, profiles/frontend.yaml, profiles/backend.yaml,
  profiles/onboard.yaml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv all retired demos + frozen fixtures into testdata/profiles/</name>
  <files>profiles/*.yaml (the 19 retired files), testdata/profiles/</files>
  <action>
    Use `git mv` for EVERY move so history follows and bytes stay identical. Move these from
    profiles/ to testdata/profiles/ (same basename unless noted):

      learn.v2.yaml, learn.v2.chatty.yaml, learn.v2.codex.yaml, learn.v2.polite.yaml,
      learn.v2.parallel.yaml, learn.v2.private-allow.yaml, learn.v2.desktop.yaml,
      dc34.yaml, dc34.ami.yaml, codex.yaml, locked.yaml, locked.ami.yaml,
      github-review.yaml, ao.yaml, goose.yaml, example-additional-snapshots.yaml,
      h1-triage.yaml, check-triage.yaml

    Special-case desktop.yaml (renamed, and guard against clobbering Plan 02's new file):
      Only move if the CURRENT profiles/desktop.yaml is the OLD monolith (no `extends:` line):
        if [ -f profiles/desktop.yaml ] && ! grep -q '^extends:' profiles/desktop.yaml; then
          git mv profiles/desktop.yaml testdata/profiles/desktop.legacy.yaml
        fi
      (If Plan 02 already authored the new composed desktop.yaml, it WILL have `extends:` and must
      NOT be moved. In that ordering the OLD desktop.yaml was already archived earlier — verify
      testdata/profiles/desktop.legacy.yaml exists; if not, the move was lost — STOP and report.)

    Do NOT move: profiles/base/**, profiles/checks/**, profiles/secrets/**, profiles/*.prompt.txt,
    profiles/learner.yaml, profiles/github.yaml, profiles/h1.yaml (Plan 02 leaves).

    Byte-identity proof after moving (git mv preserves bytes; confirm no content drift):
      git diff --cached --stat   # should show pure renames (R100), no content lines changed
  </action>
  <verify>
    <automated>set -e; for f in learn.v2 learn.v2.chatty learn.v2.codex learn.v2.polite learn.v2.parallel learn.v2.private-allow learn.v2.desktop dc34 dc34.ami codex locked locked.ami github-review ao goose example-additional-snapshots h1-triage check-triage; do test -f testdata/profiles/$f.yaml || { echo "MISSING testdata/profiles/$f.yaml"; exit 1; }; test ! -f profiles/$f.yaml || { echo "STILL IN profiles/: $f.yaml"; exit 1; }; done; test -f testdata/profiles/desktop.legacy.yaml || { echo "MISSING desktop.legacy.yaml"; exit 1; }; echo "ALL_MOVES_OK"</automated>
  </verify>
  <done>All 18 same-name profiles plus desktop.legacy.yaml are in testdata/profiles/ and absent from profiles/; moves are pure git renames (no byte changes).</done>
</task>

<task type="auto">
  <name>Task 2: Update the 6 test files / 7 path sites to testdata/profiles/</name>
  <files>pkg/compiler/userdata_phase92_byte_identity_test.go, pkg/compiler/agent_claude_golden_test.go, pkg/compiler/agent_codex_golden_test.go, pkg/profile/github_review_secrets_test.go</files>
  <action>
    Apply the 7 edits from the <interfaces> map exactly:
    - userdata_phase92_byte_identity_test.go: change filepath.Join(repoRoot, "profiles") →
      filepath.Join(repoRoot, "testdata", "profiles").
    - agent_claude_golden_test.go: change the 4 table path strings from "../../profiles/X.yaml"
      to "../../testdata/profiles/X.yaml" (learn.v2, dc34, locked, codex). Do NOT touch the
      goldenPath strings (those stay testdata/claude_settings_*.golden.json).
    - agent_codex_golden_test.go: change const profilePath to "../../testdata/profiles/codex.yaml".
    - github_review_secrets_test.go: change filepath.Join(repoRoot, "profiles", "github-review.yaml")
      → filepath.Join(repoRoot, "testdata", "profiles", "github-review.yaml").

    Then run a belt-and-suspenders audit for any REMAINING real profile path reference in test
    code (ignore the phantom tempdir filenames listed in <interfaces>):
      grep -rn 'profiles/' --include='*_test.go' pkg/ internal/
    Manually confirm any remaining hit is either (a) a phantom tempdir filename, or (b) a
    config-string value (e.g. doctor_github_test.go uses "profiles/github-review.yaml" as a
    non-empty config STRING, not a file read — leave it). If a hit is a REAL os.ReadFile /
    profile.Resolve / filepath.Join file path to a moved profile, update it too and note it in the
    SUMMARY (research expected exactly these 6 files; a 7th would be new information).
  </action>
  <verify>
    <automated>set -e; grep -q 'filepath.Join(repoRoot, "testdata", "profiles")' pkg/compiler/userdata_phase92_byte_identity_test.go; grep -q '"../../testdata/profiles/learn.v2.yaml"' pkg/compiler/agent_claude_golden_test.go; grep -q '"../../testdata/profiles/codex.yaml"' pkg/compiler/agent_codex_golden_test.go; grep -q '"testdata", "profiles", "github-review.yaml"' pkg/profile/github_review_secrets_test.go; ! grep -q '"../../profiles/' pkg/compiler/agent_claude_golden_test.go; ! grep -q '"../../profiles/' pkg/compiler/agent_codex_golden_test.go; echo "ALL_PATH_EDITS_OK"</automated>
  </verify>
  <done>All 7 path sites point at testdata/profiles/; no remaining "../../profiles/" string in the two golden tests; audit grep reviewed.</done>
</task>

<task type="auto">
  <name>Task 3: Run the byte-identity + golden + cmd suite (green gate)</name>
  <files>(no edits — verification task)</files>
  <action>
    Build and run the affected suites, capturing the COMMAND's own exit code (never a piped tail —
    a piped tail returns 0 and masks a FAIL; see MEMORY feedback_check_go_test_exit_not_pipe).

      make build
      go test ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... -count=1 -timeout 600s; echo "GO_TEST_EXIT=$?"

    The byte-identity tests (TestUserdataLearnV2Phase92ByteIdentity), the claude goldens
    (TestSynthesizeClaudeSettingsGolden), the codex golden (TestSynthesizeCodexConfigGolden), and
    the github-review secrets test (TestGitHubReviewProfileSecrets) MUST all pass against the moved
    inputs. If a golden test fails, do NOT re-capture any golden — the failure means a path edit is
    wrong (the input moved but the test still looks in profiles/), or a move changed bytes. Fix the
    path/move, never the frozen baseline.

    Known-environmental note (MEMORY): some internal/app/cmd Bootstrap/Cluster/Configure tests can
    fail with InvalidGrantException on expired AWS SSO — that is environmental, not a regression
    from this plan. If such a failure appears, confirm it is SSO-related and unrelated to profile
    paths, and record it in the SUMMARY; it does not block this plan.
  </action>
  <verify>
    <automated>make build >/dev/null 2>&1 && go test ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... -count=1 -timeout 600s; echo "GO_TEST_EXIT=$?"</automated>
  </verify>
  <done>go test ./pkg/compiler/... ./pkg/profile/... exits 0 (byte-identity + goldens + secrets test green); the cmd suite is green modulo any environmental AWS-SSO failures documented in the SUMMARY.</done>
</task>

</tasks>

<verification>
The move + path-edit must land as one coherent change with a green suite:

```bash
make build
go test ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... -count=1 -timeout 600s
echo "EXIT=$?"   # must be 0 (modulo documented environmental AWS-SSO failures)
```

Confirm no real profile path reference left dangling in test code:
```bash
grep -rn 'profiles/' --include='*_test.go' pkg/ internal/ | grep -v testdata
# Remaining hits must all be phantom tempdir names or config-string values (reviewed in Task 2).
```
</verification>

<success_criteria>
- 19 profiles archived under testdata/profiles/ via git mv (bytes unchanged; pure renames).
- profiles/ no longer contains any of the retired demos/fixtures.
- 6 test files / 7 path sites updated to testdata/profiles/.
- go test ./pkg/compiler/... ./pkg/profile/... exits 0 — byte-identity + goldens + secrets green.
- userdata_learn_v2_pre92_baseline.golden.sh untouched; golden OUTPUT files unmoved.
</success_criteria>

<output>
After completion, create `.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-03-SUMMARY.md`
</output>
