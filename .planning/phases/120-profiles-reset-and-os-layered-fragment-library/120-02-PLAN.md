---
phase: 120-profiles-reset-and-os-layered-fragment-library
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - profiles/learner.yaml
  - profiles/desktop.yaml
  - profiles/github.yaml
  - profiles/h1.yaml
autonomous: true
requirements: [R1, R7]
must_haves:
  truths:
    - "profiles/ contains exactly 4 composed operator-facing leaves: learner, desktop, github, h1"
    - "Each leaf extends base/os/* FIRST, then toolchain/plugin/knob fragments (initCommands union ordering)"
    - "km validate exits 0 with NO WARN for each of the 4 leaves"
    - "learner functionally matches today's learn.v2.yaml (same toolchain + slack + github inbound), with the documented plugin-enable delta"
    - "Each leaf owns its bool spec.runtime fields (spot/hibernation/mountEFS) — never inherited from an OS fragment"
  artifacts:
    - path: "profiles/learner.yaml"
      provides: "Learner leaf composed from os/redhat + toolchain + plugin + 8 knob fragments + slack-persandbox; t3.2xlarge/EFS/data-vol, 8h/1h/stop, github inbound, cli.noBedrock"
      contains: "extends:"
    - path: "profiles/desktop.yaml"
      provides: "Desktop leaf composed from os/debian + toolchain + plugin; leaf runtime.desktop (kiosk/firefox), email-on/slack-off"
      contains: "desktop:"
    - path: "profiles/github.yaml"
      provides: "GitHub leaf composed from os/redhat + toolchain + plugin + knob fragments; lean runtime, notification.github.inbound.enabled only (NO slack-persandbox), secrets.sopsFile"
      contains: "github:"
    - path: "profiles/h1.yaml"
      provides: "H1 leaf composed from os/redhat + toolchain + plugin + knob fragments; notification.h1.inbound.enabled, secrets.sopsFile (matches old h1-triage.yaml notification shape)"
      contains: "h1:"
  key_links:
    - from: "profiles/learner.yaml"
      to: "profiles/base/os/redhat.yaml"
      via: "extends list — os/redhat listed FIRST so OS yum/cert initCommands precede toolchain lines"
      pattern: "base/os/redhat"
    - from: "profiles/desktop.yaml"
      to: "profiles/base/os/debian.yaml"
      via: "extends — desktop uses ubuntu-24.04 from os/debian; desktop.enabled requires Ubuntu (km validate rule)"
      pattern: "base/os/debian"
---

<objective>
Author the 4 canonical composed leaves that replace the ~20-file profiles/ inventory:
learner, desktop, github, h1. Each is a thin composition over the Plan 01 fragments + the
8 pre-existing knob fragments, with only leaf-specific fields (bool runtime, lifecycle,
desktop block, secrets, notification routing) declared in-leaf.

Purpose: operator-facing demos that demonstrate the fragment library and keep version pins
out of the leaves. `learner` must functionally match today's learn.v2.yaml.
Output: profiles/learner.yaml, profiles/desktop.yaml, profiles/github.yaml, profiles/h1.yaml —
each km-validate clean (exit 0, NO WARN).

NOTE: This plan creates NEW profiles/desktop.yaml and profiles/github.yaml. The OLD
profiles/desktop.yaml is archived to testdata/profiles/desktop.legacy.yaml and the OLD
profiles/github-review.yaml is archived to testdata/profiles/github-review.yaml in Plan 03
(via git mv). Plan 03 and this plan touch DIFFERENT files and run in the same wave; the new
desktop.yaml/github.yaml here are fresh content, not edits of the archived originals.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-CONTEXT.md
@.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-RESEARCH.md

# The 5 NEW fragments authored in Plan 01 (extend these):
@profiles/base/os/redhat.yaml
@profiles/base/os/debian.yaml
@profiles/base/toolchain-agents.yaml
@profiles/base/plugin-klanker.yaml
@profiles/base/slack-persandbox.yaml

# Source profiles to functionally match (read for exact leaf-specific fields):
@profiles/learn.v2.yaml
@profiles/desktop.yaml
@profiles/github-review.yaml
@profiles/h1-triage.yaml

# Reference composition (an existing multi-parent leaf):
@profiles/dc34.yaml

<interfaces>
<!-- The 8 pre-existing knob fragments (extend by name). Verified contents: -->
- base/safenetwork          : spec.network.enforcement both, allowedDNSSuffixes/Hosts ["*"]
- base/sidecars-all         : spec.sidecars.{dnsProxy,httpProxy,auditLog,tracing} all enabled
- base/observability-learn  : spec.observability.{learnMode true, command/network log, claudeTelemetry, tlsCapture}
- base/budget-standard      : spec.budget.{compute 0.50, ai 2.00, warningThreshold 0.80}
- base/artifacts-workspace  : spec.artifacts.{paths [/workspace], maxSizeMB 500}
- base/iam-us-east-1        : spec.iam.{roleSessionDuration 1h, allowedRegions [us-east-1]}
- base/agent-claude-all-tools : spec.agent.claude.{trustedDirectories, tools.autoApprove[...], args [--dangerously-skip-permissions]}
- base/email-strict         : spec.email.{signing/verifyInbound/encryption required, allowedSenders ["*"]}

km validate WARN rules to AVOID (Research Task 7):
- S5: BOTH slack.enabled:false AND email.enabled:false ⇒ WARN. Keep at least 1 channel on per leaf.
- S2/S3/S-channelname: perSandbox-related slack fields set without perSandbox:true ⇒ WARN.
  (slack-persandbox fragment already sets perSandbox:true, so leaves that extend it are safe.)
- desktop.enabled:true on non-Ubuntu ami ⇒ WARN (desktop leaf uses os/debian = ubuntu-24.04).
- raw AMI id (^ami-) ⇒ WARN (OS fragments use slugs — safe).

BOOL ZERO-VALUE TRAP: spot/hibernation/mountEFS MUST be declared in the leaf's spec.runtime,
NOT inherited. The OS fragment only supplies the string ami.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author profiles/learner.yaml (functional match to learn.v2.yaml)</name>
  <files>profiles/learner.yaml</files>
  <action>
    Create profiles/learner.yaml — metadata.name: learner (keep prefix: learn so SANDBOX
    naming is stable; set labels tier/tool as appropriate).

    extends (ORDER MATTERS — os/* FIRST so OS initCommands precede toolchain):
      - base/os/redhat
      - base/toolchain-agents
      - base/plugin-klanker
      - base/safenetwork
      - base/sidecars-all
      - base/observability-learn
      - base/budget-standard
      - base/artifacts-workspace
      - base/iam-us-east-1
      - base/agent-claude-all-tools
      - base/email-strict
      - base/slack-persandbox

    Leaf-only fields (everything the fragments do NOT own), copied from learn.v2.yaml:
    - spec.lifecycle: ttl 8h, idleTimeout 1h, teardownPolicy stop.
    - spec.runtime: substrate ec2, instanceType t3.2xlarge, region us-east-1, rootVolumeSize 80,
      AND the bool fields IN-LEAF: spot:false, hibernation:false, mountEFS:true,
      efsMountPoint:/shared, additionalVolume {size:30, mountPoint:/data}. (Do NOT set ami here
      — it comes from base/os/redhat. The bools MUST be here, not in the fragment.)
    - spec.execution: shell /bin/bash, workingDir /workspace, useBedrock:false, privileged:true,
      and the leaf-only env key SANDBOX_MODE: learn-v2-direct-api (the rest of env comes from
      toolchain-agents). sourceAccess mode allowlist github allowedRepos:["*"] allowedRefs:["*"].
    - spec.cli.noBedrock: true.
    - spec.agent.default: claude.
    - spec.notification.github.inbound.enabled: true (leaf-only; slack block comes from
      slack-persandbox fragment). spec.notification.events {onIdle:false, onPermission:false}.
      spec.notification.email.enabled: false (slack-persandbox supplies slack.enabled:true, so
      S5 does NOT fire).

    DOCUMENTED DELTA: learner ENABLES the klanker plugin (via the enabledPlugins settings.json
    in base/plugin-klanker) — learn.v2.yaml intentionally left it disabled only to protect the
    frozen byte-identity fixture. Add a top-of-file YAML comment calling out that this is the one
    intentional functional delta from learn.v2.yaml (per CONTEXT §Resolved). This is REVIEWED in
    Plan 05, not asserted by byte-identity.
  </action>
  <verify>
    <automated>make build >/dev/null 2>&1; ./km validate profiles/learner.yaml > /tmp/v-learner.out 2>&1; rc=$?; grep -qi 'warn' /tmp/v-learner.out && w=1 || w=0; echo "VALIDATE_RC=$rc WARN_FOUND=$w"; [ "$rc" = 0 ] && [ "$w" = 0 ]</automated>
  </verify>
  <done>profiles/learner.yaml validates exit 0 with NO WARN; extends os/redhat first; bool runtime fields in-leaf; SANDBOX_MODE in-leaf; plugin-enable delta documented in a comment.</done>
</task>

<task type="auto">
  <name>Task 2: Author profiles/desktop.yaml (new composed desktop leaf)</name>
  <files>profiles/desktop.yaml</files>
  <action>
    Create the NEW profiles/desktop.yaml — metadata.name: desktop, prefix: desk. Compose from
    os/debian + toolchain + plugin; the desktop runtime block stays in-leaf.

    extends (os FIRST):
      - base/os/debian
      - base/toolchain-agents
      - base/plugin-klanker
      - base/sidecars-all
      - base/observability-learn
      - base/budget-standard
      - base/artifacts-workspace
      - base/iam-us-east-1
      - base/agent-claude-all-tools

    Leaf-only fields (from old profiles/desktop.yaml — read it before it is archived in Plan 03):
    - spec.lifecycle: ttl 4h, idleTimeout 30m, teardownPolicy stop.
    - spec.runtime: substrate ec2, instanceType t3.large, region us-east-1, rootVolumeSize 30,
      bool fields IN-LEAF: spot:false, hibernation:false. (ami ubuntu-24.04 comes from os/debian.)
      spec.runtime.desktop {enabled:true, mode:kiosk, browsers:[firefox], geometry:1920x1080} —
      STAYS IN LEAF (mixed-bool spec.runtime stays in leaf).
    - spec.execution: shell /bin/bash, workingDir /workspace, useBedrock:false, privileged:false.
      sourceAccess mode allowlist github allowedRepos:[] allowedRefs:[].
    - spec.notification: events {onIdle:false, onPermission:false}, email.enabled:true,
      slack.enabled:false. (email ON ⇒ S5 does NOT fire; NO slack-persandbox fragment here.)

    Keep desktop's explicit NARROW spec.network block IN-LEAF (copy from old desktop.yaml's
    egress.allowedDNSSuffixes list). Do NOT extend base/safenetwork here — lists union, which
    would broaden the allowlist to "*" and defeat the tightened desktop posture.

    Preserve the helpful "First-boot network caveat / AMI-bake recommended" header comment from
    the old desktop.yaml (copy it across) — operator-facing documentation.
  </action>
  <verify>
    <automated>./km validate profiles/desktop.yaml > /tmp/v-desktop.out 2>&1; rc=$?; grep -qi 'warn' /tmp/v-desktop.out && w=1 || w=0; echo "VALIDATE_RC=$rc WARN_FOUND=$w"; [ "$rc" = 0 ] && [ "$w" = 0 ]</automated>
  </verify>
  <done>profiles/desktop.yaml validates exit 0 NO WARN; os/debian first; desktop block + bools in-leaf; narrow network in-leaf (no safenetwork); email on / slack off.</done>
</task>

<task type="auto">
  <name>Task 3: Author profiles/github.yaml and profiles/h1.yaml (bridge leaves)</name>
  <files>profiles/github.yaml, profiles/h1.yaml</files>
  <action>
    profiles/github.yaml — metadata.name: github, prefix: gh. Replaces github-review.yaml.
    extends (os FIRST; FULL toolchain — single-pin-site is the point per CONTEXT §Leaf composition):
      - base/os/redhat
      - base/toolchain-agents
      - base/plugin-klanker
      - base/sidecars-all
      - base/observability-learn
      - base/budget-standard
      - base/artifacts-workspace
      - base/iam-us-east-1
      - base/agent-claude-all-tools
    Leaf-only fields (from old github-review.yaml):
    - spec.lifecycle: ttl 2h, idleTimeout 20m, teardownPolicy stop.
    - spec.runtime: substrate ec2, region us-east-1, instanceType t3.medium, spot:true (bool
      IN-LEAF). ami from os/redhat.
    - spec.execution: shell /bin/bash, workingDir /workspace, useBedrock:false.
      sourceAccess mode allowlist github allowedRepos:["whereiskurt/*"] allowedRefs:["main","develop","*"].
    - spec.notification.github.inbound.enabled: true ONLY. NO slack-persandbox fragment (avoid the
      slack WARN). Set spec.notification.email.enabled: true explicitly so S5 cannot fire and the
      operator gets email notifications for this lean bot.
    - spec.secrets.sopsFile: github-review-secrets.enc.yaml (cold-box self-auth bundle; keep the
      explanatory SOPS comment block from github-review.yaml). The sopsFile path is unchanged —
      it is resolved relative to the install's secrets staging, not the profile dir.
    Keep the operator-facing header comment describing the github comment-trigger flow.

    profiles/h1.yaml — metadata.name: h1, prefix: h1. NEW canonical leaf, replaces h1-triage.yaml.
    Compose, but H1 has a NARROW network + sourceAccess:none that do NOT fit the wide knob
    fragments — so for h1, extend ONLY the OS + toolchain + plugin + observability/iam/agent
    fragments and keep network + sourceAccess + sidecars IN-LEAF (copy from h1-triage.yaml):
      extends:
        - base/os/redhat
        - base/toolchain-agents
        - base/plugin-klanker
        - base/observability-learn
        - base/iam-us-east-1
        - base/agent-claude-all-tools
    Leaf-only fields (copy verbatim from h1-triage.yaml):
    - spec.lifecycle: ttl 2h, idleTimeout 20m, teardownPolicy stop.
    - spec.runtime: substrate ec2, spot:true (bool IN-LEAF), instanceType t3.medium, region us-east-1.
    - spec.execution: shell /bin/bash, workingDir /workspace, useBedrock:false.
    - spec.sourceAccess.mode: none (H1 reads/replies over the customer API; no repo clone).
    - spec.network: enforcement proxy, the NARROW egress.allowedDNSSuffixes from h1-triage.yaml
      (api.hackerone.com, .amazonaws.com, api.anthropic.com, .npmjs.org), allowedHosts:[].
      (NOT base/safenetwork — would broaden to "*". Keep in-leaf.)
    - spec.sidecars: the h1-triage sidecar block (dnsProxy/httpProxy/auditLog enabled, tracing
      disabled). Do NOT extend base/sidecars-all (it enables tracing — keep h1's choice in-leaf).
    - spec.notification.h1.inbound.enabled: true (the H1 bridge inbound shape — functionally match
      h1-triage.yaml). Set spec.notification.email.enabled: true so S5 cannot fire.
    - spec.secrets.sopsFile: h1-triage-secrets.enc.yaml (keep the SOPS comment block).
    Keep the operator-facing header comment describing the H1 comment-trigger triage flow.

    NOTE on toolchain weight for github/h1: the old github-review/h1-triage installed ONLY
    claude-code; the new leaves inherit the FULL toolchain (goose+codex+nvm+gsd+herdr) from
    base/toolchain-agents. This is the intended single-pin-site tradeoff per CONTEXT; call it out
    in each leaf header comment so the operator knows it is deliberate.
  </action>
  <verify>
    <automated>rc=0; for p in github h1; do ./km validate profiles/$p.yaml > /tmp/v-$p.out 2>&1; r=$?; grep -qi 'warn' /tmp/v-$p.out && w=1 || w=0; echo "$p RC=$r WARN_FOUND=$w"; { [ "$r" = 0 ] && [ "$w" = 0 ]; } || rc=1; done; echo "ALL_OK=$([ $rc = 0 ] && echo yes || echo no)"; [ $rc = 0 ]</automated>
  </verify>
  <done>profiles/github.yaml and profiles/h1.yaml both validate exit 0 NO WARN; github uses github inbound + email-on, no slack fragment; h1 uses h1 inbound + narrow network/sidecars/sourceAccess in-leaf; both carry secrets.sopsFile + deliberate-full-toolchain comment.</done>
</task>

</tasks>

<verification>
All 4 leaves validate clean (the load-bearing gate for this plan):

```bash
make build && for p in learner desktop github h1; do
  ./km validate profiles/$p.yaml > /tmp/v-$p.out 2>&1; rc=$?
  grep -qi 'warn' /tmp/v-$p.out && w=WARN || w=clean
  echo "$p: rc=$rc $w"
done
```
Every line must read `rc=0 clean`. Any WARN or non-zero rc is a failure.

Spot-check extends ordering (os/* must be first in each extends list):
```bash
for p in learner desktop github h1; do
  echo "=== $p ==="; awk '/^extends:/{f=1;next} f&&/^[^ -]/{f=0} f' profiles/$p.yaml | head -3
done
```
First extends entry of each leaf must be a base/os/* fragment.
</verification>

<success_criteria>
- profiles/ has exactly 4 leaves: learner, desktop, github, h1.
- Each `km validate profiles/<leaf>.yaml` exits 0 with NO WARN.
- Each leaf lists a base/os/* fragment first in extends.
- Bool runtime fields (spot/hibernation/mountEFS) live in each leaf, not in a fragment.
- learner is a functional match to learn.v2.yaml modulo the documented plugin-enable delta.
</success_criteria>

<output>
After completion, create `.planning/phases/120-profiles-reset-and-os-layered-fragment-library/120-02-SUMMARY.md`
</output>
