---
phase: 97-github-comment-trigger-mvp
plan: 06
type: execute
wave: 3
depends_on: ["97-01", "97-04", "97-05"]
files_modified:
  - internal/app/cmd/doctor.go
  - internal/app/cmd/doctor_github_test.go
  - docs/github-bridge.md
autonomous: false
requirements: [GH-DOCTOR, GH-E2E]
must_haves:
  truths:
    - "km doctor reports GitHub bridge health: App configured, webhook secret present, bot-login cached, bridge URL set, per-repo alias/profile resolvability + match-overlap warnings"
    - "When no github: block is configured, the github doctor checks are skipped silently (no spurious WARN)"
    - "End-to-end on a real PR: @-mention from an allowlisted login ⇒ 👀 within ack window ⇒ Claude runs (warm AND cold) ⇒ a PR review is posted by the bot via km-github review"
  artifacts:
    - path: "internal/app/cmd/doctor.go"
      provides: "GitHub bridge doctor checks"
      contains: "github"
    - path: "docs/github-bridge.md"
      provides: "operator runbook + deploy sequence + E2E UAT script"
  key_links:
    - from: "km doctor github checks"
      to: "SSM /{prefix}/config/github/* + cfg.Github.Repos"
      via: "presence + resolvability checks"
      pattern: "github"
---

<objective>
Add `km doctor` GitHub bridge health checks and write the operator runbook, then run the manual
end-to-end UAT (warm + cold paths) on a real GitHub PR.

Purpose: doctor closes the operational-visibility loop (every other Slack subsystem has doctor
checks), and the E2E is the only way to confirm the full webhook→sandbox→review round-trip against
real GitHub delivery + real AWS provisioning. This is the phase gate.

Output: doctor checks + tests, `docs/github-bridge.md`, and a verified E2E (operator checkpoint).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-CONTEXT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-RESEARCH.md

# Reuse targets (READ):
@internal/app/cmd/doctor.go                 # check pattern (slack_inbound / slack_bot_user_id_cached checks)
@internal/app/cmd/doctor_slack_inbound_test.go  # doctor test template

<interfaces>
<!-- Consumed from earlier plans: -->
<!-- config: cfg.Github.Repos + DefaultProfile (plan 01); Resolve() (plan 04) for resolvability/overlap -->
<!-- SSM (plan 01): /{prefix}/config/github/{webhook-secret,bot-login,bridge-url,app-client-id,private-key,installation-id} -->

Deploy sequence (CONTEXT § Deploy / rollout — document in docs/github-bridge.md):
1. make build-lambdas (CLEAN — avoid km-init-skips-existing-zips trap)
2. km init --dry-run=false (new Lambda + EventBridge + env block ⇒ FULL apply, NOT --sidecars)
3. km init --sidecars (schema field so create-handler picks up notification.github.inbound)
4. GitHub: km github manifest → update App scopes + add issue_comment webhook + set URL/secret → re-approve install
5. Existing sandboxes: km destroy && km create (to gain queue+poller+helper+write-scoped token)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: km doctor GitHub bridge checks + tests</name>
  <files>internal/app/cmd/doctor.go, internal/app/cmd/doctor_github_test.go</files>
  <behavior>
    - with github configured: checks PASS/WARN for App configured (app-client-id/private-key/installation-id),
      webhook-secret present, bot-login cached, bridge-url set
    - resolvability: each github.repos entry resolves to a known/validatable profile; match-overlap
      (two entries that would both match a repo) ⇒ WARN
    - with NO github: block: github checks are skipped silently (no spurious WARN), mirroring how
      slack checks skip when slack is unconfigured
  </behavior>
  <action>
    Add GitHub bridge checks to internal/app/cmd/doctor.go (clone the slack_inbound / bot_user_id_cached
    check pattern): github_app_configured, github_webhook_secret, github_bot_login_cached,
    github_bridge_url, github_repos_resolvable (+ match-overlap WARN using plan-04 Resolve / a pure
    overlap scan over cfg.Github.Repos). Gate the whole group: if cfg.Github.Repos is empty AND no
    github SSM keys ⇒ skip silently.
    Clone doctor_slack_inbound_test.go → doctor_github_test.go: configured-healthy, missing-secret-WARN,
    overlap-WARN, and unconfigured-skipped cases (mocked SSM via github.MockSSMClient).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run 'GitHubDoctor|DoctorGithub' -count=1</automated>
  </verify>
  <done>Doctor reports App/secret/bot-login/bridge-url/resolvability + overlap WARN; skips silently when unconfigured.</done>
</task>

<task type="auto">
  <name>Task 2: docs/github-bridge.md operator runbook + full-suite gate</name>
  <files>docs/github-bridge.md</files>
  <action>
    Write docs/github-bridge.md (mirror docs/slack-notifications.md structure): architecture overview
    (GitHub-shaped twin of the Slack inbound path), the github.repos config surface + example,
    GitHub App scope table (issues/pull_requests/contents/checks write + issue_comment webhook),
    the lean github-review profile, the km github init/manifest/status + km-github comment/review
    surfaces, the deploy sequence from <interfaces> (verbatim, incl. the --sidecars step for the
    schema field), the dormant invariant note, and troubleshooting. Add a CLAUDE.md "Where to look"
    pointer row if appropriate (Phase 97 entry).
    Then run the full suite + build + profile gate as the pre-E2E gate.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && make build && go test ./... -count=1 && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>Runbook documents config/scopes/profile/CLI/deploy/troubleshooting; full suite + build + profile gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end UAT on a real PR (warm + cold + negative)</name>
  <action>
    Operator runs the real-AWS + real-GitHub end-to-end verification below. This is the GH-E2E phase
    gate and cannot be automated (real webhook delivery + real sandbox provisioning).
  </action>
  <what-built>
    The complete GitHub comment-trigger path: km-github-bridge Lambda (HMAC verify, loop/dedupe/mention/allowlist,
    repo→alias resolve, warm-enqueue/cold-SandboxCreate, 👀 ACK), per-sandbox github-inbound queue + source-aware
    poller, km-github comment/review helper, github-review profile, km github init/manifest/status, km doctor checks.
  </what-built>
  <how-to-verify>
    Deploy (operator machine, AWS_PROFILE=klanker-application):
      1. make build-lambdas         # CLEAN build of the new bridge zip
      2. km init --dry-run=false    # new Lambda + EventBridge + env block (NOT --sidecars)
      3. km init --sidecars         # schema field so create-handler picks up notification.github.inbound
      4. Configure github.repos: in km-config.yaml (match owner/repo, alias, profile: github-review,
         allow: [your-github-login]); re-run km init --dry-run=false to export KM_GITHUB_REPOS.
      5. km github manifest > /tmp/km-gh.json ; update the App scopes + add the issue_comment webhook,
         set Webhook URL = bridge function_url + the generated secret (km github init); re-approve the install.
      6. km github status ; km doctor   # confirm all GitHub checks green.
    WARM path test:
      7. km create profiles/github-review.yaml --alias gh-<owner>-<repo>   # pre-warm the sandbox
      8. On a real PR in the allowlisted repo, comment:  @<bot-login> review this PR
      9. EXPECT: 👀 reaction within ~10s, then within a few minutes a PR review posted by the bot.
    COLD path test:
      10. Ensure NO sandbox for a second allowlisted repo's alias; comment @<bot-login> review this PR there.
      11. EXPECT: SandboxCreate fires, create-handler provisions, the carried envelope drains on first
          boot, and a review is eventually posted.
    NEGATIVE checks:
      12. Comment from a NON-allowlisted login ⇒ NO 👀, no review, nothing in logs dispatched (silent).
      13. Redeliver the same webhook (GitHub UI "Redeliver") ⇒ no double dispatch (GUID dedupe).
  </how-to-verify>
  <resume-signal>Type "approved" once 👀 + review post are observed on BOTH warm and cold paths and the negative checks hold, or describe what failed (for --gaps planning).</resume-signal>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run GitHubDoctor -count=1` green
- `make build && go test ./... -count=1 && bash scripts/validate-all-profiles.sh` green
- Manual E2E (warm + cold + negative) approved at the checkpoint
</verification>

<success_criteria>
- km doctor reports GitHub bridge health (App/secret/bot-login/bridge-url/resolvability + overlap); skips when unconfigured
- docs/github-bridge.md is a complete operator runbook with the exact deploy sequence
- Real-PR E2E: 👀 within ack window ⇒ Claude runs (warm + cold) ⇒ review posted; unauthorized silently ignored; redelivery deduped
</success_criteria>

<output>
After completion, create `.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-06-SUMMARY.md`
</output>
