---
phase: 97-github-comment-trigger-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/github/token.go
  - pkg/github/token_test.go
  - pkg/aws/eventbridge.go
  - pkg/aws/sqs.go
  - pkg/aws/sqs_test.go
  - cmd/create-handler/main.go
  - internal/app/cmd/create.go
autonomous: true
requirements: [GH-BRIDGE-ROUTE]
must_haves:
  truths:
    - "CompilePermissions maps comment/review verbs to issues:write + pull_requests:write (+ contents/checks:write) so per-sandbox tokens can post back"
    - "generateAndStoreGitHubToken requests write permissions (not nil) for github-inbound sandboxes"
    - "SandboxCreateDetail + CreateEvent carry an optional github envelope (pending prompt) for cold-create"
    - "GitHubInboundQueueName helper exists in pkg/aws so create-handler builds standalone (no intra-wave ordering hazard)"
    - "create-handler drains the carried github envelope into the new sandbox's github-inbound queue after provisioning so the poller drains it on first boot"
  artifacts:
    - path: "pkg/github/token.go"
      provides: "CompilePermissions extended with write scopes"
      contains: "pull_requests"
    - path: "pkg/aws/eventbridge.go"
      provides: "SandboxCreateDetail with carried-envelope field"
      contains: "SandboxCreateDetail"
    - path: "pkg/aws/sqs.go"
      provides: "GitHubInboundQueueName helper (mirrors SlackInboundQueueName)"
      contains: "GitHubInboundQueueName"
    - path: "cmd/create-handler/main.go"
      provides: "CreateEvent envelope field + post-provision enqueue into github-inbound queue"
      contains: "CreateEvent"
  key_links:
    - from: "km-github-bridge cold path"
      to: "create-handler"
      via: "SandboxCreateDetail.GithubEnvelope carried over EventBridge"
      pattern: "GithubEnvelope|PendingPrompt"
    - from: "create-handler"
      to: "github-inbound FIFO queue"
      via: "SendMessage to GitHubInboundQueueName after queue provisioned"
      pattern: "SendMessage|GitHubInboundQueueName"
---

<objective>
Two foundation pieces the bridge depends on: (1) extend the GitHub installation-token permission
compilation so per-sandbox tokens can actually post comments/reviews, and (2) adopt the research's
**cold-create correction** — carry the github envelope through the `SandboxCreate` EventBridge event
and have `create-handler` drain it into the new sandbox's `github-inbound` queue after provisioning.

Purpose: The design spec's "Phase 86 prompt-queue carry" is NOT implementable as written (Phase 86
is operator-side post-create SSM push; `SandboxCreateDetail`/`CreateEvent` have no prompt field; the
bridge has no operator shell). This plan implements the recommended fix (Pitfall 1 option A) so the
cold path actually delivers the prompt. Without the write-scope fix, `km-github review` 403s.

NOTE (intra-wave decoupling): the `GitHubInboundQueueName(prefix, id)` helper is OWNED BY THIS PLAN
(Task 3, in `pkg/aws/sqs.go`) — not plan 03 — so `cmd/create-handler` compiles standalone regardless
of plan execution order within Wave 1. Plan 03 reuses this already-existing helper for the rest of
the github-inbound trio (`CreateGitHubInboundQueue`/`DeleteGitHubInboundQueue`). Both plans stay in
Wave 1 with no cross-dependency because they touch disjoint identifiers in `pkg/aws/sqs.go`.

Output: extended `CompilePermissions` + call-site; `SandboxCreateDetail`/`CreateEvent` envelope
field; the `GitHubInboundQueueName` helper; create-handler enqueue hook.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-CONTEXT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-RESEARCH.md

# Reuse targets (READ):
@pkg/github/token.go                 # CompilePermissions:197, WriteTokenToSSM:226, ExchangeForInstallationToken:119
@pkg/aws/eventbridge.go              # SandboxCreateDetail:21, PutSandboxCreateEvent:37
@pkg/aws/sqs.go                       # SlackInboundQueueName:44 (mirror for GitHubInboundQueueName)
@cmd/create-handler/main.go          # CreateEvent:46, km create subprocess args :200

<interfaces>
<!-- Contracts the cold-create correction creates. Wave-2 bridge (plan 04) writes to these. -->

pkg/aws/eventbridge.go:
- `SandboxCreateDetail` (:21) — ADD an optional `GithubEnvelope string `json:"github_envelope,omitempty"`` field
  (the JSON-serialized {source:github,...} envelope). Keep PutSandboxCreateEvent unchanged in shape.

pkg/aws/sqs.go:
- ADD `GitHubInboundQueueName(prefix, id string) string` ⇒ "{prefix}-github-inbound-{id}.fifo"
  (mirror SlackInboundQueueName:44). THIS PLAN OWNS THIS HELPER so create-handler builds standalone.
  Plan 03 builds CreateGitHubInboundQueue/DeleteGitHubInboundQueue on top of it (does NOT redefine it).

cmd/create-handler/main.go:
- `CreateEvent` (:46) — ADD the matching `GithubEnvelope string `json:"github_envelope,omitempty"`` field.
- After the sandbox is created AND the github-inbound queue is provisioned (the queue name is
  deterministic via `awspkg.GitHubInboundQueueName(prefix, id)` — defined in Task 3 of THIS plan), if
  GithubEnvelope != "" then SendMessage it to that queue so the poller drains it on first boot.

pkg/github/token.go:
- `CompilePermissions([]string) map[string]string` (:197) — currently maps clone/fetch→contents:read,
  push→contents:write. EXTEND with comment→issues:write, review→pull_requests:write, and request
  contents:write + checks:write for github-inbound sandboxes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CompilePermissions + pass write perms at create call-site</name>
  <files>pkg/github/token.go, pkg/github/token_test.go, internal/app/cmd/create.go</files>
  <behavior>
    - CompilePermissions(["comment"]) ⇒ includes issues:write
    - CompilePermissions(["review"]) ⇒ includes pull_requests:write
    - the github-inbound permission set requests issues:write, pull_requests:write, contents:write, checks:write
    - existing clone/fetch→contents:read, push→contents:write mappings unchanged (no regression)
  </behavior>
  <action>
    EXTEND pkg/github/token.go CompilePermissions (:197): add verb→permission mappings for
    comment→issues:write and review→pull_requests:write; keep existing clone/fetch/push mappings.
    Provide a helper (or named verb set) representing the github-inbound write set
    {issues:write, pull_requests:write, contents:write, checks:write}.
    At internal/app/cmd/create.go:1317, generateAndStoreGitHubToken is currently called with
    permissions=nil ⇒ read-only token (Pitfall 6). Change the call to pass the github-inbound write
    set when SourceAccess.GitHub has non-empty AllowedRepos (the existing gate at :1310). The token
    stays repo-scoped via allowedRepos.
    Add table-driven cases to token_test.go for the new verb mappings.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/github/ -run 'CompilePermissions|Permissions' -count=1</automated>
  </verify>
  <done>New verb mappings present + tested; create.go passes write perms (not nil) for github-inbound sandboxes; no regression to existing mappings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry github envelope through SandboxCreateDetail + CreateEvent</name>
  <files>pkg/aws/eventbridge.go, cmd/create-handler/main.go</files>
  <behavior>
    - SandboxCreateDetail marshals/unmarshals a GithubEnvelope string field (omitempty)
    - CreateEvent unmarshals the github_envelope field from the EventBridge detail
    - empty GithubEnvelope ⇒ byte-identical to today's create path (dormant for non-github creates)
  </behavior>
  <action>
    pkg/aws/eventbridge.go: add `GithubEnvelope string `json:"github_envelope,omitempty"`` to
    SandboxCreateDetail (:21). PutSandboxCreateEvent (:37) already json.Marshals the whole detail —
    no signature change needed.
    cmd/create-handler/main.go: add the matching `GithubEnvelope string `json:"github_envelope,omitempty"``
    to CreateEvent (:46). Do NOT yet enqueue (that is Task 3).
    Add a round-trip test (marshal detail → unmarshal into CreateEvent) asserting the envelope survives
    and that empty stays empty.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/aws/ -run 'SandboxCreate|EventBridge' -count=1 && go test ./cmd/create-handler/ -count=1</automated>
  </verify>
  <done>GithubEnvelope round-trips detail→CreateEvent; empty value is omitted; create-handler still builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GitHubInboundQueueName helper + create-handler drains carried envelope post-provision</name>
  <files>pkg/aws/sqs.go, pkg/aws/sqs_test.go, cmd/create-handler/main.go</files>
  <behavior>
    - GitHubInboundQueueName("km","abc") == "km-github-inbound-abc.fifo" (mirrors SlackInboundQueueName)
    - create-handler: GithubEnvelope != "" ⇒ resolves the queue URL via GitHubInboundQueueName and
      SendMessage's the envelope; empty ⇒ no enqueue (non-github creates unaffected)
    - cmd/create-handler builds standalone (no dependency on plan 03 landing first)
  </behavior>
  <action>
    FIRST, in pkg/aws/sqs.go, ADD `GitHubInboundQueueName(prefix, id string) string` returning
    "{prefix}-github-inbound-{id}.fifo" — clone SlackInboundQueueName (:44). This helper lives in THIS
    plan (not plan 03) precisely so create-handler compiles regardless of intra-wave order. Add a
    unit test in pkg/aws/sqs_test.go (mirror the SlackInboundQueueName test).
    THEN, in cmd/create-handler/main.go, after the sandbox is successfully created and its github-inbound
    FIFO queue exists, if `event.GithubEnvelope != ""` SendMessage the envelope to that queue. Resolve
    the queue URL via `awspkg.GitHubInboundQueueName(prefix, id)` + GetQueueUrl, then SendMessage with a
    MessageGroupId + MessageDeduplicationId (FIFO requires both; content-based dedup is OFF per the
    slack-inbound queue config). The poller drains it on first boot. Best-effort: log (do not fail the
    create) if the enqueue errors — the operator can re-mention.
    Plan 03 will build CreateGitHubInboundQueue/DeleteGitHubInboundQueue on top of this name helper;
    it MUST NOT redefine GitHubInboundQueueName.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/aws/ -run 'GitHubInboundQueueName' -count=1 && go test ./cmd/create-handler/ -count=1 && go build ./cmd/create-handler/</automated>
  </verify>
  <done>GitHubInboundQueueName defined + tested in pkg/aws; create-handler enqueues the carried envelope into {prefix}-github-inbound-{id}.fifo after provisioning; builds standalone; non-github creates unaffected.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/github/ -run Permissions -count=1` green
- `go test ./pkg/aws/ -run 'SandboxCreate|GitHubInboundQueueName' -count=1` and `go test ./cmd/create-handler/ -count=1` green
- `go build ./cmd/create-handler/` green standalone
- `make build` succeeds
</verification>

<success_criteria>
- Per-sandbox github tokens carry issues:write + pull_requests:write (+ contents/checks:write)
- GitHubInboundQueueName helper lives in pkg/aws (owned by this plan) so create-handler builds standalone
- Cold-create correctly carries the github envelope to create-handler, which enqueues it into the
  per-sandbox github-inbound queue (the research-mandated Pitfall-1 fix)
- Non-github creates remain byte-identical (empty envelope omitted)
</success_criteria>

<output>
After completion, create `.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-02-SUMMARY.md`
</output>
