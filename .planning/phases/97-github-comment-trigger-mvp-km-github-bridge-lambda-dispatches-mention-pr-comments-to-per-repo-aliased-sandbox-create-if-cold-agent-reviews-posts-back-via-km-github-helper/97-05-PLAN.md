---
phase: 97-github-comment-trigger-mvp
plan: 05
type: execute
wave: 2
depends_on: ["97-02", "97-03"]
files_modified:
  - pkg/compiler/userdata.go
  - pkg/compiler/userdata_github_inbound_test.go
  - cmd/km-github/main.go
  - cmd/km-github/main_test.go
autonomous: true
requirements: [GH-POLLER, GH-HELPER]
must_haves:
  truths:
    - "When github-inbound is enabled the userdata emits a source-aware poller that drains the github-inbound FIFO"
    - "The poller builds a GitHub context preamble (repo/PR number/branch/head + worktree-per-PR guidance) from the {source:github,...} envelope and dispatches the free-form body to the agent via the tmux agent-run path"
    - "When github-inbound is disabled the userdata emits NO github poller block (dormant byte-identity)"
    - "km-github comment posts to /repos/{owner}/{repo}/issues/{n}/comments using the per-sandbox SSM token"
    - "km-github review posts to /repos/{owner}/{repo}/pulls/{n}/reviews with event APPROVE|COMMENT|REQUEST_CHANGES + body"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "source-aware inbound poller draining github-inbound queue"
      contains: "KM_GITHUB_INBOUND_QUEUE_URL"
    - path: "cmd/km-github/main.go"
      provides: "km-github helper: comment + review verbs (per-sandbox installation token)"
      contains: "review"
  key_links:
    - from: "cmd/km-github/main.go"
      to: "GitHub REST API"
      via: "per-sandbox token read from SSM {prefix}sandbox/{id}/github-token"
      pattern: "github-token|GitHubAPIBaseURL"
    - from: "pkg/compiler/userdata.go github poller"
      to: "agent tmux dispatch"
      via: "preamble + body to agent-run"
      pattern: "preamble|agent"
---

<objective>
Make the sandbox side source-aware: extend the userdata inbound poller to drain the `github-inbound`
FIFO queue, build a GitHub context preamble, and dispatch to the agent; and build the `km-github`
helper (`comment`, `review` verbs) that posts back via the per-sandbox installation token.

Purpose: The bridge (plan 04) puts envelopes on the queue; this plan is the sandbox-side consumer
that turns an envelope into an agent turn and lets the agent post a review back to GitHub. Clone of
the Slack poller heredoc + the `km-slack` helper.

Output: source-aware poller in `userdata.go` (+ render test) and `cmd/km-github/main.go` (+ tests).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-CONTEXT.md
@.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-RESEARCH.md

# Reuse targets (READ — clone these):
@pkg/compiler/userdata.go            # slack poller heredoc :1517-2063, dispatch fork :1889, systemd unit :2343, km-git-askpass token read :528
@cmd/km-slack/main.go                # dispatch table :48-72, runPost :87 (helper template)
@pkg/github/token.go                 # GitHubAPIBaseURL httptest var :27 (use for test base URL); X-GitHub-Api-Version header convention

<interfaces>
<!-- Consumed from wave-1 plans: -->
<!-- env: KM_GITHUB_INBOUND_QUEUE_URL (plan 03) — poller sources from this when set -->
<!-- token: per-sandbox token at SSM {prefix}sandbox/{id}/github-token, write-scoped (plan 02) -->

Envelope JSON (bridge → queue, RESEARCH/CONTEXT):
{source:"github", repo:"owner/repo", number:N, kind:"pr", comment_id:N, html_url, branch, head_sha, sender, body}

GitHub REST (RESEARCH § Code Examples):
- comment: POST /repos/{owner}/{repo}/issues/{n}/comments  body {"body":"..."}  (issues:write)
- review:  POST /repos/{owner}/{repo}/pulls/{n}/reviews     body {"event":"APPROVE|COMMENT|REQUEST_CHANGES","body":"...","commit_id":"<sha?>"}  (pull_requests:write)
- headers: Authorization: Bearer <token>; Accept: application/vnd.github+json; X-GitHub-Api-Version: 2022-11-28
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: source-aware github poller in userdata + render test</name>
  <files>pkg/compiler/userdata.go, pkg/compiler/userdata_github_inbound_test.go</files>
  <behavior>
    - when github-inbound is enabled, rendered userdata contains a poller that reads
      KM_GITHUB_INBOUND_QUEUE_URL, receives/deletes FIFO messages, parses the github envelope, builds a
      preamble (repo / PR #number / branch / head_sha + worktree-per-PR guidance), appends the body,
      and dispatches via the existing tmux agent-run path
    - when github-inbound is disabled, NO github poller block is emitted (dormant byte-identity)
    - the slack poller block is unchanged (no regression)
  </behavior>
  <action>
    Extend pkg/compiler/userdata.go: clone the slack poller heredoc (:1517-2063) into a github variant
    gated on github-inbound enabled. Source the queue URL from KM_GITHUB_INBOUND_QUEUE_URL. On each
    message build a GitHub context preamble per RESEARCH § Response model (repo/PR/branch/head +
    worktree-per-PR guidance for concurrent PRs in a long-lived box), append the free-form envelope
    body, and dispatch to the agent via the existing tmux agent-run fork (:1889). Add a systemd unit
    (or extend the existing inbound unit) like :2343. Make the dispatch source-aware so slack + github
    can coexist.
    Add pkg/compiler/userdata_github_inbound_test.go (mirror userdata_slack_inbound_test.go): assert the
    poller block + preamble render when enabled, and assert ABSENCE when disabled (dormant invariant).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/compiler/ -run 'GitHubInbound' -count=1</automated>
  </verify>
  <done>Enabled ⇒ github poller + preamble rendered; disabled ⇒ no github block; slack poller unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: km-github helper — comment + review verbs</name>
  <files>cmd/km-github/main.go, cmd/km-github/main_test.go</files>
  <behavior>
    - km-github comment --repo owner/repo --number N --body "..." ⇒ POST issues/{n}/comments with the body
    - km-github review --repo owner/repo --number N --event COMMENT --body "..." ⇒ POST pulls/{n}/reviews with {event,body}
    - event defaults / validation: APPROVE|COMMENT|REQUEST_CHANGES; body required for COMMENT/REQUEST_CHANGES
    - requests carry Authorization Bearer <per-sandbox token> + Accept + X-GitHub-Api-Version headers
    - asserted against an httptest server via GitHubAPIBaseURL (token.go:27)
  </behavior>
  <action>
    Create cmd/km-github/main.go (separate binary, mirror cmd/km-slack/main.go dispatch table :48-72 and
    runPost :87 — Open Question 4 recommends a separate binary symlinked into /usr/local/bin like km-slack
    at userdata.go:1113; add that symlink wiring in userdata if needed). Verbs:
    - comment: POST /repos/{owner}/{repo}/issues/{n}/comments {"body":...} (issues:write).
    - review: POST /repos/{owner}/{repo}/pulls/{n}/reviews {"event":...,"body":...,"commit_id":<opt>}
      (pull_requests:write); validate event ∈ {APPROVE,COMMENT,REQUEST_CHANGES} and require body for
      COMMENT/REQUEST_CHANGES.
    Read the per-sandbox token from SSM {prefix}sandbox/{id}/github-token (mirror km-git-askpass token
    read at userdata.go:528). Set Accept + X-GitHub-Api-Version: 2022-11-28. Use GitHubAPIBaseURL var so
    main_test.go can point at httptest.
    main_test.go: assert request method/path/body/headers for both verbs against an httptest server.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./cmd/km-github/ -count=1 && go build ./cmd/km-github/</automated>
  </verify>
  <done>comment + review build correct requests with the per-sandbox token + version header; event validation enforced; tests green against httptest.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/ -run GitHubInbound -count=1` green (enabled render + dormant absence)
- `go test ./cmd/km-github/ -count=1` green
- `make build` succeeds
</verification>

<success_criteria>
- Source-aware poller drains github-inbound, builds preamble, dispatches to the agent; dormant when disabled
- km-github comment + review post back via the per-sandbox installation token with correct API shape
</success_criteria>

<output>
After completion, create `.planning/phases/97-github-comment-trigger-mvp-km-github-bridge-lambda-dispatches-mention-pr-comments-to-per-repo-aliased-sandbox-create-if-cold-agent-reviews-posts-back-via-km-github-helper/97-05-SUMMARY.md`
</output>
