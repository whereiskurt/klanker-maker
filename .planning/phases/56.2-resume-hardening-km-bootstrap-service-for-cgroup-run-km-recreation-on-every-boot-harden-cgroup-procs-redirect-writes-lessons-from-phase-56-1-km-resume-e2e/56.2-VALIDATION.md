---
phase: 56.2
slug: resume-hardening-km-bootstrap-service-for-cgroup-run-km-recreation-on-every-boot-harden-cgroup-procs-redirect-writes-lessons-from-phase-56-1-km-resume-e2e
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 56.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go testing (`go test`) |
| **Config file** | none — standard Go test runner |
| **Quick run command** | `go test ./pkg/compiler/...` |
| **Full suite command** | `go test ./...` |
| **Estimated runtime** | ~10–20 seconds (quick), ~2–4 minutes (full) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/compiler/...`
- **After every plan wave:** Run `go test ./...`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 56.2-01-01 | 01 | 1 | P56.2-01, P56.2-02, P56.2-03, P56.2-04, P56.2-05, P56.2-07 | unit (RED) | `! go test ./pkg/compiler/... -run "TestKMBootstrapServiceUnit\|TestKMBootstrapScript\|TestKMBootstrapEnabled\|TestCgroupProcsRedirectHardened"` | ❌ W0 | ⬜ pending |
| 56.2-01-02 | 01 | 1 | P56.2-01, P56.2-02, P56.2-03, P56.2-04, P56.2-05, P56.2-07 | unit (GREEN) | `go test ./pkg/compiler/... -run "TestKMBootstrapServiceUnit\|TestKMBootstrapScript\|TestKMBootstrapEnabled\|TestCgroupProcsRedirectHardened"` | ❌ W0 | ⬜ pending |
| 56.2-01-03 | 01 | 1 | P56.2-06 | regression | `go test ./pkg/compiler/... && go build ./...` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `pkg/compiler/userdata_test.go` — add `TestKMBootstrapServiceUnit`: generated userdata contains the systemd unit written to `/etc/systemd/system/km-bootstrap.service` with `Type=oneshot`, `RemainAfterExit=yes`, `Before=amazon-ssm-agent.service`, and does **not** contain `Wants=km.slice`. Stubs P56.2-01, P56.2-07.
- [ ] `pkg/compiler/userdata_test.go` — add `TestKMBootstrapScript`: generated userdata contains the `/usr/local/bin/km-bootstrap` script body sourcing `/etc/profile.d/km-identity.sh`, `mkdir -p /sys/fs/cgroup/km.slice/km-${SANDBOX_ID}.scope`, `mkdir -p /run/km`, and a `learnMode`-conditional `touch /run/km/learn-commands.log` (present when learnMode=true, absent when false). Stubs P56.2-02.
- [ ] `pkg/compiler/userdata_test.go` — add `TestKMBootstrapEnabled`: generated userdata contains `systemctl enable km-bootstrap` (or `systemctl enable --now km-bootstrap`) AFTER `systemctl daemon-reload`. Stubs P56.2-03.
- [ ] `pkg/compiler/userdata_test.go` — add `TestCgroupProcsRedirectHardened`: with `enforcement: ebpf` (or `both`), generated userdata uses the compound-command form `{ echo $$ > … cgroup.procs; } 2>/dev/null || true` at both call sites (km-cgroup.sh profile.d hook and km-sandbox-shell wrapper); the bare-redirect form `echo $$ > /sys/fs/cgroup` does NOT appear without the surrounding `{ … } 2>/dev/null` wrapper. Count of compound-form occurrences ≥ 2. Stubs P56.2-04, P56.2-05.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `km shell` after `km resume` shows no `cgroup.procs` or `learn-commands.log` errors | P56.2-01..05 | Requires real EC2 stop+start cycle on a sandbox baked from Phase 56's --ami flow | `km create profiles/dc34.ami.yaml --remote`; let it stabilize; `km stop dc34-1`; `km resume dc34-1`; `km shell dc34-1`; confirm no `bash: …: No such file or directory` lines on session entry |
| Sandbox shell is correctly placed in the eBPF cgroup after resume | P56.2-01 | Requires running on EC2 with eBPF enforcer attached | `km shell dc34-1`; inside: `cat /proc/self/cgroup` should show `…/km.slice/km-dc34-7ba0e7bf.scope`; `cat /sys/fs/cgroup/km.slice/km-dc34-7ba0e7bf.scope/cgroup.procs` should include the shell PID |
| Learn-mode command capture continues to work after resume | P56.2-02 | Requires real shell + tmpfs lifecycle | On a learnMode profile, `km stop` + `km resume`, then `km shell --learn`; type a few commands, exit; confirm `learned.*.yaml` includes the typed commands as `initCommands` |
| km-bootstrap.service status reachable via systemctl | P56.2-01 | Requires running EC2 + SSM | `km shell --root dc34-1`; `systemctl status km-bootstrap` should show `active (exited)` and `Loaded: loaded (/etc/systemd/system/km-bootstrap.service; enabled)` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
