---
phase: 110-session-aware-slack-reply-thread-channel-repair
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - internal/app/cmd/slack.go
  - internal/app/cmd/slack_reply.go
  - internal/app/cmd/slack_reply_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operator can run km slack reply --session <id> to post into that session's bound thread"
    - "Operator session lookup queries the GSI directly with operator creds (no bridge)"
    - "km slack reply --thread <ts> --channel <id> posts verbatim via chat.postMessage"
    - "--sandbox/--alias resolves the channel for the root-fallback case"
  artifacts:
    - path: "internal/app/cmd/slack_reply.go"
      provides: "RunSlackReply + newSlackReplyCmd (operator reply)"
      contains: "RunSlackReply"
    - path: "internal/app/cmd/slack.go"
      provides: "reply registered in newSlackCmdInternal + DDB client in SlackCmdDeps"
      contains: "newSlackReplyCmd"
  key_links:
    - from: "internal/app/cmd/slack_reply.go"
      to: "km-slack-threads session-index GSI"
      via: "operator DDB Query IndexName=session-index"
      pattern: "session-index"
    - from: "internal/app/cmd/slack_reply.go"
      to: "Slack chat.postMessage"
      via: "deps.Slack.PostMessage (bot token, same client as test/invite)"
      pattern: "PostMessage"
---

<objective>
Add the operator-side `km slack reply`. Unlike the sandbox path, the operator has
AWS creds and queries the `session-index` GSI DIRECTLY (no bridge). It posts via
Slack `chat.postMessage` with the bot token (same `*kmslack.Client` as
`km slack test` / `invite`).

Flags:
- `--session <id>` → Query GSI directly → (channel_id, thread_ts) → post into thread
- `--sandbox <id>` / `--alias <name>` → resolve channel for the root-fallback post
- `--thread <ts>` + `--channel <id>` → post verbatim into that thread
- `--body <file>` + `--render plain|mrkdwn|blocks` (same semantics as sandbox-side)

Purpose: Operator can reply into a session's thread without a running sandbox.
Output: km slack reply subcommand + GSI client wired into SlackCmdDeps.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-RESEARCH.md
@.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-VALIDATION.md

@internal/app/cmd/slack.go
@internal/app/cmd/slack_adopt.go

<interfaces>
<!-- newSlackCmdInternal (slack.go:128) assembles subcommands via AddCommand. -->
<!-- Currently: init,test,status,rotate-token,rotate-signing-secret,manifest,invite,adopt. -->
<!-- SlackCmdDeps.Slack is a *kmslack.Client (SlackAPI) wired from bot token -->
<!-- (slack.go:228-230); chat.postMessage = SlackAPI.PostMessage. -->
<!-- slack_adopt.go is the cleanest operator-DDB-write template (Cobra + flags + -->
<!-- DDB + SSM + test). Mirror it for command shape + dep wiring. -->
<!-- The operator does NOT currently hold a km-slack-threads DDB client in -->
<!-- SlackCmdDeps — add one (table name from cfg, same DDB client used by doctor). -->

GSI Query (operator creds, no bridge): IndexName "session-index",
KeyConditionExpression "claude_session_id = :sid". KEYS_ONLY → returns
(channel_id, thread_ts) keys; GetItem the base row if agent_type/sandbox_id needed.
Channel resolution for --sandbox/--alias: reuse the km-slack-channels resolution
already used by km slack adopt / create (channel root = the sandbox's bound channel).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunSlackReply — session→GSI→post + verbatim + channel-root</name>
  <files>internal/app/cmd/slack_reply.go, internal/app/cmd/slack_reply_test.go</files>
  <behavior>
    - TestRunSlackReply: --session resolves via the GSI Query (fake DDB returning a
      row) → posts to (channel_id, thread_ts) via PostMessage (fake Slack records call).
    - --thread+--channel posts verbatim (no GSI Query).
    - --session with a GSI miss + --sandbox/--alias set → posts to the resolved
      channel root (top-level).
    - Body is read from --body file and rendered per --render.
  </behavior>
  <action>
    Create internal/app/cmd/slack_reply.go with an exported testable
    RunSlackReply(deps, opts) and newSlackReplyCmd(deps) Cobra constructor,
    mirroring slack_adopt.go structure.
    Resolution:
      1. If --thread && --channel → post verbatim.
      2. Else if --session → Query session-index GSI directly with the operator
         DDB client; on hit post to (channel_id, thread_ts); on miss fall to (3).
      3. Else/fallback: if --sandbox/--alias → resolve channel → top-level post.
      4. If none resolve → return a clear error (e.g. "no thread or channel resolved;
         pass --thread+--channel, --session, or --sandbox/--alias").
    Post via deps.Slack.PostMessage (bot token client). Honor --body (file) and
    --render (reuse the existing render helper used by adopt/test if present, else
    the plain path). Write tests FIRST (RED) with fake DDB + fake Slack, then implement.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run TestRunSlackReply -count=1 -timeout 120s</automated>
  </verify>
  <done>RunSlackReply handles --session (GSI), verbatim, and channel-root; posts via PostMessage; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DDB client into SlackCmdDeps + register subcommand</name>
  <files>internal/app/cmd/slack.go</files>
  <action>
    1. Add a km-slack-threads DDB client (or a thin GSI-query function) to
       SlackCmdDeps, populated in buildSlackCmdDeps from the same DDB client doctor
       uses (table name from cfg). Keep additive — do not break existing deps.
    2. Register newSlackReplyCmd(deps) in newSlackCmdInternal via AddCommand.
    Confirm the operator binary still builds and the existing slack command tests
    pass (internal/app/cmd is GREEN baseline per Phase 107 — a FAIL is a real regression).
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/ && go test ./internal/app/cmd/ -run 'TestRunSlackReply|TestNewSlackCmd|TestSlack' -count=1 -timeout 180s</automated>
  </verify>
  <done>reply registered under km slack; SlackCmdDeps carries the threads DDB client; cmd package builds + targeted tests green.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestRunSlackReply' -count=1 -timeout 120s` green.
Operator path queries the GSI directly (operator creds) — NOT the bridge.
MANUAL (live): `km slack reply --session <id> --body /tmp/m.md` lands in the
session's bound thread; `--thread+--channel` posts verbatim.
</verification>

<success_criteria>
- km slack reply supports --session (GSI), --thread+--channel (verbatim), --sandbox/--alias (root).
- Posts via the bot-token chat.postMessage client.
- DDB client wired into SlackCmdDeps; targeted cmd tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-04-SUMMARY.md`
</output>
