---
phase: 110-session-aware-slack-reply-thread-channel-repair
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - internal/app/cmd/doctor_slack_threads.go
  - internal/app/cmd/doctor_slack_threads_test.go
  - internal/app/cmd/doctor.go
  - skills/slack/SKILL.md
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "km doctor WARNs when thread rows point at non-existent Slack channels"
    - "km doctor WARNs when alias rows in km-slack-channels point at a gone channel"
    - "klanker:slack skill documents km-slack reply resolution order, --session/--thread overrides, channel-root fallback, and operator repair commands"
    - "plugin.json and marketplace.json are bumped so clients pick up the new skill section"
  artifacts:
    - path: "internal/app/cmd/doctor_slack_threads.go"
      provides: "checkSlackThreadDeadChannels + checkSlackChannelDeadAlias"
      contains: "checkSlackThreadDeadChannels"
    - path: "skills/slack/SKILL.md"
      provides: "Session-aware Reply section"
      contains: "km-slack reply"
    - path: ".claude-plugin/plugin.json"
      provides: "bumped plugin version"
      contains: "0.4.8"
  key_links:
    - from: "internal/app/cmd/doctor.go"
      to: "checkSlackThreadDeadChannels / checkSlackChannelDeadAlias"
      via: "checks = append(checks, ...) closures"
      pattern: "checkSlackThreadDeadChannels|checkSlackChannelDeadAlias"
---

<objective>
Close out Phase 110 with operator observability and documentation:
1. Two `km doctor` WARN checks — thread rows → dead channels; alias rows → dead
   channels — mirroring `checkSlackBotUserIDCached`.
2. Update the `klanker:slack` skill doc with the session-aware reply section.
3. Bump the plugin version (`plugin.json` + `marketplace.json`) so clients pick up
   the new skill content (plugin-version-gates-cache memory note).

Purpose: Operators can detect dead mappings; agents/operators learn the new reply UX.
Output: 2 doctor checks + skill section + version bump.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-RESEARCH.md
@.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-VALIDATION.md

@internal/app/cmd/doctor_slack_bot_user_id.go
@internal/app/cmd/doctor.go
@skills/slack/SKILL.md

<interfaces>
<!-- Doctor check pattern: checkSlackBotUserIDCached(ctx, ...) CheckResult; each -->
<!-- check in its own file; registered in doctor.go checks slice (~4081-4103) via -->
<!-- a closure that downgrades CheckError → CheckWarn. -->
<!-- Channel existence: reuse pkg/slack GetChannelInfo / conversations.info (don't -->
<!-- hand-roll). Gate on bot-token availability: SKIP when absent (risk 6). -->
<!-- km-slack-threads scan → unique channel_ids → probe each. -->
<!-- km-slack-channels scan → each channel_id → probe. -->
<!-- plugin.json + marketplace.json currently "version": "0.4.7" → bump to 0.4.8. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkSlackThreadDeadChannels + checkSlackChannelDeadAlias + register</name>
  <files>internal/app/cmd/doctor_slack_threads.go, internal/app/cmd/doctor_slack_threads_test.go, internal/app/cmd/doctor.go</files>
  <behavior>
    - TestCheckSlackThreadDeadChannels: a thread row whose channel returns
      channel_not_found → CheckResult WARN naming km slack prune-threads. All-live
      channels → OK/PASS. No bot token → SKIP.
    - TestCheckSlackChannelDeadAlias: an alias row whose channel is gone → WARN
      naming km slack forget-channel + km slack adopt. No bot token → SKIP.
  </behavior>
  <action>
    1. Create internal/app/cmd/doctor_slack_threads.go with:
       - checkSlackThreadDeadChannels(ctx, deps...) CheckResult: scan km-slack-threads
         (paginated), collect unique channel_ids, probe each via the Slack client;
         WARN on any channel_not_found, remediation = "km slack prune-threads".
         SKIP when bot token absent.
       - checkSlackChannelDeadAlias(ctx, deps...) CheckResult: scan km-slack-channels,
         probe each channel_id; WARN on channel_not_found, remediation =
         "km slack forget-channel <alias>" + "km slack adopt". SKIP when token absent.
    2. Register both in doctor.go's checks slice via the closure pattern
       (downgrade CheckError → CheckWarn) used by checkSlackBotUserIDCached
       (~doctor.go:4081-4103). Add any needed scan helpers/table names to DoctorDeps.
    Write tests FIRST (RED) with fake DDB scan + fake Slack (channel_not_found),
    then implement.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCheckSlackThreadDeadChannels|TestCheckSlackChannelDeadAlias' -count=1 -timeout 120s</automated>
  </verify>
  <done>Both checks WARN on dead channels, SKIP without a token, and are registered in the doctor checks slice; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: klanker:slack skill section + plugin version bump</name>
  <files>skills/slack/SKILL.md, .claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
    1. Add a `## Session-aware Reply (km-slack reply)` section to skills/slack/SKILL.md:
       - Sandbox-side resolution order: --thread > $KM_SLACK_THREAD_TS > session id
         (--session or auto-detected newest ~/.claude/projects/**/<id>.jsonl, Codex
         path best-effort/branched on $KM_AGENT) → bridge lookup-thread → channel root.
       - Note the Codex auto-detect path is LOW-confidence: explicit --session works;
         auto-detect WARNs + falls back to channel root if unresolved.
       - --body /file and --render honored.
       - Cross-link the operator side: km slack reply (--session/--thread+--channel/
         --sandbox/--alias) and the repair commands km slack threads / forget-thread /
         prune-threads / forget-channel.
       - Use generic placeholders only (no real tenants/IDs).
    2. Bump "version" in BOTH .claude-plugin/plugin.json and
       .claude-plugin/marketplace.json from 0.4.7 to 0.4.8 (lockstep — clients
       cache the old version otherwise; plugin-version-gates-cache).
  </action>
  <verify>
    <automated>grep -q 'km-slack reply' skills/slack/SKILL.md && grep -q '"version": "0.4.8"' .claude-plugin/plugin.json && grep -q '"version": "0.4.8"' .claude-plugin/marketplace.json && echo DOCS_OK</automated>
  </verify>
  <done>Skill doc has the session-aware reply section; both plugin manifests bumped to 0.4.8 in lockstep.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestCheckSlack(ThreadDeadChannels|ChannelDeadAlias)' -count=1 -timeout 120s` green.
Full cmd suite stays green (Phase 107 baseline): `go test ./internal/app/cmd/ -count=1 -timeout 600s` (capture exit code, read the ok/FAIL summary — do not trust a piped tail; memory feedback note).
MANUAL (live): after deleting a channel, `km doctor` shows the two new WARN lines.
Plugin/skill bump visible to clients after cache refresh.
</verification>

<success_criteria>
- Two doctor WARN checks implemented, SKIP-safe without a token, registered.
- klanker:slack skill documents the reply UX + operator repair commands.
- plugin.json + marketplace.json bumped to 0.4.8 in lockstep.
- Targeted cmd tests green; no full-suite regression.
</success_criteria>

<output>
After completion, create `.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-06-SUMMARY.md`
</output>
