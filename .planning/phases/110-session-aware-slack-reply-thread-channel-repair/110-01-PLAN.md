---
phase: 110-session-aware-slack-reply-thread-channel-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/modules/dynamodb-slack-threads/v1.1.0/main.tf
  - infra/modules/dynamodb-slack-threads/v1.1.0/outputs.tf
  - infra/modules/dynamodb-slack-threads/v1.1.0/variables.tf
  - infra/live/use1/dynamodb-slack-threads/terragrunt.hcl
autonomous: true
requirements: []
must_haves:
  truths:
    - "km-slack-threads table has a session-index GSI keyed on claude_session_id"
    - "The live terragrunt unit sources the v1.1.0 module (not v1.0.0)"
    - "km init --plan is a no-op after the bump (no drift, no destroy-class trip)"
  artifacts:
    - path: "infra/modules/dynamodb-slack-threads/v1.1.0/main.tf"
      provides: "Table schema + session-index GSI (KEYS_ONLY on claude_session_id)"
      contains: "session-index"
    - path: "infra/live/use1/dynamodb-slack-threads/terragrunt.hcl"
      provides: "Live source pointer to v1.1.0"
      contains: "dynamodb-slack-threads/v1.1.0"
  key_links:
    - from: "infra/live/use1/dynamodb-slack-threads/terragrunt.hcl"
      to: "infra/modules/dynamodb-slack-threads/v1.1.0"
      via: "source = ..."
      pattern: "dynamodb-slack-threads/v1.1.0"
---

<objective>
Add the `session-index` GSI (hash `claude_session_id`, `KEYS_ONLY`) to the
`km-slack-threads` DynamoDB table so threads can be looked up by session id.
This is an in-place module version bump (v1.0.0 → v1.1.0) following the Phase 104
pattern. This GSI is the foundation every other Phase 110 plan depends on — the
bridge `lookup-thread` action (Plan 02) and operator `km slack reply --session`
(Plan 04) both Query it.

Purpose: Enable O(1) session-id → (channel_id, thread_ts) resolution.
Output: New v1.1.0 module directory with the GSI; live unit repointed; no drift.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-RESEARCH.md

# Existing module to copy from
@infra/modules/dynamodb-slack-threads/v1.0.0/main.tf
@infra/live/use1/dynamodb-slack-threads/terragrunt.hcl

<interfaces>
<!-- Current km-slack-threads schema (from v1.0.0/main.tf) the GSI extends: -->
<!-- hash_key = channel_id (S); range_key = thread_ts (S); ttl on ttl_expiry (N). -->
<!-- Poller writes claude_session_id per row (pkg/compiler/userdata.go:2018-2030). -->
<!-- Bridge Upsert OMITS claude_session_id (aws_adapters.go:923-934) — invariant -->
<!-- that prevents empty-string GSI key collisions. Do NOT change the Upsert. -->

GSI block to add at table level:
```hcl
attribute {
  name = "claude_session_id"
  type = "S"
}

global_secondary_index {
  name            = "session-index"
  hash_key        = "claude_session_id"
  projection_type = "KEYS_ONLY"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1.1.0 module with session-index GSI</name>
  <files>infra/modules/dynamodb-slack-threads/v1.1.0/main.tf, infra/modules/dynamodb-slack-threads/v1.1.0/outputs.tf, infra/modules/dynamodb-slack-threads/v1.1.0/variables.tf</files>
  <action>
    Create the v1.1.0 module directory by copying all three files from
    infra/modules/dynamodb-slack-threads/v1.0.0/ VERBATIM, then edit ONLY
    v1.1.0/main.tf to add the GSI.

    In main.tf add, at the aws_dynamodb_table resource level:
    1. A new `attribute { name = "claude_session_id"; type = "S" }` block
       (DynamoDB requires every GSI key attribute to be declared at table level).
    2. A `global_secondary_index { name = "session-index"; hash_key =
       "claude_session_id"; projection_type = "KEYS_ONLY" }` block.

    Do NOT change hash_key/range_key/TTL/SSE or any variable. outputs.tf and
    variables.tf are byte-identical copies of v1.0.0.

    Do NOT create or leave any .terraform.lock.hcl or .terraform/ directory in
    v1.1.0/ (module-source lock drift footgun — memory project_module_source_lock_drift).
    Start clean; terragrunt manages locking from the live cache.
  </action>
  <verify>
    <automated>cd infra/modules/dynamodb-slack-threads/v1.1.0 && terraform init -backend=false >/dev/null 2>&1 && terraform validate && grep -q 'session-index' main.tf && grep -q 'KEYS_ONLY' main.tf && grep -q 'claude_session_id' main.tf && test ! -e .terraform.lock.hcl && echo VALID</automated>
  </verify>
  <done>v1.1.0/ has 3 files; main.tf validates and declares the session-index GSI (KEYS_ONLY on claude_session_id); outputs.tf and variables.tf are unchanged copies; no lock file present.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint live unit to v1.1.0</name>
  <files>infra/live/use1/dynamodb-slack-threads/terragrunt.hcl</files>
  <action>
    Change the `source = ...` line (line 32) from
    `.../dynamodb-slack-threads/v1.0.0` to `.../dynamodb-slack-threads/v1.1.0`.
    This is the ONLY change in the live unit — no var changes, no dependency
    changes. The dynamodb-slack-threads entry in regionalModules()
    (internal/app/cmd/init.go:564) is unchanged: this is an in-place source
    path bump, NOT a new module entry, so TestRunInitPlan_ModuleOrder's
    hardcoded count is unaffected (memory project_module_order_test_count_debt).
  </action>
  <verify>
    <automated>grep -q 'dynamodb-slack-threads/v1.1.0' infra/live/use1/dynamodb-slack-threads/terragrunt.hcl && ! grep -q 'dynamodb-slack-threads/v1.0.0' infra/live/use1/dynamodb-slack-threads/terragrunt.hcl && echo REPOINTED</automated>
  </verify>
  <done>Live unit sources v1.1.0; no v1.0.0 reference remains in the live terragrunt.hcl.</done>
</task>

</tasks>

<verification>
Module-level Terraform: `terraform validate` in v1.1.0/ passes and the GSI is declared.
Module-order regression: `go test ./internal/app/cmd/ -run TestRunInitPlan_ModuleOrder -count=1 -timeout 120s` stays green (no count change expected).

MANUAL (live AWS — operator runs at deploy, NOT part of automated gate):
- `make build && make build-lambdas && km init --dry-run=false` provisions the GSI.
- Confirm `session-index` reaches ACTIVE in the DynamoDB console (online GSI add; seconds–minutes for a low-volume table; existing rows with claude_session_id auto-backfill).
- A subsequent `km init --plan` MUST be a no-op (no-drift invariant; no destroy-class trip).
- Deploy is FULL `km init --dry-run=false`, NOT `--sidecars` (a GSI is a Terraform resource change; --sidecars only rebuilds binaries — pitfall 3).
</verification>

<success_criteria>
- v1.1.0 module exists with the session-index GSI (KEYS_ONLY) and validates.
- Live unit repointed to v1.1.0 with no other change.
- No new regionalModules() entry; module-order test green.
- No stale lock file in v1.1.0/.
</success_criteria>

<output>
After completion, create `.planning/phases/110-session-aware-slack-reply-thread-channel-repair/110-01-SUMMARY.md`
</output>
