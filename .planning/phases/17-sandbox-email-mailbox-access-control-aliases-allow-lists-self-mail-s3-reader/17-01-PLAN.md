---
phase: 17-sandbox-email-mailbox-access-control-aliases-allow-lists-self-mail-s3-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/profile/types.go
  - pkg/profile/schemas/sandbox_profile.schema.json
  - infra/modules/dynamodb-identities/v1.1.0/main.tf
  - infra/modules/dynamodb-identities/v1.1.0/variables.tf
  - infra/modules/dynamodb-identities/v1.1.0/outputs.tf
  - infra/live/use1/dynamodb-identities/terragrunt.hcl
  - profiles/open-dev.yaml
  - profiles/restricted-dev.yaml
  - profiles/hardened.yaml
  - profiles/sealed.yaml
autonomous: true
requirements:
  - MAIL-ALIAS-SCHEMA
  - MAIL-ALLOWLIST-SCHEMA
  - MAIL-GSI-INFRA
  - MAIL-BUILTIN-DEFAULTS

must_haves:
  truths:
    - "EmailSpec struct has Alias and AllowedSenders fields"
    - "JSON schema validates alias as lowercase dot-notation pattern"
    - "JSON schema accepts allowedSenders as array of strings"
    - "DynamoDB identities module v1.1.0 has alias GSI"
    - "Built-in profiles hardened/sealed have allowedSenders: [self], open-dev/restricted-dev have allowedSenders: [*]"
  artifacts:
    - path: "pkg/profile/types.go"
      provides: "EmailSpec with Alias and AllowedSenders fields"
      contains: "AllowedSenders"
    - path: "pkg/profile/schemas/sandbox_profile.schema.json"
      provides: "alias pattern validation and allowedSenders array"
      contains: "allowedSenders"
    - path: "infra/modules/dynamodb-identities/v1.1.0/main.tf"
      provides: "DynamoDB table with alias GSI"
      contains: "alias-index"
    - path: "infra/live/use1/dynamodb-identities/terragrunt.hcl"
      provides: "Terragrunt config pointing to v1.1.0"
      contains: "v1.1.0"
  key_links:
    - from: "pkg/profile/schemas/sandbox_profile.schema.json"
      to: "pkg/profile/types.go"
      via: "schema validates what types.go defines"
      pattern: "alias.*pattern"
---

<objective>
Add alias and allow-list fields to the email schema, create DynamoDB identities module v1.1.0 with alias GSI, and update built-in profile defaults.

Purpose: Establishes the data model (Go types, JSON schema, DynamoDB GSI, profile defaults) that Plans 02 and 03 build upon.
Output: Extended EmailSpec type, updated JSON schema, v1.1.0 Terraform module, updated built-in profiles.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sandbox-email-mailbox-access-control-aliases-allow-lists-self-mail-s3-reader/17-RESEARCH.md

<interfaces>
<!-- Existing EmailSpec from pkg/profile/types.go (lines 52-62) -->
```go
type EmailSpec struct {
    Signing       string `yaml:"signing"`
    VerifyInbound string `yaml:"verifyInbound"`
    Encryption    string `yaml:"encryption"`
}
```

<!-- Existing DynamoDB identities module v1.0.0/main.tf -->
```terraform
resource "aws_dynamodb_table" "identities" {
  name         = var.table_name
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "sandbox_id"

  attribute {
    name = "sandbox_id"
    type = "S"
  }

  ttl {
    attribute_name = "expiresAt"
    enabled        = true
  }

  dynamic "replica" {
    for_each = var.replica_regions
    content {
      region_name = replica.value
    }
  }

  tags = merge(var.tags, {
    Module  = "dynamodb-identities"
    Version = "v1.0.0"
  })
}
```

<!-- Existing JSON schema email section (sandbox_profile.schema.json lines 59-80) -->
```json
"email": {
  "type": "object",
  "description": "Optional email signing, inbound verification, and encryption policy",
  "additionalProperties": false,
  "properties": {
    "signing": { "type": "string", "enum": ["required", "optional", "off"] },
    "verifyInbound": { "type": "string", "enum": ["required", "optional", "off"] },
    "encryption": { "type": "string", "enum": ["required", "optional", "off"] }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EmailSpec type and JSON schema with alias and allowedSenders</name>
  <files>pkg/profile/types.go, pkg/profile/schemas/sandbox_profile.schema.json, pkg/profile/parse_test.go</files>
  <behavior>
    - Test: YAML with `email.alias: "research.team-a"` and `email.allowedSenders: ["self", "build.*"]` parses into EmailSpec with Alias="research.team-a" and AllowedSenders=["self", "build.*"]
    - Test: YAML with email section but no alias/allowedSenders parses with empty string Alias and nil AllowedSenders
    - Test: `km validate` rejects alias with uppercase letters (e.g. "Research.team-a") via JSON schema pattern
    - Test: `km validate` rejects alias without a dot (e.g. "research") via JSON schema pattern
  </behavior>
  <action>
1. Add two fields to `EmailSpec` in `pkg/profile/types.go`:
   ```go
   Alias          string   `yaml:"alias,omitempty"`
   AllowedSenders []string `yaml:"allowedSenders,omitempty"`
   ```

2. Update `pkg/profile/schemas/sandbox_profile.schema.json` — add to the `email.properties` object (after `encryption`):
   ```json
   "alias": {
     "type": "string",
     "description": "Human-friendly dot-notation name (e.g. 'research.team-a') registered in km-identities",
     "pattern": "^[a-z][a-z0-9-]*\\.[a-z][a-z0-9-]*$"
   },
   "allowedSenders": {
     "type": "array",
     "description": "Patterns controlling which sandboxes may send to this sandbox: 'self', sandbox IDs, alias wildcards (build.*), or '*'",
     "items": { "type": "string" }
   }
   ```

3. After schema changes, run `go generate ./pkg/profile/...` if there is a generate step, or manually copy the schema to `schemas/` root if go:embed requires it (see Phase 01 decision about dual schema location).

4. Write tests in `pkg/profile/parse_test.go` (extend existing test file) for the new fields:
   - TestParse_EmailAlias: YAML with alias + allowedSenders parses correctly
   - TestParse_EmailAlias_Empty: YAML without alias/allowedSenders is backward-compatible
   - TestValidateSchema_EmailAlias_Invalid: uppercase alias rejected by schema pattern
   - TestValidateSchema_EmailAlias_NoDot: alias without dot rejected
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run "TestParse_Email|TestValidateSchema_Email" -v</automated>
  </verify>
  <done>EmailSpec has Alias and AllowedSenders fields. JSON schema validates alias pattern. All tests pass. Existing profile tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create DynamoDB identities v1.1.0 module with alias GSI and update profiles</name>
  <files>infra/modules/dynamodb-identities/v1.1.0/main.tf, infra/modules/dynamodb-identities/v1.1.0/variables.tf, infra/modules/dynamodb-identities/v1.1.0/outputs.tf, infra/live/use1/dynamodb-identities/terragrunt.hcl, profiles/open-dev.yaml, profiles/restricted-dev.yaml, profiles/hardened.yaml, profiles/sealed.yaml</files>
  <action>
1. Create `infra/modules/dynamodb-identities/v1.1.0/` by copying v1.0.0 files.

2. In v1.1.0/main.tf, add the alias attribute and GSI:
   - Add `attribute { name = "alias" type = "S" }` alongside the existing `sandbox_id` attribute block.
   - Add a `global_secondary_index` block:
     ```terraform
     global_secondary_index {
       name            = "alias-index"
       hash_key        = "alias"
       projection_type = "ALL"
     }
     ```
   - Update the Version tag from "v1.0.0" to "v1.1.0".

3. v1.1.0/variables.tf and v1.1.0/outputs.tf: copy from v1.0.0 unchanged.

4. Update `infra/live/use1/dynamodb-identities/terragrunt.hcl`: change source reference from `v1.0.0` to `v1.1.0`.

5. Update built-in profiles (`profiles/*.yaml`):
   - `open-dev.yaml`: add under `email:` section: `allowedSenders: ["*"]`
   - `restricted-dev.yaml`: add under `email:` section: `allowedSenders: ["*"]`
   - `hardened.yaml`: add under `email:` section: `allowedSenders: ["self"]`
   - `sealed.yaml`: add under `email:` section: `allowedSenders: ["self"]`
   - Do NOT add alias to built-in profiles — alias is per-sandbox, not per-profile-template.

6. Verify built-in profiles still pass validation:
   ```bash
   go run ./cmd/km validate profiles/open-dev.yaml
   go run ./cmd/km validate profiles/hardened.yaml
   ```
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -v && go run ./cmd/km validate profiles/open-dev.yaml && go run ./cmd/km validate profiles/hardened.yaml && go run ./cmd/km validate profiles/sealed.yaml && go run ./cmd/km validate profiles/restricted-dev.yaml</automated>
  </verify>
  <done>v1.1.0 module exists with alias GSI. Live terragrunt.hcl points to v1.1.0. All four built-in profiles have allowedSenders defaults and pass km validate. v1.0.0 module is unchanged.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/profile/... -v` passes (schema + type changes)
- `go test ./...` passes (no regressions)
- `infra/modules/dynamodb-identities/v1.1.0/main.tf` contains `alias-index` GSI
- All four built-in profiles pass `km validate`
</verification>

<success_criteria>
- EmailSpec type has Alias (string) and AllowedSenders ([]string) fields with yaml tags
- JSON schema enforces alias as lowercase dot-notation via regex pattern
- JSON schema accepts allowedSenders as string array
- DynamoDB identities module v1.1.0 has alias GSI with ALL projection
- v1.0.0 module is untouched
- hardened/sealed profiles have allowedSenders: ["self"]
- open-dev/restricted-dev profiles have allowedSenders: ["*"]
</success_criteria>

<output>
After completion, create `.planning/phases/17-sandbox-email-mailbox-access-control-aliases-allow-lists-self-mail-s3-reader/17-01-SUMMARY.md`
</output>
