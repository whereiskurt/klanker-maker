---
phase: 84-ses-per-install-rule-namespacing-via-operator-address-prefix
plan: 08
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - infra/modules/ses/v1.0.0/variables.tf
  - infra/modules/ses/v1.0.0/main.tf
  - CLAUDE.md
  - OPERATOR-GUIDE.md
  - Makefile
autonomous: true
requirements:
  - SES-82.1-REMOVAL

must_haves:
  truths:
    - "`infra/modules/ses/v1.0.0/variables.tf` no longer declares the `activate_rule_set` variable"
    - "`infra/modules/ses/v1.0.0/main.tf` no longer declares the `aws_ses_active_receipt_rule_set` resource"
    - "CLAUDE.md no longer contains the 'Phase 82.1 follow-up: SES active-rule-set handoff for second installs' section (currently lines 35-65)"
    - "OPERATOR-GUIDE.md no longer contains the Phase 82.1 SES handoff section (currently lines 646-677 per research)"
    - "`grep -rn 'KM_SES_ACTIVATE_RULESET\\|activate_rule_set' infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md` returns ZERO matches"
    - "`make test-no-82.1-leftovers` exits 0 (GREEN — the gate was RED at end of Wave 0; now GREEN)"
  artifacts:
    - path: "infra/modules/ses/v1.0.0/variables.tf"
      provides: "Cleaned: activate_rule_set variable deleted"
    - path: "infra/modules/ses/v1.0.0/main.tf"
      provides: "Cleaned: aws_ses_active_receipt_rule_set resource deleted"
    - path: "CLAUDE.md"
      provides: "Phase 82.1 SES section removed"
    - path: "OPERATOR-GUIDE.md"
      provides: "Phase 82.1 SES handoff runbook removed"
    - path: "Makefile"
      provides: "test-no-82.1-leftovers wired into the standard test target (if a test umbrella target exists)"
  key_links:
    - from: "Makefile test-no-82.1-leftovers"
      to: "grep across the repo"
      via: "grep -rn pattern"
      pattern: "test-no-82.1-leftovers"
---

<objective>
Hard-remove every Phase 82.1 `activate_rule_set` / `KM_SES_ACTIVATE_RULESET` reference from the codebase. The Wave 0 grep gate (`make test-no-82.1-leftovers`) was RED at end of Wave 0; this plan turns it GREEN.

Per CONTEXT.md "Cleanup of Phase 82.1" (Locked): scope of removal is `infra/modules/ses/v1.0.0/variables.tf`, `infra/modules/ses/v1.0.0/main.tf`, CLAUDE.md Phase 82.1 section, and OPERATOR-GUIDE.md Phase 82.1 lines 646-677. The 82.1-03-PLAN.md historical record stays intact (ROADMAP.md is updated in Plan 84-09 to mark superseded).

This plan depends on Plan 84-03 (regional v2.0.0 + live wiring update) being done FIRST — once the live dir points at v2.0.0, the v1.0.0 module's `activate_rule_set` variable is dead code (the live wiring no longer references it), so deleting it is safe.

Output:
- 4 source files edited (2 Terraform, 2 docs)
- 1 Makefile target wired into the standard CI surface
- 1 grep gate turned GREEN
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-CONTEXT.md
@.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-RESEARCH.md

@infra/modules/ses/v1.0.0/variables.tf
@infra/modules/ses/v1.0.0/main.tf
@CLAUDE.md
@OPERATOR-GUIDE.md
@Makefile

<interfaces>
<!-- Removal targets. -->

In `infra/modules/ses/v1.0.0/variables.tf` (~line 33-37 per RESEARCH):
```hcl
variable "activate_rule_set" {
  type        = bool
  default     = true
  description = "Whether to activate the rule set..."
}
```
DELETE this block.

In `infra/modules/ses/v1.0.0/main.tf` (~line 61-71 per RESEARCH):
```hcl
resource "aws_ses_active_receipt_rule_set" "km_sandbox" {
  count         = var.activate_rule_set ? 1 : 0
  rule_set_name = aws_ses_receipt_rule_set.km_sandbox.rule_set_name
}
```
DELETE this resource block. Remove any `depends_on = [aws_ses_active_receipt_rule_set.km_sandbox]` references elsewhere in main.tf.

In `CLAUDE.md`:
- Lines 35-65 (the "### Phase 82.1 follow-up: SES active-rule-set handoff for second installs" section). DELETE the entire section including the `### Phase 82.1 follow-up:` heading and the trailing horizontal-rule / blank-line separator before the next section ("## CLI").
- Verify by `grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md` — should return zero matches.

In `OPERATOR-GUIDE.md`:
- Lines 646-677 per RESEARCH (the SES handoff section). DELETE the section including its heading and runbook content.
- Verify with grep.

In `Makefile`:
- Wave 0 created `test-no-82.1-leftovers`. This plan: wire it into the standard test target. If the Makefile has an umbrella `test:` or `check:` target that runs `go test ./...`, add `test-no-82.1-leftovers` as a dependency (so CI fails if the gate fails). Example:
```makefile
.PHONY: test
test: test-no-82.1-leftovers
	go test ./...
```

Critical caveat: do NOT delete the Phase 82.1 plan files in `.planning/phases/82.1-multi-instance-polish-*/` — they are historical record. The grep gate is scoped to `infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md`, NOT `.planning/`, so the gate stays GREEN even with the historical references in plan files.

Also: this plan removes the `aws_ses_active_receipt_rule_set` ONLY from the v1.0.0 module. The live `infra/live/use1/ses/terragrunt.hcl` already points at v2.0.0 (Plan 84-03), so v1.0.0 is no longer referenced by live state. The actual on-AWS active-rule-set deletion will happen during operator UAT (Plan 84-10) when `km init` plans the cutover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete activate_rule_set variable + aws_ses_active_receipt_rule_set resource from v1.0.0</name>
  <files>infra/modules/ses/v1.0.0/variables.tf, infra/modules/ses/v1.0.0/main.tf</files>
  <action>
1. `grep -n "activate_rule_set" infra/modules/ses/v1.0.0/variables.tf infra/modules/ses/v1.0.0/main.tf` to confirm exact line locations.

2. In `variables.tf`: delete the entire `variable "activate_rule_set" { ... }` block, including any leading/trailing blank lines used as separators.

3. In `main.tf`: delete the entire `resource "aws_ses_active_receipt_rule_set" "..."` block. If any other resource in main.tf has a `depends_on = [aws_ses_active_receipt_rule_set....]` reference, also delete that line. If a `count = var.activate_rule_set ? 1 : 0` appears anywhere else, change to remove the count or hard-code `1` if the resource should always create (review case-by-case).

4. Run `terraform fmt` on the v1.0.0 module.

5. Run `terraform init -backend=false` + `terraform validate` against v1.0.0 to confirm the module still validates standalone. (Note: v1.0.0 is no longer in use by any live wiring; this is a "shape" validation only.)

6. After this task, `grep -rn "activate_rule_set\|KM_SES_ACTIVATE_RULESET" infra/` should be empty.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ! grep -rn "activate_rule_set\|KM_SES_ACTIVATE_RULESET" infra/ && echo "infra/ clean" && cd infra/modules/ses/v1.0.0 && terraform fmt -check -diff && terraform init -backend=false 2>&1 | tail -3 && terraform validate 2>&1 | tail -5</automated>
  </verify>
  <done>Both files cleaned. v1.0.0 module still validates. No `activate_rule_set` / `KM_SES_ACTIVATE_RULESET` matches in `infra/`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Phase 82.1 SES sections from CLAUDE.md and OPERATOR-GUIDE.md</name>
  <files>CLAUDE.md, OPERATOR-GUIDE.md</files>
  <action>
1. **CLAUDE.md** — Read the file. Locate the heading `### Phase 82.1 follow-up: SES active-rule-set handoff for second installs` (currently at line ~35) and the next sibling/parent heading that follows it. Delete every line from the Phase 82.1 heading through (but NOT including) the start of the next section. The first non-deleted line should be whatever section follows it in the document structure (likely `## CLI` or the next `### Phase` heading).
   - Verify after edit: `grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md` → zero matches.
   - The Phase 82 section (without `.1`) STAYS — only the 82.1 follow-up is deleted.

2. **OPERATOR-GUIDE.md** — Read lines 640-680 to bracket the SES handoff section. Locate the heading (likely `### SES active-rule-set handoff for second installs` or similar). Delete the entire section including the heading and all subordinate content up to (but not including) the next section heading at the same or higher level.
   - Verify after edit: `grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET\|SES active-rule-set\|set-active-receipt-rule-set" OPERATOR-GUIDE.md` should return zero non-historical matches. If `set-active-receipt-rule-set` appears elsewhere in OPERATOR-GUIDE.md (unrelated to Phase 82.1), leave it.

3. Do NOT add a Phase 84 section here — Plan 84-09 owns the docs additions. This plan is removal-only.

4. Visual scan: confirm the surrounding markdown structure is intact (no orphaned numbered lists, no broken heading hierarchy, no double-blank-lines breaking the flow). Light cleanup if needed.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ! grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md && echo "CLAUDE.md clean" && ! grep -n "KM_SES_ACTIVATE_RULESET\|activate_rule_set" OPERATOR-GUIDE.md && echo "OPERATOR-GUIDE.md clean"</automated>
  </verify>
  <done>Both files have the Phase 82.1 SES sections removed. Grep returns zero matches. The Phase 82 (non-.1) section in CLAUDE.md is untouched. Markdown structure is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Wire test-no-82.1-leftovers into the Makefile umbrella test target + verify GREEN</name>
  <files>Makefile</files>
  <action>
1. Open Makefile. Locate the umbrella test target (likely `.PHONY: test` followed by `test:` recipe; or `check:`).

2. Add `test-no-82.1-leftovers` as a prereq:
   ```makefile
   .PHONY: test
   test: test-no-82.1-leftovers
   	go test ./...
   ```
   If the umbrella has multiple prereqs already, append: `test: <existing> test-no-82.1-leftovers`.

3. If no umbrella test target exists (the Makefile uses ad-hoc commands), just confirm the `test-no-82.1-leftovers` target itself still works and document in the SUMMARY that CI integration is operator-driven.

4. Run `make test-no-82.1-leftovers` — it MUST exit 0 now (was RED at end of Wave 0).

5. If the umbrella `test` target exists and was wired, run `make test 2>&1 | tail -20` and confirm the grep-gate prereq fires BEFORE the `go test ./...` recipe.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && make test-no-82.1-leftovers 2>&1 | tail -5 && echo "exit=$?"</automated>
  </verify>
  <done>`make test-no-82.1-leftovers` exits 0 (GREEN). Wired into umbrella test target if one exists.</done>
</task>

</tasks>

<verification>
- `grep -rn "KM_SES_ACTIVATE_RULESET\|activate_rule_set" infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md` → ZERO matches.
- `make test-no-82.1-leftovers` exits 0.
- `infra/modules/ses/v1.0.0/` still validates as a standalone module.
- CLAUDE.md and OPERATOR-GUIDE.md remain well-formed markdown.
- The Phase 82.1 plan files in `.planning/phases/82.1-*/` are UNTOUCHED.
</verification>

<success_criteria>
- Wave 0's grep gate (W0-11) is GREEN.
- All Phase 82.1 SES leftovers are gone from the code + docs surfaces enumerated in CONTEXT.md "Cleanup of Phase 82.1".
- `.planning/` history is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-08-SUMMARY.md`
</output>
