---
phase: 84-ses-per-install-rule-namespacing-via-operator-address-prefix
plan: 08
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - OPERATOR-GUIDE.md
  - Makefile
autonomous: true
requirements:
  - SES-82.1-REMOVAL

must_haves:
  truths:
    - "`infra/modules/ses/v1.0.0/` is UNTOUCHED (CONTEXT.md lock — v1.0.0 stays in tree as historical reference)"
    - "OPERATOR-GUIDE.md no longer contains the Phase 82.1 SES handoff section (currently lines 646-677)"
    - "CLAUDE.md is confirmed clean of any Phase 82.1 SES-handoff reference (a prior refactor already removed it — verification only, no edit)"
    - "`make test-no-82.1-leftovers` exits 0 (GREEN — the gate was RED at end of Wave 0; this plan turns it GREEN by deleting OPERATOR-GUIDE.md content)"
    - "The grep gate's command in the Makefile target excludes `infra/modules/ses/v1.0.0/` AND `.terragrunt-cache/` so v1.0.0's intentional `activate_rule_set` references and cached copies do NOT trip the gate"
    - "After Plan 84-03 ships, `infra/live/use1/ses/terragrunt.hcl` no longer contains `KM_SES_ACTIVATE_RULESET` (handled by 84-03; verified here)"
  artifacts:
    - path: "OPERATOR-GUIDE.md"
      provides: "Phase 82.1 SES handoff section removed (lines 646-677)"
    - path: "Makefile"
      provides: "Updated test-no-82.1-leftovers target with --exclude-dir filters; wired into umbrella test target (if one exists)"
  key_links:
    - from: "Makefile test-no-82.1-leftovers"
      to: "grep scoped over non-historical, non-cache paths"
      via: "grep -rn --exclude-dir='v1.0.0' --exclude-dir='.terragrunt-cache' pattern"
      pattern: "test-no-82.1-leftovers"
---

<objective>
Two surgical removals + grep-gate maintenance:

1. Delete the Phase 82.1 SES handoff section from OPERATOR-GUIDE.md (lines 646-677). Per plan-checker iteration 1 verification, CLAUDE.md is ALREADY CLEAN — no edit needed there, just verification.

2. Update the Wave 0 `make test-no-82.1-leftovers` grep target to exclude `infra/modules/ses/v1.0.0/` (CONTEXT.md locks v1.0.0 as historical — its `activate_rule_set` variable + resource MUST stay) AND `.terragrunt-cache/` (cached copies of v1.0.0).

3. Verify v2.0.0 has no `activate_rule_set` by construction from Plan 84-03 (verification only).

**REVISION FROM ITERATION 0:**
- Removed `infra/modules/ses/v1.0.0/variables.tf` and `infra/modules/ses/v1.0.0/main.tf` from `files_modified` (Blocker 1).
- Removed CLAUDE.md from `files_modified` (Major 4 — already clean).
- Refocused Task 2 to a clean OPERATOR-GUIDE.md deletion + CLAUDE.md verification.
- Updated grep-gate exclusions in Task 3.

Per CONTEXT.md "Cleanup of Phase 82.1" (Locked): scope of removal is the Phase 82.1 doc surfaces. CONTEXT.md "Additional Decisions" § Sandbox catchall: "v1.0.0 stays in tree for historical reference (not deleted)." The 82.1-03-PLAN.md historical record stays intact (ROADMAP.md is updated in Plan 84-09 to mark superseded).

This plan depends on Plan 84-03 (regional v2.0.0 + live wiring update) being done FIRST — once `infra/live/use1/ses/terragrunt.hcl` no longer reads `KM_SES_ACTIVATE_RULESET`, the grep gate can pass for `infra/live/`.

Output:
- 1 doc file edited (OPERATOR-GUIDE.md)
- 1 Makefile target updated with exclude-dir filters
- 1 grep gate turned GREEN
- 0 modifications to `infra/modules/ses/v1.0.0/`
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-CONTEXT.md
@.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-RESEARCH.md

@CLAUDE.md
@OPERATOR-GUIDE.md
@Makefile

<interfaces>
<!-- Removal + verification targets -->

OPERATOR-GUIDE.md — DELETE the section currently at lines 646-677, which begins with the heading:
```
### SES activation handoff (Phase 82.1)
```
and ends with the line containing:
```
See `infra/modules/ses/v1.0.0/variables.tf` for the full `activate_rule_set` variable description.
```
Delete the entire section including its heading and all content up to (but not including) the next sibling heading at the same level (or the end of the file if it's the last section).

CLAUDE.md — VERIFY clean (no edits expected per Major 4):
- `grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md` MUST return zero matches.
- If it returns matches, that means a regression happened between planning and execution — STOP and surface the regression instead of silently editing.

Makefile `test-no-82.1-leftovers` target — UPDATE to exclude historical/cached paths:

Before (from Wave 0, Plan 84-01 Task 3):
```makefile
.PHONY: test-no-82.1-leftovers
test-no-82.1-leftovers:
	@! grep -rn "KM_SES_ACTIVATE_RULESET\|activate_rule_set" infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md \
		|| (echo "Phase 82.1 leftovers found — see Phase 84"; exit 1)
```

After (per Blocker 1 fix):
```makefile
.PHONY: test-no-82.1-leftovers
test-no-82.1-leftovers:
	@! grep -rn --exclude-dir='v1.0.0' --exclude-dir='.terragrunt-cache' \
		"KM_SES_ACTIVATE_RULESET\|activate_rule_set" \
		infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md \
		|| (echo "Phase 82.1 leftovers found — see Phase 84"; exit 1)
```

Note: `--exclude-dir='v1.0.0'` matches ANY directory named `v1.0.0` anywhere in the search tree — that catches both `infra/modules/ses/v1.0.0/` (the canonical historical module) AND any cached copies inside `.terragrunt-cache/`. `--exclude-dir='.terragrunt-cache'` is belt-and-suspenders.

If a newer foundation module also lands at `infra/modules/<other>/v1.0.0/`, the exclude pattern will skip those too — that's acceptable because Phase 82.1's variable name `activate_rule_set` is unique to SES and won't appear in unrelated v1.0.0 modules.

After this plan, the gate is GREEN. The historical `infra/modules/ses/v1.0.0/` files retain their `activate_rule_set` content unchanged (CONTEXT.md lock).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify v2.0.0 has no activate_rule_set (verification only — no edits)</name>
  <files>infra/modules/ses/v2.0.0/variables.tf, infra/modules/ses/v2.0.0/main.tf</files>
  <action>
This task is verification only. Plan 84-03 created `infra/modules/ses/v2.0.0/` by construction WITHOUT `activate_rule_set` (no variable, no `aws_ses_active_receipt_rule_set` resource). Confirm this is still true:

```bash
! grep -n "activate_rule_set\|aws_ses_active_receipt_rule_set" infra/modules/ses/v2.0.0/*.tf
```

Expected: zero matches.

If the verification FAILS (matches found), STOP — Plan 84-03 didn't deliver per its contract. Surface to operator instead of silently fixing here.

If the verification PASSES, this task is a no-op — record the verification command output in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ! grep -n "activate_rule_set\|aws_ses_active_receipt_rule_set" infra/modules/ses/v2.0.0/*.tf && echo "v2.0.0 confirmed free of Phase 82.1 leftovers"</automated>
  </verify>
  <done>v2.0.0 verified clean. No edits made.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLAUDE.md clean + delete Phase 82.1 SES section from OPERATOR-GUIDE.md</name>
  <files>OPERATOR-GUIDE.md</files>
  <action>
1. **CLAUDE.md verification (per Major 4 — already clean from prior refactor):**

```bash
grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md
```

Expected: zero matches. If matches found, STOP and surface the regression — do NOT silently edit CLAUDE.md in this task. The Plan 84-09 docs plan owns CLAUDE.md additions; 84-08's responsibility is verifying the slate is clean.

2. **OPERATOR-GUIDE.md deletion:**

Locate the section starting at line 646 with the heading `### SES activation handoff (Phase 82.1)` and ending at the line containing `See \`infra/modules/ses/v1.0.0/variables.tf\` for the full \`activate_rule_set\` variable description.`

Delete the entire section including:
- The `### SES activation handoff (Phase 82.1)` heading
- All paragraph text + code blocks + commands in between
- The trailing `See ... variable description.` sentence

The first non-deleted line after the deletion should be whatever section follows it (a sibling heading or end-of-file). Verify the surrounding section boundaries are intact.

After deletion:
```bash
! grep -n "Phase 82.1\|KM_SES_ACTIVATE_RULESET\|activate_rule_set\|SES active-rule-set\|SES activation handoff" OPERATOR-GUIDE.md
```
Expected: zero matches.

3. Do NOT add a Phase 84 section here — Plan 84-09 owns the docs additions. This task is removal-only.

4. Visual scan: confirm the surrounding markdown structure is intact (no orphaned numbered lists, no broken heading hierarchy, no double-blank-lines).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ! grep -n "Phase 82.1\|activate_rule_set\|KM_SES_ACTIVATE_RULESET" CLAUDE.md && echo "CLAUDE.md clean (verification)" && ! grep -n "KM_SES_ACTIVATE_RULESET\|activate_rule_set\|SES activation handoff" OPERATOR-GUIDE.md && echo "OPERATOR-GUIDE.md clean (deletion confirmed)"</automated>
  </verify>
  <done>CLAUDE.md confirmed clean (verification only — no edits). OPERATOR-GUIDE.md Phase 82.1 SES section deleted. Markdown structure intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update Makefile grep gate with exclude-dir filters + wire into umbrella test target</name>
  <files>Makefile</files>
  <action>
1. Open Makefile. Locate the `test-no-82.1-leftovers` target created in Plan 84-01 Task 3.

2. Replace the grep command with the version that excludes `v1.0.0` and `.terragrunt-cache`:

```makefile
.PHONY: test-no-82.1-leftovers
test-no-82.1-leftovers:
	@! grep -rn --exclude-dir='v1.0.0' --exclude-dir='.terragrunt-cache' \
		"KM_SES_ACTIVATE_RULESET\|activate_rule_set" \
		infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md \
		|| (echo "Phase 82.1 leftovers found — see Phase 84"; exit 1)
```

3. Wire into umbrella test target. Locate the umbrella test target (likely `.PHONY: test` followed by `test:` recipe). Add `test-no-82.1-leftovers` as a prereq:
```makefile
.PHONY: test
test: test-no-82.1-leftovers
	go test ./...
```
If the umbrella has multiple prereqs already, append: `test: <existing> test-no-82.1-leftovers`.

4. If no umbrella test target exists, just confirm `test-no-82.1-leftovers` runs cleanly and document in the SUMMARY that CI integration is operator-driven.

5. Run `make test-no-82.1-leftovers` — it MUST exit 0 now (was RED at end of Wave 0; OPERATOR-GUIDE.md deletion in Task 2 + Plan 84-03's terragrunt.hcl update should have made it possible to pass).

6. If `make test-no-82.1-leftovers` STILL fails after Task 2 + Plan 84-03 have landed, investigate the remaining matches. Common culprits:
   - A cache directory in a non-standard location.
   - A doc-comment in `pkg/` or `cmd/` mentioning `activate_rule_set` for historical context — these should be allowed (rephrase to avoid the literal trigger) OR added to the exclude list.
   - A test fixture under `internal/app/cmd/testdata/` containing the strings — exclude `testdata` if present.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && make test-no-82.1-leftovers 2>&1 | tail -5 && echo "exit=$?"</automated>
  </verify>
  <done>`make test-no-82.1-leftovers` exits 0 (GREEN). Exclude-dir filters in place. Wired into umbrella test target if one exists.</done>
</task>

</tasks>

<verification>
- `infra/modules/ses/v1.0.0/` is UNTOUCHED — `git status -- infra/modules/ses/v1.0.0/` shows no changes.
- `grep -rn "KM_SES_ACTIVATE_RULESET\|activate_rule_set" infra/ internal/ pkg/ cmd/ OPERATOR-GUIDE.md CLAUDE.md` (without exclude-dir) returns only matches inside `infra/modules/ses/v1.0.0/` and any `.terragrunt-cache/` directories.
- `make test-no-82.1-leftovers` exits 0.
- The Phase 82.1 plan files in `.planning/phases/82.1-*/` are UNTOUCHED.
- CLAUDE.md is unchanged by this plan (Major 4 — already clean).
</verification>

<success_criteria>
- Wave 0's grep gate (W0-11) is GREEN.
- All Phase 82.1 SES doc leftovers are gone from the operator surface (OPERATOR-GUIDE.md).
- `infra/modules/ses/v1.0.0/` historical content preserved per CONTEXT.md lock.
- `.planning/` history is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-08-SUMMARY.md`
</output>
