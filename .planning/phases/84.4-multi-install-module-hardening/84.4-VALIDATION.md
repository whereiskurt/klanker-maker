---
phase: 84.4
slug: multi-install-module-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-17
---

# Phase 84.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib testing) + shell-gated UAT |
| **Config file** | none — Wave 0 adds `Makefile:test` target + `go get github.com/hashicorp/hcl/v2@v2.23.0` |
| **Quick run command** | `go test ./pkg/terragrunt/... ./internal/app/cmd/...` |
| **Full suite command** | `make test` (runs `go test ./...` + existing shell gates) |
| **Estimated runtime** | ~30s for `go test ./...` (no AWS calls); ~45min for full fresh-prefix UAT |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/terragrunt/...` (or the narrower package the task touched)
- **After every plan wave:** Run `make test` (full Go suite + shell gates)
- **Before `/gsd:verify-work`:** Full `make test` green + the fresh-prefix UAT shell artifacts present (`before.json`, `after.json`, inventory-diff output empty for `km-*` resources)
- **Max feedback latency:** ~30s for unit tests; UAT is manual + multi-hour (gated to wave-N completion)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 84.4-W0-01 | 00 | 0 | infra | toolchain | `go list -m github.com/hashicorp/hcl/v2` | ❌ W0 | ⬜ pending |
| 84.4-W0-02 | 00 | 0 | infra | toolchain | `grep -q '^test:' Makefile` | ❌ W0 | ⬜ pending |
| 84.4-01-01 | 01 | 1 | KM-LITERAL-AUDIT | unit (HCL static-analysis) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix` | ❌ W0 | ⬜ pending |
| 84.4-01-02 | 01 | 1 | KM-LITERAL-AUDIT | unit (allow-list regression) | `go test ./pkg/terragrunt/ -run TestModuleAllowList` | ❌ W0 | ⬜ pending |
| 84.4-02-01 | 02 | 1 | SCP-PREFIX-NAMESPACING | static (HCL audit) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix/scp` | ❌ W0 | ⬜ pending |
| 84.4-02-02 | 02 | 1 | SCP-PREFIX-NAMESPACING | unit (5KB-limit) | `go test ./pkg/terragrunt/ -run TestSCPPolicySizeUnderLimit` | ❌ W0 | ⬜ pending |
| 84.4-02-03 | 02 | 2 | SCP-PREFIX-NAMESPACING | integration (zero-diff plan) | `km init --plan` (manual: assert SCP shows no changes against existing `km` install) | n/a | ⬜ pending |
| 84.4-03-01 | 03 | 1 | EFS-PREFIX-NAMESPACING | static (HCL audit) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix/efs` | ❌ W0 | ⬜ pending |
| 84.4-03-02 | 03 | 2 | EFS-PREFIX-NAMESPACING | integration (zero-diff plan) | `km init --plan` (manual: assert EFS shows no changes against existing `km` install) | n/a | ⬜ pending |
| 84.4-04-01 | 04 | 1 | S3-REPLICATION-PREFIX-NAMESPACING | static (HCL audit) | `go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix/s3-replication` | ❌ W0 | ⬜ pending |
| 84.4-04-02 | 04 | 2 | S3-REPLICATION-PREFIX-NAMESPACING | integration (zero-diff plan) | `km init --plan` (manual) | n/a | ⬜ pending |
| 84.4-05-01 | 05 | 2 | BOOTSTRAP-AUTO-IMPORT-EXISTING-DNS | unit (Runner.Import method) | `go test ./pkg/terragrunt/ -run TestRunnerImport` | ❌ W0 | ⬜ pending |
| 84.4-05-02 | 05 | 2 | BOOTSTRAP-AUTO-IMPORT-EXISTING-DNS | unit (auto-detect helper) | `go test ./internal/app/cmd/ -run TestRunBootstrapSharedSESAutoImport` | ❌ W0 | ⬜ pending |
| 84.4-05-03 | 05 | 3 | BOOTSTRAP-AUTO-IMPORT-EXISTING-DNS | manual UAT | `km bootstrap --shared-ses --dry-run=false` on shared-domain second install — DKIM/MX/TXT imported without operator action | n/a | ⬜ pending |
| 84.4-06-01 | 06 | 1 | KM-LITERAL-AUDIT | unit (BuildSCPPolicy prefix) | `go test ./internal/app/cmd/ -run TestBuildSCPPolicyPrefix` | ❌ W0 | ⬜ pending |
| 84.4-07-01 | 07 | 3 | TEARDOWN-AND-RESTART-VERIFICATION | manual UAT (BEFORE snapshot) | `scripts/inventory-diff.sh snapshot before.json` against acct 052251888500 | ❌ W0 | ⬜ pending |
| 84.4-07-02 | 07 | 3 | TEARDOWN-AND-RESTART-VERIFICATION | manual UAT (teardown sequence) | `km uninit` + manual SCP detach/delete + manual EFS delete + `km unbootstrap` from `klanker-maker-whereiskurt/` | n/a | ⬜ pending |
| 84.4-07-03 | 07 | 3 | TEARDOWN-AND-RESTART-VERIFICATION | manual UAT (AFTER diff) | `scripts/inventory-diff.sh diff before.json after.json` — zero `km-*` diff, all `whereiskurt-*` gone | n/a | ⬜ pending |
| 84.4-08-01 | 08 | 4 | FRESH-PREFIX-UAT | manual UAT (bootstrap path) | fresh `rg` prefix: clone → configure → bootstrap → bootstrap --shared-ses → init --plan → init | n/a | ⬜ pending |
| 84.4-08-02 | 08 | 4 | FRESH-PREFIX-UAT | manual UAT (sandbox lifecycle) | `km create profiles/learn.yaml` against fresh `rg` install + healthy state + `km otel` shows traffic | n/a | ⬜ pending |
| 84.4-08-03 | 08 | 4 | FRESH-PREFIX-UAT | manual UAT (clean removal diff) | `km destroy` + `km uninit` + `km unbootstrap` + AWS-CLI diff against `km` install: byte-identical | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `go get github.com/hashicorp/hcl/v2@v2.23.0` — required for `pkg/terragrunt/modulehygiene_test.go` HCL AST traversal (currently only HCL1 is an indirect dep)
- [ ] `Makefile` — add `test:` target invoking `go test ./...` plus the existing shell gates; ensure `pkg/terragrunt/modulehygiene_test.go` is picked up by package globbing
- [ ] `pkg/terragrunt/testdata/synthetic-prefix-kph.tf` — synthetic HCL fixture with 3-char prefix for SCP rendering test
- [ ] `pkg/terragrunt/testdata/synthetic-prefix-whereiskurt.tf` — synthetic HCL fixture with 11-char prefix (exercises 5KB SCP boundary)
- [ ] `pkg/terragrunt/testdata/allowlist.txt` — permitted literal lines (`Component = "km-slack-inbound"` tag values, etc.)
- [ ] `scripts/inventory-diff.sh` — AWS-CLI inventory snapshot+diff helper (EFS, SCPs, IAM roles/policies, Lambda, DynamoDB, S3, KMS aliases, Route53, SES rules; jq-filtered by `km:resource-prefix` tag)
- [ ] `pkg/terragrunt/runner.go` — add `Runner.Import(addr, id string) error` method (Wave 1 prerequisite for Wave 2 bootstrap DKIM auto-import)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Whereiskurt probe teardown leaves `km` install byte-identical | TEARDOWN-AND-RESTART-VERIFICATION | Live AWS account ops + SCP detach + EFS delete sequencing dependencies; cannot be CI-automated against shared infra | Run `scripts/inventory-diff.sh snapshot` → execute teardown sequence (`km uninit`, manual SCP detach/delete, manual EFS delete, `km unbootstrap`) → re-snapshot → `diff before.json after.json` — assert all `whereiskurt-*` resources gone, all `km-*` resources unchanged |
| Fresh-prefix UAT — bootstrap+init green for `rg` | FRESH-PREFIX-UAT | Cross-account orchestration (management → application AWS account, SCP attach, foundation SES), Route53 propagation timing, real DKIM probe | Pick fresh prefix `rg`, clone repo to `klanker-maker-rg/`, run `km configure` → `km bootstrap` → `km bootstrap --shared-ses` → `km init --plan` (zero destroy-class trips) → `km init --dry-run=false` (17 regional modules green) |
| Fresh-prefix UAT — sandbox runtime works | FRESH-PREFIX-UAT | Sandbox lifecycle hits real Lambda + SCP + EFS at runtime; only a real `km create` exercises the SCP `trusted_arns_*` prefix-namespace path end-to-end (the load-bearing failure mode 84.4 exists to fix) | `km create profiles/learn.yaml` against fresh install — sandbox reaches healthy state, `initCommands` execute, `km otel` shows traffic, no SCP-deny in CloudTrail |
| Fresh-prefix UAT — clean removal | FRESH-PREFIX-UAT | Same as teardown UAT but for the fresh install; verifies the second install's uninit/unbootstrap leaves the first byte-identical | `km destroy <sandbox>` → `km uninit` → `km unbootstrap` → inventory-diff against `km` install snapshot |
| `km bootstrap --shared-ses` auto-imports DKIM/MX/TXT on second install | BOOTSTRAP-AUTO-IMPORT-EXISTING-DNS | Real Route53+SES state divergence between AWS and tfstate; requires a second install in shared-domain account | Run `km bootstrap --shared-ses --dry-run=false` from fresh-prefix working directory after `km bootstrap` already ran; assert no manual `terragrunt import` invocations needed, 3 DKIM + MX + TXT records present in foundation tfstate |
| `km init --plan` destroy-class gate trips zero protected resources | FRESH-PREFIX-UAT, SCP-/EFS-/S3-REPLICATION-PREFIX-NAMESPACING | Real terragrunt plan output against AWS-state-aware lifecycle | Run `km init --plan` against fresh `rg` install — no SES identities/Route53 records/S3 buckets/DynamoDB tables/KMS keys appear in destroy-class trip list |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify OR Wave 0 dependencies declared
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (Wave 3 UAT is intentionally grouped — gated only by AUT inventory-diff scripts being committed in Wave 0)
- [ ] Wave 0 covers all MISSING references (`hcl/v2` dep, `Makefile:test` target, `Runner.Import()`, inventory-diff script, testdata fixtures)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for unit tests
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
