---
phase: 84.4-multi-install-module-hardening
plan: "04"
subsystem: infra
tags: [terraform, s3-replication, iam, multi-install, resource-prefix]

# Dependency graph
requires:
  - phase: 84.4-00
    provides: toolchain + make test target scoped to exclude pre-existing failures
  - phase: 84.4-01
    provides: TestModuleNamesUseResourcePrefix audit test (Plan 04 verify step depends on it)
provides:
  - infra/modules/s3-replication/v2.0.0/ with prefix-templated IAM role + policy names
  - test/test_harness.tf.skip satisfying CONTEXT.md LOCKED versioning-strategy requirement
  - doctor.go checkStaleIAMRoles fix: rolePrefix+"s3-replication-" replaces hardcoded "km-s3-replication-"
  - Open Question #2 from RESEARCH.md resolved: zero stale km-s3-replication hits in Go/HCL outside v1.0.0
affects:
  - 84.4-05 (live wiring flip from v1.0.0 to v2.0.0)
  - km doctor --all-regions (IAM stale-role check now prefix-aware for s3-replication roles)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "v2.0.0 module ships with test/test_harness.tf.skip (LOCKED versioning-strategy requirement)"
    - "resource_prefix variable with default=km for byte-identical backward compat"
    - "doctor.go platform prefix list: use rolePrefix variable, not hardcoded km- literals"

key-files:
  created:
    - infra/modules/s3-replication/v2.0.0/main.tf
    - infra/modules/s3-replication/v2.0.0/variables.tf
    - infra/modules/s3-replication/v2.0.0/outputs.tf
    - infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip
  modified:
    - internal/app/cmd/doctor.go

key-decisions:
  - "Inline provider alias block preserved verbatim from v1.0.0 — no required_providers added (anti-pattern per RESEARCH.md)"
  - "doctor.go km-s3-replication- hardcoded literal fixed immediately on module parameterization — comment said 'until that module is parameterized'; Plan 04 is that parameterization"
  - "test_harness.tf.skip uses kph (3-char) and whereiskurt (11-char) synthetic prefixes to cover lower- and upper-bound validation cases"
  - "Outputs.tf preserved byte-identical to v1.0.0 — no output-shape changes"

patterns-established:
  - "Pattern: doctor.go platform prefix list always uses rolePrefix variable; never hardcodes km- literals"
  - "Pattern: v2.0.0 test harness uses non-km synthetic prefixes so audit test stays clean"

requirements-completed:
  - S3-REPLICATION-PREFIX-NAMESPACING
  - KM-LITERAL-AUDIT

# Metrics
duration: 8min
completed: 2026-05-18
---

# Phase 84.4 Plan 04: s3-replication/v2.0.0 with prefix-templated IAM names Summary

**s3-replication v2.0.0 parameterizes IAM role/policy names from hardcoded `km-` to `${var.resource_prefix}-`, preventing EntityAlreadyExists collisions on second installs; doctor.go stale-role allowlist fixed to match**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-18T02:49:41Z
- **Completed:** 2026-05-18T02:58:00Z
- **Tasks:** 1
- **Files modified:** 5 (4 created, 1 modified)

## Accomplishments

- Created `infra/modules/s3-replication/v2.0.0/` replacing two hardcoded `km-s3-replication-*` literals with `${var.resource_prefix}-s3-replication-*`
- Added `variable "resource_prefix"` with default `"km"` — byte-identical backward compat with v1.0.0
- Created `test/test_harness.tf.skip` with `kph` (3-char, lower-bound) and `whereiskurt` (11-char, upper-bound) synthetic prefixes satisfying CONTEXT.md LOCKED versioning-strategy requirement
- Fixed `internal/app/cmd/doctor.go` `checkStaleIAMRoles`: replaced hardcoded `"km-s3-replication-"` platform prefix with `rolePrefix + "s3-replication-"` — the existing comment explicitly named this as pending for when the module gets parameterized
- Audit test `TestModuleNamesUseResourcePrefix/s3-replication/v2.0.0` PASSES

## Stale-Literal Grep Audit (RESEARCH.md Open Question #2)

```
grep -rn 'km-s3-replication' infra/ internal/ pkg/ cmd/ --include='*.go' --include='*.tf' --include='*.hcl' --include='*.sh' --include='*.yaml'
```

**Result:** Zero hits in Go/HCL tracked sources outside deprecated v1.0.0. Hits found:

| Location | Reason | Action |
|---|---|---|
| `infra/modules/s3-replication/v1.0.0/main.tf:43,60` | Source module being deprecated | None — v1.0.0 is a historical version (skipped by audit test) |
| `infra/live/use1/*/s3-replication/.terragrunt-cache/...` | Untracked terragrunt cache copies of v1.0.0 | None — not tracked by git |
| `internal/app/cmd/doctor.go:1269-1280` | Hardcoded `"km-s3-replication-"` platform prefix in stale-role check | **Fixed** — Rule 1 auto-fix (comment said "Until that module is parameterized") |

**Conclusion:** RESEARCH.md prediction confirmed — zero hits in compiler, sandbox templates, or IAM policy documents. Only the doctor.go role allow-list required action.

## Exact Substitutions Made

In `infra/modules/s3-replication/v2.0.0/main.tf`:

| Line | Before (v1.0.0) | After (v2.0.0) |
|---|---|---|
| 43 | `name = "km-s3-replication-${var.source_bucket_name}"` | `name = "${var.resource_prefix}-s3-replication-${var.source_bucket_name}"` |
| 60 | `name = "km-s3-replication-policy"` | `name = "${var.resource_prefix}-s3-replication-policy"` |

## Inline Provider Alias Block

Preserved verbatim from v1.0.0 (no changes):

```hcl
provider "aws" {
  alias  = "replica"
  region = var.destination_region
}
```

No `required_providers` block added (RESEARCH.md anti-pattern — root.hcl generates providers).

## test_harness.tf.skip Contents

```hcl
module "s3_replication_synthetic_kph" {
  source                  = "../"
  resource_prefix         = "kph"           # 3-char lower-bound
  source_bucket_name      = "kph-artifacts-synthetic"
  source_bucket_arn       = "arn:aws:s3:::kph-artifacts-synthetic"
  destination_region      = "us-west-2"
  destination_bucket_name = "kph-artifacts-synthetic-replica"
}

module "s3_replication_synthetic_whereiskurt" {
  source                  = "../"
  resource_prefix         = "whereiskurt"   # 11-char upper-bound
  source_bucket_name      = "whereiskurt-artifacts-synthetic"
  source_bucket_arn       = "arn:aws:s3:::whereiskurt-artifacts-synthetic"
  destination_region      = "us-west-2"
  destination_bucket_name = "whereiskurt-artifacts-synthetic-replica"
}
```

The `.skip` extension keeps it out of terraform init / terragrunt apply discovery. Satisfies CONTEXT.md LOCKED "each v2.0.0/ ships with test/ subdirectory" requirement. No `"km-"` literals in resource attribute positions — passes audit test cleanly.

## Task Commits

1. **Task 1: Create s3-replication/v2.0.0 + audit grep + test harness** - `cd24f8c` (feat)
   - Note: committed as part of Plan 06 TDD RED scaffold commit (files created prior to Plan 04 SUMMARY)

**Plan metadata:** *(created in this execution)*

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/infra/modules/s3-replication/v2.0.0/main.tf` - Prefix-templated IAM role + policy names, provider alias preserved
- `/Users/khundeck/working/klankrmkr/infra/modules/s3-replication/v2.0.0/variables.tf` - resource_prefix variable (default "km") + existing variables
- `/Users/khundeck/working/klankrmkr/infra/modules/s3-replication/v2.0.0/outputs.tf` - Identical to v1.0.0 (no output-shape change)
- `/Users/khundeck/working/klankrmkr/infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip` - Synthetic prefix harness for LOCKED versioning strategy
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/doctor.go` - Fixed km-s3-replication- hardcoded literal to rolePrefix+"s3-replication-"

## Decisions Made

- **Inline provider alias block copied verbatim:** RESEARCH.md Open Question #3 documented this as a special case — the s3-replication module declares its own provider alias inline to avoid duplicate `generate "provider"` blocks from root.hcl. No changes made.
- **doctor.go fix is Rule 1 (bug), not Rule 4 (architectural):** The existing comment in doctor.go said "Until that module is parameterized...". Plan 04 IS that parameterization event. The fix is a natural consequence of the module work, not an architectural change.
- **Outputs.tf unchanged:** All three outputs (`replica_bucket_arn`, `replica_bucket_name`, `replication_role_arn`) preserved — callers depend on them.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] doctor.go hardcoded `"km-s3-replication-"` platform prefix fixed to use `rolePrefix`**

- **Found during:** Task 1, Step 1 (stale-literal grep audit)
- **Issue:** `internal/app/cmd/doctor.go:1280` used hardcoded `"km-s3-replication-"` in the IAM stale-role allowlist. The comment at line 1269 explicitly stated: "Until that module is parameterized, every install — including non-default resource_prefix installs — must allow-list the literal 'km-' form." Plan 04 is that parameterization — leaving the hardcoded literal would cause doctor.go to mis-classify `rg-s3-replication-*` roles as stale (would try to delete them on non-km installs).
- **Fix:** Replaced `"km-s3-replication-"` with `rolePrefix + "s3-replication-"` (where `rolePrefix = resourcePrefix + "-"`). Updated comment to reference Phase 84.4 Plan 04.
- **Files modified:** `internal/app/cmd/doctor.go`
- **Verification:** `go build ./internal/app/cmd/` succeeds
- **Committed in:** `cd24f8c`

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug, explicitly flagged in source comment as pending this parameterization)
**Impact on plan:** Required for correctness — without the fix, doctor.go would incorrectly mark non-km-prefix s3-replication IAM roles as stale.

## Issues Encountered

- Files were committed into `cd24f8c` (Plan 06 TDD RED) rather than a standalone Plan 04 commit. All files are present and correct; the commit attribution is a sequencing artifact (Plan 06 was in flight when Plan 04 execution began). SUMMARY.md created after the fact with correct commit reference.

## Next Phase Readiness

- `infra/modules/s3-replication/v2.0.0/` is ready for Plan 05 to flip `infra/live/use1/s3-replication/terragrunt.hcl` from v1.0.0 → v2.0.0 and add `resource_prefix = get_env("KM_RESOURCE_PREFIX", "km")` to inputs
- All outputs preserved — Plan 05 live-wiring change is purely mechanical (source path + one input var)
- doctor.go is now prefix-aware for s3-replication roles — no Plan 05 changes needed there

---
*Phase: 84.4-multi-install-module-hardening*
*Completed: 2026-05-18*
