---
phase: 84.4-multi-install-module-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "84.4-00"
  - "84.4-01"
files_modified:
  - infra/modules/s3-replication/v2.0.0/main.tf
  - infra/modules/s3-replication/v2.0.0/variables.tf
  - infra/modules/s3-replication/v2.0.0/outputs.tf
  - infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip
autonomous: true
requirements:
  - S3-REPLICATION-PREFIX-NAMESPACING
  - KM-LITERAL-AUDIT

must_haves:
  truths:
    - "infra/modules/s3-replication/v2.0.0/ exists with prefix-templated IAM role + policy names"
    - "With resource_prefix=km, names render byte-identical to v1.0.0"
    - "With resource_prefix=rg, IAM role becomes rg-s3-replication-${source_bucket_name} (no EntityAlreadyExists collision)"
    - "grep -rn 'km-s3-replication' across infra/, internal/, pkg/, cmd/ shows zero hits in non-v1.0.0 paths"
    - "Audit test (Plan 01) passes against s3-replication/v2.0.0/"
    - "test/test_harness.tf.skip exists per CONTEXT.md LOCKED versioning strategy"
  artifacts:
    - path: "infra/modules/s3-replication/v2.0.0/main.tf"
      provides: "Prefix-templated IAM role + policy names for cross-region S3 replication"
      contains: "var.resource_prefix"
    - path: "infra/modules/s3-replication/v2.0.0/variables.tf"
      provides: "variable resource_prefix with default \"km\""
      contains: "variable \"resource_prefix\""
    - path: "infra/modules/s3-replication/v2.0.0/outputs.tf"
      provides: "Outputs preserved from v1.0.0"
    - path: "infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip"
      provides: "Minimal HCL fixture instantiating s3-replication/v2.0.0 with synthetic prefix — satisfies CONTEXT.md LOCKED test/ subdirectory requirement"
      contains: "module \"s3_replication"
  key_links:
    - from: "infra/modules/s3-replication/v2.0.0/main.tf aws_iam_role.replication"
      to: "var.resource_prefix"
      via: "name interpolation"
      pattern: "name.*var\\.resource_prefix.*s3-replication"
    - from: "infra/modules/s3-replication/v2.0.0/main.tf aws_iam_role_policy.replication"
      to: "var.resource_prefix"
      via: "name interpolation"
      pattern: "name.*var\\.resource_prefix.*s3-replication-policy"
    - from: "infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip"
      to: "infra/modules/s3-replication/v2.0.0/"
      via: "module source path"
      pattern: "source.*v2.0.0"
---

<objective>
Create `infra/modules/s3-replication/v2.0.0/` with the two hardcoded `km-s3-replication-*`
IAM literals replaced by `${var.resource_prefix}-s3-replication-*`. IAM policy names are
unique per AWS account — a second install hits `EntityAlreadyExists` on the policy name
without this fix.

Also includes a repo-wide grep audit for stale `km-s3-replication-*` literal string
references (compiler emit, sandbox userdata, etc.) — RESEARCH.md Open Question #2.
Expected hit count: zero outside `s3-replication/v1.0.0/main.tf` itself.

Wave-ordering note: Plan 04 depends on both Plan 00 (toolchain) AND Plan 01 (audit test
scaffold). The `<verify>` step invokes `TestModuleNamesUseResourcePrefix`, which is
created by Plan 01 — without that dependency, parallel Wave 1 execution would race.
After this revision, Plan 04 runs in Wave 2 (after both 00 and 01 land).

Purpose: S3-REPLICATION-PREFIX-NAMESPACING. The simplest of the three v2.0.0 module
bumps (only 2 literals to substitute).

Output: s3-replication/v2.0.0/ module + `test/test_harness.tf.skip` (LOCKED versioning-
strategy requirement) + (if grep surfaces hits) updates to any callers referencing
the role name as a string literal.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84.4-multi-install-module-hardening/84.4-CONTEXT.md
@.planning/phases/84.4-multi-install-module-hardening/84.4-RESEARCH.md
@.planning/phases/84.4-multi-install-module-hardening/84.4-00-SUMMARY.md
@.planning/phases/84.4-multi-install-module-hardening/84.4-01-SUMMARY.md
@infra/modules/s3-replication/v1.0.0/main.tf
@infra/modules/s3-replication/v1.0.0/variables.tf
@infra/modules/s3-replication/v1.0.0/outputs.tf
@infra/live/use1/s3-replication/terragrunt.hcl

<interfaces>
v1.0.0 hardcoded literals (RESEARCH.md HIGH confidence):

infra/modules/s3-replication/v1.0.0/main.tf — substitutions needed:
- Line 43: aws_iam_role.replication.name = "km-s3-replication-${var.source_bucket_name}"
   becomes "${var.resource_prefix}-s3-replication-${var.source_bucket_name}"
- Line 60: aws_iam_role_policy.replication.name = "km-s3-replication-policy"
   becomes "${var.resource_prefix}-s3-replication-policy"

Note from RESEARCH.md Open Question #3: This module declares its own provider alias
inline (not via include "root") to avoid duplicate generate "provider" blocks. Copy the
v1.0.0 provider block verbatim into v2.0.0 — no provider changes.

Note from RESEARCH.md Pattern 1 (s3-replication live wiring):
The live wiring already uses get_env for resource_prefix — Plan 05 will flip
source = "../../modules/s3-replication/v1.0.0" -> v2.0.0 and add
resource_prefix = get_env("KM_RESOURCE_PREFIX", "km") to inputs.

Backward-compat default (CONTEXT.md LOCKED):
variable "resource_prefix" {
  type        = string
  default     = "km"
  description = "Resource-name prefix. Default 'km' renders byte-identical names to v1.0.0."
  validation {
    condition     = can(regex("^[a-z][a-z0-9]{0,11}$", var.resource_prefix))
    error_message = "resource_prefix must be 1-12 chars, start with a lowercase letter, contain only [a-z0-9]."
  }
}

Test harness pattern (CONTEXT.md LOCKED versioning strategy — mirrors Plan 03):
- `.skip` extension keeps the file out of terraform init / terragrunt apply paths
- Instantiates the module with synthetic inputs to demonstrate intended consumption
- Not executed by CI — the Plan 01 audit test parses .tf files (the harness is
  documentation; if Plan 01 walks `.skip` too, synthetic prefixes keep it audit-clean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infra/modules/s3-replication/v2.0.0/ with prefix-templated IAM names + audit grep + test harness</name>
  <files>infra/modules/s3-replication/v2.0.0/main.tf, infra/modules/s3-replication/v2.0.0/variables.tf, infra/modules/s3-replication/v2.0.0/outputs.tf, infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip</files>
  <action>
    Step 1 — Grep for stale `km-s3-replication-*` string-literal references across the
    repo (RESEARCH.md Open Question #2):
    ```
    grep -rn 'km-s3-replication' infra/ internal/ pkg/ cmd/ --include='*.go' --include='*.tf' --include='*.hcl' --include='*.sh' --include='*.yaml'
    ```
    Expected output: ONLY hits in `infra/modules/s3-replication/v1.0.0/main.tf` (the
    module being deprecated). If grep surfaces any other hit (compiler-emitted userdata,
    sandbox templates, IAM policy documents elsewhere), record it in the SUMMARY and
    add a Task 2 to update those references. The RESEARCH.md prediction is "zero hits
    beyond v1.0.0" but the actual grep is load-bearing.

    Step 2 — Copy v1.0.0 to v2.0.0:
    ```
    mkdir -p infra/modules/s3-replication/v2.0.0
    cp infra/modules/s3-replication/v1.0.0/main.tf infra/modules/s3-replication/v2.0.0/main.tf
    cp infra/modules/s3-replication/v1.0.0/variables.tf infra/modules/s3-replication/v2.0.0/variables.tf
    cp infra/modules/s3-replication/v1.0.0/outputs.tf infra/modules/s3-replication/v2.0.0/outputs.tf
    ```

    Step 3 — Edit `infra/modules/s3-replication/v2.0.0/variables.tf` — add `resource_prefix`
    variable (with default "km" for backward compat). Use the exact form from <interfaces>.

    Step 4 — Edit `infra/modules/s3-replication/v2.0.0/main.tf`:
    - `aws_iam_role.replication.name`: `"km-s3-replication-${var.source_bucket_name}"` -> `"${var.resource_prefix}-s3-replication-${var.source_bucket_name}"`
    - `aws_iam_role_policy.replication.name`: `"km-s3-replication-policy"` -> `"${var.resource_prefix}-s3-replication-policy"`
    - If `tags = {}` blocks exist on these resources, add `"km:resource-prefix" = var.resource_prefix` (Phase 82 convention)
    - Do NOT touch the inline `provider "aws" { alias = "replica" ... }` block (RESEARCH.md Open Q3)
    - Do NOT add `required_providers` (RESEARCH.md anti-pattern)

    Step 5 — Sanity check:
    ```
    grep -E 'name[[:space:]]*=[[:space:]]*"km-s3-replication' infra/modules/s3-replication/v2.0.0/main.tf
    ```
    Expected: zero matches.

    Step 6 — terraform fmt:
    ```
    terraform fmt -check infra/modules/s3-replication/v2.0.0/
    ```

    Step 7 — Create `infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip` per
    CONTEXT.md LOCKED versioning-strategy requirement (mirrors Plan 03's harness +
    `cluster-irsa/v1.0.0/test_harness.tf.skip` precedent):
    ```
    mkdir -p infra/modules/s3-replication/v2.0.0/test
    ```
    Write the file with minimal HCL:
    ```hcl
    # infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip
    # Phase 84.4 test harness for s3-replication/v2.0.0. NOT executed by terragrunt
    # apply — the .skip extension keeps it out of init/apply discovery. Documents
    # intended consumption with synthetic inputs and serves as the LOCKED test/
    # subdirectory required by CONTEXT.md versioning strategy. Mirrors the pattern
    # from infra/modules/cluster-irsa/v1.0.0/test_harness.tf.skip.
    #
    # The audit test in pkg/terragrunt/modulehygiene_test.go (Plan 01) parses every
    # .tf file under v2.0.0+/, so any hardcoded "km-" literal added here would fail
    # TestModuleNamesUseResourcePrefix.

    module "s3_replication_synthetic_kph" {
      source = "../"

      resource_prefix         = "kph"          # 3-char prefix — lower-bound case
      source_bucket_name      = "kph-artifacts-synthetic"
      source_bucket_arn       = "arn:aws:s3:::kph-artifacts-synthetic"
      destination_region      = "us-west-2"
      destination_bucket_name = "kph-artifacts-synthetic-replica"
    }

    module "s3_replication_synthetic_whereiskurt" {
      source = "../"

      resource_prefix         = "whereiskurt"  # 11-char prefix — upper-bound case
      source_bucket_name      = "whereiskurt-artifacts-synthetic"
      source_bucket_arn       = "arn:aws:s3:::whereiskurt-artifacts-synthetic"
      destination_region      = "us-west-2"
      destination_bucket_name = "whereiskurt-artifacts-synthetic-replica"
    }
    ```
    Verify input variable names against the actual `infra/modules/s3-replication/v2.0.0/variables.tf`
    — adapt the harness if the input names differ. The harness must compile as valid
    HCL even though it never runs through terraform.

    Step 8 — Run audit test (Plan 01 must have landed first per `depends_on`):
    ```
    go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix -count=1 -v
    ```

    Avoid: changing the inline provider alias block (it's load-bearing for cross-region
    replication), adding required_providers (would break live wiring), removing outputs
    (callers depend on them), reordering variables, naming the harness `.tf` (would be
    picked up by terragrunt init — keep `.tf.skip`).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ls infra/modules/s3-replication/v2.0.0/main.tf infra/modules/s3-replication/v2.0.0/variables.tf infra/modules/s3-replication/v2.0.0/outputs.tf infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip && grep -c 'var\.resource_prefix' infra/modules/s3-replication/v2.0.0/main.tf && grep -q 'variable "resource_prefix"' infra/modules/s3-replication/v2.0.0/variables.tf && ! grep -qE 'name[[:space:]]*=[[:space:]]*"km-s3-replication' infra/modules/s3-replication/v2.0.0/main.tf && terraform fmt -check infra/modules/s3-replication/v2.0.0/ && grep -q 'resource_prefix' infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip && ! grep -qE '"km-' infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip && go test ./pkg/terragrunt/ -run TestModuleNamesUseResourcePrefix -count=1</automated>
  </verify>
  <done>
    - `infra/modules/s3-replication/v2.0.0/` exists with main.tf, variables.tf, outputs.tf
    - ≥2 occurrences of `var.resource_prefix` in main.tf
    - No `km-s3-replication` literal in `name = ` positions
    - Inline `provider "aws" { alias = "replica" }` block preserved verbatim from v1.0.0
    - `variable "resource_prefix" { default = "km" }` in variables.tf
    - `terraform fmt -check` passes
    - `infra/modules/s3-replication/v2.0.0/test/test_harness.tf.skip` exists with `kph` and `whereiskurt` synthetic prefixes (no `km-` literals)
    - Audit test (Plan 01) passes against s3-replication/v2.0.0/
    - Stale-literal grep across repo recorded in SUMMARY (zero hits expected; non-zero = follow-up task)
  </done>
</task>

</tasks>

<verification>
- `make test` passes after Plan 04 merges
- Grep result for `km-s3-replication` outside v1.0.0/ is recorded in SUMMARY
- outputs.tf identical to v1.0.0 (no output-shape change)
- `test/test_harness.tf.skip` satisfies CONTEXT.md LOCKED versioning-strategy test requirement
</verification>

<success_criteria>
- S3-REPLICATION-PREFIX-NAMESPACING requirement satisfied at module-source level
- KM-LITERAL-AUDIT requirement progressed
- Backward compat preserved: `resource_prefix=km` renders byte-identical to v1.0.0
- Open Question #2 from RESEARCH.md resolved (audit grep result recorded)
- CONTEXT.md LOCKED "each v2.0.0/ ships with test/ subdirectory" requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/84.4-multi-install-module-hardening/84.4-04-SUMMARY.md` documenting:
- Stale-literal grep result (file:line of any hit, or "zero hits beyond v1.0.0" as RESEARCH.md predicts)
- Exact substitutions made
- Whether the inline provider alias block was copied unchanged
- The test_harness.tf.skip contents + how it relates to the LOCKED versioning-strategy requirement
</output>
</output>
