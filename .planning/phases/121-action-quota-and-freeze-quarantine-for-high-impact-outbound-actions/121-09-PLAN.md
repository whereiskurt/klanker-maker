---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 09
type: execute
wave: 3
depends_on: [08]
files_modified:
  - cmd/km-quota-alerter/main.go
  - cmd/km-quota-alerter/alerter.go
  - cmd/km-quota-alerter/alerter_test.go
  - infra/modules/lambda-quota-alerter/v1.0.0/main.tf
  - infra/modules/lambda-quota-alerter/v1.0.0/variables.tf
  - infra/live/use1/lambda-quota-alerter/terragrunt.hcl
  - internal/app/cmd/init.go
autonomous: true
requirements: [INIT-02, ALR-01]

must_haves:
  truths:
    - "km-quota-alerter is triggered by the action-quota DDB Stream and fires exactly one operator alert per (sandbox, action, window)"
    - "On the MODIFY where a window first breaches (breached_at just set, alert_sent absent): send SES operator email + optional Slack control-channel post, then set alert_sent via attribute_not_exists(alert_sent) conditional write"
    - "For proxy-origin trips it posts the channel-level user notice to the sandbox's main Slack channel (resolved from km-sandboxes)"
    - "All four registration points exist: TF module + live unit + regionalModules() entry + lambdaBuilds() entry"
    - "The Lambda has DDB-Stream read + SES send + SSM bot-token read + km-sandboxes GetItem + action-quota UpdateItem IAM"
  artifacts:
    - path: "cmd/km-quota-alerter/alerter.go"
      provides: "DDB-Stream handler + idempotent alert"
      contains: "attribute_not_exists"
    - path: "infra/modules/lambda-quota-alerter/v1.0.0/main.tf"
      provides: "Lambda + event_source_mapping(stream) + IAM"
      contains: "aws_lambda_event_source_mapping"
    - path: "internal/app/cmd/init.go"
      provides: "regionalModules() + lambdaBuilds() entries for km-quota-alerter"
      contains: "km-quota-alerter"
  key_links:
    - from: "infra/modules/lambda-quota-alerter/v1.0.0/main.tf"
      to: "dynamodb-action-quota stream"
      via: "aws_lambda_event_source_mapping event_source_arn = var.quota_stream_arn"
      pattern: "event_source_arn"
    - from: "internal/app/cmd/init.go lambdaBuilds()"
      to: "cmd/km-quota-alerter"
      via: "build-list entry"
      pattern: "km-quota-alerter"
---

<objective>
Build the `km-quota-alerter` Lambda: DDB-Stream-triggered, fires exactly one operator alert per
(sandbox, action, window) via SES (+ optional Slack control channel), and posts the channel-level
user notice for proxy-origin trips. Register it across ALL FOUR deploy-surface points (TF module +
live unit + `regionalModules()` + `lambdaBuilds()`) — module existence alone is invisible to
`km init` (memory `project_new_lambda_needs_live_unit_and_init_list`,
`project_km_init_skips_existing_lambda_zips`). This is the second of the two modules that complete
the 24→26 module-count.

Purpose: The decoupled operator-alert + proxy-trip-user-notice half of the dual notification model
(CONTEXT.md §5). Risk 3: there is NO existing `aws_lambda_event_source_mapping` pattern in the repo
— create it from scratch.
Output: Alerter Lambda + full registration; idempotency guard tested.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@infra/modules/lambda-slack-bridge/v1.0.0/main.tf
@infra/live/use1/lambda-slack-bridge/terragrunt.hcl
@pkg/aws/ses.go
@internal/app/cmd/init.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alerter Lambda code + idempotent alert (ALR-01)</name>
  <files>cmd/km-quota-alerter/main.go, cmd/km-quota-alerter/alerter.go, cmd/km-quota-alerter/alerter_test.go</files>
  <behavior>
    - Handler receives a DynamoDBEvent. For each record where eventName==MODIFY and the NewImage has
      breached_at set AND alert_sent absent: send the operator alert, then conditionally write
      alert_sent (attribute_not_exists(alert_sent)). A second record for the same row no-ops on the
      conditional write → exactly one alert per (sandbox, action, window).
    - Operator alert: SES email (KM_OPERATOR_EMAIL) via the SendLifecycleNotification pattern
      (pkg/aws/ses.go:41) + optional Slack control-channel post if KM_SLACK_CONTROL_CHANNEL set.
    - For proxy-origin actions (github_*/email_send), also resolve the sandbox's slack_channel_id
      from km-sandboxes (GetItem) and post the enforce-aware channel-level user notice via the bot
      token (SSM /{prefix}/slack/bot-token). Best-effort: skip if no channel.
  </behavior>
  <action>
    Create `cmd/km-quota-alerter/` (replace the plan-01 placeholder main.go). `main.go` wires
    `lambda.Start(handler)`; `alerter.go` holds the testable handler with injected interfaces (SES,
    DDB UpdateItem/GetItem, Slack poster) so the test mocks them. Parse PK=`{sandbox}#{action}`,
    SK=window. Implement the `attribute_not_exists(alert_sent)` conditional write AFTER sending.
    Fill in plan 01's `TestIdempotentAlert` (ALR-01): two stream records for the same breached row
    → SES send called once; the second conditional write returns ConditionalCheckFailed and is
    swallowed.
  </action>
  <verify>
    <automated>go test ./cmd/km-quota-alerter/... -run TestIdempotentAlert -count=1</automated>
  </verify>
  <done>Alerter sends exactly one operator alert per breached row; posts channel-level notice for proxy trips; idempotency tested.</done>
</task>

<task type="auto">
  <name>Task 2: lambda-quota-alerter TF module with event_source_mapping + IAM</name>
  <files>infra/modules/lambda-quota-alerter/v1.0.0/main.tf, infra/modules/lambda-quota-alerter/v1.0.0/variables.tf</files>
  <action>
    Create the module from `infra/modules/lambda-slack-bridge/v1.0.0/main.tf`. Differences:
    - NO Function URL. Add `resource "aws_lambda_event_source_mapping" "quota_stream"` with
      `event_source_arn = var.quota_stream_arn`, `function_name = aws_lambda_function...arn`,
      `starting_position = "LATEST"`, `batch_size = 100`, `bisect_batch_on_function_error = true`,
      `maximum_retry_attempts` reasonable. (Risk 3 — first such mapping in the repo.)
    - IAM: CloudWatch Logs + `ses:SendEmail`/`SendRawEmail` + `ssm:GetParameter` on
      `/{prefix}/slack/bot-token` + `dynamodb:GetItem` on `km-sandboxes` + `dynamodb:UpdateItem` on
      `{prefix}-action-quota` + DDB-Streams read on the stream ARN
      (`dynamodb:GetRecords`, `GetShardIterator`, `DescribeStream`, `ListStreams`).
    - Env: KM_OPERATOR_EMAIL, KM_SLACK_CONTROL_CHANNEL (optional), table names, resource_prefix.
    `variables.tf`: `quota_stream_arn`, `sandboxes_table_name`, `quota_table_name`,
    `operator_email`, `slack_control_channel` (default ""), `artifacts_bucket`, `resource_prefix`.
    No `required_providers`. `terraform fmt`-clean.
  </action>
  <verify>
    <automated>cd infra/modules/lambda-quota-alerter/v1.0.0 && terraform fmt -check . && terraform validate || true; grep -q 'aws_lambda_event_source_mapping' infra/modules/lambda-quota-alerter/v1.0.0/main.tf</automated>
  </verify>
  <done>Module wires the Lambda to the DDB Stream with full IAM (stream-read + SES + SSM + DDB).</done>
</task>

<task type="auto">
  <name>Task 3: Live unit + regionalModules() + lambdaBuilds() (INIT-02)</name>
  <files>infra/live/use1/lambda-quota-alerter/terragrunt.hcl, internal/app/cmd/init.go</files>
  <action>
    (a) Live unit `infra/live/use1/lambda-quota-alerter/terragrunt.hcl`: source the v1.0.0 module;
    declare `dependency "quota_table" { config_path = "../dynamodb-action-quota" }` and pass
    `quota_stream_arn = dependency.quota_table.outputs.stream_arn`; pass sandboxes/quota table names,
    operator_email, artifacts_bucket from the standard inputs. Add "show" to
    `mock_outputs_allowed_terraform_commands` (it has a dependency block — memory
    `project_terragrunt_show_needs_mocks`) and provide mock_outputs for stream_arn.
    (b) `regionalModules()` (init.go:511): add `{ name: "lambda-quota-alerter", dir:
    filepath.Join(regionDir, "lambda-quota-alerter"), envReqs: []string{"KM_ARTIFACTS_BUCKET"} }`
    AFTER `dynamodb-action-quota` (so its stream dependency is applied first) and before `ses`.
    This is the second module bringing the count to 26 (plan 01 already bumped the test).
    (c) `lambdaBuilds()` (init.go:2944): add `{name: "km-quota-alerter", srcDir:
    "cmd/km-quota-alerter"}` — without this the zip is NEVER built. Add the
    `TestQuotaAlerterBuildListMembership` test (INIT-02) asserting the entry is present.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... && go test ./internal/app/cmd/... -run 'TestQuotaAlerterBuildListMembership|TestRunInitPlan_ModuleOrder' -count=1</automated>
  </verify>
  <done>All four registration points present; module-count test == 26 passes; build-list membership test green.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/km-quota-alerter/... -count=1` GREEN (ALR-01).
- `go test ./internal/app/cmd/... -run 'TestQuotaAlerterBuildListMembership|TestRunInitPlan_ModuleOrder' -count=1` GREEN (count now 26).
- `grep -q 'aws_lambda_event_source_mapping' infra/modules/lambda-quota-alerter/v1.0.0/main.tf`.
- Live UAT (phase gate): trip a proxy action → alerter fires from Stream → SES email + channel notice; double-trip → one email.
</verification>

<success_criteria>
- Exactly one operator alert per (sandbox, action, window) (conditional-write guarded).
- Channel-level user notice posts for proxy trips.
- Full 4-part deploy registration so km init builds + applies the alerter.
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-09-SUMMARY.md`
</output>
