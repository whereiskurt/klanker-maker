---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - sidecars/http-proxy/httpproxy/quota_classify.go
  - sidecars/http-proxy/httpproxy/quota_classify_test.go
  - sidecars/http-proxy/httpproxy/ses.go
  - sidecars/http-proxy/httpproxy/proxy.go
  - sidecars/http-proxy/main.go
  - infra/modules/ec2spot/v1.1.0/main.tf
autonomous: true
requirements: [PRX-01, PRX-02, PRX-03]

must_haves:
  truths:
    - "Proxy classifies POST api.github.com /repos/*/pulls → github_pr, .../issues/*/comments → github_comment, .../pulls/*/reviews → github_review"
    - "Proxy MITMs email.*.amazonaws.com and classifies POST /v2/email/outbound-emails → email_send"
    - "Proxy does NOT count *.lambda-url.*.on.aws POSTs (the bridge Function URL) as slack_post — no double-count"
    - "WithActionQuota records the action via pkg/quota.Record; BLOCK mode returns a deny (429) for that action; absent config ⇒ no counting (dormant)"
    - "Per-sandbox EC2 role gets UpdateItem/GetItem on {prefix}-action-quota"
  artifacts:
    - path: "sidecars/http-proxy/httpproxy/quota_classify.go"
      provides: "URL→Action classifier + WithActionQuota proxy option"
      contains: "WithActionQuota"
    - path: "sidecars/http-proxy/httpproxy/ses.go"
      provides: "SES MITM host regex + SendEmail request match"
  key_links:
    - from: "sidecars/http-proxy/httpproxy/proxy.go"
      to: "pkg/quota.Record"
      via: "OnRequest handler scoped to github + SES hosts only"
      pattern: "quota\\.Record"
    - from: "sidecars/http-proxy/main.go"
      to: "WithActionQuota"
      via: "KM_QUOTA_TABLE / KM_ACTION_LIMITS env wiring"
      pattern: "KM_QUOTA_TABLE"
---

<objective>
Make the MITM http-proxy the agent-untrusted chokepoint for GitHub writes + SES email send. Add a
URL→Action classifier, a `WithActionQuota` proxy option (mirroring `WithBudgetEnforcement`,
proxy.go:65), and a NEW MITM registration for `email.*.amazonaws.com` (SES is currently OkConnect
passthrough — RESEARCH.md §2, Risk 2). Critically scope quota counting to ONLY `api.github.com`
and SES — NOT the bridge Function URL — to avoid double-counting slack_post (Risk 1). Add the
per-sandbox IAM grant for the quota table.

Purpose: GitHub + email are the proxy-owned actions (CONTEXT.md §3). The proxy already meters AI
spend; this reuses the same env-var + DDB-client + functional-option machinery.
Output: Proxy classifies + records github_pr/comment/review + email_send; blocks in BLOCK mode;
dormant when unconfigured. SES MITM path is the medium-confidence item flagged for live UAT.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@sidecars/http-proxy/httpproxy/proxy.go
@sidecars/http-proxy/httpproxy/github.go
@sidecars/http-proxy/main.go
@pkg/quota/quota.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: URL→Action classifier + double-count exclusion (PRX-01/03)</name>
  <files>sidecars/http-proxy/httpproxy/quota_classify.go, sidecars/http-proxy/httpproxy/quota_classify_test.go</files>
  <behavior>
    - classifyAction(method, host, path) returns:
      - POST api.github.com /repos/{o}/{r}/pulls            → github_pr
      - POST api.github.com /repos/{o}/{r}/issues/{n}/comments → github_comment
      - POST api.github.com /repos/{o}/{r}/pulls/{n}/reviews   → github_review
      - POST email.{region}.amazonaws.com /v2/email/outbound-emails → email_send
      - POST {anything}.lambda-url.{region}.on.aws /...      → "" (NO action — bridge URL, Risk 1)
      - GET / non-matching                                  → ""
  </behavior>
  <action>
    Create `quota_classify.go` with `classifyAction(method, host, path string) quota.Action`. Reuse
    `ExtractRepoFromPath` (github.go:39) intent for the GitHub path shapes. Add a `sesHostRegex`
    (`^email\.[a-z0-9-]+\.amazonaws\.com$`) and the SESv2 outbound path prefix
    `/v2/email/outbound-emails`. Explicitly return "" for `*.lambda-url.*.on.aws` so the bridge
    Function URL is never counted (this is the entire double-count defense). Turn plan 01's
    `TestClassifyGitHub`, `TestClassifySES`, `TestNoDoubleCount` GREEN (fill in the skipped bodies).
  </action>
  <verify>
    <automated>go test ./sidecars/http-proxy/httpproxy/... -run 'TestClassifyGitHub|TestClassifySES|TestNoDoubleCount' -count=1</automated>
  </verify>
  <done>Classifier maps all three GitHub writes + SES send correctly and returns "" for the bridge URL and non-write traffic.</done>
</task>

<task type="auto">
  <name>Task 2: WithActionQuota option + SES MITM registration + env wiring (PRX-02)</name>
  <files>sidecars/http-proxy/httpproxy/quota_classify.go, sidecars/http-proxy/httpproxy/ses.go, sidecars/http-proxy/httpproxy/proxy.go, sidecars/http-proxy/main.go</files>
  <action>
    (a) Add `WithActionQuota(client quota.QuotaAPI, tableName, sandboxID string, limits quota.Limits) ProxyOption`
    in quota_classify.go, mirroring `WithBudgetEnforcement` (proxy.go:65) — accumulate onto a
    `cfg.actionQuota` field on `proxyConfig` (proxy.go:52).
    (b) In `NewProxy`, inside `if cfg.actionQuota != nil { ... }`, register an `OnRequest` handler
    scoped to `ReqHostMatches(githubHostsRegex)` AND a new SES host matcher only (NOT the bridge
    URL). On a matched POST: `action := classifyAction(...)`; if `action == ""` pass through; else
    `dec, _ := quota.Record(ctx, client, table, sandboxID, action, limits[action])`. If
    `dec.Tripped && dec.OnBreach == quota.BreachBlock` (or `BreachFreeze`) → short-circuit the
    request with a 429 + `Retry-After` body (use goproxy's reject response, like the budget 403 at
    the Bedrock handler). WARN mode: record only, let the request through. Unconfigured action
    (`limits[action]` empty) ⇒ Record returns Tripped:false with no DDB write (dormant).
    (c) **SES MITM:** add `ses.go` registering `MitmConnect` for `sesHostRegex` BEFORE the general
    CONNECT handler (same first-match ordering as bedrock/anthropic/openai handlers, proxy.go
    ~575). The km CA is already system-trusted (userdata `update-ca-certificates`), so `aws sesv2`
    honors it — note this is the medium-confidence path needing live UAT.
    (d) `main.go` (~47): read `KM_QUOTA_TABLE`, `KM_ACTION_LIMITS` (JSON), `KM_SANDBOX_ID` from env;
    if `KM_QUOTA_TABLE` set, JSON-unmarshal `KM_ACTION_LIMITS` into `quota.Limits`, create the DDB
    client (reuse the budget client construction), pass `WithActionQuota(...)`. Absent table ⇒
    option not applied (dormant).
  </action>
  <verify>
    <automated>go build ./sidecars/http-proxy/... && go test ./sidecars/http-proxy/httpproxy/... -count=1</automated>
  </verify>
  <done>Proxy MITMs SES; WithActionQuota records via pkg/quota; BLOCK/FREEZE denies that action; WARN passes through; dormant when KM_QUOTA_TABLE unset.</done>
</task>

<task type="auto">
  <name>Task 3: IAM — quota-table write on the per-sandbox EC2 role</name>
  <files>infra/modules/ec2spot/v1.1.0/main.tf</files>
  <action>
    In `infra/modules/ec2spot/v1.1.0/main.tf` (~lines 348-369, the existing budgets-table DDB
    statement), add `dynamodb:UpdateItem`, `dynamodb:GetItem`, `dynamodb:Query` on the
    `{prefix}-action-quota` table ARN (derive the ARN the same way the budgets table ARN is built —
    reference the table name var/local). If the module has no var for the quota table name, add an
    `action_quota_table_name` input var defaulting to `"${var.resource_prefix}-action-quota"` and a
    matching ARN local. Do not bump the module minor version unless the repo convention requires it
    for an additive IAM statement (mirror how recent additive IAM grants were done — in-place edit
    at v1.1.0 is acceptable per CLAUDE.md Phase 100/109 precedent).
  </action>
  <verify>
    <automated>cd infra/modules/ec2spot/v1.1.0 && terraform fmt -check . && terraform validate || true</automated>
  </verify>
  <done>The per-sandbox EC2 role grants UpdateItem/GetItem/Query on {prefix}-action-quota; module fmt-clean.</done>
</task>

</tasks>

<verification>
- `go test ./sidecars/http-proxy/httpproxy/... -count=1` GREEN (PRX-01/02/03).
- `grep -q 'lambda-url' sidecars/http-proxy/httpproxy/quota_classify.go` (double-count exclusion present).
- `grep -q 'action-quota' infra/modules/ec2spot/v1.1.0/main.tf`.
- Live UAT (deferred to phase gate): SES MITM send through proxy honors the km CA (RESEARCH Risk 2).
</verification>

<success_criteria>
- Proxy counts only GitHub writes + SES sends (never the bridge URL).
- BLOCK/FREEZE denies the action synchronously; WARN observes only; unconfigured = dormant.
- Per-sandbox role can write the quota table.
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-05-SUMMARY.md`
</output>
