---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - pkg/aws/metadata.go
  - pkg/aws/sandbox.go
  - pkg/aws/sandbox_dynamo.go
  - pkg/aws/sandbox_dynamo_test.go
autonomous: true
requirements: [META-01, META-02]

must_haves:
  truths:
    - "The five new attrs (action_limits, action_frozen, frozen_reason, frozen_at, frozen_by) round-trip through marshal → unmarshal → toSandboxMetadata without loss"
    - "marshalSandboxItem emits the new attrs so a full-row PutItem (resume/extend/ttl) does NOT strip them"
    - "FreezeSandboxDynamo atomically latches action_frozen=true + reason/at/by (idempotent, no condition guard)"
    - "UnfreezeSandboxDynamo clears action_frozen + REMOVEs frozen_* (idempotent)"
    - "UpdateSandboxStringAttrDynamo can write action_limits as a resolved JSON string"
  artifacts:
    - path: "pkg/aws/metadata.go"
      provides: "SandboxMetadata ActionLimits/ActionFrozen/FrozenReason/FrozenAt/FrozenBy fields"
      contains: "ActionFrozen"
    - path: "pkg/aws/sandbox_dynamo.go"
      provides: "FreezeSandboxDynamo + UnfreezeSandboxDynamo + round-tripped marshal/unmarshal"
      contains: "FreezeSandboxDynamo"
  key_links:
    - from: "pkg/aws/sandbox_dynamo.go marshalSandboxItem"
      to: "km-sandboxes item map"
      via: "emit action_frozen/frozen_*/action_limits"
      pattern: "action_frozen"
    - from: "pkg/aws/sandbox_dynamo.go toSandboxMetadata"
      to: "SandboxMetadata"
      via: "assign new attrs from sandboxItemDynamo"
      pattern: "ActionFrozen"
---

<objective>
Thread the five new `km-sandboxes` attrs (`action_limits`, `action_frozen`, `frozen_reason`,
`frozen_at`, `frozen_by`) through ALL FOUR round-trip sites so they survive the full-row PutItem
that resume/extend/ttl perform (memory `project_sandboxmetadata_lossy_roundtrip`). Plus the atomic
`FreezeSandboxDynamo` / `UnfreezeSandboxDynamo` writers (mirroring `LockSandboxDynamo` /
`UnlockSandboxDynamo`) that the CLI (plan 10) and auto-freeze (plan 06) call.

Purpose: This is the persistence substrate for the quarantine latch and the bridge-side
`action_limits` delivery. It is independent of `pkg/quota` internals → Wave 1, parallel to 02/03.
Output: New attrs survive marshal→unmarshal (META-01); freeze/unfreeze writers exist.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@pkg/aws/metadata.go
@pkg/aws/sandbox_dynamo.go

<interfaces>
<!-- RESEARCH.md §6 anchors. SandboxMetadata at metadata.go:11. -->
Add to SandboxMetadata (pkg/aws/metadata.go:11):
```go
ActionLimits string     `json:"action_limits,omitempty"`  // resolved JSON limits map
ActionFrozen bool       `json:"action_frozen,omitempty"`
FrozenReason string     `json:"frozen_reason,omitempty"`
FrozenAt     *time.Time `json:"frozen_at,omitempty"`
FrozenBy     string     `json:"frozen_by,omitempty"`      // "auto:{action}:{window}" | "operator:{id}"
```
Add `ActionFrozen bool` to SandboxRecord (pkg/aws/sandbox.go:22) for km list rendering.

Round-trip sites in pkg/aws/sandbox_dynamo.go:
- sandboxItemDynamo struct (~line 47) — new fields
- marshalSandboxItem (~line 334) — emit attrs
- unmarshalSandboxItem / new unmarshalFrozenFields (~line 147) — read attrs
- toSandboxMetadata (~line 69) — assign
- ListAllSandboxesByDynamo (~line 520) + ReadSandboxMetadataDynamo (~line 459) — call the unmarshal helper
New writers (mirror LockSandboxDynamo ~line 644 / UnlockSandboxDynamo ~line 675):
```go
func FreezeSandboxDynamo(ctx, client, tableName, sandboxID, reason, by string) error
func UnfreezeSandboxDynamo(ctx, client, tableName, sandboxID string) error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five attrs to SandboxMetadata/SandboxRecord + thread the round-trip (META-01/02)</name>
  <files>pkg/aws/metadata.go, pkg/aws/sandbox.go, pkg/aws/sandbox_dynamo.go</files>
  <action>
    (a) Add the five fields to `SandboxMetadata` (metadata.go:11) per &lt;interfaces&gt;. Add
    `ActionFrozen bool` to `SandboxRecord` (sandbox.go:22).
    (b) In `pkg/aws/sandbox_dynamo.go`, add the corresponding fields to the `sandboxItemDynamo`
    struct (~line 47) and:
       - `marshalSandboxItem` (~line 334): emit `action_limits` (S, omit if empty), `action_frozen`
         (BOOL, omit if false), `frozen_reason`/`frozen_at`/`frozen_by` (S, omit if empty). Match
         the existing `omitempty`/conditional-attr style used for `locked`/`locked_at`.
       - Add an `unmarshalFrozenFields(item, *SandboxMetadata)` helper (~near line 147) that reads
         all five attrs back; call it from BOTH `ListAllSandboxesByDynamo` (~line 520) and
         `ReadSandboxMetadataDynamo` (~line 459), AND ensure `toSandboxMetadata` (~line 69) assigns
         them from `sandboxItemDynamo`. The footgun is missing ANY read site → silent strip; wire
         all of them.
    Parse `frozen_at` as RFC3339 into `*time.Time`.
  </action>
  <verify>
    <automated>go build ./pkg/aws/...</automated>
  </verify>
  <done>All five attrs are emitted by marshalSandboxItem and read back by every unmarshal/list/read path; SandboxRecord.ActionFrozen exists.</done>
</task>

<task type="auto">
  <name>Task 2: FreezeSandboxDynamo + UnfreezeSandboxDynamo + round-trip tests (META-01/02)</name>
  <files>pkg/aws/sandbox_dynamo.go, pkg/aws/sandbox_dynamo_test.go</files>
  <action>
    (a) Add `FreezeSandboxDynamo(ctx, client, tableName, sandboxID, reason, by string) error` —
    `UpdateItem` with `SET action_frozen = :t, frozen_reason = :reason, frozen_at = :now,
    frozen_by = :by` and `ConditionExpression = attribute_exists(sandbox_id)` (no frozen-state
    guard — idempotent re-freeze updates reason/timestamp). `frozen_at` = `time.Now().UTC()`
    RFC3339. Mirror `LockSandboxDynamo` (~line 644) for client type + error handling.
    (b) Add `UnfreezeSandboxDynamo(ctx, client, tableName, sandboxID string) error` — `UpdateItem`
    with `SET action_frozen = :f REMOVE frozen_reason, frozen_at, frozen_by` and
    `ConditionExpression = attribute_exists(sandbox_id)`. Idempotent (REMOVE of absent attrs is a
    no-op). Mirror `UnlockSandboxDynamo` (~line 675).
    (c) Add to `sandbox_dynamo_test.go`:
       - `TestSandboxMetadataRoundTrip` (META-01): build a SandboxMetadata with all five attrs set,
         `marshalSandboxItem` → `unmarshalSandboxItem`/helper → assert every field survives equal.
       - `TestMarshalFrozen` (META-02): assert the item map produced by marshalSandboxItem contains
         `action_frozen`, `frozen_reason`, `frozen_at`, `frozen_by`, `action_limits` keys when set,
         and OMITS them when unset (no false-zero attrs).
    Reuse the existing test DDB mock style in this package.
  </action>
  <verify>
    <automated>go test ./pkg/aws/... -run 'TestSandboxMetadataRoundTrip|TestMarshalFrozen' -count=1</automated>
  </verify>
  <done>Freeze/Unfreeze writers exist + tested; round-trip and marshal-emission tests GREEN.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/aws/... -run 'TestSandboxMetadataRoundTrip|TestMarshalFrozen' -count=1` GREEN.
- `go test ./pkg/aws/... -count=1` GREEN (no regression in existing sandbox_dynamo tests).
- `grep -q FreezeSandboxDynamo pkg/aws/sandbox_dynamo.go` and `grep -q UnfreezeSandboxDynamo ...`.
</verification>

<success_criteria>
- New attrs survive a full marshal→unmarshal cycle (resume/extend/ttl won't strip them).
- Atomic Freeze/Unfreeze writers ready for the CLI + auto-freeze callers.
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-04-SUMMARY.md`
</output>
