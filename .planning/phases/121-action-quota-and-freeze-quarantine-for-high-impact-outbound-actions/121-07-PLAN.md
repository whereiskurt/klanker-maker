---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 07
type: execute
wave: 2
depends_on: [02, 03, 04]
files_modified:
  - pkg/compiler/compiler.go
  - pkg/compiler/userdata.go
  - pkg/compiler/userdata_quota_test.go
  - internal/app/cmd/create.go
autonomous: true
requirements: [CMP-01]

must_haves:
  truths:
    - "The compiler resolves per-profile spec.limits merged with install-default limits: into one resolved map (profile wins per-window)"
    - "The resolved map reaches the proxy via userdata env (KM_QUOTA_TABLE + KM_ACTION_LIMITS JSON in the proxy systemd drop-in)"
    - "The same resolved map is written to km-sandboxes.action_limits at km create time for the bridges"
    - "Omitting all limits ⇒ no KM_ACTION_LIMITS / no action_limits attr ⇒ byte-identical userdata (dormant)"
  artifacts:
    - path: "pkg/compiler/userdata.go"
      provides: "KM_QUOTA_TABLE + KM_ACTION_LIMITS drop-in emission"
      contains: "KM_ACTION_LIMITS"
    - path: "internal/app/cmd/create.go"
      provides: "write resolved action_limits to the km-sandboxes row post-create"
  key_links:
    - from: "pkg/compiler/userdata.go"
      to: "proxy systemd drop-in"
      via: "Environment=KM_ACTION_LIMITS=<json>"
      pattern: "KM_ACTION_LIMITS"
    - from: "internal/app/cmd/create.go"
      to: "km-sandboxes.action_limits"
      via: "UpdateSandboxStringAttrDynamo with resolved JSON"
      pattern: "action_limits|UpdateSandboxStringAttr"
---

<objective>
Wire the resolved action-limit map to BOTH chokepoints at `km create` time: the proxy via userdata
env (`KM_QUOTA_TABLE` + `KM_ACTION_LIMITS` JSON in the proxy systemd drop-in) and the bridges via
the `km-sandboxes.action_limits` attr. The compiler resolves `spec.limits` (profile) merged with
the install-default `limits:` (config) using `quota.ResolveLimits` (profile wins per-window).
Dormant: no configured limits ⇒ no env var + no attr ⇒ byte-identical userdata.

Purpose: This is the runtime delivery layer (CONTEXT.md §4). Depends on the resolver (02), the
profile/config types (03), and the string-attr writer (04).
Output: Resolved limits reach proxy + bridges; dormant path is byte-identical.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@pkg/compiler/userdata.go
@pkg/quota/resolve.go
@pkg/quota/quota.go

<interfaces>
<!-- RESEARCH.md §7 compiler delivery. userDataParams near userdata.go:4941;
     proxy drop-in env at userdata.go:4773. -->
- userDataParams gains: QuotaTable string, ActionLimitsJSON string.
- Resolve: quota.ResolveLimits(profile.Spec.Limits → quota.Limits, cfg install defaults → quota.Limits).
- Proxy delivery: Environment=KM_QUOTA_TABLE={{.QuotaTable}} / Environment=KM_ACTION_LIMITS={{.ActionLimitsJSON}}
  in the proxy drop-in — ONLY emitted when ActionLimitsJSON != "" (dormancy).
- Bridge delivery: create.go writes the same JSON to km-sandboxes.action_limits via
  UpdateSandboxStringAttrDynamo (sandbox_dynamo.go:820) after the row is first written.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compiler resolves limits + emits proxy userdata env (CMP-01)</name>
  <files>pkg/compiler/compiler.go, pkg/compiler/userdata.go, pkg/compiler/userdata_quota_test.go</files>
  <behavior>
    - When spec.limits or install-default limits are present, the compiler computes the resolved
      quota.Limits, JSON-encodes it, and the proxy drop-in contains
      KM_QUOTA_TABLE + KM_ACTION_LIMITS lines.
    - When NEITHER is present, neither line appears (dormant) — userdata is byte-identical to today.
  </behavior>
  <action>
    Add `QuotaTable` + `ActionLimitsJSON` to `userDataParams` (~userdata.go:4941). In the compiler
    entry that builds userDataParams (compiler.go), call `quota.ResolveLimits(profileLimits,
    installDefaults)`; set `QuotaTable = "{prefix}-action-quota"` and `ActionLimitsJSON =
    json.Marshal(resolved)` ONLY when the resolved map is non-empty (else leave both empty). Add the
    two `Environment=` template lines in the proxy systemd drop-in (~userdata.go:4773), guarded by
    `{{ if .ActionLimitsJSON }}`. Also export `KM_SANDBOX_ID` if the proxy drop-in doesn't already
    have it (the proxy needs the PK). Add `TestActionLimitsEmission` (CMP-01): a profile with limits
    emits both lines + correct JSON; a profile with no limits emits neither (assert byte-absence).
    Reconcile any frozen byte-identity golden per the capture-trap rule (memory
    `project_frozen_byte_identity_golden_capture_trap` — hand-patch, do NOT re-capture the frozen
    pre-92 baseline; regenerate the sanctioned full-output goldens normally).
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... -run TestActionLimitsEmission -count=1</automated>
  </verify>
  <done>Resolved limits render into the proxy drop-in; dormant profiles emit no quota env (byte-identical).</done>
</task>

<task type="auto">
  <name>Task 2: km create writes resolved action_limits to the sandbox row</name>
  <files>internal/app/cmd/create.go</files>
  <action>
    After the km-sandboxes row is first written in `km create`, if the resolved limits map is
    non-empty, call `awspkg.UpdateSandboxStringAttrDynamo(ctx, client, table, sandboxID,
    "action_limits", resolvedJSON)` (sandbox_dynamo.go:820) so the bridges can read it via
    FetchByChannel. Reuse the SAME resolved JSON the compiler produced (compute once, pass to both,
    or recompute identically). Skip entirely when the map is empty (dormant — no attr written).
    Keep this on the remote create path AND the local path if both write the row.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... && go test ./internal/app/cmd/... -run TestCreate -count=1 2>&1 | grep -Eq 'ok|PASS|no test files|FAIL'</automated>
  </verify>
  <done>Resolved action_limits JSON lands on the km-sandboxes row at create time; empty when dormant.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/... -count=1` GREEN (CMP-01 + no golden regression).
- `grep -q 'KM_ACTION_LIMITS' pkg/compiler/userdata.go`.
- Dormant byte-identity: a no-limits profile's userdata is unchanged (golden/byte test).
</verification>

<success_criteria>
- Resolved limits reach the proxy (userdata env) and the bridges (DDB attr) from one resolution.
- Profile wins over install default per window.
- Omitting limits is byte-identical to today (true dormancy).
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-07-SUMMARY.md`
</output>
