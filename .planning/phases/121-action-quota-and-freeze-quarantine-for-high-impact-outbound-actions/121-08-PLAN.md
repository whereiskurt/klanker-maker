---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 08
type: execute
wave: 2
depends_on: [01]
files_modified:
  - infra/modules/dynamodb-action-quota/v1.0.0/main.tf
  - infra/modules/dynamodb-action-quota/v1.0.0/variables.tf
  - infra/modules/dynamodb-action-quota/v1.0.0/outputs.tf
  - infra/live/use1/dynamodb-action-quota/terragrunt.hcl
  - internal/app/cmd/init.go
autonomous: true
requirements: [INIT-01]

must_haves:
  truths:
    - "{prefix}-action-quota DynamoDB table exists with PK/SK, PAY_PER_REQUEST, SSE, TTL on ttl, and DynamoDB Streams enabled (NEW_AND_OLD_IMAGES)"
    - "The module exposes stream_arn as an output for the alerter event-source mapping"
    - "regionalModules() includes dynamodb-action-quota (before ses); the module-count test (already bumped to 26 in plan 01) goes green once both 121 modules land"
    - "A live terragrunt unit exists so km init actually applies the module"
  artifacts:
    - path: "infra/modules/dynamodb-action-quota/v1.0.0/main.tf"
      provides: "action-quota table with Streams + TTL + SSE"
      contains: "stream_enabled"
    - path: "infra/live/use1/dynamodb-action-quota/terragrunt.hcl"
      provides: "live unit so km init applies the table"
    - path: "internal/app/cmd/init.go"
      provides: "regionalModules() entry for dynamodb-action-quota"
      contains: "dynamodb-action-quota"
  key_links:
    - from: "internal/app/cmd/init.go regionalModules()"
      to: "dynamodb-action-quota live unit"
      via: "curated module list entry before ses"
      pattern: "dynamodb-action-quota"
---

<objective>
Create the `{prefix}-action-quota` DynamoDB table — the counter store — with Streams enabled (it
drives the alerter, plan 09). Register it across the FULL deploy surface: TF module + live
terragrunt unit + `regionalModules()` entry. The module-count test was already bumped to 26 in
plan 01; this plan + plan 09 are what make it pass.

Purpose: The table is the substrate. Streams are mandatory (alerter trigger). Deploy-surface
footgun: a module invisible to `km init` is never applied (memory
`project_new_lambda_needs_live_unit_and_init_list`, `project_make_build_precedes_km_init`).
Output: Table module + live unit + init.go registration.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@infra/modules/dynamodb-budget/v1.0.0/main.tf
@infra/modules/dynamodb-slack-channels/v1.0.0/main.tf
@infra/live/use1/dynamodb-slack-channels/terragrunt.hcl
@internal/app/cmd/init.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: dynamodb-action-quota TF module (Streams + TTL + SSE)</name>
  <files>infra/modules/dynamodb-action-quota/v1.0.0/main.tf, infra/modules/dynamodb-action-quota/v1.0.0/variables.tf, infra/modules/dynamodb-action-quota/v1.0.0/outputs.tf</files>
  <action>
    Create the module mirroring `infra/modules/dynamodb-budget/v1.0.0/main.tf` (it already has
    Streams). Table: `hash_key=PK` (S, `{sandbox}#{action}`), `range_key=SK` (S, `lifetime` |
    `hour#{bucket}` | `day#{bucket}`), `billing_mode=PAY_PER_REQUEST`, `ttl{ attribute_name="ttl";
    enabled=true }`, `server_side_encryption{ enabled=true }`, `stream_enabled=true`,
    `stream_view_type="NEW_AND_OLD_IMAGES"`. Tag with the standard module tags. `variables.tf`:
    `table_name` (default `"${var.resource_prefix}-action-quota"` or a plain `table_name` input
    matching the slack-channels module's input style). `outputs.tf`: `table_name`, `table_arn`,
    and **`stream_arn = aws_dynamodb_table.action_quota.stream_arn`** (the alerter needs it).
    Do NOT declare `required_providers` (root.hcl owns the provider generate stanza — memory
    `project_terragrunt_providers_in_root`).
  </action>
  <verify>
    <automated>cd infra/modules/dynamodb-action-quota/v1.0.0 && terraform fmt -check . && terraform validate || true</automated>
  </verify>
  <done>Module creates the table with Streams + TTL + SSE and outputs stream_arn; fmt-clean; no required_providers.</done>
</task>

<task type="auto">
  <name>Task 2: Live terragrunt unit for the table</name>
  <files>infra/live/use1/dynamodb-action-quota/terragrunt.hcl</files>
  <action>
    Copy `infra/live/use1/dynamodb-slack-channels/terragrunt.hcl`; change `terraform.source` to
    `dynamodb-action-quota/v1.0.0`, set the `table_name` input from the resource_prefix the same way
    the slack-channels unit does, and set the state key path. No `dependency` blocks needed (the
    table has no upstream). Ensure `mock_outputs_allowed_terraform_commands` includes "show" if the
    repo convention requires it (memory `project_terragrunt_show_needs_mocks`) — only if this unit
    gains a dependency; it doesn't, so likely N/A.
  </action>
  <verify>
    <automated>test -f infra/live/use1/dynamodb-action-quota/terragrunt.hcl && grep -q 'dynamodb-action-quota' infra/live/use1/dynamodb-action-quota/terragrunt.hcl</automated>
  </verify>
  <done>Live unit references the v1.0.0 module + correct table_name + state key.</done>
</task>

<task type="auto">
  <name>Task 3: Register in regionalModules() (INIT-01)</name>
  <files>internal/app/cmd/init.go</files>
  <action>
    In `regionalModules()` (init.go:511), add a `regionalModule` entry
    `{ name: "dynamodb-action-quota", dir: filepath.Join(regionDir, "dynamodb-action-quota"),
    envReqs: nil }` positioned after the other dynamodb-* entries and BEFORE `"ses"` (no
    upstreamOutputs dependency). This is one of the TWO additions (the other is the alerter, plan
    09) that bring the count from 24 to the 26 the test (bumped in plan 01) expects. After this lands
    WITH plan 09, `TestRunInitPlan_ModuleOrder` passes. Note: `make build` is required before a real
    `km init` so the binary carries this entry (memory `project_make_build_precedes_km_init`) —
    document in the SUMMARY deploy notes.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... && grep -q 'dynamodb-action-quota' internal/app/cmd/init.go</automated>
  </verify>
  <done>regionalModules() includes dynamodb-action-quota before ses.</done>
</task>

</tasks>

<verification>
- `terraform validate` (best-effort) clean for the module.
- `grep -q 'dynamodb-action-quota' internal/app/cmd/init.go` and the live unit exists.
- `go test ./internal/app/cmd/... -run TestRunInitPlan_ModuleOrder -count=1` — passes once plan 09 also lands (count == 26).
</verification>

<success_criteria>
- The action-quota table exists with Streams, TTL, SSE and a stream_arn output.
- Full deploy surface registered (module + live unit + regionalModules) so km init applies it.
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-08-SUMMARY.md`
</output>
