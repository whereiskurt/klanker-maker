---
phase: 121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions
plan: 10
type: execute
wave: 3
depends_on: [04]
files_modified:
  - internal/app/cmd/freeze.go
  - internal/app/cmd/freeze_test.go
  - internal/app/cmd/unlock.go
  - internal/app/cmd/list.go
  - internal/app/cmd/status.go
  - internal/app/cmd/doctor.go
  - internal/app/cmd/root.go
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]

must_haves:
  truths:
    - "km freeze <sandbox> [--reason ...] latches action_frozen=true + frozen_reason/at/by via atomic UpdateItem (FreezeSandboxDynamo)"
    - "km unlock <sandbox> is latch-aware: it clears action_frozen + frozen_* alongside the existing safety-lock, and reports what it cleared"
    - "km list and km status render a FROZEN marker; km doctor surfaces frozen sandboxes + the new {prefix}-action-quota table"
    - "Release is operator-CLI-only (no Slack trigger); the box keeps running (no km stop)"
  artifacts:
    - path: "internal/app/cmd/freeze.go"
      provides: "km freeze verb"
      contains: "FreezeSandboxDynamo"
    - path: "internal/app/cmd/unlock.go"
      provides: "latch-aware unlock clearing action_frozen"
      contains: "UnfreezeSandboxDynamo"
  key_links:
    - from: "internal/app/cmd/freeze.go"
      to: "pkg/aws.FreezeSandboxDynamo"
      via: "atomic UpdateItem"
      pattern: "FreezeSandboxDynamo"
    - from: "internal/app/cmd/unlock.go"
      to: "pkg/aws.UnfreezeSandboxDynamo"
      via: "clear after safety-lock clear"
      pattern: "UnfreezeSandboxDynamo"
---

<objective>
Add the operator control surface for the quarantine latch: a new `km freeze <sandbox> [--reason …]`
panic-button verb, a latch-aware `km unlock` that clears the freeze alongside the existing
safety-lock (per the user's explicit `./km unlock` request), and FROZEN visibility in
`km list` / `km status` / `km doctor`. Release is CLI-only — deliberately off the Slack/GitHub
attack surface (CONTEXT.md §7/§8 decision 8). The box keeps running; no `km stop`.

Purpose: The asymmetric-control half of quarantine — easy to trigger, releasable only by an
AWS-authenticated operator. Depends on the Freeze/Unfreeze DDB writers (plan 04).
Output: km freeze / latch-aware km unlock / FROZEN rendering / doctor surfacing.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/CONTEXT.md
@.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-RESEARCH.md
@internal/app/cmd/lock.go
@internal/app/cmd/unlock.go
@internal/app/cmd/list.go
@pkg/aws/sandbox_dynamo.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: km freeze verb + latch-aware km unlock (CLI-01/02)</name>
  <files>internal/app/cmd/freeze.go, internal/app/cmd/freeze_test.go, internal/app/cmd/unlock.go, internal/app/cmd/root.go</files>
  <behavior>
    - km freeze <sandbox> [--reason ...] → FreezeSandboxDynamo(..., reason, by="operator:<sandbox>")
      → prints "🛑 Frozen <id> (reason). Release with: km unlock <id>". Idempotent.
    - km unlock <sandbox>: after the existing UnlockSandboxDynamo safety-lock clear, also call
      UnfreezeSandboxDynamo and report whether a freeze was cleared. If only frozen (not
      safety-locked), still clears the freeze. --yes confirm preserved.
  </behavior>
  <action>
    (a) Create `internal/app/cmd/freeze.go` mirroring `lock.go` structure: resolve the sandbox ref,
    load the DDB client, call `awspkg.FreezeSandboxDynamo` (plan 04) with `--reason` (default
    "operator freeze") and `by="operator:<sandboxID>"`. Register the command in `root.go`.
    (b) Extend `runUnlock` (unlock.go:63): after the safety-lock clear, call
    `awspkg.UnfreezeSandboxDynamo` (plan 04). Report both outcomes ("cleared safety lock", "cleared
    action freeze") — handle the case where the sandbox was frozen but not safety-locked (don't
    fail the whole command if the safety-lock clear no-ops). Box is never stopped → actions resume
    immediately; print "actions resume immediately (no km resume needed)".
    (c) Fill in plan 01's `TestRunFreeze` (CLI-01: freeze writes action_frozen=true + frozen_*) and
    `TestRunUnlockLatchAware` (CLI-02: unlock clears action_frozen alongside the safety-lock). Mock
    the DDB client like the existing lock/unlock tests.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestRunFreeze|TestRunUnlockLatchAware' -count=1</automated>
  </verify>
  <done>km freeze latches; km unlock clears both lock + freeze and reports what it cleared; box keeps running.</done>
</task>

<task type="auto">
  <name>Task 2: FROZEN marker in km list / km status + doctor surfacing (CLI-03)</name>
  <files>internal/app/cmd/list.go, internal/app/cmd/status.go, internal/app/cmd/doctor.go</files>
  <action>
    (a) `km list` (list.go:545 `statusDisplay` + the row render loop): when `meta.ActionFrozen`
    (now on SandboxRecord per plan 04), render a `FROZEN` marker (e.g. overlay `🧊 FROZEN` or a
    flag column). Keep it visible in both narrow and `--wide`.
    (b) `km status`: add a FROZEN section showing `frozen_reason`, `frozen_at`, `frozen_by` when
    `ActionFrozen` is true; show "not frozen" otherwise (or omit when absent — match existing
    status section style).
    (c) `km doctor`: add a check that scans `km-sandboxes` for `action_frozen=true` and surfaces
    them as WARN (id + reason + duration), AND verifies `{prefix}-action-quota` table existence
    (table-health check like the other table checks). Use the existing doctor check-registration
    pattern.
    (d) Add at least one assertion test (CLI-03): `TestList...Frozen` or `TestDoctor...Frozen`
    asserting the FROZEN marker / doctor WARN appears for a frozen row. (The
    `-run 'TestList.*Frozen|TestDoctor.*Frozen'` target must resolve.)
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestList.*Frozen|TestDoctor.*Frozen' -count=1</automated>
  </verify>
  <done>km list/status show FROZEN; km doctor WARNs on frozen sandboxes + checks the action-quota table.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run 'TestRunFreeze|TestRunUnlockLatchAware|TestList.*Frozen|TestDoctor.*Frozen' -count=1` GREEN.
- `go build ./...` clean; `make build` (ldflags) succeeds.
- `grep -q FreezeSandboxDynamo internal/app/cmd/freeze.go` and `grep -q UnfreezeSandboxDynamo internal/app/cmd/unlock.go`.
- Live UAT (phase gate): km freeze then km unlock clears both flags; Slack dispatch resumes.
</verification>

<success_criteria>
- km freeze is the operator panic button; km unlock is the only release (latch-aware).
- No Slack trigger; box keeps running.
- FROZEN is visible in list/status; doctor surfaces frozen sandboxes + the new table.
</success_criteria>

<output>
After completion, create `.planning/phases/121-action-quota-and-freeze-quarantine-for-high-impact-outbound-actions/121-10-SUMMARY.md`
</output>
