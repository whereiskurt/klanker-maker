---
phase: 91-slack-inbound-mention-only-mode-for-shared-and-override-channels-polite-bot
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/profile/profile_cli_mention_test.go
  - pkg/compiler/userdata_mention_test.go
  - pkg/slack/bridge/events_handler_test.go
  - cmd/km-slack-bridge/main_mention_test.go
  - internal/app/cmd/slack_mention_init_test.go
  - internal/app/cmd/doctor_slack_bot_user_id_test.go
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-04
  - POL-06
  - POL-07
  - POL-08
  - POL-09
  - POL-10
  - POL-11
  - POL-12

must_haves:
  truths:
    - "Every POL-XX requirement that has a `go test ... -run` command in 91-VALIDATION.md has a stub test function in this commit"
    - "All stub test files compile cleanly under `go vet ./...`"
    - "All stub tests are RED — either `t.Skip(\"TODO: implement in Wave N\")` or `t.Fatal(\"not implemented yet — Phase 91 Plan NN\")` so subsequent waves have a clear RED→GREEN signal"
    - "No production code is modified in this commit — only `_test.go` files added/extended"
    - "`go test ./pkg/slack/bridge/... ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... ./cmd/km-slack-bridge/... -run TestMention -v` succeeds in finding the stubs (PASS via t.Skip OR FAIL via t.Fatal — both prove discovery)"
  artifacts:
    - path: "pkg/profile/profile_cli_mention_test.go"
      provides: "Stub TestCLISpec_NotifySlackInboundMentionOnly + TestSchema_NotifySlackInboundMentionOnly (POL-01, POL-02)"
    - path: "pkg/compiler/userdata_mention_test.go"
      provides: "Stub TestResolveMentionOnly (9-case table-driven) + TestMentionOnlyCompiler (POL-04, POL-11)"
    - path: "pkg/slack/bridge/events_handler_test.go"
      provides: "Extended with stub TestEventsHandler_MentionOnly table (7+ cases) for POL-06, POL-12 — must remain a single file (existing 961-line file extended; do NOT create sibling)"
    - path: "cmd/km-slack-bridge/main_mention_test.go"
      provides: "Stub TestWireEventsHandler_BotUserIDPrime (POL-09)"
    - path: "internal/app/cmd/slack_mention_init_test.go"
      provides: "Stub TestRunSlackInit_BotUserIDCached + TestRotateToken_BotUserIDCached (POL-07, POL-08)"
    - path: "internal/app/cmd/doctor_slack_bot_user_id_test.go"
      provides: "Stub TestCheckSlackBotUserIDCached (POL-10)"
  key_links:
    - from: "pkg/compiler/userdata_mention_test.go"
      to: "pkg/compiler/userdata.go"
      via: "calls resolveMentionOnly() helper (does not exist yet — Plan 02 implements)"
      pattern: "resolveMentionOnly"
    - from: "pkg/slack/bridge/events_handler_test.go"
      to: "pkg/slack/bridge/events_handler.go"
      via: "constructs EventsHandler with MentionOnly bool field (field does not exist yet — Plan 03 adds)"
      pattern: "MentionOnly"
    - from: "internal/app/cmd/slack_mention_init_test.go"
      to: "internal/app/cmd/slack.go + pkg/slack/client.go"
      via: "calls AuthTestWithUserID() method on SlackInitAPI (does not exist yet — Plan 04 implements)"
      pattern: "AuthTestWithUserID"
    - from: "internal/app/cmd/doctor_slack_bot_user_id_test.go"
      to: "internal/app/cmd/doctor_slack_transcript.go"
      via: "calls checkSlackBotUserIDCached (does not exist yet — Plan 05 implements)"
      pattern: "checkSlackBotUserIDCached"
---

<objective>
Wave 0 stub seeding for Phase 91. Create six test stub files (five new, one extended) so subsequent
waves have a clear RED→GREEN signal for every POL-XX requirement that has an `<automated>` command
in 91-VALIDATION.md.

Purpose: Lock the validation contract before any implementation lands. Phase 91 is a tightly-scoped
surgical change across five layers (schema, compiler, bridge handler, SSM caching, doctor) and the
validation map in 91-VALIDATION.md identifies all ❌ Wave 0 gaps that must be closed first.

Output: Six `_test.go` files compiling cleanly under `go vet`, with `t.Skip` or `t.Fatal` stubs that
expose every Phase 91 behaviour Wave 1+ will turn green. Zero production code changes.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/91-slack-inbound-mention-only-mode-for-shared-and-override-channels-polite-bot/91-CONTEXT.md
@.planning/phases/91-slack-inbound-mention-only-mode-for-shared-and-override-channels-polite-bot/91-RESEARCH.md
@.planning/phases/91-slack-inbound-mention-only-mode-for-shared-and-override-channels-polite-bot/91-VALIDATION.md
@pkg/slack/bridge/events_handler_test.go
@pkg/profile/types.go
@internal/app/cmd/doctor_slack_transcript.go

<interfaces>
<!-- Existing test scaffolding executors should re-use (do NOT re-implement). -->

From pkg/slack/bridge/events_handler_test.go (line 217+):
```go
func newHandler(now time.Time) (*EventsHandler, *fakeSQS, *fakeThreads, *fakeNonces, *fakeSandboxes, *fakePauseHinter, *fakeReactor) {
    // ... existing factory; EventsHandler is constructed with BotUserID: &fakeBotUserID{uid: "UBOT123"} ...
}

type fakeBotUserID struct {
    uid string
    err error
}
func (f *fakeBotUserID) Fetch(ctx context.Context) (string, error) { return f.uid, f.err }
```

Wave 0 must EXTEND the existing test file by adding the TestEventsHandler_MentionOnly function
to it (DO NOT create a sibling file — the 961-line `events_handler_test.go` is the single home
for events handler tests; sibling files would split the fakes). The fakes (fakeSigningSecret,
fakeBotUserID, fakeSQS, fakeThreads, fakeNonces, fakeSandboxes) already exist and must be reused.

From pkg/profile/types.go (existing pattern for *bool tri-state):
```go
NotifyEmailEnabled    *bool  `yaml:"notifyEmailEnabled,omitempty"`
NotifySlackEnabled    *bool  `yaml:"notifySlackEnabled,omitempty"`
UseSlackConnect       *bool  `yaml:"useSlackConnect,omitempty" json:"useSlackConnect,omitempty"`
SlackArchiveOnDestroy *bool  `yaml:"slackArchiveOnDestroy,omitempty"`
```

From internal/app/cmd/doctor_slack_transcript.go (existing pattern for CheckResult-returning Slack doctor checks):
```go
func checkSlackUsersReadEmailScope(
    ctx context.Context,
    getScopes func(context.Context) ([]string, error),
) CheckResult {
    name := "Slack users:read.email scope"
    if getScopes == nil { return CheckResult{ Name: name, Status: CheckSkipped, ... } }
    // ... OK / WARN paths ...
}
```

CheckResult has fields: Name, Status, Message, Remediation. Status values: CheckOK, CheckWarn, CheckSkipped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pkg/profile + pkg/compiler stub test files</name>
  <files>pkg/profile/profile_cli_mention_test.go, pkg/compiler/userdata_mention_test.go</files>
  <action>
    Create `pkg/profile/profile_cli_mention_test.go`:
    - `package profile`
    - `import "testing"`
    - Function `TestCLISpec_NotifySlackInboundMentionOnly(t *testing.T)`: `t.Skip("TODO Plan 91-01: implement after CLISpec.NotifySlackInboundMentionOnly *bool field is added — round-trip parse from YAML + JSON, assert nil/&true/&false distinguishable")`
    - Function `TestSchema_NotifySlackInboundMentionOnly(t *testing.T)`: `t.Skip("TODO Plan 91-01: implement after sandbox_profile.schema.json gains notifySlackInboundMentionOnly — assert valid bool accepted, non-bool rejected")`

    Create `pkg/compiler/userdata_mention_test.go`:
    - `package compiler`
    - `import "testing"`
    - Function `TestResolveMentionOnly(t *testing.T)`: 9-case table-driven stub. Use `t.Skip("TODO Plan 91-02: implement once resolveMentionOnly(*profile.CLISpec) bool helper exists")`. Add a comment block listing the 9 cases (Mode 1/2/3 × override nil/&true/&false) per RESEARCH.md Pattern 4 + Q5.
    - Function `TestMentionOnlyCompiler(t *testing.T)`: `t.Skip("TODO Plan 91-02: implement once compiler emits KM_SLACK_MENTION_ONLY to bridge config — assert env value matches resolveMentionOnly() output for each profile fixture")`

    Both files must compile (`go vet ./pkg/profile/... ./pkg/compiler/...` must pass). Stubs only — no real assertions yet. Do NOT touch types.go or userdata.go in this task.
  </action>
  <verify>
    <automated>go vet ./pkg/profile/... ./pkg/compiler/... && go test ./pkg/profile/... ./pkg/compiler/... -run 'TestCLISpec_NotifySlackInboundMentionOnly|TestSchema_NotifySlackInboundMentionOnly|TestResolveMentionOnly|TestMentionOnlyCompiler' -v</automated>
  </verify>
  <done>Two files exist, compile, and `go test -run ...` discovers and runs the four stub functions (each reports SKIP, exit 0).</done>
</task>

<task type="auto">
  <name>Task 2: Extend bridge events handler tests + add bridge main wiring stub</name>
  <files>pkg/slack/bridge/events_handler_test.go, cmd/km-slack-bridge/main_mention_test.go</files>
  <action>
    EXTEND `pkg/slack/bridge/events_handler_test.go` (the existing 961-line file — do NOT create a sibling):
    - Add `TestEventsHandler_MentionOnly(t *testing.T)` near the end of the file.
    - Body is a table-driven stub with all 7 cases from RESEARCH.md Q6 enumerated in the `tests` slice as inline struct literals (so Plan 03 only fills in assertions), but the test loop itself does `t.Skip("TODO Plan 91-03: implement once EventsHandler has MentionOnly field and Handle() inserts step 4b mention-scan")`.
    - The 7 stub cases must include names: "mention-only=false, no @mention → dispatched", "mention-only=true, has @mention → dispatched", "mention-only=true, no @mention → skipped", "mention-only=true, different @mention → skipped", "mention-only=true, mention at start → dispatched", "mention-only=true, mention at end → dispatched", "mention-only=true, bot_uid fetch error → fail-open".
    - Reuse the existing `newHandler(now)` factory and `fakeBotUserID` fake — do NOT duplicate them.

    Create `cmd/km-slack-bridge/main_mention_test.go`:
    - `package main`
    - `import "testing"`
    - Function `TestWireEventsHandler_BotUserIDPrime(t *testing.T)`: `t.Skip("TODO Plan 91-03: implement once wireEventsHandler reads KM_SLACK_MENTION_ONLY and primes CachedBotUserIDFetcher from KM_SLACK_BOT_USER_ID — assert MentionOnly field set, PrimeCache called when env var non-empty")`.

    Both files must compile. The bridge package's existing tests must still pass (we are extending, not replacing).
  </action>
  <verify>
    <automated>go vet ./pkg/slack/bridge/... ./cmd/km-slack-bridge/... && go test ./pkg/slack/bridge/... -run TestEventsHandler -v && go test ./cmd/km-slack-bridge/... -run TestWireEventsHandler_BotUserIDPrime -v</automated>
  </verify>
  <done>Existing events_handler_test.go tests still pass; new TestEventsHandler_MentionOnly reports SKIP; new main_mention_test.go file compiles and TestWireEventsHandler_BotUserIDPrime reports SKIP.</done>
</task>

<task type="auto">
  <name>Task 3: Create internal/app/cmd stub test files</name>
  <files>internal/app/cmd/slack_mention_init_test.go, internal/app/cmd/doctor_slack_bot_user_id_test.go</files>
  <action>
    Create `internal/app/cmd/slack_mention_init_test.go`:
    - `package cmd`
    - `import "testing"`
    - Function `TestRunSlackInit_BotUserIDCached(t *testing.T)`: `t.Skip("TODO Plan 91-04: implement once RunSlackInit calls AuthTestWithUserID and writes {prefix}slack/bot-user-id — fake SSMStore captures the Put call, fake SlackInitAPI returns UID")`.
    - Function `TestRotateToken_BotUserIDCached(t *testing.T)`: `t.Skip("TODO Plan 91-04: implement once RunSlackRotateToken re-caches bot_user_id — fake SSMStore captures the Put call after AuthTestWithUserID returns")`.

    Create `internal/app/cmd/doctor_slack_bot_user_id_test.go`:
    - `package cmd`
    - `import "testing"`
    - Function `TestCheckSlackBotUserIDCached(t *testing.T)`: `t.Skip("TODO Plan 91-05: implement once checkSlackBotUserIDCached exists — table-driven: getUID=nil → SKIPPED, getUID returns \"\" → WARN, getUID returns \"UBOT\" → OK, getUID errors → WARN")`.

    Both files must compile. Reuse existing fakes from `internal/app/cmd/slack_test.go` and `internal/app/cmd/doctor_slack_users_email_test.go` patterns when Plan 04/05 fills them in (Wave 0 only needs the stubs).
  </action>
  <verify>
    <automated>go vet ./internal/app/cmd/... && go test ./internal/app/cmd/... -run 'TestRunSlackInit_BotUserIDCached|TestRotateToken_BotUserIDCached|TestCheckSlackBotUserIDCached' -v</automated>
  </verify>
  <done>Both files exist, compile, and the three stub functions report SKIP. Existing internal/app/cmd tests still pass.</done>
</task>

</tasks>

<verification>
- All 6 stub files exist on disk and compile under `go vet ./...`
- Quick sample command from 91-VALIDATION.md succeeds in discovering the stubs:
  `go test ./pkg/slack/bridge/... ./pkg/compiler/... ./pkg/profile/... ./internal/app/cmd/... ./cmd/km-slack-bridge/... -run 'TestMention|TestCLISpec_NotifySlackInbound|TestSchema_NotifySlackInbound|TestResolveMentionOnly|TestRunSlackInit_BotUserIDCached|TestRotateToken_BotUserIDCached|TestCheckSlackBotUserIDCached|TestWireEventsHandler_BotUserIDPrime|TestEventsHandler_MentionOnly' -v`
- No production source file is modified (`git diff --name-only` shows only `_test.go` files + this PLAN's directory)
</verification>

<success_criteria>
- All 6 stub files added (one extended), compile, and test functions are discoverable
- Every POL-XX with an `<automated>` command in 91-VALIDATION.md has a corresponding stub
- Subsequent waves can flip stubs RED (replace `t.Skip` with real assertions or remove it) to drive
  implementation
</success_criteria>

<output>
After completion, create `.planning/phases/91-slack-inbound-mention-only-mode-for-shared-and-override-channels-polite-bot/91-00-SUMMARY.md`
</output>
