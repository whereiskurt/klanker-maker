---
phase: 84.1
slug: ses-upgrade-safety-gap-closure
status: draft
nyquist_compliant: true
wave_0_complete: inherited
created: 2026-05-16
inherits_from: ../84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-VALIDATION.md
---

# Phase 84.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Inherits Phase 84's validation strategy** (`../84-ses-per-install-rule-namespacing-via-operator-address-prefix/84-VALIDATION.md`).
> This document adds the 84.1-specific deltas required to verify the 8 GAPs + 3 DRIFT items.

---

## Inheritance

Phase 84.1 reuses the entire test infrastructure and sampling cadence from Phase 84:

| Property | Inherited from Phase 84 |
|----------|-------------------------|
| Framework | Go standard `testing` + `aws-sdk-go-v2` narrow-interface mocks |
| Config file | `go.mod` (existing) |
| Quick run command | `go test ./internal/app/cmd/... ./pkg/terragrunt/... -short -count=1` |
| Full suite | `go test ./... -count=1` |
| Sampling cadence | After every task commit (quick) · after every wave (full) · before UAT (full + terragrunt validate) |

The narrow-interface mock pattern (`mockSESReceiptRuleAPI` style, see `internal/app/cmd/doctor_ses_rules_test.go`) is reused for the four new narrow interfaces introduced in 84.1 (`S3StateReader`, `LockDigestReader`, `FoundationStateReader`, and the expanded `SESIdentityLister` signature).

---

## 84.1-Specific Deltas

### Wave 0 Inheritance

Phase 84.1 does NOT introduce a Wave 0. The 11 Phase 84 Wave 0 stubs (`84-VALIDATION.md` W0-01..W0-11) all stay green throughout 84.1's execution because no 84.1 plan modifies the symbols those stubs cover (`deriveOperatorEmail`, `email-create-handler` recipient verification, etc.).

If any Wave 0 stub goes red during 84.1 execution, that is a regression — STOP and audit the offending plan.

### 84.1 Per-Task Verification Map

| Plan | Task | Wave | Requirement | Test Type | Automated Command |
|------|------|------|-------------|-----------|-------------------|
| 84.1-01 | T1 | 1 | GAP-1, GAP-7 | unit (TDD) | `go test ./internal/app/cmd/ -run "TestExportTerragruntEnvVars" -count=1` |
| 84.1-01 | T2 | 1 | GAP-1, GAP-7 | unit + grep gate | `go test ./internal/app/cmd/ -run "TestRunBootstrapSharedSES_Exports" -count=1 && test "$(grep -rn ExportConfigEnvVars internal/ cmd/ pkg/ 2>/dev/null \| wc -l)" -eq 0` |
| 84.1-02 | T1 | 2 (was 1; C1 fix) | GAP-4, GAP-5 | unit (TDD) | `go test ./pkg/terragrunt/... -run "TestRunner_Apply_ContextTimeout\|TestRunner_Apply_ContextCancellation\|TestRunner_QuietMode_EmitsHeartbeat\|TestRunner_VerboseMode_NoHeartbeat" -count=1` |
| 84.1-02 | T2 | 2 | GAP-4, GAP-5 | unit | `go test ./internal/app/cmd/ -run "TestRunInitWithRunner_PerModuleTimeout\|TestRunInitWithRunner_TimeoutError\|TestRunInitWithRunner_FastApply" -count=1` |
| 84.1-02 | T3 | 2 | GAP-4, GAP-5 (H6 expansion) | unit | `go test ./internal/app/cmd/ -run "TestDefaultApplyTerragrunt_HonorsContextTimeout\|TestRunBootstrapSharedSES_HonorsBootstrapTimeout" -count=1` |
| 84.1-03 | T1 | 1 | GAP-8 | unit (TDD) | `go test ./internal/app/cmd/ -run "TestCheckStateLockDigest\|TestParseLockID" -count=1` |
| 84.1-03 | T2 | 1 | GAP-8 | unit | `go test ./internal/app/cmd/ -run "TestRunDoctor_IncludesStateLockDigestCheck\|TestCheckStateLockDigest" -count=1` |
| 84.1-04 | T1 | 2 | GAP-2, GAP-3 | unit (TDD) | `go test ./internal/app/cmd/ -run "TestDetectSharedSESState_FoundationState\|TestDefaultSESPreflight_NilStateReader" -count=1` |
| 84.1-04 | T2 | 2 | GAP-6 (highest impact) | grep gate + terraform validate | `make test-phase-84-1-import-blocks && make test-phase-84-1-removed-blocks && make test-phase-84-1-terraform-validate` |
| 84.1-05 | T1 | 3 | DRIFT-A | grep | `grep -q "State-digest mismatch recovery" OPERATOR-GUIDE.md && grep -q "Phase 84.1: Upgrade-safety gap closure" CLAUDE.md` |
| 84.1-05 | T2 | 3 | UAT closures (a)/(c)/(d) + DRIFT-B/C | operator UAT | manual — see Plan 84.1-05 Task 2 `<how-to-verify>` 9-step procedure |
| 84.1-05 | T3 | 3 | all gaps + drift | grep gate | `head -5 .planning/phases/84.1-…/84.1-05-UAT.md \| grep -qE "^status: (passed\|diagnosed\|aborted)"` |

### Manual-Only Verifications (84.1-Specific)

These complement Phase 84's manual-only list. All operator-driven; covered by Plan 84.1-05 UAT.

| Behavior | Requirement | Test Instructions (UAT step) |
|----------|-------------|------------------------------|
| In-place v1.0.0 → v2.0.0 cutover destroys ZERO shared AWS resources | GAP-6 | Plan 84.1-05 Task 2 UAT Step 2: `./km init --dry-run=true \| tee /tmp/init-dryrun.log && grep -E "destroy.*aws_ses_(domain\|receipt_rule_set\|active_receipt_rule_set)" /tmp/init-dryrun.log` → must return zero lines |
| `km bootstrap --shared-ses` re-run is a true no-op | GAP-2 | Plan 84.1-05 Task 2 UAT Step 3: invoke twice; second invocation should plan zero changes |
| State-digest recovery runbook in OPERATOR-GUIDE.md matches Plan 84.1-03 Remediation field exactly | GAP-8 + DRIFT-A | Plan 84.1-05 Task 1: side-by-side diff of doctor.go's Remediation string against the OPERATOR-GUIDE.md procedure |
| `km doctor` reports orphan SES rules WARN as documented | (covered by Phase 84) | re-verify after 84.1 fixes — should not regress |
| Foundation `import {}` blocks succeed for the 6 non-DKIM resources on in-place upgrade | GAP-3, GAP-6 | Plan 84.1-05 Task 2 UAT Step 2: bootstrap dry-run shows IMPORT verbs for the 6 blocks |
| DKIM record operator-run `terraform import` runbook works | GAP-6 (H9 follow-up) | Plan 84.1-05 Task 1 (OPERATOR-GUIDE.md runbook) + UAT Step 2 (operator runs the runbook live) |
| `km uninit` on the km2 prefix leaves shared resources + km install rules intact | (CLAUDE.md teardown_layers) | Plan 84.1-05 Task 2 UAT Step 6 (M12 fix) |

### Nyquist Sign-Off Checklist

- [x] All non-UAT tasks have `<automated>` verify commands (no MISSING markers)
- [x] No 3 consecutive tasks without automated verify
- [x] Wave 0 inherited from Phase 84 (no new W0 stubs needed — 84.1 only adds checks/wiring around existing surfaces)
- [x] No watch-mode flags
- [x] Feedback latency: quick path < 30s, full suite < 5min, Makefile gates < 10s
- [x] terraform-validate gate (C4 from plan-checker rev 1) catches HCL/import-ID typos before UAT
- [x] Operator UAT (Plan 84.1-05) is the ONLY non-autonomous task — mirrors Phase 84-10 pattern

**Approval:** approved for execution (post plan-checker revision 1)
