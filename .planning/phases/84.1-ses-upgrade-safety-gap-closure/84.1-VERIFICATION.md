---
phase: 84.1-ses-upgrade-safety-gap-closure
verified: 2026-05-17T00:00:00Z
status: passed
score: 11/11 requirement IDs code-complete (3 deferred empirical UATs explicitly tracked to Phase 84.2)
re_verification: false
mode: initial
deferred_empirical_items:
  - id: GAP-6-empirical
    description: "v1.0.0 → v2.0.0 removed{} suppression on a fresh Phase 82.x install (live destroy-blocking observation)"
    code_complete: true
    why_deferred: "No Phase 82.x snapshot in the verifying environment; operator chose minimal UAT scope per Phase 84.1 UAT decision."
    tracked_in: "STATE.md Roadmap Evolution → Phase 84.2"
  - id: UAT-closure-a
    description: "Second-install dry-run (Phase 84 UAT closure (a))"
    code_complete: true
    why_deferred: "Operationally heavy (two-install setup). Deferred per operator decision."
    tracked_in: "Phase 84.2"
  - id: UAT-closure-c
    description: "Sibling destroy isolation via km uninit (Phase 84 UAT closure (c))"
    code_complete: true
    why_deferred: "Requires two-install topology + destructive sequence."
    tracked_in: "Phase 84.2"
  - id: UAT-closure-d
    description: "Bootstrap idempotent re-run (Phase 84 UAT closure (d))"
    code_complete: true
    why_deferred: "Operator chose minimal UAT scope."
    tracked_in: "Phase 84.2"
  - id: DRIFT-B-empirical
    description: "km configure --reset-prefix → re-derive operator_email loop exercise"
    code_complete: true
    why_deferred: "Not exercised in Phase 84.1 UAT."
    tracked_in: "Phase 84.2"
  - id: DRIFT-C-empirical
    description: "Destructive InitSESPreflight block-until-rule-set-exists exercise"
    code_complete: true
    why_deferred: "Destructive scenario, deferred."
    tracked_in: "Phase 84.2"
  - id: GAP-3-bootstrap-dry-run-anomaly
    description: "km bootstrap --shared-ses --dry-run=true reported 'creating' for shared rule set + domain identity on an account that may already own them. Code wiring is verified (s3FoundationStateReader constructed in runBootstrapSharedSES) — empirical question is whether StateOwns returns true against current foundation state."
    code_complete: true
    why_deferred: "Investigation deferred per Phase 84.1 UAT § Observations. Suggested probe: terragrunt state list against infra/live/use1/ses-shared-rule-set."
    tracked_in: "Phase 84.2"
---

# Phase 84.1: SES Upgrade Safety Gap Closure — Verification Report

**Phase Goal (from ROADMAP.md):**
Close the 8 gaps diagnosed by Phase 84's UAT so an operator can upgrade an existing Phase 82.x install to Phase 84 without manual recovery: (1) `km bootstrap` exports the full terragrunt env var set, (2) `km bootstrap` is idempotent across re-runs, (3) foundation safely takes ownership of pre-existing v1.0.0 resources without data loss during regional cutover, (4) wedged `terragrunt apply` surfaces as a timeout error instead of hanging silently, (5) state-digest mismatch recovery is documented and/or detected by `km doctor`.

**Verified:** 2026-05-17
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                       |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1  | `km bootstrap` exports the full terragrunt env var set (KM_ROUTE53_ZONE_ID, KM_ARTIFACTS_BUCKET, KM_REGION_LABEL)                                                                                                                    | ✓ VERIFIED | `ExportTerragruntEnvVars` at init.go:698 exports all three; called from bootstrap.go:341 and 9 other call sites                                                                                                                |
| 2  | `km bootstrap --shared-ses` re-run is a true no-op (idempotent — auto-detect plus state-precedence prevents re-create)                                                                                                              | ✓ VERIFIED (code) / ⚠ DEFERRED (empirical) | `detectSharedSESState` checks state ownership first (bootstrap.go:146-148); foundation main.tf has 6 import{} blocks. Live re-run NOT exercised in Phase 84.1 UAT (Step 3 deferred to 84.2).                                  |
| 3  | Foundation safely takes ownership of pre-existing v1.0.0 resources without data loss during regional cutover                                                                                                                          | ✓ VERIFIED (code) / ⚠ DEFERRED (empirical) | 6 import{} blocks in ses-shared-rule-set/v1.0.0/main.tf, 7 removed{} blocks with destroy=false in ses/v2.0.0/main.tf; `terraform validate` PASS; live cutover not exercised in 84.1 UAT (no Phase 82.x state available).      |
| 4  | Wedged `terragrunt apply` surfaces as a timeout error instead of hanging silently                                                                                                                                                   | ✓ VERIFIED | runner.go has bounded ctx → DeadlineExceeded (line 254), cmd.Cancel (line 341), WaitDelay (line 342), heartbeat (lines 25-243). init.go:835 wraps Apply in context.WithTimeout. bootstrap.go:626 wraps in BootstrapApplyTimeout (10min). |
| 5  | State-digest mismatch recovery is documented and detected by `km doctor`                                                                                                                                                              | ✓ VERIFIED | `checkStateLockDigest` at doctor.go:3338; uses NewScanPaginator (line 3347); parseLockID (line 3433); wired into buildChecks at doctor.go:2584; OPERATOR-GUIDE.md § "State-digest mismatch recovery" at line 724.              |
| 6  | Re-running the UAT closures (a)/(c)/(d) that were skipped in Phase 84 all pass                                                                                                                                                       | ⚠ DEFERRED  | Per Phase 84.1 UAT § Must-haves status: all three explicitly DEFERRED to Phase 84.2. Code surface for each is complete; the empirical re-run was descoped by operator decision.                                                |

**Score:** 5/6 truths fully verified, 1/6 deferred (Truth 6 — UAT closures (a)/(c)/(d) were not re-run; they are tracked in Phase 84.2 per STATE.md Roadmap Evolution).

For Truths 2 and 3, the **code-correctness must-have is fully verified** (structural verification via `terraform validate` + unit tests + Makefile gates). The **empirical "in-anger" must-have** (a real Phase 82.x install upgraded live with zero destroys observed) is intentionally deferred to Phase 84.2 with the new `km init --plan` flag (already spec'd in commit `78241a8`, `docs/superpowers/specs/2026-05-16-km-init-plan-flag-and-destroy-class-gate-design.md`).

This verification scores `passed` rather than `gaps_found` because:
1. All 11 declared requirement IDs (GAP-1..GAP-8 + DRIFT-A/B/C) are code-complete.
2. The deferred items are explicitly documented in 84.1-05-UAT.md and STATE.md, not silently skipped.
3. Phase 84.1's CHARTER said "close the 8 gaps" — the code that closes them is shipped and merged on `main`. Live re-run of the Phase 84 UAT closures was an OUTCOME of that work, but per Phase 84.1 UAT operator decision (§ Result), it was reduced to the structural surface and the empirical surface was descoped to Phase 84.2.

---

## Required Artifacts

| Artifact                                                            | Expected                                                                                                                       | Status     | Details                                                                                                              |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------- |
| `internal/app/cmd/init.go::ExportTerragruntEnvVars`                 | Exports KM_ROUTE53_ZONE_ID + KM_ARTIFACTS_BUCKET + KM_REGION_LABEL                                                             | ✓ VERIFIED | Lines 698-714 — exports all 3 plus prior set                                                                          |
| 8+ call sites of `ExportTerragruntEnvVars`                          | bootstrap.go, init.go, budget.go, cluster.go (×2), create.go (×2), destroy.go, slack.go, uninit.go                              | ✓ VERIFIED | grep confirms 10 production sites (bootstrap:341, budget:402, cluster:397+557, create:346+1869, destroy:249, init:399, slack:720, uninit:180) |
| `pkg/terragrunt/runner.go` — heartbeat + ctx.Cancel + WaitDelay      | Bounded ctx, DeadlineExceeded, cmd.Cancel, cmd.WaitDelay=5s, ProgressInterval=15s                                              | ✓ VERIFIED | Runner struct lines 34-35; heartbeat lines 25-247; cmd.Cancel/WaitDelay lines 341-342; DeadlineExceeded line 254     |
| `internal/app/cmd/init.go::RunInitWithRunner` — per-module timeout   | `context.WithTimeout` wrap before terragrunt Apply                                                                             | ✓ VERIFIED | Lines 816 (reconfigure) + 835 (apply); error wrap with state-digest recovery reference                                |
| `internal/app/cmd/bootstrap.go::defaultApplyTerragrunt`             | `context.WithTimeout` wrap using BootstrapApplyTimeout (10min)                                                                  | ✓ VERIFIED | Lines 30-40 (constant decl) + 626 (wrap); errors at 631/637 reference OPERATOR-GUIDE § Phase 84.1 state-digest recovery |
| `internal/app/cmd/doctor.go::checkStateLockDigest`                   | Paginated DDB scan, S3 MD5 vs Digest comparison, Remediation field with copy-paste command                                     | ✓ VERIFIED | Function at line 3338; NewScanPaginator at 3347; parseLockID at 3433; wired into runDoctor at line 2584 via buildChecks |
| `internal/app/cmd/doctor_state_digest_test.go`                       | Unit tests with mocked S3/DDB                                                                                                  | ✓ VERIFIED | File exists (329 lines); `go test -run TestCheckStateLockDigest\|TestParseLockID` PASS in 0.7s                       |
| `infra/modules/ses-shared-rule-set/v1.0.0/main.tf` — import blocks   | 6 `import {}` blocks (no DKIM) for shared rule set, active rule set, domain identity, domain DKIM, ses_verification TXT, MX     | ✓ VERIFIED | `grep -c "^import "` = 6; Makefile gate `test-phase-84-1-import-blocks` PASS                                          |
| `infra/modules/ses/v2.0.0/main.tf` — removed blocks                  | 7 `removed { lifecycle { destroy = false } }` blocks                                                                            | ✓ VERIFIED | `grep -c "^removed "` = 7; Makefile gate `test-phase-84-1-removed-blocks` PASS                                       |
| `infra/live/root.hcl` — Terraform version                            | `required_version >= 1.7.0` (removed{} block syntax requirement)                                                                | ✓ VERIFIED | Line 32: `required_version = ">= 1.7.0"`                                                                              |
| `internal/app/cmd/bootstrap.go::FoundationStateReader`               | Interface (line 107) + s3FoundationStateReader impl (line 226), wired into runBootstrapSharedSES (line 375 construct, 382 use) | ✓ VERIFIED | Interface declared, S3 impl constructed in production path, passed to detectSharedSESState                            |
| `OPERATOR-GUIDE.md` — state-digest recovery runbook                  | § "State-digest mismatch recovery (Phase 84.1)"                                                                                | ✓ VERIFIED | Line 724 — full runbook present                                                                                       |
| `OPERATOR-GUIDE.md` — Phase 84.1 upgrade safety runbook              | § "Phase 84.1 upgrade safety (in-place v1.0.0 → v2.0.0 cutover)"                                                                | ✓ VERIFIED | Line 754                                                                                                              |
| `CLAUDE.md` — Phase 84.1 subsection                                  | "Phase 84.1: Upgrade-safety gap closure"                                                                                       | ✓ VERIFIED | Line 113, full 8-bullet summary of gaps closed                                                                       |
| Phase 84 SUMMARY drift notes (84-04, 84-07, 84-08)                  | Each SUMMARY has "## Phase 84.1 drift" appended                                                                                | ✓ VERIFIED | 84-04:133, 84-07:139, 84-08:93 — all three present with content                                                       |

---

## Key Link Verification

| From                                  | To                                                       | Via                                                       | Status     | Details                                                                                                                          |
| ------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `runBootstrapSharedSES`                | `s3FoundationStateReader.StateOwns`                       | Constructed at bootstrap.go:375, used at line 382          | ✓ WIRED    | Production path constructs reader unless test-injected; passed to `detectSharedSESState`                                          |
| `defaultApplyTerragrunt`               | `BootstrapApplyTimeout`                                   | `context.WithTimeout(ctx, BootstrapApplyTimeout)`         | ✓ WIRED    | Wraps both reconfigure (line 626) and apply (line 636) calls                                                                     |
| `RunInitWithRunner` (init.go)          | Per-module `context.WithTimeout`                          | applyCtx at line 835                                       | ✓ WIRED    | Error message wraps DeadlineExceeded with OPERATOR-GUIDE § Phase 84.1 state-digest recovery reference                            |
| `pkg/terragrunt/runner.go::runCommand` | heartbeat goroutine via `runHeartbeat`                    | Goroutine launched at line 243 in quiet mode               | ✓ WIRED    | Channel-based shutdown, writes to ProgressWriter (default os.Stderr)                                                              |
| `pkg/terragrunt/runner.go`             | `cmd.Cancel + cmd.WaitDelay`                              | Lines 341-342                                              | ✓ WIRED    | SIGINT on cancel, 5s WaitDelay before SIGKILL                                                                                    |
| `runDoctor::buildChecks`               | `checkStateLockDigest`                                    | doctor.go:2584                                             | ✓ WIRED    | Conditional on stateLockS3+DDB+lockTable availability                                                                            |
| `checkStateLockDigest` failure          | `Remediation` field with copy-paste command              | CheckResult.Remediation                                    | ✓ WIRED    | Printed via report formatter at line 2144                                                                                        |
| Foundation `import` blocks              | actual AWS resources (rule set, identity, MX, TXT)        | terraform import target addresses with count=1 indexing    | ✓ WIRED    | All 6 import blocks reference `resource[0]` addresses matching the count=1 instance variant; `terraform validate` PASS           |
| Regional `removed` blocks               | foundation-now-owned resources (suppresses regional destroy) | 7 blocks with `lifecycle { destroy = false }`           | ✓ WIRED    | `terraform validate` PASS for v2.0.0; addresses match the shared resources moved to foundation                                   |

---

## Requirements Coverage

| Requirement | Source Plan       | Description                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                |
| ----------- | ----------------- | -------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GAP-1       | 84.1-01           | km bootstrap exports KM_ROUTE53_ZONE_ID for foundation MX/DKIM/verification records         | ✓ SATISFIED | init.go:703-704 exports it; unit test `TestExportTerragruntEnvVars_ExportsRoute53ZoneID` PASS                                                                                                                                                                                                          |
| GAP-2       | 84.1-04           | km bootstrap --shared-ses re-run is a true no-op (idempotent + state precedence)            | ✓ SATISFIED (code) / ⚠ DEFERRED (live) | `register_*` flags now mean "manage", FoundationStateReader wiring at bootstrap.go:146-148+375-382 + `prevent_destroy=true` preserved. Live re-run is Phase 84.2 UAT closure (d).                                                                                                                       |
| GAP-3       | 84.1-04           | Foundation auto-detect prefers state ownership over AWS reality (prevents data loss)        | ✓ SATISFIED | FoundationStateReader interface at bootstrap.go:107; s3FoundationStateReader at line 226; wired into runBootstrapSharedSES at line 375. NB: bootstrap dry-run anomaly investigation deferred to 84.2 (see deferred_empirical_items.GAP-3-bootstrap-dry-run-anomaly).                                       |
| GAP-4       | 84.1-02           | Wedged terragrunt apply surfaces timeout instead of hanging                                  | ✓ SATISFIED | runner.go heartbeat (line 25-247) + cmd.Cancel/WaitDelay (line 341-342) + DeadlineExceeded (line 254); unit tests pass in pkg/terragrunt (9.1s, all green)                                                                                                                                              |
| GAP-5       | 84.1-02           | Per-module context timeout in init.go AND bootstrap.go                                       | ✓ SATISFIED | init.go:835 (per-module Apply timeout); bootstrap.go:626 (BootstrapApplyTimeout, H6 expansion)                                                                                                                                                                                                          |
| GAP-6       | 84.1-04           | v1.0.0 → v2.0.0 cutover destroys ZERO shared resources via import + removed blocks         | ✓ SATISFIED (structural) / ⚠ DEFERRED (empirical) | 6 import{} + 7 removed{} blocks present and `terraform validate` PASS. Live destroy-suppression empirically unverified because no Phase 82.x state was in the verifying environment. Tracked as Phase 84.2 follow-up.                                                                                  |
| GAP-7       | 84.1-01           | km bootstrap exports KM_ARTIFACTS_BUCKET                                                     | ✓ SATISFIED | init.go:711-712 exports it; ExportTerragruntEnvVars called from bootstrap.go:341                                                                                                                                                                                                                       |
| GAP-8       | 84.1-03           | km doctor detects state-digest mismatch with Remediation field                              | ✓ SATISFIED | checkStateLockDigest at doctor.go:3338; NewScanPaginator at 3347; parseLockID at 3433; Remediation field populated and printed at line 2144; wired into buildChecks at 2584; unit tests TestCheckStateLockDigest+TestParseLockID PASS                                                                  |
| DRIFT-A     | 84.1-05           | Three Phase 84 UAT inline fixes (7aefed3, 143798d, 80b59a3) folded back into 84-04/07/08 SUMMARYs + state-digest recovery in OPERATOR-GUIDE | ✓ SATISFIED | 84-04-SUMMARY.md:133, 84-07-SUMMARY.md:139, 84-08-SUMMARY.md:93 — all three "Phase 84.1 drift" sections present; OPERATOR-GUIDE.md line 724 has state-digest recovery; CLAUDE.md line 113 has Phase 84.1 subsection                                                                                  |
| DRIFT-B     | 84.1-05           | km configure --reset-prefix → re-derive operator_email path                                  | ✓ SATISFIED (doc) / ⚠ DEFERRED (live) | Documented in 84-04-SUMMARY.md drift section as "NOT exercised in Phase 84 UAT; deferred to 84.1-05 UAT which itself deferred to 84.2". Code path exists from Phase 84.                                                                                                                                  |
| DRIFT-C     | 84.1-05           | InitSESPreflight block-until-shared-rule-set-exists path                                     | ✓ SATISFIED (doc) / ⚠ DEFERRED (live) | Documented in 84-07-SUMMARY.md drift section as deferred to Phase 84.2. Code path exists from Phase 84.                                                                                                                                                                                                  |

**All 11 requirement IDs are marked "Complete" in REQUIREMENTS.md lines 368-378.**

**Orphan requirements check:** None. All 11 IDs (GAP-1..GAP-8 + DRIFT-A/B/C) appear in PLAN frontmatter `requirements:` fields across plans 84.1-01..84.1-05.

---

## Build + Test Sanity (at HEAD)

| Check                                                                                       | Result                                                       |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| VERSION                                                                                     | 0.2.645 (matches Phase 84.1 UAT operator binary)             |
| `go test ./internal/app/cmd/ -run TestExportTerragruntEnvVars`                              | ✓ PASS (0.8s)                                                |
| `go test ./internal/app/cmd/ -run TestCheckStateLockDigest\|TestParseLockID`                | ✓ PASS (0.7s)                                                |
| `go test ./pkg/terragrunt/...`                                                              | ✓ PASS (9.1s, full package green)                            |
| `make test-phase-84-1-import-blocks`                                                        | ✓ OK: foundation main.tf has 6 import blocks (H9-compliant) |
| `make test-phase-84-1-removed-blocks`                                                       | ✓ OK: regional v2.0.0 main.tf has 7 removed blocks (destroy=false) |
| `make test-no-82.1-leftovers`                                                               | ✓ silent — no leftovers                                      |

All Phase 84.1 gates GREEN at HEAD. No regressions from the UAT-time snapshot.

---

## Anti-Patterns Found

None blocker-class.

| File                                                | Line | Pattern                                              | Severity | Impact                                                                                                                                                            |
| --------------------------------------------------- | ---- | ---------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `internal/app/cmd/bootstrap.go::runBootstrapSharedSES` | (anomaly) | dry-run reports "creating" for shared resources that may already be owned by foundation state | ℹ️ INFO  | Observed by operator during Phase 84.1 UAT prep. Could be (a) StateOwns not returning true because state path mismatch, or (b) foundation state genuinely does not own those resources yet. **Investigation deferred to Phase 84.2** per Phase 84.1 UAT § Observations. |

No TODO/FIXME/placeholder anti-patterns introduced by Phase 84.1.

---

## Human Verification Required (For Phase 84.2 UAT — Not Blocking Phase 84.1)

These items are explicitly deferred to Phase 84.2 per Phase 84.1 UAT § Must-haves status. They are NOT blocking Phase 84.1 closure but ARE required to fully validate the in-anger upgrade path:

### 1. GAP-6 empirical verification — live in-place v1.0.0 → v2.0.0 cutover

**Test:** Stand up (or snapshot) a Phase 82.x install, run `km bootstrap --shared-ses` + `km init`, observe that no shared SES resources are destroyed.
**Expected:** All 6 shared resources imported into foundation state; regional `terragrunt apply` shows zero destroys for `aws_ses_(domain_identity|domain_dkim|receipt_rule_set|active_receipt_rule_set)` and `aws_route53_record.(dkim|mx|ses_verification)`.
**Why human:** Requires a live Phase 82.x snapshot — not synthesizable from code; the structural verification (`terraform validate` + grep on import/removed blocks) cannot exercise the live import behavior because no v1.0.0 resources exist in the verifying environment's state.

### 2. UAT closures (a)/(c)/(d) — operator-driven re-run

**Test:** Re-run Phase 84 UAT closures (a) second-install dry-run, (c) sibling destroy isolation via `km uninit`, (d) bootstrap idempotent re-run.
**Expected:** All three closures complete without `aws dynamodb update-item` digest repair or manual recovery.
**Why human:** Requires two-install topology (`resource_prefix: km` and `resource_prefix: km2`) on a real AWS account.

### 3. GAP-3 bootstrap dry-run anomaly investigation

**Test:** `terragrunt state list` against `infra/live/use1/ses-shared-rule-set`, then trace `runBootstrapSharedSES` to confirm StateOwns is being called with the right resource addresses and returning true.
**Expected:** Either StateOwns reports state-ownership and bootstrap reports `managing`, or the state genuinely doesn't own the resources (in which case the original Phase 84 UAT recovery output mid-flight didn't fully migrate ownership).
**Why human:** Requires live state inspection and decision about which side of the disjunction is true.

### 4. DRIFT-B / DRIFT-C live exercises

**Test:** Exercise `km configure --reset-prefix` clearing operator_email then re-deriving; exercise `InitSESPreflight` block-until-rule-set-exists path destructively.
**Expected:** Both paths complete without errors.
**Why human:** Both are destructive scenarios that need a sacrificial install.

---

## Gaps Summary

**No code-incomplete gaps.** All 11 requirement IDs (GAP-1..GAP-8 + DRIFT-A/B/C) are code-complete, structurally verified, and have passing unit tests + Makefile gates.

The Phase 84.1 work-product is complete and merged on `main`:
- Plan 84.1-01 shipped ExportTerragruntEnvVars + 8-caller cascade (GAP-1, GAP-7)
- Plan 84.1-02 shipped bounded terragrunt runner + heartbeat + per-module timeouts in init.go and bootstrap.go (GAP-4, GAP-5)
- Plan 84.1-03 shipped checkStateLockDigest + parseLockID + paginator (GAP-8)
- Plan 84.1-04 shipped FoundationStateReader + 6 import{} blocks + 7 removed{} blocks + Makefile validate gate + root.hcl TF >= 1.7.0 (GAP-2, GAP-3, GAP-6)
- Plan 84.1-05 shipped OPERATOR-GUIDE.md state-digest recovery + CLAUDE.md Phase 84.1 section + 3 Phase 84 SUMMARY drift notes + operator UAT (DRIFT-A/B/C)

The operationally-heavy empirical verifications (live in-anger Phase 82.x upgrade, second-install topology, destructive `InitSESPreflight`, bootstrap dry-run anomaly investigation) are explicitly deferred to Phase 84.2 per Phase 84.1 UAT § Must-haves status table and STATE.md Roadmap Evolution. This is honest scope reduction documented at multiple layers, not silently skipped work.

The `passed-with-caveats` UAT status introduced in Phase 84.1 captures this distinction precisely: code complete + structurally verified + no regressions + most operationally-heavy live tests explicitly deferred (rather than presumed-pass-because-the-code-looks-right or claimed-skipped-without-tracking).

---

_Verified: 2026-05-17_
_Verifier: Claude (gsd-verifier)_
_Verification target: HEAD of `main` (commit 21eaf65 + working tree, km v0.2.645)_
