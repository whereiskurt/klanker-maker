---
phase: 115-generic-github-webhook-event-prompt-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/github/bridge/event_router_test.go
  - pkg/github/bridge/webhook_handler_phase115_test.go
  - internal/app/config/config_test.go
  - internal/app/cmd/github_test.go
  - internal/app/cmd/doctor_test.go
autonomous: true
requirements: [GH-EVENT-ROUTER, GH-EVENT-TEMPLATE, GH-EVENT-GATING, GH-EVENT-DISPATCH, GH-EVENT-COOLDOWN, GH-EVENT-CONFIG, GH-EVENT-MANIFEST, GH-EVENT-DOCTOR]

must_haves:
  truths:
    - "Running the quick test command names the missing TestMatchEventRule / TestExpandEventTemplate symbols (RED, not 'no tests to run')"
    - "Every unit-testable Phase 115 requirement has at least one failing test scaffold that pins its expected behavior"
    - "The test scaffolds compile-fail or assert-fail (genuine RED) — they do not vacuously pass"
  artifacts:
    - path: "pkg/github/bridge/event_router_test.go"
      provides: "RED table-driven tests for MatchEventRule + ExpandEventTemplate"
      contains: "func TestMatchEventRule"
    - path: "pkg/github/bridge/webhook_handler_phase115_test.go"
      provides: "RED tests for event-route gating, dispatch, cooldown"
      contains: "func TestHandle"
    - path: "internal/app/config/config_test.go"
      provides: "RED test for github.events yaml load round-trip"
      contains: "Events"
    - path: "internal/app/cmd/github_test.go"
      provides: "RED test for manifest event-union"
      contains: "TestRunGitHubManifest"
    - path: "internal/app/cmd/doctor_test.go"
      provides: "RED test for checkGitHubEventsValid"
      contains: "TestCheckGitHubEventsValid"
  key_links:
    - from: "event_router_test.go"
      to: "MatchEventRule / ExpandEventTemplate (not yet implemented)"
      via: "direct function call"
      pattern: "MatchEventRule|ExpandEventTemplate"
---

<objective>
Wave 0 test scaffold. Create failing (RED) test files that pin the expected behavior of every unit-testable Phase 115 requirement BEFORE any production code exists. This satisfies the Nyquist rule: each downstream `<verify>` command references a test that this plan creates.

Purpose: 115-VALIDATION.md lists five Wave 0 gaps. Without these scaffolds, downstream plans have no automated verification target and the executor would write tests after the fact (or not at all).
Output: Five test files/additions, all RED (compile-fail or assert-fail), covering GH-EVENT-ROUTER, GH-EVENT-TEMPLATE, GH-EVENT-GATING, GH-EVENT-DISPATCH, GH-EVENT-COOLDOWN, GH-EVENT-CONFIG, GH-EVENT-MANIFEST, GH-EVENT-DOCTOR.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-generic-github-webhook-event-prompt-router/115-CONTEXT.md
@.planning/phases/115-generic-github-webhook-event-prompt-router/115-RESEARCH.md
@.planning/phases/115-generic-github-webhook-event-prompt-router/115-VALIDATION.md

# Existing test patterns to mirror — read these for table-driven + mock conventions
@pkg/github/bridge/resolve_test.go
@pkg/github/bridge/handle_test.go
@pkg/github/bridge/commands_test.go

<interfaces>
<!-- These are the contracts the test scaffolds target. They DO NOT EXIST YET — this Wave 0
     plan defines them as test expectations; Waves 1-2 implement them. Use these signatures
     verbatim so production code and tests agree. -->

// pkg/github/bridge/event_router.go (NEW — implemented in Plan 02)
type EventRule struct {
    On              string   `json:"on"`
    Actions         []string `json:"actions,omitempty"`
    Match           string   `json:"match"`
    Exclude         []string `json:"exclude,omitempty"`
    Profile         string   `json:"profile,omitempty"`
    Alias           string   `json:"alias,omitempty"`
    Agent           string   `json:"agent,omitempty"`
    CooldownSeconds int      `json:"cooldown_seconds,omitempty"`
    Prompt          string   `json:"prompt"`
}
type EventPayload struct {
    Repo          string
    Action        string
    Sender        string
    DefaultBranch string
    HTMLURL       string
}
func MatchEventRule(eventType string, payload EventPayload, rules []EventRule) *EventRule
func ExpandEventTemplate(tmpl string, p EventPayload, eventType string) string

// pkg/github/bridge/webhook_handler.go — WebhookHandler gains (implemented in Plan 03):
//   EventRules []EventRule
// New header constant pattern matches GitHubDeliveryNoncePrefix at webhook_handler.go:18-19.

// internal/app/config/config.go — GithubConfig gains (implemented in Plan 02):
//   Events []GithubEventRule  // GithubEventRule mirrors EventRule field-for-field

// internal/app/cmd/doctor.go — new func (implemented in Plan 05):
//   func checkGitHubEventsValid(events []appcfg.GithubEventRule, repos []appcfg.GithubRepoEntry, defaultProfile, configDir string) CheckResult

// internal/app/cmd/github.go — RunGitHubManifest extends DefaultEvents (implemented in Plan 05).
</interfaces>

<!-- Existing mock doubles to reuse (from handle_test.go): mockNonceStore, plus the SQS /
     EventBridge / reactor mocks already present in the bridge test files. Reuse them; do not
     re-declare types that already exist in the package test files. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for EventRouter matcher + template expander (event_router_test.go)</name>
  <files>pkg/github/bridge/event_router_test.go</files>
  <action>
Create pkg/github/bridge/event_router_test.go (package bridge) with table-driven RED tests
mirroring resolve_test.go and commands_test.go.

TestMatchEventRule cases (GH-EVENT-ROUTER):
  - exact match on rule.Match wins over a later glob rule (exact-before-glob, mirrors Resolve in resolve.go:51-70)
  - first-match wins among multiple glob rules
  - on != eventType → no match (returns nil)
  - actions empty → matches any action
  - actions non-empty + action not in list → no match
  - actions non-empty + action in list → match
  - match glob "myorg/*" matches "myorg/repo", does not match "other/repo"
  - exclude glob suppresses an otherwise-matching rule (e.g. match "myorg/*" exclude ["myorg/archive-*"] → "myorg/archive-foo" returns nil)
  - empty rules slice → nil

TestExpandEventTemplate cases (GH-EVENT-TEMPLATE):
  - all six vars replaced: {{repo}} {{event}} {{action}} {{sender}} {{default_branch}} {{html_url}}
  - unknown var (e.g. {{nope}}) left verbatim
  - empty template → empty string
  - repeated var replaced in all positions

Call MatchEventRule and ExpandEventTemplate directly (they do not exist yet → compile-fail = RED, which is correct for Wave 0). Use t.Run subtests; assert *EventRule identity by a discriminating field (e.g. Prompt or Profile) so a wrong-rule match is caught.
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... -run 'TestMatchEventRule|TestExpandEventTemplate' -timeout 30s 2>&1 | grep -Eiq 'MatchEventRule|ExpandEventTemplate|undefined|cannot find' && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>event_router_test.go exists; `go test` for these names fails to compile or fails assertions citing the missing MatchEventRule/ExpandEventTemplate symbols (genuine RED).</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for event-route gating, dispatch, cooldown (webhook_handler_phase115_test.go)</name>
  <files>pkg/github/bridge/webhook_handler_phase115_test.go</files>
  <action>
Create pkg/github/bridge/webhook_handler_phase115_test.go (package bridge). Mirror the construction
and mock wiring in handle_test.go and webhook_handler_phase101_test.go (reuse mockNonceStore and the
existing SQS/EventBridge/reactor mock doubles already declared in the package — do NOT redeclare them).

Build a WebhookHandler with EventRules populated, a valid HMAC signature (copy the signing helper used
in the existing webhook_handler_phaseNN tests), and craft a synthetic `repository`/`created` webhook
request (x-github-event: repository, body with repository.full_name, repository.default_branch,
repository.html_url, action, sender.login, installation.id, plus a unique X-GitHub-Delivery header).

TestHandleEventRoute_Dispatch (GH-EVENT-GATING + GH-EVENT-DISPATCH):
  - EventRules empty → non-issue_comment event returns 200 and dispatches nothing (byte-identical drop)
  - EventRules with a matching rule, no alias → PutSandboxCreate called once (cold path); Number==0, Kind=="repository", Body==expanded prompt, Agent==rule.Agent in the dispatched envelope
  - EventRules with a matching rule + alias set → SQS.Send called once (warm path), PutSandboxCreate not called
  - EventRules present but NO rule matches the event → 200, no dispatch
  - issue_comment event with EventRules present → existing issue_comment path still taken (assert the event branch did NOT fire; reuse an existing issue_comment fixture)

TestHandleEventRoute_Cooldown (GH-EVENT-COOLDOWN):
  - rule.CooldownSeconds > 0: first delivery dispatches; a second delivery (new X-GitHub-Delivery GUID, same event/repo/action) within window returns 200 with NO dispatch (cooldown nonce key gh-event-cooldown:{event}:{repo}:{action} already-seen)
  - delivery-GUID dedup runs BEFORE routing: a retry with the SAME X-GitHub-Delivery returns 200 with no dispatch (replay)
  - rule.CooldownSeconds == 0: two deliveries (distinct GUIDs) both dispatch (cooldown off)

These call h.handleEventRoute indirectly via h.Handle (handleEventRoute does not exist yet → RED).
Assert dispatch counts via the mock call counters.
  </action>
  <verify>
    <automated>go test ./pkg/github/bridge/... -run 'TestHandleEventRoute' -timeout 30s 2>&1 | grep -Eiq 'EventRules|handleEventRoute|undefined|FAIL|cannot' && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>webhook_handler_phase115_test.go exists; tests fail (compile or assertion) because EventRules field / event-route dispatch do not exist yet (genuine RED).</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for config load, manifest union, doctor check</name>
  <files>internal/app/config/config_test.go, internal/app/cmd/github_test.go, internal/app/cmd/doctor_test.go</files>
  <action>
Add focused RED test cases (do not rewrite the files):

1) internal/app/config/config_test.go — GH-EVENT-CONFIG. Add a test (e.g. TestLoadGithubEvents) that
   writes a temp km-config.yaml with a github.events: block (on: repository, actions: [created],
   match: "myorg/*", exclude: ["myorg/archive-*"], profile: profiles/onboard.yaml, cooldownSeconds: 0,
   prompt: "A new repo {{repo}}..."), calls config.Load, and asserts cfg.Github.Events has len 1 with
   the expected On/Match/Actions/Exclude/Prompt. Mirror the existing github.commands / github.repos
   load test in this file. (RED: cfg.Github.Events field does not exist yet → compile-fail.)

2) internal/app/cmd/github_test.go — GH-EVENT-MANIFEST. Add a case to TestRunGitHubManifest (or a new
   TestRunGitHubManifest_EventUnion) that builds a *config.Config with cfg.Github.Events containing an
   `on: repository` rule, runs RunGitHubManifest, and asserts the rendered manifest's default_events
   contains BOTH "issue_comment" AND "repository", and default_permissions["metadata"]=="read".
   (RED: union logic + cfg.Github.Events do not exist yet.)

3) internal/app/cmd/doctor_test.go — GH-EVENT-DOCTOR. Add TestCheckGitHubEventsValid with cases:
   empty events → CheckSkipped; rule with malformed glob in match (e.g. "myorg/[") → WARN; rule whose
   profile path does not exist on disk → WARN; rule with a reserved/unknown event name (Claude's
   discretion on the exact reserved set — at minimum assert a WARN path exists). Mirror the existing
   TestCheckGitHubCommandsValid in this file. (RED: checkGitHubEventsValid does not exist yet.)

All three are genuine RED (compile-fail on missing symbols). Keep each addition minimal and self-contained.
  </action>
  <verify>
    <automated>go test ./internal/app/config/... ./internal/app/cmd/... -run 'TestLoadGithubEvents|TestRunGitHubManifest|TestCheckGitHubEventsValid' -timeout 60s 2>&1 | grep -Eiq 'Events|checkGitHubEventsValid|undefined|FAIL|cannot' && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>All three test additions present and RED, citing the not-yet-existing cfg.Github.Events field / manifest union / checkGitHubEventsValid function.</done>
</task>

</tasks>

<verification>
- All five Wave 0 gaps from 115-VALIDATION.md have a corresponding RED test.
- Each test fails for the RIGHT reason (missing production symbol), not a typo or wrong package.
- No test vacuously passes (no empty test bodies, no `t.Skip` that hides RED).
- The bridge package still compiles for OTHER tests (only the new files reference missing symbols) — acceptable for Wave 0; downstream waves turn them GREEN.
</verification>

<success_criteria>
- `go test ./pkg/github/bridge/... -run 'TestMatchEventRule|TestExpandEventTemplate|TestHandleEventRoute' -timeout 30s` is RED citing missing EventRouter/EventRules symbols.
- `go test ./internal/app/config/... ./internal/app/cmd/... -run 'TestLoadGithubEvents|TestRunGitHubManifest|TestCheckGitHubEventsValid' -timeout 60s` is RED citing missing config/manifest/doctor symbols.
- Mark wave_0_complete: true conceptually (downstream plans depend on these targets existing).
</success_criteria>

<output>
After completion, create `.planning/phases/115-generic-github-webhook-event-prompt-router/115-01-SUMMARY.md`
</output>
