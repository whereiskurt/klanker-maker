---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: "08"
subsystem: infra
tags: [bootstrap, drift-warn, ExportTerragruntEnvVars, gap-closure]

requires:
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    provides: "Plan 06 RED tests for runBootstrap drift WARN"

provides:
  - "runBootstrap (default km bootstrap path) calls ExportTerragruntEnvVars after loadBootstrapConfig"
  - "Gap 2 closed: drift WARNs fire from all km bootstrap paths"

affects:
  - 84.3-06
  - 84.3-07
  - gap-drift-warn-runBootstrap-missing

tech-stack:
  added: []
  patterns:
    - "ExportTerragruntEnvVars called immediately after loadBootstrapConfig in every runBootstrap* entry point"

key-files:
  created: []
  modified:
    - internal/app/cmd/bootstrap.go

key-decisions:
  - "Single surgical line insertion: ExportTerragruntEnvVars(loadedCfg) added at line 1317 in runBootstrap, matching existing pattern in runBootstrapSharedSES (342) and runBootstrapSharedSESPlanWithWriter (713)"

patterns-established:
  - "Every runBootstrap* entry point that calls loadBootstrapConfig must immediately follow with ExportTerragruntEnvVars(loadedCfg)"

requirements-completed:
  - ENV-CONFIG-DRIFT-WARN
  - CONFIG-DISPLAY-VS-YAML-AUTHORITY

duration: 5min
completed: 2026-05-17
---

# Phase 84.3 Plan 08: Wire ExportTerragruntEnvVars into runBootstrap Summary

**Gap 2 closed: one-line insertion into runBootstrap makes drift WARNs fire from the default `km bootstrap` path, matching the existing pattern in runBootstrapSharedSES and runBootstrapSharedSESPlanWithWriter.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-17T20:15:00Z
- **Completed:** 2026-05-17T20:20:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `ExportTerragruntEnvVars(loadedCfg)` at line 1317 of `runBootstrap`, immediately after `loadBootstrapConfig` returns and before `warnEmptyAccountIDs`
- `TestRunBootstrap_DriftWarn_MissingExportCall` (Plan 06 RED test) now GREEN
- `TestRunBootstrap_NoDriftWarn_WhenEnvMatchesConfig` stays GREEN (negative case)
- All existing `TestBootstrap*` tests remain GREEN
- `make build` passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire ExportTerragruntEnvVars into runBootstrap** - `0c7b9aa` (fix)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `internal/app/cmd/bootstrap.go` - Added `ExportTerragruntEnvVars(loadedCfg)` at line 1317 in `runBootstrap` (2-line insertion: the call + blank line separator)

## Decisions Made
- Single surgical insertion only — no other runBootstrap* variants needed changes:
  - `runBootstrapSharedSES` (line 342): already had the call
  - `runBootstrapSharedSESPlanWithWriter` (line 713): already had the call
  - `runBootstrapAll` (line 655): calls RunBootstrapFunc and RunBootstrapSharedSESFunc which each call ExportTerragruntEnvVars internally — no direct call needed

## Deviations from Plan

None - plan executed exactly as written. The insertion point was unambiguous: one location in bootstrap.go matching the described pattern.

## Issues Encountered

Full suite `./internal/app/cmd/...` timed out at 60s due to `TestAgentRun_CodexFlag` (agent network test, unrelated to bootstrap). Confirmed pre-existing: test passes in 3s on unmodified base. All bootstrap-specific tests pass in isolation.

## Self-Check

### Verification: ExportTerragruntEnvVars hits in bootstrap.go

```
342:    ExportTerragruntEnvVars(loadedCfg)   <- runBootstrapSharedSES
713:    ExportTerragruntEnvVars(loadedCfg)   <- runBootstrapSharedSESPlanWithWriter
1317:   ExportTerragruntEnvVars(loadedCfg)   <- runBootstrap (new)
```

3 hits confirmed (plan required >= 3).

### Test Results

- `TestRunBootstrap_DriftWarn_MissingExportCall` — PASS (was FAIL before)
- `TestRunBootstrap_NoDriftWarn_WhenEnvMatchesConfig` — PASS
- `TestBootstrapDryRun_SaysApply` — PASS
- `TestBootstrapAllFlagRegistered` — PASS
- `TestBootstrapAll_MutexWithSharedSES` — PASS
- `TestBootstrapBannerWarnsMissingAccount` (3 subtests) — PASS

## Self-Check: PASSED

- Commit `0c7b9aa` exists: `fix(84.3/gap2): wire ExportTerragruntEnvVars into runBootstrap`
- `internal/app/cmd/bootstrap.go` modified with insertion at line 1317
- All target tests GREEN, no regressions in bootstrap suite

## Next Phase Readiness
- Gap 2 is fully closed
- Plans 07 + 08 together cover both runBootstrap and runBootstrapSharedSES drift-warn paths
- Ready for Phase 84.4 (module-level hard-coded `km-` prefix fixes)

---
*Phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted*
*Completed: 2026-05-17*
