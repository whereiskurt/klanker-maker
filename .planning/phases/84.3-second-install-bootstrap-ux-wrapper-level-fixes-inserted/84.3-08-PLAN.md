---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 08
type: execute
wave: 6
depends_on:
  - 84.3-06
files_modified:
  - internal/app/cmd/bootstrap.go
autonomous: true
gap_closure: true
requirements:
  - ENV-CONFIG-DRIFT-WARN
  - CONFIG-DISPLAY-VS-YAML-AUTHORITY

must_haves:
  truths:
    - "KM_ACCOUNTS_ORGANIZATION set in env with yaml org account different: running km bootstrap (default, no --shared-ses) emits WARN to stderr"
    - "KM_REGION set in env differing from yaml: running km bootstrap (default path) emits WARN for KM_REGION"
    - "runBootstrap calls ExportTerragruntEnvVars(loadedCfg) immediately after loadBootstrapConfig"
    - "runBootstrapSharedSES still calls ExportTerragruntEnvVars (no regression)"
    - "runBootstrapAll still calls ExportTerragruntEnvVars through both subflows"
  artifacts:
    - path: "internal/app/cmd/bootstrap.go"
      provides: "runBootstrap wired with ExportTerragruntEnvVars(loadedCfg) call after loadBootstrapConfig, before banner block"
      contains: "ExportTerragruntEnvVars(loadedCfg)"
  key_links:
    - from: "internal/app/cmd/bootstrap.go::runBootstrap"
      to: "internal/app/cmd/init.go::ExportTerragruntEnvVars"
      via: "called right after loadBootstrapConfig(cfg) returns loadedCfg, before warnEmptyAccountIDs"
      pattern: "ExportTerragruntEnvVars\\(loadedCfg\\)"
---

<objective>
Fix gap 2: runBootstrap (the default `km bootstrap` path without `--shared-ses`) does not call
ExportTerragruntEnvVars, so drift WARN never fires from that code path.

Root cause confirmed by UAT: `KM_ACCOUNTS_ORGANIZATION=999999999999 ./km bootstrap --dry-run=true`
produced empty stderr because `runBootstrap` (line 1307) goes through `loadBootstrapConfig` and
then `warnEmptyAccountIDs`, but never calls `ExportTerragruntEnvVars`. The `--shared-ses` path
(runBootstrapSharedSES at line 335) correctly calls `ExportTerragruntEnvVars(loadedCfg)`.

Fix: One surgical line insertion into `runBootstrap` right after `loadBootstrapConfig` returns,
matching the existing pattern in `runBootstrapSharedSES`.

This is the smallest possible change for maximum correctness — no design decisions, no new types,
just a missing function call added to the right place.

Output: bootstrap.go updated; Plan 06 TestRunBootstrap_DriftWarn_MissingExportCall turns GREEN.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md
@CLAUDE.md
@internal/app/cmd/bootstrap.go

<interfaces>
MODIFIED (bootstrap.go) — one line insertion in runBootstrap:

BEFORE (current, around line 1312):
```go
func runBootstrap(ctx context.Context, cfg *config.Config, dryRun bool, w io.Writer) error {
    if ctx == nil {
        ctx = context.Background()
    }

    loadedCfg, err := loadBootstrapConfig(cfg)
    if err != nil {
        return err
    }

    warnEmptyAccountIDs(loadedCfg, os.Stderr)
    // ... banner block follows
```

AFTER (insertion on line between loadBootstrapConfig and warnEmptyAccountIDs):
```go
    loadedCfg, err := loadBootstrapConfig(cfg)
    if err != nil {
        return err
    }

    ExportTerragruntEnvVars(loadedCfg)   // ← ADD THIS LINE

    warnEmptyAccountIDs(loadedCfg, os.Stderr)
    // ... banner block unchanged
```

Reference: runBootstrapSharedSES at line ~342 has the same ExportTerragruntEnvVars(loadedCfg)
call immediately after loadBootstrapConfig. This change makes runBootstrap consistent.

No other changes required. ExportTerragruntEnvVars is already imported from init.go (same package).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ExportTerragruntEnvVars into runBootstrap</name>
  <files>
    internal/app/cmd/bootstrap.go
  </files>
  <behavior>
    Single surgical change: add `ExportTerragruntEnvVars(loadedCfg)` to runBootstrap
    immediately after `loadBootstrapConfig` returns and before `warnEmptyAccountIDs`.

    Exact position: search for the pattern:
    ```
    loadedCfg, err := loadBootstrapConfig(cfg)
    if err != nil {
        return err
    }

    warnEmptyAccountIDs(loadedCfg, os.Stderr)
    ```
    And insert `ExportTerragruntEnvVars(loadedCfg)` between the closing `}` of the error check
    and the `warnEmptyAccountIDs` call.

    Verify no similar missing calls exist in other runBootstrap* variants by checking each:
    - runBootstrapSharedSES: already has ExportTerragruntEnvVars — no change needed.
    - runBootstrapAll: calls RunBootstrapFunc and RunBootstrapSharedSESFunc which internally
      call ExportTerragruntEnvVars — no direct call needed in runBootstrapAll.
    - runBootstrapSharedSESPlan / runBootstrapSharedSESPlanWithWriter: plan-only path, uses
      ExportTerragruntEnvVars via runBootstrapSharedSES internals — confirm no missing call.

    Tests to turn GREEN (from Plan 06):
    - TestRunBootstrap_DriftWarn_MissingExportCall
    - TestRunBootstrap_NoDriftWarn_WhenEnvMatchesConfig

    Tests that must stay GREEN (from Plans 01-04):
    - TestBootstrapDryRun_SaysApply
    - TestBootstrapAllFlagRegistered
    - TestBootstrapAll_MutexWithSharedSES
    - TestBootstrapBannerWarnsMissingAccount
    - All existing TestBootstrap* tests
  </behavior>
  <action>
    1. Read bootstrap.go lines 1306-1330 to see the exact current runBootstrap header.
    2. Insert `ExportTerragruntEnvVars(loadedCfg)` after the `if err != nil { return err }` block and before `warnEmptyAccountIDs`.
    3. Verify runBootstrapSharedSES (~line 335) still has its call (no change needed).
    4. `make build`.
    5. `go test ./internal/app/cmd/ -run 'TestRunBootstrap_DriftWarn|TestBootstrap' -v -timeout 30s`.
    6. Full suite: `go test ./internal/app/cmd/... -timeout 60s`.
    7. Commit: `fix(84.3/gap2): wire ExportTerragruntEnvVars into runBootstrap`
  </action>
  <verify>
    <automated>make build && go test ./internal/app/cmd/ -run 'TestRunBootstrap_DriftWarn_MissingExportCall|TestBootstrapDryRun_SaysApply|TestBootstrapAllFlagRegistered' -v -timeout 30s 2>&1 | tail -20</automated>
  </verify>
  <done>runBootstrap calls ExportTerragruntEnvVars(loadedCfg) before the banner block. Plan 06 TestRunBootstrap_DriftWarn_MissingExportCall turns GREEN. All Plan 01-04 bootstrap tests stay GREEN.</done>
</task>

</tasks>

<verification>
- `grep -n "ExportTerragruntEnvVars" internal/app/cmd/bootstrap.go` returns >= 3 hits (runBootstrap + runBootstrapSharedSES + at least one more).
- `go test ./internal/app/cmd/ -run 'TestRunBootstrap_DriftWarn' -timeout 30s` — GREEN.
- `go test ./internal/app/cmd/... -timeout 60s` — full suite GREEN.
- `make build` passes.
</verification>

<success_criteria>
- Gap 2 closed: `km bootstrap` (default path without --shared-ses) now emits drift WARN for all env-vs-yaml differences.
- Consistent behavior: runBootstrap matches runBootstrapSharedSES in ExportTerragruntEnvVars usage.
- No regressions in existing bootstrap tests.
- make build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-08-SUMMARY.md` documenting:
- Exact line number of the insertion in bootstrap.go.
- Verification that runBootstrapSharedSES and runBootstrapAll are correctly wired (no missing calls).
- Test results: Plan 06 test GREEN; Plan 01-04 tests GREEN.
</output>
