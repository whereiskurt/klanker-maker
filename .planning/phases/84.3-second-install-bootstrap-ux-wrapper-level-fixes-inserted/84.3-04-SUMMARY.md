---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 04
subsystem: cli
tags: [go, tdd, wave3, init, env, drift-warn, outputs-probe, hard-fail, km-env]

# Dependency graph
requires:
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    plan: 01
    provides: Wave 0 RED test scaffolding (env_test.go, init_84_3_test.go, init_plan_84_3_test.go)
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    plan: 02
    provides: S3HeadBucketAPI interface in configure.go; stub files for Plan 04 symbols
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    plan: 03
    provides: Test-seam pattern for runInit/runEnvExport tests (follows bootstrap.go shape)

provides:
  - warnAndSetEnv (init.go): drift WARN helper for ExportTerragruntEnvVars
  - ExportTerragruntEnvVars (init.go): now emits per-var WARN when env differs from cfg
  - ensureArtifactsBucketExists (init.go): HeadBucket probe; hard-fail on 404 with recovery commands
  - upstreamOutputsExist (init.go): filepath probe for upstream outputs.json siblings
  - RunInitPlanWithRunner (init.go): skip probe for modules with missing upstream outputs
  - NewEnvCmd + runEnvExport (env.go): km env subcommand printing 11 KM_* export lines
  - root.go: NewEnvCmd registered as first-class Cobra subcommand

affects:
  - 84.3-05-PLAN: all 8 wrapper-level closures (a-h) now implemented; (i) is joint UAT with Phase 84.4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "warnAndSetEnv closure inside ExportTerragruntEnvVars: emit drift WARN before setenv, env-wins preserved"
    - "smithy.APIError interface (imported from github.com/aws/smithy-go) for 404/403 detection in ensureArtifactsBucketExists"
    - "upstreamOutputsExist: filepath.Join sibling probe (moduleDir/../upstream/outputs.json)"
    - "io/fs.ErrNotExist for Stat error classification in upstreamOutputsExist"
    - "info.go file-per-command pattern applied to env.go (NewEnvCmd + runEnvExport)"

key-files:
  created:
    - internal/app/cmd/env.go
  modified:
    - internal/app/cmd/init.go
    - internal/app/cmd/root.go
  deleted:
    - internal/app/cmd/env_84_3_stubs.go
    - internal/app/cmd/init_84_3_stubs.go

key-decisions:
  - "warnAndSetEnv helper extracted from ExportTerragruntEnvVars to avoid 11x code repetition; KM_RESOURCE_PREFIX and KM_EMAIL_SUBDOMAIN keep inline logic because they always export (even when cfg is empty)"
  - "ensureArtifactsBucketExists uses smithy.APIError ErrorCode() detection (matching configure.go precedent) rather than s3types.NotFound — test mock uses smithy errors, not s3types"
  - "ensureArtifactsBucketExists signature: (ctx, cfg, io.Writer, S3HeadBucketAPI) to match stub and test call site; writer param reserved for future structured output"
  - "upstreamOutputsExist probe placed AFTER envReqs check so missing env vars short-circuit first"
  - "runEnvExport uses cfg.GetRegionLabel() for KM_REGION_LABEL and cfg.GetResourcePrefix() for KM_RESOURCE_PREFIX to respect the existing method-based accessors"

# Metrics
duration: 8min
completed: 2026-05-17
---

# Phase 84.3 Plan 04: init.go + env.go + root.go Closures c, d, f-init-side, g Summary

**ExportTerragruntEnvVars drift WARN + ensureArtifactsBucketExists hard-fail + upstreamOutputsExist skip probe + NEW km env subcommand — all 12 Plan 01 RED tests GREEN, both stub files deleted**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-17T21:46:16Z
- **Completed:** 2026-05-17T21:54:46Z
- **Tasks:** 3
- **Files modified:** 2 modified, 1 created, 2 deleted

## Accomplishments

- Closure (c) ENV-CONFIG-DRIFT-WARN: `warnAndSetEnv` helper emits `WARN: KEY=envval (env) overrides km-config.yaml yaml-key=cfgval` to stderr before each setenv block; env-wins semantics unchanged; covers all 11 KM_* vars
- Closure (f) init-side BOOTSTRAP-WORKFLOW-DISCOVERABILITY: `ensureArtifactsBucketExists` HeadBucket probe wired into `runInit` apply path (before any terragrunt invocation); hard-fails on 404 with message naming both `km bootstrap --all` and `km bootstrap --shared-ses`
- Closure (d) PLAN-FRESH-INSTALL-OUTPUTS-HANDLING: `upstreamOutputsExist` probe inserted in `RunInitPlanWithRunner` after envReqs check; `efs` populated with `upstreamOutputs: []string{"network"}`; skipped modules excluded from destroy-class gate; fresh install exits 0
- Closure (g) KM-ENV-EXPORT-HELPER: NEW `internal/app/cmd/env.go` with `NewEnvCmd` + `runEnvExport`; 11 KM_* vars in deterministic sorted order; `--aws-profile` flag adds AWS_PROFILE line; `km env` registered in `root.go` near `NewInfoCmd`
- KM_ACCOUNTS_* still exported to terragrunt subprocesses (Plan 02 yaml-authority invariant preserved)
- Both Plan 02 stub files (`env_84_3_stubs.go`, `init_84_3_stubs.go`) deleted

## Task Commits

1. **Task 1: ExportTerragruntEnvVars drift WARN + ensureArtifactsBucketExists hard-fail** - `6bf569d` (feat)
2. **Task 2: upstreamOutputsExist skip probe in RunInitPlanWithRunner** - `e4b89f6` (feat)
3. **Task 3: NEW km env subcommand + root.go registration** - `670ef44` (feat)

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/internal/app/cmd/init.go` — Modified in three places:
  1. `ExportTerragruntEnvVars` (~line 736): replaced 11 inline setenv blocks with `warnAndSetEnv` helper; KM_RESOURCE_PREFIX and KM_EMAIL_SUBDOMAIN retain inline logic for zero-value export behavior
  2. `ensureArtifactsBucketExists` (new, ~line 820): HeadBucket probe with smithy error detection; hard-fail message names both `--all` and `--shared-ses` recovery paths
  3. `upstreamOutputsExist` (new, ~line 850): sibling filepath probe for upstream outputs.json
  4. `RunInitPlanWithRunner` loop (~line 1174): inserted `upstreamOutputsExist` skip block after envReqs check
  5. `runInit` (~line 437): added `ensureArtifactsBucketExists` early-call guard
  6. `regionalModule` struct: added `upstreamOutputs []string` field
  7. `regionalModules()`: `efs` entry populated with `upstreamOutputs: []string{"network"}`
  8. Imports: added `smithy "github.com/aws/smithy-go"` and `"io/fs"`
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/env.go` — NEW file: 70 lines; `NewEnvCmd` + `runEnvExport`; follows info.go pattern
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/root.go` — 1 line added: `root.AddCommand(NewEnvCmd(cfg))` near NewInfoCmd
- DELETED: `internal/app/cmd/env_84_3_stubs.go` — Plan 02 stub replaced
- DELETED: `internal/app/cmd/init_84_3_stubs.go` — Plan 02 stub replaced

## Closure Status

- **(c) ENV-CONFIG-DRIFT-WARN** — GREEN (warnAndSetEnv in ExportTerragruntEnvVars; 5 tests passing including AccountsStillExported invariant)
- **(d) PLAN-FRESH-INSTALL-OUTPUTS-HANDLING** — GREEN (upstreamOutputsExist + efs entry; 3 tests passing: SkipsEFS, NoSkipWhenOutputsExist, SkippedModulesExcludedFromGate)
- **(f-init-side) BOOTSTRAP-WORKFLOW-DISCOVERABILITY** — GREEN (ensureArtifactsBucketExists wired into runInit; 1 test passing: HardFailsMissingArtifactsBucket)
- **(g) KM-ENV-EXPORT-HELPER** — GREEN (NewEnvCmd + runEnvExport; 4 tests passing: DefaultOutput, EvalSafe, AWSProfileFlag, Registration)

## Quick Validate

`go test ./internal/app/cmd/ -run 'TestEnvCmd|TestExportTerragruntEnvVars|TestRunInit_HardFails|TestRunInitPlan_Skips' -timeout 30s` — PASS

## Manual Verification

- `./bin/km env --help` — shows `--aws-profile` flag
- `./bin/km env` — prints exactly 11 `export KM_*` lines in deterministic order
- `./bin/km env --aws-profile` — prints 12 lines (11 KM_* + AWS_PROFILE)
- `eval $(./bin/km env)` — bash eval-safe (verified by TestEnvCmd_EvalSafe)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] smithy.APIError used instead of s3types.NotFound for 404 detection**
- **Found during:** Task 1 (ensureArtifactsBucketExists implementation)
- **Issue:** Plan spec said to use `s3types.NotFound` from `github.com/aws/aws-sdk-go-v2/service/s3/types`, but the test mock uses smithy errors (matching configure.go's existing pattern). `s3types.NotFound` is a concrete type from the SDK that the mock does not implement.
- **Fix:** Used `smithy.APIError` interface with `ErrorCode()` check for "NotFound" or "NoSuchBucket" — matching configure.go precedent and the test mock's implementation
- **Files modified:** internal/app/cmd/init.go (smithy import added)
- **Commit:** 6bf569d

**2. [Rule 1 - Bug] ensureArtifactsBucketExists signature matches stub (4-arg) not plan spec (variadic)**
- **Found during:** Task 1 (test signature analysis)
- **Issue:** Plan spec showed variadic `s3client ...S3HeadBucketAPI` but stub had `(ctx, cfg, io.Writer, S3HeadBucketAPI)` and test calls with 4 positional args
- **Fix:** Used stub's 4-arg signature to match test call site
- **Files modified:** internal/app/cmd/init.go
- **Commit:** 6bf569d

---

**Total deviations:** 2 auto-fixed (both bug corrections to match test expectations)
**Impact on plan:** Both fixes required for GREEN tests. No scope creep.

## Hand-off Note for Plan 05

All 8 wrapper-level Phase 84.3 closures are now implemented and unit-tested:
- (a) BOOTSTRAP-PREFLIGHT-BUCKET-CHECK — Plan 02 (configure.go)
- (b) BOOTSTRAP-INIT-ORDER-AWARENESS — Plan 03 (bootstrap.go)
- (c) ENV-CONFIG-DRIFT-WARN — Plan 04 (init.go)
- (d) PLAN-FRESH-INSTALL-OUTPUTS-HANDLING — Plan 04 (init.go)
- (e) ARTIFACTS-BUCKET-DERIVATION — Plan 02 (configure.go)
- (f) BOOTSTRAP-WORKFLOW-DISCOVERABILITY — Plans 02+03+04 (configure.go, bootstrap.go, init.go)
- (g) KM-ENV-EXPORT-HELPER — Plan 04 (env.go)
- (h) CONFIG-DISPLAY-VS-YAML-AUTHORITY — Plan 02 (config.go)

Closure (i) is the joint UAT with Phase 84.4 — the only remaining Phase 84.3 work.

## Self-Check

Files exist:
- [x] `internal/app/cmd/env.go` — EXISTS
- [x] `internal/app/cmd/init.go` — EXISTS with warnAndSetEnv, ensureArtifactsBucketExists, upstreamOutputsExist
- [x] `internal/app/cmd/root.go` — EXISTS with NewEnvCmd registration
- [x] `internal/app/cmd/env_84_3_stubs.go` — DELETED (confirmed)
- [x] `internal/app/cmd/init_84_3_stubs.go` — DELETED (confirmed)

Commits exist:
- [x] 6bf569d — feat(84.3-04): ExportTerragruntEnvVars drift WARN + ensureArtifactsBucketExists hard-fail
- [x] e4b89f6 — feat(84.3-04): upstreamOutputsExist skip probe in RunInitPlanWithRunner
- [x] 670ef44 — feat(84.3-04): NEW km env subcommand + root.go registration

## Self-Check: PASSED

---
*Phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted*
*Completed: 2026-05-17*
