---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 05
type: execute
wave: 4
depends_on:
  - 84.3-01
  - 84.3-02
  - 84.3-03
  - 84.3-04
files_modified:
  - .planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md
  - OPERATOR-GUIDE.md
  - CLAUDE.md
autonomous: false
requirements:
  - BOOTSTRAP-PREFLIGHT-BUCKET-CHECK
  - BOOTSTRAP-DRYRUN-TEXT-FIX
  - ENV-CONFIG-DRIFT-WARN
  - PLAN-FRESH-INSTALL-OUTPUTS-HANDLING
  - ARTIFACTS-BUCKET-DERIVATION
  - BOOTSTRAP-WORKFLOW-DISCOVERABILITY
  - KM-ENV-EXPORT-HELPER
  - CONFIG-DISPLAY-VS-YAML-AUTHORITY
validation:
  framework: "Manual operator UAT + scripted sub-checks where automatable"
  quick_command: "go test ./internal/app/cmd/... ./internal/app/config/... -timeout 120s"
  full_command: "go test ./... -timeout 600s"
  per_task:
    - task: 1
      tests:
        - "Eight per-closure UAT scenarios (a)-(h) executed by operator; results captured in 84.3-05-UAT.md"
        - "Joint third-install runtime UAT (i) executed jointly with Phase 84.4 plans; results captured in BOTH 84.3-05-UAT.md AND 84.4's corresponding UAT plan"
    - task: 2
      tests:
        - "OPERATOR-GUIDE.md Phase 84.3 section reads end-to-end; commands match the runbook"
        - "CLAUDE.md updated with km env command + km bootstrap --all flag"
must_haves:
  truths:
    - "Operator can run `git clone` → `km configure` → `km bootstrap --all --dry-run=false` → `km init --plan` → `km init --dry-run=false` end-to-end on a fresh shared-AWS-account install without hand-editing yaml, hand-exporting env vars, or reading source"
    - "All eight closures (a-h) verified individually during UAT — each has a documented pass/fail line in 84.3-05-UAT.md"
    - "Joint UAT (i) with Phase 84.4 confirms the third-install end-to-end flow including a successful `km create profiles/learn.yaml` sandbox launch on the second prefix install"
    - "OPERATOR-GUIDE.md has a Phase 84.3 § documenting the new wrapper-level UX (km bootstrap --all, km env, drift WARNs, HeadBucket re-prompt, artifacts-bucket auto-derivation, hard-fail behavior)"
    - "CLAUDE.md updated: km env added to CLI list; km bootstrap --all flag noted; Phase 84.3 § added covering the eight closures and their visible surfaces"
  artifacts:
    - path: ".planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md"
      provides: "Eight per-closure UAT scenarios + joint Phase 84.4 third-install scenario; each with pass/fail line + observed output captured"
      contains: "Closure (a)"
    - path: "OPERATOR-GUIDE.md"
      provides: "Phase 84.3 wrapper-level UX runbook section: km bootstrap --all, km env, drift WARN interpretation, HeadBucket retry UX, hard-fail recovery"
      contains: "Phase 84.3"
    - path: "CLAUDE.md"
      provides: "CLI list updated (km env, km bootstrap --all), Phase 84.3 § added with closure summary + cross-references to OPERATOR-GUIDE.md"
      contains: "km env"
  key_links:
    - from: "84.3-05-UAT.md"
      to: "84.3-CONTEXT.md closure acceptance lines (a)-(h)"
      via: "each UAT scenario maps 1:1 to a closure; pass/fail line cites the CONTEXT.md acceptance criterion verbatim"
      pattern: "Closure \\([a-h]\\):"
    - from: "84.3-05-UAT.md (i) joint scenario"
      to: "Phase 84.4 third-install UAT plan"
      via: "84.3 + 84.4 ship together for (i); shared probe install is klanker-maker-whereiskurt per ROADMAP Phase 84.4 § operator-visible probe state"
      pattern: "Phase 84.4|whereiskurt"
    - from: "OPERATOR-GUIDE.md Phase 84.3 §"
      to: "OPERATOR-GUIDE.md Phase 84.1 §, Phase 84.2 §"
      via: "Phase 84.3 doc extends the existing Phase 84 upgrade narrative — link forward from Phase 84.2's runbook to Phase 84.3's new commands"
      pattern: "Phase 84\\."
---

<objective>
Execute Wave 4 of Phase 84.3: the joint operator UAT that verifies all eight wrapper-level closures (a-h) and the joint third-install end-to-end runtime scenario (i) together with Phase 84.4's module-hardening UAT.

This plan is **NOT autonomous** — it requires OPERATOR CHECKPOINT interaction. The operator must:
1. Walk the eight per-closure UAT scenarios on a real AWS account.
2. Coordinate the joint scenario (i) with Phase 84.4's UAT timing (84.3 + 84.4 ship together for this end-to-end verification to be meaningful per CONTEXT.md/ROADMAP).
3. Record pass/fail results in 84.3-05-UAT.md.
4. Update OPERATOR-GUIDE.md + CLAUDE.md with the new commands and operator-visible behavior.

Phase 84.3 cannot close (and Phase 84.4 cannot close) until both UATs complete jointly.

Purpose: Codify operator-observed behavior for the eight closures so future regressions are caught. The unit tests in Plans 01-04 prove the helpers work in isolation; this UAT proves the wrapper experience is operator-coherent on a real AWS account with real shell, real terragrunt, and a real sibling install present.

Output: 84.3-05-UAT.md with pass/fail per closure + joint-scenario result; OPERATOR-GUIDE.md Phase 84.3 §; CLAUDE.md updates.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-CONTEXT.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-RESEARCH.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-VALIDATION.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-01-PLAN.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-02-PLAN.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-03-PLAN.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-04-PLAN.md
@CLAUDE.md
@OPERATOR-GUIDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator UAT — eight per-closure scenarios + joint scenario (i)</name>
  <what-built>
    Plans 01-04 implemented all eight wrapper-level closures (a-h):
    - (a) `km configure` HeadBucket-checks state_bucket and re-prompts on 403 with account-ID-suffix suggestion
    - (b) `km bootstrap --shared-ses --dry-run=true` says "would run: terragrunt apply"
    - (c) `ExportTerragruntEnvVars` emits per-var WARN on env-vs-yaml drift to stderr
    - (d) `km init --plan` skips fresh-install downstream modules cleanly (efs) and exits 0
    - (e) `km configure` derives `${prefix}-artifacts-${account_id}` and refuses placeholder values at load
    - (f) `km bootstrap --all` exists AND `km configure` finale prints canonical sequence AND `km init` hard-fails on missing artifacts bucket
    - (g) `km env` subcommand prints `export KEY=value` block for the 11 KM_* superset; `eval $(km env)` works in bash/zsh
    - (h) `km bootstrap` status banner is authoritative — accounts.* yaml-authoritative for org/dns_parent/application; empty required IDs emit WARN
  </what-built>
  <how-to-verify>
    Operator runs the following on a real AWS account (klanker-maker-whereiskurt probe install is the recommended target — see ROADMAP Phase 84.4 § operator-visible probe state).

    Create `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md` from the template at `.planning/templates/UAT.md` (if exists) or freshly with the following structure. For each closure, record: command run, observed output (paste verbatim), pass/fail + reason.

    ## Closure (a) BOOTSTRAP-PREFLIGHT-BUCKET-CHECK
    1. Run `km configure` against the shared AWS account.
    2. When prompted for state_bucket, type a name known to be globally taken (e.g. `tf-state` or `s3`).
    3. Observe: km should HeadBucket the name, print `is taken` + `Suggestion: <name>-<account_id>` + `[Y / edit / abort]:` prompt.
    4. Test "Y" path → suggestion accepted, no error.
    5. Re-run, test "edit" path → freeform name accepted after HeadBucket succeeds.
    6. Re-run, test "abort" path → wizard exits cleanly with operator-aborted message.
    PASS: all three paths behave per spec.

    ## Closure (b) BOOTSTRAP-DRYRUN-TEXT-FIX
    1. `km bootstrap --shared-ses --dry-run=true` (or just `km bootstrap --shared-ses` — dry-run default).
    2. Observe stdout: must contain `Dry run — would run: terragrunt apply` and NOT contain `would run: terragrunt plan`.
    PASS: text says apply.

    ## Closure (c) ENV-CONFIG-DRIFT-WARN
    1. `export KM_REGION=us-west-2` in shell (where km-config.yaml says us-east-1).
    2. Run any command that invokes ExportTerragruntEnvVars (e.g. `km bootstrap --dry-run=true` or `km init --plan`).
    3. Observe stderr: contains `WARN: KM_REGION=us-west-2 (env) overrides km-config.yaml region=us-east-1`.
    4. Confirm: stdout NOT polluted (the WARN goes ONLY to stderr — important for `eval $(km env)` consumers).
    5. Repeat for KM_ARTIFACTS_BUCKET, KM_DOMAIN, KM_RESOURCE_PREFIX (each fires its own WARN).
    PASS: per-var WARN format matches; stdout clean; behavior unchanged (env still wins for setenv).

    ## Closure (d) PLAN-FRESH-INSTALL-OUTPUTS-HANDLING
    1. Clean a regional state directory (or use a fresh prefix install where network has never been applied).
    2. Run `km init --plan`.
    3. Observe: efs (and any other module with missing upstream outputs) prints `[skip] efs — depends on network/outputs.json (apply network first)`.
    4. Confirm: exit code 0.
    5. Apply network: `cd infra/live/<region>/network && terragrunt apply`.
    6. Re-run `km init --plan`.
    7. Observe: efs is now planned (no skip line).
    PASS: skip-on-missing → exit 0; plan-when-present → no skip.

    ## Closure (e) ARTIFACTS-BUCKET-DERIVATION
    1. Run `km configure` — accept defaults.
    2. After completion, read km-config.yaml: `artifacts_bucket` should be `<prefix>-artifacts-<your-account-id>` (real account ID).
    3. Manually edit km-config.yaml to set `artifacts_bucket: "<prefix>-artifacts-12345678"` (the angle-bracket placeholder).
    4. Run any km command that loads config (e.g. `km doctor` or `km info`).
    5. Observe: hard error with message "placeholder; re-run `km configure`".
    6. Repeat with `artifacts_bucket: "km-artifacts-12345"` (literal example value).
    7. Observe: same hard error.
    PASS: real derivation produces account-ID-suffixed name; both placeholder forms rejected at load.

    ## Closure (f) BOOTSTRAP-WORKFLOW-DISCOVERABILITY
    1. `./bin/km bootstrap --help` shows `--all` flag.
    2. `./bin/km bootstrap --all --shared-ses 2>&1` returns mutex error.
    3. `km bootstrap --all --dry-run=true` prints BOTH subflows' dry-run output in sequence.
    4. `km bootstrap --all --plan` runs Phase 84.2 plan-mode for BOTH subflows.
    5. `km bootstrap --all --dry-run=false` chains: foundation (SCP+KMS+artifacts) → foundation SES rule set.
    6. Without running --all: `rm -rf` the artifacts bucket (or pick a fresh install where it's not yet created). Run `km init --dry-run=false`. Observe hard-fail message naming both `km bootstrap --all` AND `km bootstrap --dry-run=false`.
    7. Re-run `km configure` — observe finale "Next steps:" block on stdout AND the same lines as `# ` header comments at the top of the new km-config.yaml.
    PASS: all six sub-criteria pass.

    ## Closure (g) KM-ENV-EXPORT-HELPER
    1. `km env --help` shows the command + `--aws-profile` flag.
    2. `km env` prints 11 lines of `export KM_*=value`; AWS_PROFILE NOT present.
    3. `km env --aws-profile` adds `export AWS_PROFILE=<current shell value>` line.
    4. In bash: `eval $(km env)` succeeds; `echo $KM_RESOURCE_PREFIX` shows configured prefix.
    5. In zsh: same eval test.
    6. After `eval $(km env)`, `terragrunt apply` in a fresh shell from `infra/live/<region>/network/` works against the correct account (proves the export block is sufficient for direct terragrunt invocation).
    PASS: all five sub-criteria pass.

    ## Closure (h) CONFIG-DISPLAY-VS-YAML-AUTHORITY
    1. With `accounts.organization`, `accounts.dns_parent`, `accounts.application` all populated in km-config.yaml, set `export KM_ACCOUNTS_ORGANIZATION=999999999999` in shell.
    2. Run `km bootstrap --dry-run=true`.
    3. Observe banner: shows yaml-configured `accounts.organization` value (NOT the env-shadowed one).
    4. Confirm: drift WARN from (c) fires on stderr for KM_ACCOUNTS_ORGANIZATION.
    5. Now set `accounts.organization: ""` (empty) in yaml. Re-run `km bootstrap --dry-run=true`.
    6. Observe stderr: `WARN: accounts.organization is empty in km-config.yaml — required for this command` (or the locked-format equivalent).
    7. Set `export KM_ACCOUNTS_TERRAFORM=555555555555` — yaml says `444444444444`. Run any command. Observe: env wins for accounts.terraform (asymmetry preserved per CONTEXT.md).
    PASS: yaml wins for the 3 locked keys; env wins for accounts.terraform; empty-yaml WARN fires.

    ## Joint Scenario (i) — coordinated with Phase 84.4
    This is the third-install end-to-end UAT. Sequencing:
    1. Phase 84.4's UAT plan must be ready (or executing in parallel — coordinate with 84.4 operator).
    2. On a clean AWS account, with NO prior km install:
       - `git clone` the repo (fresh checkout).
       - Run `km configure` (uses Plan 02 finale → derived artifacts bucket, HeadBucket-checked state bucket).
       - Run `km bootstrap --all --plan` (uses Plan 03 --all + Phase 84.2 gate).
       - Run `km bootstrap --all --dry-run=false`.
       - Run `km init --plan` (uses Plan 04 outputs.json skip probe).
       - Run `km init --dry-run=false`.
       - Run `km create profiles/learn.yaml` — proves the install actually FUNCTIONS, not just bootstraps. This step is the joint criterion with Phase 84.4 (which fixed module-level hard-coded `km-` prefixes that previously broke the second-prefix runtime).
       - Run `km destroy <sandbox-id>` → must succeed.
       - Run `km uninit --dry-run=false` → must cleanly tear down regional infra.
       - Run `km unbootstrap --dry-run=false` → must cleanly tear down foundation.
    3. Cross-record results in BOTH 84.3-05-UAT.md AND Phase 84.4's UAT plan.
    PASS: end-to-end clean from configure → sandbox-launch → teardown with no manual hand-holding required.
  </how-to-verify>
  <resume-signal>
    Operator records pass/fail per closure in 84.3-05-UAT.md. Reply with one of:
    - `approved` (all eight closures + joint scenario PASS)
    - `partial-pass: <closure-letter(s)>` (some passed, some need re-work — list failures + diagnoses)
    - `blocked: <reason>` (cannot proceed; e.g. Phase 84.4 UAT not ready, AWS account access issue)
  </resume-signal>
  <files>.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md (created by operator during this checkpoint; pasted observations + pass/fail per closure)</files>
  <action>NONE FOR CLAUDE — this is an operator-driven UAT. Claude waits for the operator to execute the scenarios in <how-to-verify> against real AWS and record results in 84.3-05-UAT.md.</action>
  <verify>Operator replies with one of the <resume-signal> values after recording results.</verify>
  <done>84.3-05-UAT.md committed to git with pass/fail recorded for all eight closures (a-h) AND joint scenario (i); operator replied `approved` or `partial-pass: ...`.</done>
</task>

<task type="auto">
  <name>Task 2: Update OPERATOR-GUIDE.md and CLAUDE.md with Phase 84.3 documentation</name>
  <files>
    OPERATOR-GUIDE.md,
    CLAUDE.md
  </files>
  <action>
    1. Read OPERATOR-GUIDE.md to find the existing Phase 84 / Phase 84.1 / Phase 84.2 § structure.
    2. Add a new § titled `Phase 84.3 wrapper-level UX` (sibling of 84.1 and 84.2 sections). Cover, in operator-task-flow order:
       - **Configure-time changes:**
         - HeadBucket-checked state_bucket with retry/edit/abort UX (closure a)
         - Auto-derived artifacts bucket from account ID (closure e)
         - Hard rejection of placeholder values at load (closure e)
         - `Next steps:` finale + yaml header comments showing canonical bootstrap sequence (closure f)
         - Shell-env conflict WARN at configure time (closure h-shell)
       - **Bootstrap-time changes:**
         - `km bootstrap --all` chain — recommended primary command (closure f)
         - Mutex with `--shared-ses` (with example error message)
         - `--all --plan` honors Phase 84.2 destroy-class gate (cross-link to Phase 84.2 §)
         - Dry-run text now correctly says "terragrunt apply" (closure b)
         - Status banner WARNs on empty required account IDs (closure h-banner)
       - **Init-time changes:**
         - Per-var WARN on env-vs-yaml drift (closure c) — interpretation guide + when it's safe to ignore vs when it indicates a real bug
         - `km init --plan` skips fresh-install modules with missing upstream outputs (closure d) — table of which modules have upstream dependencies (today: efs depends on network)
         - Hard-fail on missing artifacts bucket — recovery command in error message (closure f init-side)
       - **New command — `km env` (closure g):**
         - `km env` prints exportable block; `eval $(km env)` workflow for direct terragrunt invocation
         - `--aws-profile` opt-in; default excludes operator-shell-local AWS_PROFILE
         - Use cases: recovering from partial bootstrap, manual terragrunt import, debugging cfg-vs-env precedence
       - **Behavior change — accounts.* yaml-authoritative (closure h):**
         - `accounts.organization`, `accounts.dns_parent`, `accounts.application`: yaml wins; KM_ACCOUNTS_* exported to terragrunt subprocesses but not read into cfg
         - `accounts.terraform`: env-precedence preserved (asymmetry — env wins) with rationale
       - **Cross-reference:** Phase 84.4 owns module-level hard-coded `km-` prefix fixes — link forward.

    3. Update CLAUDE.md:
       - In the `## CLI` block: add `km env` entry (e.g., `km env [--aws-profile] — print exportable KM_* env block for use with eval $(km env)`).
       - In the `## CLI` block: update `km bootstrap` entry to mention `--all`.
       - Add a `## Phase 84.3: Wrapper-level bootstrap UX` § after the existing Phase 84.2 § with the same level of detail (3-5 paragraphs, list closures, point at OPERATOR-GUIDE.md).
       - Update the `Where to look` table if there's a new natural row (e.g. "Run a second/third km install in a shared AWS account" → "OPERATOR-GUIDE.md § Phase 84.3 + § Phase 84.4 (when 84.4 lands)").

    4. `make build` (sanity — no doc-only build break).
    5. Sanity-grep: `grep -n "km env\|--all" CLAUDE.md OPERATOR-GUIDE.md | head -20`.
  </action>
  <verify>
    <automated>grep -q "Phase 84.3" OPERATOR-GUIDE.md && grep -q "km env" CLAUDE.md && grep -q "km bootstrap --all" CLAUDE.md && make build</automated>
  </verify>
  <done>OPERATOR-GUIDE.md has a Phase 84.3 § documenting all eight closures and the new operator-visible surfaces. CLAUDE.md lists km env in the CLI block and has a Phase 84.3 §. Build still works. Docs committed to git.</done>
</task>

</tasks>

<verification>
- `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md` exists with pass/fail recorded for all eight closures + joint scenario.
- `grep "Phase 84.3" OPERATOR-GUIDE.md | wc -l` ≥ 1.
- `grep "km env" CLAUDE.md | wc -l` ≥ 1.
- `grep "km bootstrap --all" CLAUDE.md | wc -l` ≥ 1.
- Operator UAT result: approved (or partial-pass with documented closures to revisit).
- Phase 84.4 UAT coordinated with the joint scenario.
- All changes (UAT + docs) committed to git.
</verification>

<success_criteria>
- All eight wrapper-level closures (a-h) verified operationally on a real AWS account — not just in unit tests.
- Joint scenario (i) verified end-to-end: clone → configure → bootstrap → init → sandbox-create → teardown succeeds on a fresh prefix install (joint with Phase 84.4).
- OPERATOR-GUIDE.md operator runbook complete; CLAUDE.md surface updated.
- Phase 84.3 closed: all 8 requirement IDs marked complete in REQUIREMENTS.md traceability table.
</success_criteria>

<output>
After completion, create `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-SUMMARY.md` documenting:
- UAT result per closure (a)-(h) with operator's pass/fail + any quirks.
- Joint scenario (i) result; cross-link to Phase 84.4's UAT plan.
- Documentation diff summary (which OPERATOR-GUIDE.md/CLAUDE.md sections changed).
- Phase 84.3 close-out: all 8 requirements complete, REQUIREMENTS.md updated to mark them Complete.
- If any closure regressed under UAT (operator reported partial-pass), document the gap + recommended `/gsd:plan-phase 84.3 --gaps` next step.
</output>
