---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
verified: 2026-05-17T02:00:00Z
re_verified: 2026-05-17T22:00:00Z (operator-UAT on klanker-maker-kph)
status: passed
score: 6/6 automated must-haves verified; 2/6 human_needed items operator-validated; 4/6 designated operator-only by design
re_verification: true
operator_uat_findings:
  - commit: f83dbdf
    type: diagnostic
    description: "fix(init): surface terragrunt show -json stderr on parse-fail — planModule was swallowing *exec.ExitError.Stderr, leaving operators with bare 'exit status 1'. Now the actual terragrunt error appears indented under the warning."
  - commit: 3ba1991
    type: bugfix
    description: "fix(slack-bridge): allow mock_outputs during terragrunt show — lambda-slack-bridge is the only module with `dependency` blocks; its mock_outputs_allowed_terraform_commands omitted 'show', breaking the destroy-class gate on fresh installs. Phase 84.4-shaped (module-level) but absorbed here as it was found during 84.3 operator UAT."
operator_validated_2026-05-17:
  - closure: f.4
    test: "km bootstrap --all --plan honors Phase 84.2 destroy-class gate"
    evidence: "Live on klanker-maker-kph: --all --plan reports `0 to add, 0 to change, 0 to destroy ✓` across 1 module on clean foundation; gate correctly tripped during init parse-fail (before fix 3ba1991), passed cleanly after fix."
  - closure: i (plan-and-verify path)
    test: "Joint third-install end-to-end with Phase 84.4 — plan path"
    evidence: "Live on klanker-maker-kph (resource_prefix=whereiskurt, same account as primary km install): full `km init --plan` succeeds: 16 modules planned, `Total across 16 modules: 95 to add, 0 to change, 0 to destroy`. Apply path (95-resource creation) still operator-deferred."
human_verification_remaining:
  - test: "Walk km configure Y/edit/abort bucket-check paths (closure a)"
    why_human: "Interactive stdin against a globally-taken bucket name — cannot be driven non-interactively."
  - test: "km init --plan on a fresh install with missing network/outputs.json (closure d)"
    why_human: "Different scenario than the kph parse-fail (mock_outputs whitelist). Requires a state where network has never been applied. Not exercised by today's operator UAT."
  - test: "km bootstrap --all --dry-run=false applies both foundation and shared SES (closure f.5)"
    why_human: "Destructive — operator decision."
  - test: "km configure finale Next steps + yaml header (closure f.7)"
    why_human: "Interactive tty output, not capturable by non-interactive seam."
---

# Phase 84.3: Second-install Bootstrap UX — Gap-Closure Re-Verification Report

**Phase Goal:** Make the `km configure` → `km bootstrap` → `km init` operator path safe and discoverable for a second install in the same AWS account. Eight UX closures (a–h) plus joint scenario (i) with Phase 84.4: HeadBucket retry on configure (a), dry-run text fix (b), env-config drift WARN (c), fresh-install outputs skip (d), artifacts bucket derivation and placeholder rejection (e), bootstrap workflow discoverability including `--all` flag and init hard-fail (f), `km env` export helper (g), config display vs yaml authority (h).

**Verified:** 2026-05-17 (automated re-verification); 2026-05-17 (operator-UAT on klanker-maker-kph)
**Status:** passed — all automated checks pass; closures f.4 and i (plan path) operator-validated; closures a, d, f.5, f.7 designated operator-only by design (interactive/destructive)
**Re-verification:** Yes — initial automated check produced `human_needed`; operator UAT on klanker-maker-kph closed f.4 and i (plan path), surfacing two latent bugs (`f83dbdf`, `3ba1991`) that are now fixed.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `KM_REGION=us-west-2` with yaml `region=us-east-1`: drift WARN appears in stderr after `km bootstrap --shared-ses --dry-run=true` | VERIFIED | `cfg.YAMLDefaults` snapshot exists in `config.go:357-366`; `ExportTerragruntEnvVars` reads `YAMLDefaults` at `init.go:773-779`; test `TestConfigLoad_DriftWarn_KM_REGION` PASS; test `TestExportTerragruntEnvVars_DriftWarn/KM_REGION_drift_emits_WARN` PASS |
| 2 | `KM_ACCOUNTS_ORGANIZATION` set, yaml differs: drift WARN appears in stderr after `km bootstrap --dry-run=true` (default path) | VERIFIED | `runBootstrap` calls `ExportTerragruntEnvVars(loadedCfg)` at `bootstrap.go:1317`; test `TestRunBootstrap_DriftWarn_KM_REGION` PASS; test `TestRunBootstrap_DriftWarn_MissingExportCall` PASS |
| 3 | `km-config.yaml` with placeholder `artifacts_bucket`: `config.Load()` returns error with 'placeholder' in message | VERIFIED | `isPlaceholderBucket` defined at `config.go:459`; called from `config.Load()` at `config.go:441`; returns error with exact text; tests `TestConfigLoad_RejectsPlaceholderArtifactsBucket` and `TestConfigLoad_RejectsAngleBracketPlaceholder` PASS |
| 4 | `km init --plan` with placeholder `artifacts_bucket`: fails fast with error before any plan output | VERIFIED | `validateArtifactsBucket` called in `runInitPlan` at `init.go:1131` and `runInitPlanWithWriter` at `init.go:1171`; both call before any terragrunt output; test `TestInitPlan_HardFailsOnPlaceholderBucket` PASS |
| 5 | `84.3-05-UAT.md` updated: closures (c), (e), (f.6), (h) changed from PARTIAL PASS/FAIL to PASS with evidence | VERIFIED | UAT frontmatter shows `status: closed_with_deferred`; all four closure sections contain `gap-closure: PASS` text; commit `2d77bc3` confirmed in git log |
| 6 | `REQUIREMENTS.md` updated: all four Phase 84.3 synthetic IDs marked Complete | VERIFIED | `REQUIREMENTS.md:418-421` contains all four IDs (`ENV-CONFIG-DRIFT-WARN`, `ARTIFACTS-BUCKET-DERIVATION`, `BOOTSTRAP-WORKFLOW-DISCOVERABILITY`, `CONFIG-DISPLAY-VS-YAML-AUTHORITY`) each with `Complete` status |

**Score:** 6/6 automated must-haves verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/app/config/config.go` | `YAMLDefaults map[string]string` field + `isPlaceholderBucket` helper + call in `Load()` | VERIFIED | Field at line 200; `isPlaceholderBucket` at line 459; call at line 441 — all substantive and wired |
| `internal/app/cmd/init.go` | `ExportTerragruntEnvVars` reads `cfg.YAMLDefaults` for drift comparison; `validateArtifactsBucket` called in `runInitPlan` and `runInitPlanWithWriter` | VERIFIED | `YAMLDefaults` reads at lines 773-779, 820-835; `validateArtifactsBucket` at lines 1131, 1171 |
| `internal/app/cmd/bootstrap.go` | `runBootstrap` calls `ExportTerragruntEnvVars(loadedCfg)` after `loadBootstrapConfig` | VERIFIED | Call at line 1317, immediately after `loadBootstrapConfig` result at line 1312 |
| `internal/app/config/config_load_drift_test.go` | Tests for drift WARN on env-bound keys | VERIFIED | `TestConfigLoad_DriftWarn_KM_REGION`, `TestConfigLoad_DriftWarn_KM_ARTIFACTS_BUCKET`, `TestConfigLoad_NoDriftWarn_*` — all PASS |
| `internal/app/config/load_validate_test.go` | Tests for placeholder rejection in `config.Load()` | VERIFIED | `TestConfigLoad_RejectsPlaceholderArtifactsBucket`, `TestConfigLoad_RejectsAngleBracketPlaceholder`, `TestConfigLoad_AcceptsRealBucket`, `TestConfigLoad_AcceptsEmptyBucket` — all PASS |
| `internal/app/cmd/bootstrap_drift_warn_test.go` | Tests for drift WARN on default `km bootstrap` path | VERIFIED | `TestRunBootstrap_DriftWarn_MissingExportCall`, `TestRunBootstrap_DriftWarn_KM_REGION`, `TestRunBootstrap_NoDriftWarn_WhenEnvMatchesConfig` — all PASS |
| `internal/app/cmd/init_validate_84_3_test.go` | Tests for `km init --plan` hard-fail on placeholder bucket | VERIFIED | `TestInitPlan_HardFailsOnPlaceholderBucket`, `TestInitPlan_PassesWithValidBucket` — all PASS |
| `.planning/phases/84.3-.../84.3-05-UAT.md` | Status updated to `closed_with_deferred`; closures (c)(e)(f.6)(h) show PASS | VERIFIED | Frontmatter `status: closed_with_deferred`; all four closure sections contain `gap-closure: PASS` |
| `.planning/REQUIREMENTS.md` | Phase 84.3 synthetic IDs section with all four IDs marked Complete | VERIFIED | Lines 418-421 contain all four IDs with `Complete` status |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `config.Load()` | `isPlaceholderBucket` | Call at `config.go:441` before returning `cfg` | WIRED | `isPlaceholderBucket` is defined and called in the same file; any command loading config now hits this gate |
| `config.Load()` | `YAMLDefaults` snapshot | Snapshot built at `config.go:357-366` from `v2` before `cfg` fields are baked with env values | WIRED | Snapshot covers 8 env-bound keys; stored in `cfg.YAMLDefaults` |
| `ExportTerragruntEnvVars` | `cfg.YAMLDefaults` | Reads `YAMLDefaults[yamlKey]` at `init.go:773-779` for drift comparison | WIRED | Drift comparison now uses pre-env-bake snapshot, not `cfgVal` (which would already equal the env value) |
| `runBootstrap` | `ExportTerragruntEnvVars` | Call at `bootstrap.go:1317` right after `loadBootstrapConfig` | WIRED | Covers the default `km bootstrap` path which previously never called `ExportTerragruntEnvVars` |
| `runInitPlan` | `validateArtifactsBucket` | Call at `init.go:1131` before any terragrunt output | WIRED | Defense-in-depth: also called in `runInitPlanWithWriter` at `init.go:1171` for test seam |
| `84.3-05-UAT.md` closure (c) | 84.3-07 gap closure | `gap-closure: PASS` text with binary evidence in closure section | VERIFIED | Evidence commands and grep counts embedded in UAT.md |
| `84.3-05-UAT.md` closure (e) | 84.3-09 gap closure | `gap-closure: PASS` text with binary evidence in closure section | VERIFIED | Evidence commands and grep counts embedded in UAT.md |
| `REQUIREMENTS.md` | All four Phase 84.3 synthetic IDs | Lines 418-421 in `## Phase 84.3 — Synthetic IDs` section | VERIFIED | All four IDs present and marked Complete |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ENV-CONFIG-DRIFT-WARN | 84.3-06, 84.3-07, 84.3-08, 84.3-10 | Drift WARN fires for env-bound keys (KM_REGION, KM_ARTIFACTS_BUCKET, etc.) via YAMLDefaults snapshot; also fires on default km bootstrap path | SATISFIED | `cfg.YAMLDefaults` snapshot in `config.go:357-366`; `ExportTerragruntEnvVars` reads snapshot at `init.go:773-779`; `runBootstrap` calls `ExportTerragruntEnvVars` at `bootstrap.go:1317`; 5 passing tests |
| ARTIFACTS-BUCKET-DERIVATION | 84.3-06, 84.3-09, 84.3-10 | Placeholder `artifacts_bucket` values rejected at `config.Load()` time | SATISFIED | `isPlaceholderBucket` at `config.go:459`; wired into `Load()` at `config.go:441`; 4 passing tests |
| BOOTSTRAP-WORKFLOW-DISCOVERABILITY | 84.3-06, 84.3-09, 84.3-10 | `km init --plan` hard-fails on placeholder bucket (f.6); `--all` flag chains both subflows; f.4/f.5/f.7 remain deferred | PARTIALLY SATISFIED (f.6 automated pass; f.4/f.5/f.7 deferred to operator) | `validateArtifactsBucket` in `runInitPlan` at `init.go:1131`; `TestInitPlan_HardFailsOnPlaceholderBucket` PASS; f.4/f.5/f.7 require live AWS or interactive stdin |
| CONFIG-DISPLAY-VS-YAML-AUTHORITY | 84.3-07, 84.3-08, 84.3-10 | Drift WARN fires on all bootstrap paths; yaml-authoritative keys win over env; empty-yaml emits banner WARN | SATISFIED | Both `runBootstrap` and `runBootstrapSharedSES` call `ExportTerragruntEnvVars`; yaml-authoritative key logic at `config.go:348`; `warnEmptyAccountIDs` at `bootstrap.go:1319`; passing tests |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `internal/app/config/config.go` | 443 | "placeholder" string in error message text | Info | This is correct — it is the user-facing error message, not a stub |

No blocker or warning anti-patterns found in the gap-closure files. The two "placeholder" grep hits in `config.go` are the implementation itself (error message content and a comment about the function's purpose), not stub indicators.

---

### Human Verification Required

#### 1. Closure (a) — HeadBucket retry wizard

**Test:** Run `km configure` interactively. When prompted for `state_bucket`, enter a globally-taken bucket name (e.g. `tf-state`). Walk all three paths: Y (accept suggestion), edit (type a new name), abort.
**Expected:** On 403 response: wizard prints "is taken", suggests `tf-state-{account_id}`, shows `[Y/edit/abort]:` prompt. Each path resolves cleanly.
**Why human:** Requires real stdin against a globally-taken bucket. Unit test contracts (`TestConfigureHeadBucket_403_AcceptsAccountIDSuffix` etc.) pass but the live interactive loop cannot be driven non-interactively.

#### 2. Closure (d) — Fresh-install outputs skip

**Test:** Move or delete `infra/live/use1/network/outputs.json`, then run `km init --plan`. Restore afterwards.
**Expected:** Plan output shows `[skip] efs — depends on network/outputs.json (apply network first)` and exits 0.
**Why human:** Requires a state where the network module has never been applied, or temporarily removing `outputs.json`. Cannot simulate without risk to the live install state.

#### 3. Closure (f.4) — `km bootstrap --all --plan` honors destroy-class gate

**Test:** Run `km bootstrap --all --plan` against a live AWS account with at least one destroy/replace of a protected resource type.
**Expected:** The plan gate trips and prints the protection block; `--i-accept-destroys` is the override.
**Why human:** Requires a real `terragrunt plan` surface. The gate logic is unit-tested but the trip condition requires actual plan output from AWS.

#### 4. Closure (f.5) — `km bootstrap --all --dry-run=false` applies both subflows

**Test:** Run `km bootstrap --all --dry-run=false` against a live AWS account with healthy credentials.
**Expected:** Both `=== Bootstrap: SCP + KMS + artifacts ===` and `=== Bootstrap: shared SES rule set ===` subflows complete; idempotent on re-run.
**Why human:** Destructive — cannot safely apply against the live shared install. Requires operator coordination with a test account.

#### 5. Closure (f.7) — `km configure` finale "Next steps:" block

**Test:** Run `km configure` through completion interactively.
**Expected:** stdout ends with a `Next steps:` section listing the three-command sequence; the generated `km-config.yaml` file starts with `# Next steps:` header comments.
**Why human:** Requires an interactive `km configure` session. The finale and header output appear on tty; the unit test `TestConfigure_FinaleBlock` verifies the logic but not the live file-write output.

#### 6. Closure (i) — Joint third-install end-to-end with Phase 84.4

**Test:** After Phase 84.4 ships, run `git clone → km configure → km bootstrap --all → km init → km create profiles/learn.yaml → km destroy → km uninit → km unbootstrap` against a fresh prefix (e.g. `whereiskurt` in `klanker-maker-kph`). Note: `klanker-maker-kph/km-config.yaml` currently has `artifacts_bucket: whereiskurt-artifacts-12345678` which will now be rejected by the gap-closure 84.3-09 placeholder check — re-run `km configure` there first.
**Expected:** All commands succeed for `resource_prefix != km`. No hard-coded `km-` collisions (this also exercises the Phase 84.4 module-level fixes).
**Why human:** Phase 84.4 not yet executed. Requires a live AWS account and may require clean-state teardown/recreate of the `klanker-maker-kph` install.

---

### Summary

**All four Phase 84.3 automated gaps are closed.** The gap-closure cycle (plans 84.3-06 through 84.3-09) produced:

1. `cfg.YAMLDefaults` — pre-env-bake snapshot of yaml values in `config.Load()`, enabling drift detection for env-bound keys that viper would otherwise mask.
2. `ExportTerragruntEnvVars` in `runBootstrap` — the default `km bootstrap` path now participates in drift detection alongside `runBootstrapSharedSES`.
3. `isPlaceholderBucket` wired into `config.Load()` — any command that loads configuration now fails fast on placeholder `artifacts_bucket` values before reaching terragrunt.
4. `validateArtifactsBucket` wired into `runInitPlan` and `runInitPlanWithWriter` — defense-in-depth for the `km init --plan` path.

The remaining deferred items (closures a, d, f.4, f.5, f.7, i) all require live interactive stdin or real AWS account operations that cannot be safely automated against the shared install. They are correctly documented as operator-only follow-up in `84.3-05-UAT.md`. These deferrals are structural (not regressions) and the phase correctly records `status: closed_with_deferred` in the UAT.

**Recommendation:** Phase 84.3 automated scope is complete. Operator should run `make build && km configure` to close (a) and (f.7), then schedule the joint (i) UAT once Phase 84.4 ships.

---

_Verified: 2026-05-17_
_Verifier: Claude (gsd-verifier)_
