---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 06
type: tdd
wave: 5
depends_on:
  - 84.3-01
  - 84.3-02
  - 84.3-03
  - 84.3-04
files_modified:
  - internal/app/config/config_load_drift_test.go
  - internal/app/cmd/bootstrap_drift_warn_test.go
  - internal/app/config/load_validate_test.go
autonomous: true
gap_closure: true
requirements:
  - ENV-CONFIG-DRIFT-WARN
  - ARTIFACTS-BUCKET-DERIVATION
  - BOOTSTRAP-WORKFLOW-DISCOVERABILITY
  - CONFIG-DISPLAY-VS-YAML-AUTHORITY

must_haves:
  truths:
    - "A test exists that calls config.Load() with KM_REGION in env and yaml region different, and asserts a WARN line on stderr — this test is RED against current code"
    - "A test exists that calls runBootstrap with KM_ACCOUNTS_ORGANIZATION in env, captures stderr, and asserts a WARN line — this test is RED against current code"
    - "A test exists that calls config.Load() with a placeholder artifacts_bucket in yaml and asserts an error is returned — this test is RED against current code"
    - "A test exists that confirms km init --plan path (via runInitPlanWithWriter or runInitPlan's validate step) hard-fails on placeholder bucket — RED against current code"
    - "All existing tests in Plans 01-04 remain GREEN (no regression from new test files)"
  artifacts:
    - path: "internal/app/config/config_load_drift_test.go"
      provides: "Integration test: config.Load() with env KM_REGION set emits drift WARN to stderr when yaml region differs"
      contains: "TestConfigLoad_DriftWarn_KM_REGION"
    - path: "internal/app/cmd/bootstrap_drift_warn_test.go"
      provides: "Integration test: runBootstrap missing ExportTerragruntEnvVars — drift WARN absent on that code path"
      contains: "TestRunBootstrap_DriftWarn_MissingExportCall"
    - path: "internal/app/config/load_validate_test.go"
      provides: "Integration test: config.Load() rejects placeholder artifacts_bucket; km init plan-path hard-fails"
      contains: "TestConfigLoad_RejectsPlaceholderArtifactsBucket"
  key_links:
    - from: "internal/app/config/config_load_drift_test.go"
      to: "internal/app/config/config.go::Load"
      via: "calls config.Load() after writing km-config.yaml to tmpdir and setting KM_REGION in env"
      pattern: "config\\.Load\\(\\)"
    - from: "internal/app/cmd/bootstrap_drift_warn_test.go"
      to: "internal/app/cmd/bootstrap.go::runBootstrap"
      via: "calls runBootstrap directly via RunBootstrapFunc test seam, captures stderr"
      pattern: "RunBootstrapFunc"
    - from: "internal/app/config/load_validate_test.go"
      to: "internal/app/config/config.go::Load"
      via: "calls config.Load() with km-config.yaml containing placeholder bucket"
      pattern: "config\\.Load\\(\\)"
---

<objective>
Create Wave 5 RED test scaffolding for the 4 UAT-identified gaps. Plans 07/08/09 will make these tests GREEN.

Unlike Plans 01-04's Wave 0 scaffolding (which referenced undefined symbols), these RED tests are RED at the ASSERTION level — the production symbols exist, but the behavior they test does not yet exist. The tests call real production code and assert outcomes that currently fail.

Purpose: Lock the gap-closure behavior contracts before implementation. Plans 07/08/09 become "make these assertions pass."

Output: Three new test files targeting the specific integration scenarios that the unit-level Plan 01 tests miss.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-CONTEXT.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-05-UAT.md
@CLAUDE.md
@internal/app/config/config.go
@internal/app/cmd/init.go
@internal/app/cmd/bootstrap.go
@internal/app/cmd/configure.go

<interfaces>
<!-- Production symbols exercised by these RED tests. ALL exist already (Plans 02-04 created them). -->
<!-- The tests fail at ASSERTION level (wrong behavior), not compile level (undefined symbol).    -->

From internal/app/config/config.go:
```go
func Load() (*Config, error)

type Config struct {
    PrimaryRegion   string
    ArtifactsBucket string
    // ... other fields
    YAMLDefaults map[string]string  // DOES NOT EXIST YET — Plan 07 adds this field
}
```

From internal/app/cmd/bootstrap.go:
```go
// RunBootstrapFunc is the test seam — use this in tests to call runBootstrap
var RunBootstrapFunc = runBootstrap

// Signature: func runBootstrap(ctx context.Context, cfg *config.Config, dryRun bool, w io.Writer) error
```

From internal/app/cmd/init.go:
```go
func ExportTerragruntEnvVars(cfg *config.Config)
func ensureArtifactsBucketExists(ctx context.Context, cfg *config.Config, _ io.Writer, s3client S3HeadBucketAPI) error
```

From internal/app/cmd/testhelpers_test.go (package cmd):
```go
func captureStderr(t *testing.T, fn func()) string
```

Note: config_load_drift_test.go and load_validate_test.go are in package config_test.
bootstrap_drift_warn_test.go is in package cmd.
For config_test files, stderr capture is done via os.Pipe() (no captureStderr helper in config package).
</interfaces>
</context>

<feature>
  <name>Gap closure RED test scaffolding — 4 integration-level assertions</name>
  <files>
    internal/app/config/config_load_drift_test.go,
    internal/app/cmd/bootstrap_drift_warn_test.go,
    internal/app/config/load_validate_test.go
  </files>
  <behavior>
    --- internal/app/config/config_load_drift_test.go (package config_test) ---

    TestConfigLoad_DriftWarn_KM_REGION (RED against current code):
    - Write km-config.yaml to t.TempDir() with `region: us-east-1`.
    - changeToDir(t, dir) so viper finds it.
    - t.Setenv("KM_REGION", "us-west-2").
    - Capture stderr via os.Pipe redirect before calling config.Load().
    - Call cfg, err := config.Load(). Assert err == nil.
    - Read captured stderr. Assert stderr contains "WARN: KM_REGION=us-west-2" AND "us-east-1".
    - WHY THIS IS RED: cfg.PrimaryRegion == "us-west-2" after Load() (viper baked env in).
      ExportTerragruntEnvVars then sees envVal == cfgVal → no WARN. Plan 07 adds
      YAMLDefaults snapshot so comparison uses yaml value instead.
    - Assert cfg.PrimaryRegion == "us-west-2" (env still wins — behavior unchanged).

    Note: config.Load() itself does NOT emit the WARN — ExportTerragruntEnvVars does.
    This test must call ExportTerragruntEnvVars(cfg) after Load() to trigger the WARN.
    The test asserts that after Load(), the cfg carries enough information (via YAMLDefaults)
    that ExportTerragruntEnvVars can detect the drift even though cfg.PrimaryRegion == env value.

    TestConfigLoad_DriftWarn_KM_ARTIFACTS_BUCKET (same pattern, different key):
    - yaml: artifacts_bucket: km-artifacts-123456789012
    - t.Setenv("KM_ARTIFACTS_BUCKET", "env-override-bucket")
    - After Load() + ExportTerragruntEnvVars(cfg): assert WARN line contains "KM_ARTIFACTS_BUCKET=env-override-bucket" AND "km-artifacts-123456789012".

    TestConfigLoad_NoDriftWarn_WhenNoEnvSet (negative — must stay GREEN before and after):
    - yaml: region: us-east-1
    - No KM_REGION in env.
    - After Load() + ExportTerragruntEnvVars(cfg): stderr contains no "WARN:" line for KM_REGION.

    TestConfigLoad_NoDriftWarn_WhenEnvMatchesYaml (negative):
    - yaml: region: us-east-1. t.Setenv("KM_REGION", "us-east-1").
    - After Load() + ExportTerragruntEnvVars(cfg): no WARN line (values match).

    --- internal/app/cmd/bootstrap_drift_warn_test.go (package cmd) ---

    TestRunBootstrap_DriftWarn_MissingExportCall (RED against current code):
    - t.Setenv("KM_ACCOUNTS_ORGANIZATION", "999999999999").
    - Build a minimal *config.Config with OrganizationAccountID = "111111111111" (yaml value).
    - Capture stderr. Call RunBootstrapFunc(ctx, cfg, true /* dryRun */, &bytes.Buffer{}).
    - Assert stderr contains "WARN: KM_ACCOUNTS_ORGANIZATION=999999999999".
    - WHY THIS IS RED: runBootstrap does NOT call ExportTerragruntEnvVars today.
      Only warnEmptyAccountIDs is called. Plan 08 adds the ExportTerragruntEnvVars call.

    TestRunBootstrap_DriftWarn_KM_REGION (same pattern for a top-level key):
    - t.Setenv("KM_REGION", "us-west-2").
    - cfg.PrimaryRegion = "us-east-1" AND cfg.YAMLDefaults["region"] = "us-east-1"
      (simulate post-Plan-07 cfg where snapshot is populated).
    - RunBootstrapFunc(ctx, cfg, true, &buf).
    - Assert stderr contains "WARN: KM_REGION=us-west-2".
    - Note: This test will compile against current code (cfg struct doesn't have YAMLDefaults yet).
      Mark as // TODO: enable after Plan 07 adds YAMLDefaults field, OR skip with t.Skip("needs Plan 07").
      Include the test but guard with t.Skip if the field doesn't compile.

    TestRunBootstrap_NoDriftWarn_WhenEnvMatchesConfig (negative):
    - t.Setenv("KM_ACCOUNTS_ORGANIZATION", "111111111111").
    - cfg.OrganizationAccountID = "111111111111".
    - RunBootstrapFunc(ctx, cfg, true, &buf). Assert no WARN line for KM_ACCOUNTS_ORGANIZATION.

    --- internal/app/config/load_validate_test.go (package config_test) ---

    TestConfigLoad_RejectsPlaceholderArtifactsBucket (RED against current code):
    - Write km-config.yaml with `artifacts_bucket: km-artifacts-12345`.
    - changeToDir(t, dir).
    - cfg, err := config.Load(). Assert err != nil.
    - Assert err.Error() contains "placeholder" OR "km-artifacts-12345".
    - WHY THIS IS RED: config.Load() currently returns nil error for any ArtifactsBucket value.
      Plan 09 wires validateArtifactsBucket into config.Load() for non-empty bucket values.

    TestConfigLoad_RejectsAngleBracketPlaceholder:
    - km-config.yaml: `artifacts_bucket: "<prefix>-artifacts-12345678"`
    - config.Load() returns non-nil error containing "placeholder".

    TestConfigLoad_AcceptsRealBucket:
    - km-config.yaml: `artifacts_bucket: km-artifacts-123456789012`
    - config.Load() returns nil error.

    TestConfigLoad_AcceptsEmptyBucket (fresh install / km configure path):
    - km-config.yaml: `artifacts_bucket: ""`  (or field absent)
    - config.Load() returns nil error (empty is OK — not a placeholder).
    - WHY: config.Load() must not fail for fresh installs or during km configure.

    TestInitPlan_FailsOnPlaceholderBucket (RED against current code — gaps 3+4):
    - This test lives in load_validate_test.go in package config_test BUT exercises
      the init plan path. Actually, create this in a new file internal/app/cmd/init_validate_84_3_test.go
      in package cmd instead. See NOTE below.

    NOTE: The init plan path test belongs in the cmd package where runInitPlanWithWriter lives.
    Split: load_validate_test.go covers config.Load() validation (gaps 3); a separate
    init_validate_84_3_test.go in package cmd covers runInitPlan validation (gap 4).

    TestInitPlan_HardFailsOnPlaceholderBucket (RED against current code, package cmd):
    - cfg with ArtifactsBucket = "km-artifacts-12345" (placeholder).
    - Call runInitPlanWithWriter(cfg, "klanker-terraform", "us-east-1", &buf, false, false).
    - Assert returned error != nil.
    - Assert error contains "placeholder" OR "km-artifacts-12345".
    - WHY RED: runInitPlan does not call validateArtifactsBucket today. Plan 09 adds the call.

    TestInitApply_HardFailsOnPlaceholderBucket is already covered by config.Load() validation
    (once Load() rejects placeholder, runInit can't even get a valid cfg). No separate test needed
    for the apply path beyond what config.Load() covers.
  </behavior>
  <implementation>
    Create the three test files (four files if the cmd-level init plan validation test is split out).
    All tests must be RED (assertion failures or compile errors) against current code.
    Verify RED state explicitly with `go test` before committing.

    Stderr capture pattern for config_test package (no captureStderr helper):
    ```go
    func captureStderrOS(t *testing.T, fn func()) string {
        t.Helper()
        r, w, _ := os.Pipe()
        old := os.Stderr
        os.Stderr = w
        fn()
        w.Close()
        os.Stderr = old
        var buf bytes.Buffer
        io.Copy(&buf, r)
        return buf.String()
    }
    ```
    Add this helper at top of config_load_drift_test.go (package config_test).

    For bootstrap_drift_warn_test.go: reuse captureStderr(t, fn) from testhelpers_test.go (package cmd).

    Commit after all 4 test files are RED: `test(84.3): add RED integration tests for 4 UAT gaps`
  </implementation>
</feature>

<verification>
- `go test ./internal/app/config/ -run 'TestConfigLoad_DriftWarn_KM_REGION' -v 2>&1 | grep -E 'FAIL|expected.*WARN'` — test fails with assertion.
- `go test ./internal/app/config/ -run 'TestConfigLoad_RejectsPlaceholderArtifactsBucket' -v 2>&1 | grep 'FAIL'` — test fails.
- `go test ./internal/app/cmd/ -run 'TestRunBootstrap_DriftWarn_MissingExportCall' -v 2>&1 | grep 'FAIL'` — test fails.
- `go test ./internal/app/cmd/ -run 'TestInitPlan_HardFailsOnPlaceholderBucket' -v 2>&1 | grep 'FAIL'` — test fails.
- `go test ./internal/app/cmd/ -run 'TestExportTerragruntEnvVars|TestRunInitPlan|TestBootstrap|TestConfig' -timeout 60s` — existing Plan 01-04 tests stay GREEN.
</verification>

<success_criteria>
- All 4 gap assertions are RED (FAIL) against current code.
- All previously GREEN tests stay GREEN (no regression).
- Test files committed with message: `test(84.3): add RED integration tests for 4 UAT gaps`
</success_criteria>

<output>
After completion, create `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-06-SUMMARY.md` documenting:
- The exact assertion failures (what each test expects vs. what current code produces).
- Which files were created and their line counts.
- Confirmation that existing tests remain GREEN.
</output>
