---
phase: 84.3
slug: second-install-bootstrap-ux-wrapper-level-fixes-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-17
---

# Phase 84.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Standard Go `testing` package (no testify for unit tests) |
| **Config file** | none — `go test ./...` discovers tests |
| **Quick run command** | `go test ./internal/app/cmd/... -run TestConfigure -timeout 30s` |
| **Full suite command** | `go test ./internal/app/cmd/... ./internal/app/config/... ./pkg/terragrunt/planreport/... -timeout 120s` |
| **Estimated runtime** | ~30s quick / ~120s full |

---

## Sampling Rate

- **After every task commit:** Run quick (`go test ./internal/app/cmd/... -run TestConfigure -timeout 30s`) — adapt `-run` filter to the surface under test.
- **After every plan wave:** Run full suite.
- **Before `/gsd:verify-work`:** Full suite must be green.
- **Max feedback latency:** 30 seconds for quick runs; 120 seconds for full suite.

---

## Per-Task Verification Map

> Wave / Plan / Task IDs are placeholders — planner fills these in PLAN frontmatter and re-emits the map in each PLAN.md's `validation:` section. Requirement → Test rows are locked from RESEARCH.md §Validation Architecture.

| Requirement | Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| BOOTSTRAP-PREFLIGHT-BUCKET-CHECK | HeadBucket 403 → re-prompt with `${name}-${account_id}` suggestion (1 retry then bail) | unit | `go test ./internal/app/cmd/ -run TestConfigureHeadBucket -v` | ❌ W0 (Wave 1) | ⬜ pending |
| BOOTSTRAP-PREFLIGHT-BUCKET-CHECK | HeadBucket 404 / nil → accepted name | unit | same | ❌ W0 (Wave 1) | ⬜ pending |
| BOOTSTRAP-PREFLIGHT-BUCKET-CHECK | Edit path → freeform re-prompt + re-check | unit | same | ❌ W0 (Wave 1) | ⬜ pending |
| BOOTSTRAP-DRYRUN-TEXT-FIX | Bootstrap dry-run output contains "would run: terragrunt apply" | unit | `go test ./internal/app/cmd/ -run TestBootstrapDryRun -v` | ✅ extend existing TestBootstrapDryRun at configure_test.go:276 | ⬜ pending |
| ENV-CONFIG-DRIFT-WARN | Drift WARN emitted to stderr when `KM_REGION` differs from `cfg.PrimaryRegion` | unit | `go test ./internal/app/cmd/ -run TestExportTerragruntEnvVars_DriftWarn -v` | ❌ W0 (Wave 3) | ⬜ pending |
| ENV-CONFIG-DRIFT-WARN | No WARN when env matches cfg | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| ENV-CONFIG-DRIFT-WARN | No WARN when env not set | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| PLAN-FRESH-INSTALL-OUTPUTS-HANDLING | efs skip when `network/outputs.json` absent; exit 0 | unit | `go test ./internal/app/cmd/ -run TestRunInitPlan_SkipsEFS_WhenNetworkOutputsMissing -v` | ❌ W0 (Wave 3) | ⬜ pending |
| PLAN-FRESH-INSTALL-OUTPUTS-HANDLING | Skip line `[skip: depends on <upstream>/outputs.json — apply <upstream> first]` appears in stdout | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| PLAN-FRESH-INSTALL-OUTPUTS-HANDLING | Skipped modules excluded from destroy-class gate count | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| ARTIFACTS-BUCKET-DERIVATION | configure derives `${prefix}-artifacts-${account_id}` | unit | `go test ./internal/app/cmd/ -run TestConfigure_DerivesArtifactsBucket -v` | ❌ W0 (Wave 1) | ⬜ pending |
| ARTIFACTS-BUCKET-DERIVATION | placeholder value (literal `<prefix>-artifacts-12345678` and `km-artifacts-12345`) rejected at load | unit | `go test ./internal/app/cmd/ -run TestConfigure_RejectsPlaceholder -v` | ❌ W0 (Wave 1) | ⬜ pending |
| BOOTSTRAP-WORKFLOW-DISCOVERABILITY | `km bootstrap --all` flag registered + chains `runBootstrap` then `runBootstrapSharedSES` | unit | `go test ./internal/app/cmd/ -run TestBootstrapAllFlagRegistered -v` | ❌ W0 (Wave 2) | ⬜ pending |
| BOOTSTRAP-WORKFLOW-DISCOVERABILITY | `--all --plan` plans both subflows (Phase 84.2 gate honored) | unit | same | ❌ W0 (Wave 2) | ⬜ pending |
| BOOTSTRAP-WORKFLOW-DISCOVERABILITY | configure finale stdout contains "Next steps:" block; same sequence written as yaml header comments | unit | `go test ./internal/app/cmd/ -run TestConfigure_FinaleBlock -v` | ❌ W0 (Wave 1) | ⬜ pending |
| BOOTSTRAP-WORKFLOW-DISCOVERABILITY | `runInit` hard-fails (not warns) when artifacts bucket missing; error message names `km bootstrap --all` and the granular alternative | unit | `go test ./internal/app/cmd/ -run TestRunInit_HardFailsMissingArtifactsBucket -v` | ❌ W0 (Wave 3) | ⬜ pending |
| KM-ENV-EXPORT-HELPER | `km env` prints `export KEY=value` lines for full `KM_*` superset | unit | `go test ./internal/app/cmd/ -run TestEnvCmd -v` | ❌ W0 (Wave 3) | ⬜ pending |
| KM-ENV-EXPORT-HELPER | `km env` output survives `eval` (no unescaped chars; deterministic ordering) | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| KM-ENV-EXPORT-HELPER | `km env --aws-profile` includes AWS_PROFILE line; default OMITS it | unit | same | ❌ W0 (Wave 3) | ⬜ pending |
| CONFIG-DISPLAY-VS-YAML-AUTHORITY | `accounts.organization/dns_parent/application` yaml values survive when `KM_ACCOUNTS_*` is set in shell | unit | `go test ./internal/app/config/ -run TestConfig_AccountsYamlAuthoritative -v` | ❌ W0 (Wave 1) | ⬜ pending |
| CONFIG-DISPLAY-VS-YAML-AUTHORITY | `KM_ACCOUNTS_*` still EXPORTED to terragrunt subprocesses by `ExportTerragruntEnvVars` | unit | `go test ./internal/app/cmd/ -run TestExportTerragruntEnvVars_AccountsStillExported -v` | ❌ W0 (Wave 3) | ⬜ pending |
| CONFIG-DISPLAY-VS-YAML-AUTHORITY | `accounts.terraform` preserves env-var precedence (asymmetry with the three locked keys) | unit | `go test ./internal/app/config/ -run TestConfig_AccountsTerraformEnvWins -v` | ❌ W0 (Wave 1) | ⬜ pending |
| CONFIG-DISPLAY-VS-YAML-AUTHORITY | `km bootstrap` banner WARNs to stderr on empty required account ID; cites yaml load path | unit | `go test ./internal/app/cmd/ -run TestBootstrapBannerWarnsMissingAccount -v` | ❌ W0 (Wave 2) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/app/cmd/configure_84_3_test.go` — stubs for BOOTSTRAP-PREFLIGHT-BUCKET-CHECK, ARTIFACTS-BUCKET-DERIVATION, BOOTSTRAP-WORKFLOW-DISCOVERABILITY (configure side)
- [ ] `internal/app/cmd/env_test.go` — stubs for KM-ENV-EXPORT-HELPER (3 cases)
- [ ] `internal/app/cmd/init_plan_test.go` extension — stubs for PLAN-FRESH-INSTALL-OUTPUTS-HANDLING
- [ ] `internal/app/cmd/init_test.go` (or `init_84_3_test.go`) extension — stubs for ENV-CONFIG-DRIFT-WARN + `TestRunInit_HardFailsMissingArtifactsBucket` + `TestExportTerragruntEnvVars_AccountsStillExported`
- [ ] `internal/app/config/config_test.go` extension — stubs for CONFIG-DISPLAY-VS-YAML-AUTHORITY (yaml authority + terraform asymmetry)
- [ ] `internal/app/cmd/bootstrap_test.go` extension — stubs for `TestBootstrapAllFlagRegistered`, `TestBootstrapBannerWarnsMissingAccount`, extension of existing `TestBootstrapDryRun`
- [ ] Mock seam clones: `s3HeadBucketAPI` interface + mock (clone from `KMSEnsureAPI` pattern in `bootstrap_kms_test.go`); reuse `STSCallerAPI` from `doctor.go:83`; reuse `captureStderr` / `captureStdout` from `testhelpers_test.go`.
- [ ] Framework install: none needed — `go test` already configured.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| Joint third-install UAT covering all eight closures end-to-end | (i) — phase closure criterion (i) | Requires fresh AWS account with sibling install already provisioned; multi-install runtime behavior is owned by Phase 84.4 module-hardening — 84.3 + 84.4 ship together for this UAT | Wave 4 of Phase 84.3 + corresponding Phase 84.4 wave: fresh `git clone` → `km configure` → `km bootstrap --all --dry-run=false` → `km init --plan` → `km init --dry-run=false`; verify each closure (a–h) fires/passes per CONTEXT.md acceptance lines (a)–(h). |
| Interactive HeadBucket re-prompt UX | BOOTSTRAP-PREFLIGHT-BUCKET-CHECK | Real-time TTY interaction with the wizard | Unit tests cover the branch logic; the only manual check is "does the prompt read naturally and accept Y/edit/abort responses." Use `km configure` against a deliberately-taken bucket name. |
| `eval $(km env)` in a real shell | KM-ENV-EXPORT-HELPER | Verifies no shell-quoting regressions across bash + zsh | Run `eval $(km env)` in bash, then zsh; spot-check `echo $KM_RESOURCE_PREFIX`. Unit tests verify output shape; this verifies shell semantics. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick) / 120s (full)
- [ ] `nyquist_compliant: true` set in frontmatter (after planner wires per-task validation entries)

**Approval:** pending
