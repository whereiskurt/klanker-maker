---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 03
subsystem: cli
tags: [go, tdd, wave2, bootstrap, dry-run, all-flag, banner-warn, test-seam]

# Dependency graph
requires:
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    plan: 01
    provides: Wave 0 RED test scaffolding for bootstrap closures (bootstrap_84_3_test.go)
  - phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
    plan: 02
    provides: yaml-authoritative cfg.AccountsOrganization/DNSParentAccountID/ApplicationAccountID — warnEmptyAccountIDs reads correctly post-Plan-02

provides:
  - runBootstrapAll (bootstrap.go): chains runBootstrap then runBootstrapSharedSES in order; propagates dryRun/plan/acceptDestroys; stops on subflow 1 error
  - warnEmptyAccountIDs (bootstrap.go): emits WARN per empty accounts.organization/dns_parent/application key; wired into runBootstrap banner block
  - RunBootstrapFunc/RunBootstrapSharedSESFunc/RunBootstrapAllFunc (bootstrap.go): package-level test seam vars
  - --all flag on km bootstrap: chains both subflows; mutually exclusive with --shared-ses
  - Dry-run text fix: runBootstrapSharedSES now says "would run: terragrunt apply" (was "plan")

affects:
  - Plans reading runBootstrap/runBootstrapSharedSES: both now called through test seam vars in routing block
  - 84.3-04-PLAN: env_84_3_stubs.go + init_84_3_stubs.go remain (Plans 03 does not touch them)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RunBootstrapFunc/RunBootstrapSharedSESFunc/RunBootstrapAllFunc test seam vars mirror Phase 84.2's RunBootstrapSharedSESPlanFunc pattern"
    - "runBootstrapAll wraps existing entry points without duplicating flag logic — honors plan vs dryRun branching at call site"
    - "warnEmptyAccountIDs reads loadedCfg (post-yaml-load) not the pre-load cfg passed to runBootstrap — values are yaml-authoritative after Plan 02"

key-files:
  created: []
  modified:
    - internal/app/cmd/bootstrap.go
  deleted:
    - internal/app/cmd/bootstrap_84_3_stubs.go

key-decisions:
  - "--all routing inserted before the existing sharedSES && plan check per RESEARCH.md Pitfall 5 — all+sharedSES mutex check is first"
  - "RunBootstrapSharedSESFunc declared as an adapter func literal (not a plain var = runBootstrapSharedSES) because the signature includes listerOverride which is not in the simple function type — the literal captures the call correctly"
  - "warnEmptyAccountIDs wired to os.Stderr (not the w io.Writer passed to runBootstrap) to keep stdout clean for pipe consumers per CONTEXT.md style guidance"
  - "accounts.terraform excluded from warnEmptyAccountIDs scope (env-precedence preserved per Plan 02)"
  - "Existing routing block calls updated to use RunBootstrapFunc/RunBootstrapSharedSESFunc so the test seam is active in both --all and single-subflow paths"

# Metrics
duration: 4min
completed: 2026-05-17
---

# Phase 84.3 Plan 03: bootstrap.go Closures b, f, h-banner Summary

**Dry-run text fix + --all chain flag + warnEmptyAccountIDs banner WARN + three test-seam vars — all 9 Plan 01 bootstrap_84_3_test.go tests GREEN, zero regression in bootstrap suite**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-17T21:39:18Z
- **Completed:** 2026-05-17T21:43:00Z
- **Tasks:** 2
- **Files modified:** 1 modified, 1 deleted

## Accomplishments

- Fixed bootstrap.go:407 dry-run text from "would run: terragrunt plan" to "would run: terragrunt apply" — matches the real apply path
- Implemented `warnEmptyAccountIDs(cfg, w)` helper that emits one WARN line per empty required accounts.* key (organization, dns_parent, application); wired into runBootstrap banner block at os.Stderr
- Added `--all` flag to `km bootstrap`; implemented `runBootstrapAll` that chains `runBootstrap` (SCP+KMS+artifacts) then `runBootstrapSharedSES` (shared SES rule set), honoring dryRun/plan/acceptDestroys symmetrically
- Added mutual exclusion: `--all` + `--shared-ses` returns "mutually exclusive" error
- Added three package-level test seam vars: `RunBootstrapFunc`, `RunBootstrapSharedSESFunc`, `RunBootstrapAllFunc`; updated routing block to call through them
- Deleted `bootstrap_84_3_stubs.go` — real implementations replace both stubs

## Task Commits

1. **Task 1: Fix dry-run text + add banner WARN (closures b, h-banner)** - `999d9ee` (feat)
2. **Task 2: Add --all flag + runBootstrapAll chain (closure f)** - `349456d` (feat)

## Files Created/Modified

- `/Users/khundeck/working/klankrmkr/internal/app/cmd/bootstrap.go` — Changes:
  - **Line 407**: `"would run: terragrunt plan"` → `"would run: terragrunt apply"` in runBootstrapSharedSES dry-run block
  - **Lines 619-663** (new): `RunBootstrapFunc`, `RunBootstrapSharedSESFunc`, `RunBootstrapAllFunc` var declarations + `runBootstrapAll` implementation
  - **Lines 851-867** (routing block): `--all`+`--shared-ses` mutex check first, then `--all`→`RunBootstrapAllFunc`, existing branches updated to call through test seam vars
  - **Lines 883-884** (new flag): `--all` BoolVar registered
  - **Lines 1271-1291** (new helper): `warnEmptyAccountIDs` function declaration
  - **Line 1303** (wire-in): `warnEmptyAccountIDs(loadedCfg, os.Stderr)` at top of runBootstrap banner block
- `/Users/khundeck/working/klankrmkr/internal/app/cmd/bootstrap_84_3_stubs.go` — DELETED

## Closure Status

- **(b) BOOTSTRAP-DRYRUN-TEXT-FIX** — GREEN (dry-run text fixed at bootstrap.go:407; TestBootstrapDryRun_SaysApply GREEN)
- **(f) BOOTSTRAP-WORKFLOW-DISCOVERABILITY (bootstrap-side)** — GREEN (--all flag + runBootstrapAll; TestBootstrapAllFlagRegistered, TestBootstrapAll_ChainsBothSubflows, TestBootstrapAll_PlanRespectsGate, TestBootstrapAll_MutexWithSharedSES all GREEN)
- **(h) CONFIG-DISPLAY-VS-YAML-AUTHORITY (banner-side)** — GREEN (warnEmptyAccountIDs + wire-in; TestBootstrapBannerWarnsMissingAccount 3 sub-tests GREEN)

## Test-Seam Var Contract (for future phases)

```go
var (
    RunBootstrapFunc          = runBootstrap
    RunBootstrapSharedSESFunc = func(...) error { return runBootstrapSharedSES(...) }
    RunBootstrapAllFunc       = runBootstrapAll
)
```

Tests can reassign these vars to intercept calls. Pattern mirrors `RunBootstrapSharedSESPlanFunc` (Phase 84.2) and `RunInitPlanFunc` (init.go). Plan 04 does NOT need to interact with these vars.

## warnEmptyAccountIDs — yaml-authority confirmation

`warnEmptyAccountIDs` reads from `loadedCfg` (the return value of `loadBootstrapConfig(cfg)` which calls `config.Load()`). After Plan 02's yaml-authoritative fix, `cfg.OrganizationAccountID`, `cfg.DNSParentAccountID`, and `cfg.ApplicationAccountID` are loaded from km-config.yaml regardless of `KM_ACCOUNTS_ORGANIZATION`/`KM_ACCOUNTS_DNS_PARENT`/`KM_ACCOUNTS_APPLICATION` shell env vars. The WARN will only fire when the yaml file itself lacks these values — not due to env-var shadowing.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- [x] `internal/app/cmd/bootstrap.go` — EXISTS with all changes
- [x] `internal/app/cmd/bootstrap_84_3_stubs.go` — DELETED
- [x] Task 1 commit 999d9ee — EXISTS
- [x] Task 2 commit 349456d — EXISTS
- [x] `go test ./internal/app/cmd/ -run 'TestBootstrap' -timeout 30s` — PASS (20 tests)
- [x] `grep -rn "would run: terragrunt plan" internal/app/cmd/*.go` — zero production hits
- [x] `grep -n "RunBootstrapFunc|RunBootstrapSharedSESFunc|RunBootstrapAllFunc" bootstrap.go` — 9 hits (>= 6 required)
- [x] `grep -n "warnEmptyAccountIDs" bootstrap.go` — 2 hits (declaration + wire-in)
- [x] `./km bootstrap --help | grep -- '--all'` — flag visible
- [x] `./km bootstrap --all --shared-ses` — returns "mutually exclusive" error

## Self-Check: PASSED

---
*Phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted*
*Completed: 2026-05-17*
