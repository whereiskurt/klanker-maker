---
phase: 84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/app/cmd/configure_84_3_test.go
  - internal/app/cmd/env_test.go
  - internal/app/cmd/init_84_3_test.go
  - internal/app/cmd/bootstrap_84_3_test.go
  - internal/app/config/config_84_3_test.go
  - internal/app/cmd/init_plan_test.go
autonomous: true
requirements:
  - BOOTSTRAP-PREFLIGHT-BUCKET-CHECK
  - BOOTSTRAP-DRYRUN-TEXT-FIX
  - ENV-CONFIG-DRIFT-WARN
  - PLAN-FRESH-INSTALL-OUTPUTS-HANDLING
  - ARTIFACTS-BUCKET-DERIVATION
  - BOOTSTRAP-WORKFLOW-DISCOVERABILITY
  - KM-ENV-EXPORT-HELPER
  - CONFIG-DISPLAY-VS-YAML-AUTHORITY
validation:
  framework: "Standard Go testing"
  quick_command: "go test ./internal/app/cmd/... -run '84_3' -timeout 30s"
  full_command: "go test ./internal/app/cmd/... ./internal/app/config/... -timeout 120s"
  per_task:
    - task: 1
      tests:
        - "TestConfigureHeadBucket (BOOTSTRAP-PREFLIGHT-BUCKET-CHECK)"
        - "TestConfigure_DerivesArtifactsBucket (ARTIFACTS-BUCKET-DERIVATION)"
        - "TestConfigure_RejectsPlaceholder (ARTIFACTS-BUCKET-DERIVATION)"
        - "TestConfigure_FinaleBlock (BOOTSTRAP-WORKFLOW-DISCOVERABILITY)"
        - "TestConfigure_ShellEnvDriftWarn (CONFIG-DISPLAY-VS-YAML-AUTHORITY configure-side)"
    - task: 2
      tests:
        - "TestEnvCmd_DefaultOutput (KM-ENV-EXPORT-HELPER)"
        - "TestEnvCmd_EvalSafe (KM-ENV-EXPORT-HELPER)"
        - "TestEnvCmd_AWSProfileFlag (KM-ENV-EXPORT-HELPER)"
        - "TestExportTerragruntEnvVars_DriftWarn (ENV-CONFIG-DRIFT-WARN)"
        - "TestExportTerragruntEnvVars_NoWarnOnMatch (ENV-CONFIG-DRIFT-WARN)"
        - "TestExportTerragruntEnvVars_NoWarnWhenUnset (ENV-CONFIG-DRIFT-WARN)"
        - "TestExportTerragruntEnvVars_AccountsStillExported (CONFIG-DISPLAY-VS-YAML-AUTHORITY)"
        - "TestRunInit_HardFailsMissingArtifactsBucket (BOOTSTRAP-WORKFLOW-DISCOVERABILITY init-side)"
        - "TestRunInitPlan_SkipsEFS_WhenNetworkOutputsMissing (PLAN-FRESH-INSTALL-OUTPUTS-HANDLING)"
    - task: 3
      tests:
        - "TestBootstrapDryRun (BOOTSTRAP-DRYRUN-TEXT-FIX — extend existing)"
        - "TestBootstrapAllFlagRegistered (BOOTSTRAP-WORKFLOW-DISCOVERABILITY)"
        - "TestBootstrapAll_PlanRespectsGate (BOOTSTRAP-WORKFLOW-DISCOVERABILITY)"
        - "TestBootstrapAll_MutexWithSharedSES (BOOTSTRAP-WORKFLOW-DISCOVERABILITY)"
        - "TestBootstrapBannerWarnsMissingAccount (CONFIG-DISPLAY-VS-YAML-AUTHORITY)"
        - "TestConfig_AccountsYamlAuthoritative (CONFIG-DISPLAY-VS-YAML-AUTHORITY)"
        - "TestConfig_AccountsTerraformEnvWins (CONFIG-DISPLAY-VS-YAML-AUTHORITY)"
must_haves:
  truths:
    - "All Wave 0 test files compile and fail with `undefined symbol` against production code that Plans 02-04 will create — never with framework / fixture-load errors"
    - "Every test case named in 84.3-VALIDATION.md Per-Task Verification Map exists by name and intent in this plan's test files"
    - "Plans 02, 03, and 04 can be GREEN-driven against the RED scaffolding without inventing new test cases"
    - "Mock seams (s3HeadBucketAPI clone of KMSEnsureAPI; STSCallerAPI reuse from doctor.go:83) are declared in the test files"
    - "Stdout/stderr capture uses captureStdout / captureStderr from testhelpers_test.go (package cmd, not cmd_test)"
  artifacts:
    - path: "internal/app/cmd/configure_84_3_test.go"
      provides: "RED tests for closures (a) HeadBucket retry, (e) artifacts derivation + placeholder rejection, configure-side of (f) finale, configure-side WARN of (h)"
      contains: "TestConfigureHeadBucket"
    - path: "internal/app/cmd/env_test.go"
      provides: "RED tests for closure (g) km env subcommand — 3 cases (default output, eval-safe, --aws-profile flag)"
      contains: "TestEnvCmd_DefaultOutput"
    - path: "internal/app/cmd/init_84_3_test.go"
      provides: "RED tests for closure (c) drift WARN, closure (f) hard-fail, accounts-still-exported invariant"
      contains: "TestExportTerragruntEnvVars_DriftWarn"
    - path: "internal/app/cmd/init_plan_test.go"
      provides: "Extended with RED tests for closure (d) outputs.json skip probe"
      contains: "TestRunInitPlan_SkipsEFS_WhenNetworkOutputsMissing"
    - path: "internal/app/cmd/bootstrap_84_3_test.go"
      provides: "RED tests for closure (b) dry-run text fix extension, closure (f) --all flag, closure (h) banner WARN"
      contains: "TestBootstrapAllFlagRegistered"
    - path: "internal/app/config/config_84_3_test.go"
      provides: "RED tests for closure (h) viper accounts.* yaml-authoritative AND accounts.terraform env-precedence asymmetry"
      contains: "TestConfig_AccountsYamlAuthoritative"
  key_links:
    - from: "internal/app/cmd/configure_84_3_test.go"
      to: "internal/app/cmd/testhelpers_test.go::captureStdout"
      via: "package cmd internal test helper"
      pattern: "captureStdout\\(t"
    - from: "internal/app/cmd/configure_84_3_test.go"
      to: "internal/app/cmd/bootstrap_kms_test.go::mockKMSEnsureClient"
      via: "clone the KMSEnsureAPI variadic mock pattern for new s3HeadBucketAPI / S3HeadBucketAPI interface"
      pattern: "var _ S3HeadBucketAPI = \\(\\*mockS3HeadBucketClient\\)\\(nil\\)"
    - from: "internal/app/cmd/init_84_3_test.go"
      to: "internal/app/cmd/testhelpers_test.go::captureStderr"
      via: "WARN goes to stderr per locked decision"
      pattern: "captureStderr\\(t"
    - from: "internal/app/cmd/env_test.go"
      to: "internal/app/cmd/info.go::NewInfoCmd"
      via: "km env follows km info file-per-command pattern; reuses cmd.OutOrStdout() for buffer injection"
      pattern: "NewEnvCmd\\(cfg\\)"
---

<objective>
Create the Wave 0 RED test scaffolding for Phase 84.3 per the Nyquist contract documented in `84.3-VALIDATION.md`. Every implementation task in Plans 02-04 will be driven by tests in this plan — without this scaffolding, downstream tasks have no automated verify command to point at.

This plan produces tests that reference production symbols that don't yet exist (closure-c drift WARN inside `ExportTerragruntEnvVars`, closure-d `outputs.json` skip probe inside `RunInitPlanWithRunner`, closure-e `deriveArtifactsBucket` + placeholder rejection in `configure.go` + `pkg/config`, closure-g `NewEnvCmd` + `runEnvExport`, closure-f `--all` routing + `runBootstrapAll`, closure-h viper accounts.* fix). Compilation MUST fail at the symbol level — that's GREEN-able RED.

Purpose: Lock the test-to-requirement coverage matrix from `84.3-VALIDATION.md` into actual Go test code before any production code lands. Plans 02-04 become "make these tests compile + pass" instead of "design tests after writing code."

Output: Six test files (one extension to existing `init_plan_test.go`, five new `*_84_3_test.go` files) that compile-fail in a controlled, documented way.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-CONTEXT.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-RESEARCH.md
@.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-VALIDATION.md
@CLAUDE.md
@internal/app/cmd/bootstrap_kms_test.go
@internal/app/cmd/doctor.go
@internal/app/cmd/info.go
@internal/app/cmd/testhelpers_test.go
@internal/app/cmd/init.go
@internal/app/cmd/bootstrap.go
@internal/app/cmd/configure.go
@internal/app/config/config.go

<interfaces>
<!-- These are the production symbols the Wave 0 tests will reference. -->
<!-- They do NOT exist yet — Plans 02-04 will create them. The tests are -->
<!-- written against this contract verbatim. Signatures in Claude's Discretion -->
<!-- per CONTEXT.md; signatures below are the recommended minimum surface. -->

NEW types/funcs the tests reference (Plan 02 will create in cmd/configure.go + config/config.go):
```go
// internal/app/cmd/configure.go
type S3HeadBucketAPI interface {
    HeadBucket(ctx context.Context, params *s3.HeadBucketInput, optFns ...func(*s3.Options)) (*s3.HeadBucketOutput, error)
}

// Variadic-last for test injection — clones bootstrap_kms_test.go's KMSEnsureAPI pattern
// Returns the final accepted bucket name, or error after 1 retry (per CONTEXT.md closure a).
func probeStateBucketInteractive(
    ctx context.Context,
    initialName, accountID string,
    in *bufio.Reader, out io.Writer,
    s3client ...S3HeadBucketAPI,
) (string, error)

func deriveArtifactsBucket(prefix, accountID string) string

// At-load placeholder check; rejects "<...>" angle-bracket values AND literal "km-artifacts-12345"
func validateArtifactsBucket(name string) error

// New helper printed at end of runConfigure + written as yaml header comments
func nextStepsBlock() string
```

```go
// internal/app/config/config.go
// New behavior — for these three keys, v2 (yaml) wins unconditionally when v2.IsSet
// accounts.terraform is INTENTIONALLY OMITTED (env-precedence preserved)
var accountsYamlAuthoritativeKeys = map[string]bool{
    "accounts.organization": true,
    "accounts.dns_parent":   true,
    "accounts.application":  true,
}
```

NEW types/funcs Plan 03 will create in cmd/bootstrap.go:
```go
// internal/app/cmd/bootstrap.go
// Flag added to existing NewBootstrapCmd (CONTEXT closure f)
// Routes to runBootstrapAll BEFORE the existing sharedSES check.
var allFlag bool

func runBootstrapAll(ctx context.Context, cfg *config.Config, dryRun, plan, acceptDestroys bool, w io.Writer) error

// Banner WARN — new helper added to runBootstrap header section (line ~1213-1236)
func warnEmptyAccountIDs(cfg *config.Config, w io.Writer)
```

NEW types/funcs Plan 04 will create in cmd/init.go + cmd/env.go:
```go
// internal/app/cmd/init.go
// Drift WARN inserted into ExportTerragruntEnvVars at init.go:736
// (closure c) — emits one line per drifted var BEFORE the existing setenv blocks.
// Signature unchanged; behavior change is internal.

// New hard-fail in runInit when artifacts bucket missing (closure f).
// Reuses ensureArtifactsBucket existence-probe logic but errors out instead of warning.
func ensureArtifactsBucketExists(ctx context.Context, cfg *config.Config) error

// init_plan loop probe (closure d) — file-existence check before planModule.
// Inserted in RunInitPlanWithRunner at init.go:1075-1085 zone, matching the existing
// `for _, envVar := range m.envReqs { skip }` pattern.
// Likely a small helper:
func upstreamOutputsExist(moduleDir string, upstreams []string) (missing []string)
```

```go
// internal/app/cmd/env.go  — NEW file, follows internal/app/cmd/info.go pattern
func NewEnvCmd(cfg *config.Config) *cobra.Command
func runEnvExport(cfg *config.Config, w io.Writer, includeAWSProfile bool) error
```

EXISTING helpers reused by these tests (do not redefine):
```go
// internal/app/cmd/testhelpers_test.go (package cmd)
func captureStdout(t *testing.T, fn func()) string
func captureStderr(t *testing.T, fn func()) string

// internal/app/cmd/doctor.go:83
type STSCallerAPI interface {
    GetCallerIdentity(ctx context.Context, params *sts.GetCallerIdentityInput, optFns ...func(*sts.Options)) (*sts.GetCallerIdentityOutput, error)
}

// internal/app/cmd/bootstrap_kms_test.go — mockKMSEnsureClient is the structural template
// for the new mockS3HeadBucketClient (variadic-last pattern).
```
</interfaces>

<!-- Style reminders (locked in CLAUDE.md + RESEARCH.md):
- Standard Go `testing` package (t.Errorf, t.Fatalf). NO testify.
- Use t.Setenv (NOT os.Setenv) inside tests so cleanup is automatic.
- package cmd for internal helpers (captureStdout/captureStderr live there).
- WARN format is exactly: "WARN: KM_REGION=us-west-2 (env) overrides km-config.yaml region=us-east-1\n"
- WARN goes to stderr; do NOT pollute stdout (eval $(km env) consumers).
-->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write configure_84_3_test.go (closures a, e, f-configure-side, h-configure-side)</name>
  <files>
    internal/app/cmd/configure_84_3_test.go
  </files>
  <behavior>
    All tests in `package cmd`. Reuse `captureStdout(t, func() { ... })` and `captureStderr(t, func() { ... })` from testhelpers_test.go.

    Mock setup (declare at top of file):
    - `type mockS3HeadBucketClient struct { headBucketFunc func(context.Context, *s3.HeadBucketInput, ...func(*s3.Options)) (*s3.HeadBucketOutput, error) }` with `HeadBucket(ctx, in, opts...)` method that calls `headBucketFunc`.
    - Compile-time assertion: `var _ S3HeadBucketAPI = (*mockS3HeadBucketClient)(nil)`.
    - Helper `func http403Err() error` returning an AWS SDK v2 error whose `errors.As(&smithyhttp.ResponseError)` extracts a `StatusCode == 403`.
    - Helper `func http404Err() error` likewise for 404.

    Tests:

    TestConfigureHeadBucket_403_AcceptsAccountIDSuffix:
    - Stdin: "y\n" (accept the suggested ${name}-${accountID} name).
    - Initial bucket name: "tf-km-state-us-east-1". Account ID: "123456789012".
    - Mock HeadBucket: first call (original name) returns 403; second call (suggested name) returns 404.
    - Call probeStateBucketInteractive(ctx, "tf-km-state-us-east-1", "123456789012", stdin, &stdout, mock).
    - Assert: returned name == "tf-km-state-us-east-1-123456789012", no error.
    - Assert: stdout contains "is taken" AND "Suggestion: tf-km-state-us-east-1-123456789012" AND "[Y / edit / abort]".

    TestConfigureHeadBucket_404_AcceptsOriginalName:
    - Mock HeadBucket: first call returns 404 (name available).
    - Call probeStateBucketInteractive(ctx, "tf-km-state-us-east-1", "123456789012", stdin_empty, &stdout, mock).
    - Assert: returned name == "tf-km-state-us-east-1", no error. Stdout does NOT contain "Suggestion".

    TestConfigureHeadBucket_Nil_AcceptsOriginalName:
    - Mock HeadBucket: returns nil error (bucket already owned by us — also acceptable per CONTEXT.md).
    - Same shape as 404 case. Returned name unchanged.

    TestConfigureHeadBucket_EditPath:
    - Stdin: "edit\ntf-state-custom\n".
    - Mock HeadBucket: call 1 (original) → 403; call 2 (custom) → 404.
    - Assert: returned name == "tf-state-custom".

    TestConfigureHeadBucket_Abort:
    - Stdin: "abort\n".
    - Mock HeadBucket: call 1 returns 403.
    - Assert: probeStateBucketInteractive returns non-nil error matching "aborted by operator".

    TestConfigureHeadBucket_RetryCapExceeded:
    - Stdin: "y\n" (accept suggestion).
    - Mock HeadBucket: every call returns 403 (auto-suffixed name ALSO taken — vanishingly rare).
    - Assert: returns error mentioning "--state-bucket" (per CONTEXT.md closure a bail message).

    TestConfigure_DerivesArtifactsBucket:
    - Call deriveArtifactsBucket("km", "123456789012").
    - Assert: returns "km-artifacts-123456789012".
    - Subcase: deriveArtifactsBucket("whereiskurt", "987654321098") → "whereiskurt-artifacts-987654321098".

    TestConfigure_RejectsPlaceholder (table-driven):
    - Cases:
      - "<prefix>-artifacts-12345678" → error contains "placeholder"
      - "<anything-in-angle-brackets>" → error contains "placeholder"
      - "km-artifacts-12345" → error contains "placeholder" (the literal km-config.example.yaml string)
      - "km-artifacts-123456789012" → no error (real value)
      - "whereiskurt-artifacts-987654321098" → no error
      - "" → error (empty also invalid OR documented to skip; planner's call — pick the spec from CONTEXT)
    - Use validateArtifactsBucket(name).

    TestConfigure_FinaleBlock:
    - Capture stdout while running just the finale-print step of runConfigure (or invoke nextStepsBlock() directly).
    - Assert: output contains literal "Next steps:" AND "km bootstrap --all --dry-run=false" AND "km init --plan" AND "km init --dry-run=false".
    - Also: when the wizard writes the generated km-config.yaml to a tmp dir, assert the yaml file's first non-blank lines are `# ` comments containing the same sequence (header-comment requirement from CONTEXT.md closure f).

    TestConfigure_ShellEnvDriftWarn:
    - t.Setenv("KM_REGION", "us-west-2").
    - Run the wizard step that writes region to yaml (target: us-east-1 — operator's input). Capture stderr.
    - Assert stderr contains: `WARN: KM_REGION=us-west-2 (env) overrides km-config.yaml region=us-east-1` (or the framed variant per CONTEXT.md: "exported in shell will shadow km-config.yaml in current session").
    - Assert wizard does NOT block — finale prints anyway.
    - Subcase: no env set → no WARN.
    - Subcase: env matches yaml value → no WARN (drift only when they differ).

    Use stdlib testing only. Use t.Setenv for env vars (auto-cleanup). All tests MUST compile-fail with "undefined: probeStateBucketInteractive" / "undefined: deriveArtifactsBucket" / "undefined: validateArtifactsBucket" / "undefined: nextStepsBlock" — that is the Wave 0 contract.
  </behavior>
  <action>
    1. Create internal/app/cmd/configure_84_3_test.go with `package cmd`.
    2. Imports: "bufio", "bytes", "context", "errors", "io", "os", "strings", "testing", AWS SDK s3 + smithyhttp.
    3. Declare mockS3HeadBucketClient + compile-time assertion + http403Err / http404Err helpers at top.
    4. Write the 11 tests listed above in TestConfigureHeadBucket_* + TestConfigure_DerivesArtifactsBucket + TestConfigure_RejectsPlaceholder (table-driven) + TestConfigure_FinaleBlock + TestConfigure_ShellEnvDriftWarn.
    5. Tests reference symbols (probeStateBucketInteractive, deriveArtifactsBucket, validateArtifactsBucket, nextStepsBlock, S3HeadBucketAPI) that DO NOT yet exist — Plan 02 creates them.
    6. Run `go build ./internal/app/cmd/...` — MUST fail with "undefined: probeStateBucketInteractive" or similar. Capture the failure to confirm RED at symbol level (not import / framework level).
    7. Run `go vet ./internal/app/cmd/...` — vet errors on the new file are acceptable (it references undefined symbols); vet must pass on the REST of internal/app/cmd unchanged.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestConfigureHeadBucket|TestConfigure_DerivesArtifactsBucket|TestConfigure_RejectsPlaceholder|TestConfigure_FinaleBlock|TestConfigure_ShellEnvDriftWarn' 2>&1 | grep -E 'undefined: (probeStateBucketInteractive|deriveArtifactsBucket|validateArtifactsBucket|nextStepsBlock|S3HeadBucketAPI)'</automated>
  </verify>
  <done>configure_84_3_test.go exists in package cmd; `go test` fails with `undefined: <symbol>` for each of the 5 expected symbols. No other test files in internal/app/cmd/ regress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write env_test.go + init_84_3_test.go + init_plan_test.go extension (closures c, d, f-init-side, g, h-export)</name>
  <files>
    internal/app/cmd/env_test.go,
    internal/app/cmd/init_84_3_test.go,
    internal/app/cmd/init_plan_test.go
  </files>
  <behavior>
    All tests in `package cmd`. Reuse captureStdout / captureStderr.

    --- internal/app/cmd/env_test.go (NEW file — KM-ENV-EXPORT-HELPER, 3 cases) ---

    TestEnvCmd_DefaultOutput:
    - Build cfg with sample values for the full superset: KM_RESOURCE_PREFIX, KM_REGION, KM_REGION_LABEL, KM_DOMAIN, KM_EMAIL_SUBDOMAIN, KM_ROUTE53_ZONE_ID, KM_ACCOUNTS_ORGANIZATION, KM_ACCOUNTS_DNS_PARENT, KM_ACCOUNTS_APPLICATION, KM_ARTIFACTS_BUCKET, KM_OPERATOR_EMAIL.
    - Call runEnvExport(cfg, &buf, false /* includeAWSProfile */).
    - Assert buf contains exactly 11 lines of the form `export KEY=value\n` — one per env var listed above. Order MUST be deterministic.
    - Assert AWS_PROFILE is NOT present.

    TestEnvCmd_EvalSafe:
    - Build cfg with values that contain no shell metacharacters (deferred — current km-config values are safe; if any value contains spaces/quotes, the planner must quote them — for Wave 0 the test asserts current contract: no embedded spaces/quotes in any value, and output has exact form `export KEY=value` without quotes).
    - Pipe runEnvExport output through `bash -c "eval ... && echo $KM_RESOURCE_PREFIX"` — assert echo prints the prefix value.
    - Skipped if no /bin/bash available (testing.Short or exec.LookPath check).

    TestEnvCmd_AWSProfileFlag:
    - t.Setenv("AWS_PROFILE", "klanker-terraform").
    - Call runEnvExport(cfg, &buf, true /* includeAWSProfile */).
    - Assert buf contains `export AWS_PROFILE=klanker-terraform`.
    - Default (false) subcase already covered above — DOES NOT include AWS_PROFILE.

    TestEnvCmd_Registration:
    - Call NewEnvCmd(cfg).
    - Assert returned cmd.Use == "env", cmd.Flags().Lookup("aws-profile") is non-nil with default false.

    --- internal/app/cmd/init_84_3_test.go (NEW file) ---

    ENV-CONFIG-DRIFT-WARN tests:

    TestExportTerragruntEnvVars_DriftWarn:
    - cfg.PrimaryRegion = "us-east-1". t.Setenv("KM_REGION", "us-west-2"). Capture stderr while running ExportTerragruntEnvVars(cfg).
    - Assert stderr contains: `WARN: KM_REGION=us-west-2 (env) overrides km-config.yaml region=us-east-1`.
    - Repeat for KM_ARTIFACTS_BUCKET / cfg.ArtifactsBucket, KM_DOMAIN / cfg.Domain, KM_RESOURCE_PREFIX / cfg.ResourcePrefix (one drift line per drifted var).
    - Assert os.Getenv("KM_REGION") is still "us-west-2" after the call (env-wins preserved per CONTEXT.md).

    TestExportTerragruntEnvVars_NoWarnOnMatch:
    - cfg.PrimaryRegion = "us-east-1". t.Setenv("KM_REGION", "us-east-1"). Capture stderr.
    - Assert stderr contains no "WARN:" line for KM_REGION.

    TestExportTerragruntEnvVars_NoWarnWhenUnset:
    - cfg.PrimaryRegion = "us-east-1". Ensure KM_REGION is unset (os.Unsetenv at top + t.Cleanup; or use t.Setenv("KM_REGION", "") if that semantically unsets).
    - Capture stderr; assert no WARN line for KM_REGION.

    TestExportTerragruntEnvVars_AccountsStillExported (CONFIG-DISPLAY-VS-YAML-AUTHORITY invariant):
    - cfg.AccountsOrganization = "111111111111", cfg.AccountsDNSParent = "222222222222", cfg.AccountsApplication = "333333333333".
    - Ensure KM_ACCOUNTS_* unset before call.
    - Call ExportTerragruntEnvVars(cfg).
    - Assert os.Getenv("KM_ACCOUNTS_ORGANIZATION") == "111111111111" (still exported to subprocesses).
    - Assert os.Getenv("KM_ACCOUNTS_DNS_PARENT") == "222222222222".
    - Assert os.Getenv("KM_ACCOUNTS_APPLICATION") == "333333333333".
    - This locks the invariant from CONTEXT.md closure h: yaml is authoritative for READS, but EXPORTS still happen.

    BOOTSTRAP-WORKFLOW-DISCOVERABILITY init-side test:

    TestRunInit_HardFailsMissingArtifactsBucket:
    - Stub the S3 HeadBucket via mockS3HeadBucketClient (defined in configure_84_3_test.go — but mocks are in the same package, so they're reachable).
    - cfg with ArtifactsBucket set to a name. Mock HeadBucket returns 404.
    - Call ensureArtifactsBucketExists(ctx, cfg) (or whatever helper Plan 04 names — the test names the contract).
    - Assert: returns error.
    - Assert error message contains "does not exist" AND "km bootstrap --all" AND "km bootstrap --shared-ses" (so operators using either workflow can recover, per CONTEXT.md closure f).
    - Assert error message contains the ACTUAL bucket name.

    --- internal/app/cmd/init_plan_test.go (EXTEND existing file) ---

    PLAN-FRESH-INSTALL-OUTPUTS-HANDLING tests. The existing init_plan_test.go has a `mockPlanRunner` — extend it OR add a sibling helper. Tests added (not the existing tests):

    TestRunInitPlan_SkipsEFS_WhenNetworkOutputsMissing:
    - tmpdir setup mirroring `infra/live/<regionLabel>/{network,efs}/terragrunt.hcl` layout. efs/terragrunt.hcl content includes `${get_terragrunt_dir()}/../network/outputs.json`.
    - DO NOT create network/outputs.json.
    - Call RunInitPlanWithRunner(ctx, cfg, runner, ...) — capture stdout.
    - Assert stdout contains `[skip` AND `network/outputs.json` AND `efs`.
    - Assert exit/return value indicates success (no error returned for skip-only path; gate not tripped).
    - Assert mockPlanRunner.planCalls does NOT include `efs` (probe ran before planModule).

    TestRunInitPlan_NoSkipWhenOutputsExist:
    - Same fixture, but create network/outputs.json (any valid JSON).
    - Call RunInitPlanWithRunner; assert mockPlanRunner.planCalls includes `efs`.

    TestRunInitPlan_SkippedModulesExcludedFromGate:
    - Same as TestRunInitPlan_SkipsEFS but one OTHER module (e.g. fictional `protected-module`) tries to destroy a protected type.
    - Assert: gate trips on `protected-module` only; skipped `efs` is NOT in the gate trip count.

    All three new tests use the same fixture pattern as the existing init_plan_test.go (which has mockPlanRunner + captureStdout). Tests MUST compile-fail because upstreamOutputsExist (or whatever Plan 04 names the helper) does not yet exist.
  </behavior>
  <action>
    1. Create internal/app/cmd/env_test.go with `package cmd`. Write the 4 TestEnvCmd_* tests.
    2. Create internal/app/cmd/init_84_3_test.go with `package cmd`. Write TestExportTerragruntEnvVars_DriftWarn / _NoWarnOnMatch / _NoWarnWhenUnset / _AccountsStillExported + TestRunInit_HardFailsMissingArtifactsBucket.
    3. Extend internal/app/cmd/init_plan_test.go (do NOT create a new file unless append makes the file >600 lines — read current size first; if so, create init_plan_84_3_test.go instead). Add the 3 PLAN-FRESH-INSTALL-OUTPUTS-HANDLING tests.
    4. All tests use t.Setenv for env-var mutation (auto-cleanup). All tests live in `package cmd`.
    5. Run `go test ./internal/app/cmd/ -run 'TestEnvCmd|TestExportTerragruntEnvVars_DriftWarn|TestRunInit_HardFails|TestRunInitPlan_Skips' 2>&1` — MUST fail with undefined-symbol errors for NewEnvCmd, runEnvExport, ensureArtifactsBucketExists, upstreamOutputsExist (or named equivalents).
    6. Verify existing init_plan_test.go tests still compile (the extension does not break them).
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestEnvCmd|TestExportTerragruntEnvVars_DriftWarn|TestRunInit_HardFailsMissingArtifactsBucket|TestRunInitPlan_SkipsEFS_WhenNetworkOutputsMissing|TestExportTerragruntEnvVars_AccountsStillExported' 2>&1 | grep -E 'undefined: (NewEnvCmd|runEnvExport|ensureArtifactsBucketExists|upstreamOutputsExist)'</automated>
  </verify>
  <done>env_test.go + init_84_3_test.go exist; init_plan_test.go (or init_plan_84_3_test.go) extended. `go test` fails with undefined-symbol errors for the targeted Plan-04 contract. No existing tests regress in compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write bootstrap_84_3_test.go + config_84_3_test.go (closures b, f-bootstrap-side, h)</name>
  <files>
    internal/app/cmd/bootstrap_84_3_test.go,
    internal/app/config/config_84_3_test.go
  </files>
  <behavior>
    --- internal/app/cmd/bootstrap_84_3_test.go (NEW file, package cmd) ---

    TestBootstrapDryRun_SaysApply (extends existing TestBootstrapDryRun at configure_test.go:276):
    - The existing TestBootstrapDryRun captures the runBootstrapSharedSES dry-run output. Add a new test (or table case) that asserts the captured text contains "would run: terragrunt apply" and does NOT contain "would run: terragrunt plan".
    - File this in bootstrap_84_3_test.go to keep the closure (b) acceptance criterion bundled with the other 84.3 tests; the existing TestBootstrapDryRun stays as-is.

    TestBootstrapAllFlagRegistered:
    - Call NewBootstrapCmd(cfg).
    - Assert: cmd.Flags().Lookup("all") is non-nil with default value false.
    - Assert: cmd.Flags().Lookup("shared-ses") still exists (no regression).
    - Assert: cmd.Flags().Lookup("plan") still exists (Phase 84.2 flag preserved).

    TestBootstrapAll_ChainsBothSubflows:
    - Stub runBootstrap and runBootstrapSharedSES via package-level vars (mirrors Phase 84.2's RunInitPlanFunc pattern — Plan 03 will add RunBootstrapFunc + RunBootstrapSharedSESFunc test-seam vars).
    - Call runBootstrapAll(ctx, cfg, dryRun=true, plan=false, acceptDestroys=false, &buf).
    - Assert: runBootstrap called first, runBootstrapSharedSES called second, both with the same dryRun arg.

    TestBootstrapAll_PlanRespectsGate:
    - Same setup, but plan=true. Mock runBootstrapSharedSES (the plan-mode variant) to return a destroy-class-gate trip error.
    - Call runBootstrapAll(ctx, cfg, false, true, false, &buf).
    - Assert: returns the gate-trip error; runBootstrap-plan ran first; the trip is from the SECOND subflow (per the chain order).

    TestBootstrapAll_MutexWithSharedSES:
    - --all and --shared-ses are mutually exclusive (per RESEARCH.md Open Question 3 + Pitfall 5).
    - Test the routing/PreRunE that rejects the combination. Assert error message: `"--all and --shared-ses are mutually exclusive; --all runs both subflows in order"`.

    TestBootstrapBannerWarnsMissingAccount:
    - cfg.AccountsOrganization = "" (empty after yaml load — simulates closure h pathological case).
    - cfg.AccountsApplication = "333333333333" (set).
    - Capture stderr while invoking the banner-print helper (Plan 03 will name it; per <interfaces> warnEmptyAccountIDs).
    - Assert stderr contains: `WARN:` AND `accounts.organization` AND `km-config.yaml` (cites the source).
    - Subcase: all three set → no WARN on stderr.
    - Subcase: all three empty → 3 separate WARN lines.

    --- internal/app/config/config_84_3_test.go (NEW file, package config) ---

    TestConfig_AccountsYamlAuthoritative:
    - Write a temp km-config.yaml with `accounts:\n  organization: "111111111111"\n  dns_parent: "222222222222"\n  application: "333333333333"\n  terraform: "444444444444"`.
    - t.Setenv("KM_ACCOUNTS_ORGANIZATION", "999999999999") — env shadow attempt.
    - t.Setenv("KM_ACCOUNTS_DNS_PARENT", "888888888888").
    - t.Setenv("KM_ACCOUNTS_APPLICATION", "777777777777").
    - Call config.Load(tmpdir) (or whatever the loader function is — check pkg/config/config.go).
    - Assert: cfg.AccountsOrganization == "111111111111" (yaml WINS — closure h fix).
    - Assert: cfg.AccountsDNSParent == "222222222222" (yaml wins).
    - Assert: cfg.AccountsApplication == "333333333333" (yaml wins).
    - This test MUST FAIL against current code (env wins via isSetByEnv at config.go:197); Plan 02 fixes it.

    TestConfig_AccountsTerraformEnvWins:
    - Same fixture EXCEPT t.Setenv("KM_ACCOUNTS_TERRAFORM", "555555555555").
    - yaml says "444444444444".
    - Assert: cfg.AccountsTerraform == "555555555555" (env wins — asymmetry per CONTEXT.md Open Question 1 + RESEARCH.md Pitfall 1).
    - This codifies that accounts.terraform is INTENTIONALLY OUTSIDE the yaml-authoritative set.

    TestConfig_NoEnvSet_YamlLoaded:
    - Smoke baseline. No KM_ACCOUNTS_* set. Assert all 4 yaml values load into cfg correctly.
  </behavior>
  <action>
    1. Create internal/app/cmd/bootstrap_84_3_test.go with `package cmd`. Imports: "bytes", "context", "testing", plus existing project imports. Write the 5 TestBootstrap* + TestBootstrapBannerWarnsMissingAccount tests above.
    2. Reference RunBootstrapFunc + RunBootstrapSharedSESFunc + RunBootstrapAllFunc package-level var seams that Plan 03 will create. Reference warnEmptyAccountIDs helper that Plan 03 will create.
    3. Create internal/app/config/config_84_3_test.go with `package config` (or whatever the actual package name in internal/app/config/config.go is — verify with `head -3` first).
    4. Imports for config test: "os", "path/filepath", "testing", plus the package itself (if package config, no self-import). Use t.TempDir() for the yaml fixture.
    5. Write the 3 TestConfig_* tests. TestConfig_AccountsYamlAuthoritative MUST fail against CURRENT code (closure h is exactly this bug); Plan 02 will fix.
    6. Run `go test ./internal/app/cmd/ -run 'TestBootstrap.*84|TestBootstrapAll|TestBootstrapBannerWarns' 2>&1` — MUST fail with undefined-symbol errors for RunBootstrapAllFunc / runBootstrapAll / warnEmptyAccountIDs.
    7. Run `go test ./internal/app/config/ -run 'TestConfig_Accounts' 2>&1` — TestConfig_AccountsYamlAuthoritative MUST fail with assertion (env shadows yaml — current bug). TestConfig_NoEnvSet_YamlLoaded SHOULD pass (baseline) if the loader is implemented correctly; if it fails for an unrelated reason that's a sign the fixture is wrong, not the production code.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestBootstrapAll|TestBootstrapBannerWarnsMissingAccount' 2>&1 | grep -E 'undefined: (RunBootstrapAllFunc|runBootstrapAll|warnEmptyAccountIDs)' && go test ./internal/app/config/ -run 'TestConfig_AccountsYamlAuthoritative' 2>&1 | grep -E 'FAIL|expected.*111111111111'</automated>
  </verify>
  <done>bootstrap_84_3_test.go + config_84_3_test.go exist. cmd tests fail with undefined-symbol errors; config TestConfig_AccountsYamlAuthoritative fails with assertion (the closure-h bug is now reproduced under test). config TestConfig_NoEnvSet_YamlLoaded passes (baseline).</done>
</task>

</tasks>

<verification>
- All six test files exist on disk.
- `go test ./internal/app/cmd/... 2>&1 | grep -c "undefined:"` reports >= 8 distinct undefined symbols (one per Plan 02/03/04 contract entry).
- `go test ./internal/app/config/... 2>&1 | grep "TestConfig_AccountsYamlAuthoritative" | grep -c "FAIL"` == 1 (the closure-h bug is now under test).
- No existing tests in the touched packages regress (run the full test suite minus the new tests via `-run` exclusion — or accept that the new compile failure stops the whole package; in that case `go vet` per file demonstrates non-regression).
</verification>

<success_criteria>
- Every requirement ID for Phase 84.3 has at least one Wave 0 test case that will drive its implementation.
- Wave 0 RED contract met: tests compile-fail with specific `undefined: <symbol>` errors, NOT with import / framework / fixture errors.
- Closure (h) yaml-authoritative bug is reproduced under test (TestConfig_AccountsYamlAuthoritative FAILS against current code — Plan 02 makes it pass).
- Mock seam declarations (mockS3HeadBucketClient, http403Err, http404Err) are in place for Plan 02's HeadBucket probe implementation.
- All test files committed to git on this branch with conventional commit message.
</success_criteria>

<output>
After completion, create `.planning/phases/84.3-second-install-bootstrap-ux-wrapper-level-fixes-inserted/84.3-01-SUMMARY.md` documenting:
- Wave 0 file inventory (6 files).
- The exact list of `undefined: <symbol>` errors that downstream plans must satisfy.
- Any deviations from the Wave 0 plan (e.g., if init_plan_test.go was already too large and we created init_plan_84_3_test.go instead).
- Mock seam summary (mockS3HeadBucketClient interface contract for Plan 02).
</output>
