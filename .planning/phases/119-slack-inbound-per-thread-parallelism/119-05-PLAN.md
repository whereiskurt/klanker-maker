---
phase: 119-slack-inbound-per-thread-parallelism
plan: 05
type: execute
wave: 4
depends_on: ["119-02", "119-03", "119-04"]
files_modified:
  - profiles/learn.v2.parallel.yaml
  - docs/slack-notifications.md
  - skills/slack/SKILL.md
  - CLAUDE.md
autonomous: false
requirements: [P119-B, P119-C, P119-D, P119-E, P119-F]

must_haves:
  truths:
    - "A demo profile (learn.v2.parallel.yaml) sets maxConcurrentThreads>1 and validates clean"
    - "The live synthetic-HMAC E2E proves: parallelism, per-thread ordering, cap enforcement, heartbeat-no-dup, dormant regression, dedup"
    - "docs/slack-notifications.md § Phase 119 documents the cap field, semantics, /workspace mutation caveat, and deploy surface"
    - "klanker:slack SKILL + CLAUDE.md reflect the new field and parallelism behaviour"
  artifacts:
    - path: "profiles/learn.v2.parallel.yaml"
      provides: "cap>1 demo profile"
      contains: "maxConcurrentThreads"
    - path: "docs/slack-notifications.md"
      provides: "Phase 119 operator section"
      contains: "## Phase 119"
    - path: "CLAUDE.md"
      provides: "Phase 119 summary entry"
      contains: "Phase 119"
  key_links:
    - from: "live E2E"
      to: "running per-sandbox poller"
      via: "synthetic HMAC /events POST (3 distinct thread_ts)"
      pattern: "x-slack-signature"
---

<objective>
Phase gate: the NON-OPTIONAL live synthetic-HMAC E2E (the concurrency logic is bash, invisible
to Go goldens — Research Pitfall 6 / project_skill_bash_needs_live_uat), a demo profile, and the
doc surfaces. Docs LAST so they describe shipped behaviour.

This plan is `autonomous: false` — the live E2E requires a deployed bridge + a running per-sandbox
sandbox (`make build-lambdas` + `km init` + `km create`), which is real AWS/Slack interaction the
executor drives but the operator confirms. Mirror Phase 118's UAT structure.

Purpose: prove P119-B/D/E/F at runtime + document P119-C and the operator contract.
Output: 1 demo profile + live E2E results + 3 doc updates.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/119-slack-inbound-per-thread-parallelism/119-CONTEXT.md
@.planning/phases/119-slack-inbound-per-thread-parallelism/119-RESEARCH.md
@.planning/phases/119-slack-inbound-per-thread-parallelism/119-VALIDATION.md

<interfaces>
Demo-profile precedent: profiles/learn.v2.private-allow.yaml (Phase 118 demo). Copy its shape; it
already has slack.enabled/perSandbox/inbound.enabled. Add maxConcurrentThreads: 3.

Live E2E technique (Phase 114 self-drive, memory project_slack_bridge_inbound_e2e_and_status_attr):
- Bridge URL: `km slack status` → bridge-url (or SSM /km/slack/bridge-url).
- Signing secret: SSM /km/slack/signing-secret --with-decryption.
- bot_user_id: SSM /km/slack/bot-user-id (for <@...> in mention-only channels).
- Sign: base string `v0:{ts}:{body}`; header `x-slack-signature: v0=<openssl dgst -sha256 -hmac "$SECRET">`;
  `x-slack-request-timestamp` within 300s; POST to {bridge-url}/events.
- Bot-loop bypass (events_handler.go:632): body with bot_id ABSENT, empty subtype, user=U0HUMANTEST01 PASSES.
- Observe: `km agent list <sb>` (overlapping run timestamps), `km otel <sb> --timeline`,
  `km logs <sb> --follow`, `/workspace/.km-agent/runs/*/output.json` (distinct RUN_DIRs prove the
  -$$-$RANDOM suffix), `conversations.replies` for ordered replies.
- Harness caveat: a fabricated ts is not a real Slack message → the 👀 reaction + threaded reply may
  fail with message_not_found/thread_not_found — HARMLESS; verify via side effects, not the reaction.

Docs: docs/slack-notifications.md ends with "## Phase 118 — ..." (:2807) — append "## Phase 119".
klanker:slack SKILL.md inbound section (~:170). CLAUDE.md phase-summary block (top, after Phase 118).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo profile + build/deploy</name>
  <files>profiles/learn.v2.parallel.yaml</files>
  <action>
Create profiles/learn.v2.parallel.yaml by copying profiles/learn.v2.private-allow.yaml's structure
(slack.enabled:true, perSandbox:true, inbound.enabled:true) and setting
`notification.slack.inbound.maxConcurrentThreads: 3`. Keep TTL/idle lean. Add a header comment
explaining it demos Phase-119 per-thread parallelism and the /workspace shared-mutation caveat
(parallel turns share /workspace — suited for conversational / read-mostly fan-out, NOT concurrent
repo-mutating turns).

Validate it (operator-side binary): `make build` then `km validate profiles/learn.v2.parallel.yaml`
(expect exit 0, no WARN since perSandbox+inbound.enabled are set). Also run
scripts/validate-all-profiles.sh to confirm no regression.

Then deploy for the E2E (Task 2): `make build-lambdas` + `km init --dry-run=false` (renders the new
poller userdata; NOT --sidecars). Bridge change rides the same apply (or `km init --slack`).
  </action>
  <verify>
    <automated>make build && km validate profiles/learn.v2.parallel.yaml && bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>Demo profile validates clean (no WARN); all profiles still validate; lambdas built + init applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live synthetic-HMAC E2E (NON-OPTIONAL) — assertions 1–6</name>
  <files>/tmp/km119_send_event.sh, .planning/phases/119-slack-inbound-per-thread-parallelism/119-UAT.md</files>
  <action>Write the Phase-114-style signed-/events helper and drive the six assertions below; this is the executor automation that precedes the operator confirmation. Record evidence in 119-UAT.md.</action>
  <verify><automated>test -f .planning/phases/119-slack-inbound-per-thread-parallelism/119-UAT.md</automated></verify>
  <done>All six live assertions pass with evidence captured in 119-UAT.md.</done>
  <what-built>
A deployed Phase-119 bridge (threadTS grouping) + a per-sandbox sandbox created from
profiles/learn.v2.parallel.yaml (cap=3 poller). The executor writes a Phase-114-style helper
/tmp/km119_send_event.sh that signs + POSTs synthetic event_callback bodies with distinct thread_ts
to the sandbox's channel, then drives the six validation assertions.
  </what-built>
  <how-to-verify>
Executor: create the sandbox (`km create profiles/learn.v2.parallel.yaml --alias km119test`),
discover its channel id, write /tmp/km119_send_event.sh (sign with SSM signing-secret; bot-loop-safe
body; prompt that reliably takes ~30–60s e.g. "count slowly to 30 then summarise"), and run the six
assertions, capturing evidence into 119-UAT.md:

1. PARALLELISM (P119-B): fire 3 distinct-thread_ts events simultaneously → `km agent list <sb>` shows
   3 OVERLAPPING runs; wall-clock ≈ max(turn), not sum. Distinct RUN_DIRs (proves RUN_ID suffix).
2. PER-THREAD ORDERING (P119-D): 2 back-to-back events in ONE thread → 2nd run starts only AFTER the
   1st completes; `conversations.replies` shows ordered replies.
3. CAP ENFORCEMENT (P119-E): fire 5 distinct threads against cap=3 → never >3 concurrent runs
   (count overlapping RUN_DIRs / `km agent list` timestamps).
4. HEARTBEAT / NO-DUP (P119-E/F): one turn longer than the base visibility (or against an existing
   30s queue) → EXACTLY ONE reply; poller journal (`km logs <sb> --follow`) shows ChangeMessageVisibility
   ticks; no redelivered second run.
5. DORMANT REGRESSION (dormancy): create a cap=1 sandbox (plain learn.v2.yaml) → inbound is SERIAL,
   identical to Phase 118; no KM_SLACK_MAX_CONCURRENCY line in its /etc/km/notify.env.
6. DEDUP (P119-F): replay the SAME event_id/event_ts → single-processed (bridge nonce dedup
   events_handler.go:392 + the poller last_processed_event_ts guard); exactly one reply, one run.

Harness caveat: verify via side effects (RUN_DIRs, DDB row, conversations.replies), NOT the 👀 reaction
(fabricated ts → message_not_found is harmless). Record commands + outputs in
.planning/phases/119-slack-inbound-per-thread-parallelism/119-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" once all six assertions pass (evidence in 119-UAT.md), or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Docs — slack-notifications.md § Phase 119, klanker:slack SKILL, CLAUDE.md</name>
  <files>docs/slack-notifications.md, skills/slack/SKILL.md, CLAUDE.md</files>
  <action>
Append "## Phase 119 — Slack inbound per-thread parallelism" to docs/slack-notifications.md (after the
Phase 118 section at :2807). Document, grounded in shipped behaviour:
- The two layers: bridge groups by threadTS (FIFO ⇒ parallel-across-threads / serial-within-thread);
  poller bounded-concurrent dispatch via KM_SLACK_MAX_CONCURRENCY.
- The field `notification.slack.inbound.maxConcurrentThreads` (*int, default 1 = serial = dormant);
  emit-only-when->1; poller-only (no DDB attr); km validate WARNS cap>1 without perSandbox+inbound.enabled.
- Ack-after-completion + visibility heartbeat (base raised to 1800s for NEW queues; heartbeat covers
  existing 30s queues); the crash-redelivery dup window + last_processed_event_ts idempotency guard.
- **The /workspace shared-mutation caveat**: parallel turns share /workspace — suited for conversational /
  read-mostly fan-out; concurrent repo-mutating turns are operator responsibility (per-thread worktree
  isolation is a deferred follow-up).
- Deploy surface: bridge → `make build-lambdas` + `km init --slack`; profile field + poller →
  `make build-lambdas` + `km init --dry-run=false` (NOT --sidecars); existing sandboxes need
  `km destroy && km create`; queue base raise applies to new queues only. No apiVersion bump.
- A pointer to profiles/learn.v2.parallel.yaml.

Update skills/slack/SKILL.md inbound section (~:170) with a short note on maxConcurrentThreads + the
serial-within-thread / parallel-across-threads guarantee. If this changes user-facing skill content,
bump the klanker plugin version in .claude-plugin/plugin.json + marketplace.json (memory
project_plugin_version_gates_cache) — note it in the SUMMARY.

Add a "Phase 119 (2026-06-25) — ..." summary block near the top of CLAUDE.md (after the Phase 118
block) and a "Where to look" row pointing at docs/slack-notifications.md § Phase 119, mirroring the
Phase 118 entry's density (locked decisions, deploy line, dormancy note).
  </action>
  <verify>
    <automated>grep -q "## Phase 119" docs/slack-notifications.md && grep -q "Phase 119" CLAUDE.md && grep -q "maxConcurrentThreads" docs/slack-notifications.md && echo OK</automated>
  </verify>
  <done>Phase 119 documented in slack-notifications.md, klanker:slack SKILL, and CLAUDE.md; deploy + /workspace caveat covered; plugin version bumped if skill content changed.</done>
</task>

</tasks>

<verification>
- `make build && km validate profiles/learn.v2.parallel.yaml` exit 0; scripts/validate-all-profiles.sh green.
- Live E2E: all six assertions pass with evidence recorded in 119-UAT.md (NON-OPTIONAL gate).
- Docs grep checks pass; CLAUDE.md + skill reflect the field.
- `go test ./... -count=1 -timeout 600s` GREEN (full-suite gate before /gsd:verify-work; capture the
  command's own exit code, not a piped tail — memory feedback_check_go_test_exit_not_pipe).
</verification>

<success_criteria>
- Demo profile validates clean (no WARN).
- The live synthetic-HMAC E2E proves parallelism, per-thread ordering, cap enforcement,
  heartbeat-no-dup, dormant regression, and dedup (the bash concurrency is invisible to Go goldens —
  this is the real gate).
- Operator docs describe the cap field, semantics, /workspace caveat, idempotency guard, and the exact
  deploy surface.
</success_criteria>

<output>
After completion, create `.planning/phases/119-slack-inbound-per-thread-parallelism/119-05-SUMMARY.md`
(and 119-UAT.md with the live-E2E evidence).
</output>
