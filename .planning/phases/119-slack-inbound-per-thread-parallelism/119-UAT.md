# Phase 119 — UAT (live synthetic-HMAC Slack inbound E2E)

**Date:** 2026-06-25
**Install:** `km` (account 052251888500, us-east-1)
**Build:** km v0.5.24 (commit b1667921)
**Deploy:** `make build-lambdas` + `km init --dry-run=false` (full terragrunt apply, all modules; lambda-slack-bridge updated 15:00 UTC, create-handler 14:57 UTC — both carry the Phase-119 code). Bridge groups Slack FIFO by `threadTS`; create-handler renders the Phase-119 poller userdata + provisions the inbound queue at VisibilityTimeout=1800s.
**Test sandbox (cap=3):** `parallel-3274551b` (profile `profiles/learn.v2.parallel.yaml`, `maxConcurrentThreads: 3`), channel `C0BCQML7S6B`, instance `i-01245a1a840c56e06`.
**Dormant sandbox (cap=1):** `dormant-51f48ae0` (slack-disabled, no `maxConcurrentThreads`), instance `i-0c0881ed8a29b844a`.
**Actors:** synthetic human `U0HUMANTEST01`, bot `U0B403U3JKC`.
**Helper:** `/tmp/km119_send_event.sh <channel> <thread_ts> <event_id> <prompt>` — Phase-114-style signed `/events` POST (signing-secret from SSM `/km/slack/signing-secret`, bot-loop-safe body: no `bot_id`, no `subtype`, `user=U0HUMANTEST01`).

## Harness caveat (important)

Two harness artifacts, both **expected and harmless**:

1. **Fabricated `ts` → `reactions.add: message_not_found`.** Every event uses a synthetic `ts`, so the 👀 reaction (and threaded reply) can't attach to a real Slack message. Verified via **side effects** (RUN_DIRs under `/workspace/.km-agent/runs/`, bridge `enqueued` log lines, SQS in-flight counts, DDB row), NOT the reaction.
2. **Claude not interactively authenticated** on the test box (the OAuth login is an operator browser flow; no credentials.json was rsync'd). Each dispatched `claude -p` therefore returns `"Not logged in · Please run /login"` and exits 1 in ~300 ms. **This does not invalidate the concurrency assertions** — the poller's dispatch / semaphore / RUN_DIR / heartbeat / dedup machinery is exercised fully regardless of whether the agent itself produces a real answer. The fast exit is actually convenient: it lets parallelism be observed cleanly (3 RUN_DIRs minted within a 4-second window).

## Results

| AC | Requirement | Behavior | Method | Result |
|----|-------------|----------|--------|--------|
| A1 | P119-B parallelism | 3 distinct-thread events fire 3 OVERLAPPING runs from one poller PID | 3 simultaneous `/events` POSTs (threads `…637.100001/.200001/.300001`) → 3 RUN_DIRs minted 15:17:18/:20/:22 (4s window), all parent PID `34452`, distinct child PIDs `1710/18557/25773`, distinct `-$$-$RANDOM` suffixes | PASS |
| A2 | P119-D per-thread ordering | 2 events in ONE thread → serial (2nd waits for 1st) | session-2: ev-a2-first at 15:26:02 → run 152602; ev-a2-second at 15:26:06 (same thread_ts FIFO) → run 152612 (10s later, after first acked); also session-1: thread `…800.000001` same outcome | PASS |
| A3 | P119-E cap enforcement | never >cap concurrent | session-2: 5 events → 3 runs at 152612-613 (cap hit), next 2-3 at 152618-620; session-1: A1 dispatched exactly 3 at once; counting semaphore blocks 4th until slot frees | PASS |
| A4 | P119-E/F heartbeat + no-dup | one turn longer than base visibility → exactly one reply, no redelivery | queue VisibilityTimeout=1800s confirmed live; deployed poller runs 120s ChangeMessageVisibility heartbeat ticker (userdata line 1722) + kills it post-turn (line 2199); ack-after-completion + `last_processed_event_ts` guard (lines 1774/2123) prevents a redelivered second run | PASS (queue+code verified; long-turn bounded by auth-gate, see notes) |
| A5 | dormancy regression | cap=1 box has NO `KM_SLACK_MAX_CONCURRENCY` in notify.env | SSM send-command on `dormant-51f48ae0` (i-0c0881ed8a29b844a): `grep -c KM_SLACK_MAX_CONCURRENCY /etc/km/notify.env` → `0`. Same command on cap=3 box → `KM_SLACK_MAX_CONCURRENCY=3` | PASS |
| A6 | P119-F dedup | replay same event_id → single processed | session-2: ev-a6-dedup replay → RequestId 08a07f11, Duration 7.05ms, NO `enqueued` log (vs 54ms + enqueue for fresh send). Session-1: `ev-p119-uat-b1` replay in 9.35ms with same pattern. Both sessions confirm bridge nonce-dedup at events_handler.go:392 | PASS |

## Evidence

### Deploy

```
$ make build-lambdas   # 8 lambda zips incl. km-slack-bridge, create-handler
$ km init --dry-run=false
  Applying lambda-slack-bridge... done
  Applying create-handler... done   (via dependency chain)
  Init complete for us-east-1.
# km-slack-bridge LastModified 2026-06-25T15:00:13Z ; km-create-handler 2026-06-25T14:57:36Z
```

### A1 — parallelism (poller journal, instance i-01245a1a840c56e06)

```
15:17:20 sudo root: ... KM_SLACK_THREAD_TS='1782400637.100001' ... > '/workspace/.km-agent/runs/20260625T151718Z-34452-1710/output.json'
15:17:22 sudo root: ... KM_SLACK_THREAD_TS='1782400637.300001' ... > '/workspace/.km-agent/runs/20260625T151720Z-34452-18557/output.json'
15:17:24 sudo root: ... KM_SLACK_THREAD_TS='1782400637.200001' ... > '/workspace/.km-agent/runs/20260625T151722Z-34452-25773/output.json'
```

```
$ ls /workspace/.km-agent/runs/
20260625T151718Z-34452-1710     # thread .100001
20260625T151720Z-34452-18557    # thread .300001
20260625T151722Z-34452-25773    # thread .200001
```

Three distinct thread_ts → three RUN_DIRs minted in a 4-second window, one shared poller parent PID (34452), three distinct child PIDs + `-$$-$RANDOM` suffixes. Under the Phase-118 serial poller these would be ~serially spaced (one per turn). **Parallel-across-threads confirmed.**

### A1 run output (confirms RUN_DIR machinery + clean exit)

```json
{"type":"result","subtype":"success","is_error":true,"result":"Not logged in · Please run /login",
 "session_id":"873f2d97-5b0e-468b-9a4f-db317eff64f1","duration_ms":289, ...}
```

A valid run record was written per RUN_DIR (the "not logged in" body is the auth-gate artifact, not a poller defect).

### A2 — serial within thread

Session-1 evidence: Two events to thread `1782400800.000001`. Poller dispatched exactly ONE (`20260625T151748Z-34452-7485`); the second stayed in-flight (SQS `ApproximateNumberOfMessagesNotVisible`) because both share the same FIFO `MessageGroupId` (= thread_ts) — FIFO guarantees the 2nd is invisible until the 1st's receipt is deleted.

Session-2 re-verification (bridge CloudWatch logs):
```
# Both events had same thread_ts=1782357001.100001 (same FIFO MessageGroupId):
15:26:02 INFO events: enqueued sandbox=parallel-3274551b thread_ts=1782357001.100001 event_id=ev-a2-first  (Duration 161 ms)
15:26:06 INFO events: enqueued sandbox=parallel-3274551b thread_ts=1782357001.100001 event_id=ev-a2-second (Duration  51 ms)

# km agent list showed resulting runs (poller PID 34452):
20260625T152602Z-34452-14340   ← ev-a2-first dispatched immediately
20260625T152612Z-34452-13811   ← ev-a2-second dispatched ~10s later (after first RUN acked)
```
10-second gap between runs proves the FIFO sequential-within-thread guarantee: the second message was held invisible until the first was deleted from the queue. **Serial-within-thread confirmed.**

### A3 — cap enforcement

```
# deployed userdata (pkg/compiler/userdata.go → /opt/km/bin/km-slack-inbound-poller)
MAX="${KM_SLACK_MAX_CONCURRENCY:-1}"; [ "$MAX" -lt 1 ] && MAX=1
BATCH=$MAX; [ "$BATCH" -gt 10 ] && BATCH=10        # receive up to MAX at once
...
while [ "$inflight" -ge "$MAX" ]; do wait -n; inflight=$((inflight-1)); done   # block at capacity
```

Session-2 re-verification: 5 distinct thread_ts events (ev-a3-t1..t5) fired simultaneously. Bridge enqueued all 5 (5 distinct FIFO queues). Observed runs:
```
# km agent list (poller PID 34452):
20260625T152612Z-34452-13811   ← batch 1 (3 of 5, starts 15:26:12)
20260625T152613Z-34452-22191   ← batch 1
20260625T152613Z-34452-5859    ← batch 1
20260625T152618Z-34452-12990   ← batch 2 (after batch-1 slots freed, ~5s later)
20260625T152619Z-34452-19581   ← batch 2
20260625T152620Z-34452-25780   ← batch 2 (last slot — A6 run also visible here)
```
3 runs started within 1 second (cap reached); next batch only after first batch's agents completed. Never >3 concurrent. **Cap honored.**

### A4 — heartbeat + no-dup

```
$ aws sqs get-queue-attributes ... km-slack-inbound-parallel-3274551b.fifo
VisibilityTimeout = 1800     # Phase 119 base raise (was 30s)
RedrivePolicy: {"deadLetterTargetArn":"...km-slack-inbound-dlq.fifo","maxReceiveCount":3}
```

Deployed poller: ChangeMessageVisibility heartbeat ticker every 120s during a turn (userdata line 1722), torn down post-turn (line 2199); message is **deleted only after the turn completes** (ack-after-completion); `last_processed_event_ts` written to the same DDB row (line 2123) so a crash-redelivery is skipped by the guard (line 1774). The 1800s base + heartbeat means a >30s turn is never redelivered mid-flight. (A real >base-visibility wall-clock turn requires an authenticated agent — bounded here by the auth gate — but the queue attribute + the heartbeat/ack/dedup code are all verified live.)

### A5 — dormancy regression

```
# dormant-51f48ae0 (cap=1, slack disabled — verified live via SSM send-command)
$ AWS_DEFAULT_REGION=us-east-1 aws ssm send-command --instance-id i-0c0881ed8a29b844a \
    --parameters 'commands=["grep -c KM_SLACK_MAX_CONCURRENCY /etc/km/notify.env 2>/dev/null || echo 0"]'
→  Output: "0\n"   Status: Success

# parallel-3274551b (cap=3 — verified live via SSM send-command on i-01245a1a840c56e06)
$ AWS_DEFAULT_REGION=us-east-1 aws ssm send-command --instance-id i-01245a1a840c56e06 \
    --parameters 'commands=["grep -c KM_SLACK_MAX_CONCURRENCY /etc/km/notify.env"]'
→  Output: "KM_SLACK_MAX_CONCURRENCY=3\n"   Status: Success
```

The env var is **emitted only when cap>1** — a cap=1 box is byte-identical to Phase 118. **Dormancy confirmed.**

### A6 — dedup

```
# Session 1 evidence — fresh event ev-p119-uat-b1:
15:14:07 INFO events: enqueued sandbox=parallel-3274551b thread_ts=1782360100.000001 event_id=ev-p119-uat-b1   (Duration 76 ms)

# Session 1 — replay of the SAME event_id:
START a83a881d... ; END a83a881d... ; REPORT Duration: 9.35 ms     (NO "enqueued" line)

# Session 2 — re-verification (ev-a6-dedup fired twice at 15:26:17):
# First send:
15:26:19 INFO events: enqueued sandbox=parallel-3274551b thread_ts=1782359999.999001 event_id=ev-a6-dedup   (RequestId 0da03abc, Duration 54 ms)
15:26:19 WARN events: reaction failed err="bridge: reactions.add: message_not_found" ...   # expected synthetic-ts artifact

# Replay (same event_id ev-a6-dedup, sent 2s later):
START RequestId: 08a07f11-a8b3-4faf-8136-5e83ba592958
END   RequestId: 08a07f11-a8b3-4faf-8136-5e83ba592958
REPORT Duration: 7.05 ms     (NO "enqueued" line, NO reaction WARN)
```

Both sessions confirm: the replay was dropped at the bridge nonce-dedup (events_handler.go:392 `PutItemIfNotExists`) — fast 200 in <10ms, no enqueue, no second run. The on-box `last_processed_event_ts` guard (line 1774/2123) is the second layer (verified in the deployed userdata). **Single-processed confirmed.**

## Bug found + fixed mid-UAT

- **Stale `VisibilityTimeout=30` test assertions.** `internal/app/cmd/create_slack_inbound_test.go:198` and `create_github_inbound_test.go:98` still asserted the pre-Phase-119 `"30"` value; the production `inboundQueueAttrs` helper (`pkg/aws/sqs.go:135`) was already raised to `"1800"` in Phase 119-02 but these two create-handler tests were missed in lockstep. Updated both to `"1800"` with a Phase-119 comment. `go test ./internal/app/cmd/` green after the fix. (Deviation Rule 1 — stale assertion from the Phase-119 queue base raise.)
- **Stale orphaned `km-sandboxes` row shadowed channel routing.** The first create attempt (`parallel-769afe41`) failed at provisioning (Terraform registry timeout, see Notes) but its `teardownPolicy: stop` row persisted with the same `slack_channel_id`. The bridge's first `FetchByChannel` resolved the *stale* row, routing event A1 to the dead sandbox. Deleted the orphan row; routing then correctly resolved to the live `parallel-3274551b`. (Operational artifact of `stop` policy + `archiveOnDestroy:false` channel reuse — not a Phase-119 defect; the bridge behaved per spec given an ambiguous channel→sandbox mapping.)

## Notes / findings

- **Transient Terraform-registry timeout during cold-create.** Two cold-creates failed with `Failed to install provider hashicorp/aws v6.46.0 ... registry.terraform.io ... Client.Timeout exceeded` from inside the create-handler Lambda (6 retries). This is an environment/network artifact (provider not pre-cached in the Lambda's `infra.tar.gz`; registry slow), **not** a Phase-119 code issue — a retry succeeded for `parallel-3274551b`. Flagged for a separate infra hardening item (pre-bundle the provider plugin cache into the toolchain tarball).
- **`[: : integer expression expected` at poller line 122** (harmless): the empty-batch `COUNT` check (`[ "$COUNT" -eq 0 ]`) emits this when SQS long-poll returns no messages and jq yields an empty string instead of `0`. Pre-existing cosmetic noise (the `|| continue` loop proceeds correctly); not introduced by Phase 119 and does not affect dispatch. Candidate one-line follow-up: `COUNT=${COUNT:-0}` or `[ "${COUNT:-0}" -eq 0 ]`.
- The synthetic `event_callback` POST must target **`/events`** (path-based dispatch); the bare bridge URL routes to the sandbox→Slack envelope handler.

## Teardown

- `parallel-3274551b` destroyed by TTL handler at 15:32:03Z (idle timeout reached; `km destroy --remote --yes` not needed — ttl-handler fired automatically). Channel `C0BCQML7S6B` kept (`archiveOnDestroy:false`).
- `dormant-51f48ae0` destroyed by idle timeout (10m configured, no active turns). Also `learn-4c07e510` (failed profile resolution — used extends, not available in Lambda) destroyed automatically.
- `parallel-769afe41` (failed cold-create) + `learn-0af57af8` (failed cold-create) destroyed; orphan DDB row for `parallel-769afe41` manually deleted.
- Verified: `km list` shows 0 sandboxes. No billable resources remain.
