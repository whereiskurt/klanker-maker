---
phase: 71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar
plan: 07
type: execute
wave: 3
depends_on:
  - 71-00
  - 71-04
files_modified:
  - internal/app/cmd/at.go
  - internal/app/cmd/at_playbook_test.go
autonomous: true
requirements:
  - SPEC-71-SC-5

must_haves:
  truths:
    - "schedulableCommands map (at.go:40) gains entry: \"playbook-run\": {targetARNField: \"ttl\", eventType: \"playbook-run\"}"
    - "Two-word command merge block (at.go:143-146) gains parallel: if cmdArg==\"playbook\" && extraArgs[0]==\"run\": cmdArg=\"playbook-run\"; extraArgs=extraArgs[1:] — mirrors agent-run handling exactly"
    - "buildTargetInput (at.go:405-510) gains a `playbook-run` branch: marshals {playbook, sandbox_id} into the EventBridge target Input; reads `--sandbox` flag (from extraArgs scan) for sandbox_id rather than positional arg per RESEARCH §2"
    - "Required: `playbook-run` REQUIRES --sandbox to be set; absence returns a clear error before scheduler call"
    - "TestAtPlaybookRun verifies: cmd-merge collapses 'playbook run' → 'playbook-run'; Input JSON has correct shape {event_type: playbook-run, playbook: 'morning-ops', sandbox_id: 'sb-x'}; missing --sandbox raises error; km at list shows the schedule with 'playbook-run' command label"
  artifacts:
    - path: "internal/app/cmd/at.go"
      provides: "playbook-run schedulable command + two-word merge + buildTargetInput branch"
      contains: "playbook-run"
    - path: "internal/app/cmd/at_playbook_test.go"
      provides: "TestAtPlaybookRun (replaces t.Skip stubs from plan 71-00)"
      min_lines: 100
  key_links:
    - from: "internal/app/cmd/at.go"
      to: "TTL Lambda playbook-run event handler (plan 71-04)"
      via: "EventBridge schedule Input JSON"
      pattern: "event_type.*playbook-run"
---

<objective>
Extend `km at` to schedule `playbook run` invocations. Three small additions to existing at.go landmarks: (1) one entry in schedulableCommands map, (2) one parallel block in the two-word command merge, (3) one new branch in buildTargetInput. RESEARCH §2 gave verbatim diffs for all three; this plan applies them and adds tests.

Purpose: Without this, operators cannot cron a playbook (`km at '0 8 * * 1-5 *' playbook run morning-ops --sandbox sb-ops`). The TTL Lambda handler from plan 71-04 already accepts the playbook-run event — this plan is the EventBridge → Lambda routing layer.

Output: ~30 lines added to at.go + a TestAtPlaybookRun in a new at_playbook_test.go file.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar/SPEC.md
@.planning/phases/71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar/71-RESEARCH.md
@.planning/phases/71-…/71-04-PLAN.md
@internal/app/cmd/at.go
@internal/app/cmd/at_test.go

<interfaces>
Three exact-location edits per RESEARCH §2:

1. schedulableCommands map (around line 40) — add one entry:
```go
"playbook-run": {targetARNField: "ttl", eventType: "playbook-run"},
```

2. Two-word command merge (around line 143-146) — add a parallel block immediately after the existing `agent` → `agent-run` collapse:
```go
if cmdArg == "playbook" && len(extraArgs) > 0 && extraArgs[0] == "run" {
    cmdArg = "playbook-run"
    extraArgs = extraArgs[1:]
}
```

3. buildTargetInput (around line 405-510) — add a new branch (mirroring the existing agent-run branch). RESEARCH §2 gives the minimal shape:
```go
if cmdArg == "playbook-run" {
    if len(extraArgs) == 0 {
        return "", fmt.Errorf("playbook-run requires a playbook name")
    }
    detail["playbook"] = extraArgs[0] // playbook name (required positional)
    for i := 1; i < len(extraArgs); i++ {
        switch extraArgs[i] {
        case "--sandbox":
            if i+1 < len(extraArgs) {
                detail["sandbox_id"] = extraArgs[i+1]
                i++
            }
        }
    }
    if _, ok := detail["sandbox_id"]; !ok {
        return "", fmt.Errorf("playbook-run requires --sandbox <id>")
    }
    detail["event_type"] = "playbook-run"
}
```

RESEARCH §2 important note: For playbook-run, `sandboxID` resolution comes from the `--sandbox` flag in extraArgs, NOT from the positional first-arg path that other commands use (lines 196-202). The positional first-arg is the playbook name; --sandbox carries the sandbox-id. The km at machinery's earlier ResolveSandboxID call (which assumes the first extra arg is a sandbox ref) MUST be skipped or guarded for playbook-run. Read at.go:196-202 carefully and add an `if cmdArg != "playbook-run"` guard around the ResolveSandboxID call so the playbook name doesn't get fed into ResolveSandboxID.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three-point edit of internal/app/cmd/at.go to support playbook-run</name>
  <files>internal/app/cmd/at.go</files>
  <behavior>
    - schedulableCommands map contains "playbook-run" entry with targetARNField=ttl, eventType=playbook-run
    - Two-word merge converts ["playbook", "run", "morning-ops", "--sandbox", "sb-x"] → cmdArg="playbook-run", extraArgs=["morning-ops", "--sandbox", "sb-x"]
    - buildTargetInput for playbook-run produces Input JSON with event_type, playbook, sandbox_id
    - Missing --sandbox raises a clear error before reaching EventBridge scheduler
    - The existing agent-run / destroy / etc paths remain unchanged (regression-free)
    - The ResolveSandboxID positional-arg call is guarded against the playbook name (it would otherwise mis-interpret "morning-ops" as a sandbox alias and fail the schedule)
  </behavior>
  <action>
    1. Read at.go end-to-end first. Locate exactly: line 40 (schedulableCommands map), line 143-146 (two-word merge), line 196-202 (ResolveSandboxID call), line 405+ (buildTargetInput function with its existing per-command branches).

    2. Edit (1): add the schedulableCommands entry. Place it alphabetically next to "agent-run" — keep the map readable.

    3. Edit (2): add the two-word merge block immediately AFTER the existing `agent` → `agent-run` block. Match style and indentation.

    4. Edit (3): add the `playbook-run` branch in buildTargetInput. Place it next to the existing `agent-run` branch. Use the &lt;interfaces&gt; block above verbatim.

    5. Edit (4 — the carve-out RESEARCH §2 flagged): wrap the existing ResolveSandboxID call (around lines 196-202) with a guard so it's skipped when cmdArg=="playbook-run". The sandbox-id for playbook-run comes from the --sandbox flag inside buildTargetInput, not from positional arg resolution. Example:
       ```go
       if cmdArg != "playbook-run" {
           // existing positional sandbox-id resolution via ResolveSandboxID
           sandboxID, err = ResolveSandboxID(...)
           // ...
       }
       ```
       Adjust the surrounding error handling so a missing sandboxID doesn't fail the schedule for playbook-run (since it'll be set later inside buildTargetInput).

    6. Run `go build` and `go vet` to confirm no syntax/import surprises.
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... &amp;&amp; go vet ./internal/app/cmd/...</automated>
  </verify>
  <done>Three at.go edits land cleanly; build green; existing schedulable commands unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestAtPlaybookRun unit tests (Input JSON shape, error paths, regression of existing commands)</name>
  <files>internal/app/cmd/at_playbook_test.go</files>
  <behavior>
    - TestAtPlaybookRunInputShape: invokes the test helper that exercises buildTargetInput with cmdArg="playbook-run", extraArgs=["morning-ops", "--sandbox", "sb-x"]; asserts returned JSON parses to {event_type:"playbook-run", playbook:"morning-ops", sandbox_id:"sb-x"}
    - TestAtPlaybookRunMissingSandbox: same as above but extraArgs=["morning-ops"] (no --sandbox); asserts error returned with message containing "--sandbox"
    - TestAtPlaybookRunMissingPlaybook: extraArgs=[]; asserts error containing "playbook name"
    - TestAtPlaybookRunTwoWordMerge: invokes the high-level dispatcher with input args ["playbook", "run", "morning-ops", "--sandbox", "sb-x"]; asserts cmdArg="playbook-run" after merge
    - TestAtPlaybookRunSchedulableCommandsRegistered: asserts schedulableCommands["playbook-run"] entry exists with targetARNField="ttl", eventType="playbook-run"
    - TestAtPlaybookRunResolveSandboxIDGuard: invokes the schedule path with cmdArg="playbook-run"; asserts ResolveSandboxID is NOT called against the playbook name (avoid the regression)
    - TestAtPlaybookRunDoesNotBreakExistingCommands: regression smoke — invokes buildTargetInput for "destroy", "stop", "agent-run" with their canonical args, asserts each still produces its expected Input JSON
  </behavior>
  <action>
    1. Read internal/app/cmd/at_test.go end-to-end first. Identify the helper functions / test fixtures that exercise buildTargetInput in isolation (probably a `callBuildTargetInput(t, cmdArg, extraArgs)` helper or similar). Reuse them.

    2. Write at_playbook_test.go with seven subtests under a single TestAtPlaybookRun parent. Use the same DI / fake-scheduler patterns the existing at_test.go uses (the file is already a known-good template per CLAUDE.md and prior phase work).

    3. For TestAtPlaybookRunSchedulableCommandsRegistered, just read the package-level schedulableCommands variable and assert.

    4. For TestAtPlaybookRunResolveSandboxIDGuard: this requires either (a) the test imports a function that proxies ResolveSandboxID and lets the test inject a fake (verify via at_test.go conventions whether such a hook exists), or (b) the test invokes the higher-level command-dispatcher with a known-bad playbook name (one that's not a valid sandbox alias) and asserts the call doesn't fail at the ResolveSandboxID layer. Pick whichever fits the existing test structure with minimal new mocking surface.

    5. For TestAtPlaybookRunDoesNotBreakExistingCommands: smoke-test 3 existing commands (destroy/stop/agent-run) — assert their JSON Input shape matches what the existing at_test.go covers. This catches accidental regressions in buildTargetInput when adding the new branch.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestAtPlaybookRun|TestAt' -count=1 -v</automated>
  </verify>
  <done>Seven subtests PASS, no regressions in existing TestAt* functions.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run 'TestAt' -count=1` PASS (covers both existing TestAt* and the new TestAtPlaybookRun)
- `go build ./...` clean
- Manual: `go run ./cmd/km at '*/5 * * * ? *' playbook run morning-ops --sandbox sb-fake` (against a non-existent sandbox to avoid touching real scheduler) prints a sensible "playbook morning-ops scheduled, target=playbook-run" message before the EventBridge API call (or fails cleanly at the EventBridge boundary if the test environment lacks AWS creds — both outcomes prove the cmd dispatcher works)
</verification>

<success_criteria>
SC-5 is unit-test verifiable: the EventBridge schedule's Input JSON shape matches what the TTL Lambda's playbook-run handler from plan 71-04 expects. End-to-end "wait for cron boundary, observe Lambda fire" is plan 71-10's UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/71-…/71-07-SUMMARY.md`
</output>
