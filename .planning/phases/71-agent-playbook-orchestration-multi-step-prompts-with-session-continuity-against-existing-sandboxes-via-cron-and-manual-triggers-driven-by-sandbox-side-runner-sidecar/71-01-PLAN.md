---
phase: 71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar
plan: 01
type: execute
wave: 1
depends_on:
  - 71-00
files_modified:
  - pkg/profile/types.go
  - pkg/profile/schemas/sandbox_profile.schema.json
  - pkg/profile/validate_playbook_test.go
  - pkg/playbook/types.go
  - pkg/playbook/parse.go
  - pkg/playbook/validate.go
  - pkg/playbook/playbook_test.go
autonomous: true
requirements:
  - SPEC-71-SC-1

must_haves:
  truths:
    - "spec.cli.playbookEnabled bool field exists in pkg/profile/types.go CLISpec, defaults to false (zero value)"
    - "JSON schema sandbox_profile.schema.json declares spec.cli.playbookEnabled as boolean default false"
    - "pkg/playbook.Parse reads testdata/valid.yaml without error and returns a *Playbook with metadata.name='morning-ops' and len(spec.steps)=3"
    - "pkg/playbook.Validate accepts testdata/valid.yaml and returns a specific, field-pointing error for each invalid-*.yaml fixture"
    - "Validation rules implemented: metadata.name required and matches `^[a-z0-9-]+$` (≤64 chars); apiVersion='km/v1'; kind='Playbook'; spec.sandbox.mode='existing' (others rejected with 'unknown mode' error); spec.session.scope=[playbook,sandbox] (other shapes rejected); spec.steps non-empty; each step has unique non-empty name matching `^[a-z0-9-]+$` and non-empty prompt"
    - "Errors include the field path (e.g. 'spec.steps[1].prompt: prompt must be non-empty') so SC-1's 'specific error pointing at the offending field' is verifiable"
  artifacts:
    - path: "pkg/profile/types.go"
      provides: "PlaybookEnabled field on CLISpec struct"
      contains: "PlaybookEnabled"
    - path: "pkg/profile/schemas/sandbox_profile.schema.json"
      provides: "playbookEnabled JSON schema field"
      contains: "playbookEnabled"
    - path: "pkg/profile/validate_playbook_test.go"
      provides: "TestValidatePlaybookEnabled — verifies bool parses, default false, no cross-field constraints"
      min_lines: 30
    - path: "pkg/playbook/types.go"
      provides: "Playbook struct hardened (yaml tags, optional doc comments)"
      min_lines: 35
    - path: "pkg/playbook/parse.go"
      provides: "Parse with go-yaml strict-ish error wrapping (filename context)"
      min_lines: 20
    - path: "pkg/playbook/validate.go"
      provides: "Validate implementing all SPEC § Validation rules with field-path errors"
      min_lines: 80
    - path: "pkg/playbook/playbook_test.go"
      provides: "TestParse + TestValidate filled in (table-driven over testdata/*.yaml)"
      min_lines: 120
  key_links:
    - from: "pkg/playbook/validate.go"
      to: "SPEC.md § Playbook YAML schema"
      via: "rule constants"
      pattern: "metadata.name|sandbox.mode|session.scope|steps"
    - from: "pkg/playbook/playbook_test.go"
      to: "pkg/playbook/testdata/*.yaml"
      via: "filepath.Glob + Parse + Validate"
      pattern: "testdata/.*\\.yaml"
---

<objective>
Land the two pure-Go schema surfaces this phase needs:
1. `spec.cli.playbookEnabled bool` in pkg/profile (mirrors NotifySlackInboundEnabled at types.go:436 — same shape, no cross-field constraints)
2. `pkg/playbook` parse + validate (table-driven over the testdata/*.yaml fixtures Plan 71-00 already seeded)

Purpose: This is the only Wave 1 plan that can run with no external dependencies (no AWS, no Lambda, no userdata). It unblocks SC-1 ("km playbook validate" works) end-to-end and gives downstream plans a typed Playbook struct to import. Mirrors the Phase 67 `pkg/profile.NotifySlackInboundEnabled` + `pkg/playbook` analogue split, except there's no separate "validate" package here — it lives inside pkg/playbook itself.

Output: profile schema field + JSON schema entry + complete pkg/playbook validate implementation hitting 100% of SPEC § Validation rules.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar/SPEC.md
@.planning/phases/71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar/71-RESEARCH.md
@.planning/phases/71-agent-playbook-orchestration-multi-step-prompts-with-session-continuity-against-existing-sandboxes-via-cron-and-manual-triggers-driven-by-sandbox-side-runner-sidecar/71-00-PLAN.md
@pkg/profile/types.go
@pkg/profile/schemas/sandbox_profile.schema.json
@pkg/profile/validate.go

<interfaces>
From pkg/profile/types.go (existing CLISpec fields, lines ~354-447 — add PlaybookEnabled at the end):

```go
type CLISpec struct {
    // ... existing fields up through NotifySlackTranscriptEnabled
    NotifySlackTranscriptEnabled *bool `yaml:"notifySlackTranscriptEnabled,omitempty"`

    // PlaybookEnabled enables per-sandbox playbook orchestration — provisions
    // the SQS FIFO queue + DLQ and installs km-playbook-runner.service via userdata.
    // Default: false. Profile-only — no CLI flag override (Phase 71).
    PlaybookEnabled bool `yaml:"playbookEnabled,omitempty"`
}
```

Plain `bool` (not `*bool`) per RESEARCH.md §4 — matches NotifySlackInboundEnabled at line 436. Default false is automatic.

From pkg/playbook/types.go (already created in 71-00 — refine yaml tags only if needed):
- Playbook, PlaybookMetadata, PlaybookSpec, SandboxSpec, SessionSpec, Step

From pkg/playbook/validate.go — public API:
```go
func Validate(p *Playbook) error // returns *ValidationError on failure
```

Internal helper used by tests:
```go
type ValidationError struct {
    Field string
    Msg   string
}
func (e *ValidationError) Error() string { return e.Field + ": " + e.Msg }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add spec.cli.playbookEnabled to profile schema (Go + JSON)</name>
  <files>pkg/profile/types.go, pkg/profile/schemas/sandbox_profile.schema.json, pkg/profile/validate_playbook_test.go</files>
  <behavior>
    - Profile YAML with `spec.cli.playbookEnabled: true` parses without error and CLISpec.PlaybookEnabled == true
    - Profile YAML without the field has CLISpec.PlaybookEnabled == false (zero value)
    - Profile YAML with `spec.cli.playbookEnabled: "yes"` (string) fails JSON-schema validation with a type error
    - No cross-field constraints added (RESEARCH §4: "boolean only; no cross-field constraints")
  </behavior>
  <action>
    1. Edit pkg/profile/types.go: add `PlaybookEnabled bool` field to CLISpec immediately after the existing NotifySlackTranscriptEnabled field. Use yaml tag `yaml:"playbookEnabled,omitempty"` and a doc comment matching the &lt;interfaces&gt; block.

    2. Edit pkg/profile/schemas/sandbox_profile.schema.json: add the playbookEnabled property under the same `cli` properties block where notifySlackTranscriptEnabled lives (around line 539). Shape:
       ```json
       "playbookEnabled": {
         "type": "boolean",
         "default": false,
         "description": "Enable per-sandbox playbook runner — provisions SQS FIFO queue + DLQ and installs km-playbook-runner.service via userdata. Default false. Phase 71."
       }
       ```
       Do NOT add to required[] and do NOT add any if/then validation rules.

    3. Replace the t.Skip stub in pkg/profile/validate_playbook_test.go with two table-driven test functions:
       - TestValidatePlaybookEnabledParse: parse YAML snippets with playbookEnabled true / false / absent / wrong-type, assert struct value or expected error
       - TestValidatePlaybookEnabledNoCrossFieldConstraints: parse a profile with playbookEnabled=true and verify no validation error fires regardless of other CLISpec field values (notifySlackEnabled true/false, notifyEmailEnabled true/false, etc.)

    Reference the existing TestValidate* functions in pkg/profile/validate_*_test.go for table-driven patterns and assertion idioms.
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run 'TestValidatePlaybookEnabled' -count=1 -v</automated>
  </verify>
  <done>PlaybookEnabled field round-trips through YAML and JSON schema validation; no cross-field rules added; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pkg/playbook Parse + Validate with field-path errors</name>
  <files>pkg/playbook/types.go, pkg/playbook/parse.go, pkg/playbook/validate.go, pkg/playbook/playbook_test.go</files>
  <behavior>
    - testdata/valid.yaml round-trips through Parse and passes Validate
    - Each invalid-*.yaml fixture fails Validate with a *ValidationError whose .Field includes the violating path and whose Error() string contains a substring matching the file's failure mode (e.g. invalid-empty-prompt.yaml → error contains "prompt"; invalid-duplicate-step-name.yaml → contains "duplicate" or "unique"; invalid-bad-name-chars.yaml → contains "metadata.name" and the regex)
    - apiVersion missing or != "km/v1" → error
    - kind missing or != "Playbook" → error
    - spec.sandbox.mode != "existing" → error containing "mode"
    - spec.session.scope != ["playbook", "sandbox"] → error containing "scope"
    - len(spec.steps) == 0 → error
    - Coverage: every invalid fixture in testdata/ exercises a unique rule
  </behavior>
  <action>
    1. Refine pkg/playbook/types.go yaml tags — confirm Playbook, PlaybookMetadata, PlaybookSpec, SandboxSpec (`Mode string`), SessionSpec (`Scope []string`), Step structs match SPEC § Playbook YAML schema exactly. Add a small ValidationError type:
       ```go
       type ValidationError struct {
           Field string
           Msg   string
       }
       func (e *ValidationError) Error() string {
           return fmt.Sprintf("%s: %s", e.Field, e.Msg)
       }
       ```

    2. Harden pkg/playbook/parse.go: wrap goccy/go-yaml errors with a `parse error: %w` prefix so callers can distinguish parse failures from validate failures.

    3. Replace pkg/playbook/validate.go with the full rule set. Use a flat slice of rule closures + early-return on first violation (matches the table-driven pattern in pkg/profile/validate.go). Rules in order:
       - apiVersion == "km/v1" else `&amp;ValidationError{Field: "apiVersion", Msg: "must be \"km/v1\""}`
       - kind == "Playbook" else error
       - metadata.name non-empty else error
       - metadata.name matches `^[a-z0-9-]+$` and len ≤ 64 else error citing the regex
       - spec.sandbox.mode == "existing" else `&amp;ValidationError{Field: "spec.sandbox.mode", Msg: "v1 only honors \"existing\" (got %q)"}`
       - spec.session.scope deep-equals []string{"playbook", "sandbox"} else error
       - len(spec.steps) ≥ 1 else error
       - For each step: name non-empty, name matches regex, name unique within playbook (track seen-set), prompt non-empty (after trimming whitespace)

    4. Replace t.Skip in pkg/playbook/playbook_test.go with two filled-in tests:
       - TestParse: read testdata/valid.yaml, assert Parse succeeds, assert metadata.name == "morning-ops" and len(spec.steps) == 3
       - TestValidate: filepath.Glob("testdata/invalid-*.yaml"), for each: Parse, then Validate, assert error is non-nil and the error message contains a substring derived from the filename (or a per-fixture expected-substring map). Also: TestValidate_Valid asserts Parse(valid.yaml) → Validate returns nil.
       - TestApply: keep as t.Skip (implemented in plan 71-06)

    Use `regexp.MustCompile("^[a-z0-9-]+$")` package-level var so the regex isn't recompiled per call.
  </action>
  <verify>
    <automated>go test ./pkg/playbook/... -run 'TestParse|TestValidate' -count=1 -v</automated>
  </verify>
  <done>SC-1 verifiable end-to-end at the package level: Validate accepts valid.yaml, rejects each invalid fixture with a field-pointing error.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/profile/... ./pkg/playbook/... -run 'TestValidatePlaybookEnabled|TestParse|TestValidate' -count=1` all PASS (no SKIP for these names)
- `go build ./...` clean
- Manual: `go run ./cmd/km validate &lt;profile-with-playbookEnabled-true.yaml&gt;` reports OK (CLI integration with `km playbook validate &lt;file.yaml&gt;` arrives in plan 71-06)
</verification>

<success_criteria>
SPEC-71-SC-1 is unit-test verifiable: pkg/playbook.Validate returns specific field-pointing errors for each invalid-rule variant. The CLI surface (`km playbook validate &lt;file&gt;`) lands in plan 71-06.
</success_criteria>

<output>
After completion, create `.planning/phases/71-…/71-01-SUMMARY.md`
</output>
