---
phase: 06-budget-enforcement-platform-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_test.go
  - internal/app/cmd/configure.go
  - internal/app/cmd/configure_test.go
  - internal/app/cmd/bootstrap.go
  - internal/app/cmd/root.go
  - km-config.yaml.example
autonomous: true
requirements: [CONF-01, CONF-03, CONF-04]

must_haves:
  truths:
    - "Config struct loads domain, account IDs, SSO, region from km-config.yaml in repo root"
    - "km configure wizard walks operator through setting domain, accounts, SSO, region and writes km-config.yaml"
    - "km bootstrap is a stub command that validates config and checks AWS access"
    - "All existing config (ProfileSearchPaths, LogLevel, StateBucket, etc.) continues working"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "Extended Config struct with Domain, AccountIDs, SSO, Region, BudgetTableName"
      exports: ["Config", "Load"]
    - path: "internal/app/cmd/configure.go"
      provides: "km configure wizard command"
      exports: ["NewConfigureCmd"]
    - path: "km-config.yaml.example"
      provides: "Example config file for forks"
  key_links:
    - from: "internal/app/config/config.go"
      to: "km-config.yaml"
      via: "viper SetConfigName km-config"
      pattern: "SetConfigName.*km-config"
    - from: "internal/app/cmd/root.go"
      to: "internal/app/cmd/configure.go"
      via: "rootCmd.AddCommand(NewConfigureCmd)"
      pattern: "AddCommand.*Configure"
---

<objective>
Extend the Config struct and implement km configure wizard so the platform is fully configurable from a single km-config.yaml file at repo root.

Purpose: CONF-01 (single config file), CONF-03 (AWS accounts configurable), CONF-04 (km configure wizard). This is the foundation for domain replacement (Plan 03) and budget enforcement (Plans 02-07).
Output: Extended config.go, km configure command, km bootstrap stub, example config file.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/app/config/config.go
@internal/app/cmd/root.go
@internal/app/cmd/init.go

<interfaces>
<!-- Existing Config struct that must be extended (not replaced) -->
From internal/app/config/config.go:
```go
type Config struct {
    ProfileSearchPaths []string
    LogLevel           string
    Version            string
    StateBucket        string
    TTLLambdaARN       string
    SchedulerRoleARN   string
}
func Load() (*Config, error)
```

From internal/app/cmd/root.go — NewRootCmd pattern for adding subcommands:
```go
func NewRootCmd(cfg *config.Config) *cobra.Command
// Subcommands added via rootCmd.AddCommand(NewXxxCmd(cfg))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Config struct and km-config.yaml loading</name>
  <files>internal/app/config/config.go, internal/app/config/config_test.go, km-config.yaml.example</files>
  <behavior>
    - Test: Load() with km-config.yaml in test dir returns Domain, ManagementAccountID, TerraformAccountID, ApplicationAccountID, SSOStartURL, PrimaryRegion, BudgetTableName
    - Test: Load() without km-config.yaml still returns defaults for existing fields (backward compat)
    - Test: KM_DOMAIN env var overrides km-config.yaml domain value
    - Test: BudgetTableName defaults to "km-budgets" when not set
  </behavior>
  <action>
Extend the Config struct in internal/app/config/config.go with new fields:
- Domain string (base domain, e.g. "klankermaker.ai")
- ManagementAccountID string
- TerraformAccountID string
- ApplicationAccountID string
- SSOStartURL string
- SSORegion string
- PrimaryRegion string
- BudgetTableName string (default: "km-budgets")

Update Load() to also search for "km-config" config name in addition to "config". Use viper.MergeInConfig() for the second config file so both ~/.km/config.yaml and ./km-config.yaml are loaded (km-config.yaml values take precedence for platform fields).

Map viper keys: "domain" -> Domain, "accounts.management" -> ManagementAccountID, "accounts.terraform" -> TerraformAccountID, "accounts.application" -> ApplicationAccountID, "sso.start_url" -> SSOStartURL, "sso.region" -> SSORegion, "region" -> PrimaryRegion, "budget_table_name" -> BudgetTableName.

Create km-config.yaml.example at repo root with all fields documented.

All existing config fields and loading behavior must remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/config/... -v -count=1</automated>
  </verify>
  <done>Config struct has all platform fields, Load() reads km-config.yaml, backward compat preserved, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: km configure wizard + km bootstrap stub</name>
  <files>internal/app/cmd/configure.go, internal/app/cmd/configure_test.go, internal/app/cmd/bootstrap.go, internal/app/cmd/root.go</files>
  <behavior>
    - Test: configure command with stdin providing domain/accounts/sso writes valid km-config.yaml
    - Test: configure command detects 2-account topology (terraform == application) and skips DNS delegation guidance
    - Test: configure command detects 3-account topology and shows DNS delegation message
    - Test: bootstrap command with --dry-run validates config exists and prints what would be provisioned
  </behavior>
  <action>
Create internal/app/cmd/configure.go with NewConfigureCmd(cfg):
- Interactive wizard using bufio.Scanner on os.Stdin (per research: match `aws configure` simplicity)
- Prompts in order: domain, management account ID, terraform account ID, application account ID, SSO start URL, SSO region, primary region
- Detects 2-account vs 3-account topology (if terraform == application, note that)
- When management != application, prints DNS delegation guidance (create hosted zone in app account, add NS in management)
- Writes km-config.yaml to repo root (use findRepoRoot() pattern from existing code, or current directory)
- Accept --non-interactive flag with --domain, --management-account, etc. flags for scripting

Create internal/app/cmd/bootstrap.go with NewBootstrapCmd(cfg):
- Stub that validates km-config.yaml exists and is loadable
- With --dry-run (default), prints what infrastructure would be created (S3 bucket, DynamoDB lock table, KMS key)
- Full implementation deferred — this is the command skeleton only

Register both commands in root.go via AddCommand.

For testability, accept an io.Reader for stdin and io.Writer for stdout so tests can inject input without real terminal interaction.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/ -run "TestConfigure|TestBootstrap" -v -count=1</automated>
  </verify>
  <done>km configure writes km-config.yaml from wizard input, km bootstrap stub validates config, both registered in root command</done>
</task>

</tasks>

<verification>
```bash
cd /Users/khundeck/working/klankrmkr && go test ./internal/app/config/... ./internal/app/cmd/ -run "TestConfigure|TestBootstrap|TestConfig" -v -count=1
```
</verification>

<success_criteria>
- Config struct has Domain, AccountIDs, SSO, Region, BudgetTableName fields
- Load() reads km-config.yaml from repo root alongside existing ~/.km/config.yaml
- km configure wizard writes valid km-config.yaml
- km bootstrap stub validates config
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-budget-enforcement-platform-configuration/06-01-SUMMARY.md`
</output>
