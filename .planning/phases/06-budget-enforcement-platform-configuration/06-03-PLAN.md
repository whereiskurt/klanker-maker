---
phase: 06-budget-enforcement-platform-configuration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/create.go
  - internal/app/cmd/destroy.go
  - pkg/compiler/service_hcl.go
  - pkg/compiler/userdata.go
  - pkg/compiler/service_hcl_email_test.go
  - cmd/ttl-handler/main.go
  - cmd/ttl-handler/main_test.go
  - infra/live/site.hcl
  - pkg/profile/schema.go
  - pkg/profile/validate.go
  - pkg/profile/validate_test.go
  - cmd/configui/handlers.go
  - cmd/configui/handlers_editor_test.go
  - cmd/configui/templates/editor.html
autonomous: true
requirements: [CONF-02]

must_haves:
  truths:
    - "No hardcoded klankermaker.ai domain references remain in Go source or Terraform/Terragrunt files"
    - "SES email domain derived from Config.Domain in create.go and destroy.go"
    - "Compiler receives domain from Config and passes to service.hcl templates"
    - "apiVersion validation only checks version suffix, not domain prefix"
    - "ConfigUI branding uses configured domain"
    - "JSON Schema $id remains a fixed internal constant (not user-configurable per Pitfall 5/6)"
  artifacts:
    - path: "internal/app/cmd/create.go"
      provides: "Email domain from config instead of hardcoded constant"
    - path: "pkg/compiler/service_hcl.go"
      provides: "Domain parameter in compilation instead of hardcoded string"
    - path: "pkg/profile/validate.go"
      provides: "Relaxed apiVersion regex checking only version suffix"
  key_links:
    - from: "internal/app/cmd/create.go"
      to: "internal/app/config/config.go"
      via: "cfg.Domain for email domain"
      pattern: "cfg\\.Domain"
    - from: "pkg/compiler/service_hcl.go"
      to: "CompileOptions or function param"
      via: "domain passed as parameter"
      pattern: "domain.*string"
---

<objective>
Replace all ~20 hardcoded `klankermaker.ai` references with config-derived values so forks work with any domain.

Purpose: CONF-02 (domain name configurable). After this plan, a fork with domain `mysandboxes.example.com` works end-to-end without code changes.
Output: All Go source, templates, Terraform, and test files use config-derived domain.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Config struct will have Domain field from Plan 01. If Plan 01 not yet executed,
     use KM_DOMAIN env var as fallback with default "klankermaker.ai" -->

From internal/app/config/config.go (after Plan 01):
```go
type Config struct {
    // ... existing fields ...
    Domain string // base domain
}
```

From pkg/compiler — Compile function signature:
```go
func Compile(profile *profile.SandboxProfile, sandboxID string, onDemand bool) (*CompiledArtifacts, error)
```

Hardcoded locations (from RESEARCH.md catalogue):
- internal/app/cmd/create.go:267 — const emailDomain
- internal/app/cmd/destroy.go:192 — const emailDomain
- pkg/compiler/service_hcl.go:291,406 — "sandboxes.klankermaker.ai"
- pkg/compiler/userdata.go — same pattern
- cmd/ttl-handler/main.go:153 — domain = "sandboxes.klankermaker.ai"
- infra/live/site.hcl:5 — domain = "klankermaker.ai"
- pkg/profile/validate.go — apiVersion regex
- cmd/configui/templates/editor.html — branding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded domain in Go source and update apiVersion validation</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/destroy.go, pkg/compiler/service_hcl.go, pkg/compiler/userdata.go, pkg/compiler/service_hcl_email_test.go, cmd/ttl-handler/main.go, cmd/ttl-handler/main_test.go, pkg/profile/validate.go, pkg/profile/validate_test.go, pkg/profile/schema.go</files>
  <action>
**Step 1: create.go and destroy.go** — Remove `const emailDomain = "sandboxes.klankermaker.ai"`. Instead, derive from Config: `emailDomain := "sandboxes." + cfg.Domain`. If cfg.Domain is empty, default to "klankermaker.ai". The Config is already available via the Cobra command context.

**Step 2: pkg/compiler/** — The Compile() function currently hardcodes the email domain in service.hcl templates. Add a `Domain string` field to a CompileOptions struct (or add domain as a parameter). The compiler should receive the domain and use it in templates. Update service_hcl.go and userdata.go to use the parameter instead of hardcoded string. Update service_hcl_email_test.go to pass the domain.

**Step 3: cmd/ttl-handler/main.go** — Replace hardcoded domain with `os.Getenv("KM_EMAIL_DOMAIN")` with fallback to "sandboxes.klankermaker.ai". The TTL handler Lambda already reads env vars — KM_EMAIL_DOMAIN is set from the Terraform module's environment block. Update the corresponding test.

**Step 4: pkg/profile/validate.go** — Relax the apiVersion validation. Currently checks for exact `klankermaker.ai/v1alpha1`. Change to only check the version suffix: regex `.*\/v\d+alpha\d+` (or just check that the string ends with `/v1alpha1`). This way forks can use `mydomain.com/v1alpha1` without breaking validation.

**Step 5: pkg/profile/schema.go** — Keep schemaID as a fixed internal constant (per research Pitfalls 5 and 6). Do NOT make it dynamic. The $id is an internal identifier for the JSON Schema compiler, not a user-facing value. Add a comment explaining this decision.

**Step 6: Update all tests** that assert on "klankermaker.ai" to use the configured or default domain value. Ensure existing test profiles (testdata/profiles/*.yaml) still validate — their apiVersion domain doesn't matter since validation now only checks the suffix.

Grep for all remaining `klankermaker.ai` references in Go files and ensure each is either:
(a) replaced with config-derived value, or
(b) documented as intentional (schema.go $id internal constant)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... ./pkg/compiler/... ./pkg/profile/... ./cmd/ttl-handler/... -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <done>No hardcoded klankermaker.ai in Go source except schema.go internal $id; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded domain in Terraform/Terragrunt and ConfigUI templates</name>
  <files>infra/live/site.hcl, cmd/configui/handlers.go, cmd/configui/handlers_editor_test.go, cmd/configui/templates/editor.html</files>
  <action>
**Step 1: infra/live/site.hcl** — Replace `domain = "klankermaker.ai"` with a pattern that reads from environment: `domain = get_env("KM_DOMAIN", "klankermaker.ai")`. Terragrunt's `get_env` function reads environment variables with a default fallback. Operators set KM_DOMAIN before running terragrunt, or it defaults to klankermaker.ai.

**Step 2: cmd/configui/templates/editor.html** — Replace any hardcoded "klankermaker.ai" branding text with a template variable `{{.Domain}}`. Pass the domain from the handler via the template data struct.

**Step 3: cmd/configui/handlers.go** — Update the handler that renders editor.html to include Domain in the template data. Read from config or KM_DOMAIN env var. If the ConfigUI server already has a config struct, add Domain to it.

**Step 4: cmd/configui/handlers_editor_test.go** — Update test expectations to not assert on hardcoded domain text, or set the domain in test setup.

**Step 5: Final sweep** — Run `grep -r "klankermaker.ai" --include="*.go" --include="*.hcl" --include="*.html" --include="*.tf"` on the repo (excluding .planning/, docs/, testdata profile YAML). Verify all remaining references are either (a) in test fixtures that validate fine with relaxed apiVersion, (b) in the schema.go internal constant, or (c) in documentation.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./cmd/configui/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>site.hcl uses get_env for domain, ConfigUI templates use template variable, no hardcoded domain in production code</done>
</task>

</tasks>

<verification>
```bash
cd /Users/khundeck/working/klankrmkr && go test ./... -count=1 2>&1 | tail -30
# Plus manual grep:
grep -r "klankermaker\.ai" --include="*.go" --include="*.hcl" --include="*.html" --include="*.tf" . | grep -v ".planning/" | grep -v "docs/" | grep -v "testdata/" | grep -v "schema.go"
```
</verification>

<success_criteria>
- grep for hardcoded domain in production code returns only schema.go internal $id
- All Go tests pass
- apiVersion validation accepts any domain prefix with v1alpha1 suffix
- site.hcl uses get_env for domain
- ConfigUI branding uses template variable
</success_criteria>

<output>
After completion, create `.planning/phases/06-budget-enforcement-platform-configuration/06-03-SUMMARY.md`
</output>
