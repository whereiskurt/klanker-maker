---
phase: 94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [94-01]
files_modified:
  - internal/app/cmd/doctor_ddb_rows.go
  - internal/app/cmd/doctor_ddb_rows_test.go
  - internal/app/cmd/doctor.go
autonomous: true
requirements: [DBG-DDB, DBG-DDB-AI, DBG-DDB-GUARD, DBG-DDB-SLACK, DBG-PAGE, DBG-MULTI]
must_haves:
  truths:
    - "checkOrphanedDDBRows scans all four tables (budgets, identities, slack-threads, sandboxes) and WARNs on orphaned rows"
    - "BUDGET#ai# rows are NEVER treated as orphans — preserved regardless of sandbox state (regression guard)"
    - "sandboxes rows are purged only when status is failed or nocap; running/stopped/paused/starting/destroyed rows are never deleted"
    - "slack-threads orphans are resolved via the non-key sandbox_id ATTRIBUTE; rows missing sandbox_id are skipped"
    - "Deletion happens only with DryRun=false AND --delete-ddb-rows; otherwise WARN with hint and no mutation"
    - "DynamoDB Scan pagination across LastEvaluatedKey is handled on every table"
    - "Check registered in buildChecks and runs in the parallel slice"
  artifacts:
    - path: "internal/app/cmd/doctor_ddb_rows.go"
      provides: "checkOrphanedDDBRows over four tables with AI-row preservation and status guard"
      contains: "BUDGET#ai#"
    - path: "internal/app/cmd/doctor_ddb_rows_test.go"
      provides: "Table-driven mocked-API tests incl. AI-preservation + failed-status guard regressions"
      contains: "TestDoctor_OrphanedDDBRows"
  key_links:
    - from: "buildChecks in doctor.go"
      to: "checkOrphanedDDBRows"
      via: "appended closure using deps.DDBScanDeleteClient + deps.Lister + deps.DeleteDDBRows + table names from cfg"
      pattern: "checkOrphanedDDBRows\\("
    - from: "checkOrphanedDDBRows budgets scan"
      to: "row classification"
      via: "strings.HasPrefix(sk, \"BUDGET#ai#\") → preserve"
      pattern: "BUDGET#ai#"
---

<objective>
Implement checkOrphanedDDBRows: scan budgets / identities / slack-threads / sandboxes, find rows
whose sandbox-id is gone from the active set (with the AI-row preservation and status=failed
guards), WARN with a delete hint, and delete under --delete-ddb-rows. Register in buildChecks.

Purpose: Reclaim the ~394 leaked DynamoDB rows while protecting AI-model spend history and
in-flight creates.
Output: A new doctor check that reclaims orphaned rows across four tables under explicit opt-in.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-RESEARCH.md
@.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-01-SUMMARY.md

CRITICAL FINDINGS (94-RESEARCH.md Item 2):
- budgets: composite PK=`SANDBOX#{id}` SK=`BUDGET#{type}`. DELETE `BUDGET#compute` +
  `BUDGET#limits` for orphans; PRESERVE every `BUDGET#ai#{modelID}` row unconditionally
  (Phase 88 metering history — Pitfall 4). DeleteItem key = {PK, SK}.
- identities: sole hash key `sandbox_id` (S). DeleteItem key = {sandbox_id}.
- slack-threads: PK=`channel_id`, SK=`thread_ts`; `sandbox_id` is a NON-KEY attribute.
  Extract sandbox_id from the attribute; skip rows where it is absent (legacy). DeleteItem key
  = {channel_id, thread_ts}.
- sandboxes: sole hash key `sandbox_id` (S). There is NO instance_id attribute in DDB. Implement
  the spec's "status=failed && no instance_id" guard as: delete ONLY rows where status ∈
  {"failed","nocap"}. status=="starting" is an in-flight create and MUST be skipped (Pitfall 5).
  DeleteItem key = {sandbox_id}.

<interfaces>
Canonical shape — copy checkStaleSSMParameters (doctor.go ~line 1608): list → group by
sandbox-id → ListSandboxes(ctx,false) active map → orphan diff → OK if none → WARN+hint when
dryRun||!deleteDDBRows → else delete with deleted/failed counts. Hint: "use --dry-run=false
--delete-ddb-rows to delete" / "use --delete-ddb-rows to delete".

Scan pagination (Pitfall 3): loop on out.LastEvaluatedKey passed as ExclusiveStartKey. Use a
ProjectionExpression to fetch only needed attrs (budgets: PK,SK; sandboxes: sandbox_id,status;
identities: sandbox_id; slack-threads: channel_id,thread_ts,sandbox_id). See 94-RESEARCH.md
"DynamoDB scan for budgets table", "Slack-threads scan", "Sandbox row deletion guard".

Table names from cfg (Wave 1 / existing): cfg.GetBudgetTableName(), cfg.GetIdentityTableName(),
cfg.GetSlackThreadsTableName(), cfg.GetSandboxTableName() — each falls back to
GetResourcePrefix()+"-{suffix}". deps.DDBScanDeleteClient, deps.DeleteDDBRows, deps.Lister,
deps.DryRun available from Wave 1.

A small `attrStr(item, key)` helper (read S attribute as string, "" if absent) is needed — add
it locally if not already present in the cmd package.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkOrphanedDDBRows — four-table scan with AI-preservation + status guard</name>
  <files>internal/app/cmd/doctor_ddb_rows.go, internal/app/cmd/doctor_ddb_rows_test.go</files>
  <behavior>
    - Orphan rows across the four tables (sandbox-id ∉ active set) → CheckWarn with per-table counts; no deletion when dryRun||!deleteDDBRows.
    - budgets: BUDGET#compute / BUDGET#limits for orphaned ids collected for deletion; BUDGET#ai#{modelID} rows NEVER collected even for orphaned ids (DBG-DDB-AI regression).
    - sandboxes: only status∈{failed,nocap} AND not-in-active collected; status∈{running,stopped,paused,starting,destroyed} skipped even when not in active set (DBG-DDB-GUARD); starting explicitly asserted skipped.
    - slack-threads: sandbox_id read from the attribute (not key); a row with no sandbox_id attribute is skipped, not deleted (DBG-DDB-SLACK).
    - identities: each orphaned sandbox_id row collected.
    - All-active across all tables → CheckOK.
    - Scan pagination: orphan on page 2 (LastEvaluatedKey) detected (DBG-PAGE).
    - Sibling-install sandbox-ids absent from the local active set are excluded by the diff (DBG-MULTI) — but a sibling row would still match status-guard for sandboxes; rely on the active-set diff + same-table-name prefix scoping (tables are {prefix}-scoped) so only the local install's tables are scanned. Document this.
  </behavior>
  <action>
Create internal/app/cmd/doctor_ddb_rows.go with
`func checkOrphanedDDBRows(ctx context.Context, client DDBScanDeleteAPI, lister SandboxLister,
dryRun bool, deleteDDBRows bool, budgetsTable, identitiesTable, slackThreadsTable, sandboxesTable string) CheckResult`.
Nil client → CheckSkipped.

ListSandboxes(ctx,false) → activeSandboxes map first. Then a paginated Scan per table
(ProjectionExpression per table, LastEvaluatedKey loop). Build a list of pending DeleteItem
operations (each carrying its table + key map), applying the classification rules:
  - budgets: parse PK (strip "SANDBOX#"); skip non-SANDBOX# PKs; if SK has prefix "BUDGET#ai#"
    → CONTINUE (preserve); else if sandbox-id orphaned → queue delete with key {PK, SK}.
  - identities: if sandbox_id orphaned → queue delete with key {sandbox_id}.
  - slack-threads: read channel_id, thread_ts, sandbox_id attrs; skip if any empty; if sandbox_id
    orphaned → queue delete with key {channel_id, thread_ts}.
  - sandboxes: read sandbox_id, status; skip if in active set; skip unless status ∈ {failed,nocap};
    else queue delete with key {sandbox_id}.

If no pending deletes → CheckOK ("N rows scanned, all active"). If dryRun||!deleteDDBRows →
CheckWarn with the hint and per-table orphan counts. Else execute each DeleteItem (single-item
DeleteItem per row is acceptable at this scale per 94-RESEARCH.md "DynamoDB batch deletion
limit"), tally deleted/failed, return OK when failed==0 else WARN.

Write doctor_ddb_rows_test.go table-driven against mockDDBScanDelete + mockSandboxLister:
_Detected, _PreserveAI (orphaned sandbox has compute+limits+ai rows → only compute+limits
queued; ai row never in delete set — assert against recorded DeleteItem keys), _FailedGuard
(orphaned sandbox rows with status running/starting/stopped NOT deleted; failed/nocap deleted),
_FailedStatusDeleted, _SlackThreadNoSandboxID (row without sandbox_id skipped), _Delete (counts),
_Paginate (LastEvaluatedKey), _AllActive (OK). The mock records DeleteItem calls so dry-run and
preservation assertions can prove exactly which keys were/weren't deleted.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'OrphanedDDBRows' -count=1</automated>
  </verify>
  <done>Four tables scanned; AI rows provably preserved; status guard enforced (starting skipped); slack non-key sandbox_id handled; dry-run mutates nothing; pagination covered.</done>
</task>

<task type="auto">
  <name>Task 2: Register checkOrphanedDDBRows in buildChecks</name>
  <files>internal/app/cmd/doctor.go</files>
  <action>
In buildChecks (doctor.go, beside the other stale checks ~line 2861) append:
```go
ddbScanDelete := deps.DDBScanDeleteClient
deleteDDBRows := deps.DeleteDDBRows
budgetsTbl := cfg.GetBudgetTableName()
if budgetsTbl == "" { budgetsTbl = cfg.GetResourcePrefix() + "-budgets" }
identitiesTbl := cfg.GetIdentityTableName()
if identitiesTbl == "" { identitiesTbl = cfg.GetResourcePrefix() + "-identities" }
slackThreadsTbl := cfg.GetSlackThreadsTableName()
sandboxesTbl := cfg.GetSandboxTableName()
checks = append(checks, func(ctx context.Context) CheckResult {
    return checkOrphanedDDBRows(ctx, ddbScanDelete, listerForCleanup, dryRun, deleteDDBRows, budgetsTbl, identitiesTbl, slackThreadsTbl, sandboxesTbl)
})
```
Use the same `listerForCleanup`/`dryRun` locals already in scope in buildChecks. Confirm
GetSlackThreadsTableName / GetSandboxTableName are on DoctorConfigProvider (they are — lines
~215 / used by other checks); if GetSandboxTableName needs an empty fallback, mirror the
budgets fallback.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -count=1</automated>
  </verify>
  <done>Check appears in the buildChecks slice and runs without regressing the existing suite.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'OrphanedDDBRows' -count=1` green.
- `go test ./internal/app/cmd/... -count=1` green.
- `make build` succeeds.
</verification>

<success_criteria>
- Four tables scanned with full pagination.
- BUDGET#ai# rows preserved (regression-tested against recorded delete keys).
- sandboxes purged only for status∈{failed,nocap}; starting/running/etc. never deleted.
- slack-threads orphans resolved via the sandbox_id attribute; missing-attr rows skipped.
- Dry-run report-only; --delete-ddb-rows reclaims with counts.
- Check registered in buildChecks.
</success_criteria>

<output>
After completion, create `.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-03-SUMMARY.md`
</output>
