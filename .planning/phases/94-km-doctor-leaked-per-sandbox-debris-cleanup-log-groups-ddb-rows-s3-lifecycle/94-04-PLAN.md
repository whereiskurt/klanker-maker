---
phase: 94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle
plan: 04
type: execute
wave: 4
depends_on: [94-01]
files_modified:
  - internal/app/cmd/doctor_artifacts.go
  - internal/app/cmd/doctor_artifacts_test.go
  - internal/app/cmd/doctor.go
autonomous: true
requirements: [DBG-S3, DBG-S3-SET]
must_haves:
  truths:
    - "checkS3LifecyclePolicy WARNs when no expiry rule covers the four transient prefixes on the artifacts bucket"
    - "--set-s3-lifecycle installs expiry rules for the transient prefixes only, never the build-artifact prefixes"
    - "Existing/unrelated lifecycle rules are preserved (merged, not overwritten)"
    - "Already-covered prefixes → OK / idempotent no-op (no Put when all transient prefixes already have an expiry rule)"
    - "Check registered in buildChecks and runs in the parallel slice"
  artifacts:
    - path: "internal/app/cmd/doctor_artifacts.go"
      provides: "checkS3LifecyclePolicy + transient-expiry rule builder, merge-and-put"
      contains: "checkS3LifecyclePolicy"
    - path: "internal/app/cmd/doctor_artifacts_test.go"
      provides: "Table-driven mocked-API tests for missing/already-set/merge-preserve"
      contains: "TestDoctor_S3LifecyclePolicy"
  key_links:
    - from: "buildChecks in doctor.go"
      to: "checkS3LifecyclePolicy"
      via: "appended closure using deps.S3LifecycleClient + cfg.GetArtifactsBucket + deps.SetS3Lifecycle + cfg.GetDoctorS3ExpireDays"
      pattern: "checkS3LifecyclePolicy\\("
    - from: "checkS3LifecyclePolicy"
      to: "S3"
      via: "GetBucketLifecycleConfiguration then merge then PutBucketLifecycleConfiguration"
      pattern: "PutBucketLifecycleConfiguration"
---

<objective>
Implement checkS3LifecyclePolicy in doctor_artifacts.go: detect the absent expiry rule on the
artifacts bucket's transient prefixes, WARN, and install merge-preserving expiry rules under
--set-s3-lifecycle. Register in buildChecks.

Purpose: Stop unbounded S3 growth on transient prefixes (logs/, remote-create/, agent-runs/,
slack-inbound/) without ever expiring build artifacts or clobbering operator rules.
Output: A new guardrail check + idempotent --set-s3-lifecycle application.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-RESEARCH.md
@.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-01-SUMMARY.md
@internal/app/cmd/doctor_artifacts.go

CRITICAL FINDINGS (94-RESEARCH.md):
- Transient prefixes to expire (HARDCODED — CONTEXT.md YAGNI, no config knob): `logs/`,
  `remote-create/`, `agent-runs/`, `slack-inbound/`.
- Build-artifact prefixes (`toolchain/`, `sidecars/`, `rsync/`) MUST NOT be expired.
- Pitfall 2: PutBucketLifecycleConfiguration REPLACES ALL rules. MUST Get existing rules first,
  merge (de-dup by rule ID), then Put — dropping operator rules is an incident.
- GetBucketLifecycleConfiguration returns a NoSuchLifecycleConfiguration error when none exists;
  treat that as "no rules" (existingRules = nil), not a hard failure (use errors.As).
- One Filter per LifecycleRule → use one rule per transient prefix (four new rules), each with a
  stable deterministic ID (e.g. "km-doctor-expire-logs") so re-runs are idempotent.

<interfaces>
Guardrail check shape (94-RESEARCH.md "checkS3LifecyclePolicy canonical shape"):
```go
func checkS3LifecyclePolicy(ctx context.Context, client S3LifecycleAPI, bucket string,
    expireDays int32, dryRun bool, setLifecycle bool) CheckResult {
    // 1. GetBucketLifecycleConfiguration (NoSuchLifecycleConfiguration → empty)
    // 2. Determine which transient prefixes are NOT yet covered by an existing expiry rule
    // 3. all covered → CheckOK
    // 4. missing && (dryRun || !setLifecycle) → CheckWarn "use --set-s3-lifecycle"
    // 5. else merge new rules with existing (de-dup by ID), Put, → CheckOK
}
```
Merge-and-put + builder examples: 94-RESEARCH.md "S3 lifecycle guardrail (merge-and-put pattern)".
deps.S3LifecycleClient, deps.SetS3Lifecycle, cfg.GetArtifactsBucket(), cfg.GetDoctorS3ExpireDays()
available from Wave 1. s3 + s3/types packages already imported in the cmd package (doctor.go).

buildChecks registration: append beside checkOrphanedArtifacts (doctor.go ~line 3070), reusing
the artifacts bucket local. Empty bucket → the check should CheckSkipped (mirror nil-client).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkS3LifecyclePolicy — detect missing expiry + merge-preserving --set-s3-lifecycle</name>
  <files>internal/app/cmd/doctor_artifacts.go, internal/app/cmd/doctor_artifacts_test.go</files>
  <behavior>
    - No lifecycle config (NoSuchLifecycleConfiguration) → WARN listing uncovered transient prefixes + hint (DBG-S3); no Put when dryRun||!setLifecycle.
    - Some transient prefixes covered, some not → WARN for the uncovered subset.
    - All four transient prefixes already covered by an expiry rule → CheckOK, no Put (idempotent).
    - setLifecycle && !dryRun && missing → exactly one Put; the Put input contains the existing/unrelated rules UNCHANGED plus one new expiry rule per uncovered transient prefix; build-artifact prefixes (toolchain/, sidecars/, rsync/) get NO rule (DBG-S3-SET).
    - Re-running after a set → all covered → OK, no Put (idempotent via stable rule IDs).
  </behavior>
  <action>
Add `checkS3LifecyclePolicy` to internal/app/cmd/doctor_artifacts.go (same file as
checkOrphanedArtifacts per CONTEXT). Nil client or empty bucket → CheckSkipped.

Hardcode `transientPrefixes := []string{"logs/", "remote-create/", "agent-runs/", "slack-inbound/"}`.
GetBucketLifecycleConfiguration; on NoSuchLifecycleConfiguration treat as empty (errors.As).
Determine the uncovered subset: a transient prefix is "covered" if an existing rule has a prefix
Filter (or And-filter prefix) equal to it AND an Expiration set. If none uncovered → CheckOK.

If uncovered && (dryRun || !setLifecycle) → CheckWarn with the uncovered list + hint
"use --dry-run=false --set-s3-lifecycle to install" / "use --set-s3-lifecycle to install".

Else build one s3types.LifecycleRule per uncovered prefix (Status=Enabled, deterministic ID like
"km-doctor-expire-"+sanitized-prefix, Filter=prefix, Expiration.Days=expireDays), APPEND to the
existing rules (de-dup by rule ID so a partial prior run is idempotent), and
PutBucketLifecycleConfiguration with the merged set. Return CheckOK reporting how many rules added.
NEVER add a rule for toolchain/ , sidecars/ , rsync/.

Add to doctor_artifacts_test.go (mockS3Lifecycle from Wave 1):
TestDoctor_S3LifecyclePolicy_Missing (NoSuchLifecycleConfiguration → WARN, no Put under dry-run),
_AlreadySet (all four covered → OK, zero Put calls), _SetMergesPreservesExisting (an unrelated
operator rule + missing transient prefixes; set → Put called once whose Rules include the
unrelated rule untouched + the new transient rules, and exclude any build-artifact prefix),
_Idempotent (second set run → OK, no Put). Mock records Put input so rule-set assertions hold.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'S3Lifecycle' -count=1</automated>
  </verify>
  <done>Missing → WARN; set installs transient-only expiry rules; existing rules preserved; build-artifact prefixes untouched; idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Register checkS3LifecyclePolicy in buildChecks</name>
  <files>internal/app/cmd/doctor.go</files>
  <action>
In buildChecks (doctor.go, beside checkOrphanedArtifacts ~line 3070) append:
```go
s3Lifecycle := deps.S3LifecycleClient
s3LifecycleBucket := cfg.GetArtifactsBucket()
setS3Lifecycle := deps.SetS3Lifecycle
s3ExpireDays := int32(cfg.GetDoctorS3ExpireDays())
checks = append(checks, func(ctx context.Context) CheckResult {
    return checkS3LifecyclePolicy(ctx, s3Lifecycle, s3LifecycleBucket, s3ExpireDays, dryRun, setS3Lifecycle)
})
```
Reuse the `dryRun` local already in scope. (artifactsBucket may already be a local from the
sidecar-artifacts check — reuse it if present rather than re-fetching.)
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -count=1</automated>
  </verify>
  <done>Check appears in the buildChecks slice and runs without regressing the existing suite.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/ -run 'S3Lifecycle' -count=1` green.
- `go test ./internal/app/cmd/... -count=1` green.
- `make build` succeeds.
</verification>

<success_criteria>
- Missing expiry on transient prefixes → WARN; --set-s3-lifecycle installs transient-only rules.
- Existing/unrelated rules preserved (merge, not overwrite).
- Build-artifact prefixes never expired; idempotent on re-run.
- Check registered in buildChecks.
</success_criteria>

<output>
After completion, create `.planning/phases/94-km-doctor-leaked-per-sandbox-debris-cleanup-log-groups-ddb-rows-s3-lifecycle/94-04-SUMMARY.md`
</output>
