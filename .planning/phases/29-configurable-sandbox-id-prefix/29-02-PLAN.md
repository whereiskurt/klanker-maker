---
phase: 29-configurable-sandbox-id-prefix
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - internal/app/cmd/destroy.go
  - internal/app/cmd/destroy_test.go
  - internal/app/cmd/sandbox_ref.go
  - internal/app/cmd/sandbox_ref_test.go
  - cmd/email-create-handler/main.go
  - cmd/email-create-handler/main_test.go
  - profiles/claude-dev.yaml
autonomous: true
requirements: [PREFIX-03, PREFIX-04, PREFIX-05]

must_haves:
  truths:
    - "km destroy claude-abc12345 is accepted as a valid sandbox ID"
    - "ResolveSandboxID recognizes claude-abc12345 as an ID, not a numeric reference"
    - "Email handler extractSandboxID('status claude-abc12345') returns 'claude-abc12345' (full ID with prefix)"
    - "Email handler does not prepend sb- to IDs with custom prefixes"
    - "claude-dev.yaml built-in profile has metadata.prefix: claude"
    - "Profiles without metadata.prefix still generate sb-* IDs (backwards compat)"
  artifacts:
    - path: "internal/app/cmd/destroy.go"
      provides: "Generalized sandbox ID validation pattern"
      contains: "[a-z][a-z0-9]{0,11}-[a-f0-9]{8}"
    - path: "internal/app/cmd/sandbox_ref.go"
      provides: "Generalized sandbox ID detection"
      contains: "[a-z][a-z0-9]{0,11}-[a-f0-9]{8}"
    - path: "cmd/email-create-handler/main.go"
      provides: "Full ID extraction without sb- repair"
    - path: "profiles/claude-dev.yaml"
      provides: "Built-in profile with custom prefix"
      contains: "prefix: claude"
  key_links:
    - from: "internal/app/cmd/destroy.go"
      to: "pkg/compiler/sandbox_id.go"
      via: "Uses same generalized pattern as IsValidSandboxID"
      pattern: "[a-z][a-z0-9]{0,11}-[a-f0-9]{8}"
    - from: "cmd/email-create-handler/main.go"
      to: "extractSandboxID function"
      via: "Regex captures full prefix-hex ID without repair"
      pattern: "sandboxIDPattern"
---

<objective>
Generalize all sandbox ID validation and matching patterns to accept any valid prefix, and fix the email handler's broken prefix-repair logic.

Purpose: Complete the configurable prefix feature by ensuring all downstream consumers (destroy command, sandbox ref resolution, email handler) accept sandbox IDs with any valid prefix. Update built-in profiles.

Output: Generalized regex in destroy.go and sandbox_ref.go, fixed extraction in email-create-handler, claude-dev.yaml with prefix: claude.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-configurable-sandbox-id-prefix/29-RESEARCH.md
@.planning/phases/29-configurable-sandbox-id-prefix/29-01-SUMMARY.md

@internal/app/cmd/destroy.go
@internal/app/cmd/destroy_test.go
@internal/app/cmd/sandbox_ref.go
@cmd/email-create-handler/main.go
@cmd/email-create-handler/main_test.go
@profiles/claude-dev.yaml

<interfaces>
<!-- From Plan 01 outputs -->
From pkg/compiler/sandbox_id.go:
```go
func GenerateSandboxID(prefix string) string  // prefix="" defaults to "sb"
func IsValidSandboxID(id string) bool          // validates ^[a-z][a-z0-9]{0,11}-[a-f0-9]{8}$
```

From pkg/profile/types.go:
```go
type Metadata struct {
    Name   string            `yaml:"name"`
    Labels map[string]string `yaml:"labels,omitempty"`
    Prefix string            `yaml:"prefix,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize destroy.go and sandbox_ref.go validation patterns</name>
  <files>internal/app/cmd/destroy.go, internal/app/cmd/destroy_test.go, internal/app/cmd/sandbox_ref.go, internal/app/cmd/sandbox_ref_test.go</files>
  <behavior>
    - Test: sandboxIDPattern in destroy.go matches "sb-abc12345" (existing format)
    - Test: sandboxIDPattern in destroy.go matches "claude-abc12345" (custom prefix)
    - Test: sandboxIDPattern in destroy.go matches "a-abc12345" (single-char prefix)
    - Test: sandboxIDPattern in destroy.go rejects "ABC-abc12345" (uppercase prefix)
    - Test: sandboxIDPattern in destroy.go rejects "abc12345" (no prefix/dash)
    - Test: ResolveSandboxID("claude-abc12345", ...) returns "claude-abc12345" (recognized as ID)
    - Test: ResolveSandboxID("sb-abc12345", ...) returns "sb-abc12345" (backwards compat)
    - Test: ResolveSandboxID("3", ...) falls through to numeric lookup (not an ID)
  </behavior>
  <action>
    1. In `internal/app/cmd/destroy.go`:
       - Replace `var sandboxIDPattern = regexp.MustCompile(` + "`" + `^sb-[a-f0-9]{8}$` + "`" + `)` with:
         `var sandboxIDPattern = regexp.MustCompile(` + "`" + `^[a-z][a-z0-9]{0,11}-[a-f0-9]{8}$` + "`" + `)`
       - Update the comment above it to: `// sandboxIDPattern matches valid sandbox IDs: {prefix}-{8hex}`
       - Optionally, replace the local regex usage with `compiler.IsValidSandboxID()` from Plan 01 if cleaner. Either approach is acceptable.

    2. In `internal/app/cmd/sandbox_ref.go`:
       - Replace `if strings.HasPrefix(ref, "sb-") { return ref, nil }` with a regex check:
         `var sandboxIDLike = regexp.MustCompile(` + "`" + `^[a-z][a-z0-9]{0,11}-[a-f0-9]{8}$` + "`" + `)`
         `if sandboxIDLike.MatchString(ref) { return ref, nil }`
       - Or use `compiler.IsValidSandboxID(ref)` from Plan 01. Either approach is acceptable.
       - Remove the `strings` import if no longer needed (check for other usages first).

    3. In `internal/app/cmd/destroy_test.go`:
       - Add test cases asserting the new pattern accepts `claude-abc12345`, `build-abc12345`, `a-abc12345`
       - Add test cases asserting the pattern rejects `ABC-abc12345`, `abc12345`, empty string

    4. In `internal/app/cmd/sandbox_ref_test.go` (create if it doesn't exist):
       - Add test cases for `ResolveSandboxID` with custom-prefix IDs
       - Verify `claude-abc12345` is recognized as a valid ID (returns as-is)
       - Verify numeric strings still fall through to the numeric lookup path

    IMPORTANT (from research Pitfall 1): Both destroy.go and sandbox_ref.go MUST be updated together. If only one is changed, `km destroy claude-abc12345` will fail at whichever check still has the old pattern.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -run "TestDestroy|TestResolveSandboxID|TestSandboxID" -v -count=1</automated>
  </verify>
  <done>Both destroy.go and sandbox_ref.go use generalized sandbox ID pattern accepting any valid prefix. Tests confirm claude-* IDs are accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix email handler extraction and update built-in profiles</name>
  <files>cmd/email-create-handler/main.go, cmd/email-create-handler/main_test.go, profiles/claude-dev.yaml</files>
  <behavior>
    - Test: extractSandboxID("status claude-abc12345") returns "claude-abc12345"
    - Test: extractSandboxID("status sb-abc12345") returns "sb-abc12345"
    - Test: extractSandboxID("status build-abc12345") returns "build-abc12345"
    - Test: extractSandboxID("some text without an id") returns "" (no match)
    - Test: The sb- prefix repair block (HasPrefix + prepend) is removed
    - Test: claude-dev.yaml has metadata.prefix: claude and passes km validate
  </behavior>
  <action>
    1. In `cmd/email-create-handler/main.go`:
       - Update `sandboxIDPattern` regex (around line 112) to capture the FULL ID including prefix:
         `var sandboxIDPattern = regexp.MustCompile(` + "`" + `(?i)\b([a-z][a-z0-9]{0,11}-[0-9a-f]{8})\b` + "`" + `)`
         This captures the complete `{prefix}-{8hex}` form in group 1.
       - Remove the prefix-repair block at lines 246-247:
         ```go
         // DELETE THESE LINES:
         // if !strings.HasPrefix(sandboxID, "sb-") {
         //     sandboxID = "sb-" + sandboxID
         // }
         ```
       - Update the help text at line ~313 from `"status sb-abc123de"` to `"status <sandbox-id>"` (cosmetic).
       - Also update the `GenerateSandboxID()` call in the create handler to pass the prefix from the profile. Search for the call site — if the handler has access to the resolved profile, pass `resolvedProfile.Metadata.Prefix`; otherwise pass `""` for default behavior.

    2. In `cmd/email-create-handler/main_test.go`:
       - Update/add test cases for `extractSandboxID` to verify the full ID is returned (not just hex portion)
       - Test with `claude-abc12345` in subject — must return `"claude-abc12345"`, NOT `"sb-abc12345"`
       - Test with `sb-abc12345` in subject — must return `"sb-abc12345"` (backwards compat)
       - Test with no ID in subject — must return empty string

    3. In `profiles/claude-dev.yaml`:
       - Add `prefix: claude` to the `metadata` section (after `name` and `labels`)

    4. Run `km validate profiles/claude-dev.yaml` to confirm the updated profile passes validation.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./cmd/email-create-handler/... -run "TestExtractSandboxID|TestSandboxID" -v -count=1 && go build -o /dev/null ./cmd/km/ && go build -o /dev/null ./cmd/email-create-handler/</automated>
  </verify>
  <done>Email handler extracts full sandbox IDs with any prefix, sb- repair block removed, claude-dev.yaml has prefix: claude, all tests and builds pass</done>
</task>

</tasks>

<verification>
```bash
cd /Users/khundeck/working/klankrmkr && go test ./... -count=1
```
Full test suite passes. No package has compilation errors. `km validate profiles/claude-dev.yaml` succeeds.
</verification>

<success_criteria>
- destroy.go pattern accepts claude-abc12345 and sb-abc12345
- sandbox_ref.go recognizes claude-abc12345 as a valid sandbox ID
- Email handler extractSandboxID returns full ID with any prefix
- Email handler sb- prefix repair block is removed
- claude-dev.yaml has metadata.prefix: claude
- Full test suite passes with go test ./...
- No component hardcodes the sb- prefix (PREFIX-04 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/29-configurable-sandbox-id-prefix/29-02-SUMMARY.md`
</output>
