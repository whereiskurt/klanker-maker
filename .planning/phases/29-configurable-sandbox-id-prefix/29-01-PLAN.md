---
phase: 29-configurable-sandbox-id-prefix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/profile/types.go
  - pkg/profile/schemas/sandbox_profile.schema.json
  - pkg/profile/validate_test.go
  - pkg/compiler/sandbox_id.go
  - pkg/compiler/compiler_test.go
  - internal/app/cmd/create.go
autonomous: true
requirements: [PREFIX-01, PREFIX-02, PREFIX-05]

must_haves:
  truths:
    - "Profile with metadata.prefix: claude passes km validate"
    - "Profile with invalid prefix (e.g. '0bad', 'toolongprefix00') fails km validate with clear error"
    - "Profile without metadata.prefix passes km validate unchanged"
    - "GenerateSandboxID('claude') returns claude-{8hex}"
    - "GenerateSandboxID('') returns sb-{8hex} for backwards compatibility"
    - "km create with prefix: claude generates a claude-* sandbox ID"
  artifacts:
    - path: "pkg/profile/types.go"
      provides: "Metadata.Prefix field"
      contains: "Prefix string"
    - path: "pkg/profile/schemas/sandbox_profile.schema.json"
      provides: "prefix property in metadata schema"
      contains: "prefix"
    - path: "pkg/compiler/sandbox_id.go"
      provides: "Parameterized GenerateSandboxID and IsValidSandboxID"
      exports: ["GenerateSandboxID", "IsValidSandboxID"]
  key_links:
    - from: "internal/app/cmd/create.go"
      to: "pkg/compiler/sandbox_id.go"
      via: "compiler.GenerateSandboxID(resolvedProfile.Metadata.Prefix)"
      pattern: "GenerateSandboxID\\(.*Prefix"
    - from: "pkg/profile/types.go"
      to: "pkg/profile/schemas/sandbox_profile.schema.json"
      via: "Metadata.Prefix field matches schema prefix property"
      pattern: "prefix"
---

<objective>
Add configurable sandbox ID prefix to the profile schema and ID generation layer.

Purpose: Allow operators to define meaningful sandbox ID prefixes (e.g., `claude`, `build`, `research`) per profile, replacing the hardcoded `sb-` prefix. Profiles without a prefix default to `sb` for backwards compatibility.

Output: Updated Metadata struct with Prefix field, updated JSON Schema, parameterized GenerateSandboxID function, IsValidSandboxID helper, wired call site in create.go.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-configurable-sandbox-id-prefix/29-RESEARCH.md

@pkg/profile/types.go
@pkg/profile/schemas/sandbox_profile.schema.json
@pkg/profile/validate_test.go
@pkg/compiler/sandbox_id.go
@pkg/compiler/compiler_test.go
@internal/app/cmd/create.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata.prefix to profile schema and types</name>
  <files>pkg/profile/types.go, pkg/profile/schemas/sandbox_profile.schema.json, pkg/profile/validate_test.go</files>
  <behavior>
    - Test: Profile YAML with `metadata.prefix: claude` passes schema validation
    - Test: Profile YAML with `metadata.prefix: build` passes schema validation
    - Test: Profile YAML with `metadata.prefix: a` (minimum valid -- 1 char) passes schema validation
    - Test: Profile YAML with `metadata.prefix: abcdefghijkl` (maximum valid -- 12 chars) passes schema validation
    - Test: Profile YAML with `metadata.prefix: "0bad"` (starts with digit) fails schema validation
    - Test: Profile YAML with `metadata.prefix: "toolongprefix0"` (>12 chars) fails schema validation
    - Test: Profile YAML with `metadata.prefix: "Bad"` (uppercase) fails schema validation
    - Test: Profile YAML with `metadata.prefix: "has-dash"` (non-alphanumeric) fails schema validation
    - Test: Profile YAML without `metadata.prefix` passes schema validation unchanged (existing tests cover this)
  </behavior>
  <action>
    1. Add `Prefix string` field to `Metadata` struct in `pkg/profile/types.go`:
       ```go
       type Metadata struct {
           Name   string            `yaml:"name"`
           Labels map[string]string `yaml:"labels,omitempty"`
           Prefix string            `yaml:"prefix,omitempty"`
       }
       ```

    2. Add `prefix` property to the `metadata.properties` block in `pkg/profile/schemas/sandbox_profile.schema.json`:
       ```json
       "prefix": {
         "type": "string",
         "pattern": "^[a-z][a-z0-9]{0,11}$",
         "description": "Optional sandbox ID prefix. Defaults to 'sb' when omitted."
       }
       ```
       Note: The `metadata` object already has `additionalProperties: false` -- the field MUST be declared in the schema or profiles using it will be rejected.

    3. Add test cases to `pkg/profile/validate_test.go` for prefix validation (valid values, invalid values). Use a table-driven test named `TestValidateSchema_MetadataPrefix` or extend the existing schema validation test with additional cases.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/profile/... -run TestValidate -v -count=1</automated>
  </verify>
  <done>Metadata struct has Prefix field, JSON Schema validates prefix pattern, all new and existing validation tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize GenerateSandboxID and wire prefix from profile</name>
  <files>pkg/compiler/sandbox_id.go, pkg/compiler/compiler_test.go, internal/app/cmd/create.go</files>
  <behavior>
    - Test: GenerateSandboxID("claude") returns string matching `^claude-[a-f0-9]{8}$`
    - Test: GenerateSandboxID("build") returns string matching `^build-[a-f0-9]{8}$`
    - Test: GenerateSandboxID("") returns string matching `^sb-[a-f0-9]{8}$` (backwards compat)
    - Test: GenerateSandboxID("a") returns string matching `^a-[a-f0-9]{8}$`
    - Test: IsValidSandboxID("sb-abc12345") returns true
    - Test: IsValidSandboxID("claude-abc12345") returns true
    - Test: IsValidSandboxID("abc12345") returns false (no prefix)
    - Test: IsValidSandboxID("") returns false
    - Test: IsValidSandboxID("SB-abc12345") returns false (uppercase)
  </behavior>
  <action>
    1. Update `pkg/compiler/sandbox_id.go`:
       - Change `GenerateSandboxID()` signature to `GenerateSandboxID(prefix string) string`
       - If `prefix == ""`, default to `"sb"`
       - Return `prefix + "-" + hex[:8]`
       - Add `IsValidSandboxID(id string) bool` that validates against `^[a-z][a-z0-9]{0,11}-[a-f0-9]{8}$`

    2. Update `pkg/compiler/compiler_test.go`:
       - Update existing `TestGenerateSandboxID` to call `GenerateSandboxID("")` and verify `sb-` prefix
       - Add sub-tests for custom prefixes (`"claude"`, `"build"`, `"a"`)
       - Add `TestIsValidSandboxID` with table-driven cases

    3. Update `internal/app/cmd/create.go`:
       - Find the `GenerateSandboxID()` call in `runCreate` (around line 165-167)
       - Change to `compiler.GenerateSandboxID(resolvedProfile.Metadata.Prefix)`
       - If `--sandbox-id` override is provided, validate it with `compiler.IsValidSandboxID()` before use; return error if invalid

    4. Fix ALL other callers of `GenerateSandboxID()` that now need the prefix argument. Search with: `grep -rn "GenerateSandboxID" --include="*.go"` to find them.
       - `internal/app/cmd/create.go` line ~863 in `runCreateRemote`: change `compiler.GenerateSandboxID()` to `compiler.GenerateSandboxID(resolvedProfile.Metadata.Prefix)` -- `resolvedProfile` is already in scope in that function.
       - For the email-create-handler call site in `cmd/email-create-handler/main.go`, pass `""` (empty string) for now to maintain compilation; Plan 02 will wire it properly.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./pkg/compiler/... ./internal/app/cmd/... -run "TestGenerateSandboxID|TestIsValidSandboxID" -v -count=1 && go build ./...</automated>
  </verify>
  <done>GenerateSandboxID accepts prefix parameter, IsValidSandboxID helper exists, both runCreate and runCreateRemote in create.go pass profile prefix, all callers compile, tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /Users/khundeck/working/klankrmkr && go test ./pkg/profile/... ./pkg/compiler/... ./internal/app/cmd/... -v -count=1
```
Full test suite for affected packages passes. `go build ./...` compiles cleanly.
</verification>

<success_criteria>
- Metadata.Prefix field exists in types.go with yaml:"prefix,omitempty" tag
- JSON Schema metadata block includes prefix property with pattern validation
- GenerateSandboxID("claude") produces claude-{8hex} format IDs
- GenerateSandboxID("") produces sb-{8hex} format IDs (backwards compat)
- IsValidSandboxID validates generalized sandbox ID pattern
- create.go passes resolvedProfile.Metadata.Prefix to GenerateSandboxID in both runCreate and runCreateRemote
- All tests pass, all packages compile
</success_criteria>

<output>
After completion, create `.planning/phases/29-configurable-sandbox-id-prefix/29-01-SUMMARY.md`
</output>
