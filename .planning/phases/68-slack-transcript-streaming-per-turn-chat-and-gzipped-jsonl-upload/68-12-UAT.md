---
phase: 68-slack-transcript-streaming-per-turn-chat-and-gzipped-jsonl-upload
plan: 12
type: uat
created: 2026-05-03
status: pending-operator-verification
scenarios_total: 9
scenarios_passed: 0
scenarios_failed: 0
scenarios_pending: 9
---

# Phase 68 Plan 12 — UAT Scenarios + Results

End-to-end manual verification of Slack transcript streaming (Phase 68) against
a real Slack workspace + AWS account. Each scenario has a checklist of
pass-criteria and a free-form **Evidence** section for logs, screenshots, and
command output. After all 9 scenarios are run, the operator marks the **Sign-off**
section at the bottom.

## Pre-UAT — full Go test suite

`go test ./... -timeout 300s -count=1` was executed during Plan 12 Task 3.
Result summary captured in `/tmp/68-test-summary.txt` and reproduced below.

**Disposition:** all FAIL test names confirmed pre-existing on the baseline
(see `deferred-items.md` § "Plan 68-12 confirmation"). Plan 12 is
documentation-only and introduces zero Go source changes. Phase 68 in-scope
unit tests (validate_slack_transcript, profile schema, km-slack upload, bridge
upload, doctor_slack_transcript, hook PostToolUse + Stop) are all green per
each plan's individual SUMMARY.md.

```
ok    github.com/whereiskurt/klankrmkr/cmd/budget-enforcer
ok    github.com/whereiskurt/klankrmkr/cmd/create-handler
ok    github.com/whereiskurt/klankrmkr/cmd/email-create-handler
ok    github.com/whereiskurt/klankrmkr/cmd/km-slack-bridge
ok    github.com/whereiskurt/klankrmkr/internal/app/config
ok    github.com/whereiskurt/klankrmkr/pkg/allowlistgen
ok    github.com/whereiskurt/klankrmkr/pkg/at
ok    github.com/whereiskurt/klankrmkr/pkg/aws
ok    github.com/whereiskurt/klankrmkr/pkg/ebpf/audit
ok    github.com/whereiskurt/klankrmkr/pkg/ebpf/resolver
ok    github.com/whereiskurt/klankrmkr/pkg/github
ok    github.com/whereiskurt/klankrmkr/pkg/lifecycle
ok    github.com/whereiskurt/klankrmkr/pkg/localnumber
ok    github.com/whereiskurt/klankrmkr/pkg/profile
ok    github.com/whereiskurt/klankrmkr/pkg/slack
ok    github.com/whereiskurt/klankrmkr/pkg/slack/bridge
ok    github.com/whereiskurt/klankrmkr/pkg/terragrunt
ok    github.com/whereiskurt/klankrmkr/sidecars/audit-log
ok    github.com/whereiskurt/klankrmkr/sidecars/audit-log/cmd
ok    github.com/whereiskurt/klankrmkr/sidecars/dns-proxy/dnsproxy
ok    github.com/whereiskurt/klankrmkr/sidecars/http-proxy/httpproxy
ok    github.com/whereiskurt/klankrmkr/test/e2e/slack
FAIL  github.com/whereiskurt/klankrmkr/cmd/configui            (pre-existing — schema/test drift)
FAIL  github.com/whereiskurt/klankrmkr/cmd/km-slack            (pre-existing — TestKmSlackPost_BridgeReturns503ThenSuccess_Exit0)
FAIL  github.com/whereiskurt/klankrmkr/cmd/ttl-handler         (pre-existing — package timeout)
FAIL  github.com/whereiskurt/klankrmkr/internal/app/cmd        (pre-existing — multiple state-bucket / docker / shell tests)
FAIL  github.com/whereiskurt/klankrmkr/pkg/compiler            (pre-existing — TestUserDataNotifyEnv_*)
```

- [x] Pre-UAT acknowledged: in-scope unit tests green; pre-existing failures triaged in `deferred-items.md`

---

## Scenario 1 — End-to-end stream visible in Slack thread

```bash
make build && km init --sidecars && km init
km create profiles/test-transcript.yaml   # profile with notifySlackTranscriptEnabled: true
km agent run sb-X --prompt "audit and fix the failing tests in this repo"
```

**Pass criteria:**
- [ ] Per-turn `🔧 ToolName: input` messages appear in `#sb-X` thread
- [ ] Final transcript `claude-transcript-{session_id}.jsonl.gz` posted as Slack file
- [ ] Click file → downloads → `gzip -d` produces valid JSONL → lines parse

**Result:** `___` (PASS / FAIL / N/A)

**Evidence:**
```
(operator-pasted log excerpts, screenshots, S3 key URIs, etc.)
```

---

## Scenario 2 — Auto-thread-parent for operator runs

**Pass criteria:**
- [ ] First fire creates parent `🤖 [sb-X] turn started — {prompt}` in channel root
- [ ] Subsequent fires thread under it
- [ ] After Stop, `/tmp/km-slack-thread.{sid}` is removed

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 3 — Phase 67 regression (inbound + transcript both on)

Profile: `notifySlackInboundEnabled: true` AND `notifySlackTranscriptEnabled: true`.

**Pass criteria:**
- [ ] Slack message in `#sb-X` triggers Phase 67 poller dispatch
- [ ] Reply streams in same thread (no new auto-parent)
- [ ] Final upload threaded under same parent

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 4 — Phase 63 regression (idle-ping unchanged)

Profile: `notifySlackTranscriptEnabled: false` (default).

**Pass criteria:**
- [ ] Phase 63 single idle-ping arrives at Stop
- [ ] No per-turn streaming
- [ ] No upload

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 5 — `files:write` scope missing path (failure injection)

Steps: Slack admin → remove `files:write` from bot → force bridge cold start
(`km slack rotate-token --bot-token <existing>`) → run a transcript-enabled
prompt → restore scope and re-run.

**Pass criteria:**
- [ ] Streaming continues
- [ ] Upload bridge call returns 400
- [ ] Hook stderr: `WARN: operator must re-auth Slack App with files:write`
- [ ] Hook still exits 0 (Claude not blocked)
- [ ] After restoring `files:write`, next run succeeds

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 6 — Operator warning at `km create`

```bash
km create profiles/test-transcript.yaml 2>&1 | grep "transcript streaming enabled"
```

**Pass criteria:**
- [ ] Single warning line on stderr with channel ID + member count

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 7 — `km doctor` checks

```bash
km doctor
```

**Pass criteria:**
- [ ] `slack_transcript_table_exists` OK
- [ ] `slack_files_write_scope` OK
- [ ] `slack_transcript_stale_objects` OK or WARN-with-list

After destroying a sandbox without bucket lifecycle running, re-run:
- [ ] `slack_transcript_stale_objects` WARN with the destroyed sandbox's prefix

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 8 — Synthetic 100MB transcript memory test

Steps: create a synthetic 100MB JSONL on a sandbox; manually trigger upload via
`km-slack upload --size-bytes 104857600`.

**Pass criteria:**
- [ ] Lambda CloudWatch logs show < 200 MB peak memory
- [ ] End-to-end < 60 seconds
- [ ] File appears in Slack and is openable

**Result:** `___`

**Evidence:**
```

```

---

## Scenario 9 — `km destroy` cleanup decision

**Decision (recorded in plan):** Phase 68 relies on the existing artifacts
bucket lifecycle policy for transcript cleanup (NOT explicit `km destroy`
deletion). Operators who need faster cleanup can manually
`aws s3 rm --recursive s3://${BUCKET}/transcripts/{sandbox-id}/` after destroy.

**Pass criteria:**
- [ ] Decision is documented in the UAT outcomes (this section)
- [ ] Operator acknowledges the decision and confirms bucket lifecycle is
      configured (or manual cleanup is acceptable)

**Result:** `___`

**Evidence / Notes:**
```

```

---

## Sign-off

- [ ] All 9 scenarios PASS, OR have documented follow-up tickets for Phase 68.1
- [ ] No regression observed in Phase 63 (idle-ping) or Phase 67 (inbound chat)
- [ ] Documentation (`docs/slack-notifications.md`, `CLAUDE.md`) accurately matches the implementation observed during UAT

**Operator name:** ___________________
**Date:** ___________________
**Verdict:** SHIP / HOLD / RE-PLAN

**Follow-up tickets opened (if any):**
```

```

---

## How to mark this UAT complete

When all 9 scenarios have a PASS/FAIL recorded and the Sign-off section is
filled in, reply to the executor with `approved` (or describe issues with
specific scenario references). The executor will then update STATE.md and
ROADMAP.md to mark Phase 68 verified-and-shipped.
