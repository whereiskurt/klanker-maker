---
phase: 65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - internal/app/cmd/bootstrap.go
  - internal/app/cmd/init.go
  - internal/app/cmd/uninit.go
  - internal/app/cmd/create.go
  - internal/app/cmd/info.go
  - internal/app/cmd/configure.go
  - internal/app/cmd/bootstrap_test.go
  - internal/app/cmd/configure_test.go
  - internal/app/cmd/info_test.go
  - internal/app/cmd/init_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "go build ./... succeeds (excluding doctor.go which plan 03 fixes — but cmd package compiles)"
    - "km bootstrap --dry-run with OrganizationAccountID set prints 'Organization account:' line"
    - "km bootstrap --dry-run with OrganizationAccountID blank prints SCP-skipped message naming accounts.organization"
    - "km bootstrap (real apply) skips terragrunt SCP module when OrganizationAccountID blank"
    - "km bootstrap (real apply) invokes terragrunt SCP module when OrganizationAccountID set"
    - "km show-prereqs returns nil + descriptive message when OrganizationAccountID blank (no fatal error)"
    - "km configure --non-interactive accepts --organization-account and --dns-parent-account, no longer accepts --management-account"
    - "km info prints two lines: Organization and DNS parent"
    - "km init exports KM_ACCOUNTS_ORGANIZATION and KM_ACCOUNTS_DNS_PARENT, never KM_ACCOUNTS_MANAGEMENT"
  artifacts:
    - path: "internal/app/cmd/bootstrap.go"
      provides: "SCP gate keyed on OrganizationAccountID, dry-run wording, env exports"
      contains: "OrganizationAccountID"
    - path: "internal/app/cmd/init.go"
      provides: "KM_ACCOUNTS_ORGANIZATION + KM_ACCOUNTS_DNS_PARENT exports, DNS gate on dns_parent"
      contains: "KM_ACCOUNTS_ORGANIZATION"
    - path: "internal/app/cmd/configure.go"
      provides: "--organization-account + --dns-parent-account flags, accountsConfig struct fields"
      contains: "organization-account"
    - path: "internal/app/cmd/info.go"
      provides: "Organization + DNS parent display lines"
      contains: "DNS parent"
  key_links:
    - from: "bootstrap.go runBootstrap"
      to: "config.Config.OrganizationAccountID"
      via: "if loadedCfg.OrganizationAccountID != \"\" gate around terragrunt SCP apply"
      pattern: "OrganizationAccountID\\s*!=\\s*\"\""
    - from: "init.go env exports"
      to: "site.hcl get_env(\"KM_ACCOUNTS_ORGANIZATION\")"
      via: "os.Setenv(\"KM_ACCOUNTS_ORGANIZATION\", cfg.OrganizationAccountID)"
      pattern: "KM_ACCOUNTS_ORGANIZATION"
    - from: "configure.go flag parsing"
      to: "accountsConfig.Organization + accountsConfig.DNSParent YAML output"
      via: "--organization-account and --dns-parent-account flags written to km-config.yaml"
      pattern: "organization-account|dns-parent-account"
---

<objective>
Migrate every Go-side caller of the renamed config fields. After this plan, `go build ./...` will compile successfully (modulo doctor.go which plan 03 fixes), and the bootstrap/init/configure/info/create/uninit commands all read the new fields, export the new env vars, and surface the new operator-facing strings.

Purpose: Plan 01 made the type rename non-negotiable by deleting `ManagementAccountID`. This plan fans out to every caller so the cmd package compiles again. The work is mechanical-but-semantic: each call site decides whether the legacy `ManagementAccountID` semantically referred to "org-management" (SCP) or "DNS parent zone owner" — that decision determines which of the two new fields it migrates to.

Output: Six production files updated, four test files filled in (Wave 0 skip-stubs from plan 01 get real bodies), all twelve plan-02 VALIDATION.md task IDs (65-02-01 through 65-02-09) green.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-CONTEXT.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-RESEARCH.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-VALIDATION.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-01-SUMMARY.md
@internal/app/config/config.go
@internal/app/cmd/bootstrap.go
@internal/app/cmd/init.go
@internal/app/cmd/uninit.go
@internal/app/cmd/create.go
@internal/app/cmd/info.go
@internal/app/cmd/configure.go

<interfaces>
<!-- Post-rename Config fields available to this plan (set by plan 01): -->
```go
type Config struct {
    // ... existing fields ...
    Domain                string
    OrganizationAccountID string  // SCP target, optional, blank skips SCP
    DNSParentAccountID    string  // Route53 parent zone owner, blank skips DNS delegation
    ApplicationAccountID  string  // unchanged
    TerraformAccountID    string  // unchanged
}
```

<!-- Per RESEARCH.md, the precise call-site → new-field mapping: -->
| Call site | Old field | New field | Reason |
|---|---|---|---|
| bootstrap.go runShowPrereqs (~L287-300) | ManagementAccountID | OrganizationAccountID | km-org-admin role + SCP context |
| bootstrap.go runShowSCP (~L482-484) | ManagementAccountID | OrganizationAccountID | SCP-specific |
| bootstrap.go loadBootstrapConfig (~L625) | ManagementAccountID != "" | OrganizationAccountID != "" \|\| DNSParentAccountID != "" | "is cfg populated?" check |
| bootstrap.go dry-run output (~L654) | "Management account: %s" with ManagementAccountID | "Organization account: %s\nDNS parent account: %s" with both new fields | display |
| bootstrap.go SCP gate (~L728) | ManagementAccountID | OrganizationAccountID | locked SCP gate |
| bootstrap.go env exports (~L728-730) | KM_ACCOUNTS_MANAGEMENT only | KM_ACCOUNTS_ORGANIZATION (gated) + KM_ACCOUNTS_DNS_PARENT (always) | per CONTEXT.md |
| init.go env-presence check (~L258-259) | KM_ACCOUNTS_MANAGEMENT | KM_ACCOUNTS_ORGANIZATION + KM_ACCOUNTS_DNS_PARENT | dry-run preflight |
| init.go env exports (~L301-302) | ManagementAccountID gated | both new fields, each independently gated | runtime |
| init.go ensureSandboxHostedZone gate (~L326-343, comment ~L1462-1465) | DNS skip only on Domain blank | also skip if DNSParentAccountID blank, with [warn] | locked |
| create.go (~L359-360) | ManagementAccountID | both new fields | env passthrough |
| uninit.go (~L89-90) | ManagementAccountID | both new fields | env passthrough |
| info.go (~L57) | "Management: %s" | "Organization: %s\nDNS parent: %s" | display |
| configure.go accountsConfig struct (~L37-41) | Management | Organization + DNSParent | YAML output |
| configure.go flags + non-interactive (~L90, L130) | --management-account required | --organization-account optional + --dns-parent-account optional | per CONTEXT.md open-q resolution |
| configure.go interactive prompts (~L160) | one prompt | two prompts | UX |
| configure.go DNS delegation message (~L226) | uses managementAcct | uses dnsParentAcct | semantic |

<!-- Open-question resolutions, locked by plan_breakdown_guidance: -->
1. runShowPrereqs with OrganizationAccountID == "": print non-error message + return nil (NOT an error).
2. configure.go --non-interactive: --dns-parent-account is OPTIONAL (not required); --organization-account is always optional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate bootstrap.go + bootstrap_test.go (SCP gate, dry-run output, env exports, show-prereqs)</name>
  <files>internal/app/cmd/bootstrap.go, internal/app/cmd/bootstrap_test.go</files>
  <behavior>
    Tests in bootstrap_test.go (replace `t.Skip(...)` stubs from plan 01 with real bodies):

    - TestBootstrapDryRunShowsOrganizationAccount: build cfg with OrganizationAccountID="111", DNSParentAccountID="222", run runBootstrap with dryRun=true, capture stdout, assert it contains "Organization account: 111" AND "DNS parent account: 222".
    - TestBootstrapDryRunNoOrganizationAccount: cfg with OrganizationAccountID="", DNSParentAccountID="222", dryRun=true, assert stdout contains "SKIPPED" and the substring "accounts.organization" (the SCP-skip message names the new field).
    - TestBootstrapSCPApplyPath: cfg with OrganizationAccountID="111", dryRun=false, inject a mock applyFn that records calls, assert applyFn was invoked exactly once.
    - TestBootstrapSCPSkipped_OrganizationBlank: cfg with OrganizationAccountID="", dryRun=false, mock applyFn, assert applyFn NOT invoked.
    - TestShowPrereqsNoOrganizationAccount: cfg with OrganizationAccountID="", call runShowPrereqs, assert returned error is nil AND stdout contains a descriptive non-error message naming `accounts.organization`.

    Existing bootstrap_test tests must continue to pass.
  </behavior>
  <action>
    Edit `internal/app/cmd/bootstrap.go` per RESEARCH.md §bootstrap.go (11 references):

    1. **runShowPrereqs (~L287-288)**: change the `if loadedCfg.ManagementAccountID == ""` guard.
       - OLD: `if loadedCfg.ManagementAccountID == "" { return fmt.Errorf("...no management account...") }`
       - NEW: if `loadedCfg.OrganizationAccountID == ""` → print a descriptive message via `fmt.Fprintln(os.Stdout, "...")` explaining "accounts.organization not set — SCP deployment disabled. Set accounts.organization in km-config.yaml to enable org-level sandbox containment via Service Control Policies." Then `return nil` (NOT an error). Per locked open-question resolution.

    2. **runShowPrereqs body (~L300)**: rename local var `mgmtAccount := loadedCfg.ManagementAccountID` → `orgAccount := loadedCfg.OrganizationAccountID`. Update the printf format strings that build the km-org-admin role ARN to use `orgAccount` and label it as "organization" rather than "management".

    3. **runShowSCP (~L482-484)**: same pattern — `orgAccount := loadedCfg.OrganizationAccountID`, plus add a guard: if blank, print "SCP enforcement disabled — no organization account configured" and return nil.

    4. **loadBootstrapConfig (~L625)**: change the cfg-populated check from `cfg.ManagementAccountID != "" || cfg.ApplicationAccountID != "" || cfg.Domain != ""` to `cfg.OrganizationAccountID != "" || cfg.DNSParentAccountID != "" || cfg.ApplicationAccountID != "" || cfg.Domain != ""`.

    5. **Dry-run output (~L654)**: replace the `"Management account: %s\n"` line with two lines:
       - `"Organization account: %s\n"` printing OrganizationAccountID
       - `"DNS parent account: %s\n"` printing DNSParentAccountID (or "(not set)" if blank — preserve existing convention for the management line).

    6. **Dry-run SCP block (~L694-706)**: change the `if loadedCfg.ManagementAccountID != ""` guard to `if loadedCfg.OrganizationAccountID != ""`. Inside the else (skipped) branch, change the message text from "set accounts.management to enable SCP" → "set accounts.organization to enable SCP".

    7. **SCP gate + env exports (~L728-730)**: change the `if ManagementAccountID != ""` to `if OrganizationAccountID != ""` for the SCP terragrunt apply path. INSIDE the if-block, change `os.Setenv("KM_ACCOUNTS_MANAGEMENT", loadedCfg.ManagementAccountID)` to `os.Setenv("KM_ACCOUNTS_ORGANIZATION", loadedCfg.OrganizationAccountID)`. OUTSIDE the if-block (always run, regardless of org being set), ADD `os.Setenv("KM_ACCOUNTS_DNS_PARENT", loadedCfg.DNSParentAccountID)` — DNS export is unconditional because DNS is an independent concern from SCP.

    Edit `internal/app/cmd/bootstrap_test.go`:

    8. Replace each of the 5 `t.Skip(...)` stub bodies (added in plan 01) with the real test logic per the behavior block. Reuse the existing `runBootstrapCmd(t, cfg, dryRun, applyFn)` helper signature; if a mock applyFn isn't already supported, extend the helper or pass via a closure variable that the production code consults (look at how existing dry-run tests work — they likely already inject behavior via a closure or via the dryRun flag plus output capture).

    9. For TestShowPrereqsNoOrganizationAccount, capture stdout via `os.Pipe()` or whatever the existing tests use (other tests in the file that call commands directly will have a pattern).

    Do NOT touch init.go, configure.go, or any other file in this task.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run 'TestBootstrap|TestShowPrereqs' 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep -n 'ManagementAccountID' internal/app/cmd/bootstrap.go` returns no matches.
    - `grep -n 'KM_ACCOUNTS_MANAGEMENT' internal/app/cmd/bootstrap.go` returns no matches.
    - `grep -n 'KM_ACCOUNTS_ORGANIZATION\|KM_ACCOUNTS_DNS_PARENT' internal/app/cmd/bootstrap.go` returns at least 2 matches.
    - All 5 plan-02 bootstrap test functions pass (no longer SKIP).
    - VALIDATION.md task IDs 65-02-01, 65-02-02, 65-02-03, 65-02-04, 65-02-05 verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate init.go + create.go + uninit.go + info.go + their tests (env exports, DNS gate, display)</name>
  <files>internal/app/cmd/init.go, internal/app/cmd/create.go, internal/app/cmd/uninit.go, internal/app/cmd/info.go, internal/app/cmd/init_test.go, internal/app/cmd/info_test.go</files>
  <behavior>
    - TestInitExportsNewAccountEnvVars: set up a Config with OrganizationAccountID="111" and DNSParentAccountID="222", invoke the init env-export logic (the same code path that today calls os.Setenv for KM_ACCOUNTS_MANAGEMENT), assert that os.Getenv("KM_ACCOUNTS_ORGANIZATION") == "111" AND os.Getenv("KM_ACCOUNTS_DNS_PARENT") == "222" AND os.Getenv("KM_ACCOUNTS_MANAGEMENT") == "" (the old var must be empty/unset). Use t.Setenv to clean up between tests.

    - TestInfoShowsNewAccountFields: run `km info` (or call the info-printing function directly with a mocked Config) where OrganizationAccountID="111" and DNSParentAccountID="222", capture stdout, assert two distinct lines exist: one matching `^\s*Organization:\s+111\s*$` and one matching `^\s*DNS parent:\s+222\s*$`. Assert the literal "Management:" label does NOT appear.
  </behavior>
  <action>
    Edit `internal/app/cmd/init.go`:

    1. **Env-presence map (~L258-259)** in `runInitDryRun`: replace the entry `"KM_ACCOUNTS_MANAGEMENT": cfg.ManagementAccountID != ""` with TWO entries: `"KM_ACCOUNTS_ORGANIZATION": cfg.OrganizationAccountID != ""` and `"KM_ACCOUNTS_DNS_PARENT": cfg.DNSParentAccountID != ""`.

    2. **Env exports (~L301-302)**: replace the single `if cfg.ManagementAccountID != "" && os.Getenv("KM_ACCOUNTS_MANAGEMENT") == "" { os.Setenv("KM_ACCOUNTS_MANAGEMENT", cfg.ManagementAccountID) }` block with TWO independent if-blocks:
       - `if cfg.OrganizationAccountID != "" && os.Getenv("KM_ACCOUNTS_ORGANIZATION") == "" { os.Setenv("KM_ACCOUNTS_ORGANIZATION", cfg.OrganizationAccountID) }`
       - `if cfg.DNSParentAccountID != "" && os.Getenv("KM_ACCOUNTS_DNS_PARENT") == "" { os.Setenv("KM_ACCOUNTS_DNS_PARENT", cfg.DNSParentAccountID) }`

    3. **DNS gate (~L326-343)** before `ensureSandboxHostedZone` call: today the code skips zone setup if `cfg.Domain == ""`. ADD a second skip condition: if `cfg.DNSParentAccountID == ""`, log `[warn] DNS delegation skipped — accounts.dns_parent not set in km-config.yaml` and skip the zone setup block. The two skip conditions can be combined or split — keep them as separate log messages so operators see exactly why DNS was skipped.

    4. **ensureSandboxHostedZone docstring/comments (~L1462-1465)**: update the docstring to say the function "looks up the parent Route53 zone for cfg.Domain in the AWS account designated by accounts.dns_parent" and add a note: "// Note: the AWS profile name 'klanker-management' is unchanged — it's just an SDK profile identifier, not the semantic field name. Renaming the profile is out of scope for phase 65." Do NOT change the profile string itself (`awsconfig.WithSharedConfigProfile("klanker-management")` stays verbatim).

    Edit `internal/app/cmd/create.go` (~L359-360):

    5. Replace the single management env export with the same two-block pattern from init.go step 2. Use the same env-var names.

    Edit `internal/app/cmd/uninit.go` (~L89-90):

    6. Same two-block pattern.

    Edit `internal/app/cmd/info.go` (~L57):

    7. Replace the line `fmt.Printf("  Management:       %s\n", cfg.ManagementAccountID)` with TWO lines: `fmt.Printf("  Organization:     %s\n", cfg.OrganizationAccountID)` and `fmt.Printf("  DNS parent:       %s\n", cfg.DNSParentAccountID)`. Match the column-alignment width used by adjacent lines (Application, Terraform).

    Edit `internal/app/cmd/init_test.go`:

    8. Replace `t.Skip(...)` body of TestInitExportsNewAccountEnvVars with the real test per the behavior block. Use `t.Setenv` for cleanup.

    Edit `internal/app/cmd/info_test.go`:

    9. Replace `t.Skip(...)` body of TestInfoShowsNewAccountFields with real assertions. If the file was created from scratch in plan 01, ensure `package cmd` and required imports (`testing`, `bytes`, `strings`, plus whatever info.go's print fn signature requires).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run 'TestInitExportsNewAccountEnvVars|TestInfoShowsNewAccountFields' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -rn 'ManagementAccountID' internal/app/cmd/init.go internal/app/cmd/create.go internal/app/cmd/uninit.go internal/app/cmd/info.go` returns no matches.
    - `grep -rn 'KM_ACCOUNTS_MANAGEMENT' internal/app/cmd/init.go internal/app/cmd/create.go internal/app/cmd/uninit.go` returns no matches.
    - `grep -rn 'KM_ACCOUNTS_ORGANIZATION' internal/app/cmd/init.go internal/app/cmd/create.go internal/app/cmd/uninit.go` returns matches in all three files.
    - `grep -n 'klanker-management' internal/app/cmd/init.go` still returns matches (profile name unchanged — out of scope).
    - VALIDATION.md task IDs 65-02-08 + 65-02-09 verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate configure.go + configure_test.go (flags, struct, prompts, DNS message)</name>
  <files>internal/app/cmd/configure.go, internal/app/cmd/configure_test.go</files>
  <behavior>
    - TestConfigureWritesOrganizationAndDNSParent: build km via `buildKM(t)`, run with `--non-interactive --organization-account 111111111111 --dns-parent-account 222222222222 --application-account 333333333333 --terraform-account 333333333333` (plus any required domain flag), parse the produced km-config.yaml, assert: `accounts["organization"] == "111111111111"`, `accounts["dns_parent"] == "222222222222"`, key `"management"` is ABSENT from the accounts map.

    - TestConfigureInteractivePromptsUseNewNames: feed the interactive wizard via stdin with answers, capture stderr/stdout, assert the prompt text contains the literal strings "Organization" (the new prompt name) and "DNS parent" (or "DNS Parent"). Assert the prompt "Management" no longer appears.
  </behavior>
  <action>
    Edit `internal/app/cmd/configure.go` per RESEARCH.md §configure.go (8 references):

    1. **accountsConfig struct (~L37-41)**: rename field `Management string \`yaml:"management"\`` → DELETE. ADD two new fields:
       - `Organization string \`yaml:"organization,omitempty"\``
       - `DNSParent    string \`yaml:"dns_parent,omitempty"\``
       Keep existing `Application` and `Terraform` fields unchanged.

    2. **Variable + flag declarations (~L59, ~L90-91)**: split `managementAcct` into two variables `organizationAcct` and `dnsParentAcct`. Replace the `--management-account` flag with two new flags: `--organization-account` (string, default "") and `--dns-parent-account` (string, default ""). Update flag descriptions accordingly.

    3. **runConfigure signature (~L120)**: change `func runConfigure(... managementAcct string ...)` to `func runConfigure(... organizationAcct, dnsParentAcct string ...)`. Update all callers (the cobra Run closure that wires flags into runConfigure).

    4. **Non-interactive required-fields validation (~L130-131)**: REMOVE `--management-account` from the required list. Do NOT add either new flag to the required list. Per locked open-question resolution: `--dns-parent-account` is OPTIONAL in non-interactive mode; doctor surfaces the missing config later.

    5. **Interactive wizard prompt (~L160)**: replace the single Management prompt with two prompts:
       - "AWS Organizations management account ID (optional — leave blank to skip SCP):"
       - "DNS parent zone account ID (account owning the parent Route53 zone for your domain — optional if no DNS):"

    6. **DNS delegation message (~L226)**: change `"...delegated from the management account (%s)..."` (or similar) to `"...delegated from the DNS parent account (%s)..."`, using `dnsParentAcct` not `managementAcct`. Preserve the rest of the message structure.

    7. **accountsConfig population (~L234)**: change `Management: managementAcct,` → `Organization: organizationAcct, DNSParent: dnsParentAcct,`.

    Edit `internal/app/cmd/configure_test.go`:

    8. Replace `t.Skip(...)` bodies of TestConfigureWritesOrganizationAndDNSParent and TestConfigureInteractivePromptsUseNewNames with real implementations per behavior block. Use the existing `buildKM(t)` helper. For YAML parsing, use `gopkg.in/yaml.v3` (already imported in this file per RESEARCH.md §don't-hand-roll).

    9. Existing tests in this file that pass `--management-account` — these should already have been migrated in plan 01 task 3; if any remain, migrate them to `--dns-parent-account` here.

    Do NOT touch any other file.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run 'TestConfigureWritesOrganizationAndDNSParent|TestConfigureInteractivePromptsUseNewNames|TestConfigure' 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep -n 'ManagementAccountID\|managementAcct\|--management-account\|management:' internal/app/cmd/configure.go` returns no matches (the YAML key `management` is gone too).
    - `grep -n 'organizationAcct\|dnsParentAcct\|--organization-account\|--dns-parent-account' internal/app/cmd/configure.go` returns multiple matches.
    - `accountsConfig` struct has `Organization` and `DNSParent` fields with correct YAML tags.
    - Both new test functions pass (not SKIP).
    - VALIDATION.md task IDs 65-02-06 + 65-02-07 verified.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `go build ./...` should succeed for `internal/app/cmd/` package — except doctor.go which still references the old field on its DoctorConfigProvider interface. Plan 03 fixes that. To verify cmd-without-doctor builds, run `go vet ./internal/app/cmd/init.go ./internal/app/cmd/bootstrap.go ./internal/app/cmd/configure.go ./internal/app/cmd/info.go ./internal/app/cmd/create.go ./internal/app/cmd/uninit.go` (file-level vet, not package-level — doctor.go is excluded).

2. Note: it is acceptable for `go build ./...` to fail at the package level after this plan because doctor.go still has stale references. Plan 03 makes the whole tree green again.

3. All 9 plan-02 VALIDATION.md task IDs (65-02-01 through 65-02-09) verified by their automated commands.

4. Grep audit (early signal — full audit happens in plan 04):
   - `grep -rn 'ManagementAccountID' internal/app/cmd/ | grep -v '_test.go.*Skip\|doctor'` → expected to return matches ONLY in doctor.go.
   - `grep -rn 'KM_ACCOUNTS_MANAGEMENT' internal/app/cmd/` → must return zero matches.
</verification>

<success_criteria>
- All 9 plan-02 tests in VALIDATION.md (65-02-01..09) pass.
- bootstrap.go SCP gate fires on OrganizationAccountID.
- bootstrap.go always exports KM_ACCOUNTS_DNS_PARENT, gates KM_ACCOUNTS_ORGANIZATION on the field being non-blank.
- init.go DNS skip respects DNSParentAccountID being blank.
- configure.go accepts the two new flags, neither is required in non-interactive mode.
- info.go prints two lines for the two new fields.
- All ManagementAccountID references in cmd package outside doctor.go are gone.
- Doctor.go is intentionally still broken — plan 03 fixes it.
</success_criteria>

<output>
After completion, create `.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-02-SUMMARY.md` describing:
- Per-file diff summary (which lines changed in bootstrap.go, init.go, etc).
- Open-question resolutions applied (runShowPrereqs returns nil + message; --dns-parent-account is optional).
- Confirmation that 9 plan-02 tests pass.
- Confirmation that doctor.go is the last remaining build error in the cmd package (handoff to plan 03).
</output>
