---
phase: 65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_test.go
  - internal/app/cmd/doctor_test.go
  - internal/app/cmd/bootstrap_test.go
  - internal/app/cmd/configure_test.go
  - internal/app/cmd/info_test.go
  - internal/app/cmd/init_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Loading km-config.yaml with accounts.organization sets cfg.OrganizationAccountID"
    - "Loading km-config.yaml with accounts.dns_parent sets cfg.DNSParentAccountID"
    - "Loading km-config.yaml without accounts.organization yields a blank string with no error"
    - "ManagementAccountID no longer exists on config.Config"
    - "testConfig and testDoctorConfig satisfy the post-rename DoctorConfigProvider interface (compile)"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "OrganizationAccountID + DNSParentAccountID fields, viper key bindings"
      contains: "OrganizationAccountID string"
    - path: "internal/app/config/config_test.go"
      provides: "TestLoadOrganizationAndDNSParentFields, TestLoadBlankOrganizationIsValid"
      contains: "TestLoadOrganizationAndDNSParentFields"
    - path: "internal/app/cmd/doctor_test.go"
      provides: "Updated testConfig + testDoctorConfig stubs (GetOrganizationAccountID, GetDNSParentAccountID)"
      contains: "GetOrganizationAccountID"
  key_links:
    - from: "config.Load"
      to: "viper merge keys"
      via: "v.GetString(\"accounts.organization\") + v.GetString(\"accounts.dns_parent\")"
      pattern: "accounts\\.organization|accounts\\.dns_parent"
    - from: "doctor_test.go testDoctorConfig"
      to: "DoctorConfigProvider interface (defined in doctor.go)"
      via: "interface satisfaction"
      pattern: "GetOrganizationAccountID|GetDNSParentAccountID"
---

<objective>
Rename the config struct field `ManagementAccountID` into two semantically distinct fields — `OrganizationAccountID` (SCP target, optional) and `DNSParentAccountID` (Route53 parent zone owner) — and stub all test helpers/test functions downstream plans need so they will compile after this plan ships.

Purpose: Phase 65 is a hard rename across ~44 references. Plan 01 moves the type system. Once `ManagementAccountID` is gone, every caller in plan 02/03 will fail to compile until they migrate — that is the intended forcing function. Test stubs in this plan unblock plan 02 and plan 03 from having to introduce them later.

Output: Updated config struct + viper bindings, two new config tests, test-helper accessors and test-function stubs in five `*_test.go` files (compile but skip body where appropriate).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-CONTEXT.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-RESEARCH.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-VALIDATION.md
@internal/app/config/config.go
@internal/app/config/config_test.go
@internal/app/cmd/doctor.go
@internal/app/cmd/doctor_test.go

<interfaces>
<!-- Current config.Config struct (pre-rename) — extracted from internal/app/config/config.go -->
<!-- ManagementAccountID is being removed; OrganizationAccountID + DNSParentAccountID replace it. -->

```go
// Pre-rename (DELETE):
ManagementAccountID  string

// Post-rename (ADD, after Domain field, with documentation comments):
// OrganizationAccountID is the AWS Organizations management account ID (SCP target).
// Maps to km-config.yaml key accounts.organization. Optional: blank skips SCP deployment.
OrganizationAccountID string

// DNSParentAccountID is the AWS account ID owning the parent Route53 hosted zone for cfg.Domain.
// Maps to km-config.yaml key accounts.dns_parent. Blank skips DNS delegation in km init.
DNSParentAccountID    string
```

<!-- DoctorConfigProvider interface (in doctor.go) — must be updated by plan 03, but test stubs land here -->
```go
type DoctorConfigProvider interface {
    // ... existing methods ...
    // REMOVE: GetManagementAccountID() string
    // ADD:
    GetOrganizationAccountID() string
    GetDNSParentAccountID() string
}
```

<!-- Plan 03 will update doctor.go itself. Plan 01 only updates the test stubs that mock this interface. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename config struct + viper bindings + add round-trip tests</name>
  <files>internal/app/config/config.go, internal/app/config/config_test.go</files>
  <behavior>
    - TestLoadOrganizationAndDNSParentFields: writes km-config.yaml with `accounts.organization: "111111111111"` and `accounts.dns_parent: "222222222222"`, calls config.Load(), asserts OrganizationAccountID == "111111111111" AND DNSParentAccountID == "222222222222".
    - TestLoadBlankOrganizationIsValid: writes km-config.yaml WITHOUT `accounts.organization` (only dns_parent + application + terraform), calls config.Load(), asserts no error AND OrganizationAccountID == "".
    - Existing tests must continue to pass (those that referenced ManagementAccountID get migrated to the new fields here OR stay broken-by-design until plan 02 — choose: migrate any that are pure config tests; leave any that test cmd-layer behavior for plan 02).
  </behavior>
  <action>
    Edit `internal/app/config/config.go`:
    1. In the Config struct (around lines 50-52), DELETE the `ManagementAccountID string` field and any docstring comment specific to it.
    2. After the `Domain` field, ADD the two new fields with the exact documentation comments shown in the interfaces block above:
       - `OrganizationAccountID string`
       - `DNSParentAccountID    string`
    3. In the viper merge key list (around line 214), REMOVE `"accounts.management"` and ADD `"accounts.organization"` and `"accounts.dns_parent"`.
    4. In the cfg struct initialization (around line 254), REMOVE the `ManagementAccountID: v.GetString("accounts.management"),` line and ADD:
       ```go
       OrganizationAccountID: v.GetString("accounts.organization"),
       DNSParentAccountID:    v.GetString("accounts.dns_parent"),
       ```

    Edit `internal/app/config/config_test.go`:
    5. Migrate any existing test that constructs `config.Config{ManagementAccountID: ...}` to use `OrganizationAccountID` or `DNSParentAccountID` per its semantic intent (when in doubt for a pure-load round-trip, use `DNSParentAccountID` since the legacy semantic was DNS-zone-owner in the operator's live config).
    6. Migrate any existing test that loads YAML with `accounts.management:` to use `accounts.dns_parent:` (same semantic value).
    7. Add `TestLoadOrganizationAndDNSParentFields` per the behavior block above. Use the existing `writeKMConfig(t, dir, yaml)` helper + `os.Chdir(dir)` + `config.Load()` pattern that other tests in this file already use.
    8. Add `TestLoadBlankOrganizationIsValid` per the behavior block.

    Do NOT touch any other file in this task. Builds outside `internal/app/config/...` will fail — that's expected and is fixed by tasks 2 and 3 (test stubs) and by plan 02 (callers).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/config/... -count=1 -run 'TestLoadOrganizationAndDNSParentFields|TestLoadBlankOrganizationIsValid'</automated>
  </verify>
  <done>
    - `grep -n 'ManagementAccountID' internal/app/config/config.go` returns no matches.
    - `grep -n 'OrganizationAccountID\|DNSParentAccountID' internal/app/config/config.go` returns the field declarations + the two viper assignments.
    - `go test ./internal/app/config/... -count=1` passes including the two new tests.
    - VALIDATION.md task IDs 65-01-01 + 65-01-02 verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DoctorConfigProvider stubs in doctor_test.go (Wave 0 deliverable)</name>
  <files>internal/app/cmd/doctor_test.go</files>
  <action>
    Edit `internal/app/cmd/doctor_test.go` (it lives in `package cmd`, not `cmd_test` — confirm before editing):

    1. Locate the `testConfig` struct (the one used by older doctor tests) and the `testDoctorConfig` struct (newer/expanded one). Both currently expose `GetManagementAccountID() string`.

    2. For each struct:
       - REMOVE the `GetManagementAccountID() string` method.
       - REMOVE the `mgmtAcct` (or similarly-named) backing field if present.
       - ADD a backing field `orgAcct string`.
       - ADD a backing field `dnsParentAcct string`.
       - ADD method `func (c *testConfig) GetOrganizationAccountID() string { return c.orgAcct }` (and the equivalent on testDoctorConfig).
       - ADD method `func (c *testConfig) GetDNSParentAccountID() string { return c.dnsParentAcct }` (and the equivalent on testDoctorConfig).

    3. Migrate every existing usage of `mgmtAcct:` in struct literals within this file: rename to `dnsParentAcct:` (preserve the value — the legacy field was conflated and the operator's intent in existing tests is DNS parent).

    4. Do NOT add bodies for new test functions in this task — task 3 handles that. This task is purely the interface/stub extension so plan 03 can reference these accessors.

    NOTE: doctor.go has not yet been updated — its DoctorConfigProvider interface still names `GetManagementAccountID()`. That means after this edit, `go test ./internal/app/cmd/...` will fail to compile because `testConfig`/`testDoctorConfig` no longer satisfy the (still-old) interface. That is expected and acceptable as a Wave 0 transitional state — plan 03 fixes the interface in doctor.go in wave 3.

    However, to keep the build green between waves, ALSO add a temporary one-line shim to doctor_test.go: a `_ = ` assignment or interface compliance check that the structs satisfy a NEW local interface defined in this same file:
    ```go
    // _testConfigInterfaceCheck statically asserts the test stubs expose the renamed
    // accessors. The real DoctorConfigProvider in doctor.go is updated by plan 03.
    type _testConfigPostRename interface {
        GetOrganizationAccountID() string
        GetDNSParentAccountID() string
    }
    var _ _testConfigPostRename = (*testConfig)(nil)
    var _ _testConfigPostRename = (*testDoctorConfig)(nil)
    ```

    This shim ensures the stubs themselves compile in isolation. Once plan 03 lands, the shim is no longer load-bearing but it's harmless to leave or remove.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go vet ./internal/app/cmd/doctor_test.go 2>&1 | grep -v 'GetManagementAccountID\|cannot use' | head -5; grep -c 'GetOrganizationAccountID\|GetDNSParentAccountID' internal/app/cmd/doctor_test.go</automated>
  </verify>
  <done>
    - `grep -n 'GetManagementAccountID' internal/app/cmd/doctor_test.go` returns no matches.
    - `grep -n 'GetOrganizationAccountID\|GetDNSParentAccountID' internal/app/cmd/doctor_test.go` returns at least 4 matches (2 methods on each of 2 structs).
    - `grep -n 'mgmtAcct' internal/app/cmd/doctor_test.go` returns no matches.
    - VALIDATION.md task ID 65-01-04 verified (compile check happens after plan 03 wires the interface — this task delivers the stub side).
  </done>
</task>

<task type="auto">
  <name>Task 3: Stub Wave 0 test functions in cmd test files (compile-only skeletons)</name>
  <files>internal/app/cmd/bootstrap_test.go, internal/app/cmd/configure_test.go, internal/app/cmd/info_test.go, internal/app/cmd/init_test.go</files>
  <action>
    Goal: Add empty/skipped test-function skeletons for every Wave 0 test ID listed in 65-VALIDATION.md so plan 02 + plan 03 implementers fill in the bodies, not the function declarations. This avoids name-discovery friction during execution.

    Each stub uses this exact pattern:
    ```go
    func Test{Name}(t *testing.T) {
        t.Skip("Plan 0X — implement in Phase 65 plan 0X")
    }
    ```

    Replace `0X` with the plan number that owns implementation (per VALIDATION.md). The body MUST be exactly `t.Skip(...)` — no other code — so the build passes and `go test` reports SKIP.

    Edit `internal/app/cmd/bootstrap_test.go`:
    - Migrate any existing test that uses `cfg.ManagementAccountID = ...` to `cfg.OrganizationAccountID` (when the test is asserting SCP gating) or `cfg.DNSParentAccountID` (when the test is asserting DNS behavior). Use research §bootstrap_test.go pattern.
    - Add stubs:
      - `TestBootstrapDryRunShowsOrganizationAccount` (plan 02)
      - `TestBootstrapDryRunNoOrganizationAccount` (plan 02)
      - `TestBootstrapSCPApplyPath` (plan 02)
      - `TestBootstrapSCPSkipped_OrganizationBlank` (plan 02)
      - `TestShowPrereqsNoOrganizationAccount` (plan 02)

    Edit `internal/app/cmd/configure_test.go`:
    - Migrate existing tests that pass `--management-account` flag → `--dns-parent-account` (preserve value semantics). Migrate assertions on `accounts["management"]` → `accounts["dns_parent"]`.
    - Add stubs:
      - `TestConfigureWritesOrganizationAndDNSParent` (plan 02)
      - `TestConfigureInteractivePromptsUseNewNames` (plan 02)

    Edit `internal/app/cmd/info_test.go` (CREATE THE FILE if it doesn't exist; minimal skeleton with `package cmd` + `import "testing"`):
    - Add stub: `TestInfoShowsNewAccountFields` (plan 02)
    - If the file is created fresh, ensure `package cmd` matches the rest of the directory.

    Edit `internal/app/cmd/init_test.go` (CREATE THE FILE if it doesn't exist):
    - Add stub: `TestInitExportsNewAccountEnvVars` (plan 02)

    Do NOT add stubs for plan 03 tests in this task — those land in doctor_test.go which task 2 already touched. Add the plan-03 skip-stubs there now (task 2 already did the interface part; this task adds the function skeletons in the same file):
    Edit `internal/app/cmd/doctor_test.go` (additionally):
    - Add stubs:
      - `TestCheckOrganizationAccountBlank_BlankReturnsWarn` (plan 03)
      - `TestCheckOrganizationAccountBlank_SetReturnsOK` (plan 03)
      - `TestCheckLegacyManagementField_FieldPresent` (plan 03)
      - `TestCheckLegacyManagementField_FieldAbsent` (plan 03)
      - `TestCheckLegacyManagementField_NoConfigFile` (plan 03)
      - `TestCheckConfigDoesNotRequireManagement` (plan 03)

    All stubs share the `t.Skip(...)` body — no production code, no helpers, no fixtures. Names must match VALIDATION.md exactly.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run 'TestBootstrapDryRunShowsOrganizationAccount|TestBootstrapDryRunNoOrganizationAccount|TestBootstrapSCPApplyPath|TestBootstrapSCPSkipped_OrganizationBlank|TestShowPrereqsNoOrganizationAccount|TestConfigureWritesOrganizationAndDNSParent|TestConfigureInteractivePromptsUseNewNames|TestInfoShowsNewAccountFields|TestInitExportsNewAccountEnvVars|TestCheckOrganizationAccountBlank_BlankReturnsWarn|TestCheckOrganizationAccountBlank_SetReturnsOK|TestCheckLegacyManagementField_FieldPresent|TestCheckLegacyManagementField_FieldAbsent|TestCheckLegacyManagementField_NoConfigFile|TestCheckConfigDoesNotRequireManagement' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 15 stub test names appear in their respective `*_test.go` files (verifiable via `grep -rn 'func TestBootstrapDryRunShowsOrganizationAccount\|func TestConfigureWrites' internal/app/cmd/`).
    - Running the verify command shows all 15 tests as SKIP, not FAIL.
    - `grep -rn 'ManagementAccountID' internal/app/cmd/bootstrap_test.go` returns no matches.
    - `grep -rn 'management-account' internal/app/cmd/configure_test.go` returns no matches (flag-name migration complete).
    - VALIDATION.md task ID 65-01-04 verified — all Wave 0 stubs exist.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, the project as a whole will NOT compile (`go build ./...` fails with hundreds of errors in `internal/app/cmd/`). That is the intended Wave 1 exit state. Plans 02 and 03 fix those compile errors by migrating callers and updating doctor.go.

Plan-level success commands:
1. `go test ./internal/app/config/... -count=1` — must pass (the new fields work, viper bindings are correct).
2. `grep -rn 'ManagementAccountID' internal/app/config/` — must return zero matches.
3. `grep -rn 'GetManagementAccountID' internal/app/cmd/doctor_test.go` — must return zero matches.
4. All 15 Wave 0 stub test functions exist (verifiable per grep in each task's done block).

Plan validates the Phase 65 must-have: "single-account install runs km bootstrap and km init cleanly with accounts.organization blank" — at the type level, `OrganizationAccountID == ""` is now a valid loaded state with no error from `config.Load()`. The behavioral consequence is delivered by plan 02 (bootstrap gate) and plan 03 (doctor surfaces it).
</verification>

<success_criteria>
- `go test ./internal/app/config/... -count=1` green.
- `internal/app/config/config.go` has `OrganizationAccountID` and `DNSParentAccountID` fields, no `ManagementAccountID`.
- Viper merge keys include `accounts.organization` and `accounts.dns_parent`, exclude `accounts.management`.
- `internal/app/cmd/doctor_test.go` test stubs (testConfig, testDoctorConfig) expose `GetOrganizationAccountID` + `GetDNSParentAccountID`, no `GetManagementAccountID`.
- All 15 Wave 0 test-function stubs (per VALIDATION.md table) exist as `t.Skip(...)` placeholders in their assigned test files.
- The repo's whole-project build is intentionally broken — plan 02 picks up next.
</success_criteria>

<output>
After completion, create `.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-01-SUMMARY.md` describing:
- Exact lines changed in config.go (struct, viper merge list, cfg initialization).
- Test functions added vs migrated.
- The 15 Wave 0 stubs and which plan owns each implementation.
- Confirmation that `go test ./internal/app/config/...` passes and `go build ./...` is intentionally broken.
</output>
