---
phase: 65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename
plan: 04
type: execute
wave: 4
depends_on:
  - 65-02
  - 65-03
files_modified:
  - infra/live/site.hcl
  - infra/live/management/scp/terragrunt.hcl
  - OPERATOR-GUIDE.md
  - km-config.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "infra/live/site.hcl exposes accounts.organization and accounts.dns_parent (not accounts.management)"
    - "infra/live/management/scp/terragrunt.hcl reads local.accounts.organization for the SCP provider role and inputs"
    - "OPERATOR-GUIDE.md documents KM_ACCOUNTS_ORGANIZATION + KM_ACCOUNTS_DNS_PARENT in env-var tables and shell-export examples"
    - "Repo's committed km-config.yaml uses the post-rename schema"
    - "Zero references to ManagementAccountID remain in Go source"
    - "Zero references to KM_ACCOUNTS_MANAGEMENT remain in Go source, infra, or docs"
    - "Zero references to accounts.management remain (string form)"
    - "Zero references to local.accounts.management remain in HCL"
    - "make build produces a working km binary"
  artifacts:
    - path: "infra/live/site.hcl"
      provides: "accounts block exposing organization + dns_parent get_env reads"
      contains: "organization = get_env(\"KM_ACCOUNTS_ORGANIZATION\""
    - path: "infra/live/management/scp/terragrunt.hcl"
      provides: "Provider assume_role + trusted_role_arns + inputs all keyed off local.accounts.organization"
      contains: "local.accounts.organization"
    - path: "OPERATOR-GUIDE.md"
      provides: "Updated env var table + shell export examples + topology guidance"
      contains: "KM_ACCOUNTS_ORGANIZATION"
    - path: "km-config.yaml"
      provides: "Operator's live config in post-rename schema"
      contains: "dns_parent"
  key_links:
    - from: "site.hcl accounts block"
      to: "init.go + bootstrap.go env exports"
      via: "get_env(KM_ACCOUNTS_ORGANIZATION) + get_env(KM_ACCOUNTS_DNS_PARENT)"
      pattern: "KM_ACCOUNTS_ORGANIZATION|KM_ACCOUNTS_DNS_PARENT"
    - from: "scp/terragrunt.hcl provider assume_role"
      to: "site.hcl local.accounts.organization"
      via: "role_arn references local.accounts.organization"
      pattern: "local\\.accounts\\.organization"
---

<objective>
Migrate the Terragrunt HCL files, the operator-facing documentation, and the operator's own committed `km-config.yaml` to the post-rename schema. Run a full grep audit confirming zero residual references to the old names. After this plan, Phase 65 is complete and the codebase ships.

Purpose: The Go side is fully migrated by plan 03, but Terragrunt still reads `local.accounts.management` from `site.hcl` and operators still see "Management account" in the docs. This plan finishes the rename across the non-Go surface and locks in the four grep-audit zero-counts that gate phase completion.

Output: Two HCL files, one doc, one config file updated; four grep audits return zero; `make build` produces a working binary; operator-visible smoke (`km bootstrap --dry-run`) succeeds against the migrated config.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-CONTEXT.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-RESEARCH.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-VALIDATION.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-02-SUMMARY.md
@.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-03-SUMMARY.md
@infra/live/site.hcl
@infra/live/management/scp/terragrunt.hcl
@OPERATOR-GUIDE.md
@km-config.yaml

<interfaces>
Terragrunt HCL — locals + get_env pattern.

site.hcl current accounts block (around L12):
```hcl
accounts = {
  management  = get_env("KM_ACCOUNTS_MANAGEMENT", "")
  application = get_env("KM_ACCOUNTS_APPLICATION", "")
  terraform   = get_env("KM_ACCOUNTS_TERRAFORM", "")
}
```

Post-rename:
```hcl
accounts = {
  organization = get_env("KM_ACCOUNTS_ORGANIZATION", "")
  dns_parent   = get_env("KM_ACCOUNTS_DNS_PARENT", "")
  application  = get_env("KM_ACCOUNTS_APPLICATION", "")
  terraform    = get_env("KM_ACCOUNTS_TERRAFORM", "")
}
```

scp/terragrunt.hcl uses `local.accounts.management` in the provider `assume_role.role_arn`, the `trusted_role_arns`, and the `inputs` block. All three become `local.accounts.organization`.

Operator's repo-root km-config.yaml currently:
```yaml
accounts:
  management:  "481723467561"
  application: "481723467561"
  terraform:   "481723467561"
```

Post-rename (operator chose to leave organization blank — single-account topology):
```yaml
accounts:
  organization: ""
  dns_parent:   "481723467561"
  application:  "481723467561"
  terraform:    "481723467561"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Terragrunt HCL (site.hcl + scp/terragrunt.hcl)</name>
  <files>infra/live/site.hcl, infra/live/management/scp/terragrunt.hcl</files>
  <action>
    Edit `infra/live/site.hcl`:

    1. Locate the accounts block (around L12). Replace the `management = get_env("KM_ACCOUNTS_MANAGEMENT", "")` line with two lines:
       - `organization = get_env("KM_ACCOUNTS_ORGANIZATION", "")`
       - `dns_parent   = get_env("KM_ACCOUNTS_DNS_PARENT", "")`

       Keep `application` and `terraform` lines unchanged. Final block per the interfaces section above.

    2. If there are comments above the accounts block describing the management field's purpose, update them to describe the new split (organization = SCP target, dns_parent = Route53 parent zone owner).

    Edit `infra/live/management/scp/terragrunt.hcl`:

    3. Find every reference to `local.accounts.management` (RESEARCH.md identifies one site at L28 in the provider assume_role.role_arn — verify by reading the whole file; trusted_role_arns and inputs may also reference it).

    4. Replace each `local.accounts.management` → `local.accounts.organization`. The SCP module's input `var.application_account_id` still maps from `local.accounts.application` — leave that unchanged.

    5. After editing, run a within-file grep to confirm no residual `local.accounts.management` remains.

    Do NOT modify the SCP module itself (`infra/modules/scp/v1.0.0/`) — per RESEARCH.md it takes `var.application_account_id` and is unchanged.

    Do NOT touch any `.terragrunt-cache/` content — it's gitignored and regenerates on next `terragrunt apply`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q 'KM_ACCOUNTS_ORGANIZATION' infra/live/site.hcl && grep -q 'KM_ACCOUNTS_DNS_PARENT' infra/live/site.hcl && grep -q 'local.accounts.organization' infra/live/management/scp/terragrunt.hcl && ! grep -rn 'local\.accounts\.management' ./infra && ! grep -rn 'KM_ACCOUNTS_MANAGEMENT' ./infra</automated>
  </verify>
  <done>
    - site.hcl accounts block exposes both `organization` and `dns_parent` keys, no `management` key.
    - scp/terragrunt.hcl uses `local.accounts.organization` for SCP-target lookups, no `local.accounts.management` remains.
    - Zero `KM_ACCOUNTS_MANAGEMENT` references in `./infra`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate OPERATOR-GUIDE.md and the repo's live km-config.yaml</name>
  <files>OPERATOR-GUIDE.md, km-config.yaml</files>
  <action>
    Edit `OPERATOR-GUIDE.md`:

    1. Find the env-var table (RESEARCH.md cites L109). Replace the row for `KM_ACCOUNTS_MANAGEMENT` with TWO rows:
       - `KM_ACCOUNTS_ORGANIZATION` — "AWS Organizations management account ID. Used by `km bootstrap` SCP deployment. Optional — blank skips SCP."
       - `KM_ACCOUNTS_DNS_PARENT` — "AWS account ID owning the parent Route53 hosted zone for `cfg.Domain`. Used by `km init` DNS delegation. Optional if no domain is configured."

    2. Find the shell export example (RESEARCH.md cites L186). Replace the `export KM_ACCOUNTS_MANAGEMENT=...` line with:
       ```bash
       export KM_ACCOUNTS_ORGANIZATION=""              # blank for single-account; set to AWS Org management account ID otherwise
       export KM_ACCOUNTS_DNS_PARENT=481723467561      # AWS account ID owning your domain's parent Route53 zone
       ```

    3. Find the topology tables/sections (RESEARCH.md mentions Sections 1 and 2). Update any text describing the "Management account" role to explicitly distinguish:
       - "Organization account" (AWS Organizations management — SCP target — optional)
       - "DNS parent account" (Route53 parent zone owner — required if you use a custom domain)

    4. If the doc shows a sample km-config.yaml block, update it to match the new schema:
       ```yaml
       accounts:
         organization: ""              # blank for single-account topology
         dns_parent:   "481723467561"
         application:  "481723467561"
         terraform:    "481723467561"
       ```

    5. Add a brief migration note (1-3 sentences) under the accounts section calling out that `accounts.management` was split in Phase 65 and pointing to the `km doctor` legacy-field error message for guidance.

    Edit `km-config.yaml` (the operator's live config at repo root):

    6. Replace the `accounts:` block:
       - REMOVE the `management: "481723467561"` line.
       - ADD `organization: ""` (operator chose single-account topology — no AWS Organizations access; per CONTEXT.md migration-path table).
       - ADD `dns_parent: "481723467561"` (carry over the legacy value verbatim — same semantic, just renamed).
       - Keep `application: "481723467561"` and `terraform: "481723467561"` unchanged.

    Do NOT change AWS profile names anywhere in either file (`klanker-management`, `klanker-application`, `klanker-terraform` stay verbatim — out of scope per CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q 'KM_ACCOUNTS_ORGANIZATION' OPERATOR-GUIDE.md && grep -q 'KM_ACCOUNTS_DNS_PARENT' OPERATOR-GUIDE.md && ! grep -q 'KM_ACCOUNTS_MANAGEMENT' OPERATOR-GUIDE.md && grep -q 'dns_parent' km-config.yaml && ! grep -qE '^\s*management:' km-config.yaml</automated>
  </verify>
  <done>
    - OPERATOR-GUIDE.md env-var table lists both new vars; old `KM_ACCOUNTS_MANAGEMENT` entry is gone.
    - OPERATOR-GUIDE.md sample km-config block matches the post-rename schema.
    - km-config.yaml at repo root has `organization` and `dns_parent` keys; `management` key is gone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Grep-audit verification — VALIDATION.md task IDs 65-04-01 through 65-04-05</name>
  <files></files>
  <action>
    Run the four grep-audit verifications declared in VALIDATION.md as zero-count gates for phase completion. These commands MUST return zero matches (or `grep` exits non-zero, which the `!` negation turns into a passing assertion). Run them sequentially and capture output for the SUMMARY.

    1. Audit 65-04-01: zero `ManagementAccountID` Go references
       ```bash
       ! grep -rn ManagementAccountID ./internal ./pkg ./cmd
       ```
       Expected: command exits 0 (grep found nothing).

    2. Audit 65-04-02: zero `KM_ACCOUNTS_MANAGEMENT` references anywhere
       ```bash
       ! grep -rn KM_ACCOUNTS_MANAGEMENT ./internal ./pkg ./cmd ./infra ./docs ./OPERATOR-GUIDE.md ./CLAUDE.md
       ```
       Expected: exits 0.

    3. Audit 65-04-03: zero `accounts.management` (string form, source/docs/HCL)
       ```bash
       ! grep -rn 'accounts\.management\|accounts\\.management' ./internal ./pkg ./cmd ./infra ./docs ./OPERATOR-GUIDE.md
       ```
       Expected: exits 0. NOTE: `.planning/` is intentionally excluded — research/context/summaries discuss the rename and historically reference the old name.

    4. Audit 65-04-04: zero `local.accounts.management` HCL references
       ```bash
       ! grep -rn 'local\.accounts\.management' ./infra
       ```
       Expected: exits 0.

    5. Audit 65-04-05: km-config.yaml at repo root migrated
       ```bash
       grep -q 'dns_parent' km-config.yaml && ! grep -qE '^\s*management:' km-config.yaml
       ```
       Expected: exits 0.

    6. Smoke build: `make build` (per CLAUDE.md feedback_rebuild_km — always use `make build`, not bare `go build`):
       ```bash
       make build
       ```
       Expected: exits 0 and produces `bin/km` (or wherever the Makefile drops it).

    7. Smoke run: `./bin/km doctor --help` (or whichever invocation doesn't require AWS credentials) to confirm the binary loads.

    If ANY of the five greps return matches, fix them in-place before marking the task done. Common offenders to look for if a grep fails:
    - `.terragrunt-cache/` — should be gitignored, but if matches appear there, leave them (they regenerate from site.hcl).
    - Comment blocks in moved code that mention the old name — update or delete.
    - Auto-generated files (e.g., from go-bindata or embed) — regenerate via the generator, not manual edit.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && ! grep -rn ManagementAccountID ./internal ./pkg ./cmd && ! grep -rn KM_ACCOUNTS_MANAGEMENT ./internal ./pkg ./cmd ./infra && ! grep -rn 'accounts\.management' ./internal ./pkg ./cmd ./infra && ! grep -rn 'local\.accounts\.management' ./infra && grep -q 'dns_parent' km-config.yaml && ! grep -qE '^\s*management:' km-config.yaml && make build</automated>
  </verify>
  <done>
    - All five grep audits pass (zero matches outside `.planning/`).
    - `make build` succeeds.
    - The km binary at the Makefile-defined output path runs `--help` without panic.
    - VALIDATION.md task IDs 65-04-01, 65-04-02, 65-04-03, 65-04-04, 65-04-05 all green.
  </done>
</task>

</tasks>

<verification>
Phase-completion gates (all must hold simultaneously):

1. `go test ./internal/app/...` — full test suite green.
2. `make build` — produces a working km binary.
3. Five grep audits (per task 3) all return zero matches.
4. `km bootstrap --dry-run` against the migrated `km-config.yaml` exits 0 and prints "Organization account:" + "DNS parent account:" lines (manual smoke per VALIDATION.md manual section).
5. `km doctor` runs against the migrated config and emits no FAIL for the legacy-field check (since the operator's config is now post-rename).

If all five hold, Phase 65 is ready for `/gsd:verify-work`.
</verification>

<success_criteria>
- All 5 plan-04 VALIDATION.md task IDs (65-04-01..05) green.
- HCL exposes the new fields; SCP terragrunt assumes the org-management role from the new field name.
- OPERATOR-GUIDE.md is operator-ready with the new env vars, sample config, and migration note.
- Operator's repo-root km-config.yaml is migrated.
- Five grep audits pass (zero residual references outside `.planning/`).
- `make build` produces a runnable binary.
- AWS profile names (`klanker-management`, etc.) untouched per out-of-scope guard.
</success_criteria>

<output>
After completion, create `.planning/phases/65-four-account-config-model-separate-accounts-organization-scp-from-accounts-dns-parent-and-rename/65-04-SUMMARY.md` describing:
- Per-file diff summary (site.hcl, scp/terragrunt.hcl, OPERATOR-GUIDE.md, km-config.yaml).
- Each of the five grep-audit results (with the exact command and exit code).
- `make build` output excerpt confirming the binary built.
- Manual smoke results: `km bootstrap --dry-run` and `km doctor` output snippets.
- Phase 65 ready for `/gsd:verify-work`.
</output>
