---
phase: 26-live-operations-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/init_test.go
  - internal/app/cmd/status_test.go
  - internal/app/cmd/destroy.go
  - internal/app/cmd/doctor.go
  - pkg/profile/types.go
  - internal/app/cmd/extend.go
  - internal/app/cmd/extend_test.go
autonomous: true
requirements: [HARD-01, HARD-04, HARD-05]

must_haves:
  truths:
    - "go test ./... passes with no test failures from init ordering or status timestamp"
    - "No production code path contains hardcoded us-east-1 (except valid API constraints)"
    - "km extend cannot exceed MaxLifetime cap defined in profile"
  artifacts:
    - path: "internal/app/cmd/init_test.go"
      provides: "Fixed module ordering assertion"
      contains: "s3-replication.*ttl-handler.*ses"
    - path: "internal/app/cmd/status_test.go"
      provides: "Fixed timestamp assertion"
      contains: "2026-03-22"
    - path: "internal/app/cmd/destroy.go"
      provides: "Dynamic region in monitor hint"
      contains: "cfg.PrimaryRegion"
    - path: "internal/app/cmd/doctor.go"
      provides: "Dynamic region fallbacks"
      contains: "cfg.PrimaryRegion"
    - path: "pkg/profile/types.go"
      provides: "MaxLifetime field in LifecycleSpec"
      contains: "MaxLifetime"
    - path: "internal/app/cmd/extend.go"
      provides: "MaxLifetime enforcement"
      contains: "MaxLifetime"
    - path: "internal/app/cmd/extend_test.go"
      provides: "Tests for max lifetime cap"
      contains: "MaxLifetime"
  key_links:
    - from: "internal/app/cmd/extend.go"
      to: "pkg/profile/types.go"
      via: "LifecycleSpec.MaxLifetime field"
      pattern: "MaxLifetime"
---

<objective>
Fix pre-existing test failures, audit and fix hardcoded region references, and implement max lifetime cap for sandbox extensions.

Purpose: Establish a green test suite baseline and close the two highest-risk code gaps (region hardcoding and unbounded extend) before adding new features.
Output: Green test suite for existing tests, region-safe production code, MaxLifetime enforcement in extend.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-01-SUMMARY.md
@.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From pkg/profile/types.go (current LifecycleSpec — MaxLifetime does NOT exist yet):
```go
type LifecycleSpec struct {
    TTL            string `yaml:"ttl" json:"ttl"`
    IdleTimeout    string `yaml:"idleTimeout" json:"idleTimeout"`
    TeardownPolicy string `yaml:"teardownPolicy" json:"teardownPolicy"`
}
```

From internal/app/cmd/extend.go (current extend logic — no cap enforcement):
```go
// Line 41-48: if remote { publishRemoteCommand(...) }
// else: fetches sandbox metadata, computes newExpiry = rec.TTLExpiry.Add(dur), updates S3
```

From internal/app/cmd/status.go (ANSI constants):
```go
const (
    ansiGreen  = "\033[32m"
    ansiYellow = "\033[33m"
    ansiRed    = "\033[31m"
    ansiReset  = "\033[0m"
)
```

From internal/app/cmd/destroy.go:107-125 (publishRemoteCommand):
```go
func publishRemoteCommand(ctx context.Context, sandboxID, eventType string, extra ...string) error
// Line 119: fmt.Printf("Monitor: aws logs tail ... --region us-east-1\n")  // BUG: hardcoded
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pre-existing test failures and multi-region hardcoding</name>
  <files>internal/app/cmd/init_test.go, internal/app/cmd/status_test.go, internal/app/cmd/destroy.go, internal/app/cmd/doctor.go</files>
  <action>
    1. Fix TestRunInitWithRunnerAllModules in init_test.go:
       - Update moduleNames slice (around line 74) to match actual code order: ["network", "dynamodb-budget", "dynamodb-identities", "s3-replication", "ttl-handler", "ses"]. The code is correct (ses last to avoid bucket policy races); the test is stale.

    2. Fix TestStatusCmd_Found in status_test.go:
       - Change the assertion at line ~142 from checking for RFC3339 "2026-03-22T12:00:00Z" to checking for the date portion "2026-03-22" only. The code intentionally formats as local timezone human-readable format. Do NOT change status.go — the code is correct.

    3. Fix hardcoded us-east-1 in destroy.go:
       - Line 119: Replace the literal "us-east-1" in the monitor hint printf with the region from config. The publishRemoteCommand function needs access to cfg.PrimaryRegion. Thread the config through or use the region from the sandbox metadata.

    4. Fix hardcoded us-east-1 in doctor.go:
       - Lines 964, 1032, 1058: Replace the hardcoded "us-east-1" fallback values with cfg.PrimaryRegion. These are default fallbacks when region is not otherwise available — they should use the configured primary region.

    Do NOT change: bootstrap.go S3 LocationConstraint exception (valid — us-east-1 is special for S3 API), create.go Pricing API endpoint (valid — AWS Pricing API only exists in us-east-1), test files with us-east-1 in test fixtures (test data, not production behavior).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run "TestRunInitWithRunnerAllModules|TestStatusCmd_Found" -v 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>Both TestRunInitWithRunnerAllModules and TestStatusCmd_Found pass. No hardcoded us-east-1 remains in destroy.go monitor hint or doctor.go fallbacks (verified by grep).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MaxLifetime field and enforce in km extend</name>
  <files>pkg/profile/types.go, internal/app/cmd/extend.go, internal/app/cmd/extend_test.go</files>
  <behavior>
    - Test 1: km extend with MaxLifetime set, new expiry within cap -> succeeds
    - Test 2: km extend with MaxLifetime set, new expiry exceeds cap -> returns error with clear message including the cap duration and sandbox creation time
    - Test 3: km extend with no MaxLifetime set (empty string) -> succeeds without cap enforcement (backward compatible)
    - Test 4: km extend on expired sandbox (TTL already passed) -> extends from now, still respects MaxLifetime cap
  </behavior>
  <action>
    1. Add `MaxLifetime string` field to LifecycleSpec in pkg/profile/types.go with yaml tag "maxLifetime" and json tag "maxLifetime". This field specifies the absolute maximum duration from sandbox creation time (e.g., "24h", "72h").

    2. Update extend.go runExtend logic (around line 41-48 in the non-remote path):
       - After computing newExpiry, load the sandbox profile (already available as the profile is compiled and stored in S3 metadata or can be derived from the metadata.json).
       - If LifecycleSpec.MaxLifetime is non-empty, parse it as a time.Duration.
       - Compute maxExpiry = metadata.CreatedAt.Add(maxLifetimeDuration).
       - If newExpiry.After(maxExpiry), return error: "extend would exceed max lifetime (%s); sandbox was created at %s, max expiry is %s".
       - If no MaxLifetime is set, skip enforcement (backward compatible).

    3. The extend command needs access to CreatedAt from sandbox metadata and the profile's MaxLifetime. Check how metadata is already fetched in the extend path — the SandboxRecord from S3 should have CreatedAt. For the profile MaxLifetime, the metadata.json should contain the profile name, and the compiled profile can be loaded.

    4. If loading the full compiled profile is complex, consider: store MaxLifetime directly in metadata.json during km create (simpler, avoids round-trip to load profile). Or add it as a field on SandboxMetadata. Choose whichever approach is simpler given the existing code.

    5. Create extend_test.go following the established pattern: use interface-based mocks (FakeFetcher pattern from shell_test.go/list_test.go). Mock the SandboxRecord with appropriate CreatedAt and TTLExpiry values. Test all four behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go test ./internal/app/cmd/... -count=1 -run "TestExtend" -v 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>MaxLifetime field exists in LifecycleSpec. km extend enforces the cap when set. All four test cases pass. Empty MaxLifetime is backward compatible (no cap).</done>
</task>

</tasks>

<verification>
All pre-existing test failures fixed:
```bash
go test ./internal/app/cmd/... -count=1 -run "TestRunInitWithRunnerAllModules|TestStatusCmd_Found|TestExtend" -v
```

No hardcoded us-east-1 in production paths:
```bash
grep -n "us-east-1" internal/app/cmd/destroy.go internal/app/cmd/doctor.go | grep -v test | grep -v "Pricing API\|LocationConstraint"
```
Should return empty.

Full suite still passes:
```bash
go test ./... -count=1
```
</verification>

<success_criteria>
- TestRunInitWithRunnerAllModules passes
- TestStatusCmd_Found passes
- No hardcoded us-east-1 in destroy.go monitor hint or doctor.go fallbacks
- MaxLifetime field in LifecycleSpec
- km extend respects MaxLifetime cap with clear error message
- All new extend tests pass
- Full test suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-02-SUMMARY.md`
</output>
