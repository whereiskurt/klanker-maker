---
phase: 26-live-operations-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/root.go
  - internal/app/cmd/list.go
  - internal/app/cmd/shell.go
  - internal/app/cmd/extend.go
  - internal/app/cmd/stop.go
  - internal/app/cmd/logs.go
  - internal/app/cmd/help/extend.txt
  - internal/app/cmd/help/stop.txt
  - internal/app/cmd/help/completion.txt
autonomous: true
requirements: [HARD-02]

must_haves:
  truths:
    - "km ls works as alias for km list"
    - "km sh works as alias for km shell"
    - "km completion bash outputs valid bash completion script"
    - "km completion zsh outputs valid zsh completion script"
    - "km extend --help shows usage examples"
    - "km stop --help shows usage examples"
    - "Newer commands (extend, stop, logs) use consistent color and formatting"
  artifacts:
    - path: "internal/app/cmd/root.go"
      provides: "Shell completion subcommand"
      contains: "completion"
    - path: "internal/app/cmd/list.go"
      provides: "ls alias"
      contains: "ls"
    - path: "internal/app/cmd/shell.go"
      provides: "sh alias"
      contains: "sh"
    - path: "internal/app/cmd/help/extend.txt"
      provides: "Extend command help text with examples"
    - path: "internal/app/cmd/help/stop.txt"
      provides: "Stop command help text with examples"
  key_links:
    - from: "internal/app/cmd/root.go"
      to: "cobra.Command.GenBashCompletion"
      via: "completion subcommand RunE"
      pattern: "GenBashCompletion|GenZshCompletion"
    - from: "internal/app/cmd/list.go"
      to: "cobra.Command.Aliases"
      via: "Aliases field"
      pattern: "Aliases.*ls"
---

<objective>
Add CLI UX refinements: shell completion, command aliases, help text for new commands, and consistent color output styling.

Purpose: Make the CLI more discoverable and polished for operators — tab completion, shorter aliases, proper help text, and consistent visual styling.
Output: Working completion command, ls/sh aliases, help text files, colored output for newer commands.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-01-SUMMARY.md
@.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs -->

From internal/app/cmd/root.go (existing AddCommand pattern):
```go
root.AddCommand(NewListCmd(cfg))
root.AddCommand(NewShellCmd(cfg))
root.AddCommand(NewExtendCmd(cfg))
root.AddCommand(NewStopCmd(cfg))
root.AddCommand(NewRollCmd(cfg))
```

From internal/app/cmd/configure.go (existing alias pattern):
```go
cmd := &cobra.Command{
    Use:     "configure",
    Aliases: []string{"conf"},
    ...
}
```

From internal/app/cmd/status.go (ANSI color constants):
```go
const (
    ansiGreen  = "\033[32m"
    ansiYellow = "\033[33m"
    ansiRed    = "\033[31m"
    ansiReset  = "\033[0m"
)
```

From internal/app/cmd/help.go (help text loading):
```go
func helpText(name string) string // loads from help/{name}.txt via embed
```

Existing help files: bootstrap.txt, budget_add.txt, budget.txt, configure.txt, create.txt, destroy.txt, doctor.txt, init.txt, list.txt, logs.txt, root.txt, shell.txt, status.txt, uninit.txt, validate.txt
Missing help files: extend.txt, stop.txt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add command aliases and shell completion</name>
  <files>internal/app/cmd/root.go, internal/app/cmd/list.go, internal/app/cmd/shell.go, internal/app/cmd/extend.go, internal/app/cmd/stop.go, internal/app/cmd/logs.go</files>
  <action>
    1. Add Aliases to existing commands:
       - list.go: Add `Aliases: []string{"ls"}` to the cobra.Command struct in NewListCmd
       - shell.go: Add `Aliases: []string{"sh"}` to the cobra.Command struct in NewShellCmd
       - Also consider adding: `km ext` for extend, `km log` for logs (Claude's discretion on additional aliases based on command frequency — keep it minimal, only add aliases that save significant typing)

    2. Add shell completion subcommand to root.go:
       - Add a new cobra.Command for "completion" with Use: "completion [bash|zsh]"
       - Short: "Generate shell completion script"
       - Long: Multi-line string showing usage for bash (source <(km completion bash)) and zsh (km completion zsh > "${fpath[1]}/_km")
       - Args: cobra.ExactArgs(1)
       - SilenceUsage: true
       - RunE: switch on args[0] — "bash" calls root.GenBashCompletion(os.Stdout), "zsh" calls root.GenZshCompletion(os.Stdout), default returns error "unsupported shell %q: use bash or zsh"
       - Follow the exact pattern from RESEARCH.md code example

    3. Verify helpText() behavior when file is missing — check help.go to understand what happens when helpText("extend") or helpText("stop") is called but the .txt files don't exist. If it panics, the help text files MUST be created in this task. If it returns empty string, the help files can be created in Task 2.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./cmd/km/ && ./km completion bash > /dev/null 2>&1 && echo "completion: OK" && ./km ls --help > /dev/null 2>&1 && echo "ls alias: OK" && ./km sh --help > /dev/null 2>&1 && echo "sh alias: OK"</automated>
  </verify>
  <done>km ls works as alias for km list. km sh works as alias for km shell. km completion bash/zsh outputs valid completion scripts. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add help text files and consistent color styling</name>
  <files>internal/app/cmd/help/extend.txt, internal/app/cmd/help/stop.txt, internal/app/cmd/help/completion.txt, internal/app/cmd/extend.go, internal/app/cmd/stop.go, internal/app/cmd/logs.go</files>
  <action>
    1. Create help/extend.txt with usage examples:
       ```
       Extend a running sandbox's TTL.

       Usage:
         km extend <sandbox-id | #number> <duration>

       Examples:
         km extend #1 2h          Extend sandbox #1 by 2 hours
         km extend sb-abc123 30m  Extend by 30 minutes
         km extend #1 2h --remote Dispatch extend via Lambda

       Flags:
         --remote  Dispatch to Lambda via EventBridge (no local AWS creds needed)
       ```

    2. Create help/stop.txt with usage examples:
       ```
       Stop a running sandbox's EC2 instance without destroying infrastructure.
       The sandbox can be resumed later or will be destroyed when TTL expires.

       Usage:
         km stop <sandbox-id | #number>

       Examples:
         km stop #1               Stop sandbox #1
         km stop sb-abc123        Stop by full ID
         km stop #1 --remote      Dispatch stop via Lambda

       Flags:
         --remote  Dispatch to Lambda via EventBridge (no local AWS creds needed)
       ```

    3. Optionally create help/completion.txt if the completion command uses helpText().

    4. Add consistent color styling to extend.go, stop.go, and logs.go output:
       - Import or reference the ANSI constants from status.go. If they are unexported (lowercase), either export them to a shared location or duplicate them in a colors.go file that all commands can use.
       - Color sandbox IDs green (ansiGreen) when printed
       - Color profile names yellow (ansiYellow) when printed
       - Color section headers (── separators) with consistent styling
       - Color success messages green, warnings yellow, errors red
       - Match the established pattern from status.go and the sprint's output styling (section headers with "── %s ──" separators, "done" indicators)
       - Do NOT add color to piped/redirected output — check os.Stdout.Fd() with term.IsTerminal() or simply rely on the user having a terminal (existing code does not check, so follow existing pattern)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./cmd/km/ && ./km extend --help 2>&1 | grep -q "Examples" && echo "extend help: OK" && ./km stop --help 2>&1 | grep -q "Examples" && echo "stop help: OK"</automated>
  </verify>
  <done>help/extend.txt and help/stop.txt exist with usage examples. km extend --help and km stop --help show examples. Newer commands use consistent color styling matching established patterns (green sandbox IDs, yellow profile names, section headers with separators).</done>
</task>

</tasks>

<verification>
Aliases work:
```bash
./km ls --help 2>&1 | head -1
./km sh --help 2>&1 | head -1
```

Completion works:
```bash
./km completion bash | head -5
./km completion zsh | head -5
```

Help text works:
```bash
./km extend --help | grep "Examples"
./km stop --help | grep "Examples"
```

Full suite still passes:
```bash
go test ./... -count=1
```
</verification>

<success_criteria>
- km ls is an alias for km list
- km sh is an alias for km shell
- km completion bash outputs valid bash completion script
- km completion zsh outputs valid zsh completion script
- help/extend.txt exists with usage examples
- help/stop.txt exists with usage examples
- extend, stop, logs output uses consistent color styling
- Full test suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/26-live-operations-hardening-bootstrap-init-create-destroy-ttl-auto-destroy-idle-detection-sidecar-fixes-proxy-enforcement-cli-polish/26-03-SUMMARY.md`
</output>
