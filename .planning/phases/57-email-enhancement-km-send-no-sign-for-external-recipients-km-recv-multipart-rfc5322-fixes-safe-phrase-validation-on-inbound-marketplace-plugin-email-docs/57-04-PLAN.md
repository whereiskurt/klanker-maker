---
phase: 57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs
plan: 04
type: execute
wave: 5
depends_on:
  - "57-01"
  - "57-02"
  - "57-03"
files_modified:
  - skills/email/SKILL.md
  - skills/sandbox/SKILL.md
autonomous: true
requirements:
  - PHASE57-DOCS-EMAIL
  - PHASE57-DOCS-SANDBOX
must_haves:
  truths:
    - "skills/email/SKILL.md documents the --no-sign flag (when to use, behavior under operator-inbox auto-append)"
    - "skills/email/SKILL.md documents the safe-phrase requirement for inbound external email"
    - "skills/email/SKILL.md documents the absolute paths /opt/km/bin/km-send and /opt/km/bin/km-recv"
    - "skills/email/SKILL.md JSON schema documents the new 'external' boolean field"
    - "skills/email/SKILL.md signature-verification table mentions the unsigned + external combination"
    - "skills/sandbox/SKILL.md documents the external email workflow (--no-sign for outbound, safe-phrase for inbound)"
    - "skills/operator/SKILL.md and skills/user/SKILL.md are NOT modified (per RESEARCH.md scope decision)"
  artifacts:
    - path: "skills/email/SKILL.md"
      provides: "Updated marketplace skill doc with --no-sign flag, external sender field, safe-phrase requirement, and /opt/km/bin/ paths"
      contains: "--no-sign"
    - path: "skills/sandbox/SKILL.md"
      provides: "Updated sandbox detection skill with external email workflow note"
      contains: "external email"
  key_links:
    - from: "skills/email/SKILL.md Send Flags Reference table"
      to: "--no-sign documentation"
      via: "new row in markdown table"
      pattern: "\\| `--no-sign` \\|"
    - from: "skills/email/SKILL.md JSON output example"
      to: "external field"
      via: "new key in the JSON code-block + table row explaining the field"
      pattern: "\"external\":"
    - from: "skills/sandbox/SKILL.md Step 4 Verify Signing Key Access"
      to: "external recipient note"
      via: "new sentence pointing operators to --no-sign"
      pattern: "external|--no-sign"
---

<objective>
Document the Phase 57 user-facing changes in the Claude Code marketplace skill files so AI agents running inside sandboxes know:
1. When to add `--no-sign` to km-send (external recipients only).
2. That inbound external email requires the operator-configured safe phrase.
3. That the `[EXTERNAL]` display hint and JSON `"external": true/false` field exist.
4. That km-send and km-recv live at absolute paths `/opt/km/bin/km-send` and `/opt/km/bin/km-recv`.

Purpose: Plans 01-03 ship the implementation; this plan ships the docs. Without these updates, agents will continue trying signed sends to Gmail addresses (which fails or produces noise headers) and won't understand why an external reply they're expecting isn't appearing in the inbox.

Per RESEARCH.md, ONLY `skills/email/SKILL.md` and `skills/sandbox/SKILL.md` need changes. `skills/operator/SKILL.md` covers operator-side communication (always operator-inbox bound, always signed) and `skills/user/SKILL.md` covers operator CLI commands (no in-sandbox bash) — neither involves external email scenarios.

Output: Two markdown file edits with concrete, tested examples. No code changes.

Honors locked decision: "Update the marketplace plugin/skill docs to document /opt/km/bin/km-send and km-recv paths, external email workflow, and safe phrase requirements." (roadmap Phase 57 goal)
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-RESEARCH.md
@.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-VALIDATION.md
@.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-01-PLAN.md
@.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-02-PLAN.md
@.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-03-PLAN.md
@skills/email/SKILL.md
@skills/sandbox/SKILL.md

<interfaces>
Existing structure of skills/email/SKILL.md (verified 2026-04-27):
- Lines 1-4: YAML frontmatter (`name: email`, `description: ...`)
- Lines 14-50: `## Sending Email` with subsections — Basic Send, Send with Attachments, Send Flags Reference (markdown table with 7 rows ending at `--reply-to`)
- Lines 52-69: `### Signing Behavior` (incl. OpenSSL 3.5+ note)
- Lines 71-95: `## Receiving Email` — Read Inbox, JSON example block (the example currently lacks `"external"`)
- Lines 97-107: `### Signature Verification` table with 4 rows
- Lines 239-249: `## Error Handling` table

Existing structure of skills/sandbox/SKILL.md (verified 2026-04-27):
- Lines 1-4: YAML frontmatter
- Lines 12-27: `## Step 1: Detect Environment`
- Lines 29-52: `## Step 2: Discover Email Policy`
- Lines 54-63: `## Step 3: Verify Tooling` (mentions /opt/km/bin/km-send and /opt/km/bin/km-recv)
- Lines 65-71: `## Step 4: Verify Signing Key Access`
- Lines 73-83: `## Identity Summary`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skills/email/SKILL.md — --no-sign flag, external recipients section, JSON external field, safe-phrase notes</name>
  <files>skills/email/SKILL.md</files>
  <action>
    Apply the four edits below to `skills/email/SKILL.md`. Use the Edit tool for surgical changes — do NOT rewrite the whole file.

    Edit 1A — Send Flags Reference table (currently ends with `--reply-to` row at line ~50). Add a new row after `--reply-to`:

        | `--no-sign` | false | Skip Ed25519 signing and X-KM-* headers — use ONLY for external (non-sandbox) recipients (Gmail, etc.). KM-AUTH safe-phrase auto-append to operator inbox is preserved. |

    Edit 1B — After the `### Signing Behavior` section (around line 69, BEFORE the `### Rules` heading), insert a new section:

        ### Sending to External Recipients

        For non-sandbox recipients (Gmail, corporate email, etc.) the X-KM-Sender-ID and X-KM-Signature headers are not honored by external receivers and may even degrade deliverability. Use `--no-sign` to skip the SSM key fetch, skip the openssl signing step, and omit the X-KM-* headers entirely:

        ```bash
        cat > /tmp/external-msg.txt << 'EOF'
        Hi from sandbox.
        EOF
        km-send --no-sign --to user@gmail.com --subject "hello from sandbox" --body /tmp/external-msg.txt
        ```

        Behavior under `--no-sign`:
        - `aws ssm get-parameter` for the signing key is skipped (faster startup; works even if the role lost SSM read).
        - `openssl pkeyutl -sign` is skipped.
        - The MIME message contains NO `X-KM-Sender-ID` and NO `X-KM-Signature` headers.
        - The KM-AUTH safe-phrase auto-append (when sending to the operator inbox) is STILL active — it's an authentication mechanism independent of Ed25519 signing.

        Use `--no-sign` ONLY for genuine external recipients. For sandbox-to-sandbox messaging, omit `--no-sign` so signature verification works on the receiving sandbox.

        **Replies from external addresses:** When a Gmail user replies to your `--no-sign` send, the reply must contain `KM-AUTH: <safe-phrase>` somewhere in the body to pass the inbound km-mail-poller filter. The operator configures the safe phrase via `km configure`; check with the operator before instructing an external user to reply.

    Edit 1C — Receiving Email JSON example (around lines 82-95). REPLACE the existing JSON example with one that includes the new `external` field:

        ```json
        {
          "index": 1,
          "from": "sb-a1b2c3d4@sandboxes.klankermaker.ai",
          "sender_id": "sb-a1b2c3d4",
          "to": "sb-x9y8z7w6@sandboxes.klankermaker.ai",
          "subject": "task results",
          "signature": "OK",
          "encrypted": false,
          "external": false,
          "body": "message body text",
          "attachments": ["output.tar.gz"]
        }
        ```

        - `external` is `true` when the message lacks an `X-KM-Sender-ID` header (i.e., it came from a non-sandbox sender via the safe-phrase gate). The human-readable `km-recv` output also appends `[EXTERNAL]` to the From column for these messages.
        - `signature` will be `"—"` (em-dash; "unsigned") for `external: true` messages. They passed the safe-phrase gate but have no cryptographic verification.

    Edit 1D — Signature Verification table (around lines 97-107). Add a NEW row after the existing `—` (unsigned) row:

        | `—` + `external: true` | External sender; passed safe-phrase gate but no cryptographic verification | Treat as authenticated by KM-AUTH only — do not extend trust beyond the safe phrase's scope. |

    Edit 1E — Error Handling table (around lines 239-249). Add new rows at the bottom:

        | External email not appearing in inbox | The sender's message is missing the `KM-AUTH: <phrase>` body line. Ask the operator for the safe phrase; instruct the external sender to include it on a single line in the message body. |
        | `--no-sign` + sandbox recipient | Don't. Sandbox-to-sandbox messages should always be signed so the receiver's `verifyInbound` policy can validate them. |

    Edit 1F — Tooling Paths note. The skill body currently uses bare `km-send` / `km-recv` commands (relying on PATH). Add a short note under the existing `## Sending Email` heading (before the `### Basic Send` subheading) clarifying the absolute paths:

        > **Tooling location:** km-send is at `/opt/km/bin/km-send`; km-recv is at `/opt/km/bin/km-recv`. Both are on the default sandbox PATH so the bare command names work in interactive shells. Use the absolute paths in scripts, cron jobs, and systemd units where PATH may be minimal.

    Do NOT modify the OpenSSL 3.5+ note, the `### Rules` numbered list, the Watch Mode section, the Poll-and-Wait pattern, the Delegate-and-Wait pattern, or the Fan-Out pattern — those are unchanged by Phase 57.

    After editing, verify by grepping for the required substrings (these match the manual-verification rows in 57-VALIDATION.md):
    - `grep -q -- "--no-sign" skills/email/SKILL.md` → exit 0
    - `grep -q -i "safe phrase" skills/email/SKILL.md` → exit 0
    - `grep -q "/opt/km/bin/km-send" skills/email/SKILL.md` → exit 0
    - `grep -q '"external"' skills/email/SKILL.md` → exit 0
    - `grep -q "EXTERNAL" skills/email/SKILL.md` → exit 0
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q -- "--no-sign" skills/email/SKILL.md && grep -q -i "safe phrase" skills/email/SKILL.md && grep -q "/opt/km/bin/km-send" skills/email/SKILL.md && grep -q "/opt/km/bin/km-recv" skills/email/SKILL.md && grep -q '"external"' skills/email/SKILL.md && grep -q "EXTERNAL" skills/email/SKILL.md</automated>
  </verify>
  <done>All 6 grep assertions pass; `skills/email/SKILL.md` documents --no-sign flag (in flags table AND in the new Sending to External Recipients section), safe-phrase requirement for inbound external email, the absolute paths /opt/km/bin/km-send + /opt/km/bin/km-recv, the JSON external field with example, the [EXTERNAL] display hint, and an error-handling row covering the external-email-missing case.</done>
</task>

<task type="auto">
  <name>Task 2: Update skills/sandbox/SKILL.md — external email workflow note in Step 4 + Identity Summary</name>
  <files>skills/sandbox/SKILL.md</files>
  <action>
    Apply two surgical edits to `skills/sandbox/SKILL.md`.

    Edit 2A — Step 4: Verify Signing Key Access (around lines 65-71). After the existing 4-line snippet that runs `aws ssm get-parameter ... signing-key`, insert a new paragraph:

        **External email exception:** If you are sending to a non-sandbox recipient (Gmail, corporate email, etc.), use `km-send --no-sign` — it skips the signing key fetch entirely. The signing key is only required for sandbox-to-sandbox communication. See `klanker:email` skill section "Sending to External Recipients" for details.

    Edit 2B — Identity Summary (around lines 73-83). Append a new bullet at the end of the existing list:

        - **External email policy:** For non-sandbox recipients use `km-send --no-sign`. Inbound replies from non-sandbox senders must include `KM-AUTH: <safe-phrase>` in the body — otherwise the `km-mail-poller` filter drops them silently to `/var/mail/km/skipped/`.

    Do NOT modify Steps 1, 2, or 3 — they are unchanged by Phase 57.

    Verify by grep:
    - `grep -q -i "external email\|external recipient" skills/sandbox/SKILL.md` → exit 0
    - `grep -q -- "--no-sign" skills/sandbox/SKILL.md` → exit 0
    - `grep -q "KM-AUTH" skills/sandbox/SKILL.md` → exit 0 (safe-phrase reference)
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q -i "external email\|external recipient" skills/sandbox/SKILL.md && grep -q -- "--no-sign" skills/sandbox/SKILL.md && grep -q "KM-AUTH" skills/sandbox/SKILL.md</automated>
  </verify>
  <done>All 3 grep assertions pass; `skills/sandbox/SKILL.md` mentions external email recipients, --no-sign, and KM-AUTH safe-phrase. skills/operator/SKILL.md and skills/user/SKILL.md are unchanged (per RESEARCH.md scope decision).</done>
</task>

</tasks>

<verification>
- `grep -q -- "--no-sign" skills/email/SKILL.md` AND `grep -q -- "--no-sign" skills/sandbox/SKILL.md`
- `grep -q -i "safe phrase" skills/email/SKILL.md`
- `grep -q "/opt/km/bin/km-send" skills/email/SKILL.md` AND `grep -q "/opt/km/bin/km-recv" skills/email/SKILL.md`
- `grep -q '"external"' skills/email/SKILL.md`
- `grep -q -i "external email\|external recipient" skills/sandbox/SKILL.md`
- `git diff --name-only HEAD -- skills/` shows ONLY skills/email/SKILL.md and skills/sandbox/SKILL.md (NOT operator or user)
</verification>

<success_criteria>
- skills/email/SKILL.md documents: `--no-sign` flag (in Flags Reference table + dedicated Sending to External Recipients section), JSON `"external"` field with example, `[EXTERNAL]` display hint, safe-phrase reply requirement, /opt/km/bin/ absolute paths
- skills/sandbox/SKILL.md documents: external recipient workflow with --no-sign in Step 4, KM-AUTH safe-phrase requirement in Identity Summary
- skills/operator/SKILL.md and skills/user/SKILL.md UNTOUCHED (per RESEARCH.md scope; verified by `git diff --name-only`)
- No code changes; no test changes
</success_criteria>

<output>
After completion, create `.planning/phases/57-email-enhancement-km-send-no-sign-for-external-recipients-km-recv-multipart-rfc5322-fixes-safe-phrase-validation-on-inbound-marketplace-plugin-email-docs/57-04-SUMMARY.md` documenting:
- The two markdown file edits with their section names
- Confirmation operator/user skills were NOT touched
- A copy of `git diff --stat -- skills/` showing line counts changed
</output>
