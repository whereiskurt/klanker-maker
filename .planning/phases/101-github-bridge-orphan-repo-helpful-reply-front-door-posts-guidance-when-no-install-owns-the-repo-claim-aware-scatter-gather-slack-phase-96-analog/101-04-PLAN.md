---
phase: 101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - docs/github-bridge.md
  - OPERATOR-GUIDE.md
  - CLAUDE.md
  - .planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-UAT.md
autonomous: true
requirements: [GH-ORPHAN-E2E]
must_haves:
  truths:
    - "docs/github-bridge.md gains a Phase 101 section documenting github.default_router, the orphan guidance comment, the per-(repo,number) cooldown, the rollout-safe mixed-fleet tally, and the make build-lambdas + km init --dry-run=false deploy (NOT --sidecars)"
    - "OPERATOR-GUIDE.md gains a Phase 101 orphan-repo router entry mirroring the Phase 100 federated-relay entry"
    - "CLAUDE.md gains a Phase 101 bridge section in the project-history block"
    - "101-UAT.md is a runnable two-install / one-App / unowned-repo runbook with explicit assertions: unowned-repo @-mention ⇒ exactly ONE guidance comment from the front door; owned-repo @-mention ⇒ no guidance comment; second mention within the cooldown window ⇒ no repeat"
  artifacts:
    - path: "docs/github-bridge.md"
      provides: "Phase 101 operator runbook section"
      contains: "Phase 101"
    - path: "OPERATOR-GUIDE.md"
      provides: "Phase 101 orphan-repo router operator entry"
      contains: "default_router"
    - path: "CLAUDE.md"
      provides: "Phase 101 project-history bridge section"
      contains: "Phase 101"
    - path: ".planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-UAT.md"
      provides: "GH-ORPHAN-E2E two-install manual UAT runbook"
      contains: "default_router"
  key_links:
    - from: "docs/github-bridge.md Phase 101 section"
      to: "github.default_router config + deploy sequence"
      via: "operator runbook (toggle, cooldown, mixed-fleet, make build-lambdas + km init --dry-run=false)"
      pattern: "default_router"
    - from: "101-UAT.md"
      to: "GH-ORPHAN-E2E manual verification"
      via: "two-install / one-App / unowned-repo runbook with pass/fail assertions"
      pattern: "guidance comment"
---

<objective>
Document Phase 101 for operators and provide the manual GH-ORPHAN-E2E verification runbook. This is the
ONLY deliverable for GH-ORPHAN-E2E — it cannot be exercised by unit mocks (two live installs sharing one
GitHub App webhook + real GitHub delivery + a real PR/issue on an unowned repo).

Four edits, all docs:
1. `docs/github-bridge.md` — a new "Phase 101 — Orphan-repo helpful reply" section (mirror the Phase 100
   federated-relay section style).
2. `OPERATOR-GUIDE.md` — a Phase 101 orphan-repo router entry next to the Phase 100 federated-relay entry.
3. `CLAUDE.md` — a Phase 101 bridge section in the project-history block.
4. `101-UAT.md` — the two-install / one-App / unowned-repo manual runbook with explicit assertions.

Depends on Plans 01-03 (the feature must exist + the deploy surface must be real before documenting it).
Wave 3 (docs after code). Output: 3 doc edits + the UAT runbook.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-RESEARCH.md
@docs/github-bridge.md
@OPERATOR-GUIDE.md
@docs/superpowers/specs/2026-06-05-slack-default-router-design.md

<interfaces>
<!-- Doc anchors verified 2026-06-08. -->
docs/github-bridge.md:
  - TOC at line 17; Phase 100 TOC entry at line 28; Phase 100 federated-relay content around line 203
    (Pattern B) + a dedicated "§ Phase 100 — Federated relay" section. Add the Phase 101 section + a TOC entry.
  - Deploy Sequence at line 342 — the Phase 101 deploy is the SAME shape (clean build-lambdas + km init
    --dry-run=false) MINUS the schema/sidecars steps (no SandboxProfile change in Phase 101).
OPERATOR-GUIDE.md:
  - Phase 100 federated-relay operator entry at line 769. Add the Phase 101 entry adjacent, same format.
CLAUDE.md:
  - Project-history block with Phase 97 / Phase 100 (+ Slack 95/96) sections. Add a Phase 101 section in the
    same dated-bold style.

Slack analog to cite for parity: docs/slack-notifications.md § Phase 96 (the default_router operator guide
this mirrors). Synthetic examples only (example.com, acme/widgets, synthetic account IDs, prefixes kph/sec).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/github-bridge.md + OPERATOR-GUIDE.md + CLAUDE.md Phase 101 sections</name>
  <files>docs/github-bridge.md, OPERATOR-GUIDE.md, CLAUDE.md</files>
  <action>
    1. docs/github-bridge.md: add a TOC entry (after the Phase 100 entry at line 28) and a new
       "## Phase 101 — Orphan-repo helpful reply (front-door default router)" section covering:
         - What it does: after federation (Phase 100), an @-mention on a repo NO install owns was silently
           dropped (github_relay_no_owner); now the FRONT DOOR posts ONE guidance PR/issue comment naming
           how to wire the repo (github.repos: in km-config.yaml + km init).
         - Toggle: github.default_router: true (front-door install ONLY). Absent/false ⇒ dormant
           (byte-identical to Phase 100).
         - Mechanism: claim-aware scatter-gather — each relayed-to peer returns 200 {claimed:bool}; zero
           claims ⇒ orphan ⇒ comment; any claim ⇒ owner handled ⇒ no comment.
         - Rollout-safe mixed fleet: a peer still on Phase-100 code returns a plain 200 (no body) → tallied
           as claimed:true → NEVER a false "nobody owns this". Upgrade peers in any order.
         - Cooldown: one comment per (repo,number) per 3600s via the shared nonces table
           (gh-router-cooldown:{owner}/{repo}#{number}) — no repeat spam on a busy PR.
         - No SandboxProfile schema change ⇒ no km init --sidecars, no sandbox recreate.
         - Deploy: `make build-lambdas` (clean) + `km init --dry-run=false` on the FRONT-DOOR install
           (env-block change ⇒ full apply, NOT --sidecars). Cite project_km_init_lambdas_doesnt_deploy and
           feedback_km_init_full_apply. Point to docs/slack-notifications.md § Phase 96 for the Slack analog.
         - Troubleshooting: comment never appears (default_router off? App not installed on that repo so
           Installation.ID==0? cooldown still active? not an @-mention?). A false orphan should never happen
           (rollout-safe tally).
    2. OPERATOR-GUIDE.md: after the Phase 100 entry (line 769), add a Phase 101 orphan-repo router entry: the
       one config key (github.default_router: true on the front door), what the operator sees (a guidance
       comment on unowned-repo @-mentions), the deploy command (make build-lambdas + km init --dry-run=false),
       and a pointer to docs/github-bridge.md § Phase 101.
    3. CLAUDE.md: add a "**Phase 101 (2026-06-08) — GitHub bridge orphan-repo helpful reply (complete):**"
       bullet block in the project-history section (mirror the Phase 97/100 blocks): one-paragraph summary +
       the dormant-by-default note + the deploy line + "See docs/github-bridge.md § Phase 101."
    Keep all examples synthetic; no real tenants/customers.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; grep -q 'Phase 101' docs/github-bridge.md &amp;&amp; grep -q 'default_router' docs/github-bridge.md &amp;&amp; grep -q 'default_router' OPERATOR-GUIDE.md &amp;&amp; grep -q 'Phase 101' CLAUDE.md &amp;&amp; grep -q 'dry-run=false' docs/github-bridge.md &amp;&amp; echo DOCS_OK</automated>
  </verify>
  <done>All three docs mention Phase 101 + github.default_router; the deploy section says km init --dry-run=false (NOT --sidecars); rollout-safe tally + cooldown documented.</done>
</task>

<task type="auto">
  <name>Task 2: 101-UAT.md two-install GH-ORPHAN-E2E runbook</name>
  <files>.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-UAT.md</files>
  <action>
    Write 101-UAT.md as a runnable manual runbook (mirror the Phase 100 UAT style if a 100-UAT.md exists in
    the Phase 100 phase dir). Sections:
      - Preconditions: two installs (e.g. prefix `kph` + `sec`) sharing ONE GitHub App (one webhook URL); the
        App installed on a test repo (e.g. acme/widgets) that NEITHER install lists under github.repos:; an
        owned test repo for the negative case (listed under sec's github.repos:).
      - Setup: on the FRONT-DOOR install set `github.default_router: true` AND
        `github.peer_bridges: [<sec bridge URL>]` in km-config.yaml; `make build-lambdas` (clean) + `km init
        --dry-run=false` on BOTH installs (front door for the env-block; the peer to pick up the claim-emit
        upgrade). Verify the front-door Lambda env has KM_GITHUB_DEFAULT_ROUTER=true (aws lambda
        get-function-configuration).
      - Test A (orphan ⇒ ONE comment): @-mention the bot in a PR/issue comment on the UNOWNED repo. EXPECT:
        exactly ONE guidance comment from the front door naming github.repos: + km init; no double-post.
      - Test B (cooldown): within 3600s, @-mention the bot AGAIN on the same PR. EXPECT: NO second comment.
      - Test C (owned ⇒ no orphan comment): @-mention the bot on the OWNED repo. EXPECT: normal dispatch
        (👀 + sandbox turn) and NO guidance comment (owner claimed it).
      - Test D (rollout-safe / negative, optional): point peer_bridges at a peer still on Phase-100 code;
        @-mention on an unowned repo. EXPECT: NO false orphan comment (legacy no-body 200 ⇒ tallied as claimed).
      - Pass/Fail table: each test with its expected observable + a checkbox; a Notes/Findings area; a Status
        line (draft → passed/failed) for UAT time.
    Frontmatter: phase, status: draft, requirement: GH-ORPHAN-E2E. Synthetic names only.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; test -f .planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-UAT.md &amp;&amp; grep -q 'default_router' .planning/phases/101-*/101-UAT.md &amp;&amp; grep -q 'GH-ORPHAN-E2E' .planning/phases/101-*/101-UAT.md &amp;&amp; grep -qi 'cooldown' .planning/phases/101-*/101-UAT.md &amp;&amp; echo UAT_OK</automated>
  </verify>
  <done>101-UAT.md exists with the two-install setup, Tests A-C (+ optional D), explicit EXPECT assertions, a pass/fail table, and GH-ORPHAN-E2E + default_router + cooldown referenced.</done>
</task>

</tasks>

<verification>
- `grep -q 'Phase 101' docs/github-bridge.md OPERATOR-GUIDE.md CLAUDE.md` (all three)
- `grep -q 'default_router' docs/github-bridge.md OPERATOR-GUIDE.md` and `grep -q 'dry-run=false' docs/github-bridge.md`
- `test -f .planning/phases/101-*/101-UAT.md` and it covers unowned ⇒ one comment, owned ⇒ none, cooldown ⇒ no repeat
- No real tenant/customer names in any edited doc (synthetic example.com / acme/widgets / kph / sec only)
</verification>

<success_criteria>
GH-ORPHAN-E2E: operators have a clear Phase 101 runbook (toggle, cooldown, rollout-safe mixed fleet, deploy)
across docs/github-bridge.md + OPERATOR-GUIDE.md + CLAUDE.md, and a runnable two-install / one-App /
unowned-repo UAT (101-UAT.md) with explicit pass/fail assertions: unowned-repo @-mention ⇒ exactly one
guidance comment from the front door; owned-repo @-mention ⇒ none; repeat within the window ⇒ no second comment.
</success_criteria>

<output>
After completion, create `.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-04-SUMMARY.md`
</output>
