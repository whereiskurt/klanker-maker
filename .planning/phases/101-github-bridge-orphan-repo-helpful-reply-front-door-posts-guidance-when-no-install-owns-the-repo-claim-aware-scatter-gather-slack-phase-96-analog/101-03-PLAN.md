---
phase: 101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog
plan: 03
type: tdd
wave: 2
depends_on: ["01", "02"]
files_modified:
  - pkg/github/bridge/webhook_handler.go
  - pkg/github/bridge/orphan_reply.go
  - pkg/github/bridge/webhook_handler_phase101_test.go
  - pkg/github/bridge/webhook_handler_phase100_test.go
  - cmd/km-github-bridge/main.go
autonomous: true
requirements: [GH-ORPHAN-CLAIM, GH-ORPHAN-REPLY, GH-ORPHAN-COOLDOWN, GH-ORPHAN-ROLLOUT]
must_haves:
  truths:
    - "A relayed (x-km-relayed:1) Resolve() MISS returns 200 {\"claimed\":false} JSON (was plain \"ok\"); a relayed Resolve() MATCH/dispatch returns 200 {\"claimed\":true} JSON; a NON-relayed owned delivery still returns plain \"ok\" (byte-identical)"
    - "On the front-door (!matched, non-relayed) path, Handle captures Broadcast's []PeerClaimResult; ONLY when h.DefaultRouter is true does it tally anyClaimed and, if zero claims, call maybePostGitHubOrphanComment"
    - "maybePostGitHubOrphanComment gates: (1) h.DefaultRouter, (2) ContainsMention(comment.Body, botLogin) re-check (Phase-100 reorder skipped the mention filter on !matched), (3) cooldown CheckAndStore(gh-router-cooldown:{owner}/{repo}#{number}, 3600) returns first-time, (4) h.Commenter != nil AND payload.Installation.ID != 0; any fail ⇒ silent return, Handle still 200"
    - "On zero claims + all gates pass, exactly ONE Commenter.PostComment fires with the guidance text naming github.repos: + km init; a second orphan on the same (repo,number) within 3600s is suppressed by the cooldown"
    - "When h.DefaultRouter is false (default), NO tally and NO orphan post happen — byte-identical to Phase 100 (the DefaultRouterOff assertion)"
    - "cmd/km-github-bridge/main.go sets webhookHandler.DefaultRouter=true AND webhookHandler.OrphanCooldown=nonceStore ONLY when os.Getenv(KM_GITHUB_DEFAULT_ROUTER)==\"true\"; otherwise both stay zero-value (dormant)"
  artifacts:
    - path: "pkg/github/bridge/webhook_handler.go"
      provides: "DefaultRouter + OrphanCooldown fields; peer-side claim emit; front-door tally short-circuit"
      contains: "DefaultRouter bool"
    - path: "pkg/github/bridge/orphan_reply.go"
      provides: "maybePostGitHubOrphanComment with all four gates + bounded PostComment"
      contains: "maybePostGitHubOrphanComment"
    - path: "pkg/github/bridge/webhook_handler_phase101_test.go"
      provides: "peer-side claim emit + tally + orphan happy-path + non-mention + cooldown + DefaultRouterOff + Commenter==nil + Installation.ID==0 tests"
      contains: "OrphanComment"
    - path: "cmd/km-github-bridge/main.go"
      provides: "KM_GITHUB_DEFAULT_ROUTER gate wiring DefaultRouter + OrphanCooldown=nonceStore"
      contains: "KM_GITHUB_DEFAULT_ROUTER"
  key_links:
    - from: "front-door Broadcast []PeerClaimResult"
      to: "maybePostGitHubOrphanComment"
      via: "if h.DefaultRouter { anyClaimed tally; if !anyClaimed { post } }"
      pattern: "anyClaimed"
    - from: "maybePostGitHubOrphanComment"
      to: "Commenter.PostComment (GitHub PR/issue comment)"
      via: "h.Commenter.PostComment(InstallIDString(payload.Installation.ID), owner, repo, number, body) under 5s ctx"
      pattern: "PostComment"
    - from: "maybePostGitHubOrphanComment"
      to: "OrphanCooldown.CheckAndStore (nonces table)"
      via: "key gh-router-cooldown:{owner}/{repo}#{number}, ttl 3600; seen==true ⇒ suppress"
      pattern: "gh-router-cooldown:"
---

<objective>
Wire the claim-aware machinery into the handler and the Lambda entrypoint — the behavioral heart of
Phase 101. Four moves in `pkg/github/bridge` + `cmd/km-github-bridge/main.go`:

1. PEER-SIDE CLAIM EMIT (`webhook_handler.go`): a relayed (`x-km-relayed:1`) Resolve() MISS returns
   `200 {"claimed":false}` (replace the plain `"ok"` at line 233); a relayed Resolve() MATCH that
   dispatches returns `200 {"claimed":true}` (guard the final `return 200 "ok"` at line 505 with
   `if req.Headers["x-km-relayed"] != ""`). A NON-relayed owned delivery KEEPS plain `"ok"` (dormancy
   byte-identity — GitHub ignores the body, but we preserve it). Add a small `jsonClaim(bool)` helper.

2. FRONT-DOOR TALLY SHORT-CIRCUIT (`webhook_handler.go` !matched/non-relayed branch, lines 235-242):
   capture `claimResults, bErr := h.Relayer.Broadcast(...)`. ONLY when `h.DefaultRouter` is true: tally
   `anyClaimed`; if zero claims, call `h.maybePostGitHubOrphanComment(ctx, payload, botLogin)`. When
   `DefaultRouter` is false ⇒ discard the result (no tally, no post) ⇒ byte-identical to Phase 100.

3. ORPHAN-COMMENT METHOD (new `orphan_reply.go`): `maybePostGitHubOrphanComment` with four gates —
   DefaultRouter, ContainsMention re-check (Pitfall 2 — the Phase-100 reorder skipped the mention filter
   on !matched), cooldown via `OrphanCooldown.CheckAndStore`, and `Commenter != nil && Installation.ID != 0`.
   Post synchronously under a 5s context (Pitfall 3 — Lambda freeze). Reuses the already-wired
   `InstallationCommenter` + `DynamoGitHubNonceStore` — NO new table/interface.

4. CMD WIRING (`main.go`): add `DefaultRouter bool` + `OrphanCooldown DeliveryNonceStore` fields to the
   handler struct; set them ONLY when `os.Getenv("KM_GITHUB_DEFAULT_ROUTER") == "true"`, reusing the
   `nonceStore` already built at main.go:173 as the cooldown store. NO RunningChannels analog.

DEPENDS ON Plan 02 (the new `Broadcast([]PeerClaimResult, error)` signature + `PeerClaimResult` type) and
Plan 01 (the `KM_GITHUB_DEFAULT_ROUTER` env var that main.go reads). The existing `mockPeerRelayer`
(webhook_handler_phase100_test.go:32) MUST be updated to the new return signature.
Output: peer-side claim emit + front-door tally + orphan_reply.go + phase101 tests + main.go wiring.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-RESEARCH.md
@.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-VALIDATION.md
@pkg/github/bridge/webhook_handler.go
@pkg/github/bridge/interfaces.go
@pkg/github/bridge/webhook_handler_phase99_test.go
@pkg/github/bridge/webhook_handler_phase100_test.go
@cmd/km-github-bridge/main.go
@pkg/slack/bridge/events_handler.go

<interfaces>
<!-- All anchors verified against the live tree 2026-06-08. -->

WEBHOOK_HANDLER current (the insertion points):
  pkg/github/bridge/webhook_handler.go:
    - WebhookHandler struct line 61; Commenter CommentPoster line 133; Relayer PeerRelayer line 141.
      ADD: `DefaultRouter bool` (default false ⇒ dormant) and `OrphanCooldown DeliveryNonceStore`.
    - botLogin fetched at line 189 (available before the relay branch).
    - Resolve() + !matched branch lines 222-249: relayed-miss return at 233 (→ jsonClaim(false));
      front-door Broadcast at 239 (→ capture claimResults + tally when DefaultRouter).
    - final matched/dispatch return at line 505 (→ guard with x-km-relayed ⇒ jsonClaim(true)).
    - Available helpers (all confirmed in-package): ContainsMention(body, botLogin),
      OwnerFromFullName(fullName), RepoFromFullName(fullName), InstallIDString(installID),
      payload.Installation.ID, payload.Issue.Number, payload.Repository.FullName, payload.Comment.Body.

INTERFACES (Plan 02 delivered these — re-confirm they exist before coding):
  PeerRelayer.Broadcast(...) ([]PeerClaimResult, error); PeerClaimResult{PeerURL; Claimed}.
  DeliveryNonceStore.CheckAndStore(ctx, key, ttlSeconds) (alreadySeen bool, err error) — interfaces.go:70
    — REUSE for OrphanCooldown (no new interface; the field type IS DeliveryNonceStore).
  CommentPoster.PostComment(ctx, installationID, owner, repo string, issueNumber int, body string) error — interfaces.go:114.

CMD WIRING:
  cmd/km-github-bridge/main.go: nonceStore built at line 173; WebhookHandler{} at lines 244-263; the
  Phase-100 Relayer guard at lines 266-270. ADD after that guard:
    if os.Getenv("KM_GITHUB_DEFAULT_ROUTER") == "true" {
        webhookHandler.DefaultRouter = true
        webhookHandler.OrphanCooldown = nonceStore   // *DynamoGitHubNonceStore satisfies DeliveryNonceStore
    }

SLACK TEMPLATE (the gating logic to mirror, minus channels):
  pkg/slack/bridge/events_handler.go: front-door tally ~275-300; maybePostOrphanReply ~599-700 (DefaultRouter
  gate, mention re-check ~614, cooldown Reserve, PostMessage, 5s context). GitHub drops the channel list.

TEST DOUBLES (must update / reuse):
  webhook_handler_phase100_test.go:32 mockPeerRelayer.Broadcast(...) error → MUST become ([]PeerClaimResult, error).
  webhook_handler_phase99_test.go:37 mockCommenter.PostComment(...) — reuse to capture orphan PostComment calls.
  Add a fake DeliveryNonceStore (configurable `seen`) for the cooldown gate, OR reuse an existing nonces fake.

RESEARCH code examples to follow verbatim: "Front-door tally + orphan post" (lines 238-252),
"Orphan comment method" (lines 255-275), "Peer-side claim emit" (lines 277-287).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — peer-side claim, front-door tally, orphan-comment, cooldown, dormancy tests</name>
  <files>pkg/github/bridge/webhook_handler_phase101_test.go, pkg/github/bridge/webhook_handler_phase100_test.go</files>
  <behavior>
    First update mockPeerRelayer (phase100_test.go:32) to `Broadcast(...) ([]PeerClaimResult, error)` with a
    configurable returned `[]PeerClaimResult` + error (so existing Phase-100 tests compile; their assertions
    are behavior-preserving — a Phase-100 front door with DefaultRouter=false must still just relay + 200).

    New webhook_handler_phase101_test.go (table-driven; reuse mockCommenter, a fake DeliveryNonceStore):
    - PeerClaim_RelayedMiss_ClaimedFalse: x-km-relayed:1 + unowned repo ⇒ response body is {"claimed":false}.
    - PeerClaim_RelayedOwned_ClaimedTrue: x-km-relayed:1 + owned repo (matched, dispatch mocked) ⇒ body {"claimed":true}.
    - PeerClaim_NonRelayedOwned_PlainOk: no x-km-relayed + owned ⇒ body "ok" (byte-identity).
    - OrphanComment_HappyPath: DefaultRouter=true, Relayer returns ZERO claims (all Claimed:false), comment
      @-mentions bot, cooldown clear, Commenter set, Installation.ID!=0 ⇒ EXACTLY ONE PostComment with text
      containing "github.repos:" and "km init"; response still 200.
    - OrphanComment_AnyClaim_NoPost: Relayer returns at least one Claimed:true ⇒ NO PostComment.
    - OrphanComment_NonMention_NoPost: zero claims but comment does NOT @-mention the bot ⇒ NO PostComment.
    - OrphanCooldown_SecondSuppressed: fake cooldown returns seen=true ⇒ NO PostComment; seen=false ⇒ posts.
      Assert the cooldown KEY equals gh-router-cooldown:{owner}/{repo}#{number} and ttl 3600.
    - DefaultRouterOff_Silent (GH-ORPHAN-ROLLOUT dormancy): DefaultRouter=false, even with zero claims +
      mention + Commenter set ⇒ NO tally, NO PostComment (byte-identical to Phase 100).
    - OrphanComment_CommenterNil_Skip: Commenter==nil ⇒ no panic, no post.
    - OrphanComment_InstallationIDZero_Skip: payload.Installation.ID==0 ⇒ no post.
    Name the mention/happy/claim tests `-run OrphanComment`; the cooldown test `-run OrphanCooldown`; the
    dormancy test `-run DefaultRouterOff`; the peer-emit tests `-run PeerClaim`.
  </behavior>
  <action>
    Build a WebhookHandler with the new DefaultRouter + OrphanCooldown fields and a mockPeerRelayer that
    returns a configurable []PeerClaimResult. Reuse mockCommenter (phase99) to capture PostComment. Add a
    fake DeliveryNonceStore that records the key+ttl and returns a configurable seen bool. These tests
    reference DefaultRouter/OrphanCooldown/maybePostGitHubOrphanComment/jsonClaim which do not exist yet —
    RED. Synthetic owner/repo (e.g. "acme/widgets"), synthetic install IDs.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'PeerClaim|OrphanComment|OrphanCooldown|DefaultRouterOff' -count=1 2>&amp;1 | tail -10</automated>
  </verify>
  <done>phase101 tests + updated mockPeerRelayer exist; suite FAILS (RED) — DefaultRouter, OrphanCooldown, the orphan method, and jsonClaim are undefined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — peer-side claim emit + front-door tally + struct fields + jsonClaim</name>
  <files>pkg/github/bridge/webhook_handler.go</files>
  <behavior>
    - PeerClaim + tally + DefaultRouterOff tests pass.
    - relayed miss ⇒ {"claimed":false}; relayed owned ⇒ {"claimed":true}; non-relayed owned ⇒ "ok".
    - front-door tally + orphan call happen ONLY when h.DefaultRouter (short-circuit when false).
  </behavior>
  <action>
    1. WebhookHandler struct (after Relayer, line 141): add
         `// DefaultRouter enables the Phase 101 orphan-repo guidance comment (front-door only).`
         `// false ⇒ dormant (byte-identical to Phase 100: relay + 200, no claim tally, no comment).`
         `DefaultRouter bool`
         `// OrphanCooldown rate-limits the orphan comment per (repo,number). Reuses the nonces store`
         `// (gh-router-cooldown: prefix). Nil ⇒ no cooldown gate (still gated by DefaultRouter+mention).`
         `OrphanCooldown DeliveryNonceStore`
    2. Add a small package helper:
         `func jsonClaim(claimed bool) WebhookResponse { b, _ := json.Marshal(map[string]bool{"claimed": claimed}); return WebhookResponse{StatusCode: 200, Body: string(b), Headers: map[string]string{"Content-Type": "application/json"}} }`
       (check WebhookResponse's fields — if it has no Headers map, just set Body to the JSON string; GitHub
       ignores response headers. Match the actual WebhookResponse type.)
    3. relayed-miss branch (line 229-233): replace `return WebhookResponse{200,"ok"}` with `return jsonClaim(false)`.
    4. front-door branch (line 235-242): change to
         `claimResults, bErr := h.Relayer.Broadcast(ctx, req.RawBody, req.Headers)`
         keep the partial-failure WARN; then:
         `if h.DefaultRouter { anyClaimed := false; for _, r := range claimResults { if r.Claimed { anyClaimed = true; break } }; if !anyClaimed { h.maybePostGitHubOrphanComment(ctx, payload, botLogin) } }`
       Keep the nil-Relayer else-branch and the final `return WebhookResponse{200,"ok"}` (line 248) unchanged.
    5. final matched/dispatch return (line 505): just before it, add
         `if req.Headers["x-km-relayed"] != "" { return jsonClaim(true) }`
       then keep `return WebhookResponse{200,"ok"}` for the non-relayed owned path.
    Add the encoding/json import if absent. maybePostGitHubOrphanComment is defined in Task 3 (same package) —
    this compiles once Task 3 lands; if you implement Task 3 first that is fine (same package, one build).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'PeerClaim|DefaultRouterOff' -count=1 2>&amp;1 | tail -8</automated>
  </verify>
  <done>Peer-side claim emit (false/true/plain-ok) + front-door tally short-circuit + DefaultRouterOff dormancy GREEN; struct has DefaultRouter + OrphanCooldown; jsonClaim helper added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — maybePostGitHubOrphanComment (mention re-check + cooldown + reuse Commenter)</name>
  <files>pkg/github/bridge/orphan_reply.go</files>
  <behavior>
    - OrphanComment + OrphanCooldown tests pass.
    - Gates in order: DefaultRouter → ContainsMention(comment.Body, botLogin) → Commenter!=nil && Installation.ID!=0
      → cooldown CheckAndStore(gh-router-cooldown:{owner}/{repo}#{number}, 3600) first-time; any fail ⇒ silent return.
    - On all-pass: one bounded (5s) PostComment with guidance text naming github.repos: + km init.
  </behavior>
  <action>
    New pkg/github/bridge/orphan_reply.go (package bridge), following RESEARCH "Orphan comment method"
    (lines 255-275):
      func (h *WebhookHandler) maybePostGitHubOrphanComment(ctx context.Context, payload IssueCommentPayload, botLogin string) {
        if !h.DefaultRouter { return }                                   // gate 1
        if !ContainsMention(payload.Comment.Body, botLogin) { return }   // gate 2 (Phase-100 reorder skipped mention on !matched — Pitfall 2)
        if h.Commenter == nil || payload.Installation.ID == 0 { return } // gate 3 (Pitfall 1)
        owner := OwnerFromFullName(payload.Repository.FullName)
        repo  := RepoFromFullName(payload.Repository.FullName)
        key := fmt.Sprintf("gh-router-cooldown:%s/%s#%d", owner, repo, payload.Issue.Number)
        if h.OrphanCooldown != nil {                                     // gate 4: cooldown
          seen, err := h.OrphanCooldown.CheckAndStore(ctx, key, 3600)
          if err != nil { h.log().Warn("github-bridge: orphan cooldown check failed; skipping", "err", err); return }
          if seen { h.log().Debug("github-bridge: orphan cooldown active; suppressing", "repo", payload.Repository.FullName, "number", payload.Issue.Number); return }
        }
        body := fmt.Sprintf("No klanker sandbox is bound to `%s`. To enable the bot here, an operator must add this repo under `github.repos:` in `km-config.yaml` and run `km init`. See the github-bridge runbook (docs/github-bridge.md).", payload.Repository.FullName)
        cctx, cancel := context.WithTimeout(ctx, 5*time.Second); defer cancel()
        if err := h.Commenter.PostComment(cctx, InstallIDString(payload.Installation.ID), owner, repo, payload.Issue.Number, body); err != nil {
          h.log().Warn("github-bridge: orphan comment post failed", "repo", payload.Repository.FullName, "err", err)
        }
      }
    Confirm the IssueCommentPayload type name + the exact PostComment owner/repo/number arg order against
    interfaces.go:114 and the payload type in payload.go. Add imports: context, fmt, time. The guidance text
    is Claude's discretion (RESEARCH §Discretion) — keep it a single clear paragraph with the literal
    `github.repos:` and `km init`; no real tenant names.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go test ./pkg/github/bridge/... -run 'OrphanComment|OrphanCooldown' -count=1 2>&amp;1 | tail -8</automated>
  </verify>
  <done>Orphan happy-path (one PostComment, text has github.repos: + km init), non-mention skip, cooldown suppress/expire (key gh-router-cooldown:{owner}/{repo}#{number}, ttl 3600), Commenter==nil + Installation.ID==0 skips all GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Wire DefaultRouter + OrphanCooldown in cmd/km-github-bridge/main.go</name>
  <files>cmd/km-github-bridge/main.go</files>
  <action>
    After the Phase-100 Relayer guard (main.go:266-270, the `if relayer != nil { webhookHandler.Relayer = relayer }`),
    add:
      // Phase 101: front-door orphan-repo router. Reuse the already-built nonceStore as the per-(repo,number)
      // cooldown store (gh-router-cooldown: prefix isolates it from github-delivery: keys in the shared table).
      // Dormant unless KM_GITHUB_DEFAULT_ROUTER=="true" — byte-identical to Phase 100 when off.
      if os.Getenv("KM_GITHUB_DEFAULT_ROUTER") == "true" {
          webhookHandler.DefaultRouter = true
          webhookHandler.OrphanCooldown = nonceStore
          slog.Info("km-github-bridge: orphan-repo default router ENABLED (front door)")
      }
    NO RunningChannels analog. nonceStore (*DynamoGitHubNonceStore, built at main.go:173) satisfies
    DeliveryNonceStore. Confirm os + slog are already imported (they are — used above).
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr &amp;&amp; go build ./cmd/km-github-bridge/... 2>&amp;1 | tail -5 &amp;&amp; grep -q 'KM_GITHUB_DEFAULT_ROUTER' cmd/km-github-bridge/main.go &amp;&amp; grep -q 'OrphanCooldown = nonceStore' cmd/km-github-bridge/main.go &amp;&amp; echo WIRED</automated>
  </verify>
  <done>main.go builds; DefaultRouter + OrphanCooldown=nonceStore set only under KM_GITHUB_DEFAULT_ROUTER=="true"; no RunningChannels analog.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/github/bridge/... -run 'PeerClaim|OrphanComment|OrphanCooldown|DefaultRouterOff' -count=1` GREEN
- `go test ./pkg/github/bridge/... -count=1` full package GREEN (Phase 97/98/99/100 tests preserved — no regression)
- `go build ./cmd/km-github-bridge/...` succeeds
- `grep -q 'jsonClaim(false)' pkg/github/bridge/webhook_handler.go` (relayed-miss claim emit)
- `grep -q 'maybePostGitHubOrphanComment' pkg/github/bridge/webhook_handler.go` (front-door call gated by DefaultRouter)
- `grep -q 'gh-router-cooldown:' pkg/github/bridge/orphan_reply.go` (cooldown key shape)
- `! grep -q 'RunningChannel\|SandboxChannelInfo' pkg/github/bridge/orphan_reply.go cmd/km-github-bridge/main.go`
</verification>

<success_criteria>
GH-ORPHAN-CLAIM + GH-ORPHAN-REPLY + GH-ORPHAN-COOLDOWN + GH-ORPHAN-ROLLOUT (behavioral half):
peers emit `{claimed:bool}` on relayed deliveries; the front door tallies and, on zero claims with a
real @-mention + cooldown-clear, posts EXACTLY ONE guidance comment naming `github.repos:` + `km init`
via the reused `InstallationCommenter`; a repeat within 3600s is suppressed via `DynamoGitHubNonceStore`
(`gh-router-cooldown:` key); `DefaultRouter=false` ⇒ no tally, no comment, byte-identical to Phase 100;
`Commenter==nil` / `Installation.ID==0` skip safely. No new table/interface; no channel machinery.
</success_criteria>

<output>
After completion, create `.planning/phases/101-github-bridge-orphan-repo-helpful-reply-front-door-posts-guidance-when-no-install-owns-the-repo-claim-aware-scatter-gather-slack-phase-96-analog/101-03-SUMMARY.md`
</output>
