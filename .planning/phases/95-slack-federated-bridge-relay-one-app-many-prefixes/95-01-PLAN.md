---
phase: 95-slack-federated-bridge-relay-one-app-many-prefixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_test.go
  - internal/app/cmd/init.go
  - infra/live/use1/lambda-slack-bridge/terragrunt.hcl
  - infra/modules/lambda-slack-bridge/v1.0.0/variables.tf
  - infra/modules/lambda-slack-bridge/v1.0.0/main.tf
autonomous: true
requirements: [SLACK-FED-CFG, SLACK-FED-PLUMB]
must_haves:
  truths:
    - "slack.peer_bridges in km-config.yaml round-trips through config load into cfg.Slack.PeerBridges"
    - "Absent slack.peer_bridges yields a nil slice (federation off sentinel)"
    - "km init exports KM_SLACK_PEER_BRIDGES (comma-joined) only when PeerBridges is non-empty, with an env-wins drift WARN"
    - "The TF module exposes slack_peer_bridges var and writes KM_SLACK_PEER_BRIDGES into the bridge Lambda env"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "SlackConfig.PeerBridges field + merge-list entry + GetStringSlice population"
      contains: "PeerBridges"
    - path: "internal/app/config/config_test.go"
      provides: "Round-trip + absent + drift-warn regression tests for peer_bridges"
      contains: "TestLoadSlackPeerBridges"
    - path: "internal/app/cmd/init.go"
      provides: "KM_SLACK_PEER_BRIDGES export with comma-join + drift WARN"
      contains: "KM_SLACK_PEER_BRIDGES"
    - path: "infra/modules/lambda-slack-bridge/v1.0.0/variables.tf"
      provides: "slack_peer_bridges string variable"
      contains: "slack_peer_bridges"
    - path: "infra/modules/lambda-slack-bridge/v1.0.0/main.tf"
      provides: "KM_SLACK_PEER_BRIDGES = var.slack_peer_bridges in Lambda env"
      contains: "KM_SLACK_PEER_BRIDGES"
  key_links:
    - from: "internal/app/config/config.go merge-list"
      to: "cfg.Slack.PeerBridges population"
      via: '"slack.peer_bridges" string in the v2->v merge key list'
      pattern: "slack\\.peer_bridges"
    - from: "internal/app/cmd/init.go"
      to: "infra/live/use1/lambda-slack-bridge/terragrunt.hcl get_env"
      via: "KM_SLACK_PEER_BRIDGES env var"
      pattern: "KM_SLACK_PEER_BRIDGES"
---

<objective>
Add the `slack.peer_bridges` config key and plumb it end-to-end to the bridge Lambda
environment as `KM_SLACK_PEER_BRIDGES`, mirroring the proven `slack.mention_only` /
`slack.react_always` pattern EXACTLY. This is the foundation Wave for Phase 95 federated
relay: it makes the peer-bridge URL list available to the bridge Lambda. No relay logic
yet — that is Plan 02.

Purpose: A `[]string` config field that survives the v2→v merge-list footgun and reaches
the Lambda env. Without the merge-list entry, the value is silently dropped.
Output: Config struct field + regression tests, init.go env export with drift WARN, and
the full Terraform/terragrunt plumbing chain.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-slack-federated-bridge-relay-one-app-many-prefixes/95-RESEARCH.md

# Pattern source — read these to copy the EXACT MentionOnly/ReactAlways shape:
@internal/app/config/config.go
@internal/app/cmd/init.go
@infra/live/use1/lambda-slack-bridge/terragrunt.hcl
@infra/modules/lambda-slack-bridge/v1.0.0/variables.tf
@infra/modules/lambda-slack-bridge/v1.0.0/main.tf

<interfaces>
<!-- Exact line anchors from RESEARCH.md — copy the MentionOnly/ReactAlways pattern. -->

config.go SlackConfig struct (line 24-44) — add a []string field (NOT *bool):
```go
type SlackConfig struct {
    MentionOnly *bool `mapstructure:"mention_only" yaml:"mention_only,omitempty"`   // line 33
    ReactAlways *bool `mapstructure:"react_always" yaml:"react_always,omitempty"`   // line 43
    // ADD:
    PeerBridges []string `mapstructure:"peer_bridges" yaml:"peer_bridges,omitempty"`
}
```

config.go v2->v merge-list (lines 399, 401 currently hold the two slack keys):
```go
            "slack.mention_only",   // line 399
            "slack.react_always",   // line 401
            "slack.peer_bridges",   // ADD — CRITICAL footgun; omitting silently drops the value
```

config.go population block (after ReactAlways block ending ~line 491):
```go
if v.IsSet("slack.peer_bridges") {
    cfg.Slack.PeerBridges = v.GetStringSlice("slack.peer_bridges")
}
```
Note: []string does NOT use the *bool tri-state. nil slice == "federation off".

init.go MentionOnly export template (lines 875-882) — adapt for []string (strings.Join, not strconv.FormatBool):
```go
if len(cfg.Slack.PeerBridges) > 0 {
    yamlPeerBridges := strings.Join(cfg.Slack.PeerBridges, ",")
    if envVal := os.Getenv("KM_SLACK_PEER_BRIDGES"); envVal != "" && envVal != yamlPeerBridges {
        fmt.Fprintf(os.Stderr, "WARN: KM_SLACK_PEER_BRIDGES=%s (env) overrides km-config.yaml slack.peer_bridges=%s\n", envVal, yamlPeerBridges)
    } else if envVal == "" {
        os.Setenv("KM_SLACK_PEER_BRIDGES", yamlPeerBridges) //nolint:errcheck
    }
}
```
strings is already imported in init.go.

terragrunt.hcl (slack_mention_only at line 97, slack_react_always at line 109):
```hcl
slack_peer_bridges = get_env("KM_SLACK_PEER_BRIDGES", "")
```

config_test.go template: TestLoadSlackMentionOnly_True + writeKMConfig helper (line 12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SlackConfig.PeerBridges field + merge-list + population, with regression tests</name>
  <files>internal/app/config/config.go, internal/app/config/config_test.go</files>
  <behavior>
    - TestLoadSlackPeerBridges_Set: km-config.yaml with slack.peer_bridges listing two
      /events URLs → cfg.Slack.PeerBridges has len==2 in file order.
    - TestLoadSlackPeerBridges_Absent: km-config.yaml omitting the slack block →
      cfg.Slack.PeerBridges == nil (the federation-off sentinel).
    - The merge-list regression: with the file value set, the loaded slice is NON-nil —
      this is exactly the case that fails if "slack.peer_bridges" is missing from the
      v2→v merge-list. Assert len==2, not just non-nil, to make the footgun visible.
  </behavior>
  <action>
    Mirror the slack.mention_only / slack.react_always pattern in config.go:
    1. Add `PeerBridges []string `mapstructure:"peer_bridges" yaml:"peer_bridges,omitempty"`` to
       the SlackConfig struct (after ReactAlways, ~line 43). Add a comment noting Phase 95
       and that nil == federation off.
    2. CRITICAL: add `"slack.peer_bridges",` to the v2→v merge key list (immediately after
       `"slack.react_always",` at line 401). This is the project_config_key_merge_list footgun —
       without it the value is silently dropped. Add a comment: "// Phase 95: federated relay
       peer list."
    3. Add the population block after the ReactAlways block (~line 491):
       `if v.IsSet("slack.peer_bridges") { cfg.Slack.PeerBridges = v.GetStringSlice("slack.peer_bridges") }`.
       Use GetStringSlice directly — []string, not the *bool tri-state.
    Then write the three tests in config_test.go using the writeKMConfig helper (line 12) and the
    TestLoadSlackMentionOnly_True structure (line 731 per research). The drift-warn case can be a
    separate test or folded into Task 2's init export test — either is fine; the round-trip + absent
    cases MUST land in this task to catch the merge-list footgun.
  </action>
  <verify>
    <automated>go test ./internal/app/config/... -run TestLoadSlackPeerBridges -v</automated>
  </verify>
  <done>cfg.Slack.PeerBridges is populated (len==2) when slack.peer_bridges is set in km-config.yaml and nil when absent; both tests green; "slack.peer_bridges" present in the merge-list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export KM_SLACK_PEER_BRIDGES in init.go with comma-join + drift WARN</name>
  <files>internal/app/cmd/init.go, internal/app/config/config_test.go</files>
  <behavior>
    - When cfg.Slack.PeerBridges has entries and KM_SLACK_PEER_BRIDGES is unset, the env
      var is set to the comma-joined list.
    - When the env var is already set to a DIFFERENT value, a drift WARN is printed to
      stderr and the env value is NOT overwritten (env-wins, matching MentionOnly).
    - When PeerBridges is empty/nil, no env var is set (federation-off path leaves env untouched).
  </behavior>
  <action>
    In init.go ExportTerragruntEnvVars (after the ReactAlways block ~line 895), add the
    KM_SLACK_PEER_BRIDGES export adapting the MentionOnly block (lines 875-882) for []string:
    gate on `len(cfg.Slack.PeerBridges) > 0`; value is `strings.Join(cfg.Slack.PeerBridges, ",")`
    (NOT strconv.FormatBool); env-wins drift WARN with the same fmt.Fprintf(os.Stderr, ...) shape;
    `os.Setenv` only when env is empty. `strings` is already imported.
    Add a comment block matching the MentionOnly one, noting the terragrunt get_env consumer and
    that empty list => omit => terragrunt default "" => federation off.
    Add a drift-warn regression test (TestSlackPeerBridgesDriftWarn or fold into config_test.go) that
    sets KM_SLACK_PEER_BRIDGES to a different value via t.Setenv and asserts the export does NOT
    overwrite it. If ExportTerragruntEnvVars is not unit-testable in isolation, place the assertion at
    the config layer where the comparison logic is reachable; otherwise an init_test.go case is fine.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/app/config/... ./internal/app/cmd/... -run 'Slack|PeerBridge' -v</automated>
  </verify>
  <done>km init exports KM_SLACK_PEER_BRIDGES=comma-joined when PeerBridges set; drift WARN fires and env-wins when env differs; empty list leaves env untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Terragrunt + TF module plumbing for slack_peer_bridges</name>
  <files>infra/live/use1/lambda-slack-bridge/terragrunt.hcl, infra/modules/lambda-slack-bridge/v1.0.0/variables.tf, infra/modules/lambda-slack-bridge/v1.0.0/main.tf</files>
  <action>
    Mirror slack_mention_only / slack_react_always across the three HCL layers:
    1. terragrunt.hcl — add `slack_peer_bridges = get_env("KM_SLACK_PEER_BRIDGES", "")` after the
       slack_react_always line (line 109), before the tags block. Add a Phase 95 comment noting
       empty => federation off and the km init --dry-run=false deploy requirement.
    2. variables.tf — add `variable "slack_peer_bridges" { type = string, default = "" }` after the
       Phase 91 vars (~line 130), with a description noting comma-joined /events URLs, empty == off.
    3. main.tf — add `KM_SLACK_PEER_BRIDGES = var.slack_peer_bridges` inside the Lambda
       `environment { variables = { ... } }` block (after KM_SLACK_REACT_ALWAYS, ~line 329).
    Keep all three edits string-typed and default-"" so an unset value is byte-identical to today.
  </action>
  <verify>
    <automated>terraform fmt -check -recursive infra/modules/lambda-slack-bridge/v1.0.0/ && grep -q 'slack_peer_bridges' infra/live/use1/lambda-slack-bridge/terragrunt.hcl && grep -q 'KM_SLACK_PEER_BRIDGES = var.slack_peer_bridges' infra/modules/lambda-slack-bridge/v1.0.0/main.tf</automated>
  </verify>
  <done>terragrunt.hcl reads KM_SLACK_PEER_BRIDGES via get_env; TF module declares the slack_peer_bridges var and writes KM_SLACK_PEER_BRIDGES into the Lambda env; terraform fmt clean.</done>
</task>

</tasks>

<verification>
- `go build ./... && go test ./internal/app/config/... -run Slack -v` green.
- `cfg.Slack.PeerBridges` is non-nil when set, nil when absent (merge-list footgun closed).
- `KM_SLACK_PEER_BRIDGES` exported by init.go with drift WARN; threaded through terragrunt → TF var → Lambda env.
- `terraform fmt -check -recursive infra/modules/lambda-slack-bridge/v1.0.0/` clean.
</verification>

<success_criteria>
- ROADMAP success criterion #2 (config round-trip + init export + reaches Lambda env via TF) satisfied for the config/plumbing half.
- The merge-list entry is present AND covered by a regression test (quality gate).
- No relay behavior introduced yet — Plan 02 consumes KM_SLACK_PEER_BRIDGES.
</success_criteria>

<output>
After completion, create `.planning/phases/95-slack-federated-bridge-relay-one-app-many-prefixes/95-01-SUMMARY.md`
</output>
