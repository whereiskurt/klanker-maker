---
phase: 84.4.1.1-multi-install-follow-on-gaps
plan: "05"
subsystem: infra
tags: [km-uninit, scp, organizations, aws-sdk-go-v2, tdd]

requires:
  - phase: 84.4.1.1-00
    provides: UninitOrgsAPI interface, UninitOpts.IncludeSCP field, t.Skip scaffolds in uninit_test.go

provides:
  - "--include-scp flag on km uninit cobra command"
  - "SCP detach+delete step (Step 2.5) in RunUninitWithDeps before module destroy loop"
  - "Real Organizations client wired in runUninit using klanker-terraform profile + AssumeRole into {prefix}-org-admin"
  - "TestRunUninit_DetachesSCPWhenFlagSet GREEN with 4 sub-tests (A/B/C/D)"

affects: [84.4.1.1-06, km-uninit, multi-install-teardown]

tech-stack:
  added:
    - "awssdk alias (github.com/aws/aws-sdk-go-v2/aws) in uninit.go"
    - "awsconfig alias (github.com/aws/aws-sdk-go-v2/config) in uninit.go"
    - "organizationstypes alias (github.com/aws/aws-sdk-go-v2/service/organizations/types) in uninit.go"
    - "sts (github.com/aws/aws-sdk-go-v2/service/sts) in uninit.go"
    - "organizations and organizationstypes in uninit_test.go"
  patterns:
    - "SCP cleanup gated on opt-in flag (--include-scp) mirrors --include-zone pattern from unbootstrap.go"
    - "Organizations client built via klanker-terraform profile + AssumeRole into {prefix}-org-admin — mirrors doctor.go:2900 pattern"
    - "Detach-then-delete order: DetachPolicy must succeed before DeletePolicy is called"
    - "Warn-and-continue: SCP cleanup failures do not abort uninit"
    - "OrgsClient nil check: IncludeSCP=true with nil client prints WARN with manual recovery command, no panic"

key-files:
  created: []
  modified:
    - internal/app/cmd/uninit.go
    - internal/app/cmd/uninit_test.go

key-decisions:
  - "SCP cleanup placed in Step 2.5 (after active-sandbox check, before module destroy loop) per plan spec"
  - "Default false for --include-scp: operators may want to keep SCP during partial teardowns"
  - "Without --include-scp: WARN printed with km uninit --include-scp reference so operators can clean up later"
  - "Without --include-scp: also prints manual aws organizations commands for non-km recovery"
  - "fakeUninitOrgsAPI defined in uninit_test.go (not a separate file) to keep test doubles co-located"

patterns-established:
  - "Optional cleanup steps gated on explicit --include-* flags with warn-and-skip when not set"
  - "OrgsClient nil-safety: always check before calling, print actionable WARN with manual recovery command"

requirements-completed:
  - UNINIT-DETACHES-SCP

duration: 3min
completed: 2026-05-19
---

# Phase 84.4.1.1 Plan 05: km uninit --include-scp SCP Detach Step Summary

**km uninit --include-scp detaches and deletes the {prefix}-sandbox-containment SCP via Organizations API (ListPoliciesForTarget → DetachPolicy → DeletePolicy) before the module destroy loop, with warn-and-continue on failures and a manual recovery WARN when the flag is omitted**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-19T04:24:01Z
- **Completed:** 2026-05-19T04:27:31Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Implemented SCP detach step (Step 2.5) in `RunUninitWithDeps` — inserted after active-sandbox check, before module destroy loop, using the injected `UninitOrgsAPI` interface from Plan 00
- Wired real Organizations client in `runUninit` using the `klanker-terraform` profile + `AssumeRole` into `{prefix}-org-admin` in the organization account (mirrors `doctor.go:2900` pattern exactly)
- Added `--include-scp` cobra flag to `NewUninitCmd`; `TestRunUninit_DetachesSCPWhenFlagSet` converted from `t.Skip` to 4 GREEN sub-tests (A: detach+delete called, B: WARN emitted when flag not set, C: nil client does not panic, D: detach error skips delete)

## Task Commits

1. **Task 1: Implement SCP detach step + cobra flag + TestRunUninit_DetachesSCPWhenFlagSet GREEN** - `0e7a262` (feat)

**Plan metadata:** (docs commit follows)

_Note: TDD task — RED and GREEN committed together as one atomic feat commit._

## Files Created/Modified

- `internal/app/cmd/uninit.go` — Added `awssdk`, `awsconfig`, `organizationstypes`, `sts` imports; `--include-scp` cobra flag; SCP cleanup logic in `RunUninitWithDeps` Step 2.5; Organizations client construction in `runUninit`
- `internal/app/cmd/uninit_test.go` — Added `fakeUninitOrgsAPI` test double; replaced `t.Skip` with full `TestRunUninit_DetachesSCPWhenFlagSet` (4 sub-tests A/B/C/D); added `organizations`, `organizationstypes`, `io`, `os` imports

## Decisions Made

- SCP cleanup at Step 2.5 (before module destroy, after active-sandbox check) per plan spec — ensures SCP is detached before regional modules are torn down
- Organizations client construction uses the same `klanker-terraform` profile + AssumeRole pattern as `doctor.go:2900` for consistency; falls back to current profile creds if AssumeRole fails
- Detach-then-delete ordering enforced: `DeletePolicy` only called after `DetachPolicy` succeeds (test D verifies this)
- `--include-scp` defaults to `false` — operators who want to retain the SCP during partial teardowns won't accidentally remove it

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 (`TestRunUninitWithDeps_ActiveSandboxCheck`) can proceed — the active-sandbox check region of `RunUninitWithDeps` (lines ~241-261) was not refactored; its surrounding control flow is stable for Plan 06's structured-logging instrumentation
- The `t.Skip` for `TestRunUninitWithDeps_ActiveSandboxCheck` remains in place as the Plan 06 scaffold

---
*Phase: 84.4.1.1-multi-install-follow-on-gaps*
*Completed: 2026-05-19*
