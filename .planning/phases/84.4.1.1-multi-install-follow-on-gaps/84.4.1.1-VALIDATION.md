---
phase: 84.4.1.1
slug: multi-install-follow-on-gaps
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 84.4.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go stdlib) |
| **Config file** | none — go.mod at repo root |
| **Quick run command** | `go test ./internal/app/cmd/... ./internal/app/config/...` |
| **Full suite command** | `make test` (excludes pre-existing TestUnlockCmd_RequiresStateBucket per Makefile:75) |
| **Estimated runtime** | ~20s quick, ~90s full |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/app/cmd/...` (quick — touches modified package)
- **After every plan wave:** Run `make test` (full)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

Tasks below are placeholders — final IDs and counts assigned by gsd-planner. The map establishes the verification shape per gap.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 84.4.1.1-00-01 | 00 | 0 | (scaffolding) | unit-stub | `go test ./internal/app/cmd/... ./internal/app/config/... -run TestPhase8441P1Scaffold` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-01-01 | 01 | 1 | INIT-PLAN-BUILDS-LAMBDAS | unit | `go test ./internal/app/cmd/... -run TestRunInitPlan_BuildsLambdaZips` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-02-01 | 02 | 1 | CONFIGURE-DERIVES-ARTIFACTS-BUCKET | unit | `go test ./internal/app/cmd/... -run TestConfigure_ArtifactsBucketDerivedDefault` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-02-02 | 02 | 1 | CONFIG-LOAD-VALIDATES-ARTIFACTS-BUCKET | unit | `go test ./internal/app/config/... -run TestConfigLoad_RejectsNonCanonicalBucket` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-03-01 | 03 | 2 | VALIDATE-CANONICAL-BUCKET-SHAPE | unit (table) | `go test ./internal/app/cmd/... -run TestValidateArtifactsBucket_CanonicalShape` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-04-01 | 04 | 2 | DOCTOR-WARNS-ON-ORPHAN-SCPS | unit | `go test ./internal/app/cmd/... -run TestDoctor_WarnsOnOrphanSCPs` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-05-01 | 05 | 2 | UNINIT-DETACHES-SCP | unit | `go test ./internal/app/cmd/... -run TestRunUninit_DetachesSCPWhenFlagSet` | ❌ W0 creates | ⬜ pending |
| 84.4.1.1-06-* | 06 | 3 | UNINIT-IN-USE-DETECTION | investigation | `go test ./internal/app/cmd/... -run TestUninit_InUseDetection` (added if repro succeeds) | ❌ depends on repro | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

All scaffolding for Wave 1+ tests must compile (`go build ./...` green) with t.Skip stubs referencing implementation plans.

- [ ] `internal/app/cmd/init_test.go` — extend with `TestRunInitPlan_BuildsLambdaZips` (t.Skip → plan 01)
- [ ] `internal/app/cmd/configure_84_3_test.go` — extend with `TestConfigure_ArtifactsBucketDerivedDefault` (t.Skip → plan 02)
- [ ] `internal/app/config/config_test.go` — extend with `TestConfigLoad_RejectsPlaceholderArtifactsBucket` (t.Skip → plan 02)
- [ ] `internal/app/config/config.go` — add exported `ValidateArtifactsBucket` skeleton mirroring existing `GetSandboxSessionDocumentName` package-location pattern (resolves planner-identified import-cycle risk)
- [ ] `internal/app/config/config_test.go` — table-driven `TestValidateArtifactsBucket_CanonicalShape` (t.Skip → plan 03)
- [ ] `internal/app/cmd/doctor_test.go` — extend with `TestDoctor_WarnsOnOrphanSCPs` (t.Skip → plan 04)
- [ ] `internal/app/cmd/uninit.go` — add `UninitOrgsAPI` interface stub + `UninitOpts` struct (no callsites yet — interface-first contract) for plan 05 (note: planner correctly placed this on `uninit.go` not `unbootstrap.go` — `km uninit --include-scp` is the operator-facing flag)
- [ ] `internal/app/cmd/uninit_test.go` — extend with `TestRunUninit_DetachesSCPWhenFlagSet` (t.Skip → plan 05)

*Interface-first scaffolding pattern (mirrors Phase 84.4.1 Wave 0 plan 00 — `UnbootstrapDynamoDBAPI`) keeps Wave 1 work as pure RED→GREEN cycles.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live SCP detach via `km uninit --include-scp` on real AWS account | UNINIT-DETACHES-SCP (E2E) | Requires `klanker-terraform` SSO + `km-org-admin` chain-assume; not safe to mock for trust verification | Operator runs `km uninit --include-scp --dry-run=true` then `--dry-run=false` against a real install; verifies the SCP is detached + deleted via `aws organizations list-policies-for-target` |
| `km init --plan` on a true fresh clone (no `build/`) | INIT-PLAN-BUILDS-LAMBDAS (smoke) | Test fixtures can't easily simulate "no build/ dir + AWS provider plan that calls filebase64sha256" | Operator runs `rm -rf build/ && ./bin/km init --plan` in a fresh checkout; verifies no `filebase64sha256` error |
| Gap #5 repro (suspected TTL/in-use bug during uninit) | UNINIT-IN-USE-DETECTION (investigation) | No concrete symptom yet; operator must reproduce with structured logging enabled | Plan 06 emits the structured logging needed; operator runs uninit on installs with 0/1/N active sandboxes and shares logs |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (planner enforces)
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter (after Wave 0 lands)

**Approval:** pending
