---
phase: 84.4.1.1-multi-install-follow-on-gaps
plan: "01"
subsystem: infra
tags: [km-init, plan-flag, lambda-zips, terragrunt, fresh-clone]

# Dependency graph
requires:
  - phase: 84.4.1.1-00
    provides: t.Skip stub TestRunInitPlan_BuildsLambdaZips in init_test.go
provides:
  - BuildLambdaZipsFunc exported testable seam var in init.go
  - buildLambdaZips called in RunInitPlanWithRunner before module loop
  - TestRunInitPlan_BuildsLambdaZips GREEN (two sub-tests)
affects:
  - 84.4.1.1-02
  - 84.4.1.1-03
  - 84.4.1.1-04
  - 84.4.1.1-05
  - 84.4.1.1-06

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "exported package-level var func pointer as testable seam (BuildLambdaZipsFunc = buildLambdaZips)"
    - "warn-and-continue error handling: non-fatal build step prints [warn] and continues"

key-files:
  created: []
  modified:
    - internal/app/cmd/init.go
    - internal/app/cmd/init_test.go

key-decisions:
  - "Export buildLambdaZipsFunc as BuildLambdaZipsFunc so external cmd_test package can override it without internal test package refactor"
  - "Warn-and-continue semantics: build failure prints [warn] but does not abort km init --plan (mirrors runInit behavior at line 491-496)"

patterns-established:
  - "Testable seam for package-level functions: export a var func pointer (e.g. BuildLambdaZipsFunc = buildLambdaZips) so external test packages can override without changing package visibility"

requirements-completed:
  - INIT-PLAN-BUILDS-LAMBDAS

# Metrics
duration: 8min
completed: 2026-05-19
---

# Phase 84.4.1.1 Plan 01: buildLambdaZips wired into RunInitPlanWithRunner Summary

**`km init --plan` on a fresh clone no longer fails at filebase64sha256 — BuildLambdaZipsFunc exported seam added, call inserted before module loop with warn-and-continue semantics**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-19T00:00:00Z
- **Completed:** 2026-05-19T00:08:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Added `BuildLambdaZipsFunc` exported package-level var as testable seam (mirrors `RunInitPlanFunc` pattern)
- Inserted `BuildLambdaZipsFunc(repoRoot)` call in `RunInitPlanWithRunner` after `ensureRegionHCL`, before module loop — Gap #1 closed
- Replaced `t.Skip` stub in `TestRunInitPlan_BuildsLambdaZips` with two GREEN sub-tests: call-is-made and warn-on-failure

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire buildLambdaZips into RunInitPlanWithRunner and turn TestRunInitPlan_BuildsLambdaZips GREEN** - `a758293` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `internal/app/cmd/init.go` - Added `BuildLambdaZipsFunc` var (line ~104) and call in `RunInitPlanWithRunner` (warn-and-continue)
- `internal/app/cmd/init_test.go` - Replaced t.Skip stub with two sub-tests verifying call-is-made and warn-on-failure

## Decisions Made
- Exported `BuildLambdaZipsFunc` (capital B) rather than keeping it unexported, because `init_test.go` uses `package cmd_test` (external test package) and cannot access unexported package-level vars
- Kept the var name close to the function name (`BuildLambdaZipsFunc`) to match the existing pattern of `RunInitPlanFunc = runInitPlan`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Gap #1 (INIT-PLAN-BUILDS-LAMBDAS) fully closed
- Plan 02 (configure.go), Plan 03 (uninit), Plans 04-06 may proceed independently
- `BuildLambdaZipsFunc` is available for any future test that needs to mock the Lambda build step

---
*Phase: 84.4.1.1-multi-install-follow-on-gaps*
*Completed: 2026-05-19*
