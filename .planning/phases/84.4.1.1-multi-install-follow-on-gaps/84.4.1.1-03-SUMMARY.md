---
phase: 84.4.1.1-multi-install-follow-on-gaps
plan: "03"
subsystem: cli
tags: [validation, configure, regex, artifacts-bucket, gap-closure]

requires:
  - phase: 84.4.1.1-00
    provides: t.Skip stub for TestValidateArtifactsBucket_CanonicalShape in configure_84_3_test.go
  - phase: 84.4.1.1-02
    provides: deriveArtifactsBucket wired as prompt default in configure.go

provides:
  - cmdCanonicalBucketRE package-level var in configure.go enforcing ^[a-z][a-z0-9-]*-artifacts-[0-9]{12}$
  - validateArtifactsBucket strengthened with canonical regex check after fast-path checks
  - TestValidateArtifactsBucket_CanonicalShape table test GREEN with 8 cases

affects:
  - configure-interactive-flow
  - artifacts-bucket-validation
  - gap-4-canonical-bucket-shape

tech-stack:
  added: [regexp (stdlib)]
  patterns:
    - "Package-level regexp.MustCompile for compile-time literal — panics on startup for bad regex (acceptable)"
    - "Ordered validation: empty check → angle-bracket fast-path → sentinel fast-path → canonical regex"

key-files:
  created: []
  modified:
    - internal/app/cmd/configure.go
    - internal/app/cmd/configure_84_3_test.go

key-decisions:
  - "Keep sentinel fast-path (km-artifacts-12345) even though regex catches it — better error message mentioning km-config.example.yaml placeholder"
  - "cmdCanonicalBucketRE named with cmd- prefix to distinguish from canonicalBucketRE in config package"

patterns-established:
  - "validateArtifactsBucket: empty → angle-bracket → sentinel → regex — ordered from cheapest to most specific"

requirements-completed:
  - VALIDATE-CANONICAL-BUCKET-SHAPE

duration: 5min
completed: 2026-05-19
---

# Phase 84.4.1.1 Plan 03: Strengthen validateArtifactsBucket with canonical regex

**Gap #4 closed: validateArtifactsBucket in configure.go now enforces ^[a-z][a-z0-9-]*-artifacts-[0-9]{12}$, rejecting UAT-2 non-canonical names like tg-km-artifacts-use1-abcd0123**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-19T04:15:00Z
- **Completed:** 2026-05-19T04:20:00Z
- **Tasks:** 1 (TDD)
- **Files modified:** 2

## Accomplishments

- Added `cmdCanonicalBucketRE = regexp.MustCompile(...)` as a package-level var in configure.go with `regexp` import
- Extended `validateArtifactsBucket` with ordered validation: empty check → angle-bracket fast-path → sentinel fast-path → canonical regex check
- Replaced `t.Skip` stub with 8-case table-driven `TestValidateArtifactsBucket_CanonicalShape` — all cases GREEN
- Existing `TestConfigure_RejectsPlaceholder` continues to PASS unmodified

## Task Commits

1. **Task 1: Strengthen validateArtifactsBucket with canonical regex** - `e266118` (feat)

## Files Created/Modified

- `internal/app/cmd/configure.go` — Added `regexp` import, `cmdCanonicalBucketRE` var, extended `validateArtifactsBucket` with canonical regex check
- `internal/app/cmd/configure_84_3_test.go` — Replaced `t.Skip` stub with 8-case table test

## Decisions Made

- Keep the `km-artifacts-12345` sentinel fast-path despite the regex catching it — the fast-path produces a more specific error message mentioning "km-config.example.yaml placeholder" which helps operators understand the source of the bad value.
- Named the var `cmdCanonicalBucketRE` (with `cmd-` prefix) to distinguish from `canonicalBucketRE` in the config package, making the two parallel validators clearly separate.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. The `go build ./internal/app/cmd/` reported errors in `uninit.go` from parallel Plan 05 work (unused imports), but these are pre-existing in-progress changes outside this plan's scope. My configure.go changes compile correctly and all targeted tests pass.

## Self-Check

- `internal/app/cmd/configure.go` contains `cmdCanonicalBucketRE`: FOUND
- `internal/app/cmd/configure_84_3_test.go` contains `TestValidateArtifactsBucket_CanonicalShape` with 8 cases: FOUND
- Commit `e266118`: FOUND
- `TestValidateArtifactsBucket_CanonicalShape` — 8/8 cases PASS
- `TestConfigure_RejectsPlaceholder` — 6/6 cases PASS

## Self-Check: PASSED

## Next Phase Readiness

- Gap #4 (VALIDATE-CANONICAL-BUCKET-SHAPE) is fully closed for the configure-side validation path
- The companion config-package `ValidateArtifactsBucket` (Plan 02) and this cmd-package `validateArtifactsBucket` now both enforce the same canonical regex
- Ready for Plan 06+ to wire the config.Load() hard-fail path

---
*Phase: 84.4.1.1-multi-install-follow-on-gaps*
*Completed: 2026-05-19*
