---
phase: 84.4.1.1-multi-install-follow-on-gaps
verified: 2026-05-19T05:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 84.4.1.1: Multi-Install Follow-On Gaps Verification Report

**Phase Goal:** Close 5 post-UAT-2 multi-install correctness and DX gaps: Gap #1 buildLambdaZips in km init --plan, Gap #2 artifacts_bucket derive+validate, Gap #3a doctor orphan-SCP warn, Gap #3b km uninit --include-scp, Gap #4 canonical bucket regex, Gap #5 uninit TTL investigation.
**Verified:** 2026-05-19T05:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | RunInitPlanWithRunner calls BuildLambdaZipsFunc before the module loop | VERIFIED | `init.go:1222` calls `BuildLambdaZipsFunc(repoRoot)` before `modules := regionalModules(regionDir)` at line 1227; `TestRunInitPlan_BuildsLambdaZips` PASS (2 sub-tests) |
| 2 | km configure prompt shows derived default when artifacts_bucket is empty | VERIFIED | `configure.go:621-622` — `if artifactsBucket == "" && resourcePrefix != "" && applicationAcct != "" { artifactsBucket = deriveArtifactsBucket(...) }` before prompt; `TestConfigure_ArtifactsBucketDerivedDefault` PASS (2 sub-tests) |
| 3 | config.Load() rejects non-canonical artifacts_bucket before bootstrap | VERIFIED | `config.go:448-453` — validates `yamlDefaults["artifacts_bucket"]` via `ValidateArtifactsBucket`; `TestConfigLoad_RejectsNonCanonicalBucket` PASS; Plan 02 deviation confirmed by design (validates raw yaml value, not viper-merged, to preserve KM_ARTIFACTS_BUCKET env override asymmetry) |
| 4 | validateArtifactsBucket in configure.go enforces canonical regex | VERIFIED | `configure.go:23-27` declares `cmdCanonicalBucketRE`; `configure.go:228-252` strengthened function; `TestValidateArtifactsBucket_CanonicalShape` PASS (8 cases including UAT-2 bad input `tg-km-artifacts-use1-abcd0123`) |
| 5 | km doctor warns on orphan SCPs from other installs | VERIFIED | `doctor.go:130-138` `OrgsListAllPoliciesAPI` interface; `doctor.go:670-730` `checkOrphanSCPs` function; `doctor.go:2598-2608` wired into `buildChecks`; `TestDoctor_WarnsOnOrphanSCPs` PASS (5 sub-tests including pagination) |
| 6 | km uninit --include-scp detaches and deletes the install's SCP | VERIFIED | `uninit.go:183` cobra flag `--include-scp`; `uninit.go:340-390` SCP detach step (Step 2.5) with warn-and-continue; `TestRunUninit_DetachesSCPWhenFlagSet` PASS (4 sub-tests A/B/C/D covering detach+delete, skip-warn, nil-client, detach-error) |
| 7 | uninit active-sandbox check is instrumented and tested | VERIFIED | `uninit.go:297,318,328` three zerolog log.Info lines at Steps 1 and 2; `TestRunUninitWithDeps_ActiveSandboxCheck` PASS (4 sub-tests: 0-running, 1-running-same-region, 1-running-different-region, N-mixed-status) |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/app/cmd/init.go` | BuildLambdaZipsFunc var + call in RunInitPlanWithRunner before module loop | VERIFIED | Line 104: `var BuildLambdaZipsFunc = buildLambdaZips`; Line 1222: call before `regionalModules` |
| `internal/app/cmd/init_test.go` | TestRunInitPlan_BuildsLambdaZips GREEN (not SKIP) | VERIFIED | PASS — 2 sub-tests: build_func_is_called_before_module_loop, failed_build_does_not_abort_the_plan_run |
| `internal/app/cmd/configure.go` | derive-default wiring + cmdCanonicalBucketRE + strengthened validateArtifactsBucket | VERIFIED | Lines 481-482 and 621-622 for derive-default; Lines 23-27 for regex var; Lines 228-252 for strengthened function |
| `internal/app/cmd/configure_84_3_test.go` | TestConfigure_ArtifactsBucketDerivedDefault + TestValidateArtifactsBucket_CanonicalShape GREEN | VERIFIED | Both PASS — 2 and 8 sub-tests respectively |
| `internal/app/config/config.go` | canonicalBucketRE + ValidateArtifactsBucket + config.Load() wiring | VERIFIED | Lines 485-493 for regex; Lines 495-520 for exported function; Lines 439-454 for Load() wiring |
| `internal/app/config/load_validate_test.go` | TestConfigLoad_RejectsNonCanonicalBucket GREEN | VERIFIED | PASS |
| `internal/app/cmd/doctor.go` | OrgsListAllPoliciesAPI interface + OrgsListAllPoliciesClient field + checkOrphanSCPs + buildChecks wiring | VERIFIED | Lines 130-138, 262-264, 670-730, 2598-2608 |
| `internal/app/cmd/doctor_test.go` | TestDoctor_WarnsOnOrphanSCPs GREEN (4+ sub-tests) | VERIFIED | PASS — 5 sub-tests including pagination |
| `internal/app/cmd/uninit.go` | UninitOrgsAPI + UninitOpts + --include-scp flag + SCP detach step + zerolog logging | VERIFIED | Lines 76-91 (interfaces/structs), 150/183 (flag), 340-390 (SCP detach step 2.5), 297/318/328 (logging) |
| `internal/app/cmd/uninit_test.go` | TestRunUninit_DetachesSCPWhenFlagSet + TestRunUninitWithDeps_ActiveSandboxCheck GREEN; 15+ existing tests pass via UninitOpts | VERIFIED | Both PASS; all 16 existing TestUninit* tests PASS; 23 UninitOpts occurrences in test file |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `internal/app/cmd/init.go` | `buildLambdaZips` | `BuildLambdaZipsFunc` var + call in `RunInitPlanWithRunner` before `regionalModules()` | WIRED | Line 1222 confirmed before line 1227 |
| `internal/app/cmd/configure.go` | `deriveArtifactsBucket` | Conditional default-fill at lines 481-482 and 621-622 before prompt call | WIRED | Pattern `deriveArtifactsBucket(resourcePrefix, applicationAcct)` present at both configure entry points |
| `internal/app/config/config.go` | `ValidateArtifactsBucket` | `config.Load()` after populating from viper, validates yaml-authoritative value | WIRED | Lines 448-453; preserves KM_ARTIFACTS_BUCKET env override by reading yamlDefaults |
| `internal/app/cmd/configure.go` | `cmdCanonicalBucketRE` | Package-level var + `cmdCanonicalBucketRE.MatchString` inside `validateArtifactsBucket` | WIRED | Line 27 var, line 246 MatchString call |
| `internal/app/cmd/doctor.go` | `checkOrphanSCPs` | `buildChecks` closure after existing `checkSCP` closure, line 2598-2608 | WIRED | `orphanSCPClient` captured from `deps.OrgsListAllPoliciesClient`; function called with `localResourcePrefix` |
| `internal/app/cmd/doctor.go` | `OrgsListAllPoliciesAPI` | `DoctorDeps.OrgsListAllPoliciesClient` field; set in `initRealDepsWithExisting` at lines 3009-3017 | WIRED | Field definition at line 264; assigned at lines 3009, 3013, 3017 |
| `internal/app/cmd/uninit.go` | `UninitOrgsAPI.DetachPolicy` | `RunUninitWithDeps` body, `opts.IncludeSCP` gate (Step 2.5), before Step 3 module destroy | WIRED | Lines 340-390; detach order: ListPoliciesForTarget then DetachPolicy then DeletePolicy |
| `internal/app/cmd/uninit.go` | cobra `--include-scp` flag | `newUninitCmdWithIO` flag registration at line 183; passed via `UninitOpts{IncludeSCP: includeSCP}` at line 280 | WIRED | Flag registered at 183; passed at 280 |
| `internal/app/cmd/uninit_test.go` | `RunUninitWithDeps` | Calls use `UninitOpts` struct (not positional bool) | WIRED | 23 `UninitOpts` occurrences; all 16 existing `TestUninit*` tests pass |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INIT-PLAN-BUILDS-LAMBDAS | Plan 01 | buildLambdaZips called before module loop in RunInitPlanWithRunner | SATISFIED | `init.go:1222`; TestRunInitPlan_BuildsLambdaZips PASS |
| CONFIGURE-DERIVES-ARTIFACTS-BUCKET | Plan 02 | km configure prompt shows derived default when artifacts_bucket empty | SATISFIED | `configure.go:621-622`; TestConfigure_ArtifactsBucketDerivedDefault PASS |
| VALIDATE-BUCKET-ON-LOAD | Plan 02 | config.Load() rejects non-canonical buckets via ValidateArtifactsBucket | SATISFIED | `config.go:452`; TestConfigLoad_RejectsNonCanonicalBucket PASS |
| VALIDATE-CANONICAL-BUCKET-SHAPE | Plan 03 | validateArtifactsBucket enforces canonical regex; table test GREEN | SATISFIED | `configure.go:246`; TestValidateArtifactsBucket_CanonicalShape PASS (8 cases) |
| DOCTOR-WARNS-ON-ORPHAN-SCPS | Plan 04 | checkOrphanSCPs added; wired into buildChecks; TestDoctor_WarnsOnOrphanSCPs GREEN | SATISFIED | `doctor.go:677,2598`; TestDoctor_WarnsOnOrphanSCPs PASS (5 sub-tests) |
| UNINIT-DETACHES-SCP | Plan 05 | --include-scp flag; SCP detach step in RunUninitWithDeps; TestRunUninit_DetachesSCPWhenFlagSet GREEN | SATISFIED | `uninit.go:183,342`; TestRunUninit_DetachesSCPWhenFlagSet PASS (4 sub-tests) |
| UNINIT-TTL-INVESTIGATION | Plan 06 | Structured zerolog logging + TestRunUninitWithDeps_ActiveSandboxCheck GREEN | SATISFIED | `uninit.go:297,318,328`; TestRunUninitWithDeps_ActiveSandboxCheck PASS (4 sub-tests) |

Note: These requirement IDs are phase-local synthetic IDs (consistent with the Phase 84.3 pattern). They are declared in ROADMAP.md under the phase section but not yet recorded in REQUIREMENTS.md — this is expected; REQUIREMENTS.md is updated when the phase is closed.

### Anti-Patterns Found

None. No `t.Skip` stubs remain for the 7 new test functions. No `TODO`/`FIXME`/`return null`/placeholder patterns introduced. No empty implementations.

Note on pre-existing test failure: `TestUnlockCmd_RequiresStateBucket` in `internal/app/cmd/unlock_test.go` fails — this is documented as a pre-existing failure not introduced by this phase.

Note on full cmd package test timeout: Running `go test ./internal/app/cmd/` with a 120s timeout times out due to doctor and other tests that make real AWS network calls. This is pre-existing behavior unrelated to this phase.

### Human Verification Required

None — all automated checks passed.

### Gaps Summary

No gaps found. All 7 requirements are satisfied with GREEN tests and substantive implementations:

- Gap #1: `BuildLambdaZipsFunc` called before `regionalModules()` in `RunInitPlanWithRunner` — fresh-clone `km init --plan` no longer fails on missing `build/create-handler.zip`
- Gap #2: `km configure` prompt derives `{prefix}-artifacts-{accountID}` as default when empty; `config.Load()` rejects non-canonical values at startup (yaml-authoritative, env-override-safe)
- Gap #3a: `checkOrphanSCPs` in `doctor.go` detects `{prefix}-sandbox-containment` SCPs from other installs; wired into `buildChecks` as WARN-level check
- Gap #3b: `km uninit --include-scp` detaches+deletes SCP before module destroy; without flag, a WARN is printed; `UninitOpts` struct replaces positional `force bool`
- Gap #4: `cmdCanonicalBucketRE` (`^[a-z][a-z0-9-]*-artifacts-[0-9]{12}$`) enforces canonical shape in both `configure.go` and `config.go`; UAT-2 bad input `tg-km-artifacts-use1-abcd0123` now rejected
- Gap #5: Zerolog structured logging at Steps 1/2 of active-sandbox check; behavior documented — TTL-expired "running" sandboxes block uninit until auto-destroy Lambda runs (no bug in filter logic)

---

_Verified: 2026-05-19T05:00:00Z_
_Verifier: Claude (gsd-verifier)_
