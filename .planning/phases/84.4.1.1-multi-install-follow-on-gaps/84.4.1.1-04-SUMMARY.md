---
phase: 84.4.1.1-multi-install-follow-on-gaps
plan: "04"
subsystem: infra
tags: [aws-organizations, scp, doctor, multi-install, orphan-detection]

# Dependency graph
requires:
  - phase: 84.4.1.1-00
    provides: t.Skip scaffold for TestDoctor_WarnsOnOrphanSCPs in doctor_test.go
provides:
  - OrgsListAllPoliciesAPI interface in doctor.go
  - checkOrphanSCPs function: lists all SCPs, filters -sandbox-containment suffix, WARNs on orphan prefixes
  - DoctorDeps.OrgsListAllPoliciesClient field wired in initRealDepsWithExisting
  - TestDoctor_WarnsOnOrphanSCPs GREEN (5 sub-tests, including pagination)
affects: [km-doctor, multi-install, scp-enforcement, phase-84.4.1.1]

# Tech tracking
tech-stack:
  added: [organizations/types (organizationstypes) import in doctor.go]
  patterns: [narrow interface per check — OrgsListAllPoliciesAPI kept separate from OrgsListPoliciesAPI to preserve existing checkSCP interface]

key-files:
  created: []
  modified:
    - internal/app/cmd/doctor.go
    - internal/app/cmd/doctor_test.go

key-decisions:
  - "OrgsListAllPoliciesAPI interface kept separate from OrgsListPoliciesAPI to avoid widening the existing narrow interface for checkSCP"
  - "checkOrphanSCPs uses organizationstypes.PolicyTypeServiceControlPolicy enum value (not raw string) for ListPolicies filter"
  - "Pagination handled via NextToken loop — terminates on nil NextToken"
  - "AWS-managed SCPs (FullAWSAccess, etc.) filtered by -sandbox-containment suffix check — not by explicit allow-list"
  - "WARN level not ERROR — orphan SCPs are recoverable and should not block operator workflow"
  - "buildChecks wire: skip when !orgConfigured OR orphanSCPClient == nil (either condition is sufficient)"

patterns-established:
  - "Fake client pattern: hand-written struct with pages [][]types.PolicySummary for multi-page pagination testing"

requirements-completed: [DOCTOR-WARNS-ON-ORPHAN-SCPS]

# Metrics
duration: 4min
completed: 2026-05-19
---

# Phase 84.4.1.1 Plan 04: Orphan SCP Doctor Check Summary

**checkOrphanSCPs added to km doctor — lists all SCPs via Organizations.ListPolicies, WARNs on any -sandbox-containment SCPs whose prefix is not the local install's resource_prefix**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-19T04:26:06Z
- **Completed:** 2026-05-19T04:30:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Added `OrgsListAllPoliciesAPI` interface (ListPolicies + ListTargetsForPolicy) to doctor.go
- Implemented `checkOrphanSCPs` with paginated SCP listing, -sandbox-containment filter, and WARN with recovery command
- Added `OrgsListAllPoliciesClient` field to `DoctorDeps` and wired into `initRealDepsWithExisting`
- Wired `checkOrphanSCPs` into `buildChecks` after existing `checkSCP` closure
- Turned `TestDoctor_WarnsOnOrphanSCPs` t.Skip into 5 passing sub-tests (warn/ok-own/ok-aws-managed/skip-nil/pagination)

## Task Commits

1. **Task 1: Add OrgsListAllPoliciesAPI + checkOrphanSCPs + wire into buildChecks + TestDoctor_WarnsOnOrphanSCPs GREEN** - `c2610f2` (feat)

**Plan metadata:** (see final commit below)

## Files Created/Modified

- `internal/app/cmd/doctor.go` — Added `OrgsListAllPoliciesAPI` interface, `organizationstypes` import, `OrgsListAllPoliciesClient` field in `DoctorDeps`, `checkOrphanSCPs` function, buildChecks wiring, `initRealDepsWithExisting` wiring
- `internal/app/cmd/doctor_test.go` — Replaced t.Skip stub with `fakeOrgsListAllPoliciesClient` struct and full `TestDoctor_WarnsOnOrphanSCPs` with 5 sub-tests

## Decisions Made

- `OrgsListAllPoliciesAPI` kept separate from `OrgsListPoliciesAPI` to preserve narrow interface for existing `checkSCP` — avoids inadvertently widening the mock surface for tests that don't need ListPolicies
- Used `organizationstypes.PolicyTypeServiceControlPolicy` enum (not raw string `"SERVICE_CONTROL_POLICY"`) for type safety in the new code path
- `checkOrphanSCPs` WARN level (not ERROR) — orphan SCPs from a prior km uninit are recoverable; operator should clean up but it does not block the local install
- buildChecks wire: skip when `!orgConfigured` OR `orphanSCPClient == nil` — consistent with existing SCP check guard
- `fakeOrgsListAllPoliciesClient` uses `pages [][]orgtypes.PolicySummary` field for multi-page tests — allows testing pagination without network calls

## Deviations from Plan

None — plan executed exactly as written. Added a 5th pagination sub-test (test plan specified 4 sub-tests but pagination behavior was called out in the behavior spec and deserved its own case).

## Issues Encountered

None. Build and all tests passed on first attempt.

## Next Phase Readiness

- `km doctor` now detects orphan SCPs from prior multi-install uninit runs (Gap #3a closed)
- Plans 03 and 05 running in parallel; no file conflicts (this plan touched only doctor.go and doctor_test.go)
- Recovery command in WARN message uses `<management-account-id>` placeholder — operator must fill in actual account ID; this is intentional (doctor doesn't always have access to resolve the org account)

---
*Phase: 84.4.1.1-multi-install-follow-on-gaps*
*Completed: 2026-05-19*
