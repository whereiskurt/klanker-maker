---
phase: 33.1
slug: raw-ami-id-support
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-26
updated: 2026-04-26
---

# Phase 33.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (stdlib) |
| **Config file** | none — existing test infrastructure |
| **Quick run command** | `go test ./pkg/profile/... ./pkg/compiler/... -run "AMI\|RawAMI\|IsRawAMI" -count=1 -v` |
| **Full suite command** | `go test ./pkg/profile/... ./pkg/compiler/... -count=1` |
| **Estimated runtime** | ~5 seconds (quick) / ~20 seconds (full pkg/profile + pkg/compiler) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/profile/... ./pkg/compiler/... -run "AMI\|RawAMI\|IsRawAMI" -count=1`
- **After every plan wave:** Run `go test ./pkg/profile/... ./pkg/compiler/... -count=1`
- **Before `/gsd:verify-work`:** Full suite must be green (`go test ./... -count=1` from repo root)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 33.1-01-T1 | 33.1-01 | 1 | P33.1-01 | unit (schema) | `go test ./pkg/profile/... -run "TestSchemaAMI" -count=1 -v` | Yes (TestSchemaAMIParsing) — adds TestSchemaAMIRawIDValid + TestSchemaAMIRawIDInvalid in this task | pending |
| 33.1-01-T2 | 33.1-01 | 1 | P33.1-02, P33.1-06 | unit (helper + Wave 0 scaffold) | `go test ./pkg/compiler/... -run "TestIsRawAMIID" -count=1 -v` | No — created in this task; also creates two intentionally RED tests (`TestAMIRawIDInHCL`, `TestAMISlugInHCLEmitsEmptyAMIID`) that flip green in 33.1-02 | pending |
| 33.1-02-T1 | 33.1-02 | 2 | P33.1-02, P33.1-04, P33.1-06 | unit (HCL emission + warning) | `go test ./pkg/compiler/... -count=1` | Yes — flips Plan 01's two RED tests to GREEN; adds TestHibernationRawAMIWarning | pending |
| 33.1-02-T2 | 33.1-02 | 2 | P33.1-03, P33.1-05, P33.1-07 | structural (grep) + Go regression | `go test ./pkg/profile/... ./pkg/compiler/... -count=1 && grep -q 'variable "ami_id"' infra/modules/ec2spot/v1.0.0/variables.tf && grep -q 'effective_ami_id' infra/modules/ec2spot/v1.0.0/main.tf && grep -q 'ami = local.effective_ami_id' infra/modules/ec2spot/v1.0.0/main.tf` | No — Terraform changes; grep verifies structural rewiring | pending |

*Status: pending · green · red · flaky*

### Requirement → Test cross-reference

| Req ID | Covered By |
|--------|-----------|
| P33.1-01 | 33.1-01-T1 (TestSchemaAMIRawIDValid, TestSchemaAMIRawIDInvalid) |
| P33.1-02 | 33.1-01-T2 (TestIsRawAMIID — helper) + 33.1-02-T1 (TestAMIRawIDInHCL, TestAMISlugInHCLEmitsEmptyAMIID — wiring) |
| P33.1-03 | 33.1-02-T2 (Terraform structural greps + module shape) |
| P33.1-04 | 33.1-02-T1 (TestHibernationRawAMIWarning — warning, not error) |
| P33.1-05 | 33.1-02-T2 + every task's full-suite tail (`go test ./pkg/profile/... ./pkg/compiler/... -count=1`) — all 21 Phase 33 tests stay green |
| P33.1-06 | 33.1-01-T2 (Wave 0 scaffold) + 33.1-02-T1 (flip green) |
| P33.1-07 | 33.1-02-T2 (end-to-end dataflow: profile YAML → HCL → Terraform var → effective_ami_id → instance.ami) |

---

## Wave 0 Requirements

- [x] `pkg/profile/schema_storage_test.go` — add `TestSchemaAMIRawIDValid` + `TestSchemaAMIRawIDInvalid` (covers P33.1-01) — assigned to 33.1-01-T1
- [x] `pkg/compiler/ec2_storage_test.go` — add `TestIsRawAMIID` (covers P33.1-02 helper) — assigned to 33.1-01-T2
- [x] `pkg/compiler/ec2_storage_test.go` — add `TestAMIRawIDInHCL` + `TestAMISlugInHCLEmitsEmptyAMIID` (intentionally RED in Plan 01, GREEN in Plan 02 — covers P33.1-02 wiring + P33.1-06) — assigned to 33.1-01-T2 (red) and 33.1-02-T1 (flip green)
- [x] No new test infrastructure needed — existing `minimalEC2StorageProfile()` and `minimalEC2StorageNetwork()` helpers (pkg/compiler/ec2_storage_test.go lines 11, 26) are reusable; existing YAML fixture pattern in TestSchemaAMIParsing is reusable.
- [x] Terraform module changes (33.1-02-T2) verified via `terraform fmt -check` (manual fallback if terraform CLI not in shell) + structural grep + Go regression suite. No new Terraform test framework needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Raw AMI ID provisions instance from snapshot | P33.1-07 (live confirmation) | Requires real AWS account with a private AMI | After `make build` and `km init --sidecars`, run `km create profiles/<test>.yaml` with `spec.runtime.ami: ami-xxxxxxxx` (a real account-owned AMI in `spec.runtime.region`). Verify EC2 instance launches from that exact AMI: `aws ec2 describe-instances --filters Name=tag:km:sandbox-id,Values=<id> --query 'Reservations[].Instances[].ImageId'` — must equal the raw ID specified in profile. |
| Cross-region raw AMI surfaces clean error | P33.1-04 (region scoping) | Requires live Terraform plan/apply against multi-region setup | Set `ami: ami-xxx` (from us-east-1) + `region: us-west-2` in profile, run `km create`. Verify error message references AMI not found in region (Terraform surfaces InvalidAMIID.NotFound). Phase 33.1 does NOT pre-validate this (Phase 56 owns region alignment). |
| Hibernation + raw AMI with encrypted root succeeds | P33.1-04 (warning path) | Requires real account-owned AMI with encrypted root | Profile with `hibernation: true, spot: false, ami: ami-xxx` (where AMI has encrypted root). Verify `km validate` warns but does not error; `km create` succeeds; instance shows hibernation enabled. |
| Hibernation + raw AMI with unencrypted root fails clearly at apply | P33.1-04 (warning path) | Requires real unencrypted AMI | Same as above but with unencrypted-root AMI. Verify `km validate` warns; `km create` proceeds to terraform apply which fails with `InvalidParameterCombination: Hibernation is not supported for instances with unencrypted root EBS volumes` — operator can read the warning + apply error to diagnose. |
| Lambda picks up new schema | platform | Schema changes only ship to remote create via `km init --sidecars` | After Phase 33.1 merges, operator runs `km init --sidecars`, then attempts `km create` with a profile containing `ami: ami-xxx`. Without the init refresh, the Lambda's bundled km binary still has the old enum-only schema and rejects the profile (per memory `project_schema_change_requires_km_init.md`). |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: every task has an automated command; no 3-task gap (only 4 tasks total, all sampled)
- [x] Wave 0 covers all MISSING references — TestAMIRawIDInHCL and TestAMISlugInHCLEmitsEmptyAMIID created in 33.1-01-T2 as intentional reds, flip green in 33.1-02-T1
- [x] No watch-mode flags — all `go test -count=1` (no caching, no `-watch`)
- [x] Feedback latency < 30s — full pkg/profile+pkg/compiler suite is ~20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready for execution
