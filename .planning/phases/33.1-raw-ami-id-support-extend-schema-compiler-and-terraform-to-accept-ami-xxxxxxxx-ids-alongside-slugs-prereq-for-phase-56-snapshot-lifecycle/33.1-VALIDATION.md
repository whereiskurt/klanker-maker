---
phase: 33.1
slug: raw-ami-id-support
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 33.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (stdlib) |
| **Config file** | none — existing test infrastructure |
| **Quick run command** | `go test ./pkg/profile/... ./pkg/compiler/... -run AMI -count=1` |
| **Full suite command** | `make test` (or `go test ./... -count=1`) |
| **Estimated runtime** | ~30 seconds (quick) / ~2 min (full) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/profile/... ./pkg/compiler/... -run AMI -count=1`
- **After every plan wave:** Run `make test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | TBD         | TBD       | TBD               | TBD         | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Filled in by gsd-planner during plan generation.*

---

## Wave 0 Requirements

- [ ] None expected — existing `pkg/profile/schema_storage_test.go` and `pkg/compiler/ec2_storage_test.go` infrastructure covers Phase 33.1 additions.
- [ ] Verify Terraform plan validation infrastructure exists (or add Wave 0 stub if not).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Raw AMI ID provisions instance from snapshot | TBD | Requires real AWS account with a private AMI | `km create` with `ami: ami-xxxxxxxx` (a real account-owned AMI), verify EC2 instance launches from that image |
| Cross-region raw AMI rejected with clear error | TBD | Live Terraform plan against multi-region setup | Set `ami: ami-xxx` from us-east-1 + region us-west-2, verify clean error message |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
