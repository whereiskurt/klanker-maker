---
phase: 123-klankermaker-setup-wizard-interview-and-emit-webapp
plan: 02
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - web/wizard/index.html
  - web/wizard/vite.config.ts
  - web/wizard/src/ui/App.*
  - web/wizard/src/ui/Question.*
  - web/wizard/src/ui/PresetPicker.*
  - web/wizard/src/ui/CapabilityChecklist.*
  - web/wizard/src/ui/BundlePreview.*
  - web/wizard/src/ui/download.ts
  - web/wizard/test/ui.smoke.test.ts
autonomous: false
requirements: [REQ-123-RENDER]
must_haves:
  truths:
    - "The renderer is a GENERIC walker over the Wave-1 question-graph — it contains no hard-coded question list; adding a capability descriptor in graph.ts surfaces in the UI with no renderer change"
    - "Selecting a preset pre-answers the routing questions; Custom exposes the full foundation + capability checklist"
    - "All input is validated CLIENT-SIDE before the bundle can be generated (account-ID 12-digit, region, domain syntax, resource_prefix charset, SSO start-URL)"
    - "The bundle is generated 100% in-browser by calling the Wave-1 emitter; NOTHING is transmitted (no fetch/XHR to any backend); the page works offline once loaded"
    - "The user can preview every emitted file (km-config.yaml, aws-config snippet, each script part, README) before downloading, and download as individual files or a zip"
    - "The UI is responsive, keyboard-accessible, and visually polished (frontend-design pass), reflecting the gate structure (shows which ✋ pauses their path will hit)"
  artifacts:
    - path: "web/wizard/src/ui/App.*"
      provides: "graph-driven interview shell: preset pick -> routing -> capability checklist -> per-capability questions -> preview/download"
      contains: "emit"
    - path: "web/wizard/src/ui/download.ts"
      provides: "in-browser bundle download (individual files + zip), no network"
      contains: "download"
    - path: "web/wizard/test/ui.smoke.test.ts"
      provides: "headless walk of each preset asserting a complete bundle is produced with the expected file set"
      contains: "Quick start"
  key_links:
    - from: "web/wizard/src/ui/App.*"
      to: "web/wizard/src/emit/emitter.ts"
      via: "the UI calls the pure Wave-1 emitter; it never re-implements emission"
      pattern: "emit("
    - from: "web/wizard/src/ui/App.*"
      to: "web/wizard/src/graph/graph.ts"
      via: "UI renders nodes from the graph data; no hard-coded questions"
      pattern: "graph"
---

<objective>
Wave 2: the pretty, graph-driven static renderer. Turn the Wave-1 pure core into
the public-facing klankermaker.ai wizard — a generic walker over the question-graph
that branches by preset/answers, validates input client-side, and generates the
downloadable bundle 100% in-browser by calling the Wave-1 emitter. No backend, no
network, nothing transmitted.

Purpose: this is the visible front door. It must be dependency-light and look
genuinely good (frontend-design pass) while remaining a thin shell over Wave 1 —
the renderer holds NO km knowledge of its own.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
Design spec: @docs/superpowers/specs/2026-06-27-klankermaker-setup-wizard-design.md
Use the frontend-design skill for the visual pass.
</execution_context>

<context>
Depends on Wave 1's locked graph + emitter. Framework is a deliberately-deferred
implementation detail (design §13): keep it lightweight — recommend Vite + Svelte
(or vanilla TS) — final call made during this wave via the frontend-design skill.
The renderer MUST stay generic: it reads graph.ts and presets.ts; it must not
duplicate the question list. The trust story (no transmission, source-visible) is
load-bearing — do not introduce any analytics/telemetry/fetch.
</context>

<task name="Vite + chosen-framework scaffold over the Wave-1 package">
Add `index.html`, `vite.config.ts`, and the UI entry. Keep deps minimal; no
backend, no analytics. Build target is a static bundle.
<verify>cd web/wizard && npx vite build && test -d dist && echo BUILD_OK</verify>
</task>

<task name="Graph-driven interview shell (preset -> routing -> capabilities -> questions)" requirements="REQ-123-RENDER">
Implement App + Question + PresetPicker + CapabilityChecklist rendering purely
from graph.ts/presets.ts. Preset selection pre-answers routing; Custom exposes
the full set. Show the gate/pause structure for the chosen path.
<verify>cd web/wizard && npx vitest run -t "renderer is generic over the graph (no hard-coded questions)" && echo GENERIC_OK</verify>
</task>

<task name="Client-side validation + in-browser bundle generation + preview/download" requirements="REQ-123-RENDER">
Wire client-side validators; call the Wave-1 emitter to build the Bundle in-browser;
BundlePreview shows every file; download.ts offers per-file + zip download. Assert
no network calls occur.
<verify>cd web/wizard && npx vitest run -t "emits a complete bundle with zero network calls" && echo NONET_OK</verify>
</task>

<task name="Frontend-design pass (responsive, accessible, polished)">
Apply the frontend-design skill: layout, typography, the gate/pause visualization,
responsive + keyboard-accessible. Avoid generic AI aesthetics.
<verify>cd web/wizard && npx vite build && npx vitest run web/wizard/test/ui.smoke.test.ts && echo SMOKE_OK</verify>
</task>

<task name="Wave-2 summary">
Write `123-02-SUMMARY.md`: framework chosen + why, how the generic-walker invariant
is enforced, the trust/no-transmission guarantees, handoff to Wave 4 (hosting).
<verify>test -f .planning/phases/123-klankermaker-setup-wizard-interview-and-emit-webapp/123-02-SUMMARY.md && echo SUMMARY_OK</verify>
</task>
