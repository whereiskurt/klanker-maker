---
phase: 123-klankermaker-setup-wizard-interview-and-emit-webapp
plan: 04
type: execute
wave: 4
depends_on: [123-02, 123-03]
files_modified:
  - web/wizard/src/emit/release.ts
  - infra/modules/static-site/v1.0.0/  (or equivalent hosting)
  - .github/workflows/wizard-deploy.yml
  - web/wizard/README.md
autonomous: false
requirements: [REQ-123-HOST]
must_haves:
  truths:
    - "The emitted part-1 script fetches a PINNED km release by version and verifies its SHA256 against a known checksum before executing anything (no moving 'latest', no unverified download)"
    - "klankermaker.ai serves the built static wizard over HTTPS via S3 + CloudFront (ACM cert), with the SPA/bundle cached and source viewable"
    - "Deploy is reproducible from CI: build the static bundle, publish to the bucket, invalidate CloudFront"
    - "Hosting is isolated from sandbox infrastructure — it is the public marketing/onboarding site, not part of any install's regional resources"
  artifacts:
    - path: "web/wizard/src/emit/release.ts"
      provides: "pinned km version + checksum injected into the emitted runbook"
      contains: "sha256"
    - path: "infra/modules/static-site/v1.0.0/"
      provides: "S3 + CloudFront + ACM static hosting for klankermaker.ai"
      contains: "cloudfront"
    - path: ".github/workflows/wizard-deploy.yml"
      provides: "CI build + publish + CloudFront invalidation"
      contains: "invalidation"
  key_links:
    - from: "web/wizard/src/emit/release.ts"
      to: "web/wizard/src/emit/runbook.ts"
      via: "runbook part 1 embeds the pinned version + checksum from release.ts"
      pattern: "sha256"
---

<objective>
Wave 4: ship klankermaker.ai. Host the built static wizard (S3 + CloudFront +
ACM over HTTPS), wire a reproducible CI deploy, and finalize the emitted runbook's
pinned-km fetch — part 1 downloads a specific km release version and verifies its
SHA256 before doing anything.

Purpose: make the front door public and the generated runbook trustworthy and
reproducible (no moving 'latest', no unverified binary).
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
Design spec: @docs/superpowers/specs/2026-06-27-klankermaker-setup-wizard-design.md §§6,11,13
</execution_context>

<context>
Depends on Wave 2 (a built bundle to host) and Wave 3 (the contract guarantee).
Hosting is deliberately SEPARATE from sandbox/regional infra — klankermaker.ai is
the public onboarding site. Reuse the repo's terragrunt patterns (provider stanza
in root.hcl; no required_providers in the module — see project conventions) if the
hosting module is terragrunt-managed. The pinned km version + checksum come from
the goreleaser release artifacts (the four tarballs + SHA256SUMS).
</context>

<task name="Pin km release + checksum into the emitted runbook" requirements="REQ-123-HOST">
Implement release.ts (the km version the wizard targets + its SHA256), and have
runbook.ts part-1 emit a fetch-then-verify step that aborts on checksum mismatch.
Update the Wave-1 golden bundles accordingly.
<verify>cd web/wizard && npx vitest run -t "part 1 fetches a pinned km and verifies sha256" && echo PIN_OK</verify>
</task>

<task name="Static hosting module for klankermaker.ai (S3 + CloudFront + ACM)" requirements="REQ-123-HOST">
Add the hosting (S3 private bucket + CloudFront OAC + ACM cert + DNS). Keep it
isolated from install regional infra. Document the one-time DNS/cert setup.
<verify>cd infra/modules/static-site/v1.0.0 && terraform init -backend=false && terraform validate && echo TF_OK</verify>
</task>

<task name="CI deploy workflow (build -> publish -> invalidate)" requirements="REQ-123-HOST">
`.github/workflows/wizard-deploy.yml`: build the static bundle, sync to the bucket,
invalidate CloudFront. Gated to main / tagged deploys.
<verify>grep -q 'create-invalidation' .github/workflows/wizard-deploy.yml && echo DEPLOY_WIRED</verify>
</task>

<task name="Wave-4 summary + phase wrap">
Write `123-04-SUMMARY.md`: hosting topology, the pinned-km trust model, and the
phase-123 close-out (A complete; B/C/D remain as future sub-projects with this
bundle format as their contract).
<verify>test -f .planning/phases/123-klankermaker-setup-wizard-interview-and-emit-webapp/123-04-SUMMARY.md && echo SUMMARY_OK</verify>
</task>
