---
phase: 66-multi-instance-support-configurable-resource-prefix-and-email-subdomain
plan: 03
type: execute
wave: 2
depends_on:
  - 66-01
files_modified:
  - internal/app/cmd/create.go
  - internal/app/cmd/destroy.go
  - internal/app/cmd/resume.go
  - internal/app/cmd/shell.go
  - internal/app/cmd/extend.go
  - internal/app/cmd/list.go
  - internal/app/cmd/unlock.go
  - internal/app/cmd/lock.go
  - internal/app/cmd/stop.go
  - internal/app/cmd/budget.go
  - internal/app/cmd/ami.go
  - internal/app/cmd/init.go
  - internal/app/cmd/doctor.go
  - internal/app/cmd/at.go
  - internal/app/cmd/configure.go
  - internal/app/cmd/configure_github.go
  - internal/app/cmd/create_slack.go
  - internal/app/cmd/slack.go
  - internal/app/cmd/doctor_slack.go
  - internal/app/cmd/logs.go
  - internal/app/cmd/status.go
  - cmd/ttl-handler/main.go
  - cmd/create-handler/main.go
  - cmd/email-create-handler/main.go
  - cmd/budget-enforcer/main.go
  - cmd/configui/main.go
  - cmd/configui/handlers.go
  - cmd/configui/handlers_secrets.go
  - cmd/github-token-refresher/main.go
  - cmd/km-slack-bridge/main.go
  - pkg/aws/cloudwatch.go
  - pkg/aws/scheduler.go
  - pkg/compiler/lifecycle.go
  - pkg/compiler/service_hcl.go
autonomous: true
requirements:
  - REQ-PLATFORM-MULTI-INSTANCE

must_haves:
  truths:
    - "Operator-side cmd code references resource names via cfg.GetResourcePrefix() and SSM paths via cfg.GetSsmPrefix() — zero hardcoded km-* singleton names or /km/ paths outside _test.go fixtures"
    - "Lambda handlers read every singleton resource name from env vars with safe km-*//km/* fallbacks (no operator config in Lambda runtime)"
    - "ttl-handler reads KM_TTL_HANDLER_NAME, KM_TTL_SCHEDULER_ROLE, KM_AT_GROUP_NAME from env (Pitfall 4 fix)"
    - "configui kmPrefix becomes a runtime variable from KM_RESOURCE_PREFIX (Pitfall 5 fix)"
    - "make build succeeds; full test suite (./internal/... ./pkg/... ./cmd/...) green"
    - "Grep audit: zero singleton km- resource literals remain in ./cmd ./internal ./pkg outside _test.go"
    - "Grep audit: zero /km/ literals remain in ./cmd ./internal ./pkg outside _test.go and outside specific filesystem paths (/opt/km/, ~/.km/)"
  artifacts:
    - path: "cmd/ttl-handler/main.go"
      provides: "All hardcoded km-* resource names replaced with env-var reads"
      contains: "KM_TTL_HANDLER_NAME"
    - path: "cmd/configui/handlers_secrets.go"
      provides: "kmPrefix changed from const to runtime variable from KM_RESOURCE_PREFIX env"
      contains: "KM_RESOURCE_PREFIX"
    - path: "internal/app/cmd/init.go"
      provides: "tf-{prefix}-state-{region} bucket name uses cfg.GetResourcePrefix()"
      contains: "GetResourcePrefix"
    - path: "internal/app/cmd/at.go"
      provides: "Schedule group name {prefix}-at via cfg.GetResourcePrefix()"
      contains: "GetResourcePrefix"
  key_links:
    - from: "operator-side cmd/*.go"
      to: "cfg.GetResourcePrefix() / cfg.GetSsmPrefix()"
      via: "config helper method"
      pattern: "GetResourcePrefix|GetSsmPrefix"
    - from: "Lambda handlers (cmd/*/main.go)"
      to: "process env vars (KM_SANDBOX_TABLE_NAME, KM_BUDGET_TABLE, KM_IDENTITIES_TABLE, KM_TTL_HANDLER_NAME, KM_TTL_SCHEDULER_ROLE, KM_AT_GROUP_NAME, KM_SCHEDULES_TABLE, KM_NONCE_TABLE, KM_BOT_TOKEN_PATH, KM_GITHUB_SSM_CONFIG_PREFIX)"
      via: "os.Getenv with hardcoded fallback"
      pattern: "os\\.Getenv\\(\"KM_"
    - from: "configui kmPrefix runtime"
      to: "KM_RESOURCE_PREFIX env var"
      via: "init() reads env once at startup"
      pattern: "KM_RESOURCE_PREFIX"
---

<objective>
Migrate the resource-name and SSM-path hardcoding sweep — Categories B (75+ SSM paths) and C (100+ resource names) from RESEARCH.md. Operator-side code calls `cfg.GetResourcePrefix()` and `cfg.GetSsmPrefix()`. Lambda handlers (which run without operator config) read env vars with safe fallbacks; the env vars themselves are wired up by plan 04's TF module work. Critically, this plan also fixes Pitfall 4 (ttl-handler hardcodes its own function name) and Pitfall 5 (configui's `const kmPrefix = "/km/"`).

Purpose: This is the largest migration plan in the phase — touches ~30 files across operator-side cmd, Lambda handlers, configui, and pkg/. Plan 02 ran in parallel (different call-site domain — email vs. resources) so Wave 2 finishes both.

Output: ~30 files updated; two grep-audit gates green; full test suite green; `make build` succeeds.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-RESEARCH.md
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-VALIDATION.md
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-01-SUMMARY.md
@internal/app/cmd/create.go
@internal/app/cmd/destroy.go
@internal/app/cmd/init.go
@internal/app/cmd/at.go
@cmd/ttl-handler/main.go
@cmd/configui/handlers_secrets.go

<interfaces>
Helper methods (added by 66-01):

```go
func (c *Config) GetResourcePrefix() string  // "km" by default
func (c *Config) GetSsmPrefix() string       // "/km/" by default
```

Lambda env-var convention (set in plan 04 by TF modules; this plan adds the read code):

| Env Var | Lambda(s) | Fallback | What it names |
|---------|-----------|----------|---------------|
| KM_SANDBOX_TABLE_NAME | ttl-handler, create-handler, email-create-handler | km-sandboxes | DynamoDB sandboxes table |
| KM_BUDGET_TABLE | budget-enforcer, ttl-handler, configui | km-budgets | DynamoDB budgets table |
| KM_IDENTITIES_TABLE | km-slack-bridge, create-handler | km-identities | DynamoDB identities table |
| KM_SCHEDULES_TABLE | ttl-handler | km-schedules | DynamoDB schedules table |
| KM_NONCE_TABLE | km-slack-bridge | km-slack-bridge-nonces | DynamoDB Slack nonce table |
| KM_TTL_HANDLER_NAME | ttl-handler (self) | km-ttl-handler | Own Lambda function name (Pitfall 4) |
| KM_TTL_SCHEDULER_ROLE | ttl-handler | km-ttl-scheduler | EventBridge scheduler role |
| KM_AT_GROUP_NAME | ttl-handler | km-at | EventBridge schedule group |
| KM_BOT_TOKEN_PATH | km-slack-bridge | /km/slack/bot-token | SSM bot token path |
| KM_GITHUB_SSM_CONFIG_PREFIX | github-token-refresher | /km/config/github | SSM github config prefix |
| KM_RESOURCE_PREFIX | configui (only) | km | For runtime kmPrefix var |

Per-sandbox names (DO NOT change — already collision-free):
km-budget-enforcer-{sandboxID}, km-github-token-refresher-{sandboxID}, km-ec2spot-ssm-{sandboxID}-{region}, km-ec2spot-profile-{sandboxID}-{region}, km-docker-{sandboxID}-{region}, km-sidecar-{sandboxID}-{region}, km-ttl-{sandboxID}, km-budget-{sandboxID}, km-github-token-{sandboxID}.

Lambda env-var helper pattern (use this everywhere):

```go
func sandboxTableName() string {
    if v := os.Getenv("KM_SANDBOX_TABLE_NAME"); v != "" {
        return v
    }
    return "km-sandboxes"
}
```

TF state prefix transformations:
- internal/app/cmd/init.go L832: `fmt.Sprintf("tf-km-state-%s", regionLabel)` becomes `fmt.Sprintf("tf-%s-state-%s", cfg.GetResourcePrefix(), regionLabel)`
- internal/app/cmd/{resume,unlock,extend,lock}.go uses `"tf-km/sandboxes/"` S3 path becomes `"tf-" + cfg.GetResourcePrefix() + "/sandboxes/"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate operator-side cmd resource name + SSM path call sites</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/destroy.go, internal/app/cmd/resume.go, internal/app/cmd/shell.go, internal/app/cmd/extend.go, internal/app/cmd/list.go, internal/app/cmd/unlock.go, internal/app/cmd/lock.go, internal/app/cmd/stop.go, internal/app/cmd/budget.go, internal/app/cmd/ami.go, internal/app/cmd/init.go, internal/app/cmd/doctor.go, internal/app/cmd/at.go, internal/app/cmd/configure.go, internal/app/cmd/configure_github.go, internal/app/cmd/create_slack.go, internal/app/cmd/slack.go, internal/app/cmd/doctor_slack.go, internal/app/cmd/logs.go, internal/app/cmd/status.go</files>
  <action>
    Migrate every singleton resource-name literal and SSM-path literal in operator-side cmd code to use the cfg helpers. The cfg pointer is reachable from every command (Cobra commands load config in PersistentPreRun).

    Substitution rules — apply mechanically:

    Rule 1 — DynamoDB table names (singleton):
    - "km-sandboxes" becomes cfg.GetResourcePrefix() + "-sandboxes"
    - "km-budgets" becomes cfg.GetResourcePrefix() + "-budgets"
    - "km-identities" becomes cfg.GetResourcePrefix() + "-identities"
    - "km-schedules" becomes cfg.GetResourcePrefix() + "-schedules"
    - "km-slack-bridge-nonces" becomes cfg.GetResourcePrefix() + "-slack-bridge-nonces"

    Rule 2 — Lambda function/role names (singleton):
    - "km-ttl-handler" becomes cfg.GetResourcePrefix() + "-ttl-handler"
    - "km-ttl-scheduler" becomes cfg.GetResourcePrefix() + "-ttl-scheduler"
    - "km-create-handler" becomes cfg.GetResourcePrefix() + "-create-handler"
    - "km-email-create-handler" becomes cfg.GetResourcePrefix() + "-email-create-handler"
    - "km-slack-bridge" becomes cfg.GetResourcePrefix() + "-slack-bridge"
    - "km-ecs-spot-handler" becomes cfg.GetResourcePrefix() + "-ecs-spot-handler"
    - "km-at" (EventBridge schedule group) becomes cfg.GetResourcePrefix() + "-at"

    Rule 3 — SSM paths and S3/log paths:
    - "/km/slack/..." becomes cfg.GetSsmPrefix() + "slack/..."
    - "/km/config/github/..." becomes cfg.GetSsmPrefix() + "config/github/..."
    - "/km/config/remote-create/..." becomes cfg.GetSsmPrefix() + "config/remote-create/..."
    - "/km/sandboxes/..." (CloudWatch log group prefix) becomes "/" + cfg.GetResourcePrefix() + "/sandboxes/..." — note: GetSsmPrefix is for SSM only; for log groups use the prefix directly to keep semantic intent clear
    - "tf-km/sandboxes/" (S3 path) becomes "tf-" + cfg.GetResourcePrefix() + "/sandboxes/"
    - "tf-km-state-{region}" (init.go bucket name): fmt.Sprintf("tf-%s-state-%s", cfg.GetResourcePrefix(), regionLabel)

    Rule 4 — Per-sandbox names (DO NOT change):
    - "km-budget-enforcer-" + sandboxID — already collision-free, leave alone.
    - "km-budget-{sandboxID}", "km-ttl-{sandboxID}", etc. — leave alone.

    File-by-file high-density migration:

    For each file in this task's list, run grep to enumerate the call sites:
    grep -n '"km-\|"/km/\|"tf-km' <file>
    Then mechanically apply rules 1–4 to each match.

    Special files needing extra care:

    1. internal/app/cmd/at.go (lines 319, 702): "km-at" is the EventBridge schedule group. ALSO this file's CLI command writes schedule names of the form "km-at-{name}" — those are derived from the GROUP name, so once you migrate the group name, the sub-name composes correctly. Verify by reading the function.

    2. internal/app/cmd/init.go (line 832, 833): The TF state bucket. Use fmt.Sprintf per Rule 3. Lines 1686, 1709 are about Lambda function names — Rule 2.

    3. internal/app/cmd/doctor.go (lines 496, 1025, 1048, 1179, 1204, 1897, 1906, 1919, 1983, 2244, 2260): These are doctor checks that look up resources by name. Since doctor uses provider DoctorConfigProvider (not cfg directly), call provider.GetResourcePrefix() and provider.GetSsmPrefix() — 66-01 added these to the interface.

    4. internal/app/cmd/configure.go (line 251): "km-budgets" is in the wizard output — informational. Migrate using cfg.GetResourcePrefix() (or build a temp Config for the print if cfg isn't yet populated at this point — same trick as plan 02 task 1).

    5. internal/app/cmd/{resume,unlock,extend,lock}.go: All use the "tf-km/sandboxes/" S3 path. Apply Rule 3.

    6. internal/app/cmd/configure_github.go (10 sites): All /km/config/github/* SSM paths. Apply Rule 3.

    7. internal/app/cmd/{slack,create_slack,doctor_slack}.go: All /km/slack/* SSM paths. Apply Rule 3.

    Validation:
    - After every 3-4 files edited: go build ./internal/app/cmd/... — must pass.
    - After ALL files in this task: go test ./internal/app/cmd/... -count=1 — must be green.

    Do NOT touch tests in this task — defaults reproduce the old strings byte-identical, so existing tests continue to pass without modification.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./internal/app/cmd/... && go test ./internal/app/cmd/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Grep audit: `grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' internal/app/cmd --include='*.go' | grep -v _test.go` returns no matches.
    - Grep audit: `grep -rn '"/km/' internal/app/cmd --include='*.go' | grep -v _test.go` returns no matches.
    - `grep -rn 'GetResourcePrefix\|GetSsmPrefix' internal/app/cmd/ | wc -l` shows ≥ 30 matches.
    - `go build ./internal/app/cmd/...` succeeds.
    - `go test ./internal/app/cmd/... -count=1` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Lambda handlers (cmd/*/main.go) to env-var-driven names</name>
  <files>cmd/ttl-handler/main.go, cmd/create-handler/main.go, cmd/email-create-handler/main.go, cmd/budget-enforcer/main.go, cmd/github-token-refresher/main.go, cmd/km-slack-bridge/main.go</files>
  <action>
    For each Lambda handler, replace every hardcoded "km-*" resource name and "/km/*" SSM path with an env-var read using the helper-function pattern from the interfaces block. Each Lambda gets a small set of helpers near the top of its main.go. Plan 04 (TF modules) will set these env vars in the Lambda env block. Until plan 04 ships, the Lambdas run with the env vars unset and fall back to the literal — that is intentional and preserves current behavior.

    Use this pattern (one helper per env var, near the top of main.go):

    ```go
    func sandboxTableName() string {
        if v := os.Getenv("KM_SANDBOX_TABLE_NAME"); v != "" {
            return v
        }
        return "km-sandboxes"
    }
    ```

    Per-file work:

    1. cmd/ttl-handler/main.go — the heaviest migration in this task (Pitfall 4):
       - Add helpers: sandboxTableName, budgetTable, schedulesTable, ttlHandlerName, ttlSchedulerRole, atGroupName.
       - At lines 351, 355, 711, 718 (creating EventBridge schedule targets pointing to itself): use ttlHandlerName() and ttlSchedulerRole().
       - At line 412: km-at group references go through atGroupName().
       - At line 637, 658 and similar: budgetTable() / schedulesTable().
       - At line 954, 962, 1061: tf-km state prefix — add stateBucketPrefix() helper that reads KM_STATE_BUCKET_PREFIX with fallback "tf-km".
       - Also email domain (line 1396): if not migrated by plan 02 task 2, do it here using the same pattern (KM_EMAIL_DOMAIN env var).

    2. cmd/create-handler/main.go (lines 271, 454, 459):
       - Add helpers: sandboxTableName, identitiesTable.
       - Replace literal with helper calls.

    3. cmd/email-create-handler/main.go (line 967):
       - Add helper: sandboxTableName.
       - Replace.

    4. cmd/budget-enforcer/main.go (lines 549, 586, 638, 647):
       - Add helpers: budgetTable, sandboxTableName.
       - Replace literals.

    5. cmd/github-token-refresher/main.go (2 sites with /km/config/github/*):
       - Add helper: githubSsmConfigPrefix that reads KM_GITHUB_SSM_CONFIG_PREFIX with fallback "/km/config/github".
       - Replace.

    6. cmd/km-slack-bridge/main.go (lines 48, 49, 50):
       - Add helpers: identitiesTable, sandboxTableName, nonceTable.
       - This Lambda already partially uses env vars (KM_BOT_TOKEN_PATH per RESEARCH) — confirm by reading the file. If KM_IDENTITIES_TABLE / KM_SANDBOXES_TABLE / KM_NONCE_TABLE env reads already exist, just confirm they have correct fallbacks; do NOT remove the fallback.

    Validation:
    - go build ./cmd/{handler}/... after each file.
    - go test ./cmd/... -count=1 after all files.

    Do NOT modify any TF module in this task — plan 04 wires the env vars.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./cmd/ttl-handler/... ./cmd/create-handler/... ./cmd/email-create-handler/... ./cmd/budget-enforcer/... ./cmd/github-token-refresher/... ./cmd/km-slack-bridge/... && go test ./cmd/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All six Lambda binaries build cleanly.
    - `grep -c 'os\.Getenv("KM_' cmd/ttl-handler/main.go cmd/create-handler/main.go cmd/email-create-handler/main.go cmd/budget-enforcer/main.go cmd/github-token-refresher/main.go cmd/km-slack-bridge/main.go` shows ≥ 1 per file.
    - `grep -nE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|at)"' cmd/ttl-handler/main.go cmd/create-handler/main.go cmd/email-create-handler/main.go cmd/budget-enforcer/main.go cmd/km-slack-bridge/main.go` shows ONLY occurrences inside helper-function fallback returns (i.e. each literal appears exactly once per helper, not at multiple call sites).
    - `! grep -rn '"/km/' cmd --include='*.go' | grep -v _test.go | grep -v 'helper fallback'` — no leftover hardcoded paths outside the helper fallbacks.
    - go test ./cmd/... -count=1 green.
  </done>
</task>

<task type="auto">
  <name>Task 3: configui kmPrefix runtime variable + cmd/configui/* migration + pkg/aws + pkg/compiler</name>
  <files>cmd/configui/main.go, cmd/configui/handlers.go, cmd/configui/handlers_secrets.go, pkg/aws/cloudwatch.go, pkg/aws/scheduler.go, pkg/compiler/lifecycle.go, pkg/compiler/service_hcl.go</files>
  <action>
    Two distinct subtasks bundled together because they're each small.

    Subtask A — configui (Pitfall 5 fix):

    1. cmd/configui/handlers_secrets.go: change `const kmPrefix = "/km/"` to a package-level variable initialized at startup:
       ```go
       var kmPrefix = func() string {
           if v := os.Getenv("KM_RESOURCE_PREFIX"); v != "" {
               return "/" + v + "/"
           }
           return "/km/"
       }()
       ```

       This pattern executes the func once at package init time — that's deliberate. configui is a long-running web server; reading env once at startup is correct semantics.

    2. cmd/configui/main.go (line 107): `"km-budgets"` literal — add a helper `budgetTable()` that reads KM_BUDGET_TABLE env var. Same pattern as plan 03 task 2.

    3. cmd/configui/main.go (line 66): `KM_BUCKET` default `"tf-km"` — add helper `tfStateBucket()` that reads KM_BUCKET env var, fallback "tf-" + os.Getenv("KM_RESOURCE_PREFIX") if set, else "tf-km". Or simpler: keep KM_BUCKET as the primary env, just update the fallback to compose from KM_RESOURCE_PREFIX:
       ```go
       func tfStateBucket() string {
           if v := os.Getenv("KM_BUCKET"); v != "" {
               return v
           }
           prefix := "km"
           if p := os.Getenv("KM_RESOURCE_PREFIX"); p != "" {
               prefix = p
           }
           return "tf-" + prefix
       }
       ```

    4. cmd/configui/handlers.go: any /km/sandboxes log-group references — use kmPrefix variable (from handlers_secrets.go) plus the literal log-group path suffix.

    Subtask B — pkg/ migration:

    5. pkg/aws/cloudwatch.go (lines 68, 87): /km/sandboxes/ log-group prefix. These are functions called from operator-side code. Add `cfg *config.Config` parameter. Update callers (likely in internal/app/cmd/{logs,status}.go and pkg/compiler/service_hcl.go — verify with grep).
       - The function signature change cascades: list every caller and update to pass cfg.

    6. pkg/aws/scheduler.go (line 44): "km-ttl-{sandboxID}" — this is a per-sandbox name (Rule 4 territory) BUT the "km-" prefix portion is the singleton part. Wrap the prefix:
       - Before: fmt.Sprintf("km-ttl-%s", sandboxID)
       - After: fmt.Sprintf("%s-ttl-%s", cfg.GetResourcePrefix(), sandboxID)
       - Add `cfg *config.Config` to the function signature; update callers.

    7. pkg/compiler/lifecycle.go (line 29): "km-ttl-{sandboxID}" — same pattern as scheduler.go. Compiler functions typically already take cfg.

    8. pkg/compiler/service_hcl.go (lines 316, 322): "/km/sandboxes/" log group path used in compiler-emitted HCL. Convert to use cfg.GetResourcePrefix(). If the function emits HCL-template strings that reference a site var, defer that part to plan 04 with a TODO marker.

    Validation:
    - go build ./pkg/... ./cmd/... ./internal/... — must pass after every subtask.
    - go test ./pkg/... ./cmd/configui/... ./internal/... -count=1 — green.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./... && go test ./pkg/... ./cmd/configui/... ./internal/... -count=1 2>&1 | tail -20 && grep -c 'KM_RESOURCE_PREFIX\|KM_BUCKET\|kmPrefix' cmd/configui/main.go cmd/configui/handlers_secrets.go</automated>
  </verify>
  <done>
    - cmd/configui/handlers_secrets.go has `var kmPrefix = ...` (variable, not const) reading KM_RESOURCE_PREFIX.
    - pkg/aws/cloudwatch.go and pkg/aws/scheduler.go take cfg parameter; callers updated.
    - pkg/compiler/lifecycle.go and service_hcl.go use cfg.GetResourcePrefix() for non-template-emitting code.
    - `go build ./...` succeeds.
    - All test suites green.
  </done>
</task>

</tasks>

<verification>
After all three tasks, the operator-side and Lambda-side migrations are complete. Plan-completion gates:

1. `go build ./...` — succeeds.
2. `make build` — produces a working bin/km.
3. `go test ./internal/... ./pkg/... ./cmd/... -count=1` — green.
4. Grep audit (VALIDATION.md 66-03-02 gate):
   - `! grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' ./cmd ./internal ./pkg --include='*.go' | grep -v _test.go` — only literals appearing inside helper-function fallback returns are allowed (each literal appears exactly once per helper).
   - `! grep -rn '"/km/' ./cmd ./internal ./pkg --include='*.go' | grep -v _test.go | grep -v '"/opt/km' | grep -v '"~/.km'` — only literals inside helper fallbacks remain.

5. Setting `KM_TTL_HANDLER_NAME=alt-ttl-handler` env var and running ttl-handler smoke would route to the alt-prefix Lambda — verifiable via env var injection in a one-off process; not exercised in CI but the contract is in code.

Plan 03 does NOT yet make multi-instance work end-to-end — TF modules still hardcode names. Plan 04 closes that by (a) adding var.resource_prefix to the five Lambda modules, (b) extending site.hcl, (c) wiring the env vars this plan reads in the TF module env blocks.
</verification>

<success_criteria>
- All operator-side singleton km-* resource names + /km/ SSM paths migrated to cfg helpers.
- All Lambda handlers use env-var helpers with safe fallbacks.
- ttl-handler self-name (Pitfall 4) is env-driven via KM_TTL_HANDLER_NAME and KM_TTL_SCHEDULER_ROLE.
- configui kmPrefix is a runtime variable from KM_RESOURCE_PREFIX (Pitfall 5).
- pkg/aws/{cloudwatch,scheduler}.go and pkg/compiler/{lifecycle,service_hcl}.go migrated.
- Full test suite green.
- make build succeeds.
- VALIDATION.md task IDs 66-03-01 + 66-03-02 green.
</success_criteria>

<output>
After completion, create `.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-03-SUMMARY.md` describing:
- Per-file count of literals migrated (operator-side cmd/, Lambda cmd/, pkg/, configui).
- Lambda helper-function pattern (which env vars added, where).
- ttl-handler Pitfall 4 fix (self-naming via env).
- configui kmPrefix variable conversion (Pitfall 5 fix).
- pkg signature changes (cfg added to functions in pkg/aws/{cloudwatch,scheduler}.go) and downstream caller updates.
- Two grep audits and their zero-count results.
- Confirmation make build succeeds and full test suite green.
- Final state: operator + Lambda code is multi-instance ready; TF modules (plan 04) and km init flow (plan 05) remain.
</output>
