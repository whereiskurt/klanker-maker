---
phase: 66-multi-instance-support-configurable-resource-prefix-and-email-subdomain
plan: 03
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified:
  - internal/app/cmd/create.go
  - internal/app/cmd/destroy.go
  - internal/app/cmd/resume.go
  - internal/app/cmd/shell.go
  - internal/app/cmd/extend.go
  - internal/app/cmd/list.go
  - internal/app/cmd/unlock.go
  - internal/app/cmd/lock.go
  - internal/app/cmd/stop.go
  - internal/app/cmd/budget.go
  - internal/app/cmd/ami.go
  - internal/app/cmd/init.go
  - internal/app/cmd/doctor.go
  - internal/app/cmd/at.go
  - internal/app/cmd/configure.go
  - internal/app/cmd/configure_github.go
  - internal/app/cmd/create_slack.go
  - internal/app/cmd/slack.go
  - internal/app/cmd/doctor_slack.go
  - internal/app/cmd/destroy_slack.go
  - internal/app/cmd/logs.go
  - internal/app/cmd/status.go
  - cmd/ttl-handler/main.go
  - cmd/create-handler/main.go
  - cmd/email-create-handler/main.go
  - cmd/budget-enforcer/main.go
  - cmd/configui/main.go
  - cmd/configui/handlers.go
  - cmd/configui/handlers_secrets.go
  - cmd/github-token-refresher/main.go
  - cmd/km-slack-bridge/main.go
  - pkg/aws/cloudwatch.go
  - pkg/aws/scheduler.go
  - pkg/aws/rotation.go
  - pkg/compiler/lifecycle.go
  - pkg/compiler/service_hcl.go
  - pkg/compiler/userdata.go
  - pkg/slack/bridge/aws_adapters.go
autonomous: true
requirements:
  - REQ-PLATFORM-MULTI-INSTANCE

must_haves:
  truths:
    - "Operator-side cmd code references resource names via cfg.GetResourcePrefix() and SSM paths via cfg.GetSsmPrefix() — zero hardcoded km-* singleton names or /km/ paths outside _test.go fixtures and helper-fallback returns"
    - "Lambda handlers read every singleton resource name from env vars with safe km-*//km/* fallbacks (no operator config in Lambda runtime)"
    - "ttl-handler reads KM_TTL_HANDLER_NAME, KM_TTL_SCHEDULER_ROLE, KM_AT_GROUP_NAME from env (Pitfall 4 fix)"
    - "configui kmPrefix becomes a runtime variable from KM_RESOURCE_PREFIX (Pitfall 5 fix)"
    - "create.go os.Setenv block exports KM_SLACK_THREADS_TABLE and KM_SLACK_STREAM_TABLE so the compiler picks them up (Phase 67/68 drift fix)"
    - "All 6 new Phase 67/68 hardcoded sites migrated: status.go:468, init.go:1724, slack.go:759, doctor.go:2330,2341,2392, plus the new SSM paths (/km/slack/bot-scopes-cache, /km/slack/last-test-timestamp, /km/slack/workspace, /km/slack/signing-secret)"
    - "make build succeeds; full test suite (./internal/... ./pkg/... ./cmd/...) green"
    - "Grep audit: zero singleton km- resource literals remain in ./cmd ./internal ./pkg outside _test.go and helper fallbacks"
    - "Grep audit: zero /km/ literals remain in ./cmd ./internal ./pkg outside _test.go, /opt/km filesystem paths, ~/.km filesystem paths, and helper fallbacks"
  artifacts:
    - path: "cmd/ttl-handler/main.go"
      provides: "All hardcoded km-* resource names replaced with env-var reads (KM_TTL_HANDLER_NAME, KM_TTL_SCHEDULER_ROLE, KM_AT_GROUP_NAME, etc.)"
      contains: "KM_TTL_HANDLER_NAME"
    - path: "cmd/configui/handlers_secrets.go"
      provides: "kmPrefix changed from const to runtime variable from KM_RESOURCE_PREFIX (Pitfall 5)"
      contains: "KM_RESOURCE_PREFIX"
    - path: "internal/app/cmd/init.go"
      provides: "tf-{prefix}-state-{region} bucket name uses cfg.GetResourcePrefix(); Pitfall 11 fix at line 1724 (ForceSlackBridgeColdStartWith parameterized)"
      contains: "GetResourcePrefix"
    - path: "internal/app/cmd/at.go"
      provides: "Schedule group name {prefix}-at via cfg.GetResourcePrefix()"
      contains: "GetResourcePrefix"
    - path: "internal/app/cmd/create.go"
      provides: "os.Setenv block exports KM_SLACK_THREADS_TABLE and KM_SLACK_STREAM_TABLE for the compiler (Phase 67/68 drift fix from RESEARCH Pattern 7)"
      contains: "KM_SLACK_THREADS_TABLE"
    - path: "internal/app/cmd/slack.go"
      provides: "PersistSigningSecret + bot-scopes-cache + last-test-timestamp + workspace SSM paths use cfg.GetSsmPrefix() (Phase 67 drift fix)"
      contains: "GetSsmPrefix"
    - path: "internal/app/cmd/status.go"
      provides: "Slack-threads display uses cfg.GetSlackThreadsTableName() instead of hardcoded km-slack-threads (Pitfall 10 fix)"
      contains: "GetSlackThreadsTableName"
    - path: "internal/app/cmd/doctor.go"
      provides: "doctorSlackMetadataScanner.tableName + listSandboxesWithInboundImpl + newRealLister use cfg.GetSandboxTableName() (Phase 67 drift fix at lines 2330, 2341, 2392)"
      contains: "GetSandboxTableName"
  key_links:
    - from: "operator-side cmd/*.go"
      to: "cfg.GetResourcePrefix() / cfg.GetSsmPrefix()"
      via: "config helper method"
      pattern: "GetResourcePrefix|GetSsmPrefix"
    - from: "Lambda handlers (cmd/*/main.go)"
      to: "process env vars (KM_SANDBOX_TABLE_NAME, KM_BUDGET_TABLE, KM_IDENTITIES_TABLE, KM_TTL_HANDLER_NAME, KM_TTL_SCHEDULER_ROLE, KM_AT_GROUP_NAME, KM_SCHEDULES_TABLE, KM_NONCE_TABLE, KM_BOT_TOKEN_PATH, KM_GITHUB_SSM_CONFIG_PREFIX, KM_SLACK_THREADS_TABLE, KM_SLACK_STREAM_TABLE)"
      via: "os.Getenv with hardcoded fallback"
      pattern: "os\\.Getenv\\(\"KM_"
    - from: "configui kmPrefix runtime"
      to: "KM_RESOURCE_PREFIX env var"
      via: "package-level var initialized at startup"
      pattern: "KM_RESOURCE_PREFIX"
    - from: "create.go os.Setenv block"
      to: "compiler (userdata.go, service_hcl.go) reads of KM_SLACK_THREADS_TABLE + KM_SLACK_STREAM_TABLE"
      via: "process environment propagation"
      pattern: "os\\.Setenv\\(\"KM_SLACK_(THREADS|STREAM)_TABLE\""
---

<objective>
Migrate the resource-name and SSM-path hardcoding sweep — Categories B (86 SSM paths, up from 75 — Phase 67 added 11 new sites) and C (~134 resource names, up from 100+ — Phase 67/68 added 6 new singleton sites) from RESEARCH.md. Operator-side code calls `cfg.GetResourcePrefix()` and `cfg.GetSsmPrefix()`. Lambda handlers (which run without operator config) read env vars with safe fallbacks; the env vars themselves are wired up by plan 04's TF module work.

Critically, this plan also fixes:
- **Pitfall 4** (ttl-handler hardcodes its own function name `"km-ttl-handler"` at cmd/ttl-handler/main.go:351)
- **Pitfall 5** (configui's `const kmPrefix = "/km/"` at cmd/configui/handlers_secrets.go:47)
- **Pitfall 10** (status.go:468 hardcodes `"km-slack-threads"` — Phase 67 drift)
- **Pitfall 11** (init.go:1724 `ForceSlackBridgeColdStartWith` hardcodes `"km-slack-bridge"` — Phase 67 drift)
- **Phase 67 drift sites in slack.go**: 311, 328, 426, 461, 465, 759, 836 (signing-secret, bot-scopes-cache, last-test-timestamp, workspace)
- **Phase 67 drift sites in doctor.go**: 2330, 2341, 2392 (`"km-sandboxes"` hardcoded)
- **Phase 67/68 drift in create.go**: missing `os.Setenv("KM_SLACK_THREADS_TABLE", …)` and `os.Setenv("KM_SLACK_STREAM_TABLE", …)` in the create.go env block — without these, the compiler reads stale fallbacks (RESEARCH Pattern 7)

Purpose: This is the largest migration plan in the phase — touches ~37 files across operator-side cmd, Lambda handlers, configui, and pkg/. Plan 02 ran first (different call-site domain — email vs. resources) and this plan follows in Wave 3 because plans 02 and 03 share 12 files (cmd/*.go + Lambda handlers + compiler) at the file level even though their substitution domains are semantically disjoint; serializing them avoids parallel-write conflicts.

> **Scope note (38 files, mechanical):** This plan exceeds the standard 15-file guideline. The actions are pure grep-then-replace migration work, so the risk is lower than for design-heavy plans of equivalent size. If execution-quality issues surface during implementation (mid-task context degradation, accumulated diff confusion), split along the natural Subtask boundaries used below: Task 1 (operator-side cmd/), Task 2 (Lambda handlers cmd/*/main.go), Task 3 (configui + pkg/aws + pkg/compiler + pkg/slack/bridge).

Output: ~37 files updated; two grep-audit gates green; full test suite green; `make build` succeeds.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-RESEARCH.md
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-VALIDATION.md
@.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-01-SUMMARY.md
@internal/app/cmd/create.go
@internal/app/cmd/destroy.go
@internal/app/cmd/init.go
@internal/app/cmd/at.go
@internal/app/cmd/slack.go
@internal/app/cmd/status.go
@internal/app/cmd/doctor.go
@cmd/ttl-handler/main.go
@cmd/configui/handlers_secrets.go

<interfaces>
Helper methods (added by 66-01 + Phase 67 shim):

```go
func (c *Config) GetResourcePrefix() string         // "km" by default (Phase 67)
func (c *Config) GetSsmPrefix() string              // "/km/" by default (66-01)
func (c *Config) GetEmailDomain() string            // "sandboxes.{domain}" by default (66-01)
func (c *Config) GetSlackThreadsTableName() string  // {prefix}-slack-threads (Phase 67)
func (c *Config) GetSlackStreamMessagesTableName() string  // {prefix}-slack-stream-messages (Phase 68)
```

Note: cfg may also expose `GetSandboxTableName()` already — verify via grep before using. If absent, ADD it as a trivial helper (`return c.GetResourcePrefix() + "-sandboxes"`) in this plan as a small helper-extension. The doctor.go drift at lines 2330, 2341, 2392 needs this.

Lambda env-var convention (set in plan 04 by TF modules; this plan adds the read code):

| Env Var | Lambda(s) | Fallback | What it names |
|---------|-----------|----------|---------------|
| KM_SANDBOX_TABLE_NAME | ttl-handler, create-handler, email-create-handler | km-sandboxes | DynamoDB sandboxes table |
| KM_BUDGET_TABLE | budget-enforcer, ttl-handler, configui | km-budgets | DynamoDB budgets table |
| KM_IDENTITIES_TABLE | km-slack-bridge, create-handler | km-identities | DynamoDB identities table |
| KM_SCHEDULES_TABLE | ttl-handler | km-schedules | DynamoDB schedules table |
| KM_NONCE_TABLE | km-slack-bridge | km-slack-bridge-nonces | DynamoDB Slack nonce table |
| KM_TTL_HANDLER_NAME | ttl-handler (self) | km-ttl-handler | Own Lambda function name (Pitfall 4) |
| KM_TTL_SCHEDULER_ROLE | ttl-handler | km-ttl-scheduler | EventBridge scheduler role |
| KM_AT_GROUP_NAME | ttl-handler | km-at | EventBridge schedule group |
| KM_BOT_TOKEN_PATH | km-slack-bridge | /km/slack/bot-token | SSM bot token path |
| KM_GITHUB_SSM_CONFIG_PREFIX | github-token-refresher | /km/config/github | SSM github config prefix |
| KM_RESOURCE_PREFIX | configui (only) | km | For runtime kmPrefix var |
| KM_SLACK_THREADS_TABLE | km-slack-bridge, sandbox poller (env file) | km-slack-threads | DDB Slack-threads table (Phase 67; bridge already has this var declared) |
| KM_SLACK_STREAM_TABLE | sandbox runtime (env file) | km-slack-stream-messages | DDB Slack-stream-messages (Phase 68) |

Per-sandbox names (DO NOT change — already collision-free):
km-budget-enforcer-{sandboxID}, km-github-token-refresher-{sandboxID}, km-ec2spot-ssm-{sandboxID}-{region}, km-ec2spot-profile-{sandboxID}-{region}, km-docker-{sandboxID}-{region}, km-sidecar-{sandboxID}-{region}, km-ttl-{sandboxID}, km-budget-{sandboxID}, km-github-token-{sandboxID}.

Lambda env-var helper pattern (use this everywhere):

```go
func sandboxTableName() string {
    if v := os.Getenv("KM_SANDBOX_TABLE_NAME"); v != "" {
        return v
    }
    return "km-sandboxes"
}
```

TF state prefix transformations:
- internal/app/cmd/init.go: `fmt.Sprintf("tf-km-state-%s", regionLabel)` becomes `fmt.Sprintf("tf-%s-state-%s", cfg.GetResourcePrefix(), regionLabel)`
- internal/app/cmd/{resume,unlock,extend,lock}.go uses `"tf-km/sandboxes/"` S3 path becomes `"tf-" + cfg.GetResourcePrefix() + "/sandboxes/"`

ForceSlackBridgeColdStartWith (init.go:1724) — Pitfall 11:
```go
// Before:
//   _, err := lambdaClient.UpdateFunctionConfiguration(ctx, &lambda.UpdateFunctionConfigurationInput{
//       FunctionName: aws.String("km-slack-bridge"),
//       ...
//   })

// After (add functionName parameter; callers pass cfg.GetResourcePrefix() + "-slack-bridge"):
func ForceSlackBridgeColdStartWith(ctx context.Context, lambdaClient LambdaClient, functionName string) error {
    _, err := lambdaClient.UpdateFunctionConfiguration(ctx, &lambda.UpdateFunctionConfigurationInput{
        FunctionName: aws.String(functionName),
        ...
    })
}
```

PersistSigningSecret (slack.go:759) — Phase 67 drift:
```go
// Before:
//   func PersistSigningSecret(ctx context.Context, store SlackSSMStore, secret, kmsKey string) error {
//       return store.Put(ctx, "/km/slack/signing-secret", secret, true)
//   }

// After (add ssmPrefix parameter):
func PersistSigningSecret(ctx context.Context, store SlackSSMStore, secret, kmsKey, ssmPrefix string) error {
    return store.Put(ctx, ssmPrefix+"slack/signing-secret", secret, true)
}
// Callers in slack.go pass cfg.GetSsmPrefix().
```

create.go os.Setenv block additions (RESEARCH Pattern 7) — Phase 67/68 drift fix:
```go
// In create.go around line 379, the existing os.Setenv block exports compiler env vars.
// ADD these two lines:
os.Setenv("KM_SLACK_THREADS_TABLE", cfg.GetSlackThreadsTableName())
os.Setenv("KM_SLACK_STREAM_TABLE", cfg.GetSlackStreamMessagesTableName())
```

This is critical: pkg/compiler/userdata.go:3074, 3090, 3105 and pkg/compiler/service_hcl.go:778 all read these env vars with `"km-slack-threads"` / `"km-slack-stream-messages"` as fallbacks. Without create.go setting them, custom-prefix installs use the wrong table names in sandbox userdata.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate operator-side cmd resource name + SSM path call sites (including all Phase 67/68 drift sites)</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/destroy.go, internal/app/cmd/resume.go, internal/app/cmd/shell.go, internal/app/cmd/extend.go, internal/app/cmd/list.go, internal/app/cmd/unlock.go, internal/app/cmd/lock.go, internal/app/cmd/stop.go, internal/app/cmd/budget.go, internal/app/cmd/ami.go, internal/app/cmd/init.go, internal/app/cmd/doctor.go, internal/app/cmd/at.go, internal/app/cmd/configure.go, internal/app/cmd/configure_github.go, internal/app/cmd/create_slack.go, internal/app/cmd/slack.go, internal/app/cmd/doctor_slack.go, internal/app/cmd/destroy_slack.go, internal/app/cmd/logs.go, internal/app/cmd/status.go</files>
  <action>
    Migrate every singleton resource-name literal and SSM-path literal in operator-side cmd code to use the cfg helpers. The cfg pointer is reachable from every command (Cobra commands load config in PersistentPreRun).

    **Step 0 — Discovery grep before editing:**
    ```bash
    grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|slack-threads|slack-stream-messages|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' internal/app/cmd --include='*.go' | grep -v _test.go
    grep -rn '"/km/' internal/app/cmd --include='*.go' | grep -v _test.go
    grep -rn '"tf-km' internal/app/cmd --include='*.go' | grep -v _test.go
    ```

    **Substitution rules — apply mechanically:**

    **Rule 1 — DynamoDB table names (singleton):**
    - `"km-sandboxes"` → `cfg.GetResourcePrefix() + "-sandboxes"` (or use `cfg.GetSandboxTableName()` if it exists; if not, add the helper to config.go in this plan)
    - `"km-budgets"` → `cfg.GetResourcePrefix() + "-budgets"`
    - `"km-identities"` → `cfg.GetResourcePrefix() + "-identities"`
    - `"km-schedules"` → `cfg.GetResourcePrefix() + "-schedules"`
    - `"km-slack-bridge-nonces"` → `cfg.GetResourcePrefix() + "-slack-bridge-nonces"`
    - `"km-slack-threads"` → `cfg.GetSlackThreadsTableName()` (Phase 67 helper exists)
    - `"km-slack-stream-messages"` → `cfg.GetSlackStreamMessagesTableName()` (Phase 68 helper exists)

    **Rule 2 — Lambda function/role names (singleton):**
    - `"km-ttl-handler"` → `cfg.GetResourcePrefix() + "-ttl-handler"`
    - `"km-ttl-scheduler"` → `cfg.GetResourcePrefix() + "-ttl-scheduler"`
    - `"km-create-handler"` → `cfg.GetResourcePrefix() + "-create-handler"`
    - `"km-email-create-handler"` → `cfg.GetResourcePrefix() + "-email-create-handler"`
    - `"km-slack-bridge"` → `cfg.GetResourcePrefix() + "-slack-bridge"`
    - `"km-ecs-spot-handler"` → `cfg.GetResourcePrefix() + "-ecs-spot-handler"`
    - `"km-at"` (EventBridge schedule group) → `cfg.GetResourcePrefix() + "-at"`

    **Rule 3 — SSM paths and S3/log paths:**
    - `"/km/slack/..."` → `cfg.GetSsmPrefix() + "slack/..."`
    - `"/km/config/github/..."` → `cfg.GetSsmPrefix() + "config/github/..."`
    - `"/km/config/remote-create/..."` → `cfg.GetSsmPrefix() + "config/remote-create/..."`
    - `"/km/sandboxes/..."` (CloudWatch log group prefix) → `"/" + cfg.GetResourcePrefix() + "/sandboxes/..."`
    - `"tf-km/sandboxes/"` (S3 path) → `"tf-" + cfg.GetResourcePrefix() + "/sandboxes/"`
    - `fmt.Sprintf("tf-km-state-%s", regionLabel)` → `fmt.Sprintf("tf-%s-state-%s", cfg.GetResourcePrefix(), regionLabel)`

    **Rule 4 — Per-sandbox names (DO NOT change):**
    - `"km-budget-enforcer-" + sandboxID` — already collision-free, leave alone (Plan 03 task 3 handles pkg/aws/scheduler.go for these via prefix-aware composition).
    - `"km-budget-{sandboxID}"`, `"km-ttl-{sandboxID}"` — leave alone.

    **File-by-file high-density migration:**

    For each file in this task's list, run grep to enumerate the call sites:
    ```bash
    grep -nE '"km-|"/km/|"tf-km' <file>
    ```
    Then mechanically apply rules 1–4 to each match.

    **Special files needing extra care:**

    1. **internal/app/cmd/at.go** (lines 319, 702): `"km-at"` is the EventBridge schedule group. ALSO this file's CLI command writes schedule names of the form `"km-at-{name}"` — those are derived from the GROUP name, so once you migrate the group name, the sub-name composes correctly. Verify by reading the function.

    2. **internal/app/cmd/init.go**:
       - **Bucket name**: line ~832 `fmt.Sprintf("tf-km-state-%s", regionLabel)` — apply Rule 3.
       - **Pitfall 11 fix at line 1724**: `ForceSlackBridgeColdStartWith` hardcodes `aws.String("km-slack-bridge")`. Refactor signature to accept `functionName string` per the interfaces block. Update all callers (likely `km slack rotate-token` in slack.go and tests). Tests pass the new param explicitly. Ops callers pass `cfg.GetResourcePrefix() + "-slack-bridge"`.

    3. **internal/app/cmd/doctor.go**:
       - Lines 496, 1025, 1048, 1179, 1204, 1897, 1906, 1919, 1983, 2244, 2260: doctor checks looking up resources by name. Since doctor uses `DoctorConfigProvider` (66-01 added `GetResourcePrefix`/`GetSsmPrefix`/`GetEmailDomain`/`GetSlackThreadsTableName` to the interface), call `provider.GetResourcePrefix()` and `provider.GetSsmPrefix()`.
       - **Phase 67 drift fix at lines 2330, 2341, 2392**: `"km-sandboxes"` hardcoded in `doctorSlackMetadataScanner.tableName`, `listSandboxesWithInboundImpl` call, and `newRealLister` call. Use `cfg.GetSandboxTableName()` (or `cfg.GetResourcePrefix() + "-sandboxes"` — if no helper exists yet, add `GetSandboxTableName()` to config.go as a 2-liner).

    4. **internal/app/cmd/configure.go**: line ~251 `"km-budgets"` is in wizard output — informational. Migrate using `cfg.GetResourcePrefix()` (or build a temp Config for the print if cfg isn't yet populated at this point — same trick as plan 02 task 1).

    5. **internal/app/cmd/{resume,unlock,extend,lock}.go**: All use the `"tf-km/sandboxes/"` S3 path. Apply Rule 3.

    6. **internal/app/cmd/configure_github.go** (10 sites): All `/km/config/github/*` SSM paths. Apply Rule 3.

    7. **internal/app/cmd/{slack,create_slack,doctor_slack,destroy_slack}.go**:
       - All `/km/slack/*` SSM paths. Apply Rule 3.
       - **Phase 67 drift sites in slack.go specifically** — these are NEW sites the stale plan didn't cover:
         - Lines 311, 328: `/km/slack/bot-scopes-cache`
         - Lines 426, 461, 462, 463, 464, 465: `/km/slack/last-test-timestamp`, `/km/slack/workspace`, `/km/slack/shared-channel-id`, `/km/slack/invite-email`, `/km/slack/bridge-url`
         - Line 541: `/km/slack/bot-token` (in `SlackRotateToken`)
         - Line 759: `/km/slack/signing-secret` — this is in `PersistSigningSecret`. Refactor signature per interfaces block: add `ssmPrefix string` parameter. Callers pass `cfg.GetSsmPrefix()`.
         - Line 836: log message mentioning `/km/slack/signing-secret` — update for consistency: replace literal with `cfg.GetSsmPrefix() + "slack/signing-secret"`.
       - **destroy_slack.go line 75**: `/km/slack/bridge-url` — apply Rule 3.

    8. **internal/app/cmd/status.go** — **Pitfall 10 fix at line 468**:
       - The `countActiveThreads(ctx, ddbClient, "km-slack-threads", rec.SlackChannelID)` call hardcodes the table name. The `km status` command does NOT receive `*config.Config` in this code path — it constructs a DDB client locally at line 456. Two options:
         - (a) Thread `cfg.GetSlackThreadsTableName()` from the calling Cobra command into the helper. Best — consistent with list.go:132 which already does this correctly.
         - (b) Read from env at the call site (less ideal — operator commands should use cfg, not env).
       - Choose option (a). The `km status` Cobra command has access to cfg; pass `cfg.GetSlackThreadsTableName()` down.

    9. **internal/app/cmd/create.go** — **CRITICAL Phase 67/68 drift fix (RESEARCH Pattern 7)**:
       - Around line 379, the existing `os.Setenv` block exports compiler env vars. ADD these two lines:
         ```go
         os.Setenv("KM_SLACK_THREADS_TABLE", cfg.GetSlackThreadsTableName())
         os.Setenv("KM_SLACK_STREAM_TABLE", cfg.GetSlackStreamMessagesTableName())
         ```
       - Without these, the compiler in `pkg/compiler/userdata.go:3074, 3090, 3105` and `pkg/compiler/service_hcl.go:778` falls back to literal `"km-slack-threads"` / `"km-slack-stream-messages"` even when `resource_prefix` is set. Result: custom-prefix install creates sandboxes that talk to the wrong DDB tables.
       - Verify by re-running grep on `pkg/compiler/userdata.go:3074-3105` after this plan completes — those fallbacks should still be there (they're fallbacks, fine), but the create.go env var export should now win at runtime.

    **Validation:**
    - After every 3-4 files edited: `go build ./internal/app/cmd/...` — must pass.
    - After ALL files in this task: `go test ./internal/app/cmd/... -count=1` — must be green.

    Do NOT touch tests in this task — defaults reproduce the old strings byte-identical, so existing tests continue to pass without modification. **Exception**: if `ForceSlackBridgeColdStartWith` (Pitfall 11 refactor) has unit tests, those tests must be updated to pass the new `functionName` parameter — that's a contract change, not a behavior change.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./internal/app/cmd/... && go test ./internal/app/cmd/... -count=1 2>&1 | tail -20 && (! grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' internal/app/cmd --include='*.go' | grep -v _test.go) && (! grep -rn '"/km/' internal/app/cmd --include='*.go' | grep -v _test.go) && grep -c 'KM_SLACK_THREADS_TABLE\|KM_SLACK_STREAM_TABLE' internal/app/cmd/create.go</automated>
  </verify>
  <done>
    - Grep audit: `! grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' internal/app/cmd --include='*.go' | grep -v _test.go` returns no matches.
    - Grep audit: `! grep -rn '"/km/' internal/app/cmd --include='*.go' | grep -v _test.go` returns no matches.
    - `grep -rn 'GetResourcePrefix\|GetSsmPrefix\|GetSlackThreadsTableName\|GetSlackStreamMessagesTableName\|GetSandboxTableName' internal/app/cmd/ | wc -l` shows ≥ 40 matches.
    - `grep -n 'KM_SLACK_THREADS_TABLE' internal/app/cmd/create.go` shows the os.Setenv export (Phase 67/68 drift fix).
    - `grep -n 'aws\.String("km-slack-bridge")' internal/app/cmd/init.go` returns no matches (Pitfall 11 fixed).
    - `grep -n '"km-slack-threads"' internal/app/cmd/status.go` returns no matches (Pitfall 10 fixed).
    - `grep -n '"km-sandboxes"' internal/app/cmd/doctor.go` returns no matches OR returns only matches inside helper-fallback returns (Phase 67 drift at 2330/2341/2392 fixed).
    - `go build ./internal/app/cmd/...` succeeds.
    - `go test ./internal/app/cmd/... -count=1` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Lambda handlers (cmd/*/main.go) to env-var-driven names</name>
  <files>cmd/ttl-handler/main.go, cmd/create-handler/main.go, cmd/email-create-handler/main.go, cmd/budget-enforcer/main.go, cmd/github-token-refresher/main.go, cmd/km-slack-bridge/main.go</files>
  <action>
    For each Lambda handler, replace every hardcoded `"km-*"` resource name and `"/km/*"` SSM path with an env-var read using the helper-function pattern. Each Lambda gets a small set of helpers near the top of its main.go. Plan 04 (TF modules) will set these env vars in the Lambda env block. Until plan 04 ships, the Lambdas run with the env vars unset and fall back to the literal — that is intentional and preserves current behavior.

    **Pattern (one helper per env var, near the top of main.go):**
    ```go
    func sandboxTableName() string {
        if v := os.Getenv("KM_SANDBOX_TABLE_NAME"); v != "" {
            return v
        }
        return "km-sandboxes"
    }
    ```

    **Per-file work:**

    1. **cmd/ttl-handler/main.go** — the heaviest migration in this task (Pitfall 4):
       - Add helpers: `sandboxTableName`, `budgetTable`, `schedulesTable`, `ttlHandlerName`, `ttlSchedulerRole`, `atGroupName`.
       - At lines 351, 355 (creating EventBridge schedule targets pointing to itself): use `ttlHandlerName()` and `ttlSchedulerRole()`.
       - At line 412: `km-at` group references go through `atGroupName()`.
       - At lines 711, 718 and similar: budget table + schedule helper calls.
       - At line 637, 658 and similar: `budgetTable()` / `schedulesTable()`.
       - At lines 954, 962, 1061: `tf-km` state prefix — add `stateBucketPrefix()` helper that reads `KM_STATE_BUCKET_PREFIX` with fallback `"tf-km"`.
       - Email domain (line 1396): if not migrated by plan 02 task 2, do it here (KM_EMAIL_DOMAIN env var) — coordinate via SUMMARY check.

    2. **cmd/create-handler/main.go** (lines 271, 454, 459):
       - Add helpers: `sandboxTableName`, `identitiesTable`.
       - Replace literal with helper calls.

    3. **cmd/email-create-handler/main.go** (line 967):
       - Add helper: `sandboxTableName`.
       - Replace.

    4. **cmd/budget-enforcer/main.go** (lines 549, 586, 638, 647):
       - Add helpers: `budgetTable`, `sandboxTableName`.
       - Replace literals.

    5. **cmd/github-token-refresher/main.go** (2 sites with `/km/config/github/*`):
       - Add helper: `githubSsmConfigPrefix` that reads `KM_GITHUB_SSM_CONFIG_PREFIX` with fallback `"/km/config/github"`.
       - Replace.

    6. **cmd/km-slack-bridge/main.go**:
       - This Lambda already partially uses env vars (`KM_BOT_TOKEN_PATH`, `KM_SLACK_THREADS_TABLE` per Phase 67 RESEARCH). Confirm by reading the file at line 157 (cited in RESEARCH as having `threadsTable = "km-slack-threads"` env-var fallback — already correct pattern).
       - Add helpers: `identitiesTable`, `sandboxTableName`, `nonceTable` if not already present.
       - At lines 48, 49, 50 (per stale research) — verify these are actual hardcoded sites today; if Phase 67 already migrated them, leave them.
       - **Do NOT remove** any existing env-var fallback. Pattern: env-var-with-fallback is the contract; preserve it.

    **Validation:**
    - `go build ./cmd/{handler}/...` after each file.
    - `go test ./cmd/... -count=1` after all files.

    Do NOT modify any TF module in this task — plan 04 wires the env vars.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./cmd/ttl-handler/... ./cmd/create-handler/... ./cmd/email-create-handler/... ./cmd/budget-enforcer/... ./cmd/github-token-refresher/... ./cmd/km-slack-bridge/... && go test ./cmd/... -count=1 2>&1 | tail -20 && grep -c 'os\.Getenv("KM_' cmd/ttl-handler/main.go cmd/create-handler/main.go cmd/email-create-handler/main.go cmd/budget-enforcer/main.go cmd/github-token-refresher/main.go cmd/km-slack-bridge/main.go</automated>
  </verify>
  <done>
    - All six Lambda binaries build cleanly.
    - `grep -c 'os\.Getenv("KM_' cmd/ttl-handler/main.go cmd/create-handler/main.go cmd/email-create-handler/main.go cmd/budget-enforcer/main.go cmd/github-token-refresher/main.go cmd/km-slack-bridge/main.go` shows ≥ 1 per file.
    - `grep -nE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|ttl-handler|ttl-scheduler|at)"' cmd/{ttl,create,email-create,budget-enforcer,km-slack-bridge}/main.go` shows ONLY occurrences inside helper-function fallback `return` lines (each literal appears at most once per helper, not at multiple call sites).
    - `! grep -rn '"/km/' cmd --include='*.go' | grep -v _test.go | grep -v 'return "/km/'` — no leftover hardcoded paths outside the helper fallbacks.
    - `go test ./cmd/... -count=1` green.
  </done>
</task>

<task type="auto">
  <name>Task 3: configui kmPrefix runtime variable + cmd/configui/* migration + pkg/aws + pkg/compiler + pkg/slack/bridge</name>
  <files>cmd/configui/main.go, cmd/configui/handlers.go, cmd/configui/handlers_secrets.go, pkg/aws/cloudwatch.go, pkg/aws/scheduler.go, pkg/aws/rotation.go, pkg/compiler/lifecycle.go, pkg/compiler/service_hcl.go, pkg/compiler/userdata.go, pkg/slack/bridge/aws_adapters.go</files>
  <action>
    Three distinct subtasks bundled together because they're each small.

    **Subtask A — configui (Pitfall 5 fix):**

    1. **cmd/configui/handlers_secrets.go** (line 47): change `const kmPrefix = "/km/"` to a package-level variable initialized at startup:
       ```go
       var kmPrefix = func() string {
           if v := os.Getenv("KM_RESOURCE_PREFIX"); v != "" {
               return "/" + v + "/"
           }
           return "/km/"
       }()
       ```
       This pattern executes the func once at package init time — that's deliberate. configui is a long-running web server; reading env once at startup is correct semantics.

    2. **cmd/configui/main.go** (line 107): `"km-budgets"` literal — add a helper `budgetTable()` that reads `KM_BUDGET_TABLE` env var. Same pattern as plan 03 task 2.

    3. **cmd/configui/main.go** (line 66): `KM_BUCKET` default `"tf-km"` — add helper `tfStateBucket()`:
       ```go
       func tfStateBucket() string {
           if v := os.Getenv("KM_BUCKET"); v != "" {
               return v
           }
           prefix := "km"
           if p := os.Getenv("KM_RESOURCE_PREFIX"); p != "" {
               prefix = p
           }
           return "tf-" + prefix
       }
       ```

    4. **cmd/configui/handlers.go**: any `/km/sandboxes` log-group references — use the `kmPrefix` package-level variable (from handlers_secrets.go, since it's the same package) plus the literal log-group path suffix.

    **Subtask B — pkg/aws migration:**

    5. **pkg/aws/cloudwatch.go** (lines 68, 87): `/km/sandboxes/` log-group prefix. These are functions called from operator-side code.
       - **Decision rule:** Add a `prefix string` parameter rather than `cfg *config.Config` (preserves clean `pkg/` → `internal/` dependency direction). Caller passes `cfg.GetResourcePrefix()`.
       - The function signature change cascades: list every caller and update to pass the prefix. Use `grep -rn 'pkg/aws/cloudwatch' ./internal ./pkg ./cmd` to find callers.

    6. **pkg/aws/scheduler.go** (line 44): `"km-ttl-{sandboxID}"` — this is a per-sandbox name (Rule 4 territory) BUT the `"km-"` prefix portion is the singleton part. Wrap the prefix:
       - Before: `fmt.Sprintf("km-ttl-%s", sandboxID)`
       - After: `fmt.Sprintf("%s-ttl-%s", prefix, sandboxID)` where `prefix` is a string parameter (callers pass `cfg.GetResourcePrefix()`).
       - Update all callers.

    7. **pkg/aws/rotation.go** (1 site per RESEARCH Category B — log group): same prefix-parameter pattern.

    **Subtask C — pkg/compiler migration:**

    8. **pkg/compiler/lifecycle.go** (line 29): `"km-ttl-{sandboxID}"` — same pattern as scheduler.go. Compiler functions typically already take cfg.

    9. **pkg/compiler/service_hcl.go** (lines 316, 322): `/km/sandboxes/` log group path used in compiler-emitted HCL. Convert to use `cfg.GetResourcePrefix()`. If the function emits HCL-template strings that reference a site var, defer that part to plan 04 with a TODO marker.

    10. **pkg/compiler/userdata.go** (lines 184, 1211): bash script templates with `/km/slack/bridge-url` references. These are HEREDOC sandbox-side bash that read SSM at runtime on the sandbox. **Decision rule:** if the HEREDOC interpolates a Go string, replace with `cfg.GetSsmPrefix()`. If it's a literal bash string the sandbox reads directly, defer to plan 04 with TODO marker (the bash script needs to read its prefix from `/etc/km/notify.env` instead of hardcoding).

    **Subtask D — pkg/slack/bridge:**

    11. **pkg/slack/bridge/aws_adapters.go**: 2 sites are doc comments per RESEARCH. Update the comments to reference `cfg.GetSsmPrefix() + "slack/..."` instead of literal `"/km/slack/..."` for documentation accuracy. No code change.

    **Validation:**
    - `go build ./pkg/... ./cmd/... ./internal/...` — must pass after every subtask.
    - `go test ./pkg/... ./cmd/configui/... ./internal/... -count=1` — green.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && go build ./... && go test ./pkg/... ./cmd/configui/... ./internal/... -count=1 2>&1 | tail -20 && grep -c 'KM_RESOURCE_PREFIX\|KM_BUCKET\|kmPrefix' cmd/configui/main.go cmd/configui/handlers_secrets.go && (! grep -nE '"km-ttl-%s"\|"km-budget-%s"' pkg/compiler/lifecycle.go pkg/aws/scheduler.go)</automated>
  </verify>
  <done>
    - `cmd/configui/handlers_secrets.go` has `var kmPrefix = ...` (variable, not const) reading `KM_RESOURCE_PREFIX`.
    - `cmd/configui/main.go` has `tfStateBucket()` and `budgetTable()` env-var helpers.
    - `pkg/aws/cloudwatch.go` and `pkg/aws/scheduler.go` accept a prefix parameter; callers updated.
    - `pkg/aws/rotation.go` log-group migrated.
    - `pkg/compiler/lifecycle.go` and `service_hcl.go` use `cfg.GetResourcePrefix()` for non-template-emitting code; template-emit cases marked TODO for plan 04.
    - `pkg/compiler/userdata.go` bash HEREDOCs migrated where Go-side interpolation is possible; sandbox-side bash literal cases marked TODO for plan 04.
    - `pkg/slack/bridge/aws_adapters.go` doc comments updated for accuracy.
    - `go build ./...` succeeds.
    - All test suites green.
  </done>
</task>

</tasks>

<verification>
After all three tasks, the operator-side and Lambda-side migrations are complete. Plan-completion gates:

1. `go build ./...` — succeeds.
2. `make build` — produces a working bin/km (per CLAUDE.md feedback).
3. `go test ./internal/... ./pkg/... ./cmd/... -count=1` — green.
4. Grep audit (VALIDATION.md 66-03-02 gate):
   - `! grep -rnE '"km-(budgets|sandboxes|identities|schedules|slack-bridge-nonces|slack-threads|slack-stream-messages|ttl-handler|ttl-scheduler|create-handler|email-create-handler|slack-bridge|ecs-spot-handler|at)"' ./cmd ./internal ./pkg --include='*.go' | grep -v _test.go | grep -v 'return "km-'` — only literals in helper-function fallback returns remain.
   - `! grep -rn '"/km/' ./cmd ./internal ./pkg --include='*.go' | grep -v _test.go | grep -v '"/opt/km' | grep -v '"~/.km' | grep -v 'return "/km/'` — only literals in helper fallbacks remain.
5. **Phase 67/68 drift verification:**
   - `grep -n 'KM_SLACK_THREADS_TABLE' internal/app/cmd/create.go` shows the os.Setenv export (RESEARCH Pattern 7).
   - `grep -n '"km-slack-threads"' internal/app/cmd/status.go` returns no matches (Pitfall 10 fixed).
   - `grep -n 'aws\.String("km-slack-bridge")' internal/app/cmd/init.go` returns no matches (Pitfall 11 fixed).
   - `grep -n '"km-sandboxes"' internal/app/cmd/doctor.go` returns no matches outside _test.go (Phase 67 drift at 2330/2341/2392 fixed).
   - `grep -n '"/km/slack/signing-secret"' internal/app/cmd/slack.go` returns no matches outside test fixtures (Phase 67 critical drift at 759 fixed).

6. Setting `KM_TTL_HANDLER_NAME=alt-ttl-handler` env var and running ttl-handler smoke would route to the alt-prefix Lambda — verifiable via env var injection in a one-off process; not exercised in CI but the contract is in code.

Plan 03 does NOT yet make multi-instance work end-to-end — TF modules still hardcode names. Plan 04 closes that by (a) adding var.resource_prefix to the five Lambda modules + lambda-slack-bridge function_name fix, (b) extending site.hcl, (c) wiring the env vars this plan reads in the TF module env blocks.
</verification>

<success_criteria>
- All operator-side singleton km-* resource names + /km/ SSM paths migrated to cfg helpers.
- All Lambda handlers use env-var helpers with safe fallbacks.
- ttl-handler self-name (Pitfall 4) is env-driven via KM_TTL_HANDLER_NAME and KM_TTL_SCHEDULER_ROLE.
- configui kmPrefix is a runtime variable from KM_RESOURCE_PREFIX (Pitfall 5).
- pkg/aws/{cloudwatch,scheduler,rotation}.go and pkg/compiler/{lifecycle,service_hcl,userdata}.go migrated.
- **Phase 67/68 drift fixes**: status.go:468 (Pitfall 10), init.go:1724 (Pitfall 11), slack.go:759 (PersistSigningSecret), slack.go:311/328/426/461/465 (new SSM paths), doctor.go:2330/2341/2392, create.go os.Setenv exports KM_SLACK_THREADS_TABLE + KM_SLACK_STREAM_TABLE.
- Full test suite green.
- make build succeeds.
- VALIDATION.md task IDs 66-03-01 + 66-03-02 green.
</success_criteria>

<output>
After completion, create `.planning/phases/66-multi-instance-support-configurable-resource-prefix-and-email-subdomain/66-03-SUMMARY.md` describing:
- Per-file count of literals migrated (operator-side cmd/, Lambda cmd/, pkg/, configui).
- Lambda helper-function pattern (which env vars added, where).
- ttl-handler Pitfall 4 fix (self-naming via env).
- configui kmPrefix variable conversion (Pitfall 5 fix).
- **Phase 67/68 drift fixes — list each individually with before/after diffs**:
  - create.go os.Setenv additions (Pattern 7)
  - status.go:468 (Pitfall 10)
  - init.go:1724 ForceSlackBridgeColdStartWith refactor (Pitfall 11)
  - slack.go new SSM path migrations (lines 311, 328, 426, 461, 465, 759, 836)
  - doctor.go:2330/2341/2392 (km-sandboxes references)
  - PersistSigningSecret signature change (RESEARCH Open Question #5)
- pkg signature changes (prefix added to functions in pkg/aws/{cloudwatch,scheduler,rotation}.go) and downstream caller updates.
- Two grep audits and their zero-count results.
- Confirmation make build succeeds and full test suite green.
- Final state: operator + Lambda code is multi-instance ready; TF modules (plan 04) and km init flow (plan 05) remain.
</output>
</output>
