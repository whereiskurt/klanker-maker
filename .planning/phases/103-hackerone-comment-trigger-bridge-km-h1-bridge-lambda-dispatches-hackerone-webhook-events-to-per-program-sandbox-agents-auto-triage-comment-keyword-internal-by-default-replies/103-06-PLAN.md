---
phase: 103-hackerone-comment-trigger-bridge
plan: 06
type: tdd
wave: 2
depends_on: [02]
files_modified:
  - internal/app/cmd/h1.go
  - internal/app/cmd/h1_test.go
  - internal/app/cmd/root.go
autonomous: true
requirements: [H1-CLI-INIT-STATUS]

must_haves:
  truths:
    - "km h1 init mints a 32-byte hex webhook secret, stores Basic-Auth creds + secret in SSM under /{prefix}/config/h1/*, and prints the Function URL + secret for the operator to paste into HackerOne"
    - "km h1 status prints the SSM-backed H1 config with the secret REDACTED"
    - "km h1 has NO manifest subcommand (HackerOne has no App-install model)"
  artifacts:
    - path: "internal/app/cmd/h1.go"
      provides: "RunH1Init / RunH1Status + cobra wiring; Basic-Auth capture; SSM writes under /{prefix}/config/h1/*"
      contains: "config/h1/"
  key_links:
    - from: "km h1 init"
      to: "SSM /{prefix}/config/h1/{webhook-secret,api-username,api-token}"
      via: "SSM PutParameter (SecureString for secrets)"
      pattern: "config/h1/"
    - from: "km h1 init webhook-secret write"
      to: "bridge SSMSecretFetcher read (Plan 04/08)"
      via: "same SSM path — a write the bridge reads back"
      pattern: "webhook-secret"
---

<objective>
Build the operator CLI: `km h1 init` (mint webhook secret + capture Basic-Auth creds → SSM, print Function URL) and `km h1 status` (print redacted config). Forked from `internal/app/cmd/github.go`, dropping `manifest` (no App-install model). Closes the deploy-surface "SSM write must back every runtime read" loop (footgun #9): the secret the bridge reads is written here.

Purpose: Without `km h1 init`, the bridge has no webhook secret and no Basic-Auth creds in SSM — every webhook 401s and every reply fails auth. The init command is the operator's entry point.
Output: internal/app/cmd/h1.go + cobra registration + tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md

# Port source (drop manifest, add Basic-Auth capture):
@internal/app/cmd/github.go

<interfaces>
<!-- RESEARCH.md CLI surface:
  km h1 init: mint 32-byte hex webhook secret; capture api-username + api-token (Basic Auth);
    write SSM under /{prefix}/config/h1/{webhook-secret (SecureString), api-username, api-token (SecureString)};
    read the bridge Function URL (from terragrunt outputs / SSM, mirror how km github status gets bridge-url)
    and PRINT: Function URL + secret + the HackerOne Webhooks UI path to paste them
    (Engagements -> Program -> Settings -> Automation -> Webhooks). NO manifest generator.
  km h1 status: print programs/targets/handle/bridge-url from config + SSM; secret REDACTED. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunH1Init — mint secret + capture Basic-Auth + SSM writes</name>
  <files>internal/app/cmd/h1.go, internal/app/cmd/h1_test.go, internal/app/cmd/root.go</files>
  <behavior>
    - TestH1Init: writes /{prefix}/config/h1/webhook-secret (32-byte hex, SecureString), /api-username, /api-token (SecureString) via a mock SSM client; asserts the 3 PutParameter calls with correct paths + types.
    - TestH1Init_PrintsFunctionURL: the command prints the bridge Function URL + the secret + the HackerOne Webhooks UI path (capture stdout).
    - TestH1Init_NoManifest: `km h1` exposes init + status but NOT manifest (assert the subcommand set).
  </behavior>
  <action>
    Fork internal/app/cmd/github.go → internal/app/cmd/h1.go. RunGitHubInit → RunH1Init:
    - Mint a 32-byte hex webhook secret (crypto/rand).
    - Capture api-username + api-token (flags --api-username/--api-token, or interactive prompt mirroring github init's input flow).
    - SSM PutParameter to /{prefix}/config/h1/{webhook-secret, api-username, api-token} — secrets as SecureString. Inject the SSM client (interface) so tests mock it (mirror github.go's SSM seam).
    - Resolve + print the bridge Function URL (mirror how RunGitHubStatus reads bridge-url from SSM/outputs), the secret, and the HackerOne Webhooks UI paste path.
    - Register `km h1` parent cobra command with init + status subcommands in root.go (mirror the `km github` registration). DROP manifest.
    Write h1_test.go with a mock SSM client for the init path.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/app/cmd -run "TestH1Init" -count=1</automated>
  </verify>
  <done>init writes the 3 SSM params (secrets SecureString), prints Function URL + secret + paste path; no manifest subcommand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunH1Status — redacted config dump</name>
  <files>internal/app/cmd/h1.go, internal/app/cmd/h1_test.go</files>
  <behavior>
    - TestH1Status: prints programs, targets, bot_handle, bridge-url from cfg.H1 + SSM; the webhook secret + api-token are REDACTED (assert they do NOT appear in stdout).
    - TestH1Status_Dormant: absent h1: config -> a clean "not configured" message, no error.
  </behavior>
  <action>
    Fork RunGitHubStatus → RunH1Status: read cfg.H1 (from Plan 02) + the SSM /{prefix}/config/h1/* params; print programs/targets/handle/bridge-url. REDACT webhook-secret and api-token (show e.g. "****" or last-4). Dormant when h1: absent (mirror github status's dormant path). Add TestH1Status cases.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/app/cmd -run "TestH1Status" -count=1</automated>
  </verify>
  <done>status prints config with secret/token redacted; dormant when unconfigured.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd -run "TestH1Init|TestH1Status" -count=1` green.
- `make build` succeeds (km binary exposes `km h1 init|status`, no manifest).
</verification>

<success_criteria>
- init writes secret+creds to SSM (the writes the bridge reads back) and prints the Function URL.
- status redacts secrets; both dormant-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-06-SUMMARY.md`
</output>
