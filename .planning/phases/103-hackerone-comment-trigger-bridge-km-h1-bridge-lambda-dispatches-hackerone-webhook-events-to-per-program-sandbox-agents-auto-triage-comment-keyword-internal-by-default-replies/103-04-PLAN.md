---
phase: 103-hackerone-comment-trigger-bridge
plan: 04
type: tdd
wave: 2
depends_on: [02, 03]
files_modified:
  - pkg/h1/bridge/webhook_handler.go
  - pkg/h1/bridge/webhook_handler_test.go
  - pkg/h1/bridge/aws_adapters.go
  - pkg/h1/bridge/aws_adapters_test.go
autonomous: true
requirements: [H1-BRIDGE-DEDUP, H1-TRIGGER-AUTOTRIAGE, H1-TRIGGER-MENTION, H1-FANOUT-MULTITARGET, H1-DISPATCH-3WAY, H1-THREAD-CONTINUITY, H1-REPLY-RESEARCHER-GATED, H1-REPLY-INTERNAL-DEFAULT]

must_haves:
  truths:
    - "A replayed X-H1-Delivery GUID returns 200 with no dispatch (dedup via shared nonces table, h1-delivery: prefix)"
    - "An event listed in a program's events: auto-triages (dispatches); an unlisted event drops; empty events: is dormant"
    - "A comment containing @handle dispatches; absent @handle + unknown thread drops; a known report thread bypasses the @handle requirement"
    - "One trigger fans the SAME prompt to N targets: N enqueues with DISTINCT dedupIDs + N (report_id,target) thread rows"
    - "warm->FIFO enqueue, cold->EventBridge create, resume->StartInstances+enqueue, per target"
    - "/reply_to_researcher posts researcher-visible ONLY when actor in allow AND command present; otherwise downgraded to internal"
    - "Only the FIRST (primary) target may post externally; non-primary targets are forced internal"
    - "Internal error -> 200 (never 5xx); the synchronous ACK is an internal-only comment"
  artifacts:
    - path: "pkg/h1/bridge/webhook_handler.go"
      provides: "Handle() ~10-step flow: verify, event-gate, parse, loop-guard, resolve, thread-bypass, trigger-gate, authz, dedup, command-pass, fanout-dispatch, internal-ACK"
      min_lines: 200
      contains: "for _, target := range"
    - path: "pkg/h1/bridge/aws_adapters.go"
      provides: "DynamoH1ThreadStore (PK=report_id SK=target, UpdateItem), shared nonce store reuse, SQS/EventBridge/EC2-resume adapters (share or thin-wrap), H1Commenter (Basic-Auth NOT in bridge — internal ACK only)"
      contains: "H1ThreadStore"
  key_links:
    - from: "Handle() dispatch block"
      to: "targets[] fanout loop"
      via: "per-target warm/cold/resume + per-(report_id,target) thread upsert"
      pattern: "for _, target"
    - from: "/reply_to_researcher gate"
      to: "actor in program.allow"
      via: "BOTH command-present AND allowlist-membership required; else internal"
      pattern: "ReplyToResearcher"
    - from: "DynamoH1ThreadStore"
      to: "{prefix}-h1-threads"
      via: "UpdateItem (not PutItem)"
      pattern: "UpdateItem"
---

<objective>
Build the heart of the bridge: the Handle() flow that ports GitHub's 11-step pipeline with the H1 deltas — two trigger models (auto-triage event-gate + @handle comment-keyword), the multi-target fanout loop (the one genuinely-new capability), thread continuity keyed by (report_id, target), and the safety-critical reply gate. Plus the H1 AWS adapters (DynamoH1ThreadStore + shared stateless adapters).

Purpose: This is where every requirement converges. The fanout loop and the reply gate have no GitHub precedent and are the two highest-risk pieces — they get dedicated tests. The reply guard is safety-critical (researcher-visible replies message external hackers).
Output: webhook_handler.go (Handle + dispatch fanout + reply gate) and aws_adapters.go (H1ThreadStore + adapters), fully unit-tested via interface-injected mocks.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md

# Port sources (port, do not re-derive — RESEARCH.md Pattern 1 enumerates every step delta):
@pkg/github/bridge/webhook_handler.go
@pkg/github/bridge/webhook_handler_test.go
@pkg/github/bridge/webhook_handler_phase102_test.go
@pkg/github/bridge/aws_adapters.go
@pkg/github/bridge/thread_store_test.go

<interfaces>
<!-- Handle()-flow delta (RESEARCH.md Pattern 1 table). Port GitHub Handle() (webhook_handler.go:177)
     and apply these changes in order:
  1  verify x-h1-signature (reuse VerifyH1Signature). bad/absent->401; secret-fetch error->200 (Pitfall 3).
  1b read x-h1-event. ACCEPT report_comment_created OR any event in some program's events:. else 200 drop.
  2  parse H1WebhookPayload (no "action" field — event type is the discriminator).
  3  LOOP GUARD: skip if actor username == h1 api_username (the Basic-Auth identity). NO Bot type.
  4  DELETE PR-only filter.
  4.5 resolve program handle -> []Target. DELETE federation/relay branch. miss -> silent 200 drop.
  4b known-thread bypass: lookup {report_id} in h1-threads (any target row -> bypass handle requirement).
  5  comment events: ContainsHandle(body, program.bot_handle) unless thread-known.
     auto-triage events: SKIP handle check (event presence in events: IS the trigger).
  6  authz: comment-keyword -> actor in program.allow else 200 silent (deny-by-default).
     auto-triage -> does NOT gate on allow (operator's events: choice IS the authz). (OQ3 resolved.)
  7  dedupe x-h1-delivery GUID, h1-delivery: prefix, shared nonces table, 86400 TTL.
  8/9 command pass (+ agent verb) THEN dispatch wrapped in `for _, target := range targets` (Pattern 4):
     each target: warm->FIFO / cold->EventBridge / resume->StartInstances+enqueue;
     groupID="h1-{report_id}-{target.alias}", dedupID="{deliveryGUID}-{groupID}";
     upsert (report_id, target.alias) thread row (UpdateItem).
  10 ACK: internal-only "on it" comment via H1Commenter internal:true, synchronous before return. NO reaction API.
  -- DELETE Phase 101 claim-emit.

  Reply gate (Pattern 2, the safety layer — enforce here at the handler):
  - primary target = targets[0]. ONLY targets[0] may carry ReplyToResearcher=true.
  - ReplyToResearcher=true ONLY when /reply_to_researcher present AND actor in program.allow. else force internal.
  - every non-primary target's envelope forced ReplyTarget=internal regardless.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aws_adapters.go — DynamoH1ThreadStore + shared/wrapped stateless adapters</name>
  <files>pkg/h1/bridge/aws_adapters.go, pkg/h1/bridge/aws_adapters_test.go</files>
  <behavior>
    - TestThreadStore: Upsert(report_id, target, sandbox_id, session_id, agent_type) writes a row keyed PK=report_id SK=target via UpdateItem (NOT PutItem — memory project_sandboxmetadata_lossy_roundtrip); Get returns it; N targets for one report -> N rows no collision.
    - TestThreadStore_AnyTargetKnown: a lookup by report_id reports thread-known if ANY target row exists (drives the @handle bypass).
    - TestNonceStore: CheckAndStore with h1-delivery: prefix dedupes a replayed GUID (reuse the shared nonce store shape; TTL 86400).
  </behavior>
  <action>
    Fork/port pkg/github/bridge/aws_adapters.go → pkg/h1/bridge/aws_adapters.go:
    - SHARE/thin-wrap (stateless, product-agnostic per RESEARCH.md Don't-Hand-Roll): DynamoGitHubNonceStore (reuse with H1DeliveryNoncePrefix="h1-delivery:" const), DynamoAliasResolver(+WithStatus) (alias-index GSI — identical), DynamoSandboxStatusWriter, the SQS adapter, EventBridgeAdapter, EC2Resumer, SSMSecretFetcher. Prefer thin H1-named wrappers over editing the github package (avoid coupling 6 shipped phases).
    - FORK: DynamoGitHubThreadStore → DynamoH1ThreadStore with PK=report_id, SK=target.alias; all writes UpdateItem-shaped. Implement the H1ThreadStore interface from Plan 02.
    - H1Commenter (Basic-Auth) is NOT needed in the bridge for replies (replies come from the sandbox helper) — the bridge needs ONLY the internal-ACK path. Provide an H1Commenter the handler can call for the synchronous internal "on it" ack (it may post via the customer API using bridge-held Basic-Auth creds from SSM, internal:true). Keep it injectable so tests mock it.
    Write aws_adapters_test.go for the thread store + nonce prefix (mirror thread_store_test.go).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestThreadStore|TestNonceStore" -count=1</automated>
  </verify>
  <done>DynamoH1ThreadStore keyed (report_id,target) via UpdateItem; N-target rows don't collide; nonce dedup uses h1-delivery: prefix; stateless adapters shared/wrapped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle() — two-trigger gate, dedup, fanout dispatch, thread continuity</name>
  <files>pkg/h1/bridge/webhook_handler.go, pkg/h1/bridge/webhook_handler_test.go</files>
  <behavior>
    - TestHandle_Dedup: replayed x-h1-delivery -> 200, zero dispatches.
    - TestHandle_AutoTriage: event in program.events -> dispatch with the events[event].prompt; unlisted event -> 200 drop; program with empty events -> auto-triage dormant (no dispatch on that event).
    - TestHandle_Mention: report_comment_created with @handle -> dispatch; without @handle + unknown thread -> 200 drop; without @handle but KNOWN report thread -> dispatch (bypass).
    - TestHandle_LoopGuard: actor username == api_username -> 200 drop (prevents the agent's own internal comment re-triggering).
    - TestHandle_Authz: comment-keyword by actor NOT in allow -> 200 silent drop; auto-triage event by an external reporter NOT in allow -> STILL dispatches (auto-triage doesn't gate on allow, OQ3).
    - TestHandle_Fanout: program with 2 targets -> 2 enqueues with DISTINCT dedupIDs ({guid}-h1-{report}-{alias}) + 2 thread rows.
    - TestHandle_Dispatch: warm target -> SQS enqueue; cold target -> EventBridge create; resumed target -> StartInstances then enqueue (mock resolver-with-status).
    - TestHandle_InternalError200: a forced SQS/DDB error -> 200 (Pitfall 2 — never 5xx).
    - TestHandle_ACK: a successful dispatch posts exactly one INTERNAL ack comment synchronously (mock H1Commenter asserts internal:true).
  </behavior>
  <action>
    Port pkg/github/bridge/webhook_handler.go → pkg/h1/bridge/webhook_handler.go applying the Handle()-flow delta in the interfaces block above, step by step. Key implementation notes:
    - Read x-h1-event / x-h1-delivery / x-h1-signature from lowercased headers (the Lambda main.go lowercases in Plan 08).
    - Event-gate (1b): accept report_comment_created OR any event present in some resolved program's events: map; else 200 drop.
    - Trigger branch: comment-keyword path runs ContainsHandle + allow-gate + command-pass; auto-triage path skips handle + allow and uses events[event].prompt.
    - Dispatch: wrap the single-target 3-way block in `for _, target := range targets` (Pattern 4). dedupID MUST include target.alias so N targets aren't deduped to one. Upsert (report_id, target.alias) per target.
    - All interfaces injected (SecretFetcher, NonceStore, Resolver(+status), Resumer, SQSSender, EventBridgePublisher, H1ThreadStore, H1Commenter) so tests use mocks — mirror webhook_handler_test.go's injection pattern.
    Port webhook_handler_test.go and add the fanout/auto-triage/loop-guard cases.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestHandle_" -count=1</automated>
  </verify>
  <done>Handle() ports the full flow: dedup, both triggers, loop-guard, authz asymmetry, fanout with distinct dedupIDs, 3-way dispatch, thread upsert, internal-only ACK, 200-on-error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The safety-critical reply gate (internal-by-default + primary-only external)</name>
  <files>pkg/h1/bridge/webhook_handler.go, pkg/h1/bridge/webhook_handler_test.go</files>
  <behavior>
    - TestReplyGate_DefaultInternal: a trigger with NO /reply_to_researcher -> every target envelope ReplyToResearcher=false (internal).
    - TestReplyGate_AllowedPrimary: actor IN allow AND /reply_to_researcher present -> ONLY targets[0] envelope ReplyToResearcher=true; all other targets forced false.
    - TestReplyGate_NotAllowed: /reply_to_researcher present but actor NOT in allow -> ALL targets forced internal (downgraded; command-present-alone is NOT sufficient — CONTEXT lock).
    - TestReplyGate_SingleTargetExternal: with 1 target + allowed actor + command -> that target gets ReplyToResearcher=true.
    - TestReplyGate_NeverNExternal: with N targets + allowed + command -> exactly ONE envelope has ReplyToResearcher=true (no N external replies — RESEARCH.md Pattern 4).
  </behavior>
  <action>
    Implement the reply gate inside Handle()'s fanout loop (RESEARCH.md Pattern 2 + Pattern 4 — the flagged-risk resolution):
    - Compute researcherReply := commandHasReplyToResearcher AND actorInAllow (BOTH required; the allow gate is the SAME deny-by-default gate as dispatch).
    - In the fanout loop, set envelope.ReplyToResearcher = researcherReply ONLY for index 0 (the primary/first target); force false for every other index.
    - Document in a code comment: "/reply_to_researcher is honored only by the primary (first) target; other targets reply internally" (so the operator UAT runbook matches).
    Add the TestReplyGate_* cases. These are the safety-critical tests — they must be exhaustive about the downgrade and the primary-only rule.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestReplyGate" -count=1</automated>
  </verify>
  <done>Researcher-visible reply requires command AND allowlist; only the primary target may post externally; non-allowlisted /reply_to_researcher downgrades to internal; never N external replies.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/h1/bridge -count=1` green (all Handle/ThreadStore/ReplyGate tests).
- Reply-gate tests prove internal-by-default, primary-only-external, allowlist-gated downgrade.
</verification>

<success_criteria>
- Both trigger models gate correctly; dedup works; fanout produces N distinct dispatches + N thread rows.
- 3-way dispatch per target; internal-error->200; internal-only ACK.
- Safety-critical reply guard enforced at the handler (the authoritative layer).
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-04-SUMMARY.md`
</output>
