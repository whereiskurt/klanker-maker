---
phase: 103-hackerone-comment-trigger-bridge
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/report_created.json
  - .planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/report_comment_created.json
  - .planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md
  - pkg/compiler/userdata_h1_byte_identity_test.go
  - pkg/compiler/testdata/h1_byte_identity_golden.txt
autonomous: false
requirements: [H1-DEPLOY-WIRING, H1-RESOLVE-PROGRAM, H1-REPLY-INTERNAL-DEFAULT, H1-HELPER-KM-H1]
user_setup:
  - service: hackerone
    why: "Live webhook payload capture to pin MEDIUM-confidence field paths (Open Questions 1 & 2) before payload_test.go is written"
    dashboard_config:
      - task: "Fire a HackerOne Webhooks Test request against a logging endpoint to capture one report_created + one report_comment_created body"
        location: "HackerOne: Engagements -> Program -> Settings -> Automation -> Webhooks -> Test request / Recent deliveries"

must_haves:
  truths:
    - "Two real HackerOne webhook bodies are captured (one report_created, one report_comment_created)"
    - "The exact program-handle JSON path, the activity internal-flag field, and the state-change endpoint are pinned in writing"
    - "An unrelated profile's userdata renders byte-identical with no H1 poller block (dormancy baseline captured)"
  artifacts:
    - path: "103-CAPTURE/report_created.json"
      provides: "Real report_created webhook body for payload_test.go struct-tag pinning"
    - path: "103-CAPTURE/report_comment_created.json"
      provides: "Real report_comment_created webhook body for payload_test.go"
    - path: "103-CAPTURE/field-paths.md"
      provides: "Pinned program-handle path, internal-flag field, actor-username path, state endpoint"
    - path: "pkg/compiler/testdata/h1_byte_identity_golden.txt"
      provides: "Dormancy golden — userdata of an unrelated profile BEFORE the H1 poller exists"
  key_links:
    - from: "103-CAPTURE/field-paths.md"
      to: "pkg/h1/bridge/payload.go (Plan 03)"
      via: "struct tags derived from captured bytes"
      pattern: "data.report.relationships.program"
---

<objective>
Wave 0 — resolve the two MEDIUM-confidence HackerOne field-path Open Questions with a LIVE webhook capture, and capture the userdata dormancy golden BEFORE any H1 code exists. This is the prerequisite that unblocks payload parsing (Plan 03) and the byte-identity guard (Plan 08). Nothing downstream can correctly pin struct tags without the real bytes.

Purpose: Phase 103 is a port, but two payload field paths (program-handle path, internal-flag field) and the state-change endpoint are only MEDIUM/LOW confidence in the docs (RESEARCH.md Open Questions 1 & 2). Guessing them risks a silent resolve-miss on every real webhook. Capture pins them from real bytes.
Output: Two captured JSON bodies + a pinned field-paths note + a dormancy golden for the compiler.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-VALIDATION.md

# Existing byte-identity test pattern to mirror:
@pkg/compiler/userdata_phase92_byte_identity_test.go
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture two live HackerOne webhook bodies</name>
  <action>Manual operator capture (no automation possible — HackerOne has no API to self-fire a webhook). Follow how-to-verify to save two real webhook bodies verbatim. See what-built/how-to-verify below.</action>
  <what-built>Nothing automated yet — this is the one truly-manual step. HackerOne has no API to self-fire a webhook test; the operator must click "Test request" in the program Webhooks UI. Open Question 1 (program-handle path) and Open Question 2 (state endpoint) cannot be resolved from the docs.</what-built>
  <how-to-verify>
    0. TARGET: use the free HackerOne **Sandbox program** (create at https://hackerone.com/teams/new/sandbox while logged in) — it has almost all features, lets you submit your own fake/test reports, and supports the same webhooks (X-H1-Signature) + customer API, with ZERO contact with real researchers or real programs. Do NOT use any real production program as the webhook/UAT target — those touch a real company's reports.
    1. In the Sandbox program: Engagements -> Program -> Settings -> Automation -> Webhooks.
    2. Configure a webhook payload URL pointing at any request-logging endpoint you control (e.g. a webhook.site bin, or a throwaway logging Function URL). Set "send everything" or at minimum report_created + report_comment_created.
    3. Click "Test request" to fire a report_created. Then submit a test report into the sandbox and add a comment to fire report_comment_created (or use the Recent-Deliveries replay if available).
    4. From the Recent-Deliveries inspector (or your logging endpoint), copy the FULL raw JSON body of each event and save them VERBATIM to:
       - 103-CAPTURE/report_created.json
       - 103-CAPTURE/report_comment_created.json
    5. Also note the captured request headers (X-H1-Event, X-H1-Delivery, X-H1-Signature) for at least one delivery.
  </how-to-verify>
  <resume-signal>Type "captured" once both JSON files are saved verbatim, or "blocked: <reason>" if you cannot access a live program (a redacted/synthetic capture from the HackerOne docs payload example is an acceptable fallback — say so and the parse will be made tolerant per RESEARCH.md OQ1).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin field paths from the captured bytes</name>
  <files>103-CAPTURE/field-paths.md</files>
  <action>
    PRE-CONFIRMED FROM LIVE REST API (2026-06-09, customer API read-only check — these are the report-object paths; the webhook nests the report under a `data.report` wrapper but the INNER report-object paths are the same):
    - Program handle = `…report.relationships.program.data.attributes.handle` (OQ1 CONFIRMED — matched the candidate exactly).
    - Activity internal flag = `…attributes.internal` (boolean; `activity-comment` type). Internal comments carry `internal:true`. This is the safety-critical visibility field — CONFIRMED.
    - Actor username = `…relationships.actor.data.attributes.username` — CONFIRMED.
    - Report id = top-level report-object `id`; comment body = `…attributes.message`; report attrs include `title`, `state` — CONFIRMED.
    The ONLY things the live WEBHOOK capture still must pin: the envelope WRAPPER (`data.report` vs `data.report.data` and `data.activity` placement), the header casing, and the state-change endpoint (OQ2). Seed field-paths.md with the confirmed inner paths above, then correct/extend from the captured webhook bytes.
    Read the two captured JSON bodies. Write 103-CAPTURE/field-paths.md pinning, with exact JSON paths copied from the real bytes:
    - **Program handle path** (RESEARCH.md OQ1 candidate: `data.report.relationships.program.data.attributes.handle`). Record the ACTUAL path. If the real path differs, that is the authoritative one downstream plans use.
    - **Report id path** (candidate `data.report.id`).
    - **Report attributes used for {{field}} pre-expansion**: title, state (candidate `data.report.attributes.{title,state}`).
    - **Activity actor username path** (candidate `data.activity.relationships.actor.data.attributes.username`) — this is the loop-guard identity AND the allowlist key.
    - **Activity internal flag** (candidate `data.activity.attributes.internal`) — confirms whether incoming comments expose visibility.
    - **Comment body / message path** (candidate `data.activity.attributes.message`).
    - **Header names confirmed:** X-H1-Event, X-H1-Delivery, X-H1-Signature (note the exact casing observed; the Lambda lowercases all headers anyway).
    - **State-change endpoint (OQ2):** record what the customer-API reference shows for the live program (`POST /reports/{id}/state_changes` vs `PATCH /reports/{id}`). If unconfirmed, mark it LOW-confidence and note `km-h1 state` may ship as a fast-follow (it is the least-critical verb).
    Note explicitly whether the capture was a real live program or a synthetic/docs fallback (drives parse-tolerance in Plan 03).
  </action>
  <verify>
    <automated>test -s "$(git rev-parse --show-toplevel)/.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md" && grep -q "handle" "$(git rev-parse --show-toplevel)/.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md"</automated>
  </verify>
  <done>field-paths.md pins program-handle path, report-id path, actor-username path, internal-flag field, comment-body path, and the state-endpoint determination — each marked real-capture or synthetic-fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Capture userdata dormancy golden (pre-H1 baseline)</name>
  <files>pkg/compiler/userdata_h1_byte_identity_test.go, pkg/compiler/testdata/h1_byte_identity_golden.txt</files>
  <action>
    Mirror pkg/compiler/userdata_phase92_byte_identity_test.go exactly. Pick an unrelated profile (one with NO HackerOne anything — e.g. a minimal restricted-dev or the existing github-review baseline) and compile its userdata NOW, before any H1 poller heredoc exists. Write the rendered userdata to pkg/compiler/testdata/h1_byte_identity_golden.txt and add TestUserdataH1ByteIdentity in pkg/compiler/userdata_h1_byte_identity_test.go that compiles the SAME profile and asserts byte-equality with the golden.
    The dormancy invariant this guards (verified in Plan 08): after the H1 poller block is added to userdata.go in Plan 07, this test MUST still pass for the unrelated profile — i.e. the H1 poller heredoc only appears when notification.h1.inbound.enabled is true. Capturing the golden now (before the code) is what makes the post-change assertion meaningful.
    Use the SAME capture-vs-verify dual-mode harness as the phase92 test (an env flag or const that regenerates the golden vs verifies it) so re-baselining is explicit.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/compiler -run TestUserdataH1ByteIdentity -count=1</automated>
  </verify>
  <done>TestUserdataH1ByteIdentity passes green against the freshly-captured golden; the test compiles an H1-free profile and asserts byte-identical userdata.</done>
</task>

</tasks>

<verification>
- Both 103-CAPTURE/*.json bodies saved verbatim (or a documented synthetic fallback).
- field-paths.md pins every path Plan 03 needs.
- `go test ./pkg/compiler -run TestUserdataH1ByteIdentity -count=1` green.
</verification>

<success_criteria>
- Open Questions 1 & 2 resolved (or explicitly marked synthetic-fallback with parse-tolerance noted).
- Dormancy golden captured before any H1 code exists.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-01-SUMMARY.md`
</output>
