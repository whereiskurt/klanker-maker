---
phase: 103-hackerone-comment-trigger-bridge
plan: 02
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - internal/app/config/config.go
  - internal/app/config/config_h1_test.go
  - pkg/h1/bridge/resolve.go
  - pkg/h1/bridge/resolve_test.go
  - pkg/h1/bridge/interfaces.go
autonomous: true
requirements: [H1-RESOLVE-PROGRAM, H1-EVENT-PROMPT-MAP, H1-DEPLOY-WIRING]

must_haves:
  truths:
    - "h1: config block in km-config.yaml unmarshals into H1Config (not silently dropped)"
    - "A report's program handle resolves to its targets[], allow[], events map, and commands map"
    - "A program handle not in config resolves to a miss (handler will 200-drop)"
    - "Empty/absent events: leaves a program comment-keyword-only (dormant auto-triage)"
  artifacts:
    - path: "internal/app/config/config.go"
      provides: "H1Config / H1ProgramEntry / H1Target / H1CommandEntry structs + GetH1* getters + h1 merge-list entry"
      contains: "H1Config"
    - path: "pkg/h1/bridge/resolve.go"
      provides: "ProgramEntry, Resolve(handle) -> ([]Target, allow, events, commands, ok), ContainsHandle, ExtractBody"
      exports: ["Resolve", "ContainsHandle"]
    - path: "pkg/h1/bridge/interfaces.go"
      provides: "Forked interfaces (SecretFetcher, DeliveryNonceStore reuse-shaped, SandboxAliasResolver(+WithStatus), SandboxResumer, EventBridgePublisher, SQSSender, H1ThreadStore, H1Commenter); NO PeerRelayer/BotLoginFetcher/Reactor"
  key_links:
    - from: "config.Load() merge-list"
      to: "cfg.H1"
      via: "v.UnmarshalKey(\"h1\", &cfg.H1) gated by the \"h1\" merge entry"
      pattern: "UnmarshalKey\\(\"h1\""
    - from: "pkg/h1/bridge/resolve.go Resolve()"
      to: "H1Config.Programs"
      via: "program-handle lookup"
      pattern: "Resolve"
---

<objective>
Build the routing foundation: the `h1:` config surface and the `Resolve(programHandle)` function that maps a report's program to its multi-target dispatch config. This is the deny-by-default routing key (program handle replaces GitHub's owner/repo), and the source of the events→prompt and commands→prompt maps consumed by the handler in Plan 04.

Purpose: Without config + resolve, the handler has nothing to route to. The merge-list entry is a known silent-drop footgun (memory project_config_key_merge_list) — struct+getter alone is NOT enough.
Output: H1 config structs + getters + merge-list wiring; pkg/h1/bridge resolve + interfaces.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md

# Port sources (read these — port, do not re-derive):
@pkg/github/bridge/resolve.go
@pkg/github/bridge/resolve_test.go
@pkg/github/bridge/interfaces.go
@internal/app/config/config.go

<interfaces>
<!-- CRITICAL anchors confirmed in-repo (grep verified):
config.go merge-list array contains "github" at ~line 579 — add "h1" alongside it.
config.go UnmarshalKey("github", &cfg.Github) at ~line 703 — add UnmarshalKey("h1", &cfg.H1) alongside.
config.go GithubConfig struct ~line 146; field `Github GithubConfig` at ~line 391 — mirror for H1Config.
GithubRepoEntry / GithubCommandEntry are the templates for H1ProgramEntry / H1CommandEntry. -->

Recommended YAML shape (RESEARCH.md Pattern 3 — h1.programs[].events + .commands are SEPARATE maps):
```yaml
h1:
  bot_handle: "@km"
  default_profile: h1-triage
  programs:
    - handle: acme-corp        # routing key = data.report program handle
      targets:                 # multi-target fanout (each = alias+profile)
        - {alias: h1-acme-triage, profile: h1-triage}
        - {alias: h1-acme-dupe-check, profile: h1-triage}
      allow: [alice, bob]      # HackerOne usernames, deny-by-default
      bot_handle: "@acmebot"   # optional per-program override of install bot_handle
      events:                  # auto-triage map (DORMANT if absent/empty)
        report_created: {prompt: "Triage new report {{report_id}}: {{title}}."}
      commands:                # comment-context command map
        dupe: {description: "Check for duplicates", prompt: "Search prior reports for duplicates of {{args}}"}
      default_command: ""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H1 config structs + getters + merge-list wiring</name>
  <files>internal/app/config/config.go, internal/app/config/config_h1_test.go</files>
  <behavior>
    - Test: a km-config.yaml fragment with the h1: block (above shape) loads into cfg.H1 with programs, targets, allow, events map, commands map all populated.
    - Test (the footgun guard): WITHOUT the "h1" merge-list entry the unmarshal yields an empty H1Config — assert the populated case proves the merge entry is present.
    - Test: GetH1Config()/GetH1Programs() (or equivalent getters) return the parsed values; absent h1: block => zero-value H1Config (dormant, no error).
    - Test: per-program bot_handle overrides install-wide bot_handle; absent => install default.
  </behavior>
  <action>
    Fork GithubConfig → H1Config in internal/app/config/config.go (mirror the struct at ~line 146 and the `Github GithubConfig` field at ~line 391):
    - H1Config: BotHandle string, DefaultProfile string, Programs []H1ProgramEntry (mapstructure/yaml tags).
    - H1ProgramEntry: Handle, Targets []H1Target, Allow []string, BotHandle string (override), Events map[string]H1EventEntry, Commands map[string]H1CommandEntry, DefaultCommand string.
    - H1Target: Alias, Profile.
    - H1EventEntry: Prompt string. H1CommandEntry: Description, Prompt (mirror GithubCommandEntry).
    - Add `H1 H1Config` field to the Config struct.
    - **CRITICAL (memory project_config_key_merge_list):** add "h1" to the merge-list array at config.go ~line 579 (alongside "github") AND add `v.UnmarshalKey("h1", &cfg.H1)` alongside the github unmarshal at ~line 703. Mirror the github comments documenting WHY the merge entry is mandatory.
    - Add getters mirroring the Github getters (GetH1Config / GetH1BotHandle / GetH1DefaultProfile / GetH1Programs as needed by Plans 06/07).
    Write config_h1_test.go covering the behaviors above.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/app/config -run H1 -count=1</automated>
  </verify>
  <done>h1: yaml round-trips into cfg.H1 with all sub-maps; the merge-list entry is present (proven by the populated test); absent h1: => dormant zero-value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pkg/h1/bridge resolve + interfaces</name>
  <files>pkg/h1/bridge/resolve.go, pkg/h1/bridge/resolve_test.go, pkg/h1/bridge/interfaces.go</files>
  <behavior>
    - TestResolve: known program handle -> returns its []Target, allow[], events map, commands map, ok=true.
    - TestResolve: unknown handle -> ok=false (handler 200-drops).
    - TestResolve: program with empty/absent events -> events map empty (auto-triage dormant); commands still present.
    - TestResolve: default target alias derivation when a target omits alias -> "h1-"+handle (analog of GitHub defaultAlias "gh-"+...).
    - TestContainsHandle: body containing bot_handle literal -> true; absent -> false; per-program bot_handle override respected.
    - TestExtractBody: returns the comment message minus the handle token (mirror ExtractMentionBody).
  </behavior>
  <action>
    Fork pkg/github/bridge/resolve.go → pkg/h1/bridge/resolve.go:
    - RepoEntry → ProgramEntry; Resolve(fullName) → Resolve(handle string) returning ([]Target, allow []string, events map, commands map, ok bool) — multi-target (GitHub returned a single alias).
    - defaultAlias("gh-"+...) → "h1-"+handle.
    - ContainsMention(botLogin) → ContainsHandle(body, botHandle) — literal config-string match (HackerOne has no bot user to @-mention; the handle is a literal substring per CONTEXT). ExtractMentionBody → ExtractBody.
    - Resolve reads from the config-derived program set (accept the parsed []H1ProgramEntry or a bridge-local mirror — keep the bridge package decoupled from internal/app/config by defining its own ProgramEntry/Target the handler is fed; the Lambda main.go (Plan 07/08 wiring) translates SSM/env config into these).
    Fork pkg/github/bridge/interfaces.go → pkg/h1/bridge/interfaces.go:
    - KEEP (shape-reuse): SecretFetcher, DeliveryNonceStore, SandboxAliasResolver(+WithStatus), SandboxResumer, SandboxStatusWriter, EventBridgePublisher, SQSSender.
    - DROP: PeerRelayer, BotLoginFetcher, GitHubReactor (federation + bot-login + reaction — all out of scope for H1).
    - FORK: GitHubThreadStore → H1ThreadStore (key = report_id + target). CommentPoster → H1Commenter (adds `internal bool`).
    - All thread/status writes MUST be UpdateItem-shaped (memory project_sandboxmetadata_lossy_roundtrip — no full-row PutItem).
    Port resolve_test.go → resolve_test.go for the behaviors above.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestResolve|TestContainsHandle|TestExtractBody" -count=1</automated>
  </verify>
  <done>Resolve maps handle→multi-target config; miss→ok=false; ContainsHandle/ExtractBody work on literal handle; interfaces.go forked with federation/reactor/bot-login dropped and H1ThreadStore/H1Commenter added.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/config -run H1 -count=1` green (merge-list proven).
- `go test ./pkg/h1/bridge -run "TestResolve|TestContainsHandle|TestExtractBody" -count=1` green.
</verification>

<success_criteria>
- h1: config is NOT silently dropped (merge-list entry present and tested).
- Program handle resolves to multi-target config; misses drop.
- Interfaces forked: no PeerRelayer/BotLoginFetcher/Reactor; H1ThreadStore + H1Commenter present.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-02-SUMMARY.md`
</output>
