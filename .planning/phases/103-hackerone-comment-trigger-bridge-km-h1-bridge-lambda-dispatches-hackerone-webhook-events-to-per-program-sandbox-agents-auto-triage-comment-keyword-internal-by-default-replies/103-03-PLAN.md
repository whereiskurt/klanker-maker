---
phase: 103-hackerone-comment-trigger-bridge
plan: 03
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - pkg/h1/bridge/payload.go
  - pkg/h1/bridge/payload_test.go
  - pkg/h1/bridge/commands.go
  - pkg/h1/bridge/commands_test.go
autonomous: true
requirements: [H1-BRIDGE-HMAC, H1-COMMAND-PARSE, H1-AGENT-VERB, H1-EVENT-PROMPT-MAP]

must_haves:
  truths:
    - "A captured real HackerOne webhook body parses into H1WebhookPayload (program handle, report id, actor username, comment body all extracted)"
    - "VerifyH1Signature accepts a correct sha256=<hex> HMAC of the raw (base64-decoded) body and rejects a tampered one"
    - "Two distinct /commands in one body -> MultiError (reply, no dispatch); zero/one command -> ok"
    - "/claude and /codex select the turn agent; conflicting verbs -> error; /reply_to_researcher is a reserved token"
    - "ExpandTemplate fills {{args}} and the fixed report-field refs ({{report_id}}, {{title}}, {{state}}, {{program}})"
  artifacts:
    - path: "pkg/h1/bridge/payload.go"
      provides: "H1WebhookPayload (data.activity + data.report), H1Envelope, VerifyH1Signature, field accessors"
      contains: "H1Envelope"
    - path: "pkg/h1/bridge/commands.go"
      provides: "ParseCommands, RunCommandPass, ExpandTemplate, agent-verb parse, /reply_to_researcher + /claude + /codex + /help reserved tokens"
      contains: "reply_to_researcher"
  key_links:
    - from: "VerifyH1Signature"
      to: "raw base64-decoded request body"
      via: "HMAC-SHA256 over decoded bytes (NOT the base64 string)"
      pattern: "hmac"
    - from: "payload struct tags"
      to: "103-CAPTURE/field-paths.md"
      via: "tags derived from real captured bytes"
      pattern: "report"
---

<objective>
Build the HackerOne-specific data layer: payload parse (struct tags pinned from the Wave-0 capture), the H1Envelope carried to the poller, the HMAC verifier (header-swapped, base64-aware), and the command/agent-verb parser (reused near-verbatim from GitHub Phase 99/102 plus the new /reply_to_researcher reserved token).

Purpose: This is the genuinely-new HackerOne surface (payload shape + envelope) plus the reused-verbatim command engine. It depends ONLY on Wave-0's pinned field paths — independent of config/resolve, so it runs parallel with Plan 02.
Output: payload.go (+ VerifyH1Signature), commands.go, with port-mirrored tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/report_created.json
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/report_comment_created.json

# Port sources (port, do not re-derive):
@pkg/github/bridge/payload.go
@pkg/github/bridge/commands.go
@pkg/github/bridge/commands_test.go

<interfaces>
<!-- VerifyH1Signature (RESEARCH.md Code Examples) — byte-identical scheme to GitHub's
     VerifyGitHubSignature (webhook_handler.go:612). sha256=<hex(HMAC-SHA256(rawBody, secret))>. -->
H1Envelope fields (RESEARCH.md): source="hackerone", program, report_id, kind
  (report_created|report_comment_created|...), activity_id, report_url, actor/sender,
  body (extracted prompt), agent, reply_target flags (ReplyToResearcher bool — ZERO VALUE = internal).
Reserved command tokens: /help, /claude, /codex, /reply_to_researcher (the H1 addition).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H1WebhookPayload + H1Envelope + VerifyH1Signature</name>
  <files>pkg/h1/bridge/payload.go, pkg/h1/bridge/payload_test.go</files>
  <behavior>
    - TestParsePayload (report_comment_created.json): extracts program handle, report id, actor username, comment message — using the EXACT struct tags from 103-CAPTURE/field-paths.md.
    - TestParsePayload (report_created.json): extracts program handle, report id, title, state (for {{field}} pre-expansion); no activity-comment message required.
    - TestParseTolerant: if field-paths.md marked the capture synthetic-fallback, parse tries the candidate path then falls back to any `handle` (RESEARCH.md OQ1) — assert both shapes parse.
    - TestVerifyH1Signature: correct sha256=<hex> over raw bytes -> nil; tampered body -> error; wrong-format header (no sha256= prefix) -> error; constant-time compare used.
    - TestVerifyH1Signature_Base64: HMAC computed over the BASE64-DECODED body, not the encoded string (Pitfall 1 — the silent-fail trap). Provide a base64-encoded body and assert verification succeeds only after decode.
  </behavior>
  <action>
    Create pkg/h1/bridge/payload.go:
    - H1WebhookPayload: top-level `data` -> Activity + Report. Struct tags COPIED from 103-CAPTURE/field-paths.md (program handle, report id/title/state, actor username, internal flag, comment message). Where field-paths.md flagged a synthetic capture, add fallback extraction (try the candidate path, else scan for `handle`).
    - Accessor methods: ProgramHandle(), ReportID(), ActorUsername(), CommentBody(), Title(), State() — the handler/template layer consumes these.
    - H1Envelope per the interfaces block above. ReplyToResearcher bool MUST default false (zero value = internal — the safety invariant; the actual gate lives in Plan 04).
    - VerifyH1Signature(secret, sigHeader string, rawBody []byte) error — port VerifyGitHubSignature verbatim (RESEARCH.md Code Examples), header-name swap only. (The base64 decode lives in the Lambda main.go in Plan 08; the verifier takes already-decoded bytes — the test feeds it decoded bytes and a separate test asserts the decode contract.)
    Write payload_test.go using the captured JSON bodies as fixtures.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestParsePayload|TestParseTolerant|TestVerifyH1Signature" -count=1</automated>
  </verify>
  <done>Captured bodies parse into H1WebhookPayload via pinned tags; VerifyH1Signature accepts correct sig + rejects tampered + format-validates; base64 contract covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commands.go — parse, agent-verbs, /reply_to_researcher reserved token, ExpandTemplate</name>
  <files>pkg/h1/bridge/commands.go, pkg/h1/bridge/commands_test.go</files>
  <behavior>
    - TestParseCommands: zero commands -> default; one command -> selected; TWO distinct commands -> MultiError (reply, no dispatch) — mirror GitHub's <=1 rule.
    - TestParseCommands_StripCode: /command inside a code fence is ignored (reuse StripCode).
    - TestAgentVerb: /claude -> agent=claude; /codex -> agent=codex; both -> error; neither -> "" (handler falls back to box default).
    - TestReservedTokens: /help, /claude, /codex, /reply_to_researcher are reserved (not treated as template commands); /reply_to_researcher sets the researcher-reply intent flag (the gate that consumes it is Plan 04).
    - TestExpandTemplate: {{args}} fills from trailing text; the fixed report-field refs {{report_id}} {{title}} {{state}} {{program}} expand from supplied values; unknown {{x}} left intact or errors per GitHub behavior.
  </behavior>
  <action>
    Fork pkg/github/bridge/commands.go → pkg/h1/bridge/commands.go near-verbatim (RESEARCH.md: "reuse near-verbatim"):
    - Keep StripCode, ParseCommands, RunCommandPass, ExpandTemplate, the <=1-distinct-command MultiError, the /claude /codex /help reserved tokens and agent-verb precedence (Phase 102 logic).
    - ADD /reply_to_researcher as a reserved token; parsing it sets an intent flag carried out of ParseCommands (consumed by the reply-gate in Plan 04 — here it is parse-only).
    - Extend ExpandTemplate (or wrap it) so the fixed report-field set {{report_id}}/{{title}}/{{state}}/{{program}} pre-expands from values the handler supplies (RESEARCH.md Pattern 3 — "pre-expand a small fixed set"). {{args}} unchanged.
    Port commands_test.go and add the /reply_to_researcher + report-field cases.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./pkg/h1/bridge -run "TestParseCommands|TestAgentVerb|TestReservedTokens|TestExpandTemplate" -count=1</automated>
  </verify>
  <done>Command parser ports cleanly; <=1 command rule enforced; agent verbs select; /reply_to_researcher reserved + flagged; ExpandTemplate fills {{args}} + report-field refs.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/h1/bridge -run "TestParsePayload|TestVerifyH1Signature|TestParseCommands|TestAgentVerb|TestExpandTemplate" -count=1` green.
</verification>

<success_criteria>
- Real captured webhook bodies parse via pinned struct tags.
- HMAC verify is byte-identical to GitHub's scheme; base64-decode contract covered.
- Command engine reused with the /reply_to_researcher reserved token added.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-03-SUMMARY.md`
</output>
