---
phase: 103-hackerone-comment-trigger-bridge
plan: 08
type: execute
wave: 4
depends_on: [07]
files_modified:
  - infra/modules/dynamodb-h1-threads/v1.0.0/main.tf
  - infra/modules/dynamodb-h1-threads/v1.0.0/outputs.tf
  - infra/live/use1/dynamodb-h1-threads/terragrunt.hcl
  - internal/app/cmd/init.go
  - internal/app/cmd/init_test.go
  - internal/app/cmd/create_h1_inbound.go
  - pkg/aws/sqs.go
  - Makefile
autonomous: true
requirements: [H1-DEPLOY-WIRING, H1-DISPATCH-3WAY, H1-THREAD-CONTINUITY]

must_haves:
  truths:
    - "lambda-h1-bridge is in regionalModules() ordered after dynamodb-h1-threads + dynamodb-sandboxes + dynamodb-slack-nonces"
    - "cmd/km-h1-bridge is in lambdaBuilds() and cmd/km-h1 is in sidecarBuilds() (else zip/helper never built)"
    - "KM_H1_* env vars are exported to terragrunt; h1 programs/commands/events published to SSM /{prefix}/config/h1/*"
    - "{prefix}-h1-inbound-*.fifo queues are created with the shared DLQ + RedrivePolicy(maxReceiveCount=3)"
    - "dynamodb-h1-threads table exists (PK=report_id, SK=target)"
    - "Makefile build-lambdas target builds km-h1-bridge + km-h1 (artifact lockstep with lambdaBuilds/sidecarBuilds)"
  artifacts:
    - path: "internal/app/cmd/init.go"
      provides: "regionalModules + lambdaBuilds + sidecarBuilds + ExportTerragruntEnvVars KM_H1_* + H1 profile pre-stage + publish h1 commands/events to SSM"
      contains: "lambda-h1-bridge"
    - path: "internal/app/cmd/create_h1_inbound.go"
      provides: "provisionH1InboundQueue with DLQArn threading (poison-wedge protection)"
      contains: "DLQ"
    - path: "infra/modules/dynamodb-h1-threads/v1.0.0/main.tf"
      provides: "h1-threads table PK=report_id SK=target, TTL"
      contains: "report_id"
  key_links:
    - from: "regionalModules() lambda-h1-bridge"
      to: "infra/live/use1/lambda-h1-bridge/terragrunt.hcl"
      via: "module name string matches live unit dir"
      pattern: "lambda-h1-bridge"
    - from: "create_h1_inbound.go"
      to: "shared per-install DLQ"
      via: "RedrivePolicy maxReceiveCount=3 (memory project_inbound_poller_fifo_poison_wedge)"
      pattern: "RedrivePolicy"
    - from: "ExportTerragruntEnvVars"
      to: "terragrunt.hcl get_env KM_H1_*"
      via: "env-block change needs km init --dry-run=false not --sidecars"
      pattern: "KM_H1_"
---

<objective>
Wire the H1 bridge into the deploy machinery — the single highest-risk area (Phase 97 shipped with 7 deploy gaps). Every touchpoint from RESEARCH.md's Deploy Surface table that lives in init.go / the DDB threads module / the inbound-queue provisioner / the Makefile. Cross-referenced against six memory footguns.

Purpose: A correct bridge that init.go never deploys is worthless. This plan closes footguns #1-4, #7-8, #10-12, #14-16.
Output: dynamodb-h1-threads module + live unit; init.go wiring; create_h1_inbound; SQS helper; Makefile.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md

# Port sources:
@internal/app/cmd/init.go
@internal/app/cmd/create_github_inbound.go
@infra/modules/dynamodb-github-threads/v1.0.0/main.tf
@infra/live/use1/dynamodb-github-threads/terragrunt.hcl
@pkg/aws/sqs.go

<interfaces>
<!-- RESEARCH.md Deploy Surface (confirmed init.go anchors):
  - regionalModules(): add "lambda-h1-bridge" near the github entry (~init.go:327 region). Order it AFTER
    dynamodb-h1-threads (new), dynamodb-sandboxes (alias-index), dynamodb-slack-nonces (shared nonces),
    email-handler, lambda-slack-bridge, lambda-github-bridge; BEFORE ses (always last). Add dynamodb-h1-threads
    BEFORE lambda-h1-bridge (bridge writes to it).
  - lambdaBuilds(): add {name:"km-h1-bridge", srcDir:"cmd/km-h1-bridge"} (~init.go:2123, next to km-github-bridge).
  - sidecarBuilds(): add {name:"km-h1", srcDir:"cmd/km-h1"} (~init.go:2324, next to km-github).
  - ExportTerragruntEnvVars: export KM_H1_* (bot_handle, default_profile, ...) (~init.go:1092/1146 region).
  - H1 profile pre-staging to S3 (preStageGitHubProfiles analog, ~init.go:343 region): h1-profiles/{slug}/.km-profile.yaml.
  - Publish h1.commands+events+programs to SSM /{prefix}/config/h1/* (~init.go:617 region) — the bridge reads at cold start.
  - create_h1_inbound.go (fork create_github_inbound.go): provisionH1InboundQueue, 1800s VisibilityTimeout
    (NOT 300 — Phase 97 dup-review loops), 14d retention, ContentBasedDeduplication=false, DLQArn threaded
    (memory project_inbound_poller_fifo_poison_wedge: shared per-install DLQ + RedrivePolicy maxReceiveCount=3).
  - pkg/aws/sqs.go: add H1InboundQueueName / CreateH1InboundQueue / H1InboundDLQName (or parameterize the
    github helpers with a source arg — Claude's discretion). Reuse the shared DLQ module.
  - Makefile build-lambdas: add km-h1-bridge + km-h1 (artifact lockstep — feedback_verify_deploy_surface).
  - dynamodb-h1-threads module (fork dynamodb-github-threads/v1.0.0): PK=report_id (S), SK=target (S),
    TTL attribute, no required_providers, agent_type schema-on-write (no migration). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dynamodb-h1-threads module + live unit; create_h1_inbound + SQS DLQ helper</name>
  <files>infra/modules/dynamodb-h1-threads/v1.0.0/main.tf, infra/modules/dynamodb-h1-threads/v1.0.0/outputs.tf, infra/live/use1/dynamodb-h1-threads/terragrunt.hcl, internal/app/cmd/create_h1_inbound.go, pkg/aws/sqs.go</files>
  <action>
    1. Fork infra/modules/dynamodb-github-threads/v1.0.0 → dynamodb-h1-threads/v1.0.0: table {prefix}-h1-threads, PK=report_id (S), SK=target (S), TTL attribute, PAY_PER_REQUEST, no required_providers. agent_type is schema-on-write (no column declaration needed). Add outputs.tf (table name/arn).
    2. Fork infra/live/use1/dynamodb-github-threads/terragrunt.hcl → dynamodb-h1-threads/terragrunt.hcl sourcing v1.0.0 (include "show" in mock_outputs_allowed_terraform_commands if it has dependency blocks).
    3. pkg/aws/sqs.go: add H1InboundQueueName(prefix, sandboxID), CreateH1InboundQueue(...,dlqARN), H1InboundDLQName(prefix) — or parameterize the existing GitHub helpers with a source string (Claude's discretion; favor a thin H1 variant to avoid touching shipped GitHub paths). Reuse the shared sqs-inbound-dlq module pattern.
    4. Fork internal/app/cmd/create_github_inbound.go → create_h1_inbound.go: provisionH1InboundQueue — 1800s VisibilityTimeout, 14d retention, ContentBasedDeduplication=false, DLQArn threaded with RedrivePolicy maxReceiveCount=3 (memory project_inbound_poller_fifo_poison_wedge). Store the queue URL in km-sandboxes (slack-inbound analog) and inject as KM_H1_INBOUND_QUEUE_URL.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./... && (cd infra/modules/dynamodb-h1-threads/v1.0.0 && terraform init -backend=false >/dev/null 2>&1 && terraform validate)</automated>
  </verify>
  <done>h1-threads module validates (PK=report_id, SK=target); create_h1_inbound provisions the FIFO with DLQ+RedrivePolicy; SQS helpers added; repo builds.</done>
</task>

<task type="auto">
  <name>Task 2: init.go wiring — regionalModules + lambdaBuilds + sidecarBuilds + env + SSM publish + profile pre-stage</name>
  <files>internal/app/cmd/init.go, Makefile</files>
  <action>
    Apply every init.go touchpoint from the interfaces block (RESEARCH.md Deploy Surface rows 1,3,5,7,11,12):
    - regionalModules(): add "lambda-h1-bridge" + "dynamodb-h1-threads", ordered per the interfaces note (threads BEFORE bridge; bridge after shared deps, before ses).
    - lambdaBuilds(): add {name:"km-h1-bridge", srcDir:"cmd/km-h1-bridge"}.
    - sidecarBuilds(): add {name:"km-h1", srcDir:"cmd/km-h1"}.
    - ExportTerragruntEnvVars: export KM_H1_* (bot_handle, default_profile) from cfg.H1.
    - preStage H1 profiles to S3 (preStageGitHubProfiles analog): h1-profiles/{slug}/.km-profile.yaml for cold-create.
    - Publish h1.programs/commands/events to SSM /{prefix}/config/h1/* (the bridge reads at cold start).
    - Makefile build-lambdas: add km-h1-bridge + km-h1 build steps (artifact lockstep with lambdaBuilds/sidecarBuilds).
    Do NOT run km init here — this is code wiring only. The runbook (Plan 10) covers the actual deploy with `make build` BEFORE `km init` (memory project_make_build_precedes_km_init).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./... && make build</automated>
  </verify>
  <done>init.go lists lambda-h1-bridge + dynamodb-h1-threads in regionalModules (correctly ordered), km-h1-bridge in lambdaBuilds, km-h1 in sidecarBuilds, KM_H1_* exported, H1 profiles pre-staged, h1 config published to SSM; Makefile builds both binaries; `make build` succeeds.</done>
</task>

</tasks>

<verification>
- `go build ./...` and `make build` succeed.
- `terraform validate` passes for dynamodb-h1-threads.
- Guard tests (Plan 10) will assert the regionalModules/lambdaBuilds/sidecarBuilds/merge-list membership.
</verification>

<success_criteria>
- All init.go deploy touchpoints wired; module+live unit exist for both new modules.
- FIFO queues hardened with DLQ+RedrivePolicy; Makefile in artifact lockstep.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-08-SUMMARY.md`
</output>
