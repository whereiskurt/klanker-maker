---
phase: 103-hackerone-comment-trigger-bridge
plan: 10
type: execute
wave: 6
depends_on: [08, 09]
files_modified:
  - internal/app/cmd/init_test.go
  - internal/app/config/config_h1_test.go
  - test/e2e/h1/e2e_test.go
  - .planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-UAT.md
autonomous: false
requirements: [H1-DEPLOY-WIRING, H1-E2E]

must_haves:
  truths:
    - "A guard test fails if km-h1-bridge is dropped from lambdaBuilds or regionalModules"
    - "A guard test fails if km-h1 is dropped from sidecarBuilds"
    - "A guard test fails if the h1 merge-list entry is removed from config.Load"
    - "A guard test asserts lambda-h1-bridge is ordered after dynamodb-h1-threads in regionalModules"
    - "The deploy runbook (make build BEFORE km init; --dry-run=false NOT --sidecars) is documented and followed for the live deploy"
    - "A live HackerOne webhook test request produces an INTERNAL triage comment; an allowlisted @handle /reply_to_researcher produces a researcher-visible reply; a non-allowlisted one is downgraded"
  artifacts:
    - path: "internal/app/cmd/init_test.go"
      provides: "Guard tests mirroring TestLambdaBuildsIncludesGitHubBridge + the regionalModules ordering tests, for H1"
      contains: "km-h1-bridge"
    - path: "test/e2e/h1/e2e_test.go"
      provides: "RUN_H1_E2E=1-gated live harness (skips when unset)"
      contains: "RUN_H1_E2E"
    - path: "103-UAT.md"
      provides: "Operator UAT runbook: deploy sequence + the live trigger/reply-visibility verifications"
  key_links:
    - from: "init_test.go guard"
      to: "lambdaBuilds() + regionalModules() + sidecarBuilds()"
      via: "membership + ordering assertions"
      pattern: "km-h1-bridge"
    - from: "config_h1_test.go merge-list guard"
      to: "config.Load h1 unmarshal"
      via: "populated-h1 assertion proves the merge entry"
      pattern: "h1"
---

<objective>
The final phase gate: deploy-surface GUARD TESTS (so a future refactor can't silently re-open a Phase-97 deploy gap) and the live E2E/UAT runbook (the safety-critical reply visibility can only be confirmed in the live HackerOne UI). Per feedback_verify_deploy_surface_not_just_code, code-green != deployable — this plan proves deployable.

Purpose: Lock the deploy surface with automated guards and run the one verification that cannot be mocked — researcher-vs-internal reply visibility in a real program.
Output: guard tests + gated E2E harness + 103-UAT.md, plus the actual live deploy + UAT walkthrough.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-VALIDATION.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md

# Guard-test pattern to mirror:
@internal/app/cmd/init_test.go

<interfaces>
<!-- RESEARCH.md Deploy Surface guard tests (H1-DEPLOY-WIRING) — mirror the GitHub guards:
  - TestLambdaBuildsIncludesH1Bridge (mirror TestLambdaBuildsIncludesGitHubBridge ~init_test.go:658).
  - regionalModules ordering: lambda-h1-bridge AFTER dynamodb-h1-threads/dynamodb-sandboxes/
    dynamodb-slack-nonces/lambda-slack-bridge/lambda-github-bridge, BEFORE ses (mirror the github ordering
    tests ~init_test.go:516-598).
  - sidecarBuilds includes km-h1.
  - config merge-list: a populated h1: yaml round-trips (proves the "h1" merge entry) — in config_h1_test.go.
  Deploy runbook (RESEARCH.md Deploy Surface):
    make build                       # CRITICAL before init (footgun #6)
    make build-lambdas               # clean rebuild incl km-h1-bridge zip + km-h1 sidecar
    km init --dry-run=false          # new module + live unit + KM_H1_* env block + IAM (NOT --sidecars)
    km init --sidecars               # refresh mgmt Lambdas for the new schema field
    km h1 init ...                   # mint webhook secret + Basic-Auth -> SSM; print Function URL
    # paste Function URL + secret into HackerOne program Webhooks UI
    km destroy <id> && km create profiles/h1-triage.yaml -->

  <!-- SAFETY: the UAT target MUST be a free HackerOne **Sandbox program**
       (https://hackerone.com/teams/new/sandbox) — it supports the same webhooks +
       customer API and lets you submit your own fake/test reports, so posting comments,
       changing state, and exercising /reply_to_researcher touch NOBODY real. NEVER point
       the comment-posting UAT at any production program — that messages a
       real company's reports and real researchers. The sandbox's own API token is the
       Basic-Auth credential for km h1 init. Production programs are read-only fixtures only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy-surface guard tests + gated E2E harness</name>
  <files>internal/app/cmd/init_test.go, internal/app/config/config_h1_test.go, test/e2e/h1/e2e_test.go</files>
  <action>
    1. init_test.go: add TestLambdaBuildsIncludesH1Bridge (mirror TestLambdaBuildsIncludesGitHubBridge) asserting km-h1-bridge in lambdaBuilds() and km-h1 in sidecarBuilds(). Add a regionalModules ordering test: lambda-h1-bridge appears exactly once, AFTER dynamodb-h1-threads + dynamodb-sandboxes + dynamodb-slack-nonces + lambda-github-bridge, and BEFORE ses; dynamodb-h1-threads appears BEFORE lambda-h1-bridge.
    2. config_h1_test.go: ensure a populated h1: yaml round-trips through config.Load (proves the merge-list entry) — if Plan 02 already covers this in config_h1_test.go, extend it with an explicit "merge-list guard" comment + assertion so the guard is unmistakable.
    3. test/e2e/h1/e2e_test.go: a RUN_H1_E2E=1-gated harness (t.Skip when unset, mirror test/e2e/slack). It documents the live steps and, where automatable with live creds, drives `km-h1 read`/`comment` against the configured program. Manual UAT is acceptable per Phase-97/SLCK-09 precedent — the harness is primarily the gated skeleton + the documented assertions.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/app/cmd -run "TestLambdaBuildsIncludesH1Bridge|RegionalModules" -count=1 && go test ./internal/app/config -run H1 -count=1 && go vet ./test/e2e/h1/...</automated>
  </verify>
  <done>Guard tests fail if any deploy-surface entry is dropped or mis-ordered; E2E harness compiles and skips cleanly without RUN_H1_E2E.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green + write 103-UAT.md runbook</name>
  <files>.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-UAT.md</files>
  <action>
    Run the full suite (`make test`) and confirm green. Write 103-UAT.md containing:
    - The exact deploy command sequence (interfaces block above), calling out make build BEFORE km init (footgun #6) and --dry-run=false NOT --sidecars for the env block (footgun #7).
    - The live verification checklist (the manual-only rows from 103-VALIDATION.md): webhook accepted in Recent-Deliveries with valid sig; auto-triage produces an INTERNAL comment; @handle /command comment produces a reply; allowlisted /reply_to_researcher → researcher-visible; non-allowlisted /reply_to_researcher → downgraded to internal; multi-target fanout produces N internal analyses but exactly ONE external reply.
    - The HackerOne Webhooks UI paste location (Engagements → Program → Settings → Automation → Webhooks).
    - The rate-limit/429 watch note (Pitfall 5) and the loop-guard sanity check (a report should NOT accrue repeated identical internal comments — Pitfall 4).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && make test</automated>
  </verify>
  <done>Full suite green; 103-UAT.md documents the deploy sequence + the safety-critical live reply-visibility checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live deploy + UAT — reply visibility (cannot be mocked)</name>
  <action>Run the live deploy sequence from 103-UAT.md, then perform the six-step reply-visibility UAT in the HackerOne UI. See what-built/how-to-verify below.</action>
  <what-built>The full H1 bridge: Lambda + module + helper + poller + profile, all wired into km init. Guard tests green. The ONLY thing left is the live confirmation of reply visibility, which is observable only in the real HackerOne UI.</what-built>
  <how-to-verify>
    Follow 103-UAT.md:
    1. Run the deploy sequence (make build → make build-lambdas → km init --dry-run=false → km init --sidecars → km h1 init).
    2. Paste the printed Function URL + secret into the HackerOne program Webhooks UI; fire a Test request — confirm 200 in Recent-Deliveries.
    3. Configure an h1.programs entry with 2 targets + events:[report_created] + an allow list. Trigger report_created — confirm an INTERNAL triage comment on the report (NOT researcher-visible) on the primary target's analysis.
    4. As an allowlisted user, comment "@handle /reply_to_researcher ..." — confirm a RESEARCHER-VISIBLE reply, posted exactly ONCE (primary target only) despite 2 targets.
    5. As a NON-allowlisted user, comment "@handle /reply_to_researcher ..." — confirm it is downgraded to INTERNAL (or blocked), NOT researcher-visible.
    6. Confirm the report does not accrue repeated identical internal comments (loop guard, Pitfall 4).
  </how-to-verify>
  <resume-signal>Type "verified" if all six checks pass, or describe the failure (especially any researcher-visible reply that should have been internal — that is a P0 safety bug to fix before shipping).</resume-signal>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd -run "TestLambdaBuildsIncludesH1Bridge|RegionalModules"` + `go test ./internal/app/config -run H1` green.
- `make test` full suite green.
- Live UAT: internal-by-default + primary-only-external + non-allowlisted-downgrade all confirmed in the HackerOne UI.
</verification>

<success_criteria>
- Deploy surface locked by guards (no silent Phase-97-style regression possible).
- The safety-critical reply visibility verified live.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-10-SUMMARY.md`
</output>
