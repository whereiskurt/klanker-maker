---
phase: 103-hackerone-comment-trigger-bridge
plan: 07
type: execute
wave: 3
depends_on: [04, 06]
files_modified:
  - cmd/km-h1-bridge/main.go
  - cmd/km-h1-bridge/main_test.go
  - infra/modules/lambda-h1-bridge/v1.0.0/main.tf
  - infra/modules/lambda-h1-bridge/v1.0.0/variables.tf
  - infra/modules/lambda-h1-bridge/v1.0.0/outputs.tf
  - infra/live/use1/lambda-h1-bridge/terragrunt.hcl
autonomous: true
requirements: [H1-BRIDGE-HMAC, H1-DISPATCH-3WAY, H1-DEPLOY-WIRING]

must_haves:
  truths:
    - "The Lambda entry base64-decodes the Function URL body before HMAC and lowercases headers"
    - "lambda-h1-bridge TF module exists at v1.0.0 with a Function URL (auth NONE) and the correct IAM grants"
    - "A live terragrunt unit sources the v1.0.0 module"
  artifacts:
    - path: "cmd/km-h1-bridge/main.go"
      provides: "Lambda Function URL handler: base64-body decode, lowercase headers, wire pkg/h1/bridge.Handle with real AWS adapters"
      contains: "IsBase64Encoded"
    - path: "infra/modules/lambda-h1-bridge/v1.0.0/main.tf"
      provides: "Lambda + Function URL (auth NONE) + IAM: nonces RW, sandboxes read+alias-index, h1-threads RW, h1-inbound SQS send, EventBridge PutEvents, EC2 StartInstances, SSM read /config/h1/*"
      contains: "function_url"
    - path: "infra/live/use1/lambda-h1-bridge/terragrunt.hcl"
      provides: "Live unit sourcing v1.0.0"
      contains: "v1.0.0"
  key_links:
    - from: "cmd/km-h1-bridge/main.go"
      to: "pkg/h1/bridge.Handle"
      via: "real AWS adapters injected"
      pattern: "bridge.Handle"
    - from: "main.go HMAC path"
      to: "decoded body bytes"
      via: "decodeBase64Body before VerifyH1Signature (Pitfall 1)"
      pattern: "IsBase64Encoded"
---

<objective>
Build the Lambda entrypoint and its Terraform module + live unit. The entry wires the Plan 02-04 bridge package to real AWS adapters and handles the Function URL base64/header normalization (Pitfall 1). The TF module gives the bridge a public Function URL and the exact IAM grants it needs.

Purpose: The bridge package is inert without a Lambda main and a deployable module. This plan makes it real infrastructure. Two of the Phase-97 7-gap footguns live here: the base64-body HMAC trap and IAM-vs-runtime grant mismatches.
Output: cmd/km-h1-bridge + lambda-h1-bridge module v1.0.0 + live unit.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md

# Port sources (drop App-cred/JWT/relayer/router; keep base64+lowercase normalization):
@cmd/km-github-bridge/main.go
@infra/modules/lambda-github-bridge/v1.1.0/main.tf
@infra/modules/lambda-github-bridge/v1.1.0/variables.tf
@infra/modules/lambda-github-bridge/v1.1.0/outputs.tf
@infra/live/use1/lambda-github-bridge/terragrunt.hcl

<interfaces>
<!-- RESEARCH.md Deploy Surface rows 1,2,13 + Pitfall 1.
  main.go: decodeBase64Body + IsBase64Encoded branch (km-github-bridge main.go:336-344) — HMAC the
    DECODED bytes. Lowercase all headers. DROP readAppCredentials, JWT reactor, relayer/router env.
    Read webhook secret + Basic-Auth (for internal ACK) from SSM /{prefix}/config/h1/*. Read the h1
    command/event set + programs from SSM/env (the SSMCommandsFetcher analog). Construct real adapters
    (DynamoH1ThreadStore, shared nonce store, alias resolver+status, SQS, EventBridge, EC2Resumer) and
    call pkg/h1/bridge.Handle. Return 200 on internal error (Pitfall 2).
  TF module v1.0.0 (fork v1.1.0): Function URL auth=NONE. IAM grants (RESEARCH.md row 13): nonces RW,
    sandboxes-table read + alias-index, h1-threads RW, h1-inbound SQS send, EventBridge PutEvents,
    EC2 StartInstances, SSM read /config/h1/*. DROP App-cred/JWT SSM params, peer_bridges, default_router.
    Keep the IAM<->runtime cross-check comment pattern from the github module (init_test guards it). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cmd/km-h1-bridge/main.go — Lambda entry + base64/header normalization</name>
  <files>cmd/km-h1-bridge/main.go, cmd/km-h1-bridge/main_test.go</files>
  <action>
    Fork cmd/km-github-bridge/main.go → cmd/km-h1-bridge/main.go:
    - Keep the base64-body decode + IsBase64Encoded branch (main.go:336-344) and lowercase-headers normalization — port verbatim (Pitfall 1: HMAC the DECODED bytes).
    - DROP readAppCredentials, the JWT/installation reactor, relayer + router env parsing (federation out of scope).
    - Read from SSM: webhook secret (/{prefix}/config/h1/webhook-secret), Basic-Auth creds (for the internal-ACK H1Commenter), and the h1 programs/commands/events config (the SSMCommandsFetcher analog — init.go publishes these in Plan 08).
    - Construct the real adapters from Plan 04 (DynamoH1ThreadStore, shared nonce store w/ h1-delivery: prefix, DynamoAliasResolver+WithStatus, SQS adapter, EventBridgeAdapter, EC2Resumer, internal-ACK H1Commenter) and invoke pkg/h1/bridge.Handle.
    - Return 200 on every internal error (Pitfall 2).
    Write main_test.go covering: a base64-encoded valid-signature request reaches Handle (decode-then-verify), and an internal-error path returns 200 (mirror km-github-bridge main_test if present).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./cmd/km-h1-bridge -count=1 && go build ./cmd/km-h1-bridge</automated>
  </verify>
  <done>Lambda entry decodes base64 body before HMAC, lowercases headers, wires real adapters to Handle, returns 200 on internal error; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: lambda-h1-bridge TF module v1.0.0 + live unit</name>
  <files>infra/modules/lambda-h1-bridge/v1.0.0/main.tf, infra/modules/lambda-h1-bridge/v1.0.0/variables.tf, infra/modules/lambda-h1-bridge/v1.0.0/outputs.tf, infra/live/use1/lambda-h1-bridge/terragrunt.hcl</files>
  <action>
    Fork infra/modules/lambda-github-bridge/v1.1.0 → infra/modules/lambda-h1-bridge/v1.0.0:
    - Function URL auth=NONE (app-layer HMAC). Lambda from the km-h1-bridge zip.
    - IAM grants (RESEARCH.md row 13): nonces table RW, sandboxes table read + alias-index GSI, h1-threads table RW, h1-inbound-* SQS SendMessage, EventBridge PutEvents, EC2 StartInstances, SSM GetParameter on /{prefix}/config/h1/*.
    - DROP App-cred/JWT SSM params, peer_bridges, default_router variables.
    - Do NOT declare required_providers (memory project_terragrunt_providers_in_root — root.hcl owns the provider stanza).
    - Keep the IAM<->runtime cross-check comment block the github module has (init_test guards its presence).
    Fork infra/live/use1/lambda-github-bridge/terragrunt.hcl → infra/live/use1/lambda-h1-bridge/terragrunt.hcl sourcing v1.0.0; include "show" in mock_outputs_allowed_terraform_commands if the unit has dependency blocks (memory project_terragrunt_show_needs_mocks); declare dependencies on dynamodb-sandboxes (alias-index), dynamodb-slack-nonces (shared nonces), dynamodb-h1-threads (Plan 08), and h1-inbound where applicable.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/infra/modules/lambda-h1-bridge/v1.0.0" && terraform fmt -check=false -write=false . >/dev/null 2>&1; terraform init -backend=false >/dev/null 2>&1 && terraform validate</automated>
  </verify>
  <done>Module validates; Function URL auth NONE; IAM grants match the runtime calls; live unit sources v1.0.0; no required_providers; mock "show" present if needed.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/km-h1-bridge -count=1` green; `go build ./cmd/km-h1-bridge` clean.
- `terraform validate` passes in the module dir.
</verification>

<success_criteria>
- Lambda entry handles base64/headers correctly and wires the bridge to real adapters.
- Module + live unit exist with correct Function URL + IAM (the runtime calls all have a matching grant).
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-07-SUMMARY.md`
</output>
