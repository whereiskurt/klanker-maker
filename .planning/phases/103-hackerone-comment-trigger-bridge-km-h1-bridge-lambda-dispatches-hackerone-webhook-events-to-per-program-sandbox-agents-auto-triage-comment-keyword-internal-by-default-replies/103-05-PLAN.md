---
phase: 103-hackerone-comment-trigger-bridge
plan: 05
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - cmd/km-h1/main.go
  - cmd/km-h1/main_test.go
autonomous: true
requirements: [H1-HELPER-KM-H1, H1-REPLY-INTERNAL-DEFAULT]

must_haves:
  truths:
    - "km-h1 comment posts a HackerOne comment via Basic Auth with attributes.internal == true by DEFAULT"
    - "km-h1 comment --reply-to-researcher must be explicit to post researcher-visible (internal:false)"
    - "km-h1 read fetches a report; km-h1 state changes report state (Basic Auth)"
    - "A 429 from the HackerOne API triggers backoff/retry"
  artifacts:
    - path: "cmd/km-h1/main.go"
      provides: "comment | state | read subcommands; Basic-Auth client from SSM creds; --internal default true / --reply-to-researcher"
      contains: "reply-to-researcher"
  key_links:
    - from: "km-h1 comment default body"
      to: "attributes.internal"
      via: "default true unless --reply-to-researcher"
      pattern: "\"internal\""
---

<objective>
Build the sandbox-side helper the agent uses to talk back to HackerOne: `cmd/km-h1` with comment/state/read over HTTP Basic Auth. This is a simpler fork of `cmd/km-github` (3 verbs not 4, no App-JWT — just SSM Basic-Auth creds). The internal-by-default flag here is one of the four safety layers.

Purpose: The bridge dispatches; the agent reads + replies through this helper. It is independent of bridge internals — it only needs the Wave-0 pinned API paths (comment endpoint, internal flag, state endpoint). Runs parallel with Plans 02/03/04.
Output: cmd/km-h1/main.go + httptest-based tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CONTEXT.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-RESEARCH.md
@.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-CAPTURE/field-paths.md

# Port source (3 verbs not 4; swap App-token loader for Basic-Auth):
@cmd/km-github/main.go

<interfaces>
<!-- HackerOne customer API (RESEARCH.md Code Examples). Base URL https://api.hackerone.com/v1.
     Basic Auth: api_username:api_token (from SSM /{prefix}/config/h1/api-username, /api-token).
  POST /reports/{id}/activities  body: {"data":{"type":"activity-comment",
                                        "attributes":{"message":"...","internal":true}}}
  GET  /reports/{id}
  state change endpoint: SEE 103-CAPTURE/field-paths.md (POST /reports/{id}/state_changes vs PATCH).
     If field-paths.md marked it LOW-confidence, `state` may be a thin best-effort verb.
  429 -> backoff/retry (mirror km-github 5xx retry, 1s/2s/4s). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: km-h1 comment (internal-default) + read</name>
  <files>cmd/km-h1/main.go, cmd/km-h1/main_test.go</files>
  <behavior>
    - TestCommentInternalDefault: `km-h1 comment --report 2468 --body @file` builds POST /reports/2468/activities with attributes.internal == true (httptest server asserts the JSON body). NO flag => internal.
    - TestCommentResearcherVisible: `--reply-to-researcher` => attributes.internal == false. Must be explicit.
    - TestCommentBasicAuth: the request carries the Authorization: Basic header from the supplied creds.
    - TestRead: `km-h1 read --report 2468` issues GET /reports/2468 and prints the body.
    - TestRetry429: a server returning 429 then 200 -> helper retries with backoff and ultimately succeeds.
  </behavior>
  <action>
    Fork cmd/km-github/main.go → cmd/km-h1/main.go. Subcommand dispatch like km-github (main.go:75-94) but 3 verbs: comment | state | read.
    - Drop the GitHub App-token loader; load Basic-Auth creds from SSM (/{prefix}/config/h1/api-username + /api-token) OR from KM_H1_API_USER/KM_H1_API_TOKEN env (the poller exports these — mirror how km-github gets its token). Build an http.Client with Basic Auth.
    - `comment`: flags --report, --body (file only, per CLAUDE.md OpenSSL/stdin constraint — mirror km-github's --body file handling), --internal (default TRUE), --reply-to-researcher (sets internal=false). POST /reports/{id}/activities with the JSON body from the interfaces block. **internal defaults true at the flag AND in the JSON marshalling** (safety layer 4).
    - `read`: GET /reports/{id}, print JSON.
    - 429 backoff/retry (mirror km-github's 5xx retry ladder).
    Write main_test.go with an httptest.Server asserting method/path/auth-header/body for each verb.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./cmd/km-h1 -run "TestComment|TestRead|TestRetry429|TestCommentBasicAuth" -count=1</automated>
  </verify>
  <done>comment defaults internal:true, --reply-to-researcher flips to false; read works; Basic-Auth header present; 429 retried.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: km-h1 state (best-effort per Wave-0 confidence)</name>
  <files>cmd/km-h1/main.go, cmd/km-h1/main_test.go</files>
  <behavior>
    - TestState: `km-h1 state --report 2468 --to triaged` issues the state-change request to the endpoint pinned in 103-CAPTURE/field-paths.md (POST /reports/{id}/state_changes OR PATCH /reports/{id}); httptest asserts method+path+body.
    - If field-paths.md marked the endpoint LOW-confidence: the test still pins the chosen endpoint and a code comment notes it may need a fast-follow correction against the live program.
  </behavior>
  <action>
    Implement the `state` verb using the endpoint + body shape pinned in 103-CAPTURE/field-paths.md (RESEARCH.md OQ2 — least-critical verb; read+comment are the core path). Use Basic Auth + the same 429 retry. If field-paths.md could not confirm the endpoint, implement the most-likely candidate (POST /reports/{id}/state_changes), comment the uncertainty, and keep the verb thin so a fast-follow can correct it.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./cmd/km-h1 -run "TestState" -count=1</automated>
  </verify>
  <done>state verb issues the pinned state-change request; uncertainty (if any) documented in-code.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/km-h1 -count=1` green.
- comment internal-default is asserted at the JSON-body level (safety layer).
</verification>

<success_criteria>
- 3 verbs over Basic Auth; internal-by-default explicit at flag + JSON body; 429 backoff.
</success_criteria>

<output>
After completion, create `.planning/phases/103-hackerone-comment-trigger-bridge-km-h1-bridge-lambda-dispatches-hackerone-webhook-events-to-per-program-sandbox-agents-auto-triage-comment-keyword-internal-by-default-replies/103-05-SUMMARY.md`
</output>
