---
phase: 80.1-auto-detect-existing-oidc-provider-in-cluster-irsa-module-supporting-same-account-irsa-without-manual-flags
verified: 2026-05-12T04:17:49Z
status: passed
score: 10/10 must-haves verified
re_verification:
  previous_status: human_needed
  previous_score: 9/10
  gaps_closed:
    - "docs/k8s/README.md EntityAlreadyExists troubleshooting row updated by commit 746bb64 to reference auto-detect instead of the deprecated wildcard-namespace workaround"
  gaps_remaining: []
  regressions: []
---

# Phase 80.1 Verification Report

**Phase Goal:** Make `km cluster add` Just Work whether the EKS cluster lives in the same AWS account as the klanker install or in a different account — by auto-detecting whether an OIDC provider for the cluster's issuer URL already exists and setting `register_oidc_provider` accordingly, with `--register-oidc-provider=auto|true|false` override.

**Verified:** 2026-05-12T04:17:49Z
**Status:** passed
**Re-verification:** Yes — after gap closure (commit 746bb64)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Same-account `km cluster add` against a pre-existing OIDC provider succeeds without `EntityAlreadyExists` and surfaces the existing provider ARN as the trust Principal | VERIFIED (integration) | Plan 80.1-05 SUMMARY: Scenario A passed against account 052251888500 — `reusing existing arn:aws:iam::052251888500:oidc-provider/oidc.eks.us-east-1.amazonaws.com/id/PHASE801SMOKETEST` logged; `data.aws_iam_openid_connect_provider.existing[0]` evaluated via `count = var.register_oidc_provider ? 0 : 1` in main.tf |
| 2 | Cross-account `km cluster add` against a new issuer still registers a fresh provider (register=true path preserved) | VERIFIED (integration) | Plan 80.1-05 SUMMARY: Scenario C passed — `creating` logged, local provider registered, `km cluster rm` destroyed both role and owned provider |
| 3 | Running `km cluster add` twice against the same EKS issuer (multi-stack idempotency) succeeds for both invocations | VERIFIED (integration) | Plan 80.1-05 SUMMARY: Scenario D passed — second invocation returned `Cluster "phase801-cross" already registered` and exited 0 |
| 4 | `km cluster rm` does NOT destroy a pre-existing OIDC provider when register=false | VERIFIED (integration) | Plan 80.1-05 SUMMARY: Scenario B passed — IAM role destroyed, OIDC provider still present after `km cluster rm` |
| 5 | `go test ./internal/app/cmd/...` passes all 8 cluster tests (6 pre-existing + 2 new) | VERIFIED | Live test run: all 8 PASS — `TestGenerateClusterHCL`, `TestClusterAdd` (3 subtests), `TestClusterList`, `TestClusterRm`, `TestPersistClusters`, `TestClusterAddPersistFailure`, `TestGenerateClusterHCL_RegisterOidcProviderFalse`, `TestAutoDetectOidcProvider` (4 subtests) |
| 6 | `terraform validate` passes on cluster-irsa/v1.0.0 | VERIFIED | `Success! The configuration is valid.` confirmed live |
| 7 | `terraform validate` passes on km-operator-policy/v1.0.0 | VERIFIED | `Success! The configuration is valid.` confirmed live |
| 8 | `--register-oidc-provider` flag documented in CLAUDE.md and docs/k8s/README.md | VERIFIED | CLAUDE.md line 470: flag table row present; docs/k8s/README.md line 41: `## Same-account installation` section present |
| 9 | CLAUDE.md no longer says "Single stack per issuer URL" is a hard limitation | VERIFIED | `grep "Single stack"` on CLAUDE.md returns no matches |
| 10 | docs/k8s/README.md troubleshooting table `EntityAlreadyExists` row correctly describes the auto-detect behavior and does not suggest the deprecated workaround | VERIFIED | Commit 746bb64 rewrote line 225: row now reads "Auto-detect was bypassed (e.g. `--register-oidc-provider=true` while a provider for that URL already exists in the target account). Either remove the override to let auto-detect pick `false`, or force reuse with `--register-oidc-provider=false`. See the 'Same-account installation' section above." Old "Use one stack per cluster with wildcard `--namespace=*` instead" text is gone. |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/app/cmd/cluster.go` | OidcProviderLister interface, factory, AutoDetectOIDCProvider, GenerateClusterHCLWithOIDC, {REGISTER_OIDC_PROVIDER} template placeholder, `--register-oidc-provider` flag, RunClusterAdd 11th param | VERIFIED | All symbols confirmed at lines 37, 61-74, 135, 146-168, 173-204, 271, 282, 344 |
| `internal/app/cmd/cluster_test.go` | mockOidcLister test double; TestGenerateClusterHCL_RegisterOidcProviderFalse (live); TestAutoDetectOidcProvider (live, 4 sub-cases); all 4 RunClusterAdd call sites updated with "false" as 11th arg | VERIFIED | 0 `t.Skip` calls remain; all 8 tests PASS in live run |
| `infra/modules/cluster-irsa/v1.0.0/main.tf` | moved {} block before locals; count-gated resource + data source; oidc_provider_arn_local local; trust policy via local | VERIFIED | moved block at lines 5-8; count gates at lines 29/36/52; oidc_provider_arn_local at line 19; identifiers at line 62 |
| `infra/modules/cluster-irsa/v1.0.0/variables.tf` | register_oidc_provider bool variable with default=true; updated oidc_provider_arn description | VERIFIED | Variable at line 22, default=true confirmed; "Single stack per issuer URL" removed from description |
| `infra/modules/cluster-irsa/v1.0.0/outputs.tf` | oidc_provider_arn returns local.oidc_provider_arn_local | VERIFIED | Line 12: `value = local.oidc_provider_arn_local` |
| `infra/modules/km-operator-policy/v1.0.0/main.tf` | aws_iam_role_policy "oidc_provider" with all 6 OIDC actions (Create, Delete, Get, List, Tag, Untag) | VERIFIED | Resource at line 507; all 6 actions confirmed at lines 519-525; Resource = "*" for consistency with kms/ses_send pattern |
| `CLAUDE.md` | --register-oidc-provider flag in table, auto-detect description, km cluster rm guarantee | VERIFIED | Line 470: flag table row; line 506: full auto-detect behavior description with log format and rm guarantee |
| `docs/k8s/README.md` | Same-account installation section + accurate EntityAlreadyExists troubleshooting row | VERIFIED | Line 41: `## Same-account installation` present; line 225: EntityAlreadyExists row updated by commit 746bb64 to reference auto-detect (old deprecated workaround text removed) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `cluster.go (RunClusterAdd)` | `AutoDetectOIDCProvider` | `NewOidcProviderListerFunc(awsCfg) → lister → AutoDetectOIDCProvider(ctx, lister, oidcProviderURL)` | WIRED | Lines 380-391: factory invoked, function called, result drives registerOIDCProvider bool |
| `cluster.go (GenerateClusterHCL)` | `clusterTerragruntHCLTemplate` | `generateClusterHCLWithOIDC → strings.NewReplacer with {REGISTER_OIDC_PROVIDER}` | WIRED | Line 135: placeholder in template; lines 149-162: replacer substitutes "true"/"false"; line 421: GenerateClusterHCLWithOIDC called with resolved bool |
| `cluster_test.go (mockOidcLister)` | `cluster.go (OidcProviderLister)` | `mockOidcLister.ListOpenIDConnectProviders satisfies OidcProviderLister interface` | WIRED | Interface declared at lines 61-70 in cluster.go; mock matches signature exactly; 4 live test sub-cases call cmd.AutoDetectOIDCProvider with mock |
| `infra/modules/cluster-irsa/v1.0.0/main.tf` | `data.aws_iam_openid_connect_provider.existing[0].arn` | `local.oidc_provider_arn_local conditional — trust policy Federated principal` | WIRED | Lines 19-26: ternary local; line 62: identifiers uses local; outputs.tf line 12: output sources from local |
| `infra/modules/km-operator-policy/v1.0.0/main.tf` | `infra/modules/cluster-irsa/v1.0.0` | `module.km_operator_policy in cluster-irsa — OIDC perms land on cluster-irsa role` | WIRED | km-operator-policy resource "oidc_provider" at line 507; cluster-irsa main.tf references `module "km_operator_policy"` at line 68 |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| operator-feature-80 (synthetic) | 80.1-01, 80.1-02, 80.1-03, 80.1-04, 80.1-05 | Same-account and cross-account IRSA via auto-detect OIDC provider registration | SATISFIED | End-to-end integration test (all 4 scenarios A/B/C/D) + unit tests (8/8 pass) + terraform validate + build |

Per CONTEXT.md: `operator-feature-80` is intentionally synthetic (shared with Phase 80) and not in v1 REQUIREMENTS.md. No traceability gap.

---

### Anti-Patterns Found

No blocker anti-patterns found. No `TODO`/`FIXME`/placeholder comments in modified files. No empty handlers or stub implementations in production code paths. Both new test functions are live (no `t.Skip`).

The stale troubleshooting entry that was flagged as Info severity in the initial verification (line 225 pointing to the deprecated wildcard-namespace workaround) has been corrected by commit 746bb64. No documentation anti-patterns remain.

---

### Human Verification Required

None. All items resolved.

---

### Summary

All 10 must-haves verified. The phase goal — making `km cluster add` Just Work for same-account and cross-account IRSA scenarios — is fully implemented and validated:

- The Terraform module (`cluster-irsa/v1.0.0`) supports both modes via `register_oidc_provider` with a `moved {}` block preserving Phase 80 stacks.
- The CLI (`km cluster add`) auto-detects by calling `ListOpenIDConnectProviders` before generating `terragrunt.hcl`, with `--register-oidc-provider=auto|true|false` override.
- All 8 unit tests pass. Both new tests (`TestGenerateClusterHCL_RegisterOidcProviderFalse`, `TestAutoDetectOidcProvider`) are live with no skips.
- The 4-scenario live integration test (account 052251888500) passed for same-account reuse (A), rm-does-not-destroy-existing-provider (B), cross-account create (C), and idempotency (D).
- CLAUDE.md and docs/k8s/README.md reflect the new behavior.
- The `EntityAlreadyExists` troubleshooting row (commit 746bb64) now correctly describes auto-detect and does not suggest the deprecated workaround.

---

_Verified: 2026-05-12T04:17:49Z_
_Re-verified: 2026-05-12T (commit 746bb64 doc fix)_
_Verifier: Claude (gsd-verifier)_
