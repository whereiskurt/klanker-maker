---
phase: 80.1
slug: auto-detect-existing-oidc-provider-in-cluster-irsa-module-supporting-same-account-irsa-without-manual-flags
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 80.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Populated by the planner with concrete Task ID rows once plans are written.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.21+) for the CLI/auto-detect logic; `terraform validate` for the module change |
| **Config file** | none — Wave 0 may add a mock `OidcProviderLister` interface seam to cluster.go |
| **Quick run command** | `go test ./internal/app/cmd/ -run TestCluster -count=1` |
| **Full suite command** | `go test ./internal/app/cmd/ -count=1 && terraform -chdir=infra/modules/cluster-irsa/v1.0.0/test validate` |
| **Estimated runtime** | ~10s |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/app/cmd/ -run TestCluster -count=1`
- **After every plan wave:** Run the full suite command above
- **Before `/gsd:verify-work`:** Full suite must be green AND a real `terragrunt plan` against an account with a pre-registered OIDC provider must show `0 to add, 0 to change, 0 to destroy`
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

*Populated by the planner once plans are written. Each row maps a task to its automated verification command and the test file the verification reads. Tasks without automated verify MUST cite a Wave 0 entry that establishes the test scaffold.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| {to be filled by planner} | | | | | | | |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/app/cmd/cluster_test.go` already exists (Phase 80) — extend with `TestAutoDetectOidcProvider` and `TestGenerateClusterHCL_RegisterOidcProviderFalse` skeletons (`t.Skip("pending implementation")`) so Wave 1+ verification commands compile
- [ ] `OidcProviderLister` interface seam declared in cluster.go (3-method interface mockable by `mockClusterRunner` analog) — alternatively reuse existing interface if pkg/aws has one
- [ ] If `terraform validate` becomes a verification command for the module change task, no Wave 0 install is needed (existing Phase 80 test fixture already exists at `infra/modules/cluster-irsa/v1.0.0/test/`)

*If none of the Wave 0 entries materialize during planning (e.g. planner inlines the test additions in the implementation tasks), strike this section.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `terragrunt plan` against a Phase-80-provisioned stack shows `0 to add, 0 to change, 0 to destroy` after the `moved {}` block is added | operator-feature-80 | Requires existing Terraform state in klanker-application (cannot be unit-tested) | (1) In klanker-application, run `km cluster add --name upgrade-test ...` with the OLD module to provision a Phase-80-shaped stack. (2) Pull the new module via `git pull && make build`. (3) Run `terragrunt plan` in the stack dir. Expected: `0 to add, 0 to change, 0 to destroy` AND output mentions the `moved` block reconciliation. |
| Cross-account `km cluster add` against a fresh issuer URL still registers a new provider | operator-feature-80 | Requires a real OIDC issuer URL we haven't seen before | Pick an EKS cluster (or a synthetic URL like `SMOKE-TEST-80.1`); run `km cluster add ...`; verify log says "creating new OIDC provider"; verify `aws iam list-open-id-connect-providers` shows it post-apply. |
| Same-account `km cluster add` against an existing provider reuses it | operator-feature-80 | Requires pre-registering an OIDC provider and then asserting that `km cluster rm` does NOT destroy it | (1) Manually create an OIDC provider in klanker-application via `aws iam create-open-id-connect-provider --url ... --client-id-list sts.amazonaws.com`. (2) Run `km cluster add ...` with that URL. (3) Verify log says "reusing existing OIDC provider". (4) `terragrunt apply` succeeds. (5) Run `km cluster rm`. (6) Verify the manually-created provider STILL exists via `aws iam list-open-id-connect-providers`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter (planner toggles when Per-Task Verification Map is filled)

**Approval:** pending
