---
phase: 130-km-tunnel-reverse-tunnelled-kubectl-to-operator-only-cluster
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - docs/k8s-reverse-tunnel.md
  - .planning/phases/130-km-tunnel-reverse-tunnelled-kubectl-to-operator-only-cluster/130-UAT.md
  - CLAUDE.md
  - docs/RELEASE-HIGHLIGHTS.md
autonomous: true
requirements: [REQ-130-DOCS, REQ-130-UAT]

must_haves:
  truths:
    - "An operator standing at a machine with the VPN and kubectl can bring the tunnel up from the runbook alone, without reading Go."
    - "The UAT is a numbered sequence fixed BEFORE the operator is on the other machine, escalating from --dry-run (no VPN needed) to a real token-expiry refresh."
    - "The docs state plainly that the tunnel bypasses km's egress enforcement and that the only real control is its lifetime."
    - "CLAUDE.md gains a Phase 130 block and a `Where to look` row, matching the house format."
    - "RELEASE-HIGHLIGHTS.md describes v0.8.6 — the tunnel — not the v0.8.5 MITM content."
  artifacts:
    - docs/k8s-reverse-tunnel.md
    - .planning/phases/130-.../130-UAT.md
  key_links:
    - "The UAT's escalation order mirrors the diagnostic flags from plan 03: --dry-run needs nothing, --print-ssh needs the VPN, the real run needs both plus a sandbox."
  prohibitions:
    - "Do NOT name a real cluster, tenant, account id, or internal hostname — use k8s1.corp / example.com placeholders."
    - "Do NOT document a -N / daemon mode; it does not exist and must not read as merely unimplemented."
    - "Do NOT claim the live UAT has passed — this phase ships code-complete with UAT pending."
---

<objective>
Ship the operator-facing documentation: a `docs/k8s-reverse-tunnel.md` runbook, a
numbered live-UAT script, the CLAUDE.md phase block and lookup row, and v0.8.6
release highlights.

Purpose: the operator cannot test this phase on the development machine, so the
runbook and UAT are not an epilogue — they are the interface through which the
feature will actually first be exercised, on a different machine, possibly days
later. Fixing the bring-up sequence now is what keeps first contact from becoming
an improvisation.

Output: runbook, UAT, CLAUDE.md block, release highlights.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-km-tunnel-reverse-tunnelled-kubectl-to-operator-only-cluster/130-CONTEXT.md
@docs/superpowers/specs/2026-08-26-k8s-reverse-tunnel-design.md
@docs/mitm-intercepts.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: docs/k8s-reverse-tunnel.md operator runbook</name>
  <files>docs/k8s-reverse-tunnel.md</files>
  <read_first>
    - docs/mitm-intercepts.md — house runbook format: what it is, prerequisites, worked example, precedence, deploy surface, troubleshooting, known limits
    - docs/vscode.md — the sibling SSM-port-forward verb's doc, for tone and the ~/.km/keys cross-machine note
  </read_first>
  <behavior>
    - Covers: what it does and the three nested transports; prerequisites (VPN up, kubectl context that already works locally, a sandbox whose profile did not set vscode.enabled false); the worked `km tunnel` invocation; what gets written on the box and where; the three diagnostic flags; the browser-pops-mid-kubectl behaviour; troubleshooting; known limits.
    - States the deploy surface is `make build` alone, and explains WHY (vscode default-true, stock sshd_config, connect-time provisioning) rather than just asserting it.
    - States the egress-bypass plainly in its own section, including that the honest control is the lifetime, not a policy field.
    - Documents the KUBERNETES_EXEC_INFO / provideClusterInfo limitation.
  </behavior>
  <action>
    Write the runbook following the `docs/mitm-intercepts.md` section shape. Use
    `k8s1.corp` and `example.com` placeholders throughout — never a real cluster or
    tenant name.

    Include a short "Why not X" section covering the two rejected alternatives
    (`/etc/hosts` mapping, full AWS credentials via the container-credentials
    endpoint) so a future reader does not re-propose them.

    Troubleshooting must cover the failure the design predicts most confidently:
    `connection refused` from kubectl on the box, whose cause is a `-R` bind
    collision with another operator's tunnel — and note that
    `ExitOnForwardFailure=yes` is what turns this from silent into loud.
  </action>
  <verify>
    <automated>test -f docs/k8s-reverse-tunnel.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'egress' docs/k8s-reverse-tunnel.md` returns at least 1
    - `grep -c 'make build' docs/k8s-reverse-tunnel.md` returns at least 1
    - `grep -ci 'k8s1.corp' docs/k8s-reverse-tunnel.md` returns at least 1
    - `grep -c ' -N ' docs/k8s-reverse-tunnel.md` returns 0
  </acceptance_criteria>
  <done>An operator with the VPN and kubectl can bring the tunnel up from this document alone, and knows exactly what it does not protect.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 130-UAT.md, CLAUDE.md block, and v0.8.6 release highlights</name>
  <files>.planning/phases/130-km-tunnel-reverse-tunnelled-kubectl-to-operator-only-cluster/130-UAT.md, CLAUDE.md, docs/RELEASE-HIGHLIGHTS.md</files>
  <read_first>
    - .planning/phases/127-declarative-mitm-intercepts-profile-declared-host-to-action-/127-UAT.md — house UAT format
    - CLAUDE.md — the Phase 129 block and the `Where to look` table, for format
    - docs/RELEASE-HIGHLIGHTS.md — the current v0.8.5 content being replaced
  </read_first>
  <behavior>
    - UAT escalates: (1) `--dry-run` with no VPN, asserting the resolved target matches the operator's own kubeconfig; (2) `--print-ssh` run by hand with the VPN up; (3) the real `km tunnel`; (4) `kubectl get ns` from the box; (5) confirm `curl` to a non-allowlisted host still blocked, proving the tunnel did not widen general egress; (6) sit through a token expiry to prove the browser hop; (7) exit the shell and confirm the box socket and forward are gone.
    - Each step records expected output and has a place to paste actual output.
    - CLAUDE.md gains a Phase 130 block in house style and one `Where to look` row.
    - RELEASE-HIGHLIGHTS.md is rewritten for v0.8.6.
  </behavior>
  <action>
    Write the UAT as a numbered script with an explicit "prerequisites" preamble
    naming what the operator must have on that machine. Step 1 must be runnable
    with no VPN and no sandbox — it is the cheapest possible check that kubeconfig
    resolution is right, and it is what makes a wrong parse cost minutes instead of
    another release.

    Include step 5 deliberately: it proves the tunnel is a *narrow* hole rather than
    a general egress bypass, which is the claim the design makes and the one a
    reviewer would most reasonably doubt.

    The CLAUDE.md block follows the Phase 129 shape: what shipped, the load-bearing
    non-obvious facts, the deploy surface, and the doc pointer. Lead with the fact
    that surprises most — that the deploy surface is `make build` alone and existing
    sandboxes work without recreation — and state that live UAT is pending.

    Rewrite RELEASE-HIGHLIGHTS.md for v0.8.6. The MITM content there belongs to
    v0.8.5, which is already tagged and released; leaving it would put the previous
    release's story on this one.
  </action>
  <verify>
    <automated>test -f .planning/phases/130-km-tunnel-reverse-tunnelled-kubectl-to-operator-only-cluster/130-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 130' CLAUDE.md` returns at least 1
    - `grep -c 'k8s-reverse-tunnel.md' CLAUDE.md` returns at least 1
    - `grep -ci 'dry-run' .planning/phases/130-*/130-UAT.md` returns at least 1
    - `grep -ci 'rickroll' docs/RELEASE-HIGHLIGHTS.md` returns 0
    - `scripts/validate-all-profiles.sh` exits 0
  </acceptance_criteria>
  <done>The runbook, UAT, CLAUDE.md block, and v0.8.6 highlights are in place, and the UAT's first step needs neither VPN nor sandbox.</done>
</task>

</tasks>
