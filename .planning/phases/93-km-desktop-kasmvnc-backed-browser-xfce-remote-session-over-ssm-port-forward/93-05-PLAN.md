---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 05
type: execute
wave: 3
depends_on: ["93-01"]
files_modified:
  - internal/app/cmd/desktop.go
  - internal/app/cmd/desktop_test.go
  - internal/app/cmd/root.go
autonomous: true
requirements: [DSK-09-CLI-START, DSK-10-CLI-STATUS]
must_haves:
  truths:
    - "km desktop start <id> probes the local port, runs an SSM pre-flight KasmVNC check, prints a https://localhost:<port>/ URL + the credential, then opens a blocking SSM port-forward to remote 8444"
    - "km desktop start fails fast with a clear error if the local port is in use or the ~/.km/desktop/<id> credential file is missing"
    - "km desktop status <id> runs a one-round-trip SSM probe and prints a one-line health summary, exiting non-zero when KasmVNC is not active"
    - "km desktop is registered on the root command next to km vscode"
  artifacts:
    - path: "internal/app/cmd/desktop.go"
      provides: "NewDesktopCmd, start/status subcommands, resolveDesktopDeps, desktopStatusScript, parseDesktopStatus"
      contains: "func NewDesktopCmd"
    - path: "internal/app/cmd/root.go"
      provides: "root.AddCommand(NewDesktopCmd(cfg))"
      contains: "NewDesktopCmd"
  key_links:
    - from: "runDesktopStart"
      to: "buildPortForwardCmd(... 8444)"
      via: "blocking SSM AWS-StartPortForwardingSession to remote 8444"
      pattern: "buildPortForwardCmd"
    - from: "runDesktopStart"
      to: "~/.km/desktop/<id>"
      via: "credential file read + print"
      pattern: "\\.km.*desktop"
---

<objective>
Add the `km desktop` CLI (`start` + `status`) in a new `internal/app/cmd/desktop.go`, mirroring
`vscode.go` exactly but replacing SSH concepts with a credential-file read + URL print, remote port
8444, and a KasmVNC systemd pre-flight check. Register it next to `km vscode`.

Purpose: Give the operator the `km desktop start/status` entry points the whole phase delivers.
Output: desktop.go + green TestDesktopStart / TestDesktopStatus + root registration.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-CONTEXT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Copy vscode.go structure verbatim, then apply the documented differences:
@internal/app/cmd/vscode.go
@internal/app/cmd/vscode_test.go

<interfaces>
<!-- Reused helpers already in package cmd (do NOT reimplement): -->
ResolveSandboxID, SandboxFetcher.FetchSandbox, extractResourceID(rec.Resources, ":instance/"),
sendSSMAndWait(ctx, ssmClient, instanceID, script), buildPortForwardCmd(ctx, instanceID, region, localPort, remotePort),
defaultShellExec, ShellExecFunc, SSMSendAPI, newRealFetcher, kmaws.LoadAWSConfig.

resolveVSCodeDeps (vscode.go:102-120) is the DI template → resolveDesktopDeps (same body).

Differences from vscode.go (RESEARCH.md Pattern 8):
- NO ssh-config / UpsertHost / HostOptions
- NO newVSCodeRekeyCmd (desktop has only start + status)
- private key path → credential path: filepath.Join(home, ".km", "desktop", sandboxID); missing → clear error
- remote port "8444" (not "22"); default --local-port 8444
- print: "https://localhost:<port>/" + "user: <u>  pass: <p>" read from the credential file (split on ':')
- desktopStatusScript (RESEARCH.md): `systemctl is-active kasmvnc` + test -f /home/sandbox/.kasmpasswd
- parseDesktopStatus: descriptive errors per failure mode, e.g. inactive unit →
  "desktop not running — is spec.runtime.desktop.enabled: true set and the sandbox recreated?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: desktop.go — NewDesktopCmd, start/status, resolveDesktopDeps, status script + parser</name>
  <files>internal/app/cmd/desktop.go, internal/app/cmd/root.go</files>
  <action>
Create internal/app/cmd/desktop.go (package cmd) by copying vscode.go's structure and stripping the
rekey command + all SSH/ssh-config logic. Implement:
- `const desktopStatusScript` per the interfaces block (kasmvnc unit + kasmpasswd existence).
- `NewDesktopCmd(cfg)` → `newDesktopCmdInternal(cfg, nil, nil, nil)` with `start` + `status` subcommands.
- `--local-port` flag defaulting to 8444.
- `resolveDesktopDeps` (copy resolveVSCodeDeps body verbatim).
- `runDesktopStart`: port probe → FetchSandbox → extractResourceID(:instance/) → stat
  ~/.km/desktop/<id> (clear error if absent, mirroring the vscode key-not-found message) → read+split
  credential → sendSSMAndWait(desktopStatusScript) → parseDesktopStatus → print
  "https://localhost:<port>/" + credential lines + Ctrl-C note → buildPortForwardCmd(..., "8444")
  wired to os.Stdin/out/err → execFn (blocking).
- `runDesktopStatus`: FetchSandbox → extractResourceID → sendSSMAndWait → parseDesktopStatus →
  on success print "✓ KasmVNC desktop ready"; return error (non-zero exit) when unhealthy.
- `parseDesktopStatus(out, sandboxID)`: return descriptive errors for inactive unit / missing kasmpasswd.

In root.go: add `root.AddCommand(NewDesktopCmd(cfg))` immediately after the NewVSCodeCmd line (~88).
  </action>
  <verify>
    <automated>go build ./internal/app/cmd/... && go run ./cmd/km desktop --help 2>&1 | grep -q start || go build ./...</automated>
  </verify>
  <done>Package builds; `km desktop --help` shows start + status; root registers the command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: desktop_test.go — start + status unit tests with mocked SSM/fetcher</name>
  <files>internal/app/cmd/desktop_test.go</files>
  <behavior>
    - TestDesktopStart: local port already bound → error mentioning --local-port; missing
      ~/.km/desktop/<id> → error; healthy path (mock fetcher returns an instance, mock SSM returns
      active kasmvnc + kasmpasswd yes) prints "https://localhost:8444/" and the credential, and the
      port-forward command args contain "AWS-StartPortForwardingSession" and "8444"
    - TestDesktopStatus: active kasmvnc → prints ready, nil error; inactive kasmvnc → non-nil error
      with the desktop.enabled hint
  </behavior>
  <action>
Replace the Wave-0 skips in TestDesktopStart and TestDesktopStatus with real tests. Reuse the mock
SandboxFetcher / mock SSMSendAPI / capturing ShellExecFunc patterns from vscode_test.go (they live in
package cmd already — reference them, do not redeclare). Point HOME at t.TempDir() and write a fake
~/.km/desktop/<id> credential file for the healthy cases. To assert the port-forward args without
actually forwarding, inject a ShellExecFunc that captures the *exec.Cmd and returns nil. Bind a
listener on the chosen local port to exercise the port-in-use branch.

If the Wave-0 TestDesktopCredential stub still lives in this file and is now owned by 93-04, leave it
as-is (93-04 implements it) — coordinate by not removing it.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run 'TestDesktopStart|TestDesktopStatus' -count=1</automated>
  </verify>
  <done>TestDesktopStart and TestDesktopStatus pass; port-in-use and missing-credential error paths covered; healthy path asserts URL + credential + 8444 port-forward args.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run Desktop -count=1` green (start + status; credential test owned by 93-04).
- `go build ./...` succeeds; `km desktop start --help` and `km desktop status --help` render.
</verification>

<success_criteria>
km desktop start/status work end-to-end against mocked SSM: start probes the port, reads the
credential, runs the pre-flight, prints the localhost URL + credential, and opens a blocking 8444
port-forward; status reports one-line health and exits non-zero when unhealthy.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-05-SUMMARY.md`
</output>
