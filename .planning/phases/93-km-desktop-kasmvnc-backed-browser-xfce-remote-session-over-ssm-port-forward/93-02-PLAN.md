---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - pkg/profile/validate.go
  - pkg/profile/validate_test.go
autonomous: true
requirements: [DSK-03-VALIDATE]
must_haves:
  truths:
    - "km validate rejects mode outside {kiosk,full} with a clear field-level error"
    - "km validate rejects browsers outside {firefox,chromium,brave} and requires non-empty browsers when mode is kiosk"
    - "km validate rejects geometry that does not match ^[0-9]+x[0-9]+$"
    - "km validate emits a hard ERROR when desktop.enabled and the AMI slug is a known non-Ubuntu family, and a WARN for opaque raw AMI IDs"
  artifacts:
    - path: "pkg/profile/validate.go"
      provides: "Desktop semantic validation rules inside ValidateSemantic"
      contains: "Desktop"
  key_links:
    - from: "ValidateSemantic"
      to: "p.Spec.Runtime.Desktop"
      via: "nil-guarded desktop rule block"
      pattern: "Spec.Runtime.Desktop"
---

<objective>
Add `km validate` semantic rules for `spec.runtime.desktop`: mode enum, browsers membership,
browsers-non-empty-for-kiosk, geometry regex, and the Ubuntu-only AMI guard (hard ERROR for known
non-Ubuntu slugs when desktop is enabled; WARN for opaque raw AMI IDs we cannot inspect offline).

Purpose: Catch desktop misconfiguration at validate time, before `km create` boots a sandbox that
KasmVNC won't install on.
Output: ValidateSemantic desktop rules + green TestDesktopValidate* tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-CONTEXT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Mirror these:
@pkg/profile/validate.go

<interfaces>
<!-- From pkg/profile/validate.go -->
ValidateSemantic(p *SandboxProfile) []ValidationError  // func at validate.go:220
ValidationError has Field, Message, and IsWarning (bool) fields (IsWarning at :22-26).
Existing vscode/slack semantic checks are the placement template — add desktop rules after them.

Ubuntu guard (RESEARCH.md Pitfall 6): AMI is p.Spec.Runtime.AMI.
- isUbuntu := strings.HasPrefix(ami, "ubuntu-")  // slug path → known
- raw IDs (ami-[0-9a-f]{8,17}) are opaque offline → WARN, not ERROR
- Known non-Ubuntu slug (e.g. "amazon-linux-2023") OR empty AMI (defaults to AL2023) → ERROR
Use compiler.IsRawAMIID for raw detection, OR copy the small regex locally to avoid an import cycle
(check whether pkg/profile already imports pkg/compiler; if it does not and would create a cycle,
copy the `^ami-[0-9a-f]{8,17}$` regex into validate.go).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Desktop mode/browsers/geometry validation rules</name>
  <files>pkg/profile/validate.go, pkg/profile/validate_test.go</files>
  <behavior>
    - mode "kiosk" and "full" → no error; mode "gnome" → ERROR (field spec.runtime.desktop.mode)
    - browsers [firefox,chromium,brave] OK; browsers [edge] → ERROR; browsers [] with mode kiosk → ERROR; browsers [] with mode full → OK
    - geometry "1920x1080" OK; geometry "" (unset) OK; geometry "huge" → ERROR; geometry "1920X1080" (capital X) → ERROR
    - Rules only fire when IsDesktopEnabled is true (disabled profile → no desktop errors)
  </behavior>
  <action>
In ValidateSemantic, after the existing vscode/slack checks, add a desktop block guarded by
`if IsDesktopEnabled(p.Spec.Runtime.Desktop) { ... }`. Inside:
- mode default: treat empty mode as "kiosk"; error if resolved mode ∉ {kiosk, full}
- browsers: each entry must be in {firefox, chromium, brave}; if mode==kiosk and len(browsers)==0
  (after defaulting — note default browsers is [firefox], but if the operator explicitly sets
  `browsers: []` for kiosk, error), emit ERROR. Match the locked rule: "browsers non-empty when mode: kiosk".
- geometry: if non-empty, must match `^[0-9]+x[0-9]+$` (lowercase x); else ERROR.
Use the existing ValidationError construction style (Field path like "spec.runtime.desktop.mode").

Replace the Wave-0 skips in TestDesktopValidateMode / TestDesktopValidateBrowsers /
TestDesktopValidateGeometry with table-driven cases from the behavior block. Construct profiles
with desktop enabled via a `true` pointer.
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run 'TestDesktopValidateMode|TestDesktopValidateBrowsers|TestDesktopValidateGeometry' -count=1</automated>
  </verify>
  <done>Three TestDesktopValidate* tests pass (no longer skipped); disabled profiles produce zero desktop errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ubuntu-only AMI guard</name>
  <files>pkg/profile/validate.go, pkg/profile/validate_test.go</files>
  <behavior>
    - desktop enabled + AMI "ubuntu-24.04" → no error
    - desktop enabled + AMI "ubuntu-22.04" → no error
    - desktop enabled + AMI "amazon-linux-2023" → ERROR (IsWarning false)
    - desktop enabled + AMI "" (defaults to AL2023) → ERROR
    - desktop enabled + AMI "ami-0123456789abcdef0" (raw) → WARN (IsWarning true), not ERROR
    - desktop DISABLED + AMI "amazon-linux-2023" → no error
  </behavior>
  <action>
Inside the same `if IsDesktopEnabled(...)` block, add the AMI guard per the interfaces note:
ubuntu- slug → ok; raw AMI ID → ValidationError with IsWarning=true and a message explaining km
cannot verify the family offline; otherwise (known non-Ubuntu slug or empty AMI) → hard ERROR with
a message naming the Ubuntu 24.04/22.04 v1 constraint. Decide the import-cycle question per the
interfaces note (prefer copying the raw-AMI regex into validate.go if importing pkg/compiler from
pkg/profile would create a cycle).

Replace the Wave-0 skip in TestDesktopValidateUbuntuGuard with the six cases from the behavior block,
asserting both error presence and the IsWarning flag for the raw-AMI case.
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run TestDesktopValidateUbuntuGuard -count=1</automated>
  </verify>
  <done>TestDesktopValidateUbuntuGuard passes; non-Ubuntu slug and empty AMI produce ERROR; raw AMI produces WARN; disabled profile produces nothing.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/profile/... -run TestDesktopValidate -count=1` all green.
- `go test ./pkg/profile/... -count=1` (whole package) stays green.
</verification>

<success_criteria>
km validate enforces mode/browsers/geometry shapes and the Ubuntu-only AMI guard exactly as the
locked decisions specify, firing only when desktop is enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-02-SUMMARY.md`
</output>
