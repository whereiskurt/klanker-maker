---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 07
type: execute
wave: 5
depends_on: ["93-06"]
files_modified: []
autonomous: false
requirements: [DSK-15-TESTS]
must_haves:
  truths:
    - "The full Go test suite is green and every Desktop test named in 93-VALIDATION.md passes (not skipped)"
    - "scripts/validate-all-profiles.sh exits 0"
    - "On a live Ubuntu 24.04 sandbox, KasmVNC starts, km desktop start renders a browser-in-browser with working bidirectional clipboard, and km resume restores the session"
    - "AMI bake -> fresh create launches with packages present and a freshly-seeded per-sandbox credential"
  artifacts: []
  key_links:
    - from: "operator workstation"
      to: "live KasmVNC session"
      via: "km desktop start over SSM port-forward to remote 8444"
      pattern: "km desktop start"
---

<objective>
Final phase gate: confirm the autonomous test suite is fully green (every named Desktop test passes,
not skipped) and the inventory gate is clean, then run the live operator UAT that CI cannot exercise
(real KasmVNC boot, browser-in-browser, clipboard, resume, AMI bake, full XFCE, locked-network).

Purpose: Prove the feature works on real infrastructure with a human in the loop, per the
93-VALIDATION.md Manual-Only Verifications table.
Output: A UAT record and the rollout note executed by the operator.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-VALIDATION.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autonomous phase gate — full suite + inventory + DSK-15 green</name>
  <files></files>
  <action>
Run the full autonomous gate and confirm every named Desktop test is green (not skipped):
- `go test ./... -count=1`
- `go test ./pkg/profile/... ./pkg/compiler/... ./internal/app/cmd/... -run Desktop -count=1 -v`
  and confirm none of the 12 named functions report SKIP (all the Wave-0 stubs must now be implemented).
- `bash scripts/validate-all-profiles.sh` exits 0.
If any Desktop test still skips or any package is red, STOP and report which requirement's test is
not yet green (do not paper over with a skip). This is the DSK-15-TESTS gate.
  </action>
  <verify>
    <automated>go test ./... -count=1 && bash scripts/validate-all-profiles.sh && go test ./pkg/profile/... ./pkg/compiler/... ./internal/app/cmd/... -run Desktop -count=1 -v 2>&1 | grep -q -- '--- SKIP' && { echo 'FAIL: a Desktop test is still skipped'; exit 1; } || echo 'DSK-15 gate green'</automated>
  </verify>
  <done>Full suite green; validate-all-profiles exits 0; no Desktop test reports SKIP.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator rollout — make build && km init --sidecars</name>
  <files></files>
  <action>
Operator-run (NOT autonomous — needs AWS creds + a real apply). Refresh the management Lambdas so
remote `km create` accepts `spec.runtime.desktop` (the schema-change constraint in CLAUDE.md):
1. `make build`
2. `km init --sidecars`
3. `km doctor` — confirm healthy.
Existing sandboxes do not pick up desktop retroactively — they require `km destroy && km create`.
  </action>
  <what-built>The desktop schema addition + compiler/userdata/CLI changes need the management Lambdas refreshed so remote km create accepts spec.runtime.desktop (per the schema-change constraint in CLAUDE.md).</what-built>
  <how-to-verify>
Run on the operator workstation (cannot be done in CI — needs AWS creds + real apply):
1. `make build`
2. `km init --sidecars`
3. `km doctor` — confirm healthy.
  </how-to-verify>
  <done>`km init --sidecars` completes and `km doctor` reports healthy.</done>
  <resume-signal>Type "rolled out" once `km init --sidecars` and `km doctor` succeed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live KasmVNC UAT (kiosk, clipboard, resume, AMI bake, full XFCE, locked-network)</name>
  <files></files>
  <action>
Operator-run live UAT against a real Ubuntu 24.04 sandbox. Exercise each row of the
93-VALIDATION.md Manual-Only Verifications table and record PASS/FAIL per row in 93-07-UAT.md:
1. KasmVNC starts: `km create profiles/desktop.yaml --wait`; `km desktop status <id>` reports ready.
2. Browser-in-browser: `km desktop start <id>`; open the printed https://localhost:8444/ URL; log in
   with the printed credential; Firefox is visible and maximized (kiosk).
3. Bidirectional clipboard: copy text local->remote Firefox and remote->local; both directions work.
4. km resume restores: `km pause <id>`; `km resume <id>`; `km desktop start <id>` reconnects to the
   same session (systemd unit auto-started).
5. AMI bake -> fast create: `km ami bake <id>`; create a new sandbox from the baked AMI; packages are
   present (no reinstall) and the credential is seeded fresh (different from the source sandbox).
6. Full XFCE: a profile with `mode: full`; create; the XFCE desktop + taskbar render.
7. Locked-network first boot: a profile with a locked spec.network and no AMI fails gracefully with a
   clear error (first boot needs distro mirrors + KasmVNC release URL + browser vendor repos).
  </action>
  <what-built>KasmVNC-backed km desktop: kiosk Firefox + full XFCE rendered in the operator's local browser over an SSM port-forward, with a per-sandbox credential and boot-persistent systemd unit.</what-built>
  <how-to-verify>
Exercise each row of the 93-VALIDATION.md Manual-Only Verifications table on a live Ubuntu 24.04 sandbox:
1. KasmVNC starts: `km create profiles/desktop.yaml --wait`; `km desktop status <id>` reports ready.
2. Browser-in-browser: `km desktop start <id>`; open the printed https://localhost:8444/ URL; log in
   with the printed credential; Firefox is visible and maximized (kiosk).
3. Bidirectional clipboard: copy text local->remote Firefox and remote->local; both directions work.
4. km resume restores: `km pause <id>`; `km resume <id>`; `km desktop start <id>` reconnects to the
   same session (systemd unit auto-started).
5. AMI bake -> fast create: `km ami bake <id>`; create a new sandbox from the baked AMI; packages are
   present (no reinstall) and the credential is seeded fresh (different from the source sandbox).
6. Full XFCE: a profile with `mode: full`; create; the XFCE desktop + taskbar render.
7. Locked-network first boot: a profile with a locked spec.network and no AMI fails gracefully with a
   clear error (first boot needs distro mirrors + KasmVNC release URL + browser vendor repos).
Record PASS/FAIL per row in a 93-07-UAT.md note.
  </how-to-verify>
  <done>All seven Manual-Only Verification rows recorded PASS in 93-07-UAT.md (or failures captured for gap closure).</done>
  <resume-signal>Type "approved" with the per-row PASS/FAIL results, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
- Autonomous: `go test ./... -count=1` green; no Desktop test skipped; `bash scripts/validate-all-profiles.sh` exits 0.
- Operator: `km init --sidecars` + `km doctor` succeed.
- Live UAT: all seven Manual-Only Verification rows PASS (or failures captured for gap closure).
</verification>

<success_criteria>
The full test suite proves the autonomous slice; the operator UAT proves KasmVNC works on real
infrastructure with browser-in-browser, working clipboard, resume, AMI bake, full XFCE, and graceful
locked-network failure.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-07-SUMMARY.md`
(and a 93-07-UAT.md with the per-row live results).
</output>
