---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 06
type: execute
wave: 4
depends_on: ["93-02", "93-05"]
files_modified:
  - profiles/desktop.yaml
  - scripts/validate-all-profiles.sh
  - skills/desktop/SKILL.md
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - docs/desktop.md
  - CLAUDE.md
  - OPERATOR-GUIDE.md
autonomous: true
requirements: [DSK-12-PROFILE-EXAMPLE, DSK-13-SKILL, DSK-14-DOCS]
must_haves:
  truths:
    - "profiles/desktop.yaml is a kiosk-Firefox example that passes km validate and is in the validate-all-profiles inventory"
    - "scripts/validate-all-profiles.sh exits 0 with desktop.yaml included"
    - "A klanker:desktop skill exists alongside klanker:vscode and the plugin + marketplace versions are bumped in lockstep"
    - "docs/desktop.md, a CLAUDE.md Where-to-look row, and an OPERATOR-GUIDE.md section document km desktop"
  artifacts:
    - path: "profiles/desktop.yaml"
      provides: "kiosk-Firefox desktop example profile (apiVersion v1alpha2, ubuntu AMI, spec.runtime.desktop)"
      contains: "desktop:"
    - path: "skills/desktop/SKILL.md"
      provides: "operator-side km desktop skill"
      contains: "name: desktop"
    - path: "docs/desktop.md"
      provides: "km desktop runbook"
      contains: "km desktop"
  key_links:
    - from: "scripts/validate-all-profiles.sh"
      to: "profiles/desktop.yaml"
      via: "inventory array entry"
      pattern: "desktop.yaml"
    - from: ".claude-plugin/plugin.json + marketplace.json"
      to: "klanker:desktop skill"
      via: "lockstep version bump (cache gate)"
      pattern: "version"
---

<objective>
Ship the operator-facing deliverables: a `profiles/desktop.yaml` kiosk-Firefox example wired into
the inventory gate, a `klanker:desktop` skill (with the mandatory plugin + marketplace version bump),
and documentation (`docs/desktop.md` + CLAUDE.md row + OPERATOR-GUIDE.md section).

Purpose: Make `km desktop` discoverable, validated in the inventory gate, and documented in house style.
Output: A passing validate-all-profiles run, a new skill, a bumped plugin version, and docs.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-CONTEXT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Mirror house style:
@skills/vscode/SKILL.md
@scripts/validate-all-profiles.sh
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json

<interfaces>
<!-- Profile must be apiVersion klankermaker.ai/v1alpha2 (Phase 92 STRICT) -->
desktop block shape (from 93-01): spec.runtime.desktop.{enabled, mode, browsers, geometry}
Ubuntu AMI required (93-02 guard): set spec.runtime.ami to an ubuntu-24.04 slug so km validate passes.
Plugin/marketplace current version: "0.3.0" -> bump in lockstep (e.g. "0.4.0") per the cache gate
([[project_plugin_version_gates_cache]]).
validate-all-profiles.sh is a plain bash array of profile paths -- append profiles/desktop.yaml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profiles/desktop.yaml + inventory gate</name>
  <files>profiles/desktop.yaml, scripts/validate-all-profiles.sh</files>
  <action>
Create profiles/desktop.yaml: a minimal kiosk-Firefox example. Use apiVersion klankermaker.ai/v1alpha2,
a sensible metadata name/description, spec.runtime.ami: ubuntu-24.04 (required so the 93-02 Ubuntu
guard passes), and a desktop block:
    runtime:
      ami: ubuntu-24.04
      desktop:
        enabled: true
        mode: kiosk
        browsers: [firefox]
        geometry: 1920x1080
Fill the rest of spec with the smallest valid posture by mirroring an existing minimal profile in
profiles/ (read one first to match required fields). Add a top-of-file comment noting the first-boot
network caveat (RESEARCH.md): a fresh non-AMI boot must reach distro mirrors + the KasmVNC release URL
+ browser vendor repos; under a locked-down spec.network, allowlist those OR bake the AMI on a
permissive profile and run under a locked one.

Append profiles/desktop.yaml to the inventory array in scripts/validate-all-profiles.sh.
  </action>
  <verify>
    <automated>make build >/dev/null 2>&1; bash scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>validate-all-profiles.sh exits 0 with desktop.yaml present; km validate profiles/desktop.yaml passes.</done>
</task>

<task type="auto">
  <name>Task 2: klanker:desktop skill + plugin/marketplace version bump</name>
  <files>skills/desktop/SKILL.md, .claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
Create skills/desktop/SKILL.md mirroring skills/vscode/SKILL.md house style: frontmatter
name: desktop + a one-line description; sections for the profile field (spec.runtime.desktop.*),
operator state (~/.km/desktop/<id> credential file), one-time setup (make build && km init --sidecars),
and the km desktop start/status workflow (loopback bind, SSM port-forward sole ingress, localhost URL
+ credential, kiosk vs full, Ubuntu-only). Cross-reference klanker:init and klanker:user like the
vscode skill does.

Bump the version in BOTH .claude-plugin/plugin.json and .claude-plugin/marketplace.json in lockstep
(0.3.0 -> 0.4.0) and update their description strings to mention the desktop skill. Mandatory per the
cache gate -- clients keep the old cached version otherwise.
  </action>
  <verify>
    <automated>test -f skills/desktop/SKILL.md && python3 -c "import json; a=json.load(open('.claude-plugin/plugin.json'))['version']; b=json.load(open('.claude-plugin/marketplace.json'))['plugins'][0]['version']; assert a==b and a!='0.3.0', 'version not bumped in lockstep'; print('skill+version OK', a)"</automated>
  </verify>
  <done>skills/desktop/SKILL.md exists; plugin.json and marketplace.json versions match and are bumped off 0.3.0.</done>
</task>

<task type="auto">
  <name>Task 3: docs/desktop.md + CLAUDE.md row + OPERATOR-GUIDE.md section</name>
  <files>docs/desktop.md, CLAUDE.md, OPERATOR-GUIDE.md</files>
  <action>
Create docs/desktop.md: an operator runbook covering profile config (spec.runtime.desktop.*), engine
(KasmVNC, why), the two modes (kiosk vs full), credential lifecycle (~/.km/desktop/<id>, seeded fresh
at boot, never baked), km desktop start/status usage with the localhost URL, security model
(loopback-only bind + SSL-off justified by the SSM tunnel), Ubuntu-only constraint, the first-boot
network caveat + AMI-bake workflow, and km resume behavior. Use generic placeholders (no real
tenants/account IDs) per house docs convention.

Add a "Where to look" row to CLAUDE.md mapping "Run a remote browser/desktop in a sandbox via km
desktop" -> docs/desktop.md and the klanker:desktop skill. Add a short CLI block entry for
km desktop start/status under the CLI section if appropriate.

Add an OPERATOR-GUIDE.md section for km desktop mirroring the existing km vscode section structure.
  </action>
  <verify>
    <automated>test -f docs/desktop.md && grep -q "km desktop" docs/desktop.md && grep -q "desktop" CLAUDE.md && grep -qi "km desktop" OPERATOR-GUIDE.md && echo docs OK</automated>
  </verify>
  <done>docs/desktop.md exists and mentions km desktop; CLAUDE.md has a desktop Where-to-look reference; OPERATOR-GUIDE.md has a km desktop section.</done>
</task>

</tasks>

<verification>
- `bash scripts/validate-all-profiles.sh` exits 0 (desktop.yaml included and valid).
- `skills/desktop/SKILL.md` exists; plugin.json and marketplace.json versions match and are off 0.3.0.
- `docs/desktop.md`, CLAUDE.md row, and OPERATOR-GUIDE.md section all reference km desktop.
</verification>

<success_criteria>
profiles/desktop.yaml validates and is in the inventory gate; the klanker:desktop skill ships with a
lockstep plugin version bump; km desktop is documented in docs/desktop.md, CLAUDE.md, and OPERATOR-GUIDE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-06-SUMMARY.md`
</output>
