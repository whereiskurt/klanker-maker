---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 03
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - pkg/compiler/service_hcl.go
  - pkg/compiler/userdata.go
  - pkg/compiler/userdata_test.go
autonomous: true
requirements: [DSK-05-COMPILER-THREAD, DSK-06-USERDATA-INSTALL, DSK-07-USERDATA-SESSION, DSK-11-SECURITY]
must_haves:
  truths:
    - "When a profile enables desktop, the generated EC2 userdata contains an idempotent KasmVNC install block, credential seed, kasmvnc.yaml, xstartup, and a systemd unit"
    - "kiosk mode xstartup launches matchbox-window-manager + browsers[0]; full mode xstartup execs startxfce4"
    - "The seeded kasmvnc.yaml binds interface 127.0.0.1 and sets require_ssl: false (loopback + SSM justification)"
    - "A disabled (or absent) desktop block emits no desktop userdata at all"
    - "KasmVNC install is guarded by `command -v vncserver` so a baked AMI skips reinstall but credential+config are always reseeded"
  artifacts:
    - path: "pkg/compiler/service_hcl.go"
      provides: "DesktopKasmUser/DesktopKasmPass fields on NetworkConfig"
      contains: "DesktopKasm"
    - path: "pkg/compiler/userdata.go"
      provides: "userDataParams desktop fields + the {{- if .DesktopEnabled }} template block"
      contains: "DesktopEnabled"
  key_links:
    - from: "generateUserData"
      to: "profile.IsDesktopEnabled(p.Spec.Runtime.Desktop)"
      via: "params.DesktopEnabled assignment"
      pattern: "IsDesktopEnabled"
    - from: "userdata template desktop block"
      to: "kasmvncpasswd seeding"
      via: "DesktopKasmUser/DesktopKasmPass interpolation"
      pattern: "kasmvncpasswd"
---

<objective>
Thread the desktop fields through the compiler (`NetworkConfig` + `userDataParams`) and add the
idempotent, AMI-bakeable KasmVNC userdata block: install-if-absent packages, always-reseed
credential + kasmvnc.yaml + xstartup (kiosk vs full), and the systemd unit. This is the core new
engineering of the phase; everything else mirrors `km vscode`.

Purpose: Turn an enabled desktop profile into a sandbox that boots a loopback-bound KasmVNC session.
Output: Compiler threading + userdata template block + green TestUserDataDesktop* golden tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-CONTEXT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Mirror these (read the VSCode block and km-queue.service pattern carefully):
@pkg/compiler/service_hcl.go
@pkg/compiler/userdata.go
@pkg/compiler/userdata_test.go

<interfaces>
<!-- Exact mirror points from RESEARCH.md Code Examples -->
NetworkConfig struct: service_hcl.go ~line 683 (VSCodeSSHPubKey ~line 704) →
  add: DesktopKasmUser string; DesktopKasmPass string
userDataParams struct: userdata.go ~line 3620 (VSCode fields ~lines 3636-3642) →
  add: DesktopEnabled bool; DesktopMode string; DesktopBrowsers []string; DesktopGeometry string;
       DesktopKasmUser string; DesktopKasmPass string
generateUserData (~line 4315): set params.Desktop* from p.Spec.Runtime.Desktop and NetworkConfig.
  params.DesktopEnabled = profile.IsDesktopEnabled(p.Spec.Runtime.Desktop)
  Default mode "kiosk" if empty; default browsers [firefox] if empty; default geometry "1920x1080".
Template desktop block: add after the VSCode {{- end }} (~line 2497). km-queue.service systemd unit
  pattern is at userdata.go ~lines 2464-2477.

KasmVNC specifics (verified in RESEARCH.md):
- deb: kasmvncserver_${UBUNTU_CODENAME}_1.4.0_amd64.deb from kasmtech/KasmVNC releases v1.4.0
- packages: dbus-x11 fonts-dejavu fonts-liberation x11-xserver-utils; matchbox-window-manager (kiosk)
  / xfce4 xfce4-goodies (full); firefox via ppa:mozillateam/ppa pinned DEB; chromium via ppa:xtradeb/apps;
  brave via official APT repo (see RESEARCH.md install snippet)
- credential seed: printf '%s\n%s\n' PASS PASS | kasmvncpasswd -u USER -w -r /home/sandbox/.kasmpasswd
- kasmvnc.yaml: network.interface 127.0.0.1; network.ssl.require_ssl false; network.websocket_port auto;
  desktop.resolution.width/height parsed from geometry; clipboard both directions enabled
- xstartup kiosk: unset SESSION_MANAGER/DBUS_SESSION_BUS_ADDRESS; export+mkdir XDG_RUNTIME_DIR;
  dbus-launch matchbox-window-manager + browsers[0]
- xstartup full: dbus-launch --exit-with-session startxfce4
- systemd unit: system service User=sandbox, ExecStart vncserver :1 -fg, enable+start (survives km resume)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread desktop fields through NetworkConfig + userDataParams</name>
  <files>pkg/compiler/service_hcl.go, pkg/compiler/userdata.go</files>
  <action>
In service_hcl.go: add `DesktopKasmUser string` and `DesktopKasmPass string` to `NetworkConfig`
immediately after `VSCodeSSHPubKey`, with a doc comment noting they are per-sandbox, never baked.

In userdata.go: add the six desktop fields to `userDataParams` (after the VSCode fields). In
`generateUserData`, set them from the profile + NetworkConfig:
- DesktopEnabled = profile.IsDesktopEnabled(p.Spec.Runtime.Desktop)
- DesktopMode = mode-or-"kiosk"; DesktopBrowsers = browsers-or-["firefox"]; DesktopGeometry = geometry-or-"1920x1080"
- DesktopKasmUser/Pass from network.DesktopKasmUser/Pass
Provide a helper to expose `browsers[0]` to the template (e.g. a computed `DesktopBrowser0` field set
during generateUserData, or use `index .DesktopBrowsers 0` in the template — pick one and be consistent
with how the golden test asserts it).
Do NOT add the template block yet (Task 2) — this task is pure plumbing so the package still compiles.
  </action>
  <verify>
    <automated>go build ./pkg/compiler/...</automated>
  </verify>
  <done>pkg/compiler builds with the new struct fields and generateUserData assignments; no template block referenced yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KasmVNC userdata template block (install + credential + config + xstartup + systemd)</name>
  <files>pkg/compiler/userdata.go, pkg/compiler/userdata_test.go</files>
  <behavior>
    - DesktopEnabled=false → generated userdata contains NO "kasmvnc"/"kasmvncpasswd"/"vncserver" strings (TestUserDataDesktopDisabled)
    - DesktopEnabled=true → userdata contains the deb URL, `command -v vncserver` guard, and kasmvncpasswd seed (TestUserDataDesktopEnabled)
    - mode kiosk → xstartup contains "matchbox-window-manager" and launches browsers[0] (firefox) (TestUserDataDesktopKiosk)
    - kiosk kasmvnc.yaml contains "127.0.0.1" and "require_ssl: false" (TestUserDataDesktopKiosk — also covers DSK-11)
    - mode full → xstartup contains "startxfce4" and installs xfce4 (TestUserDataDesktopFull)
    - credential seed line interpolates DesktopKasmUser + DesktopKasmPass (TestUserDataDesktopCredentialSeed)
  </behavior>
  <action>
Add the `{{- if .DesktopEnabled }} ... {{- end }}` block after the VSCode block (~line 2497),
following RESEARCH.md "generateUserData desktop template block". Structure:
1. Install-if-absent guarded by `if ! command -v vncserver`: apt deps, fetch+install KasmVNC deb by
   detected UBUNTU_CODENAME, {{- if eq .DesktopMode "kiosk" }} matchbox {{- end }} /
   {{- if eq .DesktopMode "full" }} xfce4 xfce4-goodies {{- end }}, and a
   {{- range .DesktopBrowsers }} block installing firefox/chromium/brave per RESEARCH.md snippet.
2. Always reseed credential: kasmvncpasswd from DesktopKasmUser/Pass, chmod 600, chown sandbox.
3. Always write /home/sandbox/.vnc/kasmvnc.yaml with interface 127.0.0.1, require_ssl false,
   websocket_port auto, resolution parsed from DesktopGeometry (split on "x"), clipboard both directions.
4. Always write /home/sandbox/.vnc/xstartup — kiosk vs full per DesktopMode (use the RESEARCH.md
   Pattern 3/4 bodies); chmod +x; chown -R sandbox.
5. Write /etc/systemd/system/kasmvnc.service (system service, User=sandbox, vncserver :1 -fg),
   daemon-reload, enable, start.
Keep the block heredoc/template style consistent with the surrounding VSCode/km-queue userdata.

Then replace the Wave-0 skips in all five TestUserDataDesktop* tests with golden assertions per the
behavior block. Build a desktop-enabled SandboxProfile + NetworkConfig{DesktopKasmUser:"kasm",
DesktopKasmPass:"testpass"} fixture, call generateUserData, and assert substrings (use
strings.Contains style consistent with TestUserDataVSCode*). For the disabled test assert ABSENCE.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... -run TestUserDataDesktop -count=1</automated>
  </verify>
  <done>All five TestUserDataDesktop* tests pass; disabled profile emits nothing; kiosk/full xstartup, kasmvnc.yaml security keys, and credential seed all assert correctly.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/compiler/... -count=1` (whole package) green, including all TestUserDataDesktop* and the existing Phase 92 byte-identity tests (desktop block is gated, so disabled-profile userdata is unchanged).
- `go build ./...` succeeds.
</verification>

<success_criteria>
An enabled desktop profile compiles into idempotent, AMI-bakeable userdata that installs KasmVNC,
seeds a per-sandbox credential, writes a loopback-bound non-SSL kasmvnc.yaml, the correct kiosk/full
xstartup, and a boot-persistent systemd unit; a disabled profile emits no desktop userdata.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-03-SUMMARY.md`
</output>
