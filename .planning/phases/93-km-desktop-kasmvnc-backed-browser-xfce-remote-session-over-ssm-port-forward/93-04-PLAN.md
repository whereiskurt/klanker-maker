---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 04
type: execute
wave: 3
depends_on: ["93-03"]
files_modified:
  - internal/app/cmd/create.go
  - internal/app/cmd/create_test.go
autonomous: true
requirements: [DSK-08-CREDENTIAL]
must_haves:
  truths:
    - "km create generates a per-sandbox KasmVNC credential (user + random password) when the profile enables desktop"
    - "The credential is stored at ~/.km/desktop/<sandbox-id> in user:pass format with mode 0600"
    - "The credential is threaded into NetworkConfig.DesktopKasmUser/DesktopKasmPass so the compiler seeds it at boot"
    - "On remote create the credential is passed to the Lambda subprocess via env vars (not regenerated), mirroring KM_VSCODE_SSH_PUBKEY"
    - "A desktop-disabled profile generates no credential file"
  artifacts:
    - path: "internal/app/cmd/create.go"
      provides: "Desktop credential generation + threading at both local and remote create call sites"
      contains: "desktop"
  key_links:
    - from: "create.go (local create)"
      to: "~/.km/desktop/<id> + NetworkConfig.DesktopKasm*"
      via: "crypto/rand password + os.WriteFile + field assignment"
      pattern: "\\.km.*desktop|DesktopKasm"
    - from: "create.go (remote create)"
      to: "Lambda subprocess env"
      via: "KM_DESKTOP_KASM_USER / KM_DESKTOP_KASM_PASS pass-through"
      pattern: "KM_DESKTOP_KASM"
---

<objective>
Generate the per-sandbox KasmVNC credential at `km create` (when desktop is enabled), store it at
`~/.km/desktop/<sandbox-id>`, thread it into the compiler's NetworkConfig, and pass it through to
the Lambda subprocess on remote creates — exactly mirroring the VSCode keypair lifecycle.

Purpose: Each sandbox gets a unique KasmVNC credential the operator can read; it is never baked into
an AMI (seeded fresh at boot by the 93-03 userdata block).
Output: Credential generation at both create call sites + green TestDesktopCredential.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Mirror the VSCode keypair generation exactly:
@internal/app/cmd/create.go

<interfaces>
<!-- From RESEARCH.md Pattern 7 + Pitfall 7 -->
VSCode keypair generation: create.go lines 616-638 (local) and 2057-2073 (remote) — TWO call sites.
Remote env pass-through: KM_VSCODE_SSH_PUBKEY at create.go:621-624 — desktop uses
  KM_DESKTOP_KASM_USER + KM_DESKTOP_KASM_PASS analogously.

Target (mirror create.go:616-638):
```go
if profile.IsDesktopEnabled(resolvedProfile.Spec.Runtime.Desktop) {
    homeDir, err := os.UserHomeDir() // handle err
    desktopDir := filepath.Join(homeDir, ".km", "desktop")
    os.MkdirAll(desktopDir, 0o700)
    credPath := filepath.Join(desktopDir, sandboxID)
    user := "kasm"
    pass := <crypto/rand base62, 16 chars>
    os.WriteFile(credPath, []byte(user+":"+pass), 0o600)
    network.DesktopKasmUser = user
    network.DesktopKasmPass = pass
    fmt.Fprintf(os.Stderr, "  + Desktop credential written to %s\n", credPath)
}
```
On the remote-create path: if KM_DESKTOP_KASM_USER/PASS are set in the subprocess env, use them
instead of regenerating (so local and remote agree on the credential). At the local call site that
spawns the Lambda subprocess, set those env vars after generating.
NetworkConfig.DesktopKasmUser/DesktopKasmPass were added in 93-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate + store + thread desktop credential at km create (local + remote)</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/create_test.go</files>
  <behavior>
    - desktop-enabled profile → ~/.km/desktop/<id> written as "kasm:<password>" with mode 0600
    - generated password is non-empty, random (two creates produce different passwords), and safe (base62, no ':' or newline)
    - NetworkConfig.DesktopKasmUser=="kasm" and DesktopKasmPass==<generated> after generation
    - desktop-disabled profile → no ~/.km/desktop/<id> file created, NetworkConfig.DesktopKasm* empty
    - remote path: when KM_DESKTOP_KASM_USER/KM_DESKTOP_KASM_PASS env vars are present, they are used verbatim (no regeneration)
  </behavior>
  <action>
Add a small `randomPassword(n int) string` helper (crypto/rand → base62) near the create helpers if
one does not already exist; reuse any existing random helper if present.

At the LOCAL create call site (after the VSCode keypair block, ~line 638): add the
IsDesktopEnabled credential generation from the interfaces block. Use the sandboxID already in scope.
Before spawning the remote Lambda subprocess, export KM_DESKTOP_KASM_USER / KM_DESKTOP_KASM_PASS in
the subprocess env (mirror how KM_VSCODE_SSH_PUBKEY is propagated).

At the REMOTE create call site (~line 2057-2073): if KM_DESKTOP_KASM_USER and KM_DESKTOP_KASM_PASS
are set, assign them to NetworkConfig directly (do not write a file or regenerate — the Lambda has no
operator ~/.km). Otherwise fall back to generation (defensive).

In create_test.go, replace the Wave-0 skip in TestDesktopCredential with assertions: point HOME at a
t.TempDir(), run the credential-generation path for an enabled profile, assert the file contents/mode
and the NetworkConfig fields; assert a disabled profile writes nothing; assert env-var override is
honored. If the existing create test harness makes the full create flow hard to exercise, factor the
credential logic into a small testable function (e.g. `generateDesktopCredential(home, sandboxID, network) error`)
and test that directly — keep the call sites thin wrappers.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/... -run TestDesktopCredential -count=1</automated>
  </verify>
  <done>TestDesktopCredential passes; enabled profile writes ~/.km/desktop/<id> (0600, user:pass) and threads NetworkConfig; disabled writes nothing; env-var override honored on the remote path.</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/cmd/... -run TestDesktopCredential -count=1` green.
- `go build ./...` succeeds; `go test ./internal/app/cmd/... -count=1` stays green.
</verification>

<success_criteria>
km create mints a unique per-sandbox KasmVNC credential, stores it at ~/.km/desktop/<id> (0600),
threads it into the compiler, never bakes it, and passes it to the remote Lambda via env vars.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-04-SUMMARY.md`
</output>
