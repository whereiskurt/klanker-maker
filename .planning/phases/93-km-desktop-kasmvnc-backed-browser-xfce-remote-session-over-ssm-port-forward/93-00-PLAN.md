---
phase: 93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/profile/types_test.go
  - pkg/profile/validate_test.go
  - pkg/compiler/userdata_test.go
  - internal/app/cmd/desktop_test.go
autonomous: true
requirements: [DSK-15-TESTS]
must_haves:
  truths:
    - "Every named Desktop test function from 93-VALIDATION.md exists and is discoverable by `go test -run Desktop`"
    - "All Desktop test stubs compile and pass (skipped) so the whole repo stays green before implementation"
    - "Implementation waves have a RED→GREEN target for each DSK requirement"
  artifacts:
    - path: "pkg/profile/types_test.go"
      provides: "TestIsDesktopEnabled stub (skip)"
      contains: "func TestIsDesktopEnabled"
    - path: "pkg/profile/validate_test.go"
      provides: "TestDesktopValidate* stubs (skip)"
      contains: "func TestDesktopValidate"
    - path: "pkg/compiler/userdata_test.go"
      provides: "TestUserDataDesktop* stubs (skip)"
      contains: "func TestUserDataDesktopEnabled"
    - path: "internal/app/cmd/desktop_test.go"
      provides: "Desktop CLI test stubs (skip), mock fetcher + mock SSM mirroring vscode_test.go"
      contains: "func TestDesktopStart"
  key_links:
    - from: "pkg/profile/validate_test.go"
      to: "ValidateSemantic"
      via: "test invocation (stubbed, skipped)"
      pattern: "ValidateSemantic"
---

<objective>
Land RED test stubs (behind `t.Skip`) for every Desktop test named in 93-VALIDATION.md so that
later implementation waves have a fixed RED→GREEN target per DSK requirement, and the whole repo
test suite stays green throughout. This is the Wave 0 TDD discipline from the Phase 92 precedent.

Purpose: Establish the Nyquist-compliant test scaffold before any production code changes, so each
implementation task can flip a named test from skip→green.
Output: Four test files (additions to three existing, one new) with skipped stub functions.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-VALIDATION.md
@.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-RESEARCH.md

# Templates to mirror (read these — they define the structure to copy):
@internal/app/cmd/vscode_test.go
@pkg/compiler/userdata_test.go
@pkg/profile/types_test.go
@pkg/profile/validate_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: profile package Desktop test stubs (types_test.go + validate_test.go)</name>
  <files>pkg/profile/types_test.go, pkg/profile/validate_test.go</files>
  <action>
Add stub test functions, each with `t.Skip("Wave 1/2: implement in 93-01/93-02")` as the FIRST
line so they compile-and-pass without referencing types that don't exist yet (do NOT reference
RuntimeDesktopSpec or IsDesktopEnabled inside the body — keep bodies empty after the skip so the
package still compiles before 93-01 lands the types).

In pkg/profile/types_test.go add:
- `func TestIsDesktopEnabled(t *testing.T)` — body: `t.Skip("Wave 1 (93-01): implement IsDesktopEnabled default-false")`

In pkg/profile/validate_test.go add:
- `func TestDesktopValidateMode(t *testing.T)` — skip; will assert mode ∈ {kiosk,full}
- `func TestDesktopValidateBrowsers(t *testing.T)` — skip; will assert browsers ⊆ {firefox,chromium,brave} and non-empty for kiosk
- `func TestDesktopValidateGeometry(t *testing.T)` — skip; will assert geometry matches `^[0-9]+x[0-9]+$`
- `func TestDesktopValidateUbuntuGuard(t *testing.T)` — skip; will assert non-ubuntu slug → ERROR, raw AMI ID → WARN

Each stub: `t.Skip("Wave 2 (93-02): implement desktop semantic validation")` as the first and only statement.
Match the existing house style (no testify; standard `testing` only).
  </action>
  <verify>
    <automated>go test ./pkg/profile/... -run 'TestIsDesktopEnabled|TestDesktopValidate' -count=1</automated>
  </verify>
  <done>`go vet ./pkg/profile/...` clean; the four named functions appear in `go test -run Desktop -list .` output and pass (skipped).</done>
</task>

<task type="auto">
  <name>Task 2: compiler + CLI Desktop test stubs (userdata_test.go + new desktop_test.go)</name>
  <files>pkg/compiler/userdata_test.go, internal/app/cmd/desktop_test.go</files>
  <action>
In pkg/compiler/userdata_test.go add five stub functions (each `t.Skip(...)` as first/only statement,
empty otherwise so the package compiles before 93-03 lands the params):
- `func TestUserDataDesktopEnabled(t *testing.T)` — skip "Wave 2 (93-03): asserts KasmVNC install block + kasmpasswd seed emitted"
- `func TestUserDataDesktopDisabled(t *testing.T)` — skip "Wave 2 (93-03): asserts disabled profile emits no desktop block"
- `func TestUserDataDesktopKiosk(t *testing.T)` — skip "Wave 2 (93-03): asserts kiosk xstartup matchbox + browsers[0]; kasmvnc.yaml interface 127.0.0.1 + require_ssl false"
- `func TestUserDataDesktopFull(t *testing.T)` — skip "Wave 2 (93-03): asserts full xstartup exec startxfce4"
- `func TestUserDataDesktopCredentialSeed(t *testing.T)` — skip "Wave 2 (93-03): asserts kasmvncpasswd seeded from DesktopKasmUser/Pass"

Create internal/app/cmd/desktop_test.go as a NEW file mirroring the STRUCTURE of vscode_test.go
(same package `cmd`, same mock SandboxFetcher / mock SSMSendAPI / ShellExecFunc helpers reused —
do NOT redeclare mocks that already exist in vscode_test.go in the same package; reference them).
Add skipped stubs:
- `func TestDesktopStart(t *testing.T)` — skip "Wave 3 (93-05): port-in-use err, missing credential err, healthy prints URL+credential, port-forward args contain AWS-StartPortForwardingSession + 8444"
- `func TestDesktopStatus(t *testing.T)` — skip "Wave 3 (93-05): healthy prints KasmVNC ready; inactive → error with desktop.enabled hint"
- `func TestDesktopCredential(t *testing.T)` — skip "Wave 3 (93-04): km create writes ~/.km/desktop/<id> user:pass mode 0600; threaded into NetworkConfig"

If vscode_test.go's mocks are unexported and would collide, keep desktop_test.go bodies skip-only
(no mock references) so it compiles standalone — the real mock wiring lands in 93-04/93-05.
  </action>
  <verify>
    <automated>go test ./pkg/compiler/... ./internal/app/cmd/... -run Desktop -count=1</automated>
  </verify>
  <done>All eight stub functions compile and pass (skipped); `go build ./...` succeeds; full suite `go test ./... -count=1` stays green.</done>
</task>

</tasks>

<verification>
- `go test ./... -count=1` passes (all new stubs skipped, nothing red).
- `go test ./pkg/profile/... ./pkg/compiler/... ./internal/app/cmd/... -run Desktop -count=1 -v` lists all 12 named Desktop test functions (4 profile + 5 compiler + 3 CLI) as SKIP.
</verification>

<success_criteria>
Every Desktop test named in 93-VALIDATION.md exists as a skipped stub; the repo test suite is green;
implementation waves now have a fixed RED→GREEN target per requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/93-km-desktop-kasmvnc-backed-browser-xfce-remote-session-over-ssm-port-forward/93-00-SUMMARY.md`
</output>
