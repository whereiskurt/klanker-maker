---
phase: 76-km-vscode-rekey-rotate-ed25519-keypair-for-an-existing-sandbox
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - CLAUDE.md
  - docs/vscode.md
autonomous: true
requirements:
  - REKEY-DOCS-CLAUDE-MD
  - REKEY-DOCS-VSCODE-MD
  - PHASE-73-DEPENDENCY

must_haves:
  truths:
    - "CLAUDE.md lists `km vscode rekey <sandbox-id>` in the VS Code Remote-SSH command list with `--force` and `--yes` flag descriptions"
    - "docs/vscode.md gains a 'Rotating a sandbox key' section covering all three pain-point scenarios (baked-AMI stale-keys, cross-laptop bootstrap, post-incident rotation)"
    - "docs/vscode.md mentions the lock-override flag (`--force`) and the active-session note ('Active VS Code Remote-SSH sessions stay connected with the old key until you reconnect.')"
    - "Pre-Phase-73 sandbox case is documented as a hard error with the `km destroy && km create` remediation"
    - "Documentation references match the locked CLI surface from 76-CONTEXT.md (single positional arg, `--force`, `--yes`; no `--dry-run`, no `--remote`, no `--bootstrap`)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated VS Code Remote-SSH section with km vscode rekey command entry"
      contains: "km vscode rekey"
    - path: "docs/vscode.md"
      provides: "New 'Rotating a sandbox key' section with three pain-point scenarios + sample operator interactions"
      contains: "Rotating a sandbox key"
  key_links:
    - from: "CLAUDE.md (VS Code Remote-SSH command list)"
      to: "docs/vscode.md (full operator guide)"
      via: "operator reads CLAUDE.md for command summary, docs/vscode.md for full walkthrough"
      pattern: "docs/vscode.md"
    - from: "docs/vscode.md (Rotating a sandbox key section)"
      to: "76-CONTEXT.md sample operator interactions (lines 268-313)"
      via: "doc samples mirror CONTEXT.md examples verbatim — single source of truth"
      pattern: "Rekey complete. Active VS Code sessions"
---

<objective>
Document `km vscode rekey` for operators. Add a one-line command entry to CLAUDE.md's VS Code
Remote-SSH section. Add a comprehensive "Rotating a sandbox key" section to docs/vscode.md
covering all three pain-point scenarios with sample operator interactions transcribed from
76-CONTEXT.md.

Purpose: operator knows the command exists (CLAUDE.md), how to use it (docs/vscode.md), what
each pain-point scenario looks like (samples), and what the active-session note means (key
mental model: existing connections survive, new connections pick up the new key).

This plan runs in parallel with Plan 76-02 (Wave 2). It depends on Plan 76-01 only — the CLI
surface (command name, flags, args) is locked at the end of Wave 1, so docs can be written
without waiting for the implementation to finish.

Output: Two files modified. `grep -q 'km vscode rekey' CLAUDE.md` returns 0. `grep -q
'Rotating a sandbox key' docs/vscode.md` returns 0. `grep -q 'old key until reconnect'
docs/vscode.md` returns 0.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-km-vscode-rekey-rotate-ed25519-keypair-for-an-existing-sandbox/76-CONTEXT.md
@.planning/phases/76-km-vscode-rekey-rotate-ed25519-keypair-for-an-existing-sandbox/76-RESEARCH.md
@.planning/phases/76-km-vscode-rekey-rotate-ed25519-keypair-for-an-existing-sandbox/76-01-SUMMARY.md
@CLAUDE.md
@docs/vscode.md

<interfaces>
<!-- Locked CLI surface from 76-CONTEXT.md (does NOT change between Wave 1 and Wave 2). -->

# CLI surface
```
km vscode rekey <sandbox-id> [--force] [--yes]
```
- `<sandbox-id>` — full sandbox ID, alias, or list-row number (same identifier formats as `km vscode start` / `status`).
- `--force` — override the `km lock` safety lock.
- `--yes` — skip the confirmation prompt (for scripted use).

# CLAUDE.md insertion point (line 36 area)
The current section reads:
```
- `km vscode start <sandbox-id>` — open SSM port-forward + ssh-config Host entry for VS Code Remote-SSH (`--local-port` to override 2222)
- `km vscode status <sandbox-id>` — check sshd state + authorized_keys presence
- `km init` — initialize regional infrastructure (`--sidecars` for fast binary deploy, `--lambdas` for Lambda-only deploy)
```
Add a new bullet AFTER the `km vscode status` line, BEFORE the `km init` line.

# docs/vscode.md insertion point
After the existing "Per-sandbox lifecycle" section, BEFORE the "Profile field" section. The
new section follows the established `## Section / ### Subsection` heading hierarchy.

# CONTEXT.md sample operator interactions to mirror VERBATIM
- Normal rotation (incident response): 76-CONTEXT.md lines 268-284
- Cross-laptop bootstrap: 76-CONTEXT.md lines 286-296
- Pre-Phase-73 sandbox: 76-CONTEXT.md lines 298-306
- Locked sandbox: 76-CONTEXT.md lines 308-313

These are the canonical samples. Copy them verbatim into docs/vscode.md (with appropriate
markdown code-block fencing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `km vscode rekey` entry to CLAUDE.md command list</name>
  <files>CLAUDE.md</files>
  <action>
    Open `CLAUDE.md`. Locate line 36:
    ```
    - `km vscode status <sandbox-id>` — check sshd state + authorized_keys presence
    ```

    Insert a new line directly after it (before the `km init` line on line 37):
    ```
    - `km vscode rekey <sandbox-id>` — rotate per-sandbox VS Code Remote-SSH keypair on a running sandbox without `km destroy && km create` (`--force` to override `km lock`, `--yes` to skip confirmation prompt). Active VS Code sessions stay on the old key until reconnect.
    ```

    Then update the "VS Code Remote-SSH (Phase 73)" section further down. Locate the
    "Per-sandbox workflow" subsection (around line 333). At the end of the workflow code
    block, add a new code comment line documenting rekey:
    ```
    km vscode rekey $SB --yes                                # rotate keypair without destroy/create
    ```

    Also add a new subsection at the end of the "VS Code Remote-SSH (Phase 73)" section
    (BEFORE the `See docs/vscode.md` line at the end), titled "Rotating a sandbox key":
    ```
    ### Rotating a sandbox key (Phase 76)

    Solves three pain points: (1) baked-AMI relaunch carries stale `authorized_keys`,
    (2) cross-laptop portability — `km vscode rekey` on a second laptop bootstraps a fresh
    key without manual file copy, (3) post-incident rotation if a private key is suspected
    compromised. See `docs/vscode.md` § Rotating a sandbox key for full operator walkthrough.

    Pre-flight gates (any failure = no key changes): EC2 instance must be `running`,
    `km lock` must not block (override with `--force`), sandbox must have been created with
    `vscodeEnabled:true` (pre-Phase-73 sandboxes get a clear hard error pointing at
    `km destroy && km create`).
    ```

    DO NOT touch any other section of CLAUDE.md. Specifically, do NOT modify the Slack,
    Email, Architecture, Network, Learn, or Agent sections.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -c 'km vscode rekey' CLAUDE.md && grep -q 'Rotating a sandbox key' CLAUDE.md && echo OK</automated>
  </verify>
  <done>
    `grep -c 'km vscode rekey' CLAUDE.md` returns >= 3 (one in command list, one in workflow
    code block, one in section heading + body). `grep -q 'Rotating a sandbox key' CLAUDE.md`
    returns 0 (matches found). `git diff CLAUDE.md` shows only additions in the VS Code
    section, no deletions, no other sections touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 'Rotating a sandbox key' section to docs/vscode.md</name>
  <files>docs/vscode.md</files>
  <action>
    Open `docs/vscode.md`. Locate the end of the "Per-sandbox lifecycle" section (around line
    92, just before the `## Profile field` heading on line 93).

    Insert a new top-level section between them:

    ```markdown
    ## Rotating a sandbox key

    `km vscode rekey <sandbox-id>` rotates the per-sandbox VS Code Remote-SSH ed25519 keypair
    on a running sandbox without `km destroy && km create`. Three operator scenarios drove
    this command's existence:

    1. **Baked-AMI relaunch carries stale `authorized_keys`** — `km shell --learn --ami`
       snapshots the EC2 instance mid-session, capturing `/home/sandbox/.ssh/authorized_keys`
       from the bake-source sandbox. On relaunch from that AMI, cloud-init may mark itself
       "done" and skip the userdata block that writes the new pubkey, leaving the old key in
       place. Rekey forces the new key onto the sandbox via SSM.
    2. **Cross-laptop portability** — Phase 73 keys live on the creation machine only. An
       operator who wants to `km vscode start` from a different laptop currently has to
       manually copy `~/.km/keys/<sandbox-id>*`. Rekey on the second laptop generates a fresh
       keypair locally and pushes the public key to the sandbox — no manual file copy.
    3. **Post-incident rotation** — if a private key is suspected compromised, rotate without
       rebuilding the sandbox.

    ### CLI

    ```bash
    km vscode rekey <sandbox-id> [--force] [--yes]
    ```

    | Flag | Purpose |
    |---|---|
    | `--force` | Override the `km lock` safety lock (rekey otherwise refuses on locked sandboxes; lock = "hands off this sandbox" applies to key material). |
    | `--yes` | Skip the confirmation prompt (for scripted use). |

    `<sandbox-id>` accepts the same identifier formats as other `km vscode` subcommands:
    full sandbox ID (`sb-abc12345`), alias (`my-poc`), or list-row number.

    ### Active VS Code sessions

    sshd does NOT re-read `authorized_keys` for already-authenticated sessions, so existing
    VS Code Remote-SSH connections stay connected with the old key until you reconnect. New
    connections (operator quits the VS Code window and reconnects, or starts a new
    Remote-SSH host) pick up the new key transparently because `IdentityFile` in
    `~/.ssh/config` is unchanged.

    > **Active VS Code Remote-SSH sessions stay connected with the old key until you reconnect.**

    ### Pre-flight gates

    Rekey performs these checks IN ORDER. Any failure aborts BEFORE any key material is
    generated:

    1. **EC2 instance state** — must be `running` (not `stopped`, `pending`, or `terminated`).
       Error points at `km resume <id>` for stopped/paused sandboxes.
    2. **`km lock` check** — refuses with a clear error pointing at `km unlock <id>` unless
       `--force` is provided.
    3. **Remote SSM probe** — confirms sshd is active and `/home/sandbox/.ssh/authorized_keys`
       exists. Failure modes:
       - `vscodeEnabled:false` (sshd inactive AND authkeys absent): "VS Code not enabled in
         this sandbox's profile" — rekey can't enable VS Code retroactively.
       - sshd inactive but authkeys present: "sshd is not running" — recover via
         `km shell <id> -- sudo systemctl start sshd`.
       - sshd active but authkeys absent: "unexpected state" — recreate the sandbox.
    4. **Local key state** — informational classification (no failure):
       - Local key present + remote present → normal rotation.
       - Local key absent + remote present → cross-laptop bootstrap (the new key is your first
         key on this machine).

    ### Sample operator interactions

    **Normal rotation (post-incident):**

    ```
    $ km vscode rekey sb-abc12345
    ✓ EC2 instance running (i-0... in us-east-1)
    ✓ Pre-flight check passed (sshd active, authorized_keys present)

    Rotating VS Code key for sb-abc12345
      Old: SHA256:7w2fQ... (~/.km/keys/sb-abc12345)
      New: (will be generated)
    Continue? [y/N] y

    ✓ New keypair generated (SHA256:K9m4Z...)
    ✓ Pushed to sandbox via SSM (verified — readback matches)
    ✓ Local key replaced atomically (~/.km/keys/sb-abc12345)

    Rekey complete. Active VS Code sessions stay on the old key until reconnect.
    ```

    **Cross-laptop bootstrap (no local key on this machine):**

    ```
    $ km vscode rekey sb-abc12345 --yes
    ✓ EC2 instance running (i-0... in us-east-1)
    ✓ Pre-flight check passed (sshd active, authorized_keys present)
    ✓ New keypair generated (SHA256:K9m4Z...)
    ✓ Pushed to sandbox via SSM (verified — readback matches)
    ✓ Local key created atomically (~/.km/keys/sb-abc12345)

    Rekey complete. Reconnect VS Code to pick up the new key.
    ```

    **Pre-Phase-73 sandbox (or `vscodeEnabled:false`):**

    ```
    $ km vscode rekey sb-old00001
    ✓ EC2 instance running (i-0... in us-east-1)
    ✗ VS Code not enabled in this sandbox's profile (set spec.cli.vscodeEnabled: true and recreate the sandbox)

    Hint: this sandbox predates Phase 73 or was created with vscodeEnabled:false.
    Rekey can't enable VS Code retroactively. Run: km destroy sb-old00001 --remote --yes && km create <profile.yaml>
    ```

    **Locked sandbox:**

    ```
    $ km vscode rekey sb-abc12345
    ✓ EC2 instance running (i-0... in us-east-1)
    ✗ Sandbox is locked. Use --force to override or run: km unlock sb-abc12345
    ```

    ### What rekey does NOT change

    - `~/.ssh/config` — the managed `Host km-<sandbox-id>` block already points
      `IdentityFile` at `~/.km/keys/<sandbox-id>`. Rekey overwrites the file at that path; no
      ssh-config edit needed.
    - DDB schema, SSM Parameter Store entries, Lambda code, sidecars, infra modules — none.
      Rekey is operator-laptop-side only with one SSM SendCommand round-trip to install the
      new public key.
    - Active VS Code Remote-SSH sessions (see "Active VS Code sessions" above).

    ### Atomic local commit ordering

    Rekey writes the new keypair to scratch paths first (`~/.km/keys/<id>.new` mode 0600 and
    `~/.km/keys/<id>.pub.new` mode 0644). After the SSM install + readback verification
    succeeds, it commits via two atomic `os.Rename` calls:

    1. `~/.km/keys/<id>.pub.new` → `~/.km/keys/<id>.pub`
    2. `~/.km/keys/<id>.new` → `~/.km/keys/<id>` (the private key)

    `.pub` first because the private key is what `IdentityFile` in `~/.ssh/config` points at.
    If step 2 fails (rare — atomic rename on POSIX), ssh keeps using the old private key and
    your existing access to the sandbox is preserved.

    ### Operator runbook

    | Scenario | Recovery |
    |---|---|
    | "sandbox is not running" | `km resume <id>`, wait for `km list` to show `running`, retry rekey |
    | "Sandbox is locked" | `km unlock <id>` first, OR `km vscode rekey <id> --force` |
    | "VS Code not enabled in this sandbox's profile" | Rekey cannot help; `km destroy <id> --remote --yes && km create <profile.yaml>` with `spec.cli.vscodeEnabled: true` (the default) |
    | "sshd is not running" | `km shell <id> -- sudo systemctl start sshd`, then retry rekey |
    | "remote key install verification failed" | Inspect via `km shell <id> -- cat ~/.ssh/authorized_keys`, then `km vscode rekey <id>` to retry |
    | VS Code can't connect after rekey | Quit and reopen VS Code; the existing connection used the old key. New connections pick up the new key transparently. |
    ```

    DO NOT modify any other section of docs/vscode.md (Table of Contents, How it works,
    One-time setup, Per-sandbox lifecycle, Profile field, CLI commands, Operator state on
    disk are all unchanged).

    OPTIONAL: if the existing `## Table of Contents` block lists sections, update it to
    include the new "Rotating a sandbox key" entry. Read the existing ToC structure first;
    if absent or auto-generated by another tool, leave alone.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q 'Rotating a sandbox key' docs/vscode.md && grep -q 'old key until reconnect' docs/vscode.md && grep -q 'cross-laptop bootstrap' docs/vscode.md && grep -q 'pre-Phase-73' docs/vscode.md && echo OK</automated>
  </verify>
  <done>
    `docs/vscode.md` contains a new top-level `## Rotating a sandbox key` section with all
    three pain-point scenarios documented + sample operator interactions + active-session
    note + pre-flight gate descriptions + atomic commit ordering explanation + runbook table.
    `grep` checks above all return 0. `git diff docs/vscode.md` shows only additions in the
    new section, no other sections touched.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'km vscode rekey' CLAUDE.md` returns >= 3.
- `grep -q 'Rotating a sandbox key' CLAUDE.md` returns 0.
- `grep -q 'Rotating a sandbox key' docs/vscode.md` returns 0.
- `grep -q 'old key until reconnect' docs/vscode.md` returns 0 (covers VALIDATION.md doc check).
- `grep -q 'pre-Phase-73' docs/vscode.md` returns 0.
- `grep -q 'cross-laptop bootstrap' docs/vscode.md` returns 0.
- `git status` shows exactly 2 files modified: CLAUDE.md, docs/vscode.md.
</verification>

<success_criteria>
1. CLAUDE.md command list includes `km vscode rekey` entry.
2. docs/vscode.md has a comprehensive "Rotating a sandbox key" section.
3. All three pain-point scenarios (baked-AMI stale-keys, cross-laptop bootstrap,
   post-incident rotation) are documented with rationale.
4. Active-session note appears verbatim in both CLAUDE.md (short form) and docs/vscode.md
   (callout form + section).
5. Sample operator interactions match 76-CONTEXT.md exactly.
6. Pre-flight error catalog + recovery runbook table give operators a clear remediation
   path for every failure mode.
</success_criteria>

<output>
After completion, create `.planning/phases/76-km-vscode-rekey-rotate-ed25519-keypair-for-an-existing-sandbox/76-03-SUMMARY.md`
listing: the 2 files modified, the new sections added (CLAUDE.md command-list entry +
"Rotating a sandbox key" subsection in Phase 73 area; docs/vscode.md "Rotating a sandbox
key" top-level section), and a one-line confirmation that all `grep` content checks pass.
</output>
