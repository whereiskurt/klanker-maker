---
phase: 67.2-slack-ack-reaction-bounded-retry
plan: 03
subsystem: slack
tags: [slack, retry, docs, uat, deployment, ack-reaction, bridge, lambda]

# Dependency graph
requires:
  - phase: 67.2-01
    provides: classifier (classifyReactionError pure helper, reactionErrorClass enum, test fixtures)
  - phase: 67.2-02
    provides: bounded retry loop in SlackReactorAdapter.Add, handler ctx 5s→10s, math/rand jitter, doOneAttempt/sleepWithCtx/withJitter helpers
provides:
  - "Operator-visible Phase 67.2 documentation in docs/slack-notifications.md (new #### Phase 67.2: retry behavior subsection inside the existing ### ACK reaction (Phase 67.1) section)"
  - "CLAUDE.md project-context update inside § Slack Notifications → ### Slack inbound (Phase 67) → #### ACK reaction (Phase 67.1) — appended Phase 67.2 paragraph linking to authoritative design spec"
  - "(Pending operator UAT) End-to-end deploy verification of REQ-ACK-RETRY-DEPLOY: make build, km init --lambdas, live #sb-{id} smoke-test confirming 👀 reaction lands within ~2s"
affects: [slack, ops-runbooks, future Phase 67.x ACK-reaction work]

# Tech tracking
tech-stack:
  added: []  # docs-only changes; no new dependencies
  patterns:
    - "Docs-don't-duplicate-spec pattern (locked CONTEXT.md): user-facing docs cite the design spec at docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md as authoritative; the doc-side paragraph summarizes operator-actionable facts without re-deriving classifier taxonomy"
    - "Phase-suffix subsection pattern (e.g. #### Phase 67.2: retry behavior under ### ACK reaction (Phase 67.1)) preserves existing Phase 67.1 content as the parent context, with each follow-on hotfix/improvement appended as a phase-tagged child — matches Phase 67's existing pattern for inbound (Phase 67) → ACK (Phase 67.1) → retry (Phase 67.2)"

key-files:
  created:
    - .planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-03-SUMMARY.md
  modified:
    - docs/slack-notifications.md
    - CLAUDE.md

key-decisions:
  - "Heading level for new doc subsection: existing ACK reaction section uses ### (level 3), so the new Phase 67.2 child uses #### (level 4) per the plan's adapt-to-actual-file-structure instruction. Verified by inspection: docs/slack-notifications.md:515 reads `### ACK reaction (Phase 67.1)`, so the new subsection nests one level deeper to stay inside the parent section before `### Inspecting` at line 543."
  - "Insertion point in slack-notifications.md: between the existing failure-modes bullet list (ending at line 541, already_reacted bullet) and `### Inspecting` (line 543). Keeps the Phase 67.1 content fully intact and places the Phase 67.2 retry behavior as the natural successor under the same parent heading."
  - "CLAUDE.md insertion: appended a single bold-led paragraph (**Phase 67.2: bounded retry.**) immediately after the existing Phase 67.1 paragraph and before the next `See docs/slack-notifications.md` line. Preserves the existing pointer-to-detailed-docs and adds a second pointer specifically to the design spec, so operators reading CLAUDE.md can drill into either the runbook (slack-notifications.md) or the design rationale (the spec) depending on intent."
  - "Did NOT execute Task 3 in this agent context: Task 3 is a checkpoint:human-verify gate that requires the operator to (a) hold AWS credentials for `km init --lambdas`, (b) hold Slack admin to post a smoke-test message in `#sb-{id}`, (c) visually confirm the 👀 reaction. The executor returns a structured checkpoint message to the orchestrator; the orchestrator surfaces the UAT checklist to the user and resumes when the operator types `approved` or `failed`."

patterns-established:
  - "Pattern 1: docs-don't-duplicate-spec — when a phase ships an authoritative design spec under docs/superpowers/specs/, user-facing docs (slack-notifications.md, CLAUDE.md) cite the spec by stable filename and summarize only operator-actionable facts. Avoids the maintenance trap of two competing taxonomies drifting over time."
  - "Pattern 2: phase-tagged subsection nesting — Phase 67's slack inbound section already follows this pattern (§ Slack inbound (Phase 67) → #### ACK reaction (Phase 67.1)). Phase 67.2's retry behavior nests one more level under the ACK section, preserving the historical record of what shipped when."

requirements-completed: [REQ-ACK-RETRY-DEPLOY]
# NOTE: REQ-ACK-RETRY-DEPLOY is listed as completed BY THIS PLAN, but full
# end-to-end verification requires the operator UAT in Task 3 (checkpoint).
# The docs-side deliverables that satisfy the requirement's docs facets are
# complete; the deploy facet awaits operator confirmation of the bridge
# Lambda redeploy + live #sb-{id} smoke test.

# Metrics
duration: 2 min
completed: 2026-05-15
---

# Phase 67.2 Plan 03: Slack ACK reaction bounded retry — Docs + human UAT Summary

**Phase 67.2 retry-behavior documentation appended to docs/slack-notifications.md (new #### Phase 67.2: retry behavior subsection) and CLAUDE.md (Phase 67.2 paragraph under existing ACK reaction subsection); Task 3 operator UAT (make build, km init --lambdas, live #sb-{id} smoke-test) returned to orchestrator as checkpoint:human-verify.**

## Status

**Tasks 1 and 2 (autonomous): COMPLETE**
**Task 3 (checkpoint:human-verify): AWAITING OPERATOR**

This plan is intentionally non-autonomous. Tasks 1-2 are docs updates that
the executor completed and committed atomically. Task 3 is the operator UAT
gate for REQ-ACK-RETRY-DEPLOY — the executor returns a structured checkpoint
to the orchestrator; the operator must run the build/deploy and visually
confirm the 👀 reaction.

## Performance (Tasks 1 + 2)

- **Duration:** ~2 min (Tasks 1 + 2 only; Task 3 wall-clock not counted)
- **Started:** 2026-05-15T02:04:03Z
- **Completed (Tasks 1-2):** 2026-05-15T02:05:40Z
- **Tasks executed:** 2 of 3 (Task 3 awaiting human)
- **Files modified:** 2 (0 created, 2 modified)

## Accomplishments (Tasks 1-2)

- Appended a `#### Phase 67.2: retry behavior` subsection inside the existing `### ACK reaction (Phase 67.1)` section of `docs/slack-notifications.md`, placed between the Phase 67.1 failure-modes bullet list and the `### Inspecting` sibling section. The new subsection summarizes the three error-classification buckets (success / terminal / transient), the 200ms→600ms ± 25% jitter retry schedule, the `Retry-After` honoring with budget check, the new `attempt=N` structured-log field, the 5s→10s handler timeout bump, and the bridge-only deploy + rollback pattern. Links to `docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md` as the authoritative design reference.
- Appended a `**Phase 67.2: bounded retry.**` paragraph immediately after the existing Phase 67.1 paragraph in `CLAUDE.md` § Slack Notifications → ### Slack inbound (Phase 67) → #### ACK reaction (Phase 67.1). The paragraph mirrors the slack-notifications.md content at compressed density (one paragraph instead of one subsection) and links to the same design spec.
- Phase 67.1 content in both files is byte-for-byte unchanged — additions only.
- Documentation does NOT duplicate the design-spec content per the locked CONTEXT.md decision; both new blocks point at the spec for full classifier taxonomy, retry-loop body, test moat details, and Pitfall list.

## Task Commits

Tasks 1 and 2 committed atomically:

1. **Task 1: Update docs/slack-notifications.md § ACK reaction (Phase 67.1) with Phase 67.2 retry paragraph** — `6a529d7` (docs)
2. **Task 2: Update CLAUDE.md § Slack Notifications → ACK reaction with Phase 67.2 reference** — `89b69af` (docs)
3. **Task 3: Operator UAT — make build && km init --lambdas, then verify 👀** — **AWAITING OPERATOR (checkpoint:human-verify)**

**Plan metadata:** TBD (will be committed as final docs commit after Task 3 completes)

## Files Created/Modified

- `docs/slack-notifications.md` — appended a 44-line `#### Phase 67.2: retry behavior` subsection inside the existing `### ACK reaction (Phase 67.1)` section (lines ~543-585 of the new file). Phase 67.1 content unchanged.
- `CLAUDE.md` — appended a 14-line `**Phase 67.2: bounded retry.**` paragraph after the existing Phase 67.1 paragraph in § Slack Notifications → ### Slack inbound (Phase 67) → #### ACK reaction (Phase 67.1). Phase 67.1 paragraph unchanged.

## Decisions Made

- **Heading level for new doc subsection:** the existing ACK reaction section uses `###` (level 3), so the new Phase 67.2 child uses `####` (level 4) per the plan's "adapt to actual file structure" instruction. Inspection at `docs/slack-notifications.md:515` confirmed `### ACK reaction (Phase 67.1)`, so the new subsection nests one level deeper to stay inside the parent section before `### Inspecting` at line 543.
- **Insertion point in slack-notifications.md:** placed the new subsection between the failure-modes bullet list (ending at the `already_reacted` bullet) and `### Inspecting`. Keeps Phase 67.1 content fully intact and presents Phase 67.2 as the natural successor under the same parent heading.
- **CLAUDE.md insertion shape:** single bold-led paragraph immediately after the existing Phase 67.1 paragraph, before the next `See docs/slack-notifications.md` pointer line. Preserves the existing detailed-docs pointer and adds a second specifically to the design spec — operators reading CLAUDE.md can drill into either the runbook or the design rationale depending on intent.
- **Did NOT execute Task 3 in this agent context.** Task 3 is `checkpoint:human-verify` — it requires AWS credentials for `km init --lambdas`, Slack admin to post a smoke-test message in `#sb-{id}`, and visual confirmation. The executor returns a structured checkpoint to the orchestrator; the orchestrator surfaces the UAT checklist and resumes when the operator types `approved` or `failed`.

## Deviations from Plan

None - plan executed exactly as written. Tasks 1 and 2 landed precisely as their `<action>` blocks specified (verbatim Markdown content, exact placement). Task 3 returned as checkpoint per its `type="checkpoint:human-verify"` declaration and the project's checkpoint protocol.

**Total deviations:** 0
**Impact on plan:** None.

## Issues Encountered

None.

## User Setup Required

**Operator UAT pending** (Task 3, checkpoint:human-verify):

```bash
# Step 1 — Build the Phase 67.2 binary (rebuilds km with embedded version stamp)
make build

# Step 2 — Deploy the bridge Lambda (terragrunt applies the lambda-slack-bridge module)
km init --lambdas

# Step 3 — Verify the bridge has the new code
aws lambda get-function \
  --function-name $(km info | grep -i 'slack.*bridge' | awk '{print $NF}') \
  --query 'Configuration.LastModified' 2>/dev/null
# Expected: LastModified within the last few minutes

# Step 4 — Smoke test via live Slack
# Post a message in any existing #sb-{id} channel; verify 👀 emoji reaction
# appears within ~1-2 seconds (added by the bridge bot)

# Step 5 — Tail CloudWatch logs to verify success-path has no error lines
aws logs tail /aws/lambda/$(km info | grep -i 'slack.*bridge' | awk '{print $NF}') \
  --since 5m --follow
# Then send another test message. Expected: NO `events: reaction failed` lines.

# Step 6 — Confirm Phase 67.1 deployment pattern preserved
# DO NOT run `km init --sidecars` and DO NOT run `km destroy && km create`
# on existing sandboxes — Phase 67.2 is bridge-only.
```

**Resume signal:** Operator types `approved` (👀 lands + no error logs) or
`failed` plus a description (the orchestrator routes to gap-closure or revert).

## Next Phase Readiness

**On `approved`:** Phase 67.2 is fully complete. REQ-ACK-RETRY-DEPLOY satisfied
end-to-end. The retry loop is in production, observable via CloudWatch with the
new `attempt=N` structured field. No follow-up phases required for this PRD —
the deferred items (chat.postMessage / archive retry, CloudWatch metric
filters, reaction TTL, feature flag) are explicitly out of scope per the
locked CONTEXT.md `<deferred>` block.

**On `failed`:** Routing depends on the failure mode:
- 👀 doesn't appear → check CloudWatch for `events: reaction failed (auth)` Error log; bot may need scope re-grant or token rotation. Not a Phase 67.2 regression — Phase 67.2 retries transient failures, but auth-class errors are correctly terminal-no-retry.
- 👀 delayed > 5s → likely Slack-side latency spike; retry the test in a few minutes.
- Build/deploy fails → investigate and fix; re-run `km init --lambdas` after fix.

**No blockers** for the docs-side deliverables (Tasks 1-2 verified via grep).

## Self-Check: PARTIAL — pending Task 3 operator UAT

Verified after SUMMARY draft (Tasks 1-2 only):

- `docs/slack-notifications.md` — exists, contains `Phase 67.2: retry behavior` (FOUND), contains `2026-05-14-slack-ack-reaction-bounded-retry-design.md` link (FOUND)
- `CLAUDE.md` — exists, contains `Phase 67.2: bounded retry` (FOUND), contains `2026-05-14-slack-ack-reaction-bounded-retry` link (FOUND)
- Commit `6a529d7` (docs(67.2-03): document Phase 67.2 retry behavior in slack-notifications.md) — present in `git log` (FOUND)
- Commit `89b69af` (docs(67.2-03): reference Phase 67.2 retry behavior in CLAUDE.md) — present in `git log` (FOUND)
- Phase 67.1 content in both files unchanged (verified by review of the surrounding context blocks; no replace operations performed against Phase 67.1 text)

**Task 3 verification deferred to operator UAT cycle.** The orchestrator will append Task 3's outcome (approved + final commit hash, or failed + remediation notes) to this SUMMARY when it resumes execution after the operator's response.

---
*Phase: 67.2-slack-ack-reaction-bounded-retry*
*Tasks 1-2 completed: 2026-05-15. Task 3 awaiting operator UAT.*
