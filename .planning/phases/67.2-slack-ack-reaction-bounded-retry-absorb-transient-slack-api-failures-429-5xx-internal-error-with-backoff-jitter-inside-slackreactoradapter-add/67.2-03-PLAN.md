---
phase: 67.2-slack-ack-reaction-bounded-retry
plan: 03
type: execute
wave: 3
depends_on:
  - "67.2-01"
  - "67.2-02"
files_modified:
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: false
requirements:
  - REQ-ACK-RETRY-DEPLOY
must_haves:
  truths:
    - "docs/slack-notifications.md § ACK reaction (Phase 67.1) subsection mentions the Phase 67.2 retry behavior with one paragraph addition"
    - "CLAUDE.md § Slack Notifications → ACK reaction subsection references the retry behavior with a pointer to the design spec"
    - "Documentation does NOT duplicate the design-spec content — it links to docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md"
    - "make build succeeds with the Phase 67.2 changes from plans 01-02"
    - "km init --lambdas redeploys the bridge Lambda with the new retry code"
    - "Operator-verified: after redeploy, posting a Slack message in any #sb-{id} channel produces a 👀 reaction"
    - "Phase 67.1 deployment pattern (bridge-only) is preserved; no sandbox redeploy, no km init --sidecars, no schema migration"
  artifacts:
    - path: "docs/slack-notifications.md"
      provides: "Updated ACK reaction documentation mentioning Phase 67.2 retry behavior"
      contains: "Phase 67.2"
    - path: "CLAUDE.md"
      provides: "Updated Slack Notifications → ACK reaction subsection referencing retry behavior"
      contains: "Phase 67.2"
  key_links:
    - from: "docs/slack-notifications.md"
      to: "docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md"
      via: "markdown link from the ACK section paragraph"
      pattern: "2026-05-14-slack-ack-reaction-bounded-retry"
    - from: "CLAUDE.md"
      to: "docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md"
      via: "markdown link from the ACK reaction subsection"
      pattern: "2026-05-14-slack-ack-reaction-bounded-retry"
---

<objective>
Close out Phase 67.2 with the two operator-visible deliverables that
the locked CONTEXT.md decisions specify but the code plans don't
produce:

1. Documentation updates — append a single paragraph to
   `docs/slack-notifications.md` § ACK reaction (Phase 67.1) describing
   the new retry behavior, plus update the matching section in
   `CLAUDE.md` to mirror.
2. Manual UAT — operator runs `make build && km init --lambdas`, then
   exercises a `#sb-{id}` channel and confirms 👀 reaction lands.

Purpose: The code plans (01, 02) verify behavior in unit tests. This
plan verifies (a) the deployment pattern still works post-changes
(REQ-ACK-RETRY-DEPLOY) and (b) operators can find the new behavior in
docs without re-reading the spec.

Output: 2 docs files updated, 1 successful build, 1 successful
`km init --lambdas`, 1 manually-verified 👀 on a live Slack channel.

NOTE: This plan is non-autonomous — task 3 is a `checkpoint:human-verify`
because UAT against live Slack requires the operator to (a) hold the
Slack admin role, (b) have a `#sb-{id}` channel available, (c)
post a message and visually confirm the reaction. Tasks 1 and 2 are
autonomous.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-CONTEXT.md
@.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-RESEARCH.md
@.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-VALIDATION.md
@.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-01-PLAN.md
@.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-02-PLAN.md

@docs/slack-notifications.md
@CLAUDE.md
@docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/slack-notifications.md § ACK reaction (Phase 67.1) with Phase 67.2 retry paragraph</name>
  <files>docs/slack-notifications.md</files>
  <action>
    Locate the existing § ACK reaction (Phase 67.1) section in
    `docs/slack-notifications.md`. Append (do NOT replace existing
    content) a new sub-paragraph titled "Phase 67.2: retry behavior":

    ```markdown
    #### Phase 67.2: retry behavior

    The Phase 67.1 single-attempt ACK reaction was upgraded to a bounded
    retry loop inside `SlackReactorAdapter.Add`. Transient Slack API
    failures — HTTP 429 (rate limit), HTTP 5xx, network errors, and
    Slack JSON errors `internal_error` / `service_unavailable` /
    `fatal_error` / `request_timeout` — now trigger up to 2 retries
    with exponential backoff (200ms then 600ms, each with ±25% jitter
    to de-correlate retries across many sandboxes during a Slack
    incident). On HTTP 429, the loop honors the `Retry-After` header if
    its value fits within the remaining 10-second budget; otherwise it
    returns the typed `ErrSlackRateLimited` immediately.

    Error classification follows three buckets:

    - **Success** (no retry): HTTP 200 + `ok:true`, or `already_reacted`
      (idempotency for double-delivered events).
    - **Terminal — no retry, Error log**: `invalid_auth`, `not_authed`,
      `account_inactive`, `token_revoked`, `missing_scope`,
      `token_expired`, plus related auth codes. These require operator
      action (rotate bot token, re-install app for missing scope).
    - **Terminal — no retry, Warn log via handler**: `bad_timestamp`,
      `message_not_found`, `channel_not_found`, `not_reactable`,
      `thread_locked`, `invalid_name`, plus related bad-input codes.
    - **Transient — retry**: everything in the transient list above
      PLUS any unknown error string (default-unknown→transient policy
      — safer than hard-failing on an error code Slack adds tomorrow).

    Operator observability: the existing `events: reaction failed` Warn
    line in CloudWatch is preserved on final retry exhaustion, now with
    a new `attempt=N` structured field. Intermediate retries log at
    Debug (silent at the default Lambda log level; visible when
    `KM_LOG_LEVEL=debug`).

    The handler goroutine's context timeout was bumped from 5s to 10s
    to accommodate the retry budget (worst case: ~800ms of sleeps + 3
    HTTP round-trips, comfortably under 10s for normal Slack latency
    with headroom for incident-mode slowness).

    Bridge-only change. Deploy: `make build && km init --lambdas`.
    Rollback: PR revert + `km init --lambdas`. No sandbox redeploy.
    See `docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md`
    for the full design spec.
    ```

    Place the new sub-section AFTER the existing Phase 67.1 ACK
    reaction paragraphs, BEFORE the next major heading (or at the end
    of the ACK subsection if there's no following sibling). Match the
    heading level used by the existing ACK reaction subsection — if
    the parent is `### ACK reaction (Phase 67.1)`, use `####`. If the
    parent is `#### ACK reaction (Phase 67.1)`, use `#####`. Adapt to
    the actual file structure.

    Do NOT delete any Phase 67.1 content — the new paragraph is
    additive.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q "Phase 67.2: retry behavior" docs/slack-notifications.md && grep -q "2026-05-14-slack-ack-reaction-bounded-retry-design.md" docs/slack-notifications.md && echo OK</automated>
  </verify>
  <done>
    - `docs/slack-notifications.md` contains a new "Phase 67.2: retry
      behavior" subsection inside the ACK reaction section.
    - Phase 67.1 content unchanged.
    - Subsection links to
      `docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md`
      as the authoritative design reference.
    - Three error-classification buckets are summarized.
    - 5s→10s handler timeout bump is documented.
    - Bridge-only deploy pattern documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md § Slack Notifications → ACK reaction with Phase 67.2 reference</name>
  <files>CLAUDE.md</files>
  <action>
    Locate the existing § Slack Notifications → ### Slack inbound
    (Phase 67) → "#### ACK reaction (Phase 67.1)" subsection in
    `CLAUDE.md` (around line 220-230 per CONTEXT.md). The current
    text reads:

    ```
    #### ACK reaction (Phase 67.1)

    When the bridge enqueues an inbound message to SQS, it adds a 👀
    reaction to the originating Slack message via `reactions.add`
    (fire-and-forget, ~1s round-trip). Bot needs `reactions:write` scope
    (added via Slack App config → OAuth & Permissions → reinstall app).
    Bridge-global emoji is configurable via `KM_SLACK_ACK_EMOJI` Lambda
    env var (default `eyes`, no colons). Bridge-only change — deploy
    with `make build && km init --lambdas`; no sandbox redeploy needed.
    See `docs/slack-notifications.md` § ACK reaction.
    ```

    Append a NEW paragraph IMMEDIATELY after the existing one (before
    any blank line + next heading). The new paragraph reads:

    ```
    **Phase 67.2: bounded retry.** Transient failures (HTTP 429 with
    `Retry-After`, HTTP 5xx, network errors, and Slack JSON codes
    `internal_error` / `service_unavailable` / `fatal_error` /
    `request_timeout`, plus any unknown error string per the
    default-unknown→transient policy) now retry up to 2× with 200ms→600ms
    backoff and ±25% jitter inside `SlackReactorAdapter.Add`. Terminal
    auth-class errors (`invalid_auth`, `missing_scope`, `token_expired`,
    etc.) log at Error level and do NOT retry. Handler goroutine context
    bumped 5s → 10s to fit the retry budget. The existing `events:
    reaction failed` Warn line in CloudWatch is preserved on final
    exhaustion with a new `attempt=N` field. Bridge-only deploy: `make
    build && km init --lambdas`. See
    `docs/superpowers/specs/2026-05-14-slack-ack-reaction-bounded-retry-design.md`.
    ```

    Do NOT modify the existing Phase 67.1 paragraph.

    Do NOT touch any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/klankrmkr && grep -q "Phase 67.2: bounded retry" CLAUDE.md && grep -q "2026-05-14-slack-ack-reaction-bounded-retry" CLAUDE.md && echo OK</automated>
  </verify>
  <done>
    - `CLAUDE.md` § ACK reaction (Phase 67.1) has a new Phase 67.2
      paragraph appended.
    - Phase 67.1 text is unchanged.
    - Paragraph links to the design spec.
    - All key Phase 67.2 facts (retry budget, classification buckets,
      timeout bump, deploy pattern) are summarized.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT — make build && km init --lambdas, then verify 👀 reaction lands on a live #sb-{id} message</name>
  <files>(none — verification only)</files>
  <action>
    This is a checkpoint:human-verify gate. The operator (human) executes
    the verification steps below; Claude does not act autonomously. See
    `<how-to-verify>` for the full step list. The operator's role:

    1. Build the Phase 67.2 binary: `make build`
    2. Deploy the bridge Lambda: `km init --lambdas`
    3. Confirm via `aws lambda get-function` that the LastModified
       timestamp updated.
    4. Post a smoke-test message in any `#sb-{id}` Slack channel.
    5. Visually confirm a 👀 emoji reaction appears within ~2 seconds.
    6. Tail CloudWatch logs and confirm no `events: reaction failed`
       Error lines on the success path.
    7. Confirm no `km init --sidecars` or sandbox redeploy was required
       (Phase 67.1 deployment pattern preserved).

    Resume the orchestrator with `approved` or `failed` per the resume
    signal block.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — operator must respond with 'approved' or 'failed' after running make build && km init --lambdas and confirming 👀 on a live Slack message"</automated>
  </verify>
  <done>
    - Operator typed `approved` after seeing 👀 land on a live `#sb-{id}`
      message within ~2 seconds.
    - CloudWatch logs show no `events: reaction failed` Error lines on
      the success path (only success or absence of failure logs).
    - Phase 67.1 deployment pattern preserved (no sandbox redeploy, no
      `km init --sidecars`).
    - REQ-ACK-RETRY-DEPLOY verified end-to-end.
  </done>
  <what-built>
    Phase 67.2 ships:
    - Bounded retry loop in `SlackReactorAdapter.Add` (plan 01-02)
    - Pure-function `classifyReactionError` taxonomy
    - 10 new unit tests + table-driven classifier test
    - Handler goroutine context bumped 5s → 10s
    - Docs updates in `docs/slack-notifications.md` and `CLAUDE.md`

    What's NOT yet verified: whether the new code actually runs in the
    deployed bridge Lambda and produces 👀 on a real Slack message.
    Unit tests pass with mocked HTTP, but the operator should confirm
    the deploy path mirrors Phase 67.1 exactly.
  </what-built>
  <how-to-verify>
    From the project root `/Users/khundeck/working/klankrmkr`:

    **Step 1 — Build:**
    ```bash
    make build
    ```
    Expected: clean exit, no compile errors. The `km` binary in
    `bin/km` is rebuilt with embedded version stamp.

    **Step 2 — Deploy the bridge Lambda:**
    ```bash
    km init --lambdas
    ```
    Expected: terragrunt applies the lambda-slack-bridge module,
    Lambda function updated, Function URL unchanged.

    **Step 3 — Verify the bridge has the new code:**
    ```bash
    aws lambda get-function --function-name $(km info | grep -i 'slack.*bridge' | awk '{print $NF}') --query 'Configuration.LastModified' 2>/dev/null || echo 'use km info to find function name'
    ```
    Expected: `LastModified` timestamp within the last few minutes.

    **Step 4 — Smoke test via live Slack:**

    Open any existing `#sb-{id}` channel in Slack (or `#sb-test` if
    one exists for operator smoke-testing). Post a plain text message
    like `Phase 67.2 smoke test`.

    Expected: within ~1-2 seconds of sending, a 👀 emoji reaction
    appears on the message (added by the bridge bot, NOT the operator).

    **Step 5 — Tail CloudWatch logs to confirm the structured field
    `attempt=1` is present on the success path:**
    ```bash
    aws logs tail /aws/lambda/$(km info | grep -i 'slack.*bridge' | awk '{print $NF}') --since 5m --follow
    ```
    Then send another test message. Expected: the log line for the
    successful enqueue/reaction does NOT contain `events: reaction
    failed` (success → no error log). For deeper observability of the
    retry path, optionally simulate a Slack-side flake by sending a
    message during a known Slack outage and checking for `events:
    reaction retry` Debug lines — but this is opportunistic, not a
    blocker.

    **Step 6 — Confirm Phase 67.1 deployment pattern is preserved:**
    No `km init --sidecars` should be required (Phase 67.2 ships no
    sidecar binary changes). Existing sandboxes do NOT need
    `km destroy && km create`. Verify by NOT running either of those
    commands.

    **Failure modes to watch for:**
    - 👀 doesn't appear → check CloudWatch for the
      `events: reaction failed (auth)` Error log — bot may need scope
      re-grant or token rotation.
    - 👀 appears but the message wasn't enqueued (no agent reply
      later) → that's a Phase 67 SQS issue, not Phase 67.2.
    - 👀 appears with a delay > 5s → likely a Slack-side latency spike;
      retry the test in a few minutes.
  </how-to-verify>
  <resume-signal>
    Type `approved` if the 👀 lands and CloudWatch shows no error logs.
    Type `failed` plus a description if either step doesn't behave as
    expected — the orchestrator will route to gap-closure or revert.
  </resume-signal>
</task>

</tasks>

<verification>
- Tasks 1 + 2 are autonomous and verified via grep commands.
- Task 3 is a `checkpoint:human-verify` gate — operator must
  type `approved` after running `make build && km init --lambdas` and
  visually confirming 👀.

REQ-ACK-RETRY-DEPLOY is satisfied when:
- `make build` succeeds
- `km init --lambdas` succeeds
- Live `#sb-{id}` smoke test shows the 👀 reaction within ~2s
- No `events: reaction failed` Error logs on the success path
- No sandbox redeploy required (Phase 67.1 deployment pattern preserved)
</verification>

<success_criteria>
- `docs/slack-notifications.md` § ACK reaction contains a Phase 67.2
  retry-behavior subsection that summarizes the locked CONTEXT.md
  decisions and links to the design spec.
- `CLAUDE.md` § ACK reaction (Phase 67.1) has an appended Phase 67.2
  paragraph that reads as a natural successor to the existing text.
- Operator UAT confirmed: 👀 reaction lands within ~2s of a live Slack
  message post.
- Phase 67.1 deployment pattern (bridge-only, no sandbox redeploy)
  preserved.
- REQ-ACK-RETRY-DEPLOY satisfied.
</success_criteria>

<output>
After completion, create
`.planning/phases/67.2-slack-ack-reaction-bounded-retry-absorb-transient-slack-api-failures-429-5xx-internal-error-with-backoff-jitter-inside-slackreactoradapter-add/67.2-03-SUMMARY.md`
following the standard summary template. Include in the summary:
- Operator UAT outcome (approved / failed + notes)
- Build + deploy timestamps
- Any CloudWatch log excerpts showing the new `attempt=N` field
</output>
