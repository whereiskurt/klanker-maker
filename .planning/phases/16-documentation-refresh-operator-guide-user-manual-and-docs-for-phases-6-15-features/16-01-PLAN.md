---
phase: 16-documentation-refresh-operator-guide-user-manual-and-docs-for-phases-6-15-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operator-guide.md
  - docs/user-manual.md
  - README.md
autonomous: true
requirements:
  - DOC-OPERATOR
  - DOC-USER
  - DOC-README

must_haves:
  truths:
    - "Operator guide documents km bootstrap, SCP containment, sidecar build pipeline, GitHub App setup, km doctor, and km-identities table"
    - "User manual documents km doctor, km configure github, km budget add, spec.email, and sourceAccess.github"
    - "README roadmap table shows phases 1-15 as Complete and phases 6+ listed"
  artifacts:
    - path: "docs/operator-guide.md"
      provides: "Updated operator guide with Phase 6-15 feature documentation"
      contains: "km bootstrap"
    - path: "docs/user-manual.md"
      provides: "Updated user manual with Phase 6-15 CLI and profile features"
      contains: "km doctor"
    - path: "README.md"
      provides: "Updated roadmap table"
      contains: "Complete"
  key_links:
    - from: "docs/operator-guide.md"
      to: "Phase 6-15 features"
      via: "New sections for bootstrap, SCP, sidecars, GitHub App, identity, doctor"
      pattern: "km bootstrap|SCP|km doctor|km-identities|ecr-push|GitHub App"
---

<objective>
Update the operator guide and user manual with all Phase 6-15 features, and update the README roadmap table.

Purpose: The operator guide and user manual are the two largest docs and cover the most ground. They were written during early phases and are missing budget enforcement infrastructure, SCP containment, sidecar build pipeline, GitHub App integration, sandbox identity, and km doctor. The README roadmap table is stale (shows Phase 6 as "Planned").

Output: Updated docs/operator-guide.md, docs/user-manual.md, README.md
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Source code references for accurate documentation -->
@internal/app/cmd/bootstrap.go
@internal/app/cmd/doctor.go
@internal/app/cmd/configure_github.go
@internal/app/cmd/budget.go
@internal/app/cmd/shell.go
@infra/modules/scp/
@sidecars/
@Makefile

<!-- Phase summaries for context on what was built -->
@.planning/phases/10-scp-sandbox-containment-org-level-ec2-breakout-prevention/10-01-SUMMARY.md
@.planning/phases/10-scp-sandbox-containment-org-level-ec2-breakout-prevention/10-02-SUMMARY.md
@.planning/phases/13-github-app-token-integration-scoped-repo-access-for-sandboxes/13-01-SUMMARY.md
@.planning/phases/13-github-app-token-integration-scoped-repo-access-for-sandboxes/13-04-SUMMARY.md
@.planning/phases/08-sidecar-build-deployment-pipeline/08-01-SUMMARY.md
@.planning/phases/08-sidecar-build-deployment-pipeline/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator guide with Phase 6-15 features</name>
  <files>docs/operator-guide.md</files>
  <action>
Read the full current docs/operator-guide.md. Add the following new sections in appropriate locations within the existing document structure. Reference source code for accuracy (read the actual Go files and Terraform modules to get flags, parameters, and behavior right).

Sections to add:

1. **km bootstrap command reference** — Read internal/app/cmd/bootstrap.go. Document what `km bootstrap` provisions (S3 state bucket, DynamoDB tables, KMS keys, SCP). Show the command usage, flags, and what resources it creates. This replaces the manual S3/DynamoDB/KMS setup steps that may exist in the guide.

2. **Budget enforcement infrastructure** — Document the DynamoDB `km-budgets` table schema, budget-enforcer Lambda (per-sandbox Lambda naming pattern `km-budget-enforcer-{sandbox-id}`), EventBridge schedule for compute spend checks, and how `km budget add` top-up works. Reference the budget-guide.md for detailed metering docs but cover the operator-facing infrastructure here.

3. **SCP sandbox containment** — Read infra/modules/scp/ Terraform files. Document SCP deployment via `km bootstrap`, what each deny statement blocks (EC2 breakout, IAM escalation, Organizations discovery, region lock), role carve-outs (trusted_arns_instance, trusted_arns_iam, trusted_arns_ssm), and management account prerequisites. Reference the security-model.md for threat analysis.

4. **Sidecar build pipeline** — Read the Makefile for sidecar-related targets. Document `make sidecars` (cross-compile Go binaries for linux/amd64 and linux/arm64), `make ecr-push` (Docker build + ECR push for ECS sidecar images), and S3 binary upload for EC2 sidecars. List the four sidecars (dns-proxy, http-proxy, audit-log, tracing) and their build artifacts.

5. **GitHub App setup** — Read internal/app/cmd/configure_github.go. Document `km configure github --setup` manifest flow (creates GitHub App via manifest API), the manual alternative (create App in GitHub settings, then `km configure github`), and the SSM parameter layout for App ID, private key, and installation ID.

6. **GitHub token refresh Lambda** — Document the per-sandbox Lambda (`km-github-token-refresher-{sandbox-id}`) + EventBridge schedule (every 45 min), IAM requirements, and cleanup on sandbox destroy.

7. **DynamoDB km-identities table** — Document the identity table (sandbox_id hash key, public_key, created_at), provisioned alongside km-budgets during `km bootstrap`.

8. **km doctor command** — Read internal/app/cmd/doctor.go. Document what `km doctor` checks (config, AWS creds, bootstrap resources, per-region infra, active sandboxes), `--json` and `--quiet` flags, and CI usage patterns.

Also fix the stale path reference on line 619: change `infra/templates/network.terragrunt.hcl` to the correct current path under `infra/templates/network/` if that's the new structure (verify by checking the filesystem).

Preserve all existing content. Add new sections logically within the existing structure (e.g., budget enforcement near the DynamoDB/infrastructure section, km doctor near the troubleshooting/verification section).
  </action>
  <verify>
    <automated>grep -c "km bootstrap\|km doctor\|SCP\|ecr-push\|GitHub App\|km-identities\|budget-enforcer" docs/operator-guide.md | xargs test 7 -le</automated>
  </verify>
  <done>Operator guide contains sections for all 8 new topics. Stale template path fixed. All existing content preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update user manual and README roadmap</name>
  <files>docs/user-manual.md, README.md</files>
  <action>
**User manual updates:**

Read the full current docs/user-manual.md. Add the following sections, reading source code for accurate flag/usage documentation:

1. **km doctor** — Read internal/app/cmd/doctor.go. Add a `km doctor` section to the Command Reference. Document usage, output interpretation (check categories, pass/fail/warn status), `--json` flag for machine parsing, `--quiet` flag for CI (exit code only). Add to the Table of Contents.

2. **km configure github** — Read internal/app/cmd/configure_github.go. Document both flows: `km configure github --setup` (manifest-based automated GitHub App creation) and manual flow (provide App ID, private key path, installation ID interactively). Add to the Table of Contents.

3. **km budget add** — Read internal/app/cmd/budget.go. Document `km budget add <sandbox-id> --compute <amount> --ai <amount>` usage. Show example `km status` output with budget breakdown (compute and AI pools, per-model AI breakdown).

4. **Profile spec.email section** — Document `spec.email` profile fields: `signing` (boolean, enables Ed25519 signing of outbound email), `verifyInbound` (boolean, verify signatures on inbound email), `encryption` (boolean, enables NaCl encrypted email). Reference the multi-agent-email.md for detailed protocol docs.

5. **Profile sourceAccess.github** — Document how `sourceAccess.github` works with GitHub App tokens. Explain that `km create` provisions a per-sandbox Lambda that refreshes a GitHub installation token every 45 minutes into SSM. The sandbox uses GIT_ASKPASS to read the token from SSM at git time. Show allowed_repos and permissions fields.

Update the Table of Contents to include all new sections.

**README updates:**

Read README.md. Update the roadmap table (around line 430) to:
- Show phases 1-5 status accurately (mark Complete where appropriate based on STATE.md showing 14 completed phases)
- Add phases 7-15 to the table
- Mark all through Phase 15 as Complete
- Add Phase 16 as "In Progress"
- Add Phase 17 as "Planned"
- Keep the link to .planning/ROADMAP.md for details
  </action>
  <verify>
    <automated>grep -c "km doctor\|km configure github\|km budget add\|spec.email\|sourceAccess.github" docs/user-manual.md | xargs test 5 -le && grep "Phase 15" README.md | grep -q "Complete"</automated>
  </verify>
  <done>User manual has 5 new sections with updated ToC. README roadmap table shows all phases through 15 as Complete, Phase 16 as In Progress.</done>
</task>

</tasks>

<verification>
- grep for all 8 operator guide topics returns matches
- grep for all 5 user manual topics returns matches
- README roadmap table includes phases through 17
- No stale `infra/live/sandboxes/_template/` paths remain in any modified file
</verification>

<success_criteria>
Operator guide, user manual, and README are updated with accurate Phase 6-15 feature documentation. All new sections reference correct CLI flags, file paths, and infrastructure details verified against source code.
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-refresh-operator-guide-user-manual-and-docs-for-phases-6-15-features/16-01-SUMMARY.md`
</output>
