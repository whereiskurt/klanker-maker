---
phase: 87-additionalsnapshots-snapshot-backed-ebs-volumes-in-sandboxprofile
plan: 06
subsystem: terraform-module
tags: [ebs, snapshots, terraform, ec2spot, module-versioning, phase-87]

requires:
  - "87-04 (compiler emits additional_snapshots HCL block)"

provides:
  - "infra/modules/ec2spot/v1.1.0/ — additive copy of v1.0.0 + additional_snapshots variable + aws_ebs_volume.snapshot + aws_volume_attachment.snapshot"
  - "infra/templates/sandbox/terragrunt.hcl source bumped to v1.1.0"

affects:
  - "87-07 (UAT — km create exercises v1.1.0 end-to-end)"

tech-stack:
  added: []
  patterns:
    - "Phase 80 immutable versioning: v1.0.0 unchanged, additive changes go to v1.1.0"
    - "for_each key = tostring(i) for snapshot volume map (critical_research_corrections #5)"
    - "size_gb null/0 = inherit from snapshot (conditional size attribute)"
    - "encrypted null = inherit from snapshot's encryption state"
    - "Name tag uses var.resource_prefix (modulehygiene_test.go compatible, not km-sandbox-... hardcoded)"
    - "No required_providers block — provider generated by infra/live/root.hcl"

key-files:
  created:
    - infra/modules/ec2spot/v1.1.0/main.tf
    - infra/modules/ec2spot/v1.1.0/variables.tf
    - infra/modules/ec2spot/v1.1.0/outputs.tf
  modified:
    - infra/templates/sandbox/terragrunt.hcl

key-decisions:
  - "for_each key uses tostring(i) not bare i — HCL requires string keys in for expressions used with for_each"
  - "size = conditional on size_gb != null && > 0; otherwise null so AWS inherits snapshot size"
  - "encrypted = each.value.encrypted (passes null through) — AWS EBS inherits snapshot encryption when null"
  - "instance_id ternary is verbatim copy of aws_volume_attachment.additional for consistency"
  - "Live sandbox directories under infra/live/use1/sandboxes/*/ NOT touched — they remain on v1.0.0"

metrics:
  duration: 121s
  completed: 2026-05-22
  tasks: 2
  files_modified: 4
---

# Phase 87 Plan 06: Wave 3 — ec2spot/v1.1.0 Terraform Module + Template Version Bump Summary

**Additive copy of ec2spot/v1.0.0 to v1.1.0 with additional_snapshots variable, aws_ebs_volume.snapshot, and aws_volume_attachment.snapshot; sandbox template source bumped to v1.1.0**

## Performance

- **Duration:** 121s (~2 min)
- **Started:** 2026-05-22T21:52:14Z
- **Completed:** 2026-05-22T21:54:15Z
- **Tasks:** 2
- **Files modified:** 4 (3 created + 1 modified)

## Accomplishments

### Task 1: ec2spot/v1.1.0 Module Creation

Created `infra/modules/ec2spot/v1.1.0/` as an additive copy of v1.0.0:

**File listing (infra/modules/ec2spot/v1.1.0/):**
```
-rw-r--r--  26532  main.tf       (v1.0.0 + 35 lines added)
-rw-r--r--   1253  outputs.tf    (unchanged from v1.0.0)
-rw-r--r--   5756  variables.tf  (v1.0.0 + 17 lines added)
```

**Diff stats (v1.0.0 → v1.1.0, additions only):**

`variables.tf`: 17 lines added — `additional_snapshots` variable with `list(object({snapshot_id, device_name, encrypted optional(bool), size_gb optional(number)}))`, `default = []`.

`main.tf`: 35 lines added — Phase 87 comment block + `aws_ebs_volume.snapshot` + `aws_volume_attachment.snapshot` resources.

**Key implementation details:**
- `for_each = { for i, s in var.additional_snapshots : tostring(i) => s }` on both resources
- `size = (each.value.size_gb != null && each.value.size_gb > 0) ? each.value.size_gb : null` — null inherits snapshot size
- `encrypted = each.value.encrypted` — null value passes through; AWS EBS inherits from snapshot
- `"km:source-snapshot" = each.value.snapshot_id` tag for traceability
- `Name = "${var.resource_prefix}-sandbox-${var.sandbox_id}-snap-${each.key}"` — uses var.resource_prefix not hardcoded `km-`
- `instance_id` ternary is verbatim copy of `aws_volume_attachment.additional` line 706

**Verifications passed:**
- `terraform fmt -check -recursive infra/modules/ec2spot/v1.1.0/` — exit 0 (clean)
- `grep -rn "required_providers" infra/modules/ec2spot/v1.1.0/` — empty (no block present)
- `git diff --name-only infra/modules/ec2spot/v1.0.0/` — empty (v1.0.0 immutable)

### Task 2: Sandbox Template Version Bump

Changed `infra/templates/sandbox/terragrunt.hcl` line 43:
```hcl
# Before:
source = "${local.repo_root}/infra/modules/${local.svc_config.locals.substrate_module}/v1.0.0"
# After:
source = "${local.repo_root}/infra/modules/${local.svc_config.locals.substrate_module}/v1.1.0"
```

Live sandbox directories under `infra/live/use1/sandboxes/*/` (e.g. `gebpf-117159c0`, `gebpf-4620b2bc`, etc.) remain pinned to v1.0.0 — `git status infra/live/` shows no changes.

## Task Commits

1. **Task 1: Create ec2spot/v1.1.0 module** — `47bde02` (feat)
2. **Task 2: Bump sandbox template to v1.1.0** — `ef51664` (feat)

## Files Created / Modified

- `infra/modules/ec2spot/v1.1.0/main.tf` — CREATED (copy of v1.0.0 + Phase 87 snapshot resources)
- `infra/modules/ec2spot/v1.1.0/variables.tf` — CREATED (copy of v1.0.0 + additional_snapshots variable)
- `infra/modules/ec2spot/v1.1.0/outputs.tf` — CREATED (verbatim copy of v1.0.0)
- `infra/templates/sandbox/terragrunt.hcl` — MODIFIED (v1.0.0 → v1.1.0 on line 43)

## Note: Live Sandbox Directories

Existing sandbox directories under `infra/live/use1/sandboxes/*/terragrunt.hcl` are operator-committed copies pinned to v1.0.0. They are NOT modified by Phase 87 and continue to function without the `additional_snapshots` variable (which defaults to `[]`).

## Note for Plan 07 (UAT)

UAT scenarios will exercise this module end-to-end via real `km create` calls with a profile containing `spec.runtime.additionalSnapshots`. The compiler (plan 04) emits `additional_snapshots = [...]` HCL; this module consumes it via the `additional_snapshots` variable and creates one `aws_ebs_volume` + `aws_volume_attachment` per entry.

## go build Check

`go build ./...` — exit 0 (no Go compilation issues; this plan touches no Go files).

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- `infra/modules/ec2spot/v1.1.0/main.tf` — FOUND
- `infra/modules/ec2spot/v1.1.0/variables.tf` — FOUND
- `infra/modules/ec2spot/v1.1.0/outputs.tf` — FOUND
- `infra/templates/sandbox/terragrunt.hcl` (v1.1.0 on line 43) — FOUND
- Commit `47bde02` — FOUND
- Commit `ef51664` — FOUND

## Self-Check: PASSED

---
*Phase: 87-additionalsnapshots-snapshot-backed-ebs-volumes-in-sandboxprofile*
*Completed: 2026-05-22*
