---
phase: 104-slack-channel-o-1-resolution-on-alias-reuse
plan: 03
type: execute
wave: 3
depends_on: [104-01, 104-02]
files_modified:
  - infra/modules/km-operator-policy/v1.0.0/variables.tf
  - infra/modules/km-operator-policy/v1.0.0/main.tf
  - infra/modules/create-handler/v1.0.0/variables.tf
  - infra/modules/create-handler/v1.0.0/main.tf
  - infra/live/use1/create-handler/terragrunt.hcl
  - internal/app/config/config.go
  - internal/app/config/config_test.go
  - pkg/aws/slack_channels.go
  - pkg/aws/slack_channels_test.go
  - internal/app/cmd/create.go
  - internal/app/cmd/create_slack.go
autonomous: true
requirements: [SLACK-CHAN-STORE, SLACK-CHAN-DEPLOY]
must_haves:
  truths:
    - "create-handler Lambda role can GetItem/PutItem/DescribeTable on the km-slack-channels table (count-gated on a non-empty table name)"
    - "GetSlackChannelsTableName() derives {prefix}-slack-channels, honors an explicit override, and is nil-safe"
    - "A custom slack_channels_table_name: in km-config.yaml is NOT silently ignored (present in the v2→v merge-list)"
    - "SlackChannelStore round-trips alias→channel_id via DDB GetItem/PutItem; GetByAlias miss returns empty string"
    - "km create builds a real SlackChannelStore and threads it into resolveSlackChannel so a fresh-alias create writes the mapping and a recreate hits the O(1) DDB path"
    - "With the real store wired (non-nil), a pre-104 channel that resolves via the SSM by-name cache is back-filled into the km-slack-channels table on first touch (migrate-on-touch — activated by 104-01's back-fill logic once create.go passes a non-nil store)"
  artifacts:
    - path: "pkg/aws/slack_channels.go"
      provides: "SlackChannelStore (GetByAlias/UpsertByAlias) + SlackChannelGetPutAPI interface; no TTL, no ConditionExpression"
      contains: "func (s *SlackChannelStore) UpsertByAlias"
    - path: "internal/app/config/config.go"
      provides: "SlackChannelsTableName field + GetSlackChannelsTableName getter + SetDefault + merge-list entry"
      contains: "GetSlackChannelsTableName"
    - path: "infra/modules/km-operator-policy/v1.0.0/main.tf"
      provides: "aws_iam_role_policy.dynamodb_slack_channels (count-gated, GetItem/PutItem/DescribeTable)"
      contains: "dynamodb_slack_channels"
  key_links:
    - from: "internal/app/cmd/create.go"
      to: "pkg/aws.SlackChannelStore"
      via: "build store with cfg.GetSlackChannelsTableName() and pass into resolveSlackChannel"
      pattern: "SlackChannelStore\\{"
    - from: "infra/modules/create-handler/v1.0.0/main.tf"
      to: "infra/modules/km-operator-policy/v1.0.0"
      via: "slack_channels_table_name pass-through into module km_operator_policy"
      pattern: "slack_channels_table_name = var.slack_channels_table_name"
    - from: "internal/app/config/config.go GetSlackChannelsTableName"
      to: "km-slack-channels table name string"
      via: "{prefix}-slack-channels runtime derivation (no Lambda env var)"
      pattern: "slack-channels"
---

<objective>
Connect the durable store end-to-end: IAM so the create-handler Lambda can read/write the `km-slack-channels` table, a `GetSlackChannelsTableName()` config getter (with the mandatory merge-list entry), the `pkg/aws.SlackChannelStore` Go helper (mirroring `DDBThreadStore`), and the wiring that builds the store in `km create` and threads it into `resolveSlackChannel` so the lookup-first resolver from plan 104-01 actually reads/writes DDB.

This plan depends on 104-02 (the table + live unit must exist for the IAM ARN + live input to reference) and 104-01 (the `SlackChannelStore` interface + resolver state machine that this store satisfies and feeds).

Purpose: Make the O(1) hot path durable across destroy/recreate — a fresh-alias create writes the mapping, a recreate reads it first.
Output: create-handler IAM grant, config getter + merge-list, `SlackChannelStore` helper, and `km create` wiring.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-CONTEXT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-RESEARCH.md

# Master TDD plan — code-bearing source for this plan (Tasks 7–10). APPLY THE 4 CORRECTIONS BELOW.
@docs/superpowers/plans/2026-06-10-slack-channel-o1-resolution.md

# Reference implementations to mirror
@pkg/slack/bridge/aws_adapters.go
@infra/modules/km-operator-policy/v1.0.0/main.tf
@infra/modules/create-handler/v1.0.0/main.tf
@infra/live/use1/create-handler/terragrunt.hcl
@internal/app/config/config.go
@internal/app/cmd/create.go

<interfaces>
<!-- From plan 104-01 (cmd package): the resolver consumes this interface; the struct below satisfies it. -->
type SlackChannelStore interface {            // internal/app/cmd/create_slack.go
    GetByAlias(ctx context.Context, alias string) (channelID string, err error)
    UpsertByAlias(ctx context.Context, alias, channelID string) error
}

<!-- New concrete struct THIS plan creates (pkg/aws) — same method set, satisfies the cmd interface. -->
type SlackChannelGetPutAPI interface {         // pkg/aws/slack_channels.go
    GetItem(ctx, *dynamodb.GetItemInput, ...func(*dynamodb.Options)) (*dynamodb.GetItemOutput, error)
    PutItem(ctx, *dynamodb.PutItemInput, ...func(*dynamodb.Options)) (*dynamodb.PutItemOutput, error)
}
type SlackChannelStore struct { Client SlackChannelGetPutAPI; TableName string }

<!-- Reference patterns (confirmed in RESEARCH): -->
<!-- km-operator-policy main.tf:109 aws_iam_role_policy.dynamodb_slack_threads — count=var.X!=""?1:0 -->
<!-- create-handler main.tf:67 module "km_operator_policy" { slack_threads_table_name = var.slack_threads_table_name } -->
<!-- create-handler/terragrunt.hcl:63 slack_threads_table_name = "${local.site_vars.locals.site.label}-slack-threads"  (STATIC string, NO dependency block) -->
<!-- config.go:667 merge-list loop; :582 SetDefault; getter mirrors GetSlackThreadsTableName -->
<!-- create.go:504 os.Setenv("KM_SLACK_THREADS_TABLE", ...) — table names set at RUNTIME, NOT via TF env var -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: create-handler IAM grant + plumbing (km-operator-policy + create-handler + live input)</name>
  <files>infra/modules/km-operator-policy/v1.0.0/variables.tf, infra/modules/km-operator-policy/v1.0.0/main.tf, infra/modules/create-handler/v1.0.0/variables.tf, infra/modules/create-handler/v1.0.0/main.tf, infra/live/use1/create-handler/terragrunt.hcl</files>
  <action>
Lift from master plan Task 7 (lines ~726-832) BUT apply CORRECTIONS #1 and #2 from the planning context (they override the master plan).

1. km-operator-policy variables.tf: add `variable "slack_channels_table_name" { type = string; default = "" }`.
2. km-operator-policy main.tf: add `resource "aws_iam_role_policy" "dynamodb_slack_channels"` mirroring the `dynamodb_slack_threads` resource at line ~109: `count = var.slack_channels_table_name != "" ? 1 : 0`, name `"${var.resource_prefix}-create-handler-dynamodb-slack-channels"`, role `var.role_id`, policy granting `dynamodb:GetItem`, `dynamodb:PutItem`, `dynamodb:DescribeTable` on `arn:aws:dynamodb:*:${data.aws_caller_identity.current.account_id}:table/${var.slack_channels_table_name}`. (No UpdateItem — upsert is a full PutItem.)
3. create-handler variables.tf: add `variable "slack_channels_table_name" { type = string; default = "" }`.
4. create-handler main.tf: in the `module "km_operator_policy"` block, add `slack_channels_table_name = var.slack_channels_table_name` next to the existing `slack_threads_table_name` pass-through.
5. **CORRECTION #1 — create-handler live unit: NO dependency block.** In `infra/live/use1/create-handler/terragrunt.hcl`, DISCARD the master plan Task 7 Step 4 `dependency "slack_channels"` block. Instead set the input as a STATIC string mirroring the existing `slack_threads_table_name` at line 63: `slack_channels_table_name = "${local.site_vars.locals.site.label}-slack-channels"`. The IAM grant uses the table NAME string (not an ARN output), so no output reference / dependency coupling is needed — and a dependency block would break `km init --plan` on fresh installs where the table unit hasn't applied yet.
6. **CORRECTION #2 — no new TF env var.** Do NOT add `KM_SLACK_CHANNELS_TABLE` to the create-handler Lambda `environment.variables` block. The table name is derived at runtime via `cfg.GetSlackChannelsTableName()` (set programmatically, mirroring how `create.go:504` does `os.Setenv("KM_SLACK_THREADS_TABLE", ...)`). The IAM grant is what matters at runtime.
7. Smoke (best-effort; live ADD verification is in 104-05): `make build && ./km init --plan 2>&1 | grep -iE 'slack-channels|create-handler' | head` should plan cleanly with the new IAM resource as an add.
8. Commit: `feat(infra): create-handler IAM + static-string plumbing for km-slack-channels (no dependency block, no env var)`.
  </action>
  <verify>
    <automated>cd infra/modules/km-operator-policy/v1.0.0 && terraform init -backend=false && terraform validate && cd ../../create-handler/v1.0.0 && terraform init -backend=false && terraform validate</automated>
  </verify>
  <done>Both modules terraform-validate clean; IAM resource is count-gated GetItem/PutItem/DescribeTable; create-handler passes the var through; live unit uses a STATIC string input (no dependency block); no KM_SLACK_CHANNELS_TABLE env var added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config getter + merge-list, and pkg/aws.SlackChannelStore DDB helper</name>
  <files>internal/app/config/config.go, internal/app/config/config_test.go, pkg/aws/slack_channels.go, pkg/aws/slack_channels_test.go</files>
  <behavior>
    GetSlackChannelsTableName (3 cases): nil receiver → "km-slack-channels"; explicit SlackChannelsTableName="custom-tbl" → "custom-tbl"; ResourcePrefix="sec", no override → "sec-slack-channels".
    SlackChannelStore (fake DDB): UpsertByAlias("github-bot","C0X") then GetByAlias("github-bot") → "C0X"; GetByAlias("absent") → ""/nil.
  </behavior>
  <action>
Lift from master plan Task 8 (lines ~836-893) and Task 9 (lines ~897-1011). APPLY CORRECTION #3 (merge-list mandatory).

1. config_test.go: add `TestGetSlackChannelsTableName` (the 3 cases above). Run `go test ./internal/app/config/ -run TestGetSlackChannelsTableName -v` — expect FAIL.
2. config.go: add struct field `SlackChannelsTableName string` (same mapstructure/yaml tag style as `SlackThreadsTableName`); add `v.SetDefault("slack_channels_table_name", "")` (~line 582); add the getter `GetSlackChannelsTableName()` mirroring `GetSlackThreadsTableName` (nil → "km-slack-channels"; explicit override wins; else `GetResourcePrefix() + "-slack-channels"`); populate the struct field where `SlackThreadsTableName` is populated (~line 755).
3. **CORRECTION #3 — merge-list:** add `"slack_channels_table_name"` to the v2→v merge loop at `internal/app/config/config.go:667` (alongside `"slack_threads_table_name"`). Without this the km-config.yaml key is silently ignored (memory project_config_key_merge_list).
4. Run `go test ./internal/app/config/ -run TestGetSlackChannelsTableName -v` — expect PASS.
5. pkg/aws/slack_channels_test.go: add `TestSlackChannelStore_UpsertThenGet` + `TestSlackChannelStore_GetMiss` (master plan lines ~906-925). Add a `fakeDDB` (GetItem/PutItem map-backed) if one is not already present in the package. Run `go test ./pkg/aws/ -run TestSlackChannelStore -v` — expect FAIL.
6. pkg/aws/slack_channels.go: create the file (master plan lines ~936-997) — `SlackChannelGetPutAPI` interface (GetItem + PutItem only), `SlackChannelStore{Client, TableName}`, `GetByAlias` (returns "" on miss, reads `out.Item["channel_id"]` as AttributeValueMemberS), `UpsertByAlias` (PutItem with `alias`/`channel_id`/`updated_at` attrs). **NO ConditionExpression — always overwrite** (this is a write-through upsert, unlike DDBThreadStore.Upsert which guards with attribute_not_exists). **No TTL attribute.**
7. Run `go test ./pkg/aws/ -run TestSlackChannelStore -v` — expect PASS.
8. Commit: `feat(config+aws): GetSlackChannelsTableName getter (+merge-list) + SlackChannelStore DDB helper`.
  </action>
  <verify>
    <automated>go test ./internal/app/config/ -run TestGetSlackChannelsTableName -v && go test ./pkg/aws/ -run TestSlackChannelStore -v</automated>
  </verify>
  <done>TestGetSlackChannelsTableName (3 cases) and TestSlackChannelStore (UpsertThenGet + GetMiss) pass; merge-list entry present; SlackChannelStore has no ConditionExpression and no TTL.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the DDB store into km create resolution</name>
  <files>internal/app/cmd/create.go, internal/app/cmd/create_slack.go, internal/app/cmd/create_slack_test.go</files>
  <action>
Lift from master plan Task 10 (lines ~1015-1049).

1. create_slack.go: extend `resolveSlackChannel` signature to accept the `store SlackChannelStore` parameter (the interface from plan 104-01) and thread it into `lookupStoredChannelID` / `storeChannelMapping`. (If plan 104-01 already added the param with a nil pass-through, this task just confirms it and removes the nil.)
2. create.go (~597, alongside the existing `slack.NewClient(botToken, nil)` + SSM store construction): build `ddbClient := dynamodb.NewFromConfig(awsCfg)` and `channelStore := &awspkg.SlackChannelStore{ Client: ddbClient, TableName: cfg.GetSlackChannelsTableName() }`, then pass `channelStore` into `resolveSlackChannel(...)`. Confirm the `awsCfg` / `awspkg` import aliases match what create.go already uses (the file already imports the pkg/aws package and an AWS config).
3. Update any test call sites of `resolveSlackChannel` to pass a `fakeChannelStore` (the test fake from plan 104-01).
4. `make build && go test ./internal/app/... ./pkg/... 2>&1 | tail -20` — expect PASS.
5. Commit: `feat(slack): wire SlackChannelStore into km create resolution (P2)`.

Note (CORRECTION #2 reinforced): no env var is set for the table — `cfg.GetSlackChannelsTableName()` is read directly at create time; `cfg` is fully loaded before `resolveSlackChannel` runs.
  </action>
  <verify>
    <automated>make build && go test ./internal/app/... ./pkg/... 2>&1 | tail -20</automated>
  </verify>
  <done>km create builds a real pkg/aws.SlackChannelStore from cfg.GetSlackChannelsTableName() and passes it into resolveSlackChannel; whole Go suite green; the resolver's DDB read-first / write-through path is now live (no longer nil).</done>
</task>

</tasks>

<verification>
- `go test ./internal/app/config/ ./pkg/aws/ ./internal/app/cmd/ ./pkg/slack/ 2>&1 | tail -20` — green.
- `cd infra/modules/km-operator-policy/v1.0.0 && terraform validate` and `cd infra/modules/create-handler/v1.0.0 && terraform validate` — both Success.
- Per 104-VALIDATION.md: TestSlackChannelStore, TestGetSlackChannelsTableName green; the `km init --plan` create-handler/slack-channels ADD smoke is a deploy/manual gate (104-05).
- create-handler live unit contains a STATIC `slack_channels_table_name` string, NOT a `dependency "slack_channels"` block.
</verification>

<success_criteria>
- ROADMAP success criterion 6: fresh-alias create writes the mapping to DDB (and SSM); recreate hits the O(1) DDB path.
- ROADMAP success criterion 8: full deploy surface for the store — IAM (var→policy→wiring→live input), config getter, Go helper, runtime table-name derivation. Multi-install prefixes don't collide.
- The 4 corrections are honored: no dependency block, no TF env var, merge-list present, no orphan-scan involvement (orphan exclusion enforced in plan 104-04 doctor task).
</success_criteria>

<output>
After completion, create `.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-03-SUMMARY.md`
</output>
