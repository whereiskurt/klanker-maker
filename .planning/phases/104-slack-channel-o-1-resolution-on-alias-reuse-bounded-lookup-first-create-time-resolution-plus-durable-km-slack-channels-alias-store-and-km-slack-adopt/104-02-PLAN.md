---
phase: 104-slack-channel-o-1-resolution-on-alias-reuse
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - infra/modules/dynamodb-slack-channels/v1.0.0/main.tf
  - infra/modules/dynamodb-slack-channels/v1.0.0/variables.tf
  - infra/modules/dynamodb-slack-channels/v1.0.0/outputs.tf
  - infra/live/use1/dynamodb-slack-channels/terragrunt.hcl
  - internal/app/cmd/init.go
autonomous: true
requirements: [SLACK-CHAN-STORE, SLACK-CHAN-DEPLOY]
must_haves:
  truths:
    - "A dedicated km-slack-channels DynamoDB table exists with hash_key alias and NO TTL (mapping survives destroy/recreate)"
    - "km init enumerates the dynamodb-slack-channels module (after a fresh make build)"
    - "Multi-install prefixes don't collide — table name derives from site label"
  artifacts:
    - path: "infra/modules/dynamodb-slack-channels/v1.0.0/main.tf"
      provides: "aws_dynamodb_table.slack_channels (PK alias, PAY_PER_REQUEST, SSE on, no TTL)"
      contains: "hash_key     = \"alias\""
    - path: "infra/live/use1/dynamodb-slack-channels/terragrunt.hcl"
      provides: "Live terragrunt unit deriving table_name from site label"
      contains: "slack-channels"
    - path: "internal/app/cmd/init.go"
      provides: "regionalModules entry for dynamodb-slack-channels"
      contains: "dynamodb-slack-channels"
  key_links:
    - from: "internal/app/cmd/init.go regionalModules"
      to: "infra/live/use1/dynamodb-slack-channels/terragrunt.hcl"
      via: "module name → live unit dir resolution"
      pattern: "dynamodb-slack-channels"
---

<objective>
Provision the durable authoritative store: a dedicated `km-slack-channels` DynamoDB table (hash_key `alias`, **no TTL**, PAY_PER_REQUEST, SSE on) — the P2 store read first during create-time resolution and written through on create/resolve. Unlike `km-slack-threads` (which TTLs conversations) this table must persist mappings across destroy/recreate cycles indefinitely; a stale mapping self-heals via the `channel_not_found` recreate path from plan 104-01.

Mirror the existing `dynamodb-slack-threads` module, live unit, and `init.go` registration exactly. This plan delivers only the table + its registration; the create-handler IAM grant, config getter, Go store helper, and resolver wiring land in plan 104-03.

Purpose: A durable alias→channel_id mapping that survives the destroy/recreate cycle that `archiveOnDestroy:false` forces (and that the deleted-on-destroy `km-sandboxes` row could not provide).
Output: TF module `dynamodb-slack-channels/v1.0.0`, live unit, and `init.go` regionalModules entry.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-CONTEXT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-RESEARCH.md

# Master TDD plan — code-bearing source for this plan (Tasks 4–6)
@docs/superpowers/plans/2026-06-10-slack-channel-o1-resolution.md

# Reference implementations to mirror EXACTLY (read the real source, not a .terragrunt-cache copy)
@infra/modules/dynamodb-slack-threads/v1.0.0/main.tf
@infra/live/use1/dynamodb-slack-threads/terragrunt.hcl
@internal/app/cmd/init.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: dynamodb-slack-channels Terraform module (PK alias, no TTL)</name>
  <files>infra/modules/dynamodb-slack-channels/v1.0.0/main.tf, infra/modules/dynamodb-slack-channels/v1.0.0/variables.tf, infra/modules/dynamodb-slack-channels/v1.0.0/outputs.tf</files>
  <action>
Mirror `dynamodb-slack-threads` but with a single hash key `alias` and NO TTL. Lift from master plan Task 4 (lines ~547-629).

1. Read `infra/modules/dynamodb-slack-threads/v1.0.0/main.tf` first to match the exact resource attribute style (billing_mode, point_in_time_recovery, server_side_encryption, tags shape).
2. Create `main.tf` with `resource "aws_dynamodb_table" "slack_channels"`: `name = var.table_name`, `billing_mode = "PAY_PER_REQUEST"`, `hash_key = "alias"`, one `attribute { name = "alias"; type = "S" }`, `point_in_time_recovery { enabled = false }`, `server_side_encryption { enabled = true }`, and `tags = merge(var.tags, { Name = var.table_name, Component = "km-slack-channels" })`. Comment that there is NO TTL — the mapping must persist across recreate; stale rows self-heal via the channel_not_found recreate path.
3. Create `variables.tf` with `table_name` (string, default `"km-slack-channels"`) and `tags` (map(string), default `{}`).
4. Create `outputs.tf` with `table_name` (= .name) and `table_arn` (= .arn).
5. Do NOT add a `required_providers` block — root.hcl's generate stanza owns the provider declaration (memory project_terragrunt_providers_in_root).
6. Validate: `cd infra/modules/dynamodb-slack-channels/v1.0.0 && terraform init -backend=false && terraform validate`.
7. Commit: `feat(infra): dynamodb-slack-channels table module (PK alias, no TTL)`.

CORRECTION applied: no TTL attribute, no `ttl {}` block (contrast km-slack-threads which has a 30-day ttl_expiry). No ConditionExpression concern here (that's a store-side concern in 104-03).
  </action>
  <verify>
    <automated>cd infra/modules/dynamodb-slack-channels/v1.0.0 && terraform init -backend=false && terraform validate</automated>
  </verify>
  <done>terraform validate prints "Success! The configuration is valid."; module has main/variables/outputs.tf; PK is alias; no TTL; no required_providers block.</done>
</task>

<task type="auto">
  <name>Task 2: Live terragrunt unit + init.go regionalModules registration</name>
  <files>infra/live/use1/dynamodb-slack-channels/terragrunt.hcl, internal/app/cmd/init.go</files>
  <action>
Lift from master plan Task 5 (lines ~633-688) and Task 6 (lines ~692-722).

1. Read `infra/live/use1/dynamodb-slack-threads/terragrunt.hcl` (the real file, not a cache copy) and replicate its `locals` / `include "root"` / `remote_state` / `terraform { source }` / `inputs` shape EXACTLY. site.hcl key names must match identically.
2. Create `infra/live/use1/dynamodb-slack-channels/terragrunt.hcl` changing only: the remote_state `key` to `.../dynamodb-slack-channels/terraform.tfstate`, `terraform.source` to `infra/modules/dynamodb-slack-channels/v1.0.0`, `inputs.table_name` to `"${local.site_vars.locals.site.label}-slack-channels"`, and `tags["km:component"]` to `"km-slack-channels"`.
3. In `internal/app/cmd/init.go`, add a regionalModules entry immediately AFTER the `dynamodb-slack-threads` entry (~277-282): `{ name: "dynamodb-slack-channels", dir: filepath.Join(regionDir, "dynamodb-slack-channels"), envReqs: nil }`.
4. `make build` (NOT bare `go build` — needs ldflags; memory feedback_rebuild_km). A stale km binary silently skips the new module (memory project_make_build_precedes_km_init), so the binary MUST be rebuilt before the plan-list check.
5. Smoke: `./km init --plan 2>&1 | grep -i slack-channels` should enumerate the new module. (If AWS creds aren't reachable in this environment the grep may not run a full plan; the build-succeeds + module-enumerated check is the gate. The live `km init --plan` ADD verification with `AWS_PROFILE=klanker-application` is captured in plan 104-05 deploy audit / 104-VALIDATION manual-only.)
6. Commit: `feat(init): live terragrunt unit + regional-module registration for dynamodb-slack-channels`.

CORRECTION applied (planning context #5): `make build` BEFORE the `km init` check, every time.
  </action>
  <verify>
    <automated>make build && ./km init --plan 2>&1 | grep -i slack-channels || echo "NOTE: plan grep may require live AWS; build success is the hard gate"</automated>
  </verify>
  <done>km binary rebuilds clean; init.go has the dynamodb-slack-channels regionalModules entry; live unit mirrors slack-threads with the three changed lines; module enumerated by km init (or build-green if AWS unreachable here).</done>
</task>

</tasks>

<verification>
- `cd infra/modules/dynamodb-slack-channels/v1.0.0 && terraform init -backend=false && terraform validate` — Success.
- `make build` — succeeds with ldflags.
- `./km init --plan 2>&1 | grep -i slack-channels` — module enumerated (live ADD verification deferred to 104-05 / manual UAT).
- Per 104-VALIDATION.md: the terraform validate smoke and the init.go enumeration smoke are green.
</verification>

<success_criteria>
- ROADMAP success criterion 8 (partial): TF module + live unit + init.go regionalModules entry present.
- The table has PK `alias` and no TTL (criterion 6 substrate — mapping survives recreate).
- Multi-install prefixes don't collide (table name derives from site label).
</success_criteria>

<output>
After completion, create `.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-02-SUMMARY.md`
</output>
