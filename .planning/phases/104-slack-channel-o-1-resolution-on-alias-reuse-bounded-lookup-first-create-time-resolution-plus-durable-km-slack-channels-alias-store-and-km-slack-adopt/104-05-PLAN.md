---
phase: 104-slack-channel-o-1-resolution-on-alias-reuse
plan: 05
type: execute
wave: 5
depends_on: [104-01, 104-02, 104-03, 104-04]
files_modified:
  - docs/slack-notifications.md
  - CLAUDE.md
autonomous: false
requirements: [SLACK-CHAN-E2E, SLACK-CHAN-DEPLOY]
must_haves:
  truths:
    - "docs/slack-notifications.md documents the bounded resolver, KM_SLACK_RESOLVE_BUDGET / KM_SLACK_MAX_SCAN_PAGES (default 0=off), the km-slack-channels table, channelOverride manual escape, and km slack adopt"
    - "CLAUDE.md has a Phase 104 note + deploy sequence (make build BEFORE km init; full apply not --sidecars) + a where-to-look row"
    - "Full deploy surface audited present: module + live unit + init.go entry + IAM (var→policy→wiring→live input) + config getter + Go store helper + runtime table-name derivation"
    - "On the real large workspace, a reused-alias km create completes bounded (~2 min) with slack_resolve path=cache_hit|created, never an unbounded scan"
    - "A cold orphan channel adopted via km slack adopt then resolves O(1) on recreate"
  artifacts:
    - path: "docs/slack-notifications.md"
      provides: "O(1) channel resolution section + km slack adopt runbook"
      contains: "km slack adopt"
    - path: "CLAUDE.md"
      provides: "Phase 104 note + deploy sequence + where-to-look row"
      contains: "Phase 104"
  key_links:
    - from: "CLAUDE.md Phase 104 note"
      to: "docs/slack-notifications.md O(1) resolution section"
      via: "where-to-look table row"
      pattern: "slack-notifications"
---

<objective>
Close the phase: write the operator-facing docs, run the deploy-surface self-audit (this repo has shipped code-green-but-undeployable phases before — memory feedback_verify_deploy_surface_not_just_code), and gate the live large-workspace UAT that the incident demands (the spec's Phase 0 confirmation, now shipped inline via the `slack_resolve` log).

The unit-level behavior is proven by plans 01–04; this plan proves the thing is documented, fully deployable, and actually fixes the incident on the real corporate Slack.

Purpose: Ship-ready docs + a verified deploy surface + the live confirmation that the 900s wedge is gone.
Output: Updated `docs/slack-notifications.md` and `CLAUDE.md`; a deploy-surface audit; an operator UAT checkpoint.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-CONTEXT.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-RESEARCH.md
@.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-VALIDATION.md

# Master TDD plan — Task 13 + Final verification checklist
@docs/superpowers/plans/2026-06-10-slack-channel-o1-resolution.md

# Files to update
@docs/slack-notifications.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs — slack-notifications.md section + CLAUDE.md phase note</name>
  <files>docs/slack-notifications.md, CLAUDE.md</files>
  <action>
Lift from master plan Task 13 (lines ~1172-1187).

1. docs/slack-notifications.md: add a section "O(1) channel resolution on alias reuse (Phase 104)" covering: the incident (unbounded conversations.list wedging the 900s create-handler); the bounded lookup-first resolver; the env knobs `KM_SLACK_RESOLVE_BUDGET` (default 45s) and `KM_SLACK_MAX_SCAN_PAGES` (default 0 = scan OFF / fail-fast, opt-in >0 for a page-capped scan); the durable `km-slack-channels` DDB table (PK alias, no TTL, authoritative read-first + write-through, SSM by-name kept as back-compat fallback); `notification.slack.channelOverride` as the zero-lookup manual escape (Mode 3, unchanged); and `km slack adopt <alias> <channelID>` for orphaned channels with the "find the Channel ID in Slack → channel → About → Channel ID" hint + the bot-must-be-a-member precondition. Note the `slack_resolve path=cache_hit|cache_optimistic|created|scan_capped|failfast ms=…` observability line.
2. CLAUDE.md: add a Phase 104 note (mirror the existing phase-note style at the top) summarizing: new `km-slack-channels` table (PK alias, no TTL); bounded/fail-fast create-time resolution; `km slack adopt`; bridge is NOT a consumer (no lambda-slack-bridge change); NO SandboxProfile schema change ⇒ no `--sidecars`, existing sandboxes unaffected (create-time fix). Deploy = **`make build` (binary picks up the new module) BEFORE `km init`** + `make build-lambdas` + `km init --dry-run=false` (new table + IAM ⇒ full apply, NOT `--sidecars`). Add a "Where to look" table row → `docs/slack-notifications.md` § Phase 104.
3. `scripts/validate-all-profiles.sh` — confirm no profile-schema regression (none expected; no schema change in this phase).
4. Commit: `docs(slack): O(1) channel resolution + km slack adopt runbook (Phase 104)`.

Use generic placeholders in any examples (example.com / synthetic IDs) per memory feedback_generic_doc_examples. The real incident channel ID C0B91RA9CPR may appear only where it is genuinely operational context (UAT runbook), not as a customer-naming example.
  </action>
  <verify>
    <automated>scripts/validate-all-profiles.sh</automated>
  </verify>
  <done>slack-notifications.md has the Phase 104 O(1) section (resolver + env knobs + table + channelOverride + km slack adopt); CLAUDE.md has the phase note + deploy sequence + where-to-look row; validate-all-profiles.sh green (no schema regression).</done>
</task>

<task type="auto">
  <name>Task 2: Deploy-surface self-audit + full suite green</name>
  <files>docs/slack-notifications.md</files>
  <action>
Run the deploy-surface audit (memory feedback_verify_deploy_surface_not_just_code). This is a verification task — record the audit result in the SUMMARY; touch docs only if a gap is found and fixed.

1. `make build` — succeeds with ldflags + the new module registered in init.go.
2. `go test ./... 2>&1 | tail -30` — all green.
3. Audit each deploy-surface item is present (grep/read, not assume):
   - TF module `infra/modules/dynamodb-slack-channels/v1.0.0/{main,variables,outputs}.tf` ✓
   - live unit `infra/live/use1/dynamodb-slack-channels/terragrunt.hcl` ✓
   - `init.go` regionalModules entry `dynamodb-slack-channels` ✓
   - km-operator-policy `slack_channels_table_name` var + `aws_iam_role_policy.dynamodb_slack_channels` (count-gated) ✓
   - create-handler `slack_channels_table_name` var + pass-through into `module "km_operator_policy"` ✓
   - create-handler live unit STATIC input `slack_channels_table_name = "${...site.label}-slack-channels"` (NO dependency block — CORRECTION #1) ✓
   - NO `KM_SLACK_CHANNELS_TABLE` in the create-handler TF env block (CORRECTION #2) ✓
   - config: `SlackChannelsTableName` field + `GetSlackChannelsTableName()` getter + SetDefault + merge-list entry (CORRECTION #3) ✓
   - `pkg/aws.SlackChannelStore` (no TTL, no ConditionExpression) ✓
   - runtime table-name derivation via `cfg.GetSlackChannelsTableName()` (no env var) ✓
   - `km slack adopt` registered ✓
   - `km doctor` existence check present; slack-channels NOT in checkOrphanedDDBRows (CORRECTION #4) ✓
   - `make build` before `km init` documented (CORRECTION #5) ✓
4. If live AWS is reachable here: `AWS_PROFILE=klanker-application ./km init --plan 2>&1 | grep -iE 'slack-channels|create-handler'` should show the table + IAM as ADDs with no destroy-class trip. If not reachable, mark as operator-gated (Task 3 checkpoint covers it).
5. Record the audit checklist outcome in the SUMMARY. Commit only if a fix was required: `fix(104): close deploy-surface gap <desc>`.
  </action>
  <verify>
    <automated>make build && go test ./... 2>&1 | tail -30</automated>
  </verify>
  <done>Full Go suite green; every deploy-surface item confirmed present (12-point checklist) with all 5 corrections honored; audit outcome recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live large-workspace UAT (operator)</name>
  <files>(none — operator-run live verification on the real install; no repo files modified)</files>
  <action>
    BLOCKING CHECKPOINT — operator-run live UAT, not autonomous work. Claude pauses here. The operator deploys to the real install and exercises the reused-alias create on the corporate Slack, then reports the observed `slack_resolve path=` values and create wall-clock times. The exact deploy + test steps are in <how-to-verify>; resume per <resume-signal>. Do not mark this task done until the operator confirms.
  </action>
  <what-built>
    Bounded, lookup-first, durable-store Slack channel resolution at km create time, deployed to the real install:
    - `make build` + `make build-lambdas` + `km init --dry-run=false` apply the km-slack-channels table + create-handler IAM (full apply, NOT --sidecars).
    - The resolver reads the DDB store first, validates with bounded-retry, never enumerates on a transient blip, and fails fast (no infra provisioned) on name_taken+no-mapping+scan-off.
  </what-built>
  <how-to-verify>
    Operator, on the REAL corporate Slack (thousands of channels) with AWS_PROFILE=klanker-application:

    1. DEPLOY (order matters — stale binary skips the new module):
       `make build` then `make build-lambdas` then `AWS_PROFILE=klanker-application km init --plan` (confirm dynamodb-slack-channels + create-handler IAM show as ADDs, NO destroy-class trips), then `AWS_PROFILE=klanker-application km init --dry-run=false`.
       Then `AWS_PROFILE=klanker-application km doctor 2>&1 | grep -i slack-channels` — table reported present.

    2. REUSED-ALIAS CREATE (the incident scenario):
       `km create profiles/github-review.yaml --alias github-bot --wait`
       Expected: completes in ~2 min (NOT the 900s wedge). Inspect create-handler logs for the `slack_resolve path=…` line — must be `cache_hit` or `created`, NEVER an unbounded scan. Re-run a second time (recreate on the same alias) → `path=cache_hit` (O(1) DDB hit).

    3. COLD ORPHAN ADOPT:
       Pick a real orphaned channel (e.g. the incident's `C0B91RA9CPR` for `sb-github-bot-sec`). Run `km slack adopt github-bot C0B91RA9CPR`. Expected: validates format + bot membership, prints `✓ adopted …`. Then `km create … --alias github-bot --wait` → `slack_resolve path=cache_hit`.

    4. NEGATIVE: `km slack adopt github-bot not-an-id` → rejected with the `^C[A-Z0-9]+$` guidance; adopting a channel the bot is NOT in → "not a member, /invite first".

    Report back: the create wall-clock times, the `slack_resolve path=` values observed, and that no create wedged.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed slack_resolve paths + create times, or describe any unbounded scan / wedge / adopt failure.</resume-signal>
</task>

</tasks>

<verification>
- `make build && go test ./... 2>&1 | tail -30` — green.
- `scripts/validate-all-profiles.sh` — green (no schema regression).
- Deploy-surface 12-point checklist all ✓ with the 5 corrections honored.
- Operator UAT: reused-alias create bounded (~2 min), `slack_resolve path=cache_hit|created`, never an unbounded scan; cold orphan adopted → recreate O(1).
</verification>

<success_criteria>
- ROADMAP success criterion 8: full deploy surface present and verified; km doctor reports the table; multi-install prefixes don't collide.
- ROADMAP success criterion 9: no SandboxProfile schema change; existing SSM by-name entries keep working; no regression to Mode-1 (shared) / Mode-3 (override); with slack.enabled:false create is byte-identical.
- SLACK-CHAN-E2E: the live large-workspace UAT confirms the incident is fixed (bounded create, observable O(1) path).
</success_criteria>

<output>
After completion, create `.planning/phases/104-slack-channel-o-1-resolution-on-alias-reuse-bounded-lookup-first-create-time-resolution-plus-durable-km-slack-channels-alias-store-and-km-slack-adopt/104-05-SUMMARY.md`
</output>
