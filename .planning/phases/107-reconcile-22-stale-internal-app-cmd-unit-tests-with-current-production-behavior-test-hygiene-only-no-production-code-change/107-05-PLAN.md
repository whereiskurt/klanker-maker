---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/create_docker_test.go
  - internal/app/cmd/create_override_test.go
autonomous: true
requirements: [TEST-HYGIENE-CREATE]
must_haves:
  truths:
    - "TestCreateDockerWritesComposeFile asserts only placeholders that exist in create.go today"
    - "TestApplyLifecycleOverrides_RunCreateRemoteSignature asserts the current runCreateRemote signature"
  artifacts:
    - path: "internal/app/cmd/create_docker_test.go"
      provides: "Docker compose placeholder source-grep reconciled to current create.go"
    - path: "internal/app/cmd/create_override_test.go"
      provides: "runCreateRemote signature source-grep reconciled to current create.go"
  key_links:
    - from: "internal/app/cmd/create_override_test.go"
      to: "internal/app/cmd/create.go:runCreateRemote"
      via: "exact signature string grep"
      pattern: "computeBudgetOverride float64, aiBudgetOverride float64"
---

<objective>
Reconcile the 2 stale create-command source-grep tests. Both read `create.go` as
text and assert it contains specific strings; production drifted:
1. The docker-compose injection no longer uses a `PLACEHOLDER_OPERATOR_KEY` token
   (operator-key handling changed); current placeholders are
   `PLACEHOLDER_SANDBOX_ROLE_ARN`, `PLACEHOLDER_SIDECAR_ROLE_ARN`,
   `PLACEHOLDER_PROXY_CA_B64`.
2. `runCreateRemote` gained `computeBudgetOverride float64, aiBudgetOverride
   float64` parameters before `clonedFromOverride ...string`.

Purpose: Pure test hygiene; create.go is the source of truth.
Output: create_docker_test.go + create_override_test.go grep assertions aligned.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current create.go facts (verified) — DO NOT modify create.go. -->
Docker-compose placeholders actually present (create.go ~1810-1824):
  PLACEHOLDER_SANDBOX_ROLE_ARN, PLACEHOLDER_SIDECAR_ROLE_ARN, PLACEHOLDER_PROXY_CA_B64
  (no PLACEHOLDER_OPERATOR_KEY anywhere in create.go)

Current runCreateRemote signature (create.go:2074):
  func runCreateRemote(cfg *config.Config, profilePath string, onDemand bool, noBedrock bool, awsProfile string, aliasOverride string, ttlOverride string, idleOverride string, computeBudgetOverride float64, aiBudgetOverride float64, clonedFromOverride ...string) (string, error)

Verified failures (go test output):
  create_docker_test.go:89 create.go missing placeholder replacement for operator key (expected "PLACEHOLDER_OPERATOR_KEY")
  create_override_test.go:149 runCreateRemote signature missing ttlOverride, idleOverride, or clonedFromOverride parameters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile docker-compose placeholder grep + runCreateRemote signature grep</name>
  <files>internal/app/cmd/create_docker_test.go, internal/app/cmd/create_override_test.go</files>
  <action>
create.go is authoritative; do NOT edit it.

A. TestCreateDockerWritesComposeFile (create_docker_test.go ~lines 76-91): the
   `checks` slice asserts create.go contains `PLACEHOLDER_OPERATOR_KEY`. That
   token no longer exists. Remove the
   `{"placeholder replacement for operator key", "PLACEHOLDER_OPERATOR_KEY"}`
   entry from the `checks` slice. Keep the other checks (sandbox role ARN,
   compose path, compose write, DockerComposeExecFunc). Optionally replace the
   removed entry with a current placeholder that DOES exist, e.g.
   `{"placeholder replacement for sidecar role ARN", "PLACEHOLDER_SIDECAR_ROLE_ARN"}`,
   to keep coverage. Do not touch the functional-write portion of the test below
   the checks slice (it already passes).

B. TestApplyLifecycleOverrides_RunCreateRemoteSignature (create_override_test.go
   ~lines 138-151): it asserts an exact-substring match of the runCreateRemote
   signature that omits the two budget params. Update the expected signature
   string to the CURRENT one (copy verbatim from the interfaces block above /
   create.go:2074), i.e. insert `computeBudgetOverride float64, aiBudgetOverride
   float64, ` before `clonedFromOverride ...string`. Keep the assertion an exact
   `strings.Contains` on that full signature.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestCreateDockerWritesComposeFile|TestApplyLifecycleOverrides' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>TestCreateDockerWritesComposeFile and TestApplyLifecycleOverrides_RunCreateRemoteSignature PASS; output `ok` + EXIT=0. (TestApplyLifecycleOverrides_* sibling tests, if any, stay green.)</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestCreateDocker|TestApplyLifecycleOverrides' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`git diff --stat` shows ONLY internal/app/cmd/create_docker_test.go and create_override_test.go changed.
</verification>

<success_criteria>
Both create source-grep tests pass against the current create.go text; no
production file edited.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-05-SUMMARY.md`
</output>
