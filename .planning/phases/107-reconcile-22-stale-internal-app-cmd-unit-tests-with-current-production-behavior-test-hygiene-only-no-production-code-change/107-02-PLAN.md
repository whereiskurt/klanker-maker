---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/email_test.go
autonomous: true
requirements: [TEST-HYGIENE-EMAIL]
must_haves:
  truths:
    - "Email send/read tests seed the prefix-scoped SSM key path the production code now reads"
    - "TestEmailSend_SuccessNoAttachments / _TwoAttachments / _BodyFromStdin pass (no ParameterNotFound)"
    - "TestEmailRead_EncryptedMessageAutoDecrypts decrypts the plaintext"
  artifacts:
    - path: "internal/app/cmd/email_test.go"
      provides: "Email SSM key-path mocks reconciled to /{prefix}/sandbox/{id}/... convention"
      contains: "/km/sandbox/sb-sender1/signing-key"
  key_links:
    - from: "internal/app/cmd/email_test.go"
      to: "pkg/aws/identity.go:SigningKeyPath"
      via: "mock-seeded SSM parameter name"
      pattern: "/km/sandbox/.*signing-key"
---

<objective>
Reconcile the 4 stale email tests whose SSM mocks seed the old un-prefixed key
path. Production now reads `/{resource_prefix}/sandbox/{id}/signing-key` (and
`.../encryption-key`); with the default prefix `km`, that is `/km/sandbox/...`.

Purpose: `SigningKeyPath`/`EncryptionKeyPath` (pkg/aws/identity.go:99-108) were
prefix-scoped to avoid multi-install SSM collisions. The test mocks still seed
`/sandbox/{id}/signing-key`, so the lookup misses → `ParameterNotFound`. Pure
test hygiene — update the seeded key names.
Output: email_test.go mocks aligned to the prefix-scoped SSM convention.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current production key-path helpers (pkg/aws/identity.go) — DO NOT modify them. -->
func SigningKeyPath(resourcePrefix, sandboxID string) string  // "/{prefix}/sandbox/{id}/signing-key"
func EncryptionKeyPath(resourcePrefix, sandboxID string) string // "/{prefix}/sandbox/{id}/encryption-key"

testEmailCfg() sets no ResourcePrefix → cfg.GetResourcePrefix() == "km".
So the production-read paths are:
  /km/sandbox/sb-sender1/signing-key
  /km/sandbox/{id}/encryption-key

Verified failures (go test output):
  email_test.go:406 send email: read signing key from SSM (/km/sandbox/sb-sender1/signing-key): ParameterNotFound
  email_test.go:456 ...same...
  email_test.go:514 ...same...
  email_test.go:788 expected decrypted plaintext "secret message" ... got a non-decrypted listing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-key the email SSM mocks to the /km/ prefix-scoped paths</name>
  <files>internal/app/cmd/email_test.go</files>
  <action>
The mock SSM seed maps use the OLD un-prefixed paths. Update each seeded SSM
parameter NAME to the prefix-scoped form the production code now reads. Do NOT
change production code or the helpers in pkg/aws.

Signing-key seeds (currently `/sandbox/sb-sender1/signing-key`), at the maps near
lines 356, 387, 427, 480 → change to `/km/sandbox/sb-sender1/signing-key`.

The invalid-id seed near line 325 (`/sandbox/INVALID/signing-key`) → change to
`/km/sandbox/INVALID/signing-key` for consistency (that test asserts a downstream
validation error, but keep the key namespace correct).

Encryption-key seed near line 779 — currently
`fmt.Sprintf("/sandbox/%s/encryption-key", sandboxID)` → change to
`fmt.Sprintf("/km/sandbox/%s/encryption-key", sandboxID)` so
TestEmailRead_EncryptedMessageAutoDecrypts finds the NaCl private key and
decrypts.

Rule of thumb: every seeded SSM parameter name of the form `/sandbox/...` becomes
`/km/sandbox/...`. Grep the file for `"/sandbox/` and `/sandbox/%s` to find them
all; there should be no remaining un-prefixed `/sandbox/...signing-key` or
`/sandbox/...encryption-key` seeds after this change. Do not touch the `Domain`,
`IdentityTableName`, or other config fields.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestEmail(Send|Read)' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>TestEmailSend_SuccessNoAttachments, _TwoAttachments, _BodyFromStdin, and TestEmailRead_EncryptedMessageAutoDecrypts all PASS; no ParameterNotFound; output `ok` + EXIT=0.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestEmail(Send|Read)' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`git diff --stat` shows ONLY internal/app/cmd/email_test.go changed.
</verification>

<success_criteria>
All four email tests pass against the prefix-scoped SSM key paths; no production
code edited.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-02-SUMMARY.md`
</output>
