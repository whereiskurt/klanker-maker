---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - internal/app/cmd/shell.go
autonomous: true
requirements: [TEST-HYGIENE-SHELL-FIX]
must_haves:
  truths:
    - "km shell on a stopped sandbox returns (not swallows) a pre-flight error"
    - "km shell on an unsupported substrate returns a pre-flight error"
    - "km shell on an EC2 sandbox with no instance ARN returns a pre-flight error"
    - "km shell still swallows interactive SSM session-exit errors (no spurious cobra output)"
  artifacts:
    - path: "internal/app/cmd/shell.go"
      provides: "NewShellCmdWithFetcher RunE returns pre-flight errors, swallows only session-exit errors"
      contains: "preflight"
  key_links:
    - from: "internal/app/cmd/shell.go:NewShellCmdWithFetcher RunE"
      to: "internal/app/cmd/shell.go:runShellWithSSM pre-flight returns"
      via: "typed pre-flight error returned to cobra"
      pattern: "errors.As"
---

<objective>
THE ONE SANCTIONED PRODUCTION CHANGE of Phase 107 (LOCKED escalation decision).
Fix `NewShellCmdWithFetcher` so its RunE returns pre-flight errors
(stopped sandbox / unsupported substrate / missing instance ARN) instead of
silently swallowing them with an unconditional `return nil`, while STILL swallowing
the interactive SSM/ECS/docker session-exit error (the original intent of
commit aaf76a4f — suppress spurious cobra output after a normal session exit).

Purpose: `km shell <stopped|unknown-substrate|missing-instance>` currently exits 0
with no output — a real operator-facing regression. After this fix, pre-flight
errors propagate; session-exit noise is still suppressed. This is ISOLATED in its
OWN plan and MUST be its OWN commit so the production change is cleanly reviewable.
Output: shell.go RunE distinguishes pre-flight errors from session-exit errors.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current shell.go structure (verified). -->
NewShellCmdWithFetcher RunE (shell.go:255-297): non-learn path ends with an
  unconditional `return nil` (line 296) — this swallows EVERYTHING.
newShellCmdWithSSM RunE (shell.go:178-211): the reference pattern — it returns
  runErr directly (lines 207-211); pre-flight AND session errors both propagate
  there because that constructor is test-injection only.

runShellWithSSM (shell.go:333-411) — the PRE-FLIGHT error sites (all return
  BEFORE any execFn is invoked):
    - fetch error (line 356-357)            fmt "fetch sandbox: %w"
    - stopped sandbox (line 360-362)        fmt "...is stopped..."
    - missing EC2 instance ARN (line 366-369) fmt "find EC2 instance...: %w"
    - ECS cluster/task ARN missing (line 397-403)
    - unsupported substrate default (line 408-409) fmt "unsupported substrate %q..."
  The SESSION-EXIT error is whatever execFn returns via execSSMSession /
  execECSCommand / execDockerShell (execFn call sites: shell.go lines
  62, 588, 656, 774, 851).

CONSTRAINT — these existing tests MUST stay green (verified currently passing):
  TestShellCmd_EC2, TestShellCmd_EC2_Root, TestShellCmd_ECS  (execFn returns nil → err nil)
  TestShellCmd_MissingSSMDoc  (execFn returns an InvalidDocument error → MUST still be SWALLOWED;
    the test runs `_ = root2.Execute()` and asserts via the captured execFn error, NOT the RunE return)
  All TestShellDocker* (Plan 01) and shell port-forward/transcript/notify tests.

TARGET — these escalation tests MUST pass after the fix (they already assert err != nil):
  TestShellCmd_StoppedSandbox      (shell_test.go:305-311) expects err containing "stopped"
  TestShellCmd_UnknownSubstrate    (shell_test.go:331-338) expects err containing "k8s"
  TestShellCmd_MissingInstanceID   (shell_test.go:361-364) expects err != nil
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag pre-flight errors and return them from NewShellCmdWithFetcher RunE</name>
  <files>internal/app/cmd/shell.go</files>
  <action>
Apply the SMALLEST change that makes RunE return pre-flight errors while still
swallowing session-exit errors. Recommended approach (a sentinel error type):

1. Add an unexported sentinel error type to shell.go, e.g.
       type preflightError struct{ err error }
       func (e *preflightError) Error() string { return e.err.Error() }
       func (e *preflightError) Unwrap() error  { return e.err }
   (Or use a sentinel `var errPreflight = errors.New(...)` wrapped via fmt %w and
   matched with errors.Is — either is fine. The point: pre-flight errors must be
   distinguishable from session-exit errors.)

2. In runShellWithSSM (shell.go:333-411), wrap ONLY the PRE-FLIGHT returns with
   the sentinel — i.e. the returns at the fetch error, stopped sandbox, missing
   EC2 instance ARN, ECS cluster/task ARN missing, and the unsupported-substrate
   default. Do NOT wrap the values returned by execSSMSession / execECSCommand /
   execDockerShell (those are the session-exit path). Concretely, each pre-flight
   `return fmt.Errorf(...)` becomes `return &preflightError{fmt.Errorf(...)}`
   (preserve the existing message text verbatim — e.g. "stopped", "%q ... unsupported
   substrate", "find EC2 instance" — so the escalation tests' substring checks pass).

3. In NewShellCmdWithFetcher RunE (shell.go:255-297), replace the final
   `return nil` (line 296) in the non-learn path with:
       var pf *preflightError
       if errors.As(runErr, &pf) {
           return runErr   // pre-flight error: surface to the operator
       }
       return nil          // session-exit error: swallow (no spurious cobra output)
   Leave the learn path (lines 290-294) untouched. Leave newShellCmdWithSSM
   (lines 178-211) untouched — its `return runErr` already propagates everything,
   which is correct for the test-injection constructor.

4. Ensure `errors` is imported (it likely already is). Do NOT change runShell,
   runShellWithSSM's control flow, or the exec* helpers beyond wrapping the
   pre-flight returns. Keep the diff tight — this is a reviewable production fix.

5. Sanity-build to confirm compilation: `make build` (per project rule:
   make build, not bare go build). The build is only a compile sanity check; this
   plan ships no Lambda/TF change.

Note the escalation tests (TestShellCmd_StoppedSandbox/_UnknownSubstrate/
_MissingInstanceID) already assert `err != nil` and the right substrings — do NOT
weaken or edit them; the production fix alone must make them pass.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestShellCmd_' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>
TestShellCmd_StoppedSandbox, _UnknownSubstrate, _MissingInstanceID PASS;
TestShellCmd_EC2, _EC2_Root, _ECS, _MissingSSMDoc still PASS; output `ok` + EXIT=0.
`make build` succeeds (compiles). Only shell.go changed.
  </done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestShellCmd_' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`go test ./internal/app/cmd/ -run 'TestShellDocker' -count=1 -timeout 600s; echo "EXIT=$?"` → still `ok` (docker path unaffected; Plan 01 owns those assertions).
`make build` exits 0.
`git diff --stat` for THIS plan's commit shows ONLY internal/app/cmd/shell.go changed — this is the single approved production edit and MUST be committed separately from the test-only plans.
</verification>

<success_criteria>
The three shell pre-flight escalation tests pass via a production fix (not a test
rewrite); all previously-green shell tests stay green; the change is isolated to
shell.go in its own commit.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-07-SUMMARY.md`
</output>
