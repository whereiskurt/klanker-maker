---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/shell_docker_test.go
autonomous: true
requirements: [TEST-HYGIENE-SHELL]
must_haves:
  truths:
    - "TestShellDockerContainerName passes against the current `bash --login` docker exec command"
    - "TestShellDockerNoRootFlag passes against the current always-present `-u sandbox` flag"
    - "TestShellDockerRootFlag and TestShellDockerRouting remain green (unchanged)"
  artifacts:
    - path: "internal/app/cmd/shell_docker_test.go"
      provides: "Docker shell command assertions reconciled to current production"
      contains: "bash --login"
  key_links:
    - from: "internal/app/cmd/shell_docker_test.go"
      to: "internal/app/cmd/shell.go:execDockerShell"
      via: "asserted command string"
      pattern: "bash --login"
---

<objective>
Reconcile the 2 stale docker-shell assertions so they match `execDockerShell`'s
current output: `docker exec -it -u sandbox km-{id}-main bash --login`.

Purpose: Production drifted — `execDockerShell` (shell.go:837-852) now emits a
login shell (`bash --login`, not `/bin/bash`) and ALWAYS passes `-u sandbox`
(not just `-u root` when `--root` is set). The tests still assert the old
behavior. This is pure test hygiene; production is the source of truth.
Output: shell_docker_test.go assertions aligned to current behavior.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current production behavior (shell.go:837-852, execDockerShell) — DO NOT modify shell.go in this plan. -->
Observed command for a docker sandbox WITHOUT --root:
    docker exec -it -u sandbox km-sb-docker-1-main bash --login
Observed command WITH --root:
    docker exec -it -u root km-sb-docker-2-main bash --login

Verified failures (go test output):
  shell_docker_test.go:52  expected '/bin/bash' in docker exec args, got: ...bash --login
  shell_docker_test.go:124 expected NO '-u' in docker exec args without --root, got: ...-u sandbox...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile docker-shell login-shell + -u sandbox assertions</name>
  <files>internal/app/cmd/shell_docker_test.go</files>
  <action>
Two stale assertions to fix; production is authoritative (do NOT touch shell.go):

1. TestShellDockerContainerName (shell_docker_test.go ~line 50-53): the comment
   says "Must include /bin/bash" and asserts `strings.Contains(fullCmd, "/bin/bash")`.
   Production now emits `bash --login`. Change the assertion to require
   `strings.Contains(fullCmd, "bash --login")` (and update the comment to
   "Must use a login shell (bash --login)"). This proves the login-shell behavior
   that runs /etc/profile.d scripts.

2. TestShellDockerNoRootFlag (shell_docker_test.go ~line 120-125): it asserts
   `if strings.Contains(fullCmd, "-u")` is an error (i.e. expects NO `-u`).
   Production now ALWAYS passes `-u sandbox` when not root. Invert the intent:
   assert that the command DOES contain `-u sandbox` and does NOT contain
   `-u root` (e.g. require `strings.Contains(fullCmd, "-u sandbox")` and
   `!strings.Contains(fullCmd, "-u root")`). Update the comment to
   "Without --root, exec runs as the unprivileged sandbox user (-u sandbox, never -u root)".

Leave TestShellDockerRootFlag and TestShellDockerRouting untouched — they already
pass. Do not edit the shared `indexOf` helper or `fakeShellFetcher`.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestShellDocker' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>All four TestShellDocker* tests PASS; output shows `ok` and EXIT=0. No change to any non-test file.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestShellDocker' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`git diff --stat` shows ONLY internal/app/cmd/shell_docker_test.go changed.
</verification>

<success_criteria>
TestShellDockerContainerName and TestShellDockerNoRootFlag pass against current
production; the other two docker-shell tests remain green; no production file edited.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-01-SUMMARY.md`
</output>
