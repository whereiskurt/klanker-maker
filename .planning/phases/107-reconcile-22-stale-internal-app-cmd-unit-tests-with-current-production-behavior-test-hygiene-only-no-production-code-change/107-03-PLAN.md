---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/uninit_test.go
autonomous: true
requirements: [TEST-HYGIENE-UNINIT]
must_haves:
  truths:
    - "TestUninitDestroyOrder asserts the exact reverse of the current 22-module regionalModules() order"
    - "TestUninitContinuesPastModuleErrors expects 22 Destroy calls"
    - "TestUninitDetectsBackendDrift expects 22 Destroy calls"
  artifacts:
    - path: "internal/app/cmd/uninit_test.go"
      provides: "Uninit module-order/count assertions reconciled to regionalModules()==22"
      contains: "dynamodb-h1-threads"
  key_links:
    - from: "internal/app/cmd/uninit_test.go"
      to: "internal/app/cmd/init.go:regionalModules"
      via: "wantOrder slice == reverse(regionalModules)"
      pattern: "lambda-h1-bridge"
---

<objective>
Reconcile the 3 stale uninit tests to the current 22-module `regionalModules()`
inventory. Uninit destroys in the exact reverse of the apply order; the test's
`wantOrder` slice (19 entries) and two hardcoded `19` counts are stale after
Phases 98/100/103 added `dynamodb-slack-channels`, `dynamodb-github-threads`,
`dynamodb-h1-threads`, `lambda-h1-bridge`, etc.

Purpose: Same class of debt as TestRunInitPlan_ModuleOrder (already reconciled
17→22). Pure test hygiene; init.go is the source of truth.
Output: uninit_test.go module order + counts aligned to regionalModules()==22.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current regionalModules() APPLY order (init.go), verified, 22 entries: -->
1 network          2 efs            3 dynamodb-budget   4 dynamodb-identities
5 dynamodb-sandboxes  6 dynamodb-schedules  7 ssm-session-doc  8 s3-replication
9 create-handler   10 ttl-handler  11 email-handler  12 dynamodb-slack-nonces
13 dynamodb-slack-threads  14 dynamodb-slack-channels  15 dynamodb-slack-stream-messages
16 dynamodb-github-threads  17 sqs-inbound-dlq  18 lambda-slack-bridge
19 lambda-github-bridge  20 dynamodb-h1-threads  21 lambda-h1-bridge  22 ses

Uninit DESTROY order = the REVERSE of the above (verified from the actual
runner.calls in the failing test output):
  ses, lambda-h1-bridge, dynamodb-h1-threads, lambda-github-bridge,
  lambda-slack-bridge, sqs-inbound-dlq, dynamodb-github-threads,
  dynamodb-slack-stream-messages, dynamodb-slack-channels, dynamodb-slack-threads,
  dynamodb-slack-nonces, email-handler, ttl-handler, create-handler,
  s3-replication, ssm-session-doc, dynamodb-schedules, dynamodb-sandboxes,
  dynamodb-identities, dynamodb-budget, efs, network

Verified failures (go test output):
  uninit_test.go:103 expected 19 Destroy calls, got 22: [<the 22-entry reverse list above>]
  uninit_test.go:223 expected 19 Destroy calls (all modules attempted), got 22
  uninit_test.go:467 expected 19 Destroy calls (continue past drift), got 22
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update wantOrder to the 22-entry reverse + bump two count consts to 22</name>
  <files>internal/app/cmd/uninit_test.go</files>
  <action>
init.go is authoritative (do NOT touch it). Three stale spots:

1. TestUninitDestroyOrder `wantOrder` slice (uninit_test.go ~lines 79-100):
   replace the 19-entry slice with the EXACT 22-entry reverse-of-apply order
   listed in the interfaces block above, in that order. The test compares with
   `strings.HasSuffix(runner.calls[i], want)` per index, so the order must match
   the destroy order precisely. Specifically vs. the current slice you must:
     - add `lambda-h1-bridge` (2nd, right after `ses`)
     - add `dynamodb-h1-threads` (3rd)
     - reorder the two bridges so `lambda-github-bridge` precedes
       `lambda-slack-bridge`
     - add `dynamodb-slack-channels` (between `dynamodb-slack-stream-messages`
       and `dynamodb-slack-threads`)
   Update the explanatory comment to note "reverse of the 22-module
   regionalModules() apply order (init.go)".

2. TestUninitContinuesPastModuleErrors (uninit_test.go ~line 221):
   `const wantCalls = 19` → `const wantCalls = 22`.

3. TestUninitDetectsBackendDrift (uninit_test.go ~line 466):
   `if len(runner.calls) != 19` → `!= 22` (and the message text "expected 19"
   stays via the `%d` format — just change the literal).

Do not change any other uninit test. Add a short comment near each count linking
it to regionalModules()==22, mirroring the existing Module-order debt note, so the
next module addition is easy to find.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestUninit' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>TestUninitDestroyOrder, TestUninitContinuesPastModuleErrors, TestUninitDetectsBackendDrift all PASS; other TestUninit* stay green; output `ok` + EXIT=0.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestUninit' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`git diff --stat` shows ONLY internal/app/cmd/uninit_test.go changed.
</verification>

<success_criteria>
All uninit tests pass against the 22-module inventory; the wantOrder slice is the
exact reverse of regionalModules(); no production file edited.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-03-SUMMARY.md`
</output>
