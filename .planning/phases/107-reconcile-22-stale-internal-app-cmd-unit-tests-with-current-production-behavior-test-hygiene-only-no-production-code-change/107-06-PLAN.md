---
phase: 107-reconcile-22-stale-internal-app-cmd-unit-tests
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cmd/agent_auth_test.go
  - internal/app/cmd/at_test.go
  - internal/app/cmd/shell_learn_test.go
  - internal/app/cmd/init_test.go
autonomous: true
requirements: [TEST-HYGIENE-MISC]
must_haves:
  truths:
    - "TestRunAgentAuthClaude_TeesAndCleans stubs the new `claude auth status` loggedIn check"
    - "TestAtList_WithRecords uses a future at() time so it survives the reconcile-stale-schedule pass"
    - "TestLearnOutputPath asserts the current empty default for --learn-output"
    - "TestLoadEFSOutputs_NotExist asserts only err==nil (return value unconstrained)"
  artifacts:
    - path: "internal/app/cmd/agent_auth_test.go"
      provides: "Auth test mock routes claude auth status loggedIn=true"
    - path: "internal/app/cmd/at_test.go"
      provides: "At-list fixture uses a future schedule time"
    - path: "internal/app/cmd/shell_learn_test.go"
      provides: "learn-output default assertion reconciled"
    - path: "internal/app/cmd/init_test.go"
      provides: "EFS not-exist assertion relaxed to err==nil per locked decision"
  key_links:
    - from: "internal/app/cmd/agent_auth_test.go"
      to: "internal/app/cmd/agent_auth.go:verifyClaudeAuthStatus"
      via: "mock SSM route for 'claude auth status'"
      pattern: "loggedIn"
---

<objective>
Reconcile the 4 remaining stale misc tests, each a distinct production drift:
1. agent-auth: production added a post-exit `claude auth status` loggedIn check;
   the mock doesn't route it → loggedIn=false error.
2. at-list: production now reconciles (deletes) one-time `at()` schedules whose
   time has passed; the fixture's hardcoded 2026-04-04 time is in the past →
   "No scheduled operations."
3. learn: `--learn-output` default changed to "" (empty); test expects
   "observed-profile.yaml".
4. EFS: LoadEFSOutputs now falls back to S3 and returns a real `fs-...` id when
   the local file is missing — LOCKED decision: assert only `err == nil`.

Purpose: Pure test hygiene; production is the source of truth.
Output: four misc tests aligned to current behavior.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-CONTEXT.md

<interfaces>
<!-- Current production behavior (verified) — DO NOT modify any production file. -->
agent_auth.go verifyClaudeAuthStatus(): runs
  "sudo -u sandbox bash -lc 'claude auth status 2>&1' 2>&1"
  and treats success as the output containing `"loggedIn": true` (or `"loggedIn":true`).
  This runs BEFORE the tee-file cleanup. The mock must add a route for the
  "claude auth status" substring returning JSON with loggedIn true.

at.go (NewAtListCmdWithDeps): after listing, it DELETES one-time schedules whose
  CronExpr is `at(<ts>)` and <ts> is before time.Now().UTC() ("reconcile stale").
  The fixture's cron_expr = "at(2026-04-04T09:00:00)" is in the past → pruned.

shell.go:219: cmd.Flags().StringVar(&learnOutput, "learn-output", "", ...) → DefValue "".

init.go LoadEFSOutputs(): on missing efs/outputs.json it falls back to
  fetchAndCacheOutputs (S3) and can return a real "fs-..." id with err==nil.

Verified failures (go test output):
  agent_auth_test.go:821 runAgentAuthClaude returned error: ... reports loggedIn=false ...
  at_test.go:449/452 expected schedule name/command in output, got: "No scheduled operations.\n"
  shell_learn_test.go:39 expected default learn-output to be 'observed-profile.yaml', got ""
  init_test.go:469 expected empty filesystem_id when file missing, got "fs-0bd1911c39bff2d4d"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent-auth mock routes claude auth status; at-list fixture uses a future time</name>
  <files>internal/app/cmd/agent_auth_test.go, internal/app/cmd/at_test.go</files>
  <action>
Production is authoritative; do NOT edit agent_auth.go or at.go.

A. TestRunAgentAuthClaude_TeesAndCleans (agent_auth_test.go ~lines 797-823): the
   mock's `routedOutputs` (authSSMRoute slice ~lines 804-807) lacks a route for
   the new `claude auth status` verification. Add a route so verifyClaudeAuthStatus
   sees a logged-in result, e.g. append:
     {cmdSubstr: "claude auth status", output: `{"loggedIn": true}`}
   Use the exact JSON token the production checks (`"loggedIn": true`). Keep the
   existing routes (tmux list-sessions, credentials test -f). After this the
   function returns nil and the cleanup `rm -f /tmp/km-claude-auth-sb-tee-123.out`
   assertion (lines ~825-834) still holds. Do not change the cleanup assertion or
   the browserOpener stub.

B. TestAtList_WithRecords (at_test.go ~lines 422-454): the mock item's `cron_expr`
   is `at(2026-04-04T09:00:00)` (past) so the reconcile pass deletes it →
   "No scheduled operations." Change the fixture so the schedule SURVIVES
   reconciliation. Simplest robust fix: compute a FUTURE timestamp relative to
   `time.Now()` (e.g. now.Add(48*time.Hour)) and set both `cron_expr` to
   `fmt.Sprintf("at(%s)", future.Format("2006-01-02T15:04:05"))` and a matching
   human `time_expr`. (Keep it dynamic so the test never goes stale again — do NOT
   hardcode another fixed future date.) Leave the rest of the fixture
   (schedule_name "km-at-kill-sb-001", command "kill", status "active") intact so
   the table assertions at lines 448-453 pass.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestRunAgentAuthClaude_TeesAndCleans|TestAtList' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>TestRunAgentAuthClaude_TeesAndCleans and TestAtList_WithRecords PASS (and TestAtList_Empty stays green); output `ok` + EXIT=0.</done>
</task>

<task type="auto">
  <name>Task 2: learn-output default + EFS not-exist (locked err==nil) assertions</name>
  <files>internal/app/cmd/shell_learn_test.go, internal/app/cmd/init_test.go</files>
  <action>
Production is authoritative; do NOT edit shell.go or init.go.

A. TestLearnOutputPath (shell_learn_test.go ~lines 30-40): production registers
   `--learn-output` with an EMPTY default (shell.go:219). Change the expected
   default from "observed-profile.yaml" to "" (assert
   `flag.DefValue != ""` is the failure, i.e. expect DefValue == ""). Update the
   comment to "default is empty (operator must pass a path or rely on the
   timestamped default written at runtime)". Keep the flag-set capture portion
   (lines ~42-49) intact.

B. TestLoadEFSOutputs_NotExist (init_test.go ~lines 458-471) — LOCKED DECISION:
   relax to `err == nil` only. DELETE the empty-string assertion block
   (the `if fsID != "" { t.Errorf("expected empty filesystem_id...") }` at
   ~lines 468-470). Keep the `err != nil → t.Fatalf` check (lines 465-467) so the
   contract is exactly "no error; return value unconstrained (empty OR a valid
   fs-... id from the S3 fallback)". Update the test's doc comment to reflect the
   new S3-fallback contract (drop the "returns ("", nil)" wording). Do NOT add any
   environment-isolation logic. Leave TestLoadEFSOutputs_MalformedJSON untouched.
  </action>
  <verify>
    <automated>go test ./internal/app/cmd/ -run 'TestLearnOutputPath|TestLoadEFSOutputs' -count=1 -timeout 600s; echo "EXIT=$?"</automated>
  </verify>
  <done>TestLearnOutputPath and TestLoadEFSOutputs_NotExist PASS; TestLoadEFSOutputs_MalformedJSON stays green; output `ok` + EXIT=0.</done>
</task>

</tasks>

<verification>
`go test ./internal/app/cmd/ -run 'TestRunAgentAuthClaude|TestAtList|TestLoadEFSOutputs|TestLearnOutputPath' -count=1 -timeout 600s; echo "EXIT=$?"` → `ok` + EXIT=0.
`git diff --stat` shows ONLY the four *_test.go files changed.
</verification>

<success_criteria>
All four misc tests pass against current production; TEST-21 uses the err==nil-only
fix per the locked decision; no production file edited.
</success_criteria>

<output>
After completion, create `.planning/phases/107-reconcile-22-stale-internal-app-cmd-unit-tests-with-current-production-behavior-test-hygiene-only-no-production-code-change/107-06-SUMMARY.md`
</output>
