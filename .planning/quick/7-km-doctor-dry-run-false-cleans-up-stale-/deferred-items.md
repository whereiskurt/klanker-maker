# Quick-7 — Deferred Items

Items discovered during execution that are out-of-scope for Plan quick-7
(per the scope-boundary rule: only auto-fix issues directly caused by the
current task's changes).

## Pre-existing test failure (unrelated)

- **`TestUnlockCmd_RequiresStateBucket`** in `internal/app/cmd/unlock_test.go`
  fails on baseline (verified by `git stash` + retest). The error message
  expected by the test (`"state bucket"`) doesn't match the actual error
  (`"sandbox sb-aabbccdd is not locked"`). Either the production error
  message changed without updating the test, or the test sets up state
  in a way that hits the lock-check before the state-bucket check.
- **Not caused by Plan quick-7** — none of my edits touch `unlock.go` or
  its test.
- **Recommended:** open a small follow-up to either update the test
  assertion or restore the original error message.

## Pre-existing `go vet` warning (unrelated)

- `sidecars/http-proxy/httpproxy/transparent.go:204:26` — vet flags
  `address format "%s:%d" does not work with IPv6 (passed to net.Dial at L238)`.
- **Not caused by Plan quick-7** — sidecars package never touched here.
- **Recommended:** wrap host with `net.JoinHostPort` in a follow-up.

## Pre-existing `km-config.example.yaml` drift

- File showed up modified post-`make build`. The diff adds `resource_prefix`
  and `email_subdomain` example blocks that were already shipped in Phase 66
  but never landed in this example file.
- **Not caused by Plan quick-7** — likely auto-regenerated by some
  side-effect of running km commands in tests.
- **Not staged in any quick-7 commit** — left as a working-tree diff for
  the operator to handle in a separate doc-refresh PR.
