apiVersion: klankermaker.ai/v1alpha2
kind: SandboxProfile
metadata:
  name: base-budget-standard
  abstract: true

spec:
  budget:
    compute:
      maxSpendUSD: 0.50
    ai:
      maxSpendUSD: 2.00
    warningThreshold: 0.80
