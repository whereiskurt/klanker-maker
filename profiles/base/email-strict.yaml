apiVersion: klankermaker.ai/v1alpha2
kind: SandboxProfile
metadata:
  name: base-email-strict
  abstract: true

spec:
  email:
    signing: required
    verifyInbound: required
    encryption: required
    allowedSenders:
      - "*"
