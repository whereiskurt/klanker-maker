# Mark compiled BPF bytecode as binary to prevent git diff noise.
# These files are generated by `make generate-ebpf` and committed to the repo.
# bpf2go generates both little-endian (bpfeb) and big-endian (bpfeb) variants.
pkg/ebpf/*.o binary
pkg/ebpf/sni/*.o binary
