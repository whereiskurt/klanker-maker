// Command km is the klankrmkr CLI for managing sandbox profiles and environments.
// This is a placeholder entry point — CLI commands (validate, create, destroy, list)
// will be wired in Plan 04.
package main

import "fmt"

const version = "0.1.0-dev"

func main() {
	fmt.Printf("km version %s\n", version)
}
